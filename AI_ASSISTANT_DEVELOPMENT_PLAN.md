# WMS AI 助手详细开发计划

## 1. 文档目标

本计划面向当前 `SIX2090/wms` 仓库，目标不是重新做一个聊天机器人，而是把现有 AI 助手升级为一个**可审计、权限受控、以仓库业务闭环为中心的 WMS Copilot/Agent**。

计划遵守以下产品边界：

- AI 可以查询、解释、分析、导航、生成草稿和执行提交前检查。
- AI 默认不得自动提交、审核、完成、反审、作废、删除或直接扣减库存。
- 所有写操作必须由确定性后端服务执行，不能让大模型直接拼 SQL 或直接修改数据库。
- 图片、微信截图、送货单等非结构化内容必须先经过识别、物料匹配和人工确认，再生成业务草稿。
- 仓库、采购、生产、管理员等角色必须使用与现有页面一致的服务端权限规则。
- AI 失败时不得影响 WMS 主业务，必须可以回退到本地规则和手工页面。

## 2. 当前项目概况

### 2.1 技术与部署形态

- 后端：Python 3.11、Flask 2.3、Flask-SQLAlchemy。
- 数据库：默认 SQLite，已通过 WAL、超时和原子库存更新缓解单机并发问题。
- 前端：Jinja2、Bootstrap、jQuery 和页面内原生 JavaScript。
- 生产服务：Waitress；支持 Windows 离线安装和便携包。
- 仓库规模：约 207 个 Git 跟踪文件，`app/app.py` 约 3 万行、1.3 MB，包含模型、路由、业务服务和 AI 实现。
- 路由规模：约 431 条 Flask 路由，覆盖库存、采购、入库、出库、盘点、调拨、调整、生产领料、委外、报表、系统管理和移动扫码。
- GitHub 当前只有 `main` 分支，没有 Issues 和 Pull Requests，需要建立正式迭代流程。

### 2.2 现有业务闭环

1. 基础资料：物料分类、物料、单位、供应商、客户、仓库、库位、部门、员工、用户。
2. 采购：采购申请 → 审批 → 采购订单 → 到货/采购入库。
3. 仓库：期初库存、普通入库、采购入库、领料出库、销售/售后出库、调拨、盘点、库存调整。
4. 生产与委外：BOM、生产领料、委外订单、委外发料、委外收货、进度跟踪。
5. 管理：待处理中心、审批中心、操作审计、备份、系统参数、通知。
6. 终端：移动扫码接口、Excel 导入导出、打印和标签。

### 2.3 现有 AI 能力

当前代码不是空白项目，已经实现了较丰富的 AI 雏形：

- OpenAI 兼容的 `/chat/completions` 模型配置、连接测试和视觉模型测试。
- 大模型意图识别、本地规则兜底、普通聊天和 SSE 流式输出。
- 页面上下文感知，可根据当前页面识别采购订单或当前单据。
- 库存查询、物料查询、库存流水、今日收发、待处理、库存预警和多类分析。
- 入库、领料、调拨、盘点、调整、采购申请等草稿生成。
- 送货单图片、微信文本/截图、Excel 复制表格的单据识别。
- 物料编码、名称、规格和 AI 物料别名匹配；人工确认后学习别名。
- OCR 低置信度、未匹配物料和采购订单不唯一时进入人工确认页。
- 采购工作台、供应商画像、催交话术、异常工作台、Agent 巡检、主数据体检、系统体检、权限助手、日志审计和页面导航等本地技能。
- 当前约有 26 个本地 AI 技能，项目已进入“能力很多，但需要平台化治理”的阶段。

## 3. 当前优势与主要差距

### 3.1 可直接复用的优势

1. **确定性业务优先**：库存查询和建草稿最终调用本地数据库与业务函数，不依赖模型编造业务结果。
2. **已坚持草稿边界**：多数 AI 写操作只生成草稿，提示人工核对后提交。
3. **已有 OCR 人工确认**：低置信度和未匹配物料不会无条件落单。
4. **已有物料别名学习**：可持续改善供应商简称、旧料号、微信口语名称的匹配率。
5. **已有页面上下文**：为“在当前采购订单上收货”“检查当前草稿”提供基础。
6. **已有审计与回归意识**：仓库包含 Bug 基线、风险扫描和回归脚本。
7. **本地离线能力较强**：大模型不可用时，WMS 主系统和部分规则助手仍可运行。

### 3.2 P0：正式扩展前必须解决的问题

#### 3.2.1 AI 权限没有与业务路由完全对齐

`/api/ai/warehouse_assistant` 和 `/api/ai/chat/stream` 当前主要依赖 `login_required`。部分内部 AI 函数可直接创建不同模块草稿，权限判断没有统一放在工具执行层。

风险包括：

- 普通用户可能通过 AI 调用本来只允许仓库、采购或生产角色执行的能力。
- 权限账号助手、操作日志审计、系统体检可能向非管理员暴露用户和系统风险信息。
- 人工确认页面有角色判断，但直接文本建草稿和部分意图执行可能绕过确认页权限。

必须改为：**每个 AI 工具单独声明允许角色、数据范围、风险等级和确认策略，执行前后都校验权限。**

#### 3.2.2 AI 代码与主应用高度耦合

AI 逻辑集中在 `app/app.py`，与模型、路由和业务函数混在一起。继续增加技能会造成路由冲突、测试困难、权限和错误处理重复，以及 AI 与页面业务规则口径分裂。

#### 3.2.3 会话、任务与审计不可持久化

当前聊天历史保存在进程内字典中：服务重启后丢失，多进程不共享，也不能追踪一次请求调用了哪些工具、使用哪个模型、耗时和成本是多少。

#### 3.2.4 图片与业务数据外发缺少治理

送货单、微信截图、供应商、客户、物料和数量会发送到外部模型，需要补充：

- 明确的云端/本地模型标识和管理员确认。
- 图片发送前的隐私提示、供应商配置和保留说明。
- 手机号、地址、联系人等敏感字段可选脱敏。
- 模型供应商白名单、HTTPS 校验、超时、重试和熔断。
- 日志禁止记录 API Key、完整 Base64 图片和敏感原文。

#### 3.2.5 密钥与配置管理不足

API Key 页面会掩码，但实际值保存在系统设置表中。生产环境应优先使用环境变量或受限密钥文件，数据库存储应加密，并支持轮换、失效提示和审计。

#### 3.2.6 源码存在历史乱码和字段语义风险

模板、注释和少量业务字符串仍能找到乱码，AI 建采购入库草稿处也存在乱码业务类型字面量。扩展 AI 前必须进行 UTF-8 完整性扫描，禁止新增替换字符和错误中文状态；清理历史数据前必须预览、备份和人工确认。

#### 3.2.7 数据模型不足以支撑高级仓储 AI

当前核心出入库明细主要有物料、数量、价格和备注。OCR 虽可提取批号，但部分信息只能拼进备注；项目号、序列号、批次、库位尚未形成统一贯穿字段。

前置治理包括：

- 出入库、调拨、盘点、调整明细统一支持批次/批号。
- 序列号物料建立序列号台账，不能仅放备注。
- 单据表头和明细按业务需要支持项目号。
- 仓库与库位使用外键或稳定编码，减少自由文本。
- 状态逐步规范为草稿、已提交、已审核、已完成、已关闭、已作废等明确状态。

#### 3.2.8 缺少 AI 专项自动化测试

需要补充意图分类、OCR 黄金样本、物料匹配准确率、角色权限矩阵、工具幂等、重复请求、模型超时/非法 JSON 和提示注入测试。

## 4. 目标产品定位

AI 助手应分为五层能力，而不是把所有问题都交给大模型：

### 4.1 L1：自然语言导航与帮助

- 打开物料、采购订单、入库列表、盘点、报表、操作审计等页面。
- 解释状态、字段和标准操作流程。
- 根据用户角色只展示有权限的入口。

### 4.2 L2：真实数据查询与解释

- 查询物料库存、库位库存、流水、单据和待办。
- 解释库存不足、采购逾期或单据不能提交的原因。
- 输出必须来自数据库工具，并显示数据口径和查询时间。

### 4.3 L3：业务分析与任务建议

- 仓库异常工作台、采购工作台、主数据体检和操作审计。
- 给出“今天优先处理什么”和阻塞原因。
- 建议必须能打开对应记录，不只输出泛化文字。

### 4.4 L4：文档识别与草稿生成

- 送货单、微信通知、Excel 表格、标签和拍照单据识别。
- 结构化字段提取、物料匹配、采购订单匹配和低置信度确认。
- 只生成草稿，提交、审核和完成保持人工操作。

### 4.5 L5：受控业务 Agent

- 将复杂目标拆成多个只读检查和草稿动作。
- 每一步显示计划、工具、数据范围、结果和下一步。
- 高风险步骤必须暂停等待明确确认，默认不自动执行库存变更。

## 5. 目标架构

### 5.1 代码目录建议

在不一次性重写 Flask 项目的前提下，逐步拆出：

```text
app/
├── ai/
│   ├── __init__.py
│   ├── routes.py              # chat、stream、run、confirm、feedback API
│   ├── orchestrator.py        # 意图、计划、工具选择、回退
│   ├── policies.py            # 角色、风险级别、确认与数据范围
│   ├── schemas.py             # JSON Schema / 数据类
│   ├── prompts.py             # 版本化提示词
│   ├── providers.py           # OpenAI兼容、本地模型适配器
│   ├── history.py             # 会话与摘要
│   ├── audit.py               # AI运行和工具调用审计
│   ├── documents/
│   │   ├── extractor.py       # OCR/视觉结构化提取
│   │   ├── matcher.py         # 物料、供应商、采购订单匹配
│   │   └── confirmation.py    # 低置信度确认与草稿提交
│   └── tools/
│       ├── registry.py
│       ├── inventory.py
│       ├── purchase.py
│       ├── warehouse.py
│       ├── production.py
│       ├── master_data.py
│       ├── audit_tools.py
│       └── navigation.py
├── services/
│   ├── inventory_service.py
│   ├── purchase_service.py
│   ├── document_service.py
│   └── permission_service.py
└── models/
    └── ai_models.py
```

迁移原则：先提取 AI 新代码和公共服务，不在第一阶段大规模拆分所有旧路由。

### 5.2 工具注册协议

每个 AI 工具必须声明：

```text
name                  稳定工具名
description           面向模型的用途说明
input_schema          严格 JSON Schema
allowed_roles         admin/warehouse/purchase/production/user
risk_level            read/draft/high_risk
confirmation_required 是否必须人工确认
idempotent            是否幂等
audit_category        审计分类
handler               确定性业务函数
```

执行流程：

1. 接收用户消息和当前页面上下文。
2. 判断是否可由本地规则直接处理。
3. 必要时让模型输出结构化意图或工具计划。
4. 校验工具名和 JSON Schema。
5. 校验登录状态、角色、数据范围和业务状态。
6. 只读工具直接执行；草稿工具生成预览或草稿。
7. 高风险工具返回确认卡，不直接执行。
8. 保存 AI Run、Tool Call、结果摘要和操作审计。
9. 返回卡片、下一步和可追踪的 `run_id`。

### 5.3 风险等级

| 等级 | 典型动作 | 默认策略 |
|---|---|---|
| `read` | 查库存、查单据、报表、分析、导航 | 有权限即可执行 |
| `draft` | 创建入库、出库、请购、调拨、盘点、调整草稿 | 明确提示，保存草稿并审计 |
| `high_risk` | 提交、审核、完成、反审、作废、删除、恢复备份 | 默认不开放给 AI；未来开放也必须二次确认和短期授权 |

### 5.4 建议新增数据表

1. `ai_conversation`：用户、标题、状态、创建时间、最后活动时间。
2. `ai_message`：会话、角色、文本、附件摘要、模型、token 和时间。
3. `ai_run`：请求类型、状态、模型、提示版本、耗时、错误码、成本估算。
4. `ai_tool_call`：工具名、输入/输出摘要、风险等级、权限结果、关联业务对象。
5. `ai_confirmation`：确认类型、payload、创建者、过期时间、确认时间和幂等键。
6. `ai_feedback`：有帮助/无帮助、错误类型、用户备注、关联消息或运行。
7. `ai_document_job`：来源、文件摘要、识别状态、原始 OCR、置信度和关联草稿。
8. `ai_document_item`：原始行、结构化字段、匹配物料、匹配方式、置信度和人工修正。

原有 `ai_material_alias` 继续保留，并增加别名来源、组织范围、最后确认人和误匹配禁用状态。

## 6. 分阶段开发路线

### 阶段 0：基线冻结与安全整改（1–2 周）

**目标：** 在增加新能力前，确保现有 AI 不越权、不重复建单、不产生乱码业务数据。

任务：

1. 保护 `main`，所有功能从 `feature/*` 分支通过 Pull Request 合并。
2. 建立 `ai-assistant` Milestone、阶段 Issues 和 PR 模板。
3. 将现有 AI 技能逐一标注允许角色和风险等级。
4. 修复 AI API 只有登录校验的问题，系统体检、权限和日志能力仅允许管理员。
5. 仓库草稿、采购草稿、生产相关草稿分别限制对应角色。
6. 为草稿创建增加幂等键，SSE 断线重试不得重复执行写操作。
7. 修复源码乱码和错误业务类型字面量。
8. 建立 AI 安全日志规范，禁止记录 API Key、Base64 图片和完整敏感原文。
9. 收集 50–100 条真实仓库问法作为回归语料。

验收：

- 角色越权自动化测试全部通过。
- 非管理员无法通过 AI 获取用户、日志、备份和敏感系统信息。
- 重复发送相同建单请求只产生一个草稿。
- UTF-8 扫描无新增乱码、替换字符和错误中文状态。
- 关闭大模型后，WMS 主业务和本地规则查询正常。

### 阶段 1：AI 平台化重构（2–3 周）

**目标：** 把 AI 从 `app.py` 中逐步抽离，建立工具注册、权限、审计和模型适配的统一框架。

任务：

1. 创建 `app/ai/` 包和 Blueprint。
2. 实现 Tool Registry、Schema Validator 和 Policy Engine。
3. 先迁移导航、库存查询、单据查询、今日收发和待处理等低风险工具。
4. 再迁移入库、出库、调拨、盘点、调整和请购草稿工具。
5. 新增 `ai_run`、`ai_tool_call`、`ai_feedback` 表。
6. 封装 OpenAI 兼容和可选本地模型适配器，统一错误、超时、重试和熔断。
7. 提示词版本化，记录每次运行使用的 Prompt Version。
8. 保留原 API 作为兼容层，新实现走 `/api/ai/v2/*`。

验收：

- 新增工具不需要修改主路由的大段 `if/elif`。
- 所有工具调用都产生可查询的 `run_id` 和审计记录。
- 非法工具名、非法字段、超范围数量在执行业务函数前被拒绝。
- AI 新包核心工具单元测试覆盖率不低于 80%。
- 旧前端在兼容 API 下功能不回退。

### 阶段 2：文档智能与送货单入库强化（3–4 周）

**目标：** 把“能识别图片”升级为“可量化准确率、可复盘、可人工纠错”的处理流水线。

任务：

1. 建立文档任务中心：上传中、识别中、待确认、已生成草稿、失败。
2. 支持正式送货单、微信发货通知、Excel 表格和 PDF 多页文档。
3. 定义统一提取 Schema：单据类型、供应商、客户、单号、日期、采购订单号、仓库、物料、规格、单位、数量、箱数、批号、条码、原始行和置信度。
4. 匹配优先级：精确编码 → 已确认别名 → 名称+规格+单位 → 条码 → 模糊候选。
5. 模糊候选不得自动选中；超采购未到货数量必须阻断。
6. 当前采购订单上下文优先，多采购订单均可匹配时必须人工选择。
7. 确认页增加原图对照、匹配依据、置信度、批量修正和错误别名撤销。
8. 建立正式单据、模糊照片、手写、微信截图和错列表格的脱敏样本集。
9. 批次、条码等暂时不能结构化落库的字段必须明确提示“仅写入备注”。

验收指标：

- 清晰送货单表头关键字段准确率 ≥ 95%。
- 物料行召回率 ≥ 95%，数量准确率 ≥ 98%。
- 精确编码或已确认别名的自动匹配准确率 ≥ 99%。
- 未匹配、低置信度或超采购数量场景 100% 进入人工确认或阻断。
- 识别失败不会创建任何业务草稿。

### 阶段 3：仓库与采购受控 Agent（3–4 周）

**目标：** 让 AI 能规划多步任务，但每一步都受权限、状态、确认和审计控制。

首批 Agent：

1. **仓库主管每日巡检**：负库存、低库存、库位异常、呆滞、待处理单据和草稿阻塞，输出优先任务卡。
2. **采购到货跟进**：查询逾期/即将到货订单，按供应商归组并生成催交话术，不自动发送微信。
3. **低库存补货**：结合现存、重订货点、未到货和待审批请购，生成可解释的建议量和请购草稿。
4. **草稿检查**：检查必填、重复物料、异常数量/单价、库存、采购未到货量、仓库/库位/批次和状态。

执行要求：

- 先显示计划，再执行工具。
- 每一步显示成功、失败或跳过原因。
- 写草稿步骤必须有预览和幂等键。
- 关键只读步骤失败时，不得继续写操作。
- 用户可以取消运行。
- 完成后保留未完成项和下一步入口。

验收：

- 计划、工具调用和审计记录完全一致。
- Agent 不得调用当前角色无权使用的工具。
- Agent 不得自动完成、审核或删除单据。
- 中间失败时不留下半成品或重复草稿。

### 阶段 4：知识、分析与主数据增强（2–3 周）

任务：

1. 建立页面操作、状态机、异常 SOP、字段和报表口径知识库。
2. 知识检索只用于解释，真实库存和单据仍必须调用数据库工具。
3. 回答标注数据来源、查询时间和范围。
4. 增加主数据质量评分和修复清单，默认不自动修改。
5. 补充库存周转、呆滞、缺料、预计可用天数、供应商履约和盘点差异分析。

验收：

- 数据回答包含查询时间和范围。
- 知识回答能定位到对应页面或规则。
- 报表结果与系统现有报表抽样核对一致。
- 模型不得用知识库内容替代实时库存查询。

### 阶段 5：生产化、评估和灰度发布（2 周）

任务：

1. 建立调用次数、成功率、超时率、耗时、成本、工具使用、草稿转化率和反馈看板。
2. 增加全局、视觉、角色、用户和技能级 Feature Flags。
3. 建立本地规则回退、熔断、恢复探测和人工录入降级流程。
4. 第一周仅管理员/测试账号，第二周开放仓库只读和草稿能力，稳定后开放采购和生产。
5. 完成备份、迁移、回滚和便携包升级验证。

验收指标：

- AI API 可用率 ≥ 99.5%，模型故障不影响 WMS 主业务。
- 只读查询 P95 ≤ 3 秒；本地规则查询 P95 ≤ 1 秒。
- 文档识别 P95 ≤ 20 秒，超时有重试和手工入口。
- 未授权工具执行成功数、AI 直接库存变化数、重复草稿数均为 0。

## 7. 详细测试计划

### 7.1 单元测试

- 意图和参数解析、工具 Schema、角色策略、物料匹配、数量解析、幂等键、确认令牌。
- 模型非法 JSON、空响应、错误码、超时和提示注入。

### 7.2 集成测试

- 使用临时 SQLite 数据库创建完整业务数据。
- 采购申请 → 采购订单 → AI 送货单 → 入库草稿。
- 超采购数量、库存不足和中间事务失败不得留下部分数据。
- AI 草稿与手工页面创建结果字段一致。

### 7.3 权限矩阵测试

| 能力 | admin | warehouse | purchase | production | user |
|---|---:|---:|---:|---:|---:|
| 查公共库存 | 允许 | 允许 | 允许 | 允许 | 按现有策略 |
| 建入库/出库草稿 | 允许 | 允许 | 禁止 | 禁止 | 禁止 |
| 建采购草稿 | 允许 | 禁止 | 允许 | 禁止 | 禁止 |
| 生产领料/委外建议 | 允许 | 按现有策略 | 禁止 | 允许 | 禁止 |
| 权限/系统/日志体检 | 允许 | 禁止 | 禁止 | 禁止 | 禁止 |
| 提交/审核/完成/删除 | 人工页面 | 人工页面 | 人工页面 | 人工页面 | 禁止 |

最终矩阵以业务确认结果为准，并由自动化测试固化。

### 7.4 文档黄金样本

- 20 张清晰正式送货单。
- 10 张倾斜、阴影或反光照片。
- 10 张微信发货通知截图。
- 10 张手写或涂改单据。
- 10 组 Excel 粘贴表格。
- 物料不存在、同名不同规格、错误单位、超采购数量等负面样本。

### 7.5 浏览器端到端测试

- AI 面板、文本/图片粘贴、多图上传、SSE 中断重连、卡片跳转、多标签上下文。
- 确认页人工修正、生成草稿、清空历史、反馈和错误恢复。

## 8. 数据与安全治理

1. 默认最小化发送，仅发送完成任务所需字段。
2. 图片可选脱敏手机号、联系人和地址。
3. 禁止把 API Key、密码、Token、Cookie、数据库路径放入提示词。
4. 外部模型返回全部视为不可信输入，必须经过 Schema 和业务校验。
5. 文档里的“忽略规则、执行删除”等文字只作为单据内容，防止提示注入。
6. Markdown 安全渲染，链接限制为站内白名单或明确安全地址。
7. 确认令牌绑定用户、用途、过期时间和幂等键。
8. 数据库迁移前自动备份；AI 不得调用备份恢复。
9. Windows 离线部署验证密钥文件和数据库目录 ACL。

## 9. 业务数据模型升级建议

1. 单据表头：项目号、业务伙伴、仓库 ID、状态原因、来源单据类型和 ID。
2. 单据明细：批次/批号、序列号、库位、单位换算、原始数量和基本单位数量。
3. 采购：承诺/实际到货日期、关闭原因和未到货数量明确语义。
4. 库存：现存量、可用量、占用量和在途量分离。
5. 状态机：草稿、已提交、已审核、执行中、已完成、已关闭、已作废。
6. 异常：阻塞原因、处理人、处理时限和处理结果。

## 10. 前端交互改造

1. 角色化建议按钮。
2. Agent 计划和步骤视图。
3. 查询、执行、确认、失败工具状态。
4. 显示草稿类型、物料和数量的确认卡。
5. 显示实时查询、系统规则或大模型解释等数据来源。
6. 有帮助、识别错误、匹配错误、权限错误等反馈入口。
7. 文档任务中心和错误恢复入口。

## 11. GitHub 任务拆分建议

Milestones：

1. `AI-0 Security Baseline`
2. `AI-1 Platform Refactor`
3. `AI-2 Document Intelligence`
4. `AI-3 Warehouse Agents`
5. `AI-4 Analytics and Knowledge`
6. `AI-5 Production Rollout`

首批 Issues：

1. 建立 AI 技能权限矩阵。
2. 为 AI 草稿工具增加服务端角色校验。
3. 为 AI 建单增加幂等键。
4. 修复 AI 和模板中的乱码常量。
5. 抽取 `app/ai/routes.py` 和 Blueprint。
6. 实现 Tool Registry 与 JSON Schema 校验。
7. 新增 AI Run/Tool Call/Audit 数据表。
8. 建立 OCR 黄金样本和评测脚本。
9. 文档确认页增加匹配依据和置信度。
10. 建立模型供应商适配器和熔断回退。
11. AI 权限矩阵自动化测试。
12. Agent 巡检任务的计划和步骤 UI。

## 12. 人员与时间估算

### 方案 A：1 名全栈开发

- 总周期约 16–22 周。
- 风险是 AI、WMS 业务、测试和部署集中在一人。

### 方案 B：2 名开发 + 1 名兼职测试/业务人员（推荐）

- 后端/AI：工具框架、文档识别、数据模型和权限。
- 前端/全栈：浮窗、确认页、任务中心和管理看板。
- 测试/业务：真实样本、流程验收、权限矩阵和现场反馈。
- 总周期约 10–14 周。

### 方案 C：3 名开发 + 专职测试

- 并行推进平台、文档智能、业务 Agent 和自动化测试。
- 总周期约 8–12 周。

## 13. 项目成功指标

### 安全与正确性

- AI 越权成功、自动提交/审核/删除、直接库存变化和重复草稿：全部为 0。

### 文档质量

- 清晰送货单物料行召回率 ≥ 95%。
- 数量准确率 ≥ 98%。
- 物料自动匹配准确率 ≥ 99%。
- 低置信度和未匹配行人工确认覆盖率 100%。

### 业务价值

- 送货单到入库草稿平均处理时间降低 60% 以上。
- 常规库存/单据查询 AI 自助完成率 ≥ 70%。
- AI 草稿被采用并最终手工提交的比例 ≥ 60%。
- 仓库异常从发现到打开处理页面的时间降低 50%。

## 14. 推荐立即执行顺序

1. **权限整改**：统一 AI 工具角色校验。
2. **幂等与审计**：防止重试重复建单并支持复盘。
3. **乱码与数据口径清理**：避免 AI 基于错误状态判断。
4. **抽取 AI 模块**：建立 Tool Registry、Policy 和 Provider。
5. **建立 OCR 黄金样本**：用真实数据评测提示词和模型。
6. **强化送货单入库**：优先完成价值最高场景。
7. **上线仓库巡检和采购跟进 Agent**。
8. **最后扩展知识库和更多分析**，避免先做“会说但不会可靠执行”的助手。

## 15. 第一迭代建议交付物

建议第一个正式迭代控制在 2 周，交付：

1. AI 技能权限矩阵文档和自动化测试。
2. AI API 工具级角色校验。
3. 文本/图片建草稿幂等保护。
4. AI 运行和工具调用审计表的最小版本。
5. 乱码扫描与现有 AI 关键乱码修复。
6. 50 条文本意图用例和 20 张脱敏送货单黄金样本框架。
7. GitHub Milestone、Issues、PR 模板和 CI 基础检查。

完成该迭代后，再开始 `app/ai/` 平台化拆分，能够显著降低后续开发风险。
