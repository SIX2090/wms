# WMS 销售管理模块开发计划

> 文档状态：已实施（首轮闭环）
> 编制依据：现有采购管理、库存管理、单据、报表、客户资料和销售出库实现
> 目标：在复用现有库存出库能力的基础上，补齐销售订单与销售执行闭环

## 阶段 8 实施记录（2026-07-19）：对齐采购报表差异

- 新增 `/sales/execution_report` 销售订单执行报表及 Excel 导出：按客户、物料和日期筛选，展示订单数量、已发货数量、待发货数量、订单金额、已发货金额和金额执行率，并可下钻销售订单。
- 新增 `/sales/price_analysis` 销售价格分析及 Excel 导出：按物料汇总销售数量、客户数、平均/最低/最高含税成交价和销售金额。
- 销售报表口径与采购报表保持可比但不混淆业务：采购按供应商、到货执行和采购价分析；销售按客户、发货执行和销售成交价分析。销售收入取销售订单金额，库存扣减仍只认完成的销售出库单。
- 销售管理菜单新增“销售订单执行”和“销售价格分析”入口。

## 阶段 9 实施记录（2026-07-19）：销售单据追踪与分批下推

- 销售订单详情新增全部关联销售出库单清单，兼容 `source_sales_order_id` 外键和历史 `purpose` 来源文本，展示出库日期、仓库、状态、数量和金额。
- `/sales/<id>/create_outbound` 支持可选明细数量下推；未传明细时保持整单剩余数量下推，传入明细时服务端拦截无效明细和超过未发货数量的请求。
- 销售订单仍只能由确认状态下推，库存扣减仍由仓库完成销售出库单触发，销售模块不自动完成库存动作。
- 销售订单详情页新增“按选定数量下推”入口，默认整单下推行为保留。

## 阶段 10 实施记录（2026-07-19）：销售出库行级来源关联（SM-P1-01）

- 对齐采购入库 `source_purchase_order_item_id` 模式，为 `OutOrderItem` 增加 `source_sales_order_item_id` 外键和索引。
- 销售订单下推出库草稿时逐行写入来源销售订单明细；完成、反提交回写时优先按来源行处理，同一物料多行不再串行。
- 启动迁移仅对销售订单中“物料唯一对应一行”的历史出库明细自动回填；无来源或同物料多行记录保留为空并写入迁移告警，禁止静默猜测。
- 销售验证新增同物料多行下推/完成回写场景，验证每行数量和来源 ID 一一对应。

**验证**：`python -m py_compile app/app.py scripts/verify_sales_module.py` 通过；`python scripts/verify_sales_module.py` 22/22 通过；`python scripts/verify_wms_bugs.py` 通过。提交：`4159c57`。剩余子任务：销售出库选单工作台及仓库外键约束。

**验证**：`python -m py_compile app/app.py` 通过；新增路由、菜单入口和模板静态检查通过。当前目录缺少 `.git` 元数据，无法执行提交和推送。

## 0. 实施记录（2026-07-16）

基于本计划进行的增量增强已实施并通过验证：

**阶段 3 修复（菜单与工作台）**
- 修复 `base.html` 销售管理菜单可见性 Bug（多余反引号字符 + `bi-speedometer2` 拼写错误）。

**阶段 1 扩展（销售订单字段）**
- `SalesOrder` 模型新增：`salesperson_id`、`project_no`、`currency`（默认 CNY）、`settlement_method`、`untaxed_amount`、`tax_amount`、`shipped_amount`、`remaining_amount`。
- `SalesOrderItem` 模型新增：`tax_rate`（默认 0.13）、`untaxed_price`、`untaxed_amount`、`tax_amount`、`tax_included_amount`、`batch_no`、`serial_no`。
- `auto_migrate_database()` 增加 `ALTER TABLE ADD COLUMN` 迁移及历史数据回填 SQL（含税/未税/税额自动计算）。
- `recalculate_sales_order()` 重写为税感知总额计算。
- 录入页、详情页、列表页、打印页同步展示税额明细与新增字段。

**阶段 4 补齐（销售报表）**
- `/sales/report` 增加客户/物料/业务员钻取，三张汇总表支持点击钻取，钻取明细联动销售订单详情。
- 新增 `/sales/outflow_report`（销售出库明细表）+ Excel 导出，基于 `OutOrder(business_type='销售出库')`。
- 新增 `/sales/trend_report`（销售趋势分析表）+ Excel 导出，按月聚合订单/未税/税额/含税/已发货金额及环比增长。

**阶段 5 扩展（导入模板字段）**
- `/sales/download_template` 与 `/sales/import` 支持：业务员、项目号、币别、结算方式、税率、批次号、序列号。

**验证结果**
- `python scripts/verify_wms_bugs.py` 全部通过（含回归检查）。
- Flask test_client 对 10 个销售路由（含 HTML 与 Excel 导出）均返回 HTTP 200，无 `TemplateNotFound`。
- 修改文件清单：
  - `app/app.py`（模型、迁移、路由、导入逻辑）
  - `app/templates/base.html`（菜单 Bug 修复）
  - `app/templates/sales_order_add.html`（税额字段录入）
  - `app/templates/sales_order_detail.html`（税额明细展示）
  - `app/templates/sales_order.html`（业务员/项目号列与筛选）
  - `app/templates/sales_report.html`（钻取与汇总）
  - `app/templates/sales_outflow_report.html`（新增）
  - `app/templates/sales_trend_report.html`（新增）

后续阶段 6（单元测试与浏览器关键流程验证）建议作为后续任务单独推进。

---

## 阶段 6 实施记录（2026-07-16）

**新增验证脚本**
- `scripts/verify_sales_module.py`：覆盖 6 项静态检查 + 13 项运行时测试，共 19 项全部通过。

**静态检查（SALES-STC-001 ~ 006）**
- 数据库迁移含新字段 ALTER TABLE 语句
- `recalculate_sales_order` 税感知计算逻辑
- `build_sales_outbound_draft` 重复草稿防护
- 销售路由 `require_role` 权限装饰器
- 新增报表模板存在
- 导入模板含新字段列头

**运行时测试（SALES-RT-001 ~ 013）**
- 创建销售订单（含税额/批次/序列号字段）
- 税额计算正确性（含税 1000 → 未税 884.96 + 税额 115.04）
- 确认销售订单
- 生成销售出库草稿
- 重复生成出库草稿返回已存在（防护）
- 完成出库单后回写已发货数量（一单一出，shipped_qty=10/5，shipped_amount=2000.00）
- 订单状态变为 closed / shipped
- 取消未发货订单
- 删除草稿订单
- 报表页面渲染（10 个路由全部 HTTP 200）
- Excel 导出（4 个导出路由全部成功）
- 权限边界（viewer 角色被拒绝创建订单）
- 中文页面无乱码

**附带修复**
- 修复 `_check_out_order_anomalies` 中 `order.customer_id` 引用不存在属性的预存 Bug（OutOrder 使用 `customer` 字符串字段），改为 `getattr(order, 'customer', None)`，确保销售出库完成流程不再崩溃。

**验证结果**
- `python scripts/verify_sales_module.py`：19/19 通过
- `python scripts/verify_wms_bugs.py`：全部通过（含回归检查）

---

## 阶段 7 实施记录（2026-07-16）：四项不完善修复

针对模块评审发现的 4 项最影响数据正确性和日常可用性的不完善，本轮已全部修复：

### #4 出库外键关联（替代 purpose 字符串解析）
- `OutOrder` 模型新增 `source_sales_order_id` 外键字段指向 `sales_order.id`。
- `auto_migrate_database()` 新增 `ALTER TABLE out_order ADD COLUMN source_sales_order_id INTEGER` 迁移 + 从 `purpose` 字符串回填历史数据。
- `sync_sales_order_shipment()` 优先使用外键关联，兜底 `purpose` 字符串解析。
- `build_sales_outbound_draft()` 创建出库单时同时设置外键与 purpose 字符串（双写兼容），查询待完成草稿时优先外键匹配。

### #1 销售订单修改（仅草稿可改）
- 新增 `GET/POST /sales/<id>/edit` 路由，`sales_order_edit_page` 渲染编辑页并回填已有明细。
- 新增 `sales_order_edit.html` 模板，基于 `sales_order_add.html`，支持回填已有明细项、税额自动计算。
- 详情页 `sales_order_detail.html` 新增"修改订单"按钮（仅草稿状态可见）。
- 编辑逻辑：删除旧明细重建新明细（草稿状态无出库记录，安全），更新订单头字段，重算税额，记录审计日志。
- 权限：`require_role('warehouse', 'purchase')`，已确认订单不可改。

### #6 金额字段精度提升（Float → Numeric(18,2)）
- `SalesOrder` 模型：`total_amount`、`untaxed_amount`、`tax_amount`、`shipped_amount`、`remaining_amount` 改为 `db.Numeric(18, 2)`。
- `SalesOrderItem` 模型：`amount`、`untaxed_amount`、`tax_amount`、`tax_included_amount` 改为 `db.Numeric(18, 2)`。
- 消除浮点累加误差，大订单量下报表汇总会精确到分。

### #7 报表筛选维度扩展
- `/sales/report` 新增筛选维度：订单状态（draft/confirmed/closed/cancelled）、发货状态（pending/partial/shipped）、项目号（模糊匹配）、仓库。
- `/sales/outflow_report` 新增筛选维度：仓库、客户名称。
- 销售出库明细表（HTML + Excel 导出）增加未税金额、税额、税率三列，从关联销售订单明细获取税率。
- 报表与出库明细页样式统一为仓库管理模块风格（`page-header` + `card mb-3` 筛选区 + `table-responsive-wrapper` 表格区 + `btn-sm` + `bi-*` 图标 + `bg-warning/bg-success/bg-secondary` 状态徽章三色规范）。

### 修改文件清单
- `app/app.py`（OutOrder 模型、迁移、sync/build 函数、sales_report/outflow_report 路由、edit 路由、SalesOrder/SalesOrderItem Numeric 字段）
- `app/templates/sales_order_detail.html`（新增"修改订单"按钮）
- `app/templates/sales_order_edit.html`（新增编辑页模板）
- `app/templates/sales_report.html`（筛选表单扩展为仓库风格）
- `app/templates/sales_outflow_report.html`（筛选扩展 + 未税/税额列 + 仓库风格统一）
- `scripts/verify_sales_module.py`（适配 Numeric Decimal 比较）
- `SALES_MANAGEMENT_DEVELOPMENT_PLAN.md`（阶段 7 实施记录）

### 验证结果
- `python scripts/verify_sales_module.py`：19/19 通过
- `python scripts/verify_wms_bugs.py`：全部通过（含回归检查）

---

## 1. 现状结论

当前 WMS 已具备部分销售能力，但销售功能主要挂在库存出库模块中，并非完整的独立销售管理模块。

### 1.1 已有能力

- 客户基础资料：客户编码、名称、联系人、电话、地址。
- 直接销售出库：复用通用 `OutOrder` / `OutOrderItem`。
- 销售出库状态：待处理、已完成，支持完成、反提交、删除和批量操作。
- 销售出库库存联动：完成出库时扣减库存并写入库存流水。
- 销售出库打印与导出。
- 售后出库：独立的 `AfterSaleOutOrder` / `AfterSaleOutOrderItem`。
- AI 销售出库草稿：支持文本、表格或文档识别后生成销售出库草稿，仍需人工确认和完成。
- 通用库存报表：可查询销售出库明细，但尚无独立销售经营分析报表。

### 1.2 主要缺口

- 独立销售订单和销售订单明细。
- 销售订单确认、关闭、取消流程。
- 销售订单与销售出库单的来源关联。
- 已发货数量、未发货数量和部分发货状态。
- 销售工作台和销售待办。
- 客户销售汇总、物料销售汇总和订单执行报表。
- 销售价格、税率、交期和业务员等销售订单字段。

## 2. 建设目标

建立以下业务闭环：

```text
客户资料
  -> 销售订单
  -> 销售订单确认
  -> 生成销售出库草稿
  -> 仓库人工备货并完成出库
  -> 库存扣减
  -> 回写发货进度
  -> 销售执行与销售报表
```

本项目不重新建设库存扣减逻辑，销售出库必须复用现有库存单据完成逻辑。

## 3. 菜单规划

新增“销售管理”一级菜单，建议包含：

```text
销售管理
├── 销售工作台
├── 销售订单
│   ├── 新建销售订单
│   ├── 销售订单列表
│   └── 销售订单执行
├── 销售出库
│   ├── 待出库销售订单
│   ├── 销售出库草稿
│   └── 销售出库明细
├── 客户管理
└── 销售报表
    ├── 销售订单执行统计
    ├── 客户销售汇总
    ├── 物料销售汇总
    ├── 销售出库明细
    └── 销售趋势分析
```

## 4. 分阶段开发计划

### 阶段 0：现状与数据审计

目标：在编码前确认不重复建设现有能力。

工作项：

- 复核 `Customer`、`OutOrder`、`OutOrderItem`、`AfterSaleOutOrder` 数据结构。
- 梳理销售出库完成、反提交、库存流水和操作审计链路。
- 确认销售订单与现有出库单的来源关联方案。
- 确认数据库迁移方式，禁止依赖 `create_all()` 修改已有表结构。
- 确认客户、仓库、物料、价格和税率字段的复用策略。
- 输出字段映射、权限矩阵和报表口径。

交付物：现状审计清单、数据迁移方案、权限矩阵。

### 阶段 1：销售订单基础能力

目标：实现销售订单从录入到保存的基础闭环。

工作项：

- 新增销售订单主表。
- 新增销售订单明细表。
- 新建销售订单页面。
- 销售订单列表页面。
- 销售订单详情页面。
- 客户、仓库、物料搜索和自动带出。
- 订单金额自动计算。
- 草稿保存、修改和删除。
- 日期、数量、物料和客户基础校验。
- 操作审计日志。

### 阶段 2：销售订单流程与出库联动

目标：把销售订单与现有销售出库能力连接起来。

工作项：

- 销售订单确认。
- 销售订单状态控制。
- 根据未发货数量生成销售出库草稿。
- 销售订单与 `OutOrder` 建立来源关系。
- 防止重复生成相同未发货数量。
- 支持一单多次部分出库。
- 出库完成后回写已发货数量。
- 自动更新待发货、部分发货、已发货和已完成状态。
- 保留仓库人工完成出库边界。

状态建议：

```text
草稿 -> 已确认 -> 待出库 -> 部分发货 -> 已发货 -> 已完成
草稿/已确认 -> 已取消
```

### 阶段 3：销售工作台与菜单

目标：对齐首页和库存管理的操作体验。

工作项：

- 新增销售管理菜单。
- 新增销售工作台。
- 增加新建销售订单快捷入口。
- 增加待确认、待发货、部分发货、逾期订单待办。
- 增加库存不足预警入口。
- 增加订单数、待发货金额、已发货金额等指标卡片。
- 增加从待办直接打开单据的入口。

金额指标必须明确标注：

- 含税销售金额
- 未税销售金额
- 待发货金额
- 已发货金额

### 阶段 4：销售报表

目标：复用现有报表中心的筛选、查询、导出和打印样式。

工作项：

- 销售订单执行统计表。
- 客户销售汇总表。
- 物料销售汇总表。
- 销售出库明细表。
- 销售趋势分析表。
- 日期、客户、物料、仓库、业务员、订单状态和发货状态筛选。
- 报表导出和打印。
- 明细钻取到销售订单或销售出库单。

报表页面只允许查询、刷新、导出、打印和钻取，不混入新建、确认、完成等单据动作。

### 阶段 5：导入、打印与批量操作

目标：提高高频销售录入和批量处理效率。

工作项：

- 销售订单 Excel 模板下载。
- 销售订单批量导入。
- 销售订单导出。
- 批量确认。
- 批量生成销售出库草稿。
- 销售订单打印模板。
- 销售订单与销售出库打印关联。
- Excel 复制粘贴明细。
- 表格列宽、列顺序和显示隐藏设置。

### 阶段 6：测试与上线验收

目标：确认销售、库存、权限和报表数据一致。

工作项：

- 单元测试。
- 路由和权限测试。
- 销售订单保存、确认、取消测试。
- 一单一出、部分出库、一单多出测试。
- 库存扣减和反提交测试。
- 重复生成出库草稿测试。
- 客户和物料删除保护测试。
- 报表汇总与单据明细对账测试。
- 中文页面、乱码和历史数据检查。
- 浏览器关键流程验证。
- 数据库迁移和回滚验证。

## 5. 单据页面设计要求

### 5.1 销售订单列表工具栏

参考采购订单和库存单据列表，工具栏包含：

- 新建
- 批量确认
- 批量生成出库草稿
- 导入
- 导出
- 打印
- 刷新
- 查询
- 列设置

### 5.2 销售订单查询条件

- 销售订单号
- 客户编码
- 客户名称
- 物料编码
- 物料名称
- 订单日期
- 交货日期
- 发货仓库
- 订单状态
- 发货状态
- 项目号
- 业务员

### 5.3 销售订单表头字段

- 销售订单号
- 订单日期
- 客户
- 联系人
- 联系电话
- 交货日期
- 发货仓库
- 业务员
- 项目号
- 币别
- 结算方式
- 备注

### 5.4 销售订单明细字段

- 物料编码
- 物料名称
- 规格型号
- 单位
- 订单数量
- 已发货数量
- 未发货数量
- 含税单价
- 未税单价
- 税率
- 未税金额
- 税额
- 含税金额
- 批次
- 序列号
- 备注

选定物料后自动带出名称、规格、单位、默认销售价格、默认税率和库存可用量。

## 6. 数据模型规划

建议新增：

```text
sales_order
├── id
├── order_no
├── date
├── customer_id
├── warehouse_id / warehouse
├── delivery_date
├── salesperson_id
├── project_no
├── status
├── shipment_status
├── total_amount
├── shipped_amount
├── remaining_amount
├── remark
├── operator_id
└── created_at

sales_order_item
├── id
├── sales_order_id
├── material_id
├── quantity
├── shipped_quantity
├── price
├── tax_rate
├── untaxed_amount
├── tax_amount
├── tax_included_amount
├── batch_no
├── serial_no
└── remark
```

与现有库存单据建立来源关系：

```text
sales_order.id -> out_order.source_sales_order_id
```

如果现有数据库需要增加字段，必须使用明确的迁移脚本，并提供回滚方案。

## 7. 权限与人工确认边界

### 7.1 权限建议

| 功能 | 管理员 | 仓库 | 业务/采购 | 查看角色 |
|---|---:|---:|---:|---:|
| 查看销售工作台 | √ | √ | √ | √ |
| 新建销售订单 | √ | √ | √ | - |
| 确认销售订单 | √ | √ | √ | - |
| 生成销售出库草稿 | √ | √ | √ | - |
| 完成销售出库 | √ | √ | - | - |
| 查看销售报表 | √ | √ | √ | √ |
| 客户资料维护 | √ | 按现有权限 | 按现有权限 | - |

### 7.2 人工确认要求

以下动作必须保持人工操作：

- 确认销售订单
- 生成正式销售出库
- 完成销售出库
- 修改已确认订单
- 作废订单
- 删除订单
- 调整库存

AI 后续只能生成或检查销售订单/销售出库草稿，不能自动执行上述高风险动作。

## 8. 首批开发范围

第一轮建议只做稳定闭环：

1. 销售订单主表和明细表。
2. 销售订单列表、录入和详情。
3. 销售订单草稿保存。
4. 销售订单确认。
5. 生成现有销售出库草稿。
6. 销售订单与出库单来源关联。
7. 发货数量和订单状态回写。
8. 销售订单执行统计表。
9. 销售管理菜单和工作台基础入口。
10. 权限、审计和关键流程测试。

首轮暂不开发：

- 应收账款。
- 收款核销。
- 销售发票。
- 复杂价格体系。
- 信用额度自动控制。
- 销售退货。
- AI 自动确认或自动出库。

## 9. 验收标准

### 9.1 单据验收

- 可以创建销售订单草稿。
- 客户和物料明细校验有效。
- 可以确认销售订单。
- 可以生成销售出库草稿。
- 不会重复生成相同未发货数量。
- 支持部分出库。
- 出库完成后库存正确扣减。
- 销售订单已发货数量正确回写。

### 9.2 权限验收

- 查看角色只能查看单据和报表。
- 无权限角色不能创建、确认或完成单据。
- 销售订单确认权限与库存出库完成权限分离。
- 删除、作废和反提交受权限控制。

### 9.3 报表验收

- 客户汇总金额与订单明细一致。
- 物料汇总数量与销售订单一致。
- 出库明细与库存出库单一致。
- 部分出库金额计算正确。
- 筛选、导出和打印口径一致。

### 9.4 页面验收

- 菜单样式与采购、库存管理一致。
- 工具栏样式与现有单据页一致。
- 表格支持筛选、排序和列设置。
- 页面不出现乱码、问号或错误状态。
- 销售录入页支持键盘快速录入。
- 关键流程可以通过浏览器完整操作。

## 10. 开发原则

- 不重复开发现有销售出库和库存扣减能力。
- 优先复用 `OutOrder`、库存流水、打印、导出和报表基础组件。
- 销售订单与销售出库必须建立可追溯来源关系。
- 订单金额、库存数量和发货状态必须可对账。
- 单据、报表、主数据和系统管理页面保持职责分离。
- 所有高风险操作保留人工确认。
- 每完成一个阶段都必须执行代码验证、功能验证和数据一致性验证。

---

# 销售管理模块开发计划 V2（全 WMS 对齐版）

> 编制日期：2026-07-19
> 适用范围：PC 端销售管理、仓库销售出库、客户主数据、销售报表、售后出库和受控 AI 草稿能力。
> 计划原则：以当前 WMS 实际代码和数据流为准；已完成能力不重复开发，新增能力必须先补测试和权限边界。

## A. 全系统基线

### A.1 已存在并可复用的 WMS 能力

| 领域 | 现有能力 | 销售模块复用方式 |
|---|---|---|
| 主数据 | 物料、分类、单位、仓库、库位、客户、供应商、员工、部门 | 销售订单只引用客户、物料、仓库、业务员，不复制主数据 |
| 库存 | `Material.stock`、库位库存、原子扣减、库存流水、负库存策略 | 销售出库完成时统一走现有库存扣减和流水逻辑 |
| 出库单 | `OutOrder`/`OutOrderItem`，草稿、完成、反提交、删除、打印、导出 | 销售出库使用 `business_type='销售出库'`，不再另建库存扣减模型 |
| 采购链路 | 请购 -> 采购订单 -> 采购入库，支持按未执行数量下推和来源追踪 | 对齐采购的单据、选单、执行数量和报表设计，但业务方向相反 |
| 报表 | 通用报表中心、Excel、打印、筛选、钻取 | 销售经营报表保留销售口径，库存报表保留出库事实口径 |
| 审计 | 操作审计、角色装饰器、AI 审计 | 所有销售状态变化、出库下推和批量操作记录审计 |
| AI | 销售草稿验证、销售出库草稿、文档识别和统一权限矩阵 | AI 只创建/检查草稿，不确认订单、不完成出库、不作废删除 |

### A.2 当前销售模块真实状态

已完成：

- `SalesOrder`/`SalesOrderItem` 主从单据及草稿、确认、取消、删除、复制。
- 客户、业务员、项目号、币别、结算方式、交货日期、税率、批次、序列号、含税/未税金额。
- 销售订单生成销售出库草稿，支持重复草稿防护、整单下推和按明细数量分批下推。
- `OutOrder.source_sales_order_id` 外键及历史 `purpose` 兼容回溯。
- 完成/反提交销售出库时回写已发货数量、发货金额和销售订单状态。
- 销售工作台、订单列表、订单详情、打印、导入、导出、销售订单执行、客户/物料/业务员汇总、出库明细、趋势和价格分析。
- 销售出库从普通领料列表中隔离，售后出库保持独立单据。
- 销售模块验证脚本 `scripts/verify_sales_module.py` 已覆盖订单、税额、出库、回写、报表和权限主流程。

真实缺口：

- `OutOrderItem` 尚未直接关联 `SalesOrderItem`，同一销售订单含相同物料的多行时只能按物料回写，无法做到行级对账。
- 销售订单的仓库仍是字符串，尚未建立仓库外键；仓库改名、删除和跨仓发货校验不够强。
- 销售角色尚未独立存在，当前业务员/销售操作权限暂借 `purchase` 或 `warehouse` 角色，需明确是否新增 `sales` 角色。
- 销售订单确认前缺少可配置的库存可用量提示、价格底线和客户信用提示；这些提示不能直接阻止首期下单。
- 销售出库草稿尚未提供完整的“销售订单选单工作台”，当前主要从订单详情或批量按钮下推。
- 售后出库尚未完整回链原销售订单、原出库单、退回原因和责任归属。
- 销售报表已有页面，但订单金额、出库事实、库存流水和售后冲减的统一经营口径仍需建立对账视图。
- 真实浏览器多角色验收、历史数据迁移演练和生产数据对账仍需持续执行。

## B. 业务边界和目标流程

### B.1 首期明确不做

- 报价单、合同、收款、应收账款、发票、核销和会计凭证。
- 复杂价格政策、客户等级价、阶梯价、促销价和自动最低价拦截。
- 运输、物流跟踪、签收回单和电子签章。
- 自动信用审批、自动确认订单、自动完成出库。
- 把售后出库直接当作销售退货入库；退货入库另立任务，不能用负数出库替代。

### B.2 目标主流程

```text
客户/物料/仓库/业务员
        -> 销售订单草稿
        -> 人工确认订单
        -> 销售出库选单或订单下推草稿
        -> 仓库备货、校验库存、人工完成出库
        -> 库存扣减 + 库存流水
        -> 回写销售订单发货进度
        -> 销售执行/客户/物料/趋势/出库报表
        -> 售后时关联原销售来源并进入独立售后流程
```

### B.3 销售订单状态

| 状态 | 允许动作 | 进入条件 | 退出条件 |
|---|---|---|---|
| `draft` 草稿 | 编辑、复制、确认、删除 | 新建或导入 | 人工确认、删除 |
| `confirmed` 已确认 | 下推出库、取消、查看 | 明细和客户校验通过 | 部分发货、全部发货、人工取消 |
| `closed` 已完成 | 查看、打印、报表 | 所有明细已完成出库 | 不允许直接恢复 |
| `cancelled` 已取消 | 查看、报表排除 | 草稿或未完成订单人工取消 | 不允许继续下推 |

发货状态独立于订单状态：`pending`、`partial`、`shipped`。库存事实只认 `OutOrder.status='completed'`，草稿不得提前改变库存或销售订单已发货量。

## C. 数据模型演进

### C.1 P0 必须完成的来源精确化

新增 `out_order_item.source_sales_order_item_id`，外键指向 `sales_order_item.id`，并建立索引。下推时每个销售出库明细写入来源销售订单行；历史数据按销售订单外键、物料和唯一可匹配行回填，无法唯一匹配的记录进入迁移报告，不静默猜测。

来源关系目标：

```text
sales_order 1 ---- n sales_order_item
     |                    |
     |                    +---- n out_order_item
     +--------- n out_order (header compatibility)
```

### C.2 P1 仓库和发货事实

- 增加 `sales_order.warehouse_id`，保留 `warehouse` 字符串作为历史兼容字段。
- 新订单必须引用有效启用仓库；仓库删除前检查销售订单和销售出库引用。
- 销售出库完成时记录实际仓库、完成时间、操作人和来源销售订单行。
- 严禁通过重新计算订单金额来推断实际发货；实际发货必须来自已完成出库明细。

### C.3 P2 售后来源

为 `AfterSaleOutOrder` 增加原销售订单、原销售出库单及原因字段；售后出库仍保持独立库存单据，后续退货入库另行设计。售后金额是否冲减销售报表必须由业务口径开关控制，并在报表上明确显示。

### C.4 迁移要求

- 所有既有表结构变化必须进入 `auto_migrate_database()` 的幂等迁移分支或独立迁移脚本。
- 迁移前生成表结构和来源匹配审计报告，迁移后执行行数、数量和金额对账。
- 不用 `create_all()` 代替已有表结构迁移。
- 任何无法唯一回填的来源关系保留为空并生成待人工处理清单，不能错误绑定客户或销售行。

## D. 分阶段交付计划

### SM-P0：现状审计与口径冻结

交付：销售数据字典、角色矩阵、状态机、订单/出库/库存对账口径、历史来源匹配报告。

验收：随机抽取历史销售出库单，能从出库单追到销售订单；无法追溯的记录有数量和原因清单。

### SM-P1：销售订单基础和主数据约束

任务：仓库外键、客户启用状态校验、物料可销售状态、数量/金额精度统一、交货日期和订单日期校验、业务员字段权限、草稿编辑边界。

验收：客户/物料/仓库无效时服务端拒绝；金额精确到分；导入、页面和 API 使用同一校验函数。

### SM-P2：行级下推和仓库执行

任务：新增销售订单行来源外键；建设销售出库选单工作台；支持多仓选择策略；限制数量不超过未发货量；处理并发下推和重复草稿；完成/反提交按行回写。

验收：相同物料两行、多次部分出库、跨仓禁止、超发拒绝、重复请求幂等、反提交后数量恢复全部通过。

### SM-P3：销售工作台和作业入口

任务：待确认、待出库、部分发货、逾期、库存不足、异常来源、异常价格待办；销售订单和出库单双向跳转；销售出库草稿列表独立于普通领料列表；批量操作逐单提交并隔离失败。

验收：业务员能找到待办，仓库能按仓库处理出库，普通用户只能查看；所有待办卡片都能下钻到真实单据。

### SM-P4：经营报表和对账

任务：统一报表服务层，提供订单执行、客户销售、物料销售、销售出库事实、趋势、成交价格和销售对账；所有报表支持日期、客户、物料、仓库、业务员、订单状态、发货状态筛选；导出和页面共用同一查询口径。

口径：

- 订单金额：销售订单含税金额，取消订单默认排除。
- 已发货金额：销售订单行含税单价 × 已完成出库数量。
- 销售出库事实：只统计 `OutOrder.status='completed'`。
- 待发货金额：订单金额减已发货金额，不允许小于 0。
- 售后冲减：首期单独列示，不直接混入销售收入。

验收：订单、完成出库、库存流水、报表四方数量和金额可对账；草稿不进入实际出库报表。

### SM-P5：售后和异常闭环

任务：售后出库关联原订单/出库；售后原因、责任、客户反馈；销售异常（超发、无来源、库存不足、价格异常、逾期）统一工作台；退货入库单独立项。

验收：售后单不能伪装为普通销售出库；原销售来源可追踪；异常有责任人、处理状态和审计记录。

### SM-P6：权限、审计和上线

任务：评估新增 `sales` 角色；销售录单、确认、取消、下推、仓库完成、反提交和删除分权；补齐 CSRF、审计、批量隔离、并发和数据脱敏；浏览器多角色验收。

推荐权限：

| 能力 | admin | sales/业务 | warehouse | purchase | user |
|---|---:|---:|---:|---:|---:|
| 查看销售订单/报表 | 是 | 是 | 是 | 按需 | 只读 |
| 新建/编辑草稿 | 是 | 是 | 按授权 | 否 | 否 |
| 确认/取消订单 | 是 | 是 | 按授权 | 否 | 否 |
| 生成销售出库草稿 | 是 | 是 | 是 | 否 | 否 |
| 完成/反提交出库 | 是 | 否 | 是 | 否 | 否 |
| 删除/作废 | 是 | 按授权 | 按授权 | 否 | 否 |
| AI 销售草稿 | 是 | 是 | 是 | 否 | 否 |

在新增 `sales` 角色前，先评估现有用户数据和前端角色筛选；迁移必须有默认映射和回滚方案。

### SM-P7：AI 销售能力接入

任务：把已有 `AI-SALES-F01` 草稿验证接入销售订单/出库确认台；提供销售订单草稿、销售出库草稿、部分发货校验和销售报表一致性证据；补充销售订单图片/表格识别入口。

硬边界：AI 不能确认、提交、完成、取消、关闭、删除、自动发货或直接改库存；AI 只能在权限通过、人工确认和幂等保护下生成草稿。

每项 AI 改动必须先检查 `WMS_AI_FUNCTION_DEVELOPMENT_PLAN.md`，使用唯一任务 ID；普通销售功能不擅自登记为 AI 任务。

## E. 测试和验收矩阵

### E.1 单据流

- 草稿创建、编辑、复制、删除、确认、取消。
- 单行、多行、同物料多行、零数量、负数量、超精度、超发。
- 整单下推、按行部分下推、一单多出、重复请求、并发下推。
- 出库完成扣库存、反提交回库存、删除草稿不影响库存。
- 订单、出库单、库存流水、报表数量和金额对账。

### E.2 权限和安全

- 查看角色不能写入；销售角色不能完成仓库出库；仓库角色不能修改已确认订单。
- 所有 POST/批量接口校验 CSRF、登录、角色和来源单据归属。
- 删除、取消、反提交和完成均保留操作审计。
- 不记录客户手机号等不必要敏感信息到导出和 AI 反馈。

### E.3 页面和数据

- 订单列表、详情、出库选单、工作台、报表和打印页面无乱码。
- 页面和 Excel 同筛选条件得到相同数量和金额。
- 采购入库、普通领料、销售出库、售后出库互不串单。
- 历史来源迁移报告可复核，未知来源不会被静默归类。

## F. 近期执行顺序

1. `SM-P0-01` 固化销售订单、销售出库、库存流水和售后字段数据字典。
2. `SM-P0-02` 增加销售订单/出库/库存三方对账脚本，先只读发现问题。
3. `SM-P1-01` 增加 `OutOrderItem.source_sales_order_item_id` 及历史匹配迁移。**已完成：阶段 10。**
4. `SM-P2-01` 建设销售出库选单工作台，接入行级数量校验和幂等。**已完成：阶段 11。**
5. `SM-P3-01` 完善销售待办、销售出库草稿入口和双向来源跳转。
6. `SM-P4-01` 统一报表查询服务，补销售对账和售后独立口径。
7. `SM-P5-01` 设计售后原单关联和退货入库需求，不直接修改库存模型。
8. `SM-P6-01` 评估 `sales` 独立角色并完成多角色浏览器验收。
9. `SM-P7-01` 将 AI-SALES-F01 接入人工确认台，保持 AI 草稿边界。

## 阶段 11 实施记录（2026-07-19）：销售出库选单工作台（SM-P2-01）

- 新增 `/sales/outbound_selection` 页面和销售管理菜单入口，按客户、发货仓库、业务员、订单/物料关键字查询已确认待发货明细。
- 新增 `/api/sales_order/selectable` 查询接口和 `/sales/create_outbound_from_selection` 生成接口；支持同客户、同仓库多张销售单合并生成一张销售出库草稿。
- 服务端重新校验销售订单状态、客户/仓库一致性、待发货数量、重复草稿和权限；每行保留 `source_sales_order_item_id`，多订单出库完成时按来源行回写各销售订单。
- 页面和接口均不自动完成出库，库存扣减仍由仓库人工完成销售出库单触发；跨客户、跨仓库选单明确拒绝。

**验证**：`python -m py_compile app/app.py scripts/verify_sales_module.py` 通过；`python scripts/verify_sales_module.py` 23/23 通过；WMS 回归通过。剩余子任务：销售订单仓库外键约束、跨仓库策略和选单并发控制。

## 阶段 12 实施记录（2026-07-19）：销售订单仓库主数据约束

- `SalesOrder` 新增 `warehouse_id` 外键和索引，保留 `warehouse` 字符串用于历史单据和库存报表兼容。
- 启动迁移会先补齐旧 `warehouse` 字段，再按仓库名称、仓库编码分两步回填 `warehouse_id`；未匹配历史数据保留人工处理，不静默绑定。
- 新建、编辑、导入、确认、批量确认、订单下推和选单下推统一要求有效且启用的仓库；销售选单按仓库 ID 和名称双重约束，禁止跨仓合并。
- 仓库删除检查销售订单引用，仓库改名同步已关联销售订单的兼容名称字段。

**验证**：`python -m py_compile app/app.py scripts/verify_sales_module.py` 通过；`python scripts/verify_sales_module.py` 25/25 通过；`python scripts/verify_wms_bugs.py` 通过；服务重启后 `/login` 返回 200。提交：`66ba42b`。剩余子任务：跨仓库策略细化、选单并发控制和销售报表仓库外键筛选统一。

## 阶段 13 实施记录（2026-07-19）：销售出库跨仓库边界

- 直接销售出库保存时要求仓库存在且启用；销售出库完成时再次校验单据仓库与来源销售订单/来源明细仓库一致。
- 销售选单继续按仓库 ID 和名称双重约束，禁止跨仓库合并；普通领料和其他出库保持原有仓库处理逻辑。
- 新增无效销售订单仓库、无效销售出库仓库的接口验收；单事务内的重复草稿检查继续生效。

**验证**：`python -m py_compile app/app.py scripts/verify_sales_module.py` 通过；`python scripts/verify_sales_module.py` 26/26 通过；`python scripts/verify_wms_bugs.py` 通过；服务重启后 `/login` 返回 200。提交：`a1a12ee`。剩余子任务：多进程选单并发锁、销售报表仓库外键筛选统一。

## 阶段 14 实施记录（2026-07-19）：销售报表仓库筛选统一

- 销售汇总、订单执行、价格分析、销售出库明细和趋势分析统一使用 `warehouse_id` 筛选；历史未回填订单/出库单保留名称兜底。
- 页面仓库下拉框使用启用仓库主数据，页面与 Excel 导出复用同一请求参数和筛选口径。
- 销售报表查询和导出均覆盖仓库筛选回归；销售出库事实仍只统计销售出库单，订单经营报表仍统计销售订单。

**验证**：`python -m py_compile app/app.py scripts/verify_sales_module.py` 通过；`python scripts/verify_sales_module.py` 26/26 通过；`python scripts/verify_wms_bugs.py` 通过；服务重启后 `/login` 返回 200。提交：`ddff2a0`。剩余子任务：多进程选单并发锁。

## 阶段 15 实施记录（2026-07-19）：销售选单并发保护

- 销售选单生成接口在 SQLite worker 进程中使用 `BEGIN IMMEDIATE` 获取短写锁，并在加锁后重新读取销售明细。
- 加锁后再次检查来源订单状态、未发货数量和待处理销售出库草稿，避免两个进程同时通过预检查并重复占用同一销售明细。
- 非 SQLite 数据库使用 `SELECT ... FOR UPDATE` 读取来源明细；异常时事务回滚，库存仍只由人工完成出库触发。
- 新增重复选单保护回归用例，验证已有待处理草稿时接口拒绝第二次下推。

**验证**：`python -m py_compile app/app.py scripts/verify_sales_module.py` 通过；`python scripts/verify_sales_module.py` 28/28 通过；`python scripts/verify_wms_bugs.py` 通过；服务重启后 `/login` 返回 200。提交：`d506b23`。

## 阶段 16 实施记录（2026-07-19）：销售工作台和作业入口（SM-P3-01）

- 销售工作台新增待确认、待出库/部分发货、销售出库草稿、逾期订单和库存不足待核对分组，并提供真实单据下钻。
- 新增 `/sales/outbound` 独立销售出库列表，销售出库不再依赖普通领料列表查看；支持状态和关键词筛选。
- 销售出库详情显示来源销售订单链接；订单详情保留关联销售出库单清单，形成订单与出库双向跳转。
- 库存不足仅作为待核对提示，不自动改库存、不自动完成销售出库；异常处理继续保留仓库人工确认边界。

**验证**：`python -m py_compile app/app.py scripts/verify_sales_module.py` 通过；`python scripts/verify_sales_module.py` 28/28 通过；`python scripts/verify_wms_bugs.py` 通过；服务重启后 `/login` 返回 200。提交：`652c3bb`。

## G. 每阶段交付门槛

每个任务必须同时具备：代码、权限、审计、迁移说明、测试、页面入口、报表口径、运行验证和回滚说明。完成后更新本计划的任务状态、提交哈希、验证命令和剩余风险；提交和推送必须落在 `main`，不得创建工作分支。
