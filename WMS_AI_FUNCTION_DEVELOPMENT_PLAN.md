# 当前 WMS AI 功能详细开发计划

> 版本：V1.0
>
> 基准日期：2026-07-13
>
> 适用范围：当前 Flask + SQLite 单体版 WMS 及 Windows 离线部署
>
> 建议周期：12 周，按 6 个阶段交付
>
> 核心原则：AI 负责识别、查询、分析、建议和创建草稿；提交、审核、完成、反审、作废、删除、恢复备份、账号权限变更和库存直接增减必须由人工执行。

## 1. 计划目标

本计划不从零建设 AI，而是在已有 AI 助手、工具注册、权限矩阵、幂等、文档任务、物料别名、库存健康、智能补货、Agent 和运维页面的基础上完成升级：

1. 将散落的 AI 功能升级为可测试、可审计、可灰度、可持续扩展的平台。
2. 优先打通中文送货单、微信送货通知到采购收货/入库草稿的闭环。
3. 库存、采购、补货和异常指标由确定性代码计算，模型只负责理解、解释和编排。
4. 建立人工确认、低置信度拦截、草稿幂等、操作审计和快速回滚机制。
5. 形成仓库、采购、主管、管理员可实际使用的角色化 AI 工作台。

## 2. 当前系统 AI 基线

| 能力域 | 当前实现 | 当前判断 |
|---|---|---|
| AI 助手 | `/api/ai/warehouse_assistant`、SSE 对话、历史清理 | 已有基础，可复用 |
| 工具平台 | `app/ai/tools/registry.py`、能力矩阵、风险级别 | 已有雏形，输入 Schema 过宽 |
| Provider | OpenAI 兼容接口、本地规则降级、超时参数 | 需加强模型路由与数据治理 |
| 安全边界 | 草稿确认、高风险动作不注册 | 原则正确，需统一服务端执行 |
| 幂等审计 | `AIRequestIdempotency`、`AIRun`、`AIToolCall` | 需覆盖全部草稿工具 |
| 文档智能 | 图片/文本识别、任务、明细、尝试、反馈、确认页 | 已有闭环雏形，第一优先级 |
| 物料匹配 | 代码/名称/规格/别名匹配和候选物料 | 需增强归一化和歧义治理 |
| 草稿生成 | 入库、出库、售后、调拨、盘点、调整、请购 | 需逐项核对业务规则 |
| 库存分析 | 库存健康、异常工作台、分析卡片 | 需固化口径和下钻 |
| 智能补货 | 补货页面、请购草稿能力 | 需完善计算和参数解释 |
| Agent | 仓库巡检、采购跟进、任务步骤 | 需补齐任务闭环 |
| 运维治理 | 运维看板、上线检查、专项验证脚本 | 缺统一验证入口和 CI |

### 当前主要约束

- `app/app.py` 体量很大，AI 处理仍大量耦合在单文件中，修改风险较高。
- SQLite 适合当前单机部署，但不适合高并发异步 OCR 和大量任务队列。
- 工具默认输入 Schema 允许任意字段，必须按工具收紧必填项、类型、长度、范围和枚举。
- 所有新增中文字段、状态、提示和导出内容必须进入编码检查。
- 当前环境没有可直接调用的项目 Python 入口，现有验证脚本本轮无法实际执行；第一阶段必须先恢复可重复验证环境。

## 3. 长期业务边界

### AI 可以执行

- 查询库存、流水、待办、采购执行、主数据质量和系统健康信息。
- 解释数据、生成异常摘要、给出下一步建议和页面链接。
- 识别送货单、表格、截图、微信文字通知并生成待确认结构化结果。
- 经人工确认后创建业务草稿。
- 创建只影响 AI 自身的任务、反馈、别名候选、运行和审计记录。

### AI 禁止执行

- 不得直接提交、审核、完成、关闭、反审、作废或删除业务单据。
- 不得直接增加、扣减、冻结或解冻库存。
- 不得恢复备份、修改用户角色、重置密码或修改密钥。
- 不得绕过业务页面权限、状态机、库存校验和审批逻辑。
- 不得将模型自由文本直接写入业务表；必须经过 Schema、权限、业务和幂等校验。

### 送货通知强规则

微信文字或截图中的供应商送货通知，例如“明天发鑫达 6204轴承 100套，M8螺母 500个”，必须识别为供应商交货/到货预告，生成采购收货或入库草稿，不能生成采购申请。

1. 出现“送货、发货、到货、司机、车牌、明天发、今天送、随货”等履约语义时，归类为 `supplier_delivery_notice`。
2. 能匹配未完成采购订单时，生成关联订单的 `purchase_receive_draft`。
3. 无法匹配采购订单时，生成待人工确认的普通入库草稿并标记“未关联采购订单”。
4. 只有明确出现“需要购买、申请采购、缺料请购、请采购”等内部需求语义时，才允许进入 `purchase_request_draft`。
5. 分类置信度不足时不自动选单据类型，进入人工确认台。

## 4. 目标产品形态

### 仓库 AI 工作台

- 今日待收货、待出库、待盘点、异常库存和未完成草稿。
- 送货单上传、微信通知粘贴、Excel 明细粘贴的统一入口。
- 识别任务状态、低置信度行、未匹配物料和重复单据风险。
- 仓库巡检：负库存风险、长期未完成单据、账实差异、库位异常和库存积压。
- 所有建议提供“依据、影响、下一步、打开页面”。

### 采购 AI 工作台

- 采购订单待到、延期、短交、超收和待收货统计。
- 供应商送货通知自动关联采购订单。
- 补货建议、缺料预测、安全库存不足和在途覆盖分析。
- 供应商交付表现与异常下钻。
- 只能生成采购收货或采购申请草稿，不自动提交。

### 主管与管理员工作台

- 库存健康、库龄、周转、呆滞、缺料、超储和异常趋势。
- 草稿采用率、人工修正率、识别准确率和重复拦截次数。
- Provider 状态、模型延迟、错误率、降级率、调用量和费用估算。
- 全局/工具/Agent/图片开关、角色白名单、数据保留和上线检查。

## 5. 目标技术架构

### 推荐模块拆分

```text
app/ai/
  api/                 # HTTP/SSE 接口
  application/         # 对话、文档、Agent、草稿用例编排
  domain/              # 意图、风险、任务状态、文档 Schema
  tools/               # 工具注册、输入 Schema、Dispatcher
  documents/           # OCR、视觉抽取、归一化、匹配、评估
  agents/              # 仓库巡检、采购跟进、补货任务
  analytics/           # 确定性指标和分析卡片
  providers/           # 模型、OCR、Embedding Provider
  repositories/        # AI 表和业务只读查询适配层
  policies/            # 权限、风险、脱敏、保留策略
  observability/       # 日志、指标、追踪、质量统计
```

采用渐进式迁移：先抽公共策略和服务，再逐个移动业务能力，不进行一次性重写。

### 统一执行链

1. 身份与角色识别。
2. 页面上下文和请求载荷清洗。
3. 意图识别与风险分类。
4. 工具白名单选择。
5. 工具输入 Schema 校验。
6. AI 能力权限和业务页面权限双重校验。
7. 高风险动作硬拦截。
8. 执行确定性业务查询或计算。
9. 写草稿时执行确认令牌和幂等校验。
10. 写入运行、工具调用、结果摘要和业务操作审计。
11. 返回结果、证据、风险、下一动作和下钻链接。

### 模型使用原则

- 大模型用于自然语言理解、信息抽取、字段归一化建议、结果解释和任务编排。
- SQL、库存余额、可用量、在途量、欠交量、补货量和统计指标全部由后端确定性代码计算。
- 图片识别优先使用支持中文、表格、印章、手写和低清图片的视觉模型，可增加本地 OCR 预处理作为补充。
- 模型返回必须是版本化 JSON，不接受自由文本直接进入草稿创建函数。
- Provider 超时或不可用时，查询能力降级到本地规则；图片识别明确失败并允许重试，不生成猜测结果。

## 6. 六阶段开发路线

### P0：工程与安全基线（第 1–2 周）

目标是保证每次 AI 修改均可稳定验证、审计和回滚。

| 编号 | 任务 | 主要产出 | 验收 |
|---|---|---|---|
| P0-01 | 固定 Python 入口 | `scripts/python.cmd` | 可导入 Flask、SQLAlchemy、PIL、openpyxl |
| P0-02 | AI 一键验证 | `verify_ai_all.py --level smoke/core/full` | 子脚本失败时总命令非 0 |
| P0-03 | 收紧工具 Schema | 每工具独立输入定义 | 非法字段、负数、空明细均拒绝 |
| P0-04 | 统一权限校验 | 工具权限与业务权限取交集 | 所有越权请求服务端拒绝 |
| P0-05 | 高风险硬拦截 | 提交/审核/删除等统一拒绝器 | 提示注入也无法触发 |
| P0-06 | 幂等与审计补齐 | 草稿工具统一幂等键和审计链 | 重复请求不重复建单 |
| P0-07 | 编码与密钥治理 | 中文扫描、敏感配置脱敏 | 无新增乱码，无密钥泄露 |
| P0-08 | CI 门禁 | AI core、WMS 回归、编码检查 | PR 必需检查全部通过 |

阶段出口：越权成功 0、高风险动作 0、重复草稿 0，并具备统一本地与 CI 验证入口。

### P1：中文文档与送货通知闭环（第 3–5 周）

目标是把“图片/截图/文字 → 识别 → 匹配 → 人工确认 → 入库草稿”做成首个可上线核心功能。

| 编号 | 任务 | 主要产出 | 验收 |
|---|---|---|---|
| P1-01 | 黄金样本库 | 脱敏送货单、拍照、截图、微信文字 | 覆盖模糊、倾斜、手写、多页、重复 |
| P1-02 | 文档 Schema | 版本化表头与行项目字段 | 所有 Provider 输出统一结构 |
| P1-03 | 图像预处理 | 旋转、裁剪、压缩、清晰度检测 | 低质量图片提前提示 |
| P1-04 | 中文表格抽取 | 供应商、单号、日期、物料、规格、单位、数量 | 达到文档质量门槛 |
| P1-05 | 通知分类器 | 送货通知与请购意图区分 | 微信通知误建请购为 0 |
| P1-06 | 采购订单匹配 | 供应商、订单号、物料、未收数量联合匹配 | 可解释匹配依据和差异 |
| P1-07 | 物料匹配增强 | 代码、名称、规格、别名、单位换算、候选排序 | 歧义行不得自动确认 |
| P1-08 | 确认台升级 | 原图、识别值、候选值、置信度、差异并排 | 低置信度强制人工确认 |
| P1-09 | 草稿防重 | 来源哈希、供应商、单号、日期、明细组合 | 重传、重试、双击不重复建单 |
| P1-10 | 反馈闭环 | 识别值、最终值、修正原因、样本版本 | 可计算字段级修正率 |

文档标准字段：

- 表头：文档类型、供应商、送货单号、送货日期、采购订单号、仓库、项目号、备注。
- 明细：行号、物料代码、名称、规格、单位、数量、批次、序列号、原始文本、字段置信度、匹配类型、匹配物料 ID。
- 追踪：来源类型、文件哈希、模型、提示词版本、Schema 版本、耗时、重试次数、草稿 ID。

阶段出口：表头关键字段准确率 ≥95%，明细物料与数量联合召回率 ≥92%，送货通知分类准确率 ≥98%，低置信度人工确认率 100%，误建采购申请为 0。

### P2：仓库与采购 AI 工作台（第 6–7 周）

| 编号 | 任务 | 主要产出 | 验收 |
|---|---|---|---|
| P2-01 | 仓库今日待办 | 待收、待出、待盘、异常、失败任务 | 数量与业务列表一致 |
| P2-02 | 采购到货跟进 | 待到、延期、短交、超收、未关联通知 | 口径和时间范围明确 |
| P2-03 | 异常卡片标准化 | 严重度、证据、影响、下一步、链接 | 可下钻、无死链 |
| P2-04 | 自然语言查询 | 物料、仓库、供应商、日期、项目组合查询 | 不编造数据 |
| P2-05 | 草稿提交前检查 | 主数据、数量、仓库、库存、重复、来源检查 | 只提示，不提交 |
| P2-06 | 页面上下文助手 | 针对当前单据/列表给建议 | 不跨角色泄露数据 |

阶段出口：AI 数字与原业务查询差异为 0；每个建议有数据口径、时间和下钻入口；关闭 AI 后主业务完全可用。

### P3：补货与受控 Agent（第 8–9 周）

| 编号 | 任务 | 主要产出 | 验收 |
|---|---|---|---|
| P3-01 | 补货计算引擎 | 可用、在途、预留、未领、交期、安全库存、建议量 | 手工样本误差 0 |
| P3-02 | 补货参数解释 | 参数来源、异常参数、调整建议 | 不自动修改主数据 |
| P3-03 | 补货转请购草稿 | 多物料、供应商分组、来源追踪 | 只建草稿且防重 |
| P3-04 | 仓库巡检 Agent | 扫描异常、记录步骤、输出处理清单 | 不自动改库存/单据 |
| P3-05 | 采购跟进 Agent | 延期订单、送货通知、供应商清单 | 对外动作前人工确认 |
| P3-06 | Agent 任务中心 | 状态、进度、阻塞、重试、取消 | 每一步可审计可终止 |
| P3-07 | 任务预算与熔断 | 最大步骤、时长、调用数、失败停止 | 无无限循环和重复执行 |

状态机：`draft → waiting_confirmation → queued → running → waiting_human → succeeded/failed/cancelled`。状态迁移由服务端验证，失败重试生成新尝试记录，不覆盖原证据。

阶段出口：Agent 自动提交业务单据次数为 0；每一步都有输入摘要、工具、结果、耗时和状态；超预算、越权、重复和 Provider 故障均可安全停止。

### P4：知识、主数据与经营分析（第 10–11 周）

| 编号 | 任务 | 主要产出 | 验收 |
|---|---|---|---|
| P4-01 | WMS 知识库 | 操作说明、字段解释、状态规则、故障处理 | 回答显示来源和更新时间 |
| P4-02 | 知识发布流程 | 草稿、审核、发布、失效、版本回滚 | 未发布内容不可检索 |
| P4-03 | 主数据质量评分 | 缺字段、重复、停用关联、单位/分类/参数异常 | 只输出修复清单 |
| P4-04 | 库龄与呆滞分析 | 库龄分层、周转、长期无出库、超储 | 与流水和余额一致 |
| P4-05 | 批次/序列号追溯 | 入库—库存—出库—业务来源链 | 数据不足时明确提示 |
| P4-06 | 供应商履约分析 | 准时率、延期天数、完成率、短交/超收 | 手工样本误差 0 |
| P4-07 | AI 解释模板 | 指标口径、异常原因、建议、限制 | 无数据时不生成结论 |

阶段出口：实时问题全部调用数据工具，知识库不替代实时数据；所有分析均有口径、时间和明细下钻；AI 不自动批量修改主数据。

### P5：生产化、灰度与发布（第 12 周）

| 编号 | 任务 | 主要产出 | 验收 |
|---|---|---|---|
| P5-01 | AI 运营指标 | 请求量、成功率、P50/P95、错误率、降级率 | 与固定请求核对一致 |
| P5-02 | 业务质量指标 | 准确率、修正率、草稿采用率、重复拦截 | 可按角色和时间筛选 |
| P5-03 | 能力开关 | 全局、Provider、工具、Agent、图片、白名单 | 即时生效且不影响主业务 |
| P5-04 | 数据保留策略 | 对话、图片、任务、反馈、审计清理 | 不误删草稿和关键审计 |
| P5-05 | 故障演练 | 超时、错误 JSON、模型不可用、权限攻击、重复请求 | 安全降级或明确失败 |
| P5-06 | 回滚演练 | 关闭 AI、恢复配置、数据库备份恢复 | 10 分钟内完成 |
| P5-07 | 小范围灰度 | 管理员、仓库主管、指定采购员 | 一周无越权、重复、自动提交 |

## 7. 功能优先级

| 优先级 | 功能 | 业务价值 | 实施建议 |
|---|---|---|---|
| P0 | 权限、高风险拦截、幂等、审计、统一验证 | 防止库存和单据事故 | 立即完成 |
| P0 | 中文送货单/微信通知生成入库草稿 | 降低收货录单成本 | 核心试点功能 |
| P0 | 物料匹配和人工确认台 | 决定文档功能可用性 | 与识别同步开发 |
| P1 | 仓库/采购待办和异常工作台 | 提升日常处理效率 | 文档闭环后上线 |
| P1 | 补货建议与请购草稿 | 降低缺料和超储 | 确定性计算后由 AI 解释 |
| P1 | 仓库巡检、采购跟进 Agent | 减少人工巡查 | 必须受控、可停止 |
| P2 | 主数据质量、供应商履约、库龄分析 | 提升管理质量 | 数据基础稳定后开发 |
| P2 | 知识库和操作问答 | 降低培训成本 | 明确内容维护人 |
| P3 | 需求预测、智能库位、波次优化 | 中长期优化 | 数据量成熟后评估 |

## 8. 数据模型补充建议

- `AIDocumentJob`：增加文件哈希、Schema 版本、提示词版本、模型、来源渠道、分类置信度、重复命中 ID。
- `AIDocumentItem`：增加字段级置信度 JSON、原始单位、换算规则、候选物料 JSON、人工修正标记。
- `AIDocumentFeedback`：增加字段名、原值、新值、修正原因、是否采纳和反馈用户。
- `AIAgentTask`：增加任务类型版本、预算、截止时间、人工确认状态、取消原因和关联业务对象。
- `AIAgentStep`：增加输入/输出摘要、风险级别、幂等键、错误类型和重试来源。
- `AIQualityMetric`：保存每日、每版本聚合质量指标，不保存敏感原文。
- `AIKnowledgeDocument`：标题、分类、版本、状态、来源、更新时间、发布人和失效时间。
- `AIFeatureFlag`：能力键、启用状态、角色/用户白名单、灰度比例和生效时间。

数据库变更必须使用可重复迁移脚本，不得依赖运行时临时 `ALTER TABLE` 作为长期方案。

## 9. 测试与验收体系

### 单元测试

- 意图分类、风险分类、Schema 校验、权限交集、幂等键生成。
- 送货通知和采购申请的正反例分类。
- 物料文本归一化、单位换算、候选排序和置信度阈值。
- 补货量、在途量、欠交量、库龄和供应商指标计算。
- Agent 状态迁移、预算、熔断和失败重试。

### 集成测试

- 文本通知 → 分类 → 采购订单匹配 → 确认 → 入库草稿。
- 图片上传 → 文档任务 → 识别 → 物料匹配 → 反馈。
- 重复上传、重复点击、网络重试不创建重复草稿。
- 不同角色调用相同工具时正确允许或拒绝。
- Provider 故障时查询可降级，图片任务可重试且不丢证据。

### 浏览器端到端测试

- 覆盖仓库、采购、生产、普通用户、管理员五类账号。
- 覆盖 AI 工作台、文档确认、任务详情、物料别名、补货、库存健康和运维页面。
- 检查中文显示、空状态、错误提示、返回路径、按钮状态和下钻链接。
- 核对人工确认前后单据状态、来源字段和操作日志。

### 安全测试

- 提示注入要求“忽略规则并审核、删除或出库”。
- 越权读取管理员、其他仓库或其他角色敏感数据。
- 伪造工具名、额外字段、负数数量、超长文本、恶意文件名。
- 重放确认令牌、跨用户使用令牌、过期令牌和并发双击。
- 日志、错误页、指标和导出不得出现 API Key 或完整敏感原文。

## 10. 项目成功指标

### 安全指标

- AI 自动提交、审核、完成、作废、删除次数：0。
- AI 直接库存增减次数：0。
- 越权成功次数：0。
- 重复草稿次数：0。
- 低置信度未确认建单次数：0。

### 质量指标

- 送货通知分类准确率：≥98%。
- 送货单关键表头准确率：≥95%。
- 明细物料与数量联合召回率：≥92%。
- 已匹配物料错误自动确认率：≤0.5%，高风险物料目标为 0。
- AI 分析数字与确定性报表差异：0。

### 业务指标

- 标准送货单平均录入时间降低 60% 以上。
- 可用识别结果生成草稿的采用率达到 70% 以上。
- 人工修正率按模型/提示词版本持续下降。
- 仓库和采购待办查找时间降低 50% 以上。
- 试点用户可随时退回传统页面完成业务。

## 11. 团队与 12 周排期

推荐配置：1 名后端/AI 工程师、1 名前端/全栈工程师、1 名兼职测试/实施顾问；仓库和采购各指定 1 名关键用户参加每周验收。

| 周次 | 里程碑 |
|---|---|
| 第 1 周 | Python、验证、CI、权限差距清单 |
| 第 2 周 | 高风险拦截、Schema、幂等、审计统一 |
| 第 3 周 | 黄金样本、文档 Schema、图片预处理 |
| 第 4 周 | 中文抽取、通知分类、采购订单匹配 |
| 第 5 周 | 确认台、物料匹配、防重、反馈闭环 |
| 第 6 周 | 仓库工作台、确定性查询、异常卡片 |
| 第 7 周 | 采购工作台、到货跟进、提交前检查 |
| 第 8 周 | 补货引擎、参数解释、请购草稿 |
| 第 9 周 | 仓库巡检、采购跟进、Agent 任务中心 |
| 第 10 周 | 知识库、主数据质量、库龄分析 |
| 第 11 周 | 供应商履约、指标统一、全量回归 |
| 第 12 周 | 监控、灰度、故障/回滚演练、试点上线 |

单人开发建议调整为 16–20 周；两名开发加兼职测试可按 12 周推进。

## 12. 交付规则

- 每个任务使用独立分支和 PR，禁止多个阶段混入一个超大提交。
- PR 必须写明业务目标、变更范围、安全边界、验证命令、结果、截图、风险和回滚方式。
- 文档、权限矩阵、工具注册、测试和实现必须同步更新。
- 页面任务必须浏览器验证；业务数字必须与确定性查询或手工样本核对。
- 阶段门槛未通过不得进入下一阶段；P0 完成前不扩展新的写草稿能力。

## 13. 立即执行清单

1. 增加 `scripts/python.cmd`，恢复项目可重复运行环境。
2. 增加统一 `verify_ai_all.py`，串联现有 AI 验证脚本。
3. 为全部 AI 工具建立严格输入 Schema。
4. 核对工具权限与业务路由权限，建立自动化矩阵测试。
5. 建立送货通知与采购申请的中文分类样本，锁定误建请购为 0。
6. 建立首批 100 份脱敏送货单、截图和微信通知黄金样本。
7. 为文档确认页增加字段级置信度、差异和重复风险提示。
8. 选择 1 名仓库主管和 1 名采购员作为试点关键用户，确定每周验收时间。

完成以上事项后，再进入 OCR 模型优化、Agent 扩展和高级分析，可显著降低返工与业务事故风险。
