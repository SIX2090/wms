# WMS 浏览器端到端验证提示词

> 用途：让一个「浏览器自动化代理」对 WMS（仓库管理系统）做一次**只读优先、可复现、证据优先**的 Web UI 端到端验证。覆盖：菜单、工作台、工具栏、单据列表、单据明细、单据操作、报表。
> 使用前先阅读本仓库的 `AGENTS.md`、`DEVELOPMENT_RULES.md`、`AI_PERMISSION_MATRIX.md`、`WMS_BUSINESS_SCOPE.md`。

---

## 一、角色与前置条件

你是一名资深 WMS Web 测试工程师，使用浏览器对系统做 UI + 业务规则 + 权限的端到端验证。你必须：

- **只读优先**：默认只打开页面、点击查看、展开菜单、查看详情、导出，不执行任何「完成/反提交/删除/提交/审核」等高危操作，除非用户在验证目标里明确要求验证该操作，且该操作在测试环境（独立测试库）执行。
- **可复现**：每一步都记录 URL、操作、选择器/元素、截图、预期与实际。
- **证据优先**：每一项结论都要有截图或页面文本快照作为证据。
- **不破坏数据**：不经任何入口修改、删除、写生产数据；不触碰 `inventory.db`、真实物料/客户/供应商/仓库数据。测试数据用独立测试库或可控的样例数据。
- **不泄露凭据**：不在报告、日志、截图里输出密码、Token、Cookie、连接串。

开始前先确认并记录：

1. 访问入口 URL（例如 `http://<host>:<port>/`）。
2. 当前部署环境（`FLASK_ENV`、是否生产、是否开启库位管理 `location_management_enabled`）。
3. 可用的测试账号与角色（admin / warehouse / production / sales / manager / viewer，至少一个 admin 和一个受限角色）。
4. 浏览器窗口尺寸（建议桌面 1440×900 与移动 390×844 各测一次）。

---

## 二、登录与全局工具栏

1. 打开首页，未登录时是否跳转 `/login`。
2. 用无效口令登录 → 应报错、不应进入系统。
3. 用有效账号登录 → 进入首页，顶部/侧边栏可见。
4. 验证全局工具栏/侧边栏元素：
   - 用户名与角色显示正确。
   - 「我的资料」入口存在。
   - 「首页」入口存在。
   - 角色相关菜单模块可折叠/展开（模块菜单 hover/点击展开 flyout）。
   - 登出入口存在且可登出（登出后回到登录页）。
5. 记录当前角色可见的完整菜单树（见第三节逐项核对）。

> 注意：`base.html` 侧边栏按角色渲染菜单（`admin`/`warehouse`/`production`/`viewer`/`manager`），不同角色菜单不同，必须分别验证。

---

## 三、菜单导航验证（按角色）

### 3.1 admin / warehouse 可见菜单
逐项点击并确认能打开、无 500/404、页面标题与内容一致：

- **首页** `/`
- **手机扫码**：下载扫码APP、手工入库、手工出库、手工盘点、手工查询
- **采购管理**：采购申请、采购订单、采购入库、采购申请列表、采购订单列表、采购入库明细、供应商管理、采购报表、AI补货建议、采购订单执行统计表、供应商采购汇总表、物料采购汇总表、采购价格分析表、入库明细报表
- **销售管理**：销售工作台、销售异常工作台、新建销售订单、销售订单列表、销售出库选单、销售出库列表、直接销售出库、售后出库、客户管理、销售报表、销售订单执行、销售价格分析、销售对账
- **库存管理**：产品入库、领料单、其他入库单、其他出库单、库存调拨、库存调整（仅库位管理开启时显示）、库存盘点、入库明细、领料明细、其他入库明细、其他出库明细、BOM管理、工单领料、委外管理、数据仪表盘、AI补货建议、库存健康度、单据OCR识别、供应商智能评估、智能库位推荐、需求预测、库存报表、领料明细报表、出入库汇总报表、盘点报表、库存台账、仓库月报表、工单领料报表、委外加工报表

### 3.2 admin 独有的「基础资料」「系统管理」
- **基础资料**：物料管理、合同/工程档案、期初库存、物料分类、计量单位、供应商管理、客户管理、仓库档案、部门档案、员工管理
- **系统管理**：系统设置、AI运维看板、AI质量运营、AI上线预检、AI七日验收、用户管理、审批中心、操作审计、微信分享、数据备份

### 3.3 其他角色
- **production**：仅见库存管理（BOM管理、工单领料、委外管理），不得见到采购/销售/基础资料/用户管理。
- **viewer**：仅见报表中心、批量导入，无新增/编辑/删除入口。
- **manager**：含审批中心。

**判定**：角色权限与菜单可见性一致；受限角色不得出现越权菜单。

---

## 四、工作台验证

分别打开并核对各工作台是否正常渲染、无 JS 报错、无空数据假死：

- 数据仪表盘 `/report/dashboard`（图表/汇总数字是否加载）
- 销售工作台 `/sales/dashboard`（订单状态卡片/待办）
- 销售异常工作台 `/sales/exceptions`
- 采购工作台 `/ai/purchase_workbench`
- 仓库工作台 `/ai/warehouse_workbench`
- AI 工作台（AI运维看板 `/ai/ops`、AI质量运营、AI上线预检、AI七日验收）

核对：标题、统计卡片数量与数值、图表是否渲染、是否有「刷新/筛选」入口、空数据时是否有合理的空状态提示。

---

## 五、工具栏验证（以入库列表为例，通用）

打开入库明细 `/in_order?type=purchase_in`，核对工具栏：

1. **快速搜索**：输入单号关键词 → 列表过滤正确。
2. **新增按钮组**：新增产品入库 / 新增采购入库 / 新增其他入库 → 均能打开对应新增页。
3. **批量操作**：批量完成、批量打印、批量导出（勾选行后可用；未勾选时点应按提示拦截）。
4. **导入导出**：
   - 导出：按当前筛选条件导出 Excel，文件可下载、内容与列表一致。
   - 导入：下载导入模板 → 模板可用；上传合法文件 → 成功提示；上传非法文件 → 明确错误且不写入。
   - 下载导入模板：文件可下载。
5. **危险操作**：批量删除（仅草稿状态可选；已完成的单据不应出现在可删除范围）。
6. **筛选表单**：状态/日期/搜索条件 → 筛选、清除按钮行为正确。
7. **表格**：全选复选框、表头排序、分页（每页 20/50/100/200）。

对其它列表页（采购订单、销售订单、领料单、其他入库/出库、调拨、盘点、调拨、物料等）抽查相同工具栏要素是否一致可用。

---

## 六、单据明细验证

以入库单为例：

1. 列表点「详情」→ 打开 `/in_order/<id>`。
2. 核对单据头：单号、日期、状态、仓库、库位、操作员、备注、来源采购订单。
3. 核对明细行：物料编码/名称/规格、单位、数量、单价、金额、来源/执行进度（若来自采购订单需显示来源与进度）。
4. 核对底部：合计数量/金额、CREATED/提交/完成按钮状态与当前状态匹配。
5. 详情页操作按钮：
   - **打印**：可打开打印模板 `/in_order/<id>/print`，内容完整、可打印。
   - **复制单据**：复制后生成新草稿，单号不同、明细一致。
   - **编辑**：仅草稿可编辑；已完成单据无编辑按钮或点击被拦截。
   - **完成/提交**：状态机正确（草稿→完成/提交），完成前校验仓库必填。
   - **反提交**：已完成单据可反提交回草稿并回退库存（仅验证测试环境的只读展示时，不实际点）。
   - **删除**：已完成单据禁止删除；草稿可删。
6. 重复核对其它单据类型：采购订单、销售订单、领料单、其他入库/出库、售后出库、调拨、盘点、调整、期初库存。

**判定**：单据状态与可执行操作严格匹配；状态机（草稿→完成→反提交→草稿）逻辑正确；已完成单据不可直删。

---

## 七、单据操作验证（仅测试环境、仅验证目标明确授权的操作）

按需验证以下操作，每个操作都：记录操作前状态 → 执行 → 记录操作后状态 → 反向验证（如可反提交/删除则回滚）：

1. **新增**：打开新增页 → 填表（必填项校验：仓库必填、库位在开启时必填）→ 保存 → 出现在列表。
2. **保存草稿**：不提交，仅保存 → 列表出现草稿。
3. **完成/提交**：草稿 → 完成，库存相应增减；校验仓库必填逻辑。
4. **反提交**：已完成 → 草稿，库存准确回退。
5. **删除**：仅草稿可删；已完成被拒。
6. **复制**：生成副本。
7. **导入/导出/打印**：见第五节、第六节。
8. **批量操作**：批量完成/打印/导出/删除。

> 强制提醒：以上任何「完成/提交/反提交/删除」若无法确认在隔离测试库执行，一律不点击，只验证「按钮存在 + 语义正确 + 无越权」。

---

## 八、报表验证

逐项打开并核对：能渲染、无 500、数据与列表/台账一致、有筛选与导出、空数据有合理空状态：

- 采购报表、采购订单执行统计表、供应商采购汇总表、物料采购汇总表、采购价格分析表、入库明细报表
- 销售报表、销售订单执行、销售价格分析、销售对账
- 库存报表、领料明细报表、出入库汇总报表、盘点报表、库存台账、仓库月报表、工单领料报表、委外加工报表
- 数据仪表盘、AI 工作台报表

**判定**：报表口径正确；金额/数量合计与明细一致；日期/仓库筛选生效；导出文件可下载。

---

## 九、安全与业务规则专项抽查

1. **仓库必填**（无论库位管理是否开启）：库存查询、出入库报表、库存台账的查询入口必须将仓库作为必填筛选项；不指定仓库不得返回数据。出入库新增/编辑表单仓库字段必须 `required`。
2. **库位管理开启时**：出入库单据仓库与库位均必填；报表仓库必填、库位可选。
3. **CSRF**：Web 表单（如登录、新增）无 CSRF token → 被拒绝；移动端 `/api/login` 不应被错误要求 CSRF（Bearer 认证）。
4. **权限**：viewer 无新增/编辑/删除；production 无采购/销售/用户管理；未登录访问受保护页 → 跳登录。
5. **移动端接口**：`/api/inbound`、`/api/outbound` 在关闭库位管理且无默认仓库时不传仓库 → HTTP 400，不创建单据、不改库存；开启库位管理时缺库位 → 400。
6. **AI 边界**：AI 相关入口只创建/检查草稿，不自动完成/提交/删除；带鉴权的高危操作需人工确认。

---

## 十、证据与报告格式

### 10.1 证据收集
每个验证点至少保留：
- 页面 URL 与截图（成功/失败各一张）。
- 关键 DOM 文本快照（如表格行、按钮、错误提示）。
- 控制台错误（记录 `console.error` / 网络失败 / JS 异常）。
- 操作前后状态对比（如库存、单据状态）。

### 10.2 报告模板
按以下结构输出：

```
## 验证范围与角色账号（脱敏）
## 一、全局/登录/工具栏        通过/失败 + 证据
## 二、菜单导航（按角色）       通过/失败 + 证据
## 三、工作台                   通过/失败 + 证据
## 四、工具栏                   通过/失败 + 证据
## 五、单据列表                 通过/失败 + 证据
## 六、单据明细                 通过/失败 + 证据
## 七、单据操作（仅授权项）     通过/失败 + 证据
## 八、报表                     通过/失败 + 证据
## 九、安全与业务规则           通过/失败 + 证据
## 十、问题清单
   - 严重度：P0 数据损坏/越权 / P1 功能错误 / P2 体验 / P3 轻微
   - 复现步骤、URL、实际 vs 预期、截图、建议修复
## 十一、安全边界核验
   - 是否改动过数据/密码/密钥
   - 是否在测试库执行高危操作
   - 是否保持仓库与库位概念分离
## 十二、结论
```

---

## 十一、禁止事项（硬约束）

- 不得触碰生产数据库、真实物料/客户/供应商/仓库数据。
- 不得经 UI 或接口执行真实业务写操作，除非明确授权且在隔离测试库。
- 不得输出密码、Token、Cookie、连接串、密钥完整值。
- 不得为「通过」而修改断言、放宽校验、跳过失败项。
- 不得把「库位」当成「仓库」；不得把移动端 API 的 Bearer 认证误判为 CSRF 缺失。
- 未获取授权的高危操作（完成/提交/反提交/删除/审核/作废）绝不允许替用户执行。