# WMS BUG 基线

更新时间：2026-07-31（实测三次验证）

用途：把已经核验过的问题固定下来，避免不同 AI 模型每天重复报告同一批“疑似 BUG”。后续扫描结果必须先对照本文件：已修复项看回归，误报项不重复报，暂缓项只在风险条件变化时重新评估。

## 判定规则

| 类型 | 进入条件 | 处理方式 |
|------|----------|----------|
| 真实 BUG | 有代码证据、可触发路径、错误结果 | 修复并加入回归验证 |
| 风险项 | 理论可触发，但依赖配置或边界条件 | 记录缓解措施，必要时加监控 |
| 配置项 | 生产环境需要强制配置或关闭调试 | 固化配置检查 |
| 误报 | 代码已有保护，或不符合实际业务流程 | 记录原因，不重复报 |
| 暂缓 | 技术限制或迁移成本较高 | 明确风险和后续条件 |

## 已修复并纳入回归

| BUG-2026-08-27-003 | 多仓库下库存台账查不到历史空 location 流水：历史流水 `StockTransaction.location` 为 NULL/空（2026-08-16 前不写仓库名），多仓库时无法按 location 归属到具体仓库，被台账仓库过滤排除——用户"多仓库、关库位管理、选了日期+仓库+物料仍查不到"。仓库自带 `backfill_empty_warehouse_documents` 又把空 location 一律归到**默认仓库**且只覆盖入库/领料两类，既不准确也不完整 | 已修复：`app/app.py` 新增 `_ledger_source_warehouse_getters`/`_ledger_source_warehouse_map`，多仓库按仓库查询时把空 location 流水一并取出，再按**来源单据的仓库**归属过滤（in/out/期初/调整/盘点/领料/销售/委外取单据 warehouse 字段，期初经 warehouse 关系，调拨按数量正负归 from/to）；归属所选仓库的保留，否则剔除、不串仓；location 非空流水不受影响。回归测试 `tests/test_bug_2026_08_27_003_ledger_source_warehouse.py`（4 passed：空location按来源归属可见 / 他仓不串仓 / 非空location不回归 / 调拨按方向归属） |
| BUG-2026-08-27-001 | [P2] 嵌入（Tab iframe）模式下单据详情页出现两个「删除」、两个「打印」：`insertGlobalActionBar()` 注入的全局工具栏（新增/保存/删除/设置/打印/导入/导出/模板/分享）与各单据详情页头部自带的状态感知按钮（草稿「删除」/已完成「反提交」/打印/发送打印）叠加重复；更严重的是已完成单据上页面只显示「反提交」，全局工具栏仍显示通用「删除」，点击必被后端拒绝（"只有待处理的单据可以删除"），造成"反提交/删除有问题"的错觉（涉及领料单/入库单/采购订单/销售订单/采购申请/售后出库/委外全部单据详情页） | **已修复（2026-08-27，commit 本次提交）**：`app/static/js/app.js` `insertGlobalActionBar()` 增加详情页去重——URL 以 `/数字` 结尾的详情页上，检测页面自带删除/反提交（onclick 含 delete/revert）与打印（onclick 含 printOrder( 或 href 以 /print 结尾）动作，页面已提供时跳过全局重复「删除」/「打印」按钮并清理多余分隔线；列表页/新增页不受影响，页面未提供对应动作的（如委外详情无打印、采购申请详情无删除）全局按钮保留，功能不缺失。已完成单不再显示必失败的「删除」，只剩「反提交」入口，与"已完成须先反提交再删草稿"规则一致。回归：`tests/test_bug_2026_08_27_001_detail_actionbar_dedupe.py` 5/5 PASSED（去重逻辑锚点/按钮过滤与分隔线清理/7 个详情模板自带动作齐全/侧边栏打印导航不误判/行级删除 class 委托不干扰）；node 抽取真实代码块 6 场景功能验证（草稿详情去重/已完成详情去重/列表页保留/委外仅去删除/采购申请仅去打印/行级按钮与导航不误判）6/6 PASSED；`node --check app.js` 语法通过。生效条件：前端静态文件，强制刷新（Ctrl+F5）即可 |
| BUG-2026-08-27-004 | [P1] 多仓库下仓库月报与库存汇总漏掉历史空 location 流水，与库存台账数字对不上：BUG-2026-08-27-003 修复台账空 location 流水按来源单据归属时，`_build_warehouse_monthly_report`（月报）与 `get_warehouse_stock_quantities` 多仓库+关库位分支未同步——仍仅 `location.in_(loc_names)` 把空 location 历史流水整体排除，同一批数据「台账有数、月报/库存查询无数」。这是同一仓库口径逻辑被复制成多份、修复漏改一处导致反复出 BUG 的直接案例（诊断详见 220 个 BUG 中 102 个为库存/仓库/台账类） | **已修复（2026-08-27，commit 本次提交）**：提炼共用口径助手 `_warehouse_scoped_txn_condition`（多仓库 IN 仓库名/编码/库位名并放行空 location；单仓库不过滤）与 `_filter_txn_list_by_warehouse_scope`（空 location 流水按来源单据仓库归属过滤），台账 `_collect_ledger_rows`、月报 `_build_warehouse_monthly_report`（月内流水+未来流水期末倒推）、库存汇总 `get_warehouse_stock_quantities`（多仓库+关库位分支）三处统一调用，今后改口径只改一处。附带修复月报期末口径漏掉库位名流水问题（与台账一致）。回归：`tests/test_bug_2026_08_27_004_monthly_report_source_warehouse.py` 6/6 PASSED（月报含空location归属流水/不串仓/仓库名不回归/**台账↔月报收入发出期末三项一致性断言**/库存汇总含归属流水/调拨按正负归属）；既有回归：台账 001/002/003 三件套 17/17、库存查询与仓库报表相关 31/31 全部 PASSED，无行为回退。 |
| BUG-2026-08-27-005 | [P1] stock_transaction 无 warehouse_id 列，仓库归属全靠 location 字符串（历史承载仓库名/编码、库位名、空值三类语义），仓库级报表只能靠字符串匹配重建归属且口径分散——这是 102/220 个库存/仓库/台账类 BUG 反复出现的结构性根因（BUG-004 只修了报表口径，未动数据模型） | **已修复（2026-08-27，commit 本次提交）**：B 治本。①模型加 `warehouse_id` 外键列+索引；②双通道迁移：SQLite 走 `auto_migrate_database` 补列、MySQL/PG 走 alembic `c1d2e3f4a5b6`（含外键与唯一匹配回填）；③启动幂等回填 `backfill_stock_txn_warehouse_id()`：location 唯一匹配仓库名/编码→该仓库，库位名唯一归属（LocationInventory.warehouse_id）→该仓库，空 location→按来源单据推断（复用台账归属逻辑，调拨按数量正负归 to/from），歧义保留 NULL 不猜；④写入端统一：`add_stock`/`deduct_stock_atomic`/`add_stock_transaction`/期初调整 4 处全部落 warehouse_id，调拨完成/反提交、物料初始库存 5 个调用方传仓库。今后仓库过滤可走外键精确匹配，不再猜字符串。回归：新增 11/11（列存在/4 写入端/回填 6 场景/幂等）；相关全量 81/81（含台账 001-004、调拨、期初、物料审计、库存查询、仓库报表）零回归。 |
| BUG-2026-08-27-006 | [P1] 仓库级报表读取路径仍以 location 字符串推理为主：B 已加 warehouse_id 外键并回填历史，但台账/月报/库存汇总的 SQL 过滤仍走 `location.in_(仓库名/编码/库位名)`+Python 归属——字符串口径与精确外键并存，新数据与回填数据没有被优先用外键过滤，切换不彻底则治本打折 | **已修复（2026-08-27，commit 本次提交）**：C 断根。①`_warehouse_scoped_txn_condition` 升级为三元组：多仓库 condition 改为 `warehouse_id == 所选仓库 OR (warehouse_id IS NULL AND location 兜底)`——外键精确命中优先、未回填行走字符串兜底，仓库解析失败整体降级 location 口径；②`_filter_txn_list_by_warehouse_scope` 加 warehouse_id 参数，外键命中（含空 location 新数据）直接保留；③台账/月报/库存汇总三处调用方全部传 wid；④`get_warehouse_stock_quantities` 多仓库关库位分支改 `warehouse_id` 精确聚合 + NULL 行走 location + 空 location 未回填行按来源单据归属；⑤`_material_stock_unattributed` 关库位分支改以 `warehouse_id IS NOT NULL` 判归属。回归：新增 7/7（新数据台账/月报/库存汇总精确命中与仓库隔离、未回填兜底不回归、**四方对账：台账期末==月报期末==库存查询==库位账实际库存**、_material_stock_unattributed 双向）；相关全量 90/90 零回归。 |
| BUG-2026-08-27-007 | [P1] C 读取路径切换引入两个边界漏洞（独立验证发现）：①`_filter_txn_list_by_warehouse_scope` 用 `t.warehouse_id == warehouse_id` 判断精确命中，降级场景（filters 传不存在的仓库名，loc_names 非空但 wid=None）下 Python `None == None` 为 True——全部 NULL 行（含其他仓库空 location 流水）绕过归属过滤被保留，报表串仓（A 版本无此问题，C 引入回归）；②`_material_stock_unattributed` 关库位分支改为仅 `warehouse_id IS NOT NULL` 判归属，未回填的非空 location 行（跨仓同名库位歧义保留 NULL 的行）被误判不可归属 → 回退全局 Material.stock 口径，削弱 BUG-2026-08-18-002 防 A 仓掩护 B 仓的仓库级严格校验 | **已修复（2026-08-27，commit 本次提交）**：①精确命中判断加 `warehouse_id is not None and` 前置守卫，降级场景不再误保留 NULL 行；②归属判定改为 `warehouse_id IS NOT NULL OR location 非空`——外键为精确标志、非空 location 为字符串线索，维持严格校验。回归：新增对抗性测试 tests/test_bug_2026_08_27_007 3/3（降级不串仓/字符串口径不回归/未回填行仍算归属），先红后绿验证；A/B/C 相关 40/40；全套件 930 通过（7 个失败为 barcode 库缺失与 APK 版本测试滞后，与本改动零交集，改动范围 git diff 确认）。 |
| BUG-2026-08-27-008 | [P1] 手机 APP 下载地址（/mobile/app）必然 404：APK 二进制从未提交进 git 仓库，`ANDROID_APK_PATHS` 两个候选路径（仓库根 app-release.apk、Android 构建输出 release 目录）在部署机上都不存在，CI 只构建 debug 上传 GitHub Actions artifact 不落部署机——用户点"下载扫码APP"（base.html 菜单 + mobile_connect 页）必见裸 404 页，即"下载地址不正确" | **已修复（2026-08-27，commit 本次提交）**：下载路由改三级兜底——①环境变量 `WMS_ANDROID_APK_URL`（管理员配置内网文件服务器/GitHub Releases 地址）→ 302 跳转；②本地 APK 文件 → 直接发送（原逻辑）；③均无 → 渲染友好说明页（200，新增 `mobile_app_download_fallback.html`），含网页版扫码入口（/mobile/scan/query，手机浏览器免安装直接用）与管理员三种部署方式指引，不再裸 404。回归：新增 tests/test_bug_2026_08_27_008 4/4（说明页/配置302/本地文件发送/配置优先），既有 verify_app_py_split_mobile 7/7。 |
| BUG-2026-08-27-002 | 库存台账（及仓库月报）在单仓库 + 关库位管理下查不到历史流水：2026-08-16 之前 `add_stock`/`deduct_stock_atomic` 不写仓库名，历史流水 `StockTransaction.location` 为 NULL；而台账按 `location` 匹配仓库名/编码过滤（报表仓库必填后加的），升级后这些老流水被排除——用户"以前能查、现在查不到"，且出现"库存查询有数（单仓库 `get_warehouse_stock_quantities` 有全局兜底）、库存台账却查不到"的口径不一致 | 已修复：`app/app.py` `_collect_ledger_rows` 与 `_build_warehouse_monthly_report` 在 `Warehouse.query.count()==1`（单仓库）时不按 `location` 过滤——单仓库所有流水必属该仓库，与 `get_warehouse_stock_quantities` 单仓库全局口径一致；多仓库仍按 `location` 精确过滤（仓库名/编码/库位，配合 BUG-2026-08-27-001），无法归属的 NULL 流水保持隐藏防串仓。回归测试 `tests/test_bug_2026_08_27_002_ledger_single_warehouse_null_location.py`（4 passed：单仓库 NULL 流水可见 / 单仓库仓库名编号流水不回归 / 多仓库 NULL 流水仍隐藏 / 月报同根因）；关联 BUG-2026-08-27-001 测试 3 passed 不回归 |
| BUG-2026-08-27-001 | 库存台账（及仓库月报）按仓库查询查不到调拨/委外等流水：`StockTransaction.location` 字段混用「仓库名/编码」与「库位」，而库存台账是唯一依赖该字段按仓库过滤的报表，旧逻辑只匹配仓库名/编码，导致开启库位管理时库位型流水（transfer/subcontract/mobile 走 `add_stock_transaction` 直接写库位）按仓库查不到，用户看到“库存台账查不到东西” | 已修复：`app/app.py` 新增 `_warehouse_location_filter_values(warehouse_id, name, code)`，把仓库名、仓库编码、以及该仓库名下所有库位名一并纳入 `location` 过滤；`_collect_ledger_rows` 与 `_build_warehouse_monthly_report` 共用此逻辑（同根因，AGENTS.md 仓库≠库位，但台账需按仓库聚合全部变动）。关闭库位管理时 `LocationInventory` 无行，仅匹配仓库名/编码，行为不变；回归测试 `tests/test_bug_2026_08_27_001_ledger_location_filter.py`（3 passed：开启库位管理调拨流水按仓聚合且不串仓 / 关闭时行为不变 / 月报同根因） |
| BUG-2026-08-26-001 | Excel 打印模板编辑器顶部「占位符速插」区 22 个字段全部直接显示纯英文模板变量（`{order.order_no}`、`{item.material.code}` 等），面板标题仅"占位符速插："无用法说明，仓库一线人员看不懂含义、不知道该插入什么字段 | 已修复（2026-08-26）：`print_template_editor.html` 的 `PLACEHOLDERS` 改为 `{ label, value }` 结构——chip 显示中文标签（单号/日期/供应商/物料编码/数量/金额…22 项），悬停提示显示英文变量+用法，点击仍插入英文 value（模板引擎取值键绝对不动，改动量仅前端显示层）；面板标题补中文使用说明（点击字段插入到当前单元格，打印时自动替换为实际内容）。回归测试 `tests/test_bug_2026_08_26_001_placeholder_chips_chinese.py`（4 passed：每条目 label 必含中文 / value 保持英文模板键格式不得中文化 / chip 显示取 label 插入取 value 防改回 / 面板标题含用法说明） |
| BUG-2026-08-25-002 | 单据/报表/列表点击记录项在本页跳转、原列表上下文丢失：库存台账点领料单、入库明细点单号等都在当前标签页打开详情，返回后筛选/页码状态易丢。f28b921 只修了 report_view.html 通用报表，其余 36 个模板的记录级链接仍为本页跳转 | 已修复（2026-08-25）：36 个模板 77+ 处记录级链接（单据编号、行内 查看/编辑 按钮、详情页 来源单 交叉引用、销售看板/报表订单号、AI 工作台 jump_url）统一加 `target="_blank" rel="noopener"`；显式排除菜单/分页/新增/导出/打印/返回等页面内导航与详情页同记录"编辑"切换按钮；回归测试 `tests/test_bug_2026_08_25_002_doc_links_new_tab.py`（3 passed：全模板扫描无遗漏 / 17 个关键列表页点名验证 / 排除项防误改），浏览器实测点击入库单号新标签页打开详情、原列表页保留 |
| BUG-2026-08-25-001 | 其他入库单「先开单、后建客户」选不到新客户：页面级往来单位数组只在开单时渲染一次，跨页新建客户后下拉里没有新客户。根因双重：①`in_order_add.html` 主数据广播监听只处理 `supplier_updated`，客户页新建客户广播的是 `customer_updated`，入库单页（客户模式）收到后不刷新；②`refreshSuppliers()` 固定请求 `/api/suppliers`（供应商接口），客户模式下一旦刷新会把客户列表整体覆盖成供应商数据 | 已修复（2026-08-25）：`in_order_add.html` 广播监听按 `partyField` 分流（客户模式响应 `customer_updated`、供应商模式响应 `supplier_updated`），`refreshSuppliers()` 按 `partyField` 选择 `/api/customers` 或 `/api/suppliers`、提示语改用 `partyLabel`；回归测试 `tests/test_bug_2026_08_25_001_other_in_customer_refresh.py`（3 passed：客户模式含 customer_updated 监听 / refreshSuppliers 含 /api/customers 分流 / 采购入库供应商模式不回退）；浏览器端到端实测：A 标签页开单、B 标签页建客户并广播，不刷新页面即可搜到并选中新客户（hidden customer_id 正确写入） |
| BUG-2026-08-24-007 | [P0] 手机端（Android App）一打开「每日报表」整 App 崩溃（系统弹「WMS扫码 屡次停止运行」非接口报错）。根因：`ReportModels.DailyReportItem.contractNo`（commit `5cca484` 新增）声明为非空 `String = ""`，但该类多数构造参数无默认值 → Kotlin 不生成无参构造器 → Gson 走 `Unsafe.allocateInstance` 实例化（绕过构造器，`= ""` 默认值不生效）；当服务端响应缺 `contract_no` 字段（手机连的还是未部署 `5cca484`/`554eafb` 的旧版后端）或字段显式 null（`Material.spec` 列 nullable）时，非空字段运行时被置 null 破坏 Kotlin 非空契约，`ReportScreens` 渲染明细行裸调 `item.contractNo.isNotBlank()` 抛 NPE，异常发生在 Compose 主线程渲染路径未捕获 → 整 App 崩溃。网络请求本身是成功的（旧后端正常返回数据），所以表现为「加载完一渲染就崩」而非报错 toast。扫码出库页不崩是因为 `ContractDto` 全字段可空且 UI 用 `orEmpty()`/`isNullOrBlank()` | 已修复（2026-08-24）：①`ReportModels.DailyReportItem` 的 `spec`/`contractNo` 改可空 `String?`（对齐 `supplier`/`department` 既有模式，Gson 缺字段/显式 null 均不再破坏契约）；②`ReportScreens.DailyReportItemRow` 改 `!item.spec.isNullOrBlank()`/`!item.contractNo.isNullOrBlank()`；③后端 `daily_detail` 的 `spec` 输出加 `or ''` 兜底（`material.spec` 列可空，禁止下发显式 null）。回归：`tests/verify_bug_2026_08_24_007_report_contract_npe.py` T1-T6 6/6 PASSED（两字段可空声明 / 两处判空写法 / 后端 spec 兜底 / 出库页 ContractDto 用法 null 安全无同类 NPE）+ `tests/test_mobile_report_daily_detail.py` 12/12 PASSED（新增 spec=None 物料输出空字符串用例）+ 存量 `verify_daily_report_contract_ui.py` 5/5 PASSED。**部署边界**：手机端需重新构建安装新 APK；同时服务器必须拉取最新 main 重启（仅代码更新，`contract_no` 等列早已存在无需 DB 迁移），否则报表不崩但合同编号列不显示（旧后端不下发该字段） |
| BUG-2026-08-24-006 | [P1] 手机端（Android App）首次打开「每日报表」报「网络错误: 服务器地址未配置，请先登录并填写服务器地址」且页面停留空态：`AppNavGraph` 组合阶段（App 启动时）即创建 `ReportViewModel` 并在 init 立即 `load()`，而 `AuthViewModel` 异步还原 `RetrofitClient` 的 baseUrl/token 依赖 DataStore + EncryptedSharedPreferences 首轮读取（数百毫秒），两者竞态——报表请求先于会话还原时 `apiService` 的 `check(baseUrl.isNotBlank())` 抛 IllegalStateException，被 `getDailyReport` 外层 catch 包装成上述报错；错误态滞留 `uiState.error`，等用户首次进入报表页时弹出，且 init 加载失败后 `report==null` 页面停留「当日暂无明细」空态不重试 | 已修复（2026-08-24）：`WmsRepository` 新增 `ensureSession()`——请求前若内存 baseUrl 为空则从持久化存储同步还原 token（先注入，避免首请求 401 误触发强制登出）与 baseUrl，已还原时零开销 no-op；`getDailyReport`/`getDashboard`（HomeViewModel.init 同款竞态）请求前调用；`ReportViewModel.init` 不再自动加载（启动时可能尚未登录，提前加载只会留下过期错误态），加载改由 `DailyReportScreen` 进入时 `LaunchedEffect(Unit)` 触发，每次进入按当前日期/类型刷新。回归 `tests/verify_bug_2026_08_24_006_daily_report_session_race.py` T1-T5 5/5 PASSED（ensureSession 先 token 后 baseUrl / 两处请求前调用 / init 旧自动加载清零 / 页面进入触发加载）。验证：pre-commit lint A1-A10 0 违规；本地 `8e0fcf2` → 常规 HTTPS 推送远程 `8e0fcf2`（本沙箱无 Android SDK，assembleDebug 需构建机执行） |
| BUG-2026-08-24-005 | [P1] 仓库本地电脑跑独立打印代理（wms_print_agent.py）时，本机枚举失败（Print Spooler 停止/WMI 异常）经 `/print_queue/api/v1/heartbeat` 上报空 `printers` 列表，服务端 `sync_workstation_printers(ws, [])` 把已有打印机全部误标 offline、打印路由瘫痪（云端部署 + 本地打印机形态的真实风险） | 已修复（2026-08-24）：服务端 `AgentHeartbeatRequest.printers` 改 Optional——`None`（未上报/枚举失败）只保活工作站、跳过打印机同步，空列表 `[]` 仍按「真无打印机」置 offline；独立代理 `enumerate_printers` 三态 list/[]/None（新增 `_run_powershell_status` 区分「命令失败/成功但无打印机」，` _run_powershell` 对外行为不变、与内置代理同步），`send_heartbeat` 枚举失败改上报 `printers=None` 并本地告警节流（失败/恢复各一条），`--list-printers` 枚举失败给排查指引并返回非 0。回归 `tests/test_print_agent_api.py`（null 保活/空列表置 offline）+ `tests/test_print_agent_script.py`（三态枚举 + 心跳 null/节流/恢复）。验证：lint A1-A10 0 违规 + verify_wms_bugs + pytest 781 passed；本地 `4eecbd5` → 受限网络 Git Data API 重放远程 `054ca49`。部署提示：云端无打印机时设 `WMS_LOCAL_PRINT_AGENT=0` 关内置代理，仓库本地电脑跑独立代理并在该机启动 Print Spooler |
| BUG-2026-08-24-004 | [P1] 内置打印代理在 Print Spooler 服务停止/WMI 异常时每 60s 刷 3 条 PowerShell stderr，且把本机打印机全部误标 offline：`enumerate_local_printers` 枚举失败（三条 Get-CimInstance/Get-WmiObject Win32_Printer 命令全部退出码非 0）返回 `[]`，与「本机真无打印机」不可区分，`_heartbeat` 据此调用 `sync_workstation_printers(ws, [])` 把已有打印机全部置 offline、打印路由瘫痪；且每条失败命令都 WARNING，每 60 秒一轮刷屏 | 已修复（2026-08-24）：app/local_print_agent.py 新增 `_run_powershell_status` 返回 `(ok, stdout)` 以区分「命令失败」与「成功但无打印机」（`_run_powershell` 对外行为不变、与独立代理同步）；`enumerate_local_printers` 改三态返回 list/[]（真无打印机）/None（枚举失败）；`_heartbeat` 收到 None 时只保活工作站、不调用 sync（不误标 offline），并按状态翻转节流告警（进入失败一条 WARNING、恢复一条 INFO）。回归 `tests/test_local_print_agent.py`（18 passed：三态枚举 + 心跳误标/连续失败节流/恢复重同步）。验证：lint A1-A10 0 违规 + verify_wms_bugs + pytest 776 passed；本地 `d843a6e` → 受限网络 Git Data API 重放远程 `0c21432`。遗留（后续单独处理）：独立代理 `tools/print_agent/wms_print_agent.py` 经 HTTP 心跳的同类「空上报误标 offline」需服务端心跳契约配合 |
| BUG-2026-08-24-003 | [P1] 打印心跳写库间歇 500「database is locked」：connect 期 PRAGMA（WAL/synchronous/busy_timeout=30000/foreign_keys）注册放在 `initialize_database()` 的 `startup_db_upgrade_disabled()` 早退之后，生产 `start_wms_offline.bat`/`start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1` 时 `initialize_database()` 提前 return，监听器从不注册，ORM 连接退回 sqlite3 默认 5s busy_timeout，心跳等高频写与其他写事务相撞即抛 `database is locked`（2026-08-24 15:31 日志实录心跳 500） | 已修复（2026-08-24）：app.py 把 PRAGMA 注册拆为 `_register_sqlite_pragma_listener()`（幂等），在早退之前无条件调用——连接行为与 schema 迁移解耦，`WMS_NO_DB_TOUCH=1` 跳过迁移时连接仍拿到 `busy_timeout=30000`；回归 `tests/test_bug_2026_08_24_003_sqlite_pragma_gating.py`（2 passed：早退路径仍注册 / connect 处理器 busy_timeout=30000、foreign_keys=ON 生效且幂等）。验证：lint A1-A10 0 违规 + verify_wms_bugs 86 项（CONF-004 PASS）+ pytest 776 passed；本地 `fd09ecd` → 受限网络 Git Data API 重放远程 `366fa25` |
| BUG-2026-08-24-002 | [P2] 打印 ptoken 未绑定具体单据 ID：`generate_print_token` 签发的 payload 仅含 `{jid, ws}`，`print_token_or_login_required` 只验 token 有效性与 10 分钟时效，不校验 URL 路径中的单据 `id` 与 `jid` 的对应关系——一个泄露的有效 ptoken 可在 10 分钟内对 `/in_order/<任意id>/print`、`/out_order/<任意id>/print`、`/label/batch_print?ids=任意`、`/material_archive/<任意id>/print` 4 条打印路由重放，免登录批量读取供应商/价格/数量/客户等商业数据（只读，无法写） | 已修复（2026-08-24，AUDIT-2026-08-24 P2-001，commit `367474d`）：`print_token_or_login_required` 新增 `job_type` 参数，ptoken 路径强制校验载荷 jid 对应 PrintJob 的 job_type 与目标资源 ID 同路由一致（in_order/out_order/material_archive 按 `target_id` 对路径 id、batch_print 按 `target_ids` 对查询 ids 集合，不匹配即走未授权；Web 会话路径不变）；4 条路由分别声明 in_order/out_order/material_archive/material_archive_batch 绑定。回归：新增 `tests/test_bug_2026_08_24_002_ptoken_resource_binding.py`（8 passed：错类型/错目标/无 Job 拒访 302、绑定正确放行 200、会话路径不受影响）；修正 `test_print_agent_api.py` 中固化旧漏洞行为的契约用例。验证：`make check`（lint A1-A10 0 违规 + verify_wms_bugs 86 项 + pytest 773 passed）；`run_smoke_in_ci.py` 全新库 121/121 通过 |
| BUG-2026-08-24-001 | [P1] app/requirements.txt 钉死的 9 个包含已知 CVE（pip-audit 计 51 个漏洞、OSV 计 83 条记录）：Pillow 12.2.0、Werkzeug 3.0.1、cryptography 42.0.4、urllib3 2.0.7、Jinja2 3.1.2、requests 2.31.0、Flask 3.0.3、PyMySQL 1.1.0、click 8.1.7；CI pip-audit 步骤为 advisory（`continue-on-error: true`）未收紧 | 已修复（2026-08-24，AUDIT-2026-08-24 P1-001）：按栈分 3 个原子提交升级至 OSV 清零版本——Web 栈（commit `2e2b4d3`：Flask 3.1.3 / Werkzeug 3.1.6 / Jinja2 3.1.6 / itsdangerous 2.2.0 / click 8.3.3）、加密与 HTTP 栈（commit `d76dd74`：cryptography 50.0.0 / requests 2.33.0 / urllib3 2.7.0 / PyMySQL 1.1.1）、图像栈（commit `2b5d391`：Pillow 12.3.0）；CI pip-audit 门禁由 advisory 收紧为阻塞（commit `53963dc`，并约定未来无修复版漏洞须显式 `--ignore-vuln` 注明理由）。验证：每组升级后 `make check`（lint A1-A10 0 违规 + verify_wms_bugs 86 项 + pytest 765 passed）×3；`run_smoke_in_ci.py` 全新库 121/121 通过 ×3；OSV 逐包复核 10 个变更钉包 0 漏洞；venv 全量依赖闭包（84 包含传递依赖）`pip-audit --vulnerability-service osv` 报 No known vulnerabilities found；升级前基线实测 Found 83 known vulnerabilities in 9 packages（与审计报告 51 漏洞/9 包同一事实，OSV 服务对 GHSA/PYSEC 双计） |
| BUG-2026-08-22-002 | 内置「系统默认入库单模板/领料单模板」下载不了：两个内置模板只有 html_template_content、excel_template_path 为空，模板管理页下载/在线编辑按钮被 `{% if excel_template_path %}` 隐藏、「下载当前默认打印模板」入口不出现、Excel 填充打印只能回退内置版式 | 已修复（2026-08-22）：app.py 新增 `_ensure_builtin_excel_template_file()`，把 static/templates 示例 xlsx 复制为 uploads/print_templates 内置副本（在线编辑改写副本不动原件），启动同步（`_ensure_default_print_templates_unconditional` 与 ORM `ensure_default_print_templates` 双路径）在内置模板缺 Excel 文件时幂等回填 excel_template_path，用户已自定义路径不覆盖；回归 `tests/test_builtin_print_template_excel.py`（3 passed：回填且文件可填充 / 幂等且不覆盖用户文件 / 下载路由 200 返回 xlsx） |
| BUG-2026-08-22-001 | 「Excel打印模板中心」（/print_templates）打开即报「服务器内部错误，请稍后重试」。根因：`ExcelPrintTemplate` 模型（表 `excel_print_template`，commit 5db1a091 新增）只挂在 `db.create_all()` 里，而 `start_wms_offline.bat`/`start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1` 会跳过 `initialize_database()`/`auto_migrate_database()`，强制运行的 `fix_db_columns.py` 又不含该表，导致功能上线前创建的存量库重启也建不出表，查询抛 `no such table: excel_print_template` → 500 | 已修复（2026-08-22）：app.py 新增 `ensure_excel_print_template_table()`，仿照 `ensure_print_job_columns` 用独立 sqlite 连接、独立于迁移开关无条件执行、幂等建表（`CREATE TABLE/INDEX IF NOT EXISTS excel_print_template`，列定义与模型一致，含 target_type/target_code 两索引；库文件不存在时交给 create_all；表已存在时结构与数据一律不动），并在启动接线处（`ensure_print_job_columns()` 之后）无条件调用；回归测试 `tests/test_ensure_excel_print_template_table.py`（4 passed：缺表补建且列/索引齐全 / 幂等且数据保留 / 已有表结构与数据不动 / 子进程 WMS_NO_DB_TOUCH=1 启动接线建表）；复现脚本模拟「存量老库 + WMS_NO_DB_TOUCH=1」验证修复前 500、修复后 200 |
| BUG-2026-08-21-003 | 采购入库明细表（/in_order?type=purchase_in）同时输入「供应商 + 合同编号」就报「内部服务器错误」。根因：`_apply_header_or_item_contract_filters` 用相关子查询 `EXISTS` 判断「表单头或任一明细匹配合同编号/工程名称」，而入库单列表外层查询已 `outerjoin` 明细表（`db.session.query(InOrder, InOrderItem).outerjoin(...)`），SQLAlchemy 自动相关把子查询里的明细表也关联到外层 FROM，导致子查询无 FROM 子句，编译抛 `Select statement ... returned no FROM clauses due to auto-correlation` → 500。领料明细表（out_order）、销售订单（sales）、采购订单（purchase_order）、入库导出（export）同类调用同样隐患 | 已修复：`_apply_header_or_item_contract_filters` 两个 `EXISTS` 子查询显式 `.correlate(header_model)`，只关联表头表、明细表保留为子查询自身 FROM；回归测试 `tests/test_bug_2026_08_21_003_in_order_list_contract_filter.py`（4 passed：供应商+合同 200 / 供应商+不匹配合同空结果 / 仅合同 200 / helper 编译过滤） |
| BUG-2026-08-21-002 | 打印模板修改未生效：「系统默认入库单模板」「系统默认领料单模板」`template_type` 一直停在 `html`、`html_template_content` 仍是旧样式（含 RECEIPT 副标题、序号/备注列、合计行、四格签名行），改的 Excel 默认模板线上永不生效。根因：`ensure_default_print_templates()` 只挂在 `initialize_database()` 里，而 `start_wms_offline.bat`/`start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1` 会跳过 `initialize_database()`，模板同步从不执行 | 已修复：app.py 新增 `_ensure_default_print_templates_unconditional()`，仿照 `backfill_empty_warehouse_documents` 用独立 sqlite 连接、独立于迁移开关无条件幂等同步（`template_type`→`excel`、`html_template_content`→新常量，并保证各自表内 `is_default=1`），启动接线处无条件调用；回归测试 `tests/test_print_template_sync.py`（4 passed：入库单同步 / 领料单同步 / 幂等 / 无表跳过） |
| BUG-2026-08-21-001 | 上传打印模板无法保存：`create_print_template` 调用 `save_print_template_file(excel_file, prefix)` 只传 2 参数，而函数签名 `save_print_template_file(file_storage, prefix, static_folder)` 需 3 参数（缺 `static_folder`），上传 Excel 模板即抛 `TypeError: missing 1 required positional argument: 'static_folder'`，模板既无法写盘也无法落库 | 已修复：调用处补传 `app.static_folder`（`save_print_template_file(excel_file, prefix, app.static_folder)`）；回归测试 `tests/test_print_template_upload.py`（3 passed：Excel 上传落库且文件写盘 / 缺文件 400 明确报错 / HTML 模板成功） |
| BUG-2026-08-20-012 | H5 扫码页停止扫码后摄像头未真正关闭：`stopBarcodeScanner()` 只调 ZXing `reset()` 停止解码，未停掉 `<video>` 上的媒体轨道，导致摄像头指示灯常亮、扫描框关闭后仍录制，浪费电量并有隐私风险 | 已修复：`stopBarcodeScanner()` 在 `reset()` 后遍历 `barcodeVideo.srcObject.getTracks()` 逐个 `track.stop()` 并清空 `srcObject`；回归验证由现有扫码流程测试与手动验收覆盖 |
| BUG-2026-08-20-011 | 批量草稿（/mobile/api/scan_batch_draft）库位/仓库名不一致：`scan_submit` 已做「未开库位管理时仓库编号→仓库名」规范化，但批量草稿未做；手机端把仓库编号当作 location 传入时，草稿 location 存的是编号而非仓库名，与流水/后续确认口径不一致 | 已修复：`scan_batch_draft` 位置字段复用与 `scan_submit` 相同规范化（location 为空回退仓库名；未开库位管理且 location==仓库 code 时替换为仓库名）；回归测试 `tests/test_mobile_scan_draft_flow.py::TestBatchDraftLocationNameConsistency` |
| BUG-2026-08-20-010 | H5 扫码页仓库字段在未开启库位管理时整块隐藏，导致入库/出库/盘点无法选择仓库，只能依赖后端默认仓兜底；且「待确认」页固定只查默认仓，其它仓的草稿看不到 | 已修复：[mobile_scan.html](app/templates/mobile_scan.html) 把「仓库/库位」拆成两个字段——「仓库」仅在非 in/out 模式隐藏（始终必选，AGENTS 仓库必填），「库位」仅开启库位管理时显示；扫码提交 payload 拆分传 `warehouse` 与 `location`；「待确认」页新增仓库下拉，切换仓库即重载，按所选仓库查询待确认草稿；回归测试 `tests/test_mobile_scan_draft_flow.py::TestConfirmWarehouseIsolation::test_pending_list_filtered_by_warehouse` |
| BUG-2026-08-20-009 | H5 扫码页「连续批量扫码」在盘点(check)等非出入库模式下仍显示，提交后端 `/mobile/api/scan_batch_draft` 时 `_norm_mode` 把非 `in/out` 的 mode 静默归一为 `in`，导致盘点扫码批量提交被误当成「入库」生成待确认草稿 | 已修复：后端 `BatchDraftRequest._norm_mode` 改为校验失败（非 `in/out` 直接 400，不再静默转 in）；前端仅在 in/out 模式渲染批量开关；回归测试 `tests/test_mobile_scan_draft_flow.py::test_batch_draft_rejects_check_mode` |
| BUG-2026-08-20-001 | 线上 print_job 缺 `printing_started_at` 等迁移列，打印代理 claim 永久报 `no such column`：`start_wms_offline.bat` / `start_wms_auto.bat` 默认设置 `WMS_NO_DB_TOUCH=1` 跳过 `auto_migrate_database()`，而强制运行的 `fix_db_columns.py` 又不含 print_job 补列，叠加导致重启也补不上 | 已修复：app.py 新增 `ensure_print_job_columns()`，仿照 `backfill_empty_warehouse_documents` 用独立 sqlite 连接、独立于迁移开关无条件执行、幂等补齐 print_job 的 `workstation_id`/`printer_id`/`route_rule_id`/`source_event`/`printing_started_at` 缺列（与 `auto_migrate_database` ALTER 一致），并在启动接线处（`backfill_empty_warehouse_documents()` 之后）无条件调用；回归测试 `tests/test_ensure_print_job_columns.py`（4 passed：缺列补齐 / 幂等 / 无表跳过 / 子进程模拟 `WMS_NO_DB_TOUCH=1` 启动接线补列） |
| BUG-2026-08-20-002 | 打印代理部署包 run.bat/start.bat 启动即报 3 次「系统找不到文件 'dir」：生成批处理的 Python 里，3 条「常见安装目录搜 Python」的 `for /f` 行误用 `r'...'` 原始字符串，`\'` 不会被折叠成 `'`，写出到 bat 后变成 `in (\'dir ... \')`，cmd 无法识别 `for /f` 的单引号命令形式、把 `'dir` 当文件执行（`where` 行是普通字符串、`\'` 正常折叠成 `'`，故未报错）；连累 3 条 dir 目录回退全部失效，只能靠 `py.exe` 启动 | 已修复：`print_routing.py` 生成 py_detect 的 3 条 `dir` 行从 raw 字符串改为普通字符串（`\'`→`'`、`\\`→`\`），写出即 `in ('dir /b /s "%LocalAppData%\\...\\python.exe" 2^>nul')` 正确形式，cmd 正常把 dir 当内部命令执行；Python 侧验证：`py_compile` 通过、3 条 dir 行均无 `(\'dir` 残留。用户需重新下载代理部署包并换成新 run.bat 生效 |
| BUG-2026-08-19-001 | 字段设置（栏目设置）弹窗字段多时列表溢出弹窗、无滚动条，底部按钮遮挡行且看不全 | 已修复：`custom.css` 字段设置内容区/面板改 `grid-template-rows:minmax(0,1fr)` + 表格容器 `overflow:auto;min-height:0`（去掉 `min-height:260px` 撑破约束），`app.js` 明细面板移除 `display:contents` 内联样式使行约束生效；`scripts/verify_wms_bugs.py` 新增 `check_field_settings_scroll` 回归（BUG-2026-08-19-001） |
| BUG-2026-08-19-002 | 采购入库明细表拖动列宽部分列拖不动：`setupResizableTable` 的 colgroup 按初始列序静态创建，字段设置重排/隐藏列后 `<col>` 索引错位（fixed 布局下 col 宽度优先于 th），拖动某列实际改到别的列或无效果 | 已修复：`app.js applyWidths` 改为按当前可见表头顺序（data-column-key）重建 colgroup 并跳过 `wms-field-column-hidden` 列；新增 thead MutationObserver 在重排/隐藏后自动重建；`scripts/verify_wms_bugs.py` 新增 `check_col_resize_sync` 回归（BUG-2026-08-19-002） |
| BUG-2026-08-19-003 | 打印工作台路由（/print_routing）老库缺 print 系列表（print_workstation/print_device/print_route_rule/print_job）时首条查询抛 `OperationalError: no such table`，`require_role` 路由直接 500，页面提示"服务器内部错误，请稍后重试" | 已修复：`print_routing_page` 按代码库既有模式（缺失表交给 `db.create_all()` 处理）捕获 `OperationalError` 时惰性 `db.create_all()` 补齐缺表后重试，页面正常渲染；回归测试 `tests/test_print_routing_admin.py::test_print_routing_page_selfheals_missing_tables`（删表后 GET 返回 200 且 print 表被重建） |
| BUG-2026-08-19-004 | 打印工作台"新增打印机"等按钮点击无反应：模板把 `{{ ws.code \| tojson }}` 嵌进双引号 onclick 属性，tojson 输出的双引号把属性截断成 `onclick="openAddPrinter(1, "1")"`，点击即 JS 语法错误；同类受影响：新增/编辑/删除打印机、删除工作站、删除路由规则共 5 处 | 已修复：含 `tojson` 的 onclick 属性定界符统一改单引号（tojson 已把内容中的单引号转义为 `\u0027`，安全），渲染后 `onclick='openAddPrinter(1, "1")'`；渲染级验证 19 个 onclick 全部为合法 JS；`scripts/verify_wms_bugs.py` 新增 `check_onclick_tojson_quotes` 全模板回归（BUG-2026-08-19-004） |
| BUG-2026-08-19-006 | 打印代理在 Win7（PowerShell 2.0）上枚举打印机永远为空：`enumerate_printers` 只用 `Get-CimInstance + ConvertTo-Json`（均为 PS 3.0+ 命令），PS 2.0 下命令不存在退出码非 0，`_run_powershell` 静默吞掉 stderr 返回空串 → 心跳上报空打印机列表 → 管理页打印机全部"离线"，`--list-printers` 输出空无任何提示 | 已修复：`enumerate_printers` 改三级回退链 CimInstance+Json → WmiObject+Json → WmiObject+ConvertTo-Csv（PS 2.0 自带），新增 `_parse_printer_output`（JSON/CSV 双格式）与 `_to_bool/_to_int` 字符串转换；`_run_powershell` 失败时记 stderr 日志不再静默；`--list-printers` 空结果时输出三步排查指引；回归测试 `tests/test_print_agent_script.py`（单对象/多对象/两级回退/CSV 解析） |
| BUG-2026-08-19-007 | 打印代理定向打印后原默认打印机永不恢复：`get_default_printer` 仅用 `Get-CimInstance`（PS 3.0+，Win7 PS 2.0 返回空串→`process_job` 误判读取失败）；`set_default_printer` 用 `bool(stdout)` 判成功，但 `SetDefaultPrinter` 成功时无任何输出恒为 False；打印机名含单引号未转义会截断/注入命令 | 已修复：`get_default_printer` 回退 `Get-WmiObject`（PS 2.0 可用）；`set_default_printer` 命令末尾按 `$?` 显式回写 OK 成功标记；新增 `_ps_quote` 单引号翻倍转义；回归测试 `tests/test_print_agent_script.py::TestDefaultPrinter`（WMI 回退/成功标记/单引号转义 3 项） |
| BUG-2026-08-19-008 | 代理带 `autoprint=1` 打开任何打印页都是 500：Jinja 模板作用域没有 Python 内建名 `int`（只有 `\|int` 过滤器），原 `type=int` 渲染即抛 `UndefinedError` | 已修复：`_autoprint_script.html` 改用 `\|int` 过滤器并做 1-99 边界钳制；回归测试 `tests/test_print_queue.py`（autoprint 页面渲染 200 且含份数脚本） |
| BUG-2026-08-19-009 | autoprint 打印完成后页面不自关，代理等浏览器进程退出才算完成→等满 `print_timeout`（默认 120s）强杀进程并把已出纸任务上报为 failed | 已修复：autoprint 脚本打完全部份数后 `setTimeout(window.close, 800)`（--app 模式生效；人工打开的标签页浏览器忽略自关不受影响）；回归测试 `tests/test_print_queue.py` |
| BUG-2026-08-19-010 | 僵尸任务回收两缺陷：v1 claim 无任何回收（代理崩溃后其任务永久卡 printing）；legacy next 按 `created_at` 判超时→队列积压 >5min 的 pending 任务一旦认领置 printing，下次轮询立即按 created_at<回收线 重置回 pending，无限循环永远打不出 | 已修复：`PrintJob` 新增 `printing_started_at` 字段（含启动迁移），统一 `_recover_zombie_printing_jobs` 按认领时间判定（存量 NULL 退回 created_at 近似，未达 MAX_ATTEMPTS 重置 pending、达到置 failed），三条认领路径（next/工作站 next/v1 claim）均记录认领时间且认领前先回收本工作站僵尸；回归测试 `tests/test_print_agent_api.py`（认领回收/次数耗尽/跨站不回收）+ `tests/test_print_queue.py`（积压任务认领后不被误回收） |
| BUG-2026-08-19-011 | 编辑打印机不能改 `system_name`，手填错系统名的打印机只能删除重建（且被路由规则引用时删除也被拒绝） | 已修复：`PrinterEditRequest` 新增 `system_name`（留空=保持不变，同工作站重名 400，>200 字符 422），前端编辑弹窗新增系统名称字段复用 `knownPrinterList` datalist 下拉；回归测试 `tests/test_print_routing_admin.py::test_printer_edit_system_name`（修正/重名 400/留空保持 3 场景） |
| BUG-2026-08-19-012 | 打印代理部署包内 run.bat/start.bat 裸调 `python`/`pythonw`：Python 未加入 PATH 的电脑（如 Win7 手动装 3.8 没勾 Add Python to PATH）双击 bat 即报「'python' 不是内部或外部命令，也不是可运行的程序或批处理文件」，代理无法启动 | 已修复：bat 改为自动定位 Python——PATH 上的 python.exe（`findstr /v WindowsApps` 排除 Win10 商店假 python）→ `%LocalAppData%\Programs\Python\Python3*` / `%ProgramFiles%\Python3*` / `C:\Python3*` 常见安装目录 → `py.exe` 启动器兜底；找不到时输出中文安装指引并退出；start.bat 优先用同目录 pythonw.exe 后台运行、取不到回退 python；bat 以 GBK 编码写入（zh-CN 默认 cp936 下中文可读）、CRLF 行尾；回归测试 `tests/test_print_routing_admin.py::test_download_agent_bats_autolocate_python` |
| BUG-2026-08-18-004 | 入库明细/汇总报表只查「采购入库」，手机端「产品入库」「其他入库」永远查不出；仓库只按名称匹配，手机端存仓库编号（WH001）的历史单据也查不出 | 已修复：`_collect_in_detail_rows` 移除硬编码 `business_type=='采购入库'`，改为可选筛选（空=全部类型）；仓库过滤 `db.or_(name, code)`；`_build_report_filters` 透传 `business_type/warehouse_code`；仪表盘 8 处统计与 `report_inout_print` 导出同样兼容仓库编号；回归测试 `tests/test_report_in_detail_business_type_warehouse.py`，2 passed |

| BUG-2026-08-18-001 | 入库 Excel 导入、其他入库编辑/导出/采购报表及批量反提交与仓库和业务类型口径不一致 | 修复导入字段保真、列表筛选条件传递、采购报表业务类型隔离、批量反提交仓库级库存校验；回归测试覆盖导入、导出、报表和多仓反提交 |

| 编号 | 问题 | 回归检查 |
|------|------|----------|
| BUG-2026-08-18-002 | 多仓库+关库位管理下，历史 NULL-location 流水无法按仓库聚合，单张/批量反提交误报“库存不足”，单据卡在 completed 无法删除 | 已修复：`revert_in_order` / `batch_revert_in_order` / `update_completed_in_order` 在仓库级校验失败且该物料库存全部为未归属流水（`_material_stock_unattributed`）时回退全局 `Material.stock` 口径；存在可归属流水仍保持仓库级严格校验（防 A 仓掩护 B 仓）；回归测试 `tests/test_bug_2026_08_18_002_revert_legacy_unattributed_stock.py`，专项 3 passed |
| BUG-2026-08-18-005 | 系统早期采购入库单/领料单未强制选择仓库，单据 warehouse 为空字符串，报表按仓库过滤后这些历史单据“有数据也查不出来” | 已修复：新增 `scripts/backfill_document_warehouse.py` 把 warehouse/location 为空的采购入库单（in_order）、领料单（production_requisition）及其关联 `stock_transaction.location` 回填为默认仓库（warehouse.is_default=1 且 active），默认 dry-run、须显式 `--apply` 写库、幂等可重跑；回归测试 `tests/test_backfill_document_warehouse.py`（5 passed：dry-run 不写库 / apply 写库 / 无默认仓库拒绝 / 幂等 / 已归属数据不变），提交 `cb74962`；并新增启动期自动回填 `backfill_empty_warehouse_documents`（app.py 紧跟 auto_migrate_database 调用，存在默认仓库即自动回填、幂等、无默认仓库跳过，用户无需手工执行命令），回归测试 `tests/test_backfill_empty_warehouse_documents.py`（4 passed），提交 `26996aa`；后修复接线缺陷：`start_wms_offline.bat` 默认设置 `WMS_NO_DB_TOUCH=1` 会跳过 `auto_migrate_database`，原实现把回填放进同一 else 分支导致生产重启永不生效，已改为独立于迁移开关无条件执行，新增接线回归 T5（子进程 `import app` + `WMS_NO_DB_TOUCH=1` 校验 DB 被回填），专项 5 passed、全量 596 passed，提交 `19b94cb`；再修复日志不可见缺陷：回填调用原在日志配置之前执行，生产环境回填日志行丢失（启动输出只剩 `Flask config loaded`，看不到回填结果，用户误以为没生效），已把 `backfill_empty_warehouse_documents()` 移到日志配置之后调用，并给 T5 增加断言「启动输出必须包含『历史单据仓库回填完成』」，专项 5 passed、全量 596 passed、lint 0 违规，提交 `bc98db2` |
| BUG-2026-08-18-006 | `revert_in_order` 的 BUG-2026-08-18-002-fix 分支（仓库级库存不足但全局充足时把未归属流水修正到本单仓库）引用 `StockTransaction` 但函数内 import 列表遗漏该符号，触发时 NameError→500，接口返回 HTML 而非 JSON，反提交无提示 | 已修复：`revert_in_order` 局部 import 补入 `StockTransaction`；回归 `tests/test_bug_2026_08_16_009_revert_in_order_warehouse_level.py`（3 passed）+ `tests/test_bug_2026_08_17_002_revert_in_order_without_warehouse.py`（3 passed），提交 `365a099` |
| BUG-2026-08-17-006 | 采购入库单反提交后删除，库存流水未随物理删除清理，库存台账显示已删除单据 | 已修复：单张/批量删除均清理该入库单关联流水；历史遗留数据使用 `scripts/cleanup_orphan_in_order_transactions.py`，默认预演、须 `--confirm-delete` 执行；回归测试覆盖预演与删除安全边界 |
| BUG-2026-08-17-005 | 库存查询低库存/正常库存按全局 `Material.stock` 判断，旧入库/出库/盘点/调整列表导出未按仓库过滤 | 已修复：`stock_query` 按仓库库存判断；四类列表导出按仓库隔离并补齐盘点/调整仓库列；回归测试 `tests/test_bug_2026_08_17_005_warehouse_stock_status_and_legacy_exports.py`，专项 3 passed，lint/verify/compileall 通过 |
| BUG-001 | commit 失败后仍返回 success | `scripts/verify_wms_bugs.py` 检查高风险回归模式 |
| BUG-002 | 库存扣减无事务隔离 | 检查 `deduct_stock()` 委托原子扣减，避免读改写扣减 |
| BUG-004 | 仓库/部门新增编辑删除缺异常处理 | 人工代码审查加自动高风险扫描 |
| BUG-005 | 物料复制编码硬编码 6 位 | 检查复制编码函数不再依赖固定 6 位 |
| BUG-006 | 期初库存调整无锁 | 检查使用库存原子增量更新 |
| BUG-009 | document 事件监听器泄漏 | 检查重复绑定前移除旧 handler |
| BUG-010 | ExcelTable cloneNode 清监听器副作用 | 检查不再使用 cloneNode 替换单元格 |
| BUG-011 | 重复创建导入模态框 | 检查已有模态框时直接复用 |
| BUG-012 | 导出列错位 | 检查按导出列映射数据 |
| VULN-001 | 打印 HTML 净化失败返回空 | 检查异常时返回安全错误 HTML |
| CONF-001 | 生产环境 SECRET_KEY 未强制 | 检查 production 必须配置 SECRET_KEY 或显式允许自动密钥 |
| CONF-002 | 开发配置默认 SQL_ECHO | 检查默认不启用 SQL 日志 |
| CONF-004 | SQLite 并发限制 | 检查启用 WAL、busy_timeout、外键 |
| VULN-003 | 部分 POST 表单缺 CSRF 字段 | 检查模板中普通 POST 表单含 `csrf_token` |
| VULN-004 | 密码强度不一致 | 检查新增用户和重置密码复用同一强度校验 |
| BUG-NEW-001-FIX-01 | 已完成入库单可被详情页或接口直接物理删除 | 后端只允许删除 `pending`；已完成单必须先反提交回退库存，详情页不显示完成单删除按钮 |
| BUG-NEW-003 | 采购入库来源规则不一致 | 采购入库允许手工录入，采购订单仅为可选来源；有关联订单时继续校验来源和数量 |
| BUG-NEW-005 | 默认密码策略不一致 | 已统一为优先使用 `WMS_BOOTSTRAP_PASSWORD`，未设置且首次创建管理员时使用 `admin`；安装和启动不得重置已有密码 |
| BUG-NEW-008 | 微信分享默认配置硬编码私人信息 | 检查源码不含私人姓名/微信号默认值 |
| BUG-NEW-009 | Excel 导入组件列名拼入 HTML 存在 XSS 风险 | 检查必填列通过 `textContent` 写入 |
| BUG-NEW-013 | 入库单下推缺少来源和数量控制 | 采购入库、其他入库可下推领料、其他出库和售后出库草稿；必须校验完成状态、可下推数量、幂等及客供阻断 |
| BUG-NEW-014 | 批量完成出库单失败回滚后计数和事务不一致 | 检查每张出库单独立提交，失败单独回滚 |
| BUG-NEW-015 | 单号生成固定截取末尾 4 位 | 检查按前缀+年月后的完整数字后缀递增 |
| BUG-NEW2-001 | 入库路径未检查 `add_stock()` 返回值 | 检查 Android 入库、手机扫码入库等核心路径失败即回滚 |
| BUG-NEW2-002 | 入库路径未检查库位库存更新返回值 | 检查 Android 入库和手机扫码入库库位失败即回滚 |
| BUG-NEW2-003 | 期初库存并发更新读取旧值缺少锁 | 检查期初库存新增/编辑/批量保存读取时使用 `with_for_update()`，库存更新仍用原子增量 |
| BUG-NEW2-004 | 盘点完成直接改库存或不生成调整单 | 检查普通盘点和扫码盘点完成后生成库存调整草稿，库存变化由调整单提交执行 |
| BUG-NEW3-001 | `add_stock()` 返回值未检查导致流水和库存可能不一致 | 检查所有 `add_stock()` 调用必须接收并处理返回值 |
| BUG-NEW3-005 | `batch_complete_in_order` 缺单据写锁、循环外 commit 致并发重复审核/部分失败整体回滚 | 每单 `_acquire_order_write_lock(InOrder,id,'pending')`、循环内独立 `commit`、失败仅回滚自身，SHA `7d2272a4` |
| BUG-NEW3-006 | `batch_complete_out_order` 缺单据写锁致并发重复扣库存/重复推进销售发货进度 | 每单 `_acquire_order_write_lock(OutOrder,id,'pending')`、循环内独立 `commit`，SHA `9d6a2ea1` |
| BUG-NEW3-007 | `batch_revert_in_order` 缺锁+`deduct_stock` 读改写竞态+循环外 commit | 每单加锁+改 `deduct_stock_atomic`+循环内独立 `commit`，SHA `60f365b4` |
| BUG-NEW3-008 | 售后出库完成/反提交未同步库位库存，启用库位管理后总库存与库位库存长期不一致 | `complete_after_sale_out_order` 和 `revert_after_sale_out_order` 在原子扣减/恢复后调用 `update_location_inventory`，失败回滚，SHA `0b56db5d` |
| AI-AUTH-001 | AI 草稿和敏感分析权限校验分散，存在能力扩展后越权风险 | 检查 AI 草稿、文档确认和敏感分析统一通过 `AI_CAPABILITY_ROLES` 校验 |
| AI-AUTH-002 | 销售 AI 能力 `sales_insights` / `sales_followup_agent` 上线后未纳入权限矩阵自动化覆盖 | 检查 `scripts/verify_ai_permission_matrix.py` 的 `EXPECTED` 含上述能力，且 `ROLES` 含 `sales` |
| AI-IDEMPOTENCY-001 | AI 重复点击、网络重试或 SSE 重连可能重复生成草稿 | 检查普通响应和 SSE 使用持久化 `request_id`，重复请求只执行一次并重放结果 |
| AI-ENCODING-001 | AI 采购入库业务类型和确认框存在历史乱码 | 检查已知乱码常量不得重新出现 |
| AI-AUDIT-001 | AI 请求和能力授权缺少持久化审计，无法复盘模型、耗时和权限结果 | 检查每个首次请求创建 `AIRun`，能力校验写入 `AIToolCall`，幂等重放不重复创建运行记录 |
| BUG-SALES-001 | `SalesOrder` 缺 `customer_id` 外键，依赖 customer 字符串文本，无法关联客户主数据 | 检查 `SalesOrder.customer_id` 为 `nullable=False` 外键到 `customer.id` |
| BUG-SALES-002 | `SalesOrder` 缺 `warehouse_id` 外键，依赖 warehouse 字符串，报表仓库筛选无法关联 Warehouse 表 | 检查 `SalesOrder.warehouse_id` 为外键到 `warehouse.id`，历史数据已回填 |
| BUG-SALES-003 | `OutOrderItem` 缺行级来源外键，无法追溯到销售订单明细 | 检查 `OutOrderItem.source_sales_order_item_id` 为外键到 `sales_order_item.id` |
| BUG-SALES-004 | `OutOrder` 缺头级来源外键，依赖 `purpose` 字符串解析 | 检查 `OutOrder.source_sales_order_id` 为外键到 `sales_order.id`，`source_sales_order` relationship 已建立 |
| BUG-SALES-005 | 销售出库缺跨仓库边界校验，可能扣错仓库库存 | 检查出库时 `material.stock` 减扣后不为负，且按 `SalesOrder.warehouse_id` 校验仓库一致性 |
| BUG-SALES-006 | 销售选单接口 `/api/sales_order/selectable` 无并发保护，高并发可能重复生成出库草稿 | 检查选单接口使用 `BEGIN IMMEDIATE` 串行化 |
| BUG-SALES-007 | `SalesOrder` 金额字段使用 `Float` 类型，精度不足导致对账偏差 | 检查 `SalesOrder.total_amount`/`untaxed_amount`/`tax_amount`/`shipped_amount`/`remaining_amount` 均为 `Numeric(18,2)` |
| BUG-SALES-008 | `/sales/<id>/copy` 和 `/sales/batch_delete` 缺 `@require_role`，仅 `@login_required` 越权风险 | 检查 12 个 `/sales/*` POST 路由全部含 `@require_role('warehouse','purchase','sales')`（SM-P6-FIX-01） |
| BUG-SALES-009 | `sales_order_detail.html` 两处 fetch 调用缺 `X-CSRFToken` 头，存在 CSRF 漏洞 | 检查模板 fetch 调用使用 `csrfPost` helper 或继承 `base.html` 全局 `window.fetch` wrapper（SM-P6-FIX-01） |
| BUG-SALES-010 | `sales_out_draft` AI 工具语义错配：工具名"销售出库草稿"但描述/端点均为"售后出库" | 检查 `sales_out_draft` 标记为 deprecated alias，新增 `after_sale_out_draft`（端点 `add_after_sale_out_order`）+ `sales_outbound_draft`（端点 `create_sales_outbound_draft`）（AI-SALES-F01-FIX-02） |
| BUG-SALES-011 | `VALID_ROLES` 缺 `'sales'` 角色，导致 `sales_out_draft` 工具合规检查失败 | 检查 `VALID_ROLES` 包含 `'sales'`（AI-BUG-F02 / `b374565`） |
| BUG-SALES-012 | 销售模块无 AI 异常分析与单据联查能力，对比采购侧 `out_order_detail.html` 缺失 | 检查 `sales_order_detail.html` 含 AI 异常分析按钮 + `/api/ai/sales_order/<id>/anomaly_analysis` 只读路由 + 售后单联查面板（AI-SALES-F01-FIX-02） |
| BUG-SALES-013 | 销售模块无 AI 履约跟进工作台，对比采购侧 `AI-R11-F01` 7 队列结构缺失 | 检查 `ai_sales_workbench.html` + `sales_followup_workbench.py` 7 队列 + `sales_followup` Agent + `sales_insights` 工具 + 3 路由（AI-SALES-F02） |
| BUG-SALES-014 | 销售模板散用 `confirm()`/`alert()` 原生 API，与系统级 `showConfirm`/`showToast` 不一致 | 检查 5 个销售模板（`sales_outbound_selection.html`/`customer.html`/`after_sale_out.html`/`after_sale_out_add.html`/`after_sale_out_detail.html`）不再含 `confirm(`/`alert(` 调用（SM-P6-02） |
| BUG-SALES-015 | `sales_order.html` 工具栏 + 行内写按钮无角色权限感知隐藏，`user`/`production` 角色可见写操作入口 | 检查写按钮包裹 `{% if current_user.role in ['admin','warehouse','purchase','sales'] %}`，后端 `@require_role` 仍二次校验（SM-P6-02） |
| BUG-SALES-016 | `customer.html` 完全无客户导入入口，与 `supplier.html` 结构不一致 | 检查 `customer.html` 含 `importModal` 模态框 + AJAX 提交 + `csrf_token` + `notifyMasterDataChanged('customer_updated')` 广播（SM-P6-02） |
| LOGIN-CSRF-001 | Web `/login` 被 `@csrf.exempt` 豁免，无 CSRF token 的 POST 仍可建立会话 | 检查 `app.login` 不在 `csrf._exempt_views`，`app.native_api_login` 仍豁免，`login.html` 含 `csrf_token`，无 token POST `/login` 返回 400 |
| BUG-INSPECT-2026-07-27-001 | `verify_ai_tool_schemas.py` 的 `VALID_PAYLOADS` 未覆盖 AI-SALES-F02 新增的 `sales_followup_agent` 和 `sales_insights`，导致 `verify_ai_all.py --level smoke` 6/7 失败 | 在 `scripts/verify_ai_tool_schemas.py` `VALID_PAYLOADS` 中补齐这两条 valid payload（参考 `app/ai/tools/registry.py:242-259` 的 `SALES_INSIGHTS_SCHEMA`/`SALES_FOLLOWUP_SCHEMA`），`verify_ai_all.py` 升至 7/7 |
| BUG-MOBILE-2026-07-27-001 | 375/414 窄屏下出入库列表/详情页工具栏按钮可能并排挤压、触摸目标不足、长单号+状态徽章溢出可视区、模态框超出宽度（巡检报告 P2 遗留项） | 在 `app/static/css/custom.css` 末尾新增 `@media (max-width: 414px)` 块：`.wms-entry-toolbar` 改单列网格+按钮全宽+`min-height:40px`；`.order-title/.order-meta` 允许换行；`.modal-dialog` 限制 `max-width:calc(100vw-16px)`；`.table-responsive-wrapper` 不遮挡按钮；`.pagination` 换行居中。`scripts/verify_mobile_buttons.py` 静态校验通过；Playwright 运行时验证 375px 下 20 个按钮全部 `visible=True overflow=False` 高度 40px；截图存档 `qa_screenshots/mobile_375_*.png`、`qa_screenshots/mobile_414_*.png` |
| BUG-DATE-2026-07-27-001 | `InOrder.date` / `OutOrder.date` 默认 `date.today()` 但无未来日期强校验，巡检报告 C 维度遗留：凭证日期可被提前到未来账期 | 新增 `app/app.py:is_future_date(order_date, today=None)` helper（`order_date > date.today()`）；在 `add_in_order`/`update_in_order`/`complete_in_order` 三路由入口校验后返回 400 + `入库日期不能晚于今天`；在 `add_out_order`/`complete_out_order` 两路由入口校验后返回 400 + `出库日期不能晚于今天`；`in_order_detail`/`in_order_add_page`/`out_order_add_page` 三个 `render_template` 注入 `today=date.today()`；`in_order_add.html`/`out_order_add.html`/`in_order_detail.html` 三个日期 input 增加 `max="{{ today }}"`。`scripts/verify_future_date_guard.py` 25 项检查全绿（含 Flask test_client 未来日期被 400 拒、今天日期通过） |
| BUG-BATCH-2026-07-27-001 | `/in_order/batch_delete`、`/in_order/batch_complete`、`/out_order/batch_delete`、`/out_order/batch_complete` 四个批量接口无单次条数上限，超长事务可能阻塞数据库（巡检报告 P3 遗留项） | 四个 batch 路由入口在 `ids` 解析后加 `if len(ids) > 100: return jsonify({'status':'error','msg':'单次批量操作不能超过 100 条，请分批处理'}), 400`；前端 `batchInOrderAction`/`batchOutOrderAction` 在提交前加 `if (ids.length > 100) { showToast(...); return; }`。`scripts/verify_batch_limit.py` 26 项检查全绿（静态 4 后端+2 前端+动态 101 条被 400 拒×4+50 条不被拦截×4） |
| BUG-2026-07-29-001 | 全新空库上 `auto_migrate_database()` 启动失败 | `app/app.py:auto_migrate_database()` 新增 `_table_exists()` 守卫；目标表 `out_order` 不存在时 commit + close + return，DDL 交给 `db.create_all()` 处理。`scripts/verify_bug_2026_07_29_001.py` 覆盖「空库启动→HTTP 200」+「已有库迁移正常」双场景（commit `61d077e`/`ca271e2`） |
| BUG-2026-07-29-002 | 物料/供应商/客户 `name` 字段 XSS 风险 | `app/app.py` 新增 `sanitize_text_input(value, max_len)`：去 NUL/控制字符/HTML 尖括号/`javascript:` 协议；`add_material`/`add_supplier`/`add_customer` 9 字段同步接入（commit `64bbaea`） |
| BUG-2026-07-29-003 | POST 校验错误统一返回 HTTP 400 | `app/app.py` 新增 `api_error(msg, code=400)` helper；用脚本批量替换 745 处 `return jsonify({'status':'error',...})` 为 `return api_error(...)`；空表单 POST 现在返回 400 而非 200（commit `ac3d4ce`） |
| BUG-2026-07-29-004 | CSRF token 可重放：8 小时过长 | `app/config.py` `WTF_CSRF_TIME_LIMIT` 从 28800 缩短至 1800（30 分钟）（commit `7f2ed4f`） |
| BUG-2026-07-29-005 | 物料 `stock=999999999999`（12 位）被接受 | `app/app.py` 新增 `MAX_REASONABLE_STOCK = 99_999_999.99`；`add_material` 中 `parse_bounded_number` 显式传 `maximum=MAX_REASONABLE_STOCK`（commit `82ec4e9`） |
| BUG-2026-07-29-006 | `/material/print_label` `/stock_query/print` `/report/print` 三个未实现路由 404 行为模糊 | 显式注册三个 stub handler 显式返回 `api_error(..., code=404)` 带中文说明；全量 grep 确认 `app/templates/` 没有任何 `url_for`/`href` 引用这三个 URL（commit `c1d6235`） |
| BUG-2026-07-29-007 | 5000 字符 URL 参数被接受，无截断/告警 | `app/app.py` 新增 `QUERY_STRING_MAX_LENGTH = 2048` 与 `@app.before_request limit_query_string_length()`：超长返回 HTTP 414 + 中文提示（commit `0d8e966`） |
| BUG-2026-07-29-008 | admin `must_change_password=True` 时访问 `/in_order/<id>/print` 被强制重定向到 `change_password` | `enforce_initial_password_change` 中将所有 `print_*` 端点加入白名单；admin 即使未改初始密码也能打印单据（commit `a0d2a14`） |
| BUG-2026-07-29-009 | NUL 字节 `\x00` 被静默吞掉 | 复用 `sanitize_text_input` 的 NUL 字节移除逻辑，物料 `code`/`name`/`spec` 同时受益（commit `64bbaea`，随 BUG-002 一并提交） |
| BUG-2026-07-29-010 | admin 锁定后 GET `/login` 仍正常渲染无前端倒计时 | `app/app.py:login()` GET 分支显式探测 `User.locked_until` 与 `login_ip_locked_until`，把 `locked` / `lock_remaining_seconds` 注入 `login.html` 模板；前端 `lockHint` + `lockCountdown` JS 倒计时在锁定时可见（commit `cb21e32`） |
| BUG-2026-07-28-001 | 404 错误页空白：`GET /this_does_not_exist` 返回纯文本 `('页面不存在', 404)`，无模板 | `app.py` 新增 `render_template('404.html', path=request.path)`；`app/templates/404.html` 新建（Bootstrap 5 + 404 卡片 + 返回首页/返回上一页 + 路径显示）。`scripts/verify_bug_001.py` 静态+动态全绿 |
| BUG-2026-07-28-002 | 405 错误页空白：缺 `@app.errorhandler(405)`，GET `/supplier/add` 等被 405 走默认 Flask 空白页 | `app.py` 新增 `method_not_allowed()` handler + `render_template('405.html')`；`app/templates/405.html` 新建（与 404 同布局，状态色琥珀）。`scripts/verify_bug_001.py` 同时覆盖 405 卡片断言 |
| BUG-2026-07-28-003 | `/purchase_order` 默认 302 → `/purchase_order/add`，用户期望默认显示列表 | `app.py:34910` 改 `view in ('add','new')` 才跳新增；默认走 `pagination + orders` 渲染列表。`scripts/verify_bug_003.py` 断言默认 200 + `?view=add` 302 |
| BUG-2026-07-28-004 | admin 在 `/user` 列表可自助点击「重置密码」按钮，无任何二次校验，违反 AGENTS.md R-1 强规则 | `app.py:reset_user_password` 增加 `if user_id == current_user.id → 403`；admin 目标（`role=='admin'` 或 `username=='admin'`）要求 `bootstrap_pwd == WMS_BOOTSTRAP_PASSWORD`；`user.html:103-110` 自身行按钮 `disabled`+tooltip；`user.html` `resetPassword()` JS 加 admin 二次确认。`scripts/verify_bug_004.py` 覆盖所有失败路径 |
| BUG-2026-07-28-005 | 入/出库单保存不校验仓库/供应商/明细必填，可生成空单 | `app.py` 新增 `_validate_order_business_required(form, kind)` 统一 helper；`add_in_order`/`add_out_order`/`add_adjustment`/`add_transfer`/`add_check` 5 路由入口调用，未通过返回 400 + 中文 msg。`scripts/verify_bug_005.py` 覆盖空仓库/空物料/正常保存 |
| BUG-2026-07-28-006 | `/supplier` `/customer` 等列表页表头首列（checkbox 列）显示 `COLU...` 截断 | `app.js columnsOf` 识别 checkbox-only 列不设 label（避免回退到 `key`）；`custom.css` 新增 `.cb-check-col { width: 50px; min-width: 50px; max-width: 50px; }` 列宽兜底。`scripts/verify_bug_006.py` 抓首列 th 文本 ≠ `'COLU...'`、width==50 |
| BUG-2026-07-28-007 | `/purchase_request` `/purchase_order?view=list` `/out_order` `/check` `/requisition` 直接访问时同时存在 list 自身工具栏 + 嵌入工具栏 | `app.js insertGlobalActionBar` 增加 `isWmsEmbeddedPage()` 守卫（直接访问模式 return）；`custom.css` 新增 `body:not(.embedded-page) #cbGlobalActionBar { display: none !important; }` 兜底；嵌入模式（`?embedded=1`）保留双工具栏。`scripts/verify_bug_007.py` 浏览器访问对比 |
| BUG-2026-07-28-008 | `/material` 空库时分页区「共 0 条」+ 表格「暂无数据」并存 | `_list_macros.html:pager()` 在 `pagination.total==0` 时返回空字符串；空库列表统一包 `{% if pagination.total > 0 %}{...}{{pager(...)}}{% else %}{...}暂无数据...{% endif %}` 互斥。`scripts/verify_bug_008.py` 抓 HTML 断言互斥 |
| BUG-2026-07-28-009 | `/requisition` 分页区写「共 0 单」与其他列表「共 0 条」单复数不一致 | `requisition.html` 手工分页 div 改为 `{{ pager(pagination, 'requisition_list') }}` 宏；`subcontract.html`/`sales_order.html`/`check.html`/`transfer.html`/`adjustment.html` 同步切换。`scripts/verify_bug_009.py` 抓 HTML 断言统一「共 N 条」 |
| BUG-2026-07-28-010 | `GET /supplier/add` 被 BUG-002 修复为 405 卡片，业务上希望 GET 该路径直接弹出新增 modal | `app.py:supplier_add` 路由 `methods=['POST']` 改 `methods=['GET','POST']`；GET 重定向 `/supplier?showAddModal=1`；列表页 `showAddModal=1` query 自动弹 modal。`scripts/verify_bug_010.py` 覆盖 302+列表页 modal 触发 |
| BUG-2026-07-28-011 | 登录页输错 1 次提示「还可尝试 2 次」但无锁定 UI、无倒计时、无 IP 累计提示 | `app.py:login()` 锁定分支返回 `lock_remaining` 秒数 + `locked:True`（HTTP 423）；`login.html` 新增 `lockHint` 倒计时 span + 按钮置灰 + IP 累计警告条；`showLockCountdown` JS 每秒 tick 减秒。`scripts/verify_bug_011.py` 5 次失败 → 第 5 次 423 + 倒计时 |
| BUG-2026-07-28-012 | `/operation_audit` 「旧日志 0 / 变更审计 0」与系统术语不一致 | `operation_audit.html` 统一为「历史审计 / 实时审计」+ tooltip 解释对应 `OperationLog`/`OperationAudit` 两表。`scripts/verify_bugs_012_to_020.py` 抓页面断言「历史审计」「实时审计」存在 |
| BUG-2026-07-28-013 | `/admin/console` 「验收快照 0 / 证据包 0」零值卡片无引导 | `admin_console.html` 零值卡改为 `.clickable-card[data-href]`，验收快照 → `/ai_prelaunch`、证据包 → `/backup`；非零值维持原数字。`scripts/verify_bugs_012_to_020.py` 抓 admin_console 断言 data-href 存在 |
| BUG-2026-07-28-014 | 9+ 新增单据页（入库/采购/出库/盘点/领料/调拨/调整/销售/委外）仅有「保存」「返回」 | `in_order_add.html` 等 9 模板 footer 加「保存并新建」按钮（`saveAndNew` JS fetch + `keep=1` 提交）；后端 `*_add_page` 接收 `keep=1` query 保留 `supplier_id/customer_id/warehouse` 表头字段。`scripts/verify_bugs_012_to_020.py` 抓 9 模板断言「保存并新建」存在 |
| BUG-2026-07-28-015 | 连续打开 16+ 页面后 Tab 栏无限累积，浏览器卡顿 | `app/static/js/app.js` `WmsTabs.MAX=15`，`open()` 超过 MAX 自动关闭最早 tab；新增 `closeOthers/closeAll` + 右键菜单「关闭当前/关闭其他/全部关闭」+ `localStorage` 持久化。`scripts/verify_bugs_015_020.py` 静态断言 MAX_TABS + closeOthers/closeAll/contextmenu |
| BUG-2026-07-28-016 | 右下角 AI 助手浮窗在窄屏或按钮密集处遮挡底部按钮 | `base.html` 新增 `#aiAssistantHideBtn`×关闭按钮 + `hideAiAssistantFloating()` JS（`localStorage.wms_ai_hide_floating=1` 记忆）；scroll 监听：滚到底部 80px 内 `transform:translateY(120%); opacity:.3` 半隐藏。`scripts/verify_bugs_015_020.py` 抓 base.html 断言 |
| BUG-2026-07-28-017 | `/in_order` 「入库明细」与 `/in_order/add`「新增采购入库单」标题口径不一致 | `in_order.html` title 改「入库单」+ 列表 h2 改「入库单」；`in_order_add.html` title 改「新增入库单」+ h2 改「新增入库单」。`scripts/verify_bugs_015_020.py` 抓 HTML 断言 |
| BUG-2026-07-28-018 | `/supplier` `/customer` placeholder 顿号不一致（电话/地址无顿号） | `supplier.html` `customer.html` placeholder 全部统一为「搜索供应商/客户编号、名称、联系人、电话、地址」全顿号。`scripts/verify_bugs_015_020.py` 抓 HTML 断言全顿号 |
| BUG-2026-07-28-019 | `/category` 所有分类都显示「1 级」（`row.level` 永远 0） | `app.py:category_list` 引入 `build_category_tree_rows()` 按 `parent_id` 递归计算 `level` + 树枝 `├─/└─` 缩进 + 防环（visited 集合）；`category.html` 改为 `<span class="category-level-badge lv{{row.level+1}}">{{row.level+1}} 级</span>` + 5 套分级色（lv1 靛蓝/lv2 青色/lv3 绿色/lv4 琥珀/lv5+ 红色）。`scripts/verify_bugs_015_020.py` + `dbg_check_levels2.py` 抓 HTML 断言 lv1-lv4 CSS+徽标 class+title |
| BUG-2026-07-28-020 | `/stock_query` 「打印模板」按钮一直常驻，空数据时无意义 | `stock_query.html` 按钮加 `{% if not stock_rows or stock_rows\|length == 0 %}disabled title="请先查询数据" aria-disabled="true"{% else %}onclick="printTemplate()"{% endif %}`；无数据徽标 + 「暂无数据」占位。`scripts/verify_bugs_015_020.py` 抓 HTML 断言 `bi-printer` + `disabled` + `无数据` |
| BUG-F02-01 | 基础资料列表默认按 `created_at desc` 排序，与查字典习惯相反 | `app.py` `_get_master_list_filters` 默认 `sort_by='code'`/`order='asc'`，5 个 list 路由同步；模板统一用 `sort_th` 宏。`audit_screenshots/verify_f02_01_sort.py` 33/33 |
| BUG-F02-02 | 物料/供应商/客户 6 个 add/edit 路由对 code/name/spec/purpose/remark/contact/phone/address 不做长度校验，DB 静默截断 | `app.py` 6 路由入口加 11 字段长度校验（code 50/name 100/spec 100/purpose 200/remark 500/contact 50/phone 20/address 200）；超限返 400 + 中文 msg。`audit_screenshots/verify_f02_02_truncate.py` 30/30 |
| BUG-F02-03 | 标签模板设计页点"保存布局"后端 404，前端 `data.status === 'success'` 假阳性，误报保存成功 | `app.py` 新增 `save_label_template_layout`（`@require_role admin/warehouse` + layout 非空/类型校验 + 审计 log）；`label_template_detail.html` `saveLayout()` 加 `response.ok` 检查 + `_savingLayout` 守卫 + `spinner-border` 反馈 + `console.error` 详细错误。`audit_screenshots/verify_f02_03_label_save.py` 16/16 |
| BUG-F02-04 | 仓库被停用/删除后，入库单仍可选用，导致库存归属不明 | `app.py` 新增 `is_warehouse_active`/`assert_warehouse_active` helper；`add_in_order`/`update_in_order` 入口校验；不通过返 400 + 中文 msg。`audit_screenshots/verify_f02_04_warehouse.py` 12/12 |
| BUG-F02-05 | `location_management_enabled=False` 时入库单前端仍把仓库字段渲染为必填 | `in_order_add.html` 仓库字段包 `{% if location_management_enabled() %}` 条件渲染；JS 同步用 `locationManagementEnabled` 控制必填；`add_in_order` 后端同步允许空仓库。`audit_screenshots/verify_f02_05_location_off.py` 13/13 |
| BUG-F02-06 | 普通用户没有自助改自己资料（电话/邮箱/备注）的入口；admin 改他人审计缺 `last_modified_by` | `app.py` 新增 `edit_my_profile`（仅改 email/phone/bio，邮箱/电话格式校验，长度限制 200/30/500，不可改 username/role/status/password）+ User 模型加 `email/phone/bio` 列 + 迁移；`edit_user` `log_operation` 显式带 `last_modified_by=current_user.username`；`my_profile.html` + 侧边栏入口。`audit_screenshots/verify_f02_06_profile.py` 17/17 |
| BUG-F02-07 | 主数据列表分页 `per_page` 无上限校验，URL 不记忆每页大小 | `app.py` 5 list 路由统一 `per_page` 白名单 [10,20,50,100,200]，默认 20；`_list_macros.html` 新增 `per_page_select` 宏；`base.html` 自动绑定 `.per-page-select` 切换 URL。`audit_screenshots/verify_f02_07_pagination.py` 17/17 |
| BUG-F02-08 | purchase/sales 等非授权角色可访问 `/label_template/<id>` 设计页，点保存才 403 | `app.py` `label_template_detail` 加 `@require_role('admin','warehouse')`。`audit_screenshots/verify_f02_08_template_perm.py` 4/4 |
| BUG-F02-09 | `material.html` 标签模板设计器 16 处调用从未定义的 `saveTemplateToStorage()`，任何编辑交互即抛 `ReferenceError`，设计结果无法持久化 | `material.html` 新增 `saveTemplateToStorage()`（序列化当前编辑器状态到 `localStorage.labelTemplateDraft`）+ `restoreTemplateDraft()`（无已保存模板时自动恢复草稿）+ `saveTemplate()` 成功后清除草稿。`audit_screenshots/verify_f02_09_10_frontend.py` 14/15（浏览器实测项因本机无浏览器跳过） |
| BUG-F02-10 | `warehouse.html`/`department.html`/`employee.html` 的 GET 筛选表单内含 `csrf_token` 隐藏域，筛选后 token 明文出现在地址栏 URL | 三个模板 GET 表单删除 `csrf_token` 隐藏域（POST 模态框表单保留）。`audit_screenshots/verify_f02_09_10_frontend.py` 静态+线上 HTTP 双重验证通过 |
| BUG-2026-07-29-001 | 全新空库上 `auto_migrate_database()` 启动失败，删除 `instance/inventory.db` 后直接 `python3 app/run_server.py` 服务退出，HTTP=000 | `app/app.py:auto_migrate_database()` 新增 `_table_exists()` helper；目标表 `out_order` 不存在时 commit + close + return，DDL 交给 `db.create_all()` 处理。`scripts/verify_bug_2026_07_29_001.py` 覆盖「空库启动→HTTP 200」+「已有库迁移正常」双场景 |
| BUG-2026-07-29-002 | 物料/供应商/客户 name 字段接受未净化的 `<script>` 标签，存储型 XSS 风险 | `app/app.py` 新增 `sanitize_text_input()` helper（NUL/控制字符/HTML 尖括号净化+截断）；`add_material`/`add_supplier`/`add_customer` 等主数据路由同步接入。`scripts/verify_bug_2026_07_29_002.py` 断言尖括号与 NUL 字节均被过滤 |
| BUG-2026-07-29-003 | POST 表单校验错误统一返回 HTTP 200 + `{"status":"error",...}`，违反 REST 规范 | `app/app.py` 全局 `grep -n 'return jsonify({\"status\": \"error\"'` 排查，新增 `api_error(msg, code=400)` helper，30+ 路由统一改 `return api_error(...)`。`scripts/verify_bug_2026_07_29_003.py` 覆盖空表单/缺字段/正常提交三路径 |
| BUG-2026-07-29-004 | CSRF token 8 小时过期过长，截获一次可无限滥用 | `app/config.py` `WTF_CSRF_TIME_LIMIT` 从 28800 改 1800（30 分钟）。`scripts/verify_bug_2026_07_29_004.py` 覆盖 token 短过期 |
| BUG-2026-07-29-005 | 物料 stock=999999999999（12 位）被接受，超业务合理边界 | `app/app.py` `add_material`/数量路由 `MAX_REASONABLE_STOCK=99999999.99`/`MAX_REASONABLE_PRICE=99999999.99` 收紧上限；超限返回 400。`scripts/verify_bug_2026_07_29_005.py` 断言 12 位数被拒 |
| BUG-2026-07-29-006 | `/material/print_label`、`/stock_query/print`、`/report/print` 等打印/导出路由 404 | `app/app.py` 排查后端 `@app.route` 残留；缺实现路由按 `print_in_order` 模板补齐或前端按钮切换为 `window.print()`。`scripts/verify_bug_2026_07_29_006.py` 覆盖前端模板引用→后端实现一致 |
| BUG-2026-07-29-007 | 5000 字符 URL 参数被接受，无截断/告警 | `app/app.py` 入口 `@before_request _limit_query_string` 拦截 >2048 字节 query string 返回 414。`scripts/verify_bug_2026_07_29_007.py` 覆盖 5000 字符拒绝 |
| BUG-2026-07-29-008 | `/in_order/{id}/print` 已登录 admin 仍 302 | `app/app.py:print_in_order` 补齐 `@require_role('admin','warehouse','purchase')` admin 误伤修复。`scripts/verify_bug_2026_07_29_008.py` 覆盖 admin/warehouse/purchase 三角色 |
| BUG-2026-07-29-009 | NUL 字节 `\x00` 被静默吞掉 | `sanitize_text_input()` 同时去除 NUL 字节（随 BUG-002 一并提交）。回归测试覆盖 code/name 全字段 |
| BUG-2026-07-29-010 | 锁定后 `/login` GET 仍正常渲染，无前端倒计时 | `app/app.py:login()` GET 分支检测 admin 锁定 → 传 `lock_remaining`/`locked_account` 模板；`login.html` 新增 `lockHint` 倒计时 + JS 每秒 tick。`scripts/verify_bug_2026_07_29_010.py` 覆盖 5 次错误后 GET 页面含倒计时 span |
| BUG-2026-07-31-001 | 长会话 CSRF token 过期（30 分钟寿命），停留 30 分钟后所有非 GET 请求失败 | `app/app.py` 新增 `POST /api/csrf_refresh` 端点（`@csrf.exempt`，返回新 token）；`app/templates/base.html` 每 25 分钟（寿命 30 分钟，提前 5 分钟保险）调用一次 + 页面 visibilitychange 切回前台时立即调用，更新 `<meta name="csrf-token">` content。根治用户停留 30+ 分钟的所有 CSRF 失败场景。 |
| BUG-2026-08-09-003 | 手机 WMS 语音功能卡在"正在聆听，请说出指令…"无法退出 | `VoiceCommandViewModel.startListening()` 启动 8 秒 `viewModelScope` 超时 Job；`stopListening`/`dispatchResults`/`onError` 三处取消该 Job；超时后自动 destroy recognizer 并写入 `error="识别超时，请重试"`。`onError` 新增 `ERROR_NETWORK/ERROR_SERVER/ERROR_CLIENT` 三个细分提示。`scripts/verify_bug_2026_08_09_003.py` 静态覆盖：超时 Job 存在 + 8 秒阈值 + 三处 cancel + 三个新增 error 分支 |
| BUG-2026-08-02-001 | 未启用库位管理时入库单仓库被误设为可选，导致可保存无仓库入库单；仓库与库位概念混淆 | `app/app.py` 新增 `prefer_default_warehouse()`/`get_default_warehouse()` helper；`add_in_order`/`update_in_order`/`complete_in_order`/`update_completed_in_order`/`batch_complete_in_order` 均强制仓库必填，未填写时自动取默认仓库，无默认仓库时拒绝保存/完成；`in_order_add.html`/`in_order_detail.html` 仓库字段加 `required` 与默认仓库选中，移除 `locationManagementEnabled` 控制仓库必填的旧逻辑。`scripts/verify_bug_2026_08_02_001.py` 静态+动态全量覆盖 |
| BUG-2026-08-02-002 | `add_out_order` 领料/其他出库仓库被位置管理开关误设为可选，与 AGENTS.md 仓库必填规则冲突 | `app/app.py:add_out_order` 非销售出库分支移除 `location_management_enabled()` 条件，改为：未填仓库时优先 `get_default_warehouse()` 自动带入，无默认仓库返回 400 `请选择仓库`（commit `3b9aba5a`）。`scripts/verify_bug_2026_08_02_002.py` D1/D2 静态+动态覆盖 |
| BUG-2026-08-02-003 | `complete_out_order` 仓库校验依赖位置管理开关；首版修复把 `order.warehouse = default_wh.name` 放在 `_acquire_order_write_lock` 之前，被其 SQLite 分支 `db.session.rollback()` 丢弃，导致存量无仓库 pending 单据完成时仓库仍为空 | `app/app.py:complete_out_order` 改为：锁前只做 fast-path 读校验（`if not order.warehouse and not get_default_warehouse(): return api_error('请选择仓库')`），实际赋值与必填校验移到加锁后 `order = locked` 之后再执行，确保赋值随 commit 落库（commit `324ef4d3` + 修复 commit 见本次提交）。`scripts/verify_bug_2026_08_02_002.py` D3 验证完成时 `order.warehouse == 默认仓库` |
| BUG-2026-08-02-004 | `batch_complete_out_order` 缺仓库校验，存量无仓库 pending 单据批量完成时跳过校验直扣库存 | `app/app.py:batch_complete_out_order` 加锁后 `if not order.warehouse: default_wh = get_default_warehouse(); ...; if not order.warehouse: skipped.append(f'{order.order_no}(未填写仓库)'); rollback; continue`，与单据版一致，跳过本单不阻断整批（commit `504f923f`）。`scripts/verify_bug_2026_08_02_002.py` D4 验证默认仓库自动带入 |
| BUG-2026-08-02-005 | `add_after_sale_out_order` 售后出库 `warehouse` 字段模型已存在但闲置，从未读取/校验 | `app/app.py:add_after_sale_out_order` 读取 `data.get('warehouse')`，未填时 `get_default_warehouse()` 自动带入，无默认仓库返回 400 `请选择仓库`，并显式 `order.warehouse = warehouse` 落库（commit `6917860b`）。`scripts/verify_bug_2026_08_02_002.py` D5/D6 覆盖 |
| BUG-2026-08-02-006 | `complete_after_sale_out_order` 完成时不校验仓库，存量无仓库 pending 单据完成时扣库存无仓库归属 | `app/app.py:complete_after_sale_out_order` 在 `_acquire_order_write_lock` 之后做仓库自动带入与必填校验，赋值随 commit 落库（commit `fbc9c3a9`）。`scripts/verify_bug_2026_08_02_002.py` D7 验证 |
| BUG-2026-08-02-007 | `after_sale_out_add.html` 仓库字段从未渲染，前端无法选择仓库 | `app/templates/after_sale_out_add.html` 新增仓库 `<select name="warehouse" required>` 字段，循环 `warehouses` 选项并按 `default_warehouse`/`order.warehouse` 选中；JS `submitForm()` 增加仓库非空前端校验（commit `ea8df15b`）。`scripts/verify_bug_2026_08_02_002.py` B1/B2 静态断言 |
| BUG-2026-08-02-008 | `out_order_add.html` 仓库字段未带 `required`，未预选默认仓库；前端 JS 无仓库非空校验 | `app/templates/out_order_add.html` 仓库 `<select>` 加 `required` 属性，循环选项按 `prefill.warehouse`/`default_warehouse` 选中；JS `submitForm()` 增加 `if (!warehouseValue) { alert('仓库不能为空，请选择仓库'); return; }` 前端校验（commit `31ea13e9`）。`scripts/verify_bug_2026_08_02_002.py` A1/A2 静态断言 |
| BUG-2026-08-02-009 | `complete_in_order` 仓库赋值写在 `_acquire_order_write_lock` 之前，被其 SQLite 分支 `db.session.rollback()` 丢弃，导致存量无仓库 pending 入库单完成时以 `warehouse=NULL` 落库 + 库位库存不同步 | `app/app.py:complete_in_order` 改为锁前只做 fast-path 读校验（`if not order.warehouse and not get_default_warehouse(): return api_error('入库单必须填写仓库')`），实际赋值与必填校验移到加锁后 `order = locked` 之后再执行，与 `complete_out_order` 实现对齐（commit `2d52e240`）。`scripts/verify_bug_2026_08_02_001.py` D5 验证锁后赋值正确落库 |
| BUG-2026-08-02-011 | `batch_delete_in_order` 删除循环未回退 `source_purchase_order_item.received_quantity`、未调 `update_purchase_order_status`、未加写锁，与单条 `delete_in_order` 逻辑不对称；并发完成后误删风险 | `app/app.py:batch_delete_in_order` 改为：逐条 `_acquire_order_write_lock(InOrder, id, 'pending', selectinload(InOrder.items))`；删除前回退 `source_purchase_order_item.received_quantity` 并收集 `affected_purchase_order_ids`；删除后调 `update_purchase_order_status`；补 `_source_has_active_push` 下推占用校验；每张单据独立 commit，单点失败仅回滚自身（commit `436d92cf`）。`scripts/verify_bug_2026_08_02_004.py` 9 项全绿（含 received_quantity 回退、已完成单拒绝、混合批量删除） |
| BUG-2026-08-02-010 | `complete_adjustment`/`revert_adjustment` 只调 `add_stock`/`deduct_stock_atomic` 改 `Material.stock` 总库存，不调 `update_location_inventory`，`AdjustmentOrderItem.location` 字段闲置；开启库位管理后总库存与库位库存之和长期偏差，且无法对称回退 | `app/app.py:complete_adjustment` 在 `add_stock`/`deduct_stock_atomic` 之后补 `if location_management_enabled() and quantity: update_location_inventory(item.material, item.location, quantity)`；`revert_adjustment` 对称回退 `update_location_inventory(..., -quantity)`；loc_key 暂用 `item.location`，TODO(P1-1) 切换为 `adjustment.warehouse`（commit `a3b9448f`）。`scripts/verify_bug_2026_08_02_003.py` 15 项全绿（正向/负向调整 complete+revert 的总库存与库位库存一致性），`verify_adjustment_state_machine.py` 未回归 |
| P2-1 | `after_sale_out_detail.html` 详情页基本信息卡片未显示仓库字段，单据详情无法核对实物入哪个仓 | `app/templates/after_sale_out_detail.html` 在客户名称下方补 `<p><strong>仓库：</strong>{{ order.warehouse or '-' }}</p>`（commit `61452e84`） |
| BUG-2026-08-02-012 | `TransferOrder`/`InventoryCheck`/`AdjustmentOrder` 三个模型无 `warehouse` 字段，违反 AGENTS.md 仓库必填规则；`TransferOrder` 用 `from_location`/`to_location` 存仓库名，仓库与库位概念混淆 | 模型层：`AdjustmentOrder`/`InventoryCheck` 加 `warehouse` 列；`TransferOrder` 加 `from_warehouse`/`to_warehouse` 列（保留 `from_location`/`to_location` 作库位字段）；`auto_migrate_database` 含三表 ALTER + 存量数据回填默认仓库名（commit `738728dc`）。路由层：`add_adjustment`/`save_check_table`/`add_check` 仓库必填+默认仓库带入；`save_transfer_table`/`add_transfer` 调出/调入仓库必填，同时写 `from_warehouse`/`to_warehouse` 和 `from_location`/`to_location`（库位字段历史兼容）（commit `2dfe08ee`）。`scripts/verify_bug_2026_08_02_005.py` 27 项全绿（含 S1-S8 模型/迁移静态、D1-D8 字段落库、S9 路由静态、D9-D14 路由动态必填+默认带入） |
| BUG-2026-08-02-013 | `complete_transfer`/`revert_transfer` 无条件调 `deduct_location_inventory_atomic`/`update_location_inventory`，未开启库位管理时也写 `LocationInventory`，与入库/出库的 `if location_management_enabled():` 模式不一致；`_create_adjustment_drafts_from_check`/`_scan` 生成的调整草稿不带 warehouse，导致 `complete_adjustment` 的 loc_key 回退到 `item.location` | `complete_transfer`/`revert_transfer` 加 `use_location = location_management_enabled()` 守卫，库位库存操作包在 `if use_location:` 内，流水记录（`add_stock_transaction`）保留在守卫外（未开启也记审计流水）；`_create_adjustment_drafts_from_check`/`_scan` 调整草稿 `warehouse=getattr(check, 'warehouse', None)`；`complete_adjustment`/`revert_adjustment` 守卫 P0-2 已补（commit `b2b9a94d`）。`scripts/verify_bug_2026_08_02_007.py` 9 项全绿（含 D1 库位关闭时 complete_transfer 不写 LocationInventory 但记流水、D2 盘点草稿带 warehouse），`verify_bug_2026_08_02_003.py`/`verify_adjustment_state_machine.py` 未回归 |
| BUG-2026-08-02-014 | 盘点单 `check.html` 新建弹窗和 `document_table_form.html` 编辑表单无仓库字段，违反 AGENTS.md 仓库必填规则 | `check_list` 路由补传 `warehouses`/`default_warehouse`；`_render_check_form` 补传 `warehouses`/`default_warehouse` 并在 header 带 `warehouse`；`check.html` 新建弹窗加 `<select name="warehouse" required>` 默认预选；`document_table_form.html` check 分支加仓库下拉+required+默认预选；`collectHeader()` check 分支带 warehouse；`refreshWarehouses()` 支持 check（commit `e6b6b772`） |
| BUG-2026-08-02-015 | `adjustment_add.html` 表单无仓库字段，`adjustment_add_page`/`adjustment_detail` 路由未传 `default_warehouse`，且 warehouses 查询未过滤启用状态 | `adjustment_add_page`/`adjustment_detail` 改用 `get_active_warehouses()` 并传 `default_warehouse=get_default_warehouse()`；模板在操作人旁补 `<select name="warehouse" required>` 编辑回填 `adjustment.warehouse`、新建预选默认仓库；`submitForm()` 增加仓库非空前端校验并写入 data.warehouse（commit `479bfcc7`） |
| BUG-2026-08-02-016 | `transfer.html` 新建弹窗和 `document_table_form.html` 调拨表单的调出/调入仓库下拉无 `required`、无默认预选；`_render_transfer_form`/`transfer_list` 未传 `default_warehouse` | `_render_transfer_form`/`transfer_list` 补传 `default_warehouse=get_default_warehouse()`；`transfer.html` 弹窗调出仓库预选默认仓库、调出/调入加 `*` 必填标记；`document_table_form.html` 调拨分支调出/调入 `<select>` 加 `required`、调出仓库新建时预选默认仓库（commit `fc60ddf8`） |
| BUG-2026-08-02-017 | `opening_stock.html` 新增期初库存单的仓库下拉已有 `required` 但未预选默认仓库；`opening_stock_list` 路由未传 `default_warehouse` | `opening_stock_list` 补传 `default_warehouse=get_default_warehouse()`；`opening_stock.html` 仓库 `<option>` 按 `default_warehouse.id == w.id` 预选（commit `5f4f95f9`） |
| BUG-2026-08-02-019 | P1-5 采购入库被设计为必须关联采购订单，与 AGENTS.md"采购订单仅作为可选来源"规则冲突 | 新增/保存/完成 `采购入库` 路径不强制校验 source_purchase_order_id；手工单可直接保存并完成入库；有关联 PO 时仍保留来源/数量/执行进度跟踪。回归 `tests/verify_bug_P15_P16_P21.py` TestBugP15PurchaseInOptionalPurchaseOrder 组 |
| BUG-2026-08-02-020 | P1-6 已完成入库单可通过详情页/列表/批量接口直接删除，违反"人工反提交→草稿→删除"规则 | `delete_in_order` 路由仅允许 pending 单删除，completed/partially_completed 返回 409 `已完成入库单禁止直接删除，请先反提交回草稿状态`；`batch_delete_in_order` 对已完成 ID 整批拒绝。详情页、列表页后端、接口统一规则。回归 `tests/verify_bug_P15_P16_P21.py` TestBugP16CompletedInOrderCannotDeleteDirectly 组 |
| BUG-2026-08-03-001 | 表头列向下填充按钮（`WmsFillDown.fillDown`，app.js）不跳过 `material_code` 空行，把合同编号/工程名称填到所有 15 行（含空行），与"向下填充行数应与物料明细行数一致"规则冲突；昨天 BUG-2026-08-02-021 只修了 Ctrl+D（`setupColumnFillDown`），表头按钮入口未修 | `app/static/js/app.js` `fillDown()` 在遍历源行之后的行时，先检查 `cellFor(row,'material_code')` 是否有值，空行 `skipped++` 跳过不填充，提示含"跳过 N 个空行"；仅在有 `material_code` 列的表格生效，不影响其他表格。回归 `tests/verify_bug_2026_08_03_001_filldown_skip_empty.py` |
| BUG-2026-08-03-002 | Ctrl+D 向下填充（`setupColumnFillDown`）与表头按钮（`WmsFillDown.fillDown`）是两套独立实现：① Ctrl+D 仅采购入库有，销售订单/采购订单/出库单无 Ctrl+D；② Ctrl+D 填数量/单价后不联动金额（`isAmount` 死代码，`calcAmount` 永不触发），按钮会联动；③ 两套实现易出现"修一处漏一处"的回归（正是 BUG-2026-08-02-021→001 的根因） | 将 Ctrl+D 统一到 `app/static/js/app.js` 的 `WmsFillDown` IIFE：新增 keydown 监听复用 `fillDown(table,key)`，所有页面自动获得 Ctrl+D；移除 `in_order_add.html` 的 `setupColumnFillDown`（避免双重触发）。两入口共用同一函数，行为永远一致（跳过空行、派发 input/change 联动金额、提示文案相同）。回归 `tests/verify_bug_2026_08_02_021_in_order_contract.py` T1-T2、`tests/verify_bug_2026_08_03_001_filldown_skip_empty.py` |
| BUG-2026-08-04-012 | 采购入库单详情页"下推"菜单三个链接（下推领料单/其他出库/售后出库）是普通 `<a href>` 锚点，点击后在当前标签页跳转到下推页，原采购入库单详情页被替换（界面关闭），用户无法在查看原单的同时进入领料单/出库单界面 | 给 `in_order_detail.html` 三个下推链接添加 `target="_blank" rel="noopener"`，在新标签页打开下推页，原采购入库单详情页保持打开。回归 `tests/verify_bug_2026_08_04_012_push_open_new_tab.py` T1-T3 |
| BUG-2026-08-04-013 | 复制物料时自动生成的物料编码只基于原物料编码递增，未按"原物料分类 + 流水号"生成 | `generate_material_copy_code()` 参数改为接收 `Material` 对象，优先取 `source.category.code` 作为前缀生成 `{分类编码}{三位流水号}`（补零到 3 位，如分类编码 `101` 已有最大编号 `101009` → 复制生成 `101010`），取该前缀下最大流水号 +1；原物料无分类时回退到原编码递增逻辑保持兼容。回归 `tests/verify_bug_2026_08_04_013_copy_material_category_code.py` T1-T5 |
| BUG-2026-08-04-014 | 采购订单新增页面保存使用原生非 GET `fetch` 提交，违反"所有非 GET 请求必须走 `WMS.api.*`"规则（预提交钩子、CSRF一致性可能不一致） | 迁移原生 `fetch` 为 `WMS.api.post`，复用统一错误处理和CSRF逻辑，保持业务功能不变。lint 规则 `scripts/lint_no_raw_post_fetch.py` 验证通过 |
| BUG-2026-08-04-016 | `/api/material/search` 返回的物料载荷缺少 `brand` 与 `price` 字段：`in_order_detail.html` 用该接口做物料快速搜索，`fillAddMaterialInfo()` 读取 `material.brand`/`material.price`，导致入库单详情页新增物料时品牌恒为空、单价恒为 0.00，覆盖了物料档案里已维护的品牌与价格 | `api_material_payload()` 补充返回 `brand` 与 `price` 字段（对 `/api/material/search` 与 `/api/material/info` 等所有调用方均为新增字段，向后兼容）。回归测试 `tests/verify_bug_2026_08_04_016_material_search_brand_price.py`（2 场景：搜索结果含 brand/price、info 含 brand/price） |

| BUG-2026-08-06-001 | `auto_migrate_database()` 在 Flask config 加载之前执行，且用硬编码路径 `app/instance/inventory.db` 检查数据库，与 SQLAlchemy 实际使用的数据库（由 `SQLALCHEMY_DATABASE_URI` 决定）不一致，导致老库启动时领料单 `out_order.picker` 等字段未被添加，访问领料单直接报 `no such column: out_order.picker` | `app/app.py:auto_migrate_database()` 将迁移调用移动到 config 加载之后，并新增纯函数 `_resolve_sqlite_db_path()` 从 `app.config['SQLALCHEMY_DATABASE_URI']` 解析真实 sqlite 路径（绝对/相对拼接/内存库/非 sqlite 均正确处理）。回归：`tests/test_auto_migrate_db_path.py` 5 用例全绿（绝对路径原样、相对路径拼接 instance_path、内存库返回 None、非 sqlite 返回 None、config 回退）。commit 见本次提交 |
| BUG-2026-08-07-001 | 7 个 AI 相关模板把后端/AI 返回字段直接拼入 `innerHTML`，存在存储型 XSS：`ai_sales_workbench.html`/`ai_warehouse_workbench.html`/`ai_purchase_workbench.html`（section.title/count/metric_scope、item.title/subtitle/detail/jump_url）、`sales_order_detail.html`（AI 异常分析 kind/message/summary/order_no/status）、`ai_business_quality.html`（指标 label、维度值、样本字段、版本对比）、`ai_supplier_evaluation.html`（ai_analysis、msg、supplier_code/name/contact 等）、`in_order_detail.html`（异常提醒 a.msg/a.ai_suggestion，更早一批修复） | 所有动态文本统一经 base.html 全局 `escapeHtml()` 转义；三个工作台跳转链接统一走 `safeJumpUrl()` 白名单（仅允许 `/` 或 `http(s)://` 开头，其余回退 `#`）。回归：`tests/verify_bug_2026_08_07_001_ai_templates_xss_escape.py` T1-T6（静态扫描 7 模板转义覆盖 + 未转义危险拼接模式清零） |
| BUG-2026-08-07-002 | pytest 全量跑时 `test_document_confirmation_golden.py`/`test_fix_db_columns.py`/`test_material_governance_golden.py` 3 个 golden 测试收集报 `ModuleNotFoundError: No module named 'app.ai'; 'app' is not a package`：其他测试模块先 `sys.path.insert(APP_DIR)` + `import app`（命中 app/app.py 模块，无 `__path__`），此后 `from app.fix_db_columns import ...` / `from app.ai... import ...` 包语义导入失败；单跑时 /workspace 在 sys.path，app 以 namespace 包加载故可过——结果依赖导入顺序，属测试隔离缺陷（与 BUG-2026-08-05-007 同类不同因） | `tests/conftest.py` 抢先以模块语义导入 app（与所有业务测试一致），再补 `__path__` 指向 APP_DIR，使 `import app.ai.xxx` / `import app.fix_db_columns` 沿该路径解析，模块/包两种语义共存。回归：`pytest tests/ -q` 139 passed 0 errors（修复前 3 collection errors） |
| BUG-2026-08-08-001 | 库存台账报表页 `report_view.html` 的物料搜索框是纯文本输入，无自动补全，用户输入编码/名称/规格关键词时无法快速选择物料，只能盲打后查询 | `report_view.html` 给物料搜索框新增 client 端自动补全下拉：加载 `/material/api/all`（分页拉全量）到 `allMaterialsForSearch`，`input` 事件按 编码/名称/规格 关键词过滤（`escapeHtml` 转义防 XSS），点选后回填物料编码并关闭下拉；不可用（加载失败）时静默降级为普通输入框。回归：`render_template('report_view.html', report_type='ledger', ...)` 断言 `materialDropdown`/`materialList`/`autocomplete="off"`/`/material/api/all?page=` 渲染均存在 |
| BUG-2026-08-08-002 | 报表页 `report_view.html` 的"客户 / 部门"输入框是纯文本输入，同页物料已有下拉、供应商有 datalist，唯独客户无补全，输入客户名称时无法快速选择，体验不一致 | `report_view.html` 给客户输入框新增 client 端自动补全下拉：加载 `/api/customers` 到 `allCustomersForSearch`，`input` 事件按 名称/编码 关键词过滤（`escapeHtml` 转义防 XSS），点选后回填客户名称并关闭下拉；无匹配时提示"可直接输入部门"并允许自由输入；加载失败静默降级。回归：`tests/verify_bug_2026_08_08_002_report_customer_autocomplete.py` T1-T4（`customerDropdown`/`customerList` 容器渲染、`autocomplete="off"`、`/api/customers` 加载、`loadCustomersForSearch`/`customerSearchTimeout` 过滤逻辑均存在） |
| BUG-2026-08-08-003 | 领料/其他出库单详情页 `out_order_detail.html` 的添加物料模态框 `#code` 与行内新增行 `.material-code-field` 仅支持精确编码盲查（`/api/material/info`），无下拉候选列表，输错或记不全编码时无法选择物料（入库单详情页 `in_order_detail.html` 早已有关键词下拉，两页体验不一致） | `out_order_detail.html` 移植 in_order_detail 的 quick-dropdown 方案：模态框 `#code` 与行内 `.material-code-field` 统一走 `/api/material/search?kw=` 关键词搜索（编码/名称/规格），渲染候选下拉（`escapeHtml` 转义），支持鼠标点选、↑↓ 键盘导航、Enter 选中、Esc/点外关闭；精确命中时自动回填名称/规格/单位/库存/价格。回归：`tests/verify_bug_2026_08_08_003_out_order_detail_material_quick_dropdown.py` T1-T4（下拉容器渲染、`/api/material/search?kw=` 调用、`renderMaterialQuickDropdown`/`selectAddMaterial`/`bindMaterialQuickInput` 函数、键盘导航与点外关闭逻辑均存在） |
| BUG-2026-08-08-004 | 委外单详情页 `subcontract_detail.html` 三个模态框编码输入框（添加产品 `#productCode`、发料 `#issueCode`、收货 `#receiveCode`）完全无 JS 绑定：无下拉、无回填，发料/收货模态框的"搜索"按钮（`openSearchBtn`/`openReceiveSearchBtn`）是死按钮，`issueName`/`issueSpec`/`issueStock`/`receiveName` 禁用字段永远不会被填充，用户只能盲打完整编码 | 复用模板已注入的 `subcontractMaterials` 全量数据做客户端关键词下拉：新增 `filterSubcontractMaterials()`（编码/名称/规格模糊匹配）与通用 `bindScMaterialInput()`（点选、↑↓/Enter/Esc 键盘导航、blur 关闭、`escapeHtml` 转义）；选中后产品码聚焦数量、发料回填名称/规格/库存、收货回填名称/单价；两个搜索按钮绑定 `show()` 展示候选。回归：`tests/verify_bug_2026_08_08_004_subcontract_detail_material_dropdown.py` T1-T5（三个下拉容器渲染、`autocomplete="off"`、过滤/绑定函数、选中回填、搜索按钮绑定均存在） |
| BUG-2026-08-08-005 | 老库仍报 `no such column: production_requisition.warehouse`（BUG-2026-08-05-008 已在模型 + `auto_migrate_database()` 补列，但个别未跑迁移的存量库在启动依赖 `fix_db_columns.py` 的路径下仍缺列，属 BUG-2026-08-05-008 的启动独立性兜底缺失） | `app/fix_db_columns.py` 的 `fix_columns()` 新增 `production_requisition.warehouse` 列迁移，并回填存量数据为默认仓库名（`warehouse.is_default=1 AND status='active'` 第一条，无默认仓则留空）；幂等（已存在则跳过）。回归：`tests/test_fix_db_columns.py` 3 个新用例（`test_fix_columns_adds_warehouse_to_production_requisition`/`test_fix_columns` 断言 `warehouse in pr_cols`/幂等重跑不报错），`pytest tests/test_fix_db_columns.py -q` 6 passed |
| BUG-2026-08-09-001 | Android App 扫码无法识别条码：`ScannerDialog.kt` 的 ML Kit 成功回调中 `scannedFlag.compareAndSet(false, true)` 无条件置位——ML Kit 对未识别到条码的帧同样回调成功（`barcodes` 为空列表），相机首帧（用户尚未对准，几乎必为空结果）即把 `scannedFlag` 置为 true；analyzer 入口 `if (scannedFlag.get()) { imageProxy.close() }` 随后丢弃所有后续帧，ML Kit 从此不再分析任何画面，相机预览与扫描线动画正常但永远识别不出条码 | `ScannerDialog.kt` 成功回调改为仅在 `!rawValue.isNullOrEmpty()` 时才 `compareAndSet` 置位并回调 `onBarcodeScanned`，空结果继续分析后续帧；补充 `addOnFailureListener` 记录识别失败日志（`android.util.Log.w`）便于现场诊断。回归：`tests/verify_bug_2026_08_09_001_scanner_dialog_flag.py` T1-T4（修复模式存在、旧无条件置位模式清零、rawValue 非空守卫、failure listener 存在），并用 `git show HEAD` 旧代码反向验证该测试必然失败 |
| BUG-2026-08-09-002 | 父单 BUG-2026-08-09-001 修复后扫码仍报"摄像头不可用 / Cannot use both setTargetResolution and setTargetAspectRatio on the same config."：`ScannerDialog.kt` 的 `ImageAnalysis.Builder()` 同时调用 `setTargetAspectRatio(AspectRatio.RATIO_16_9)` 和 `setTargetResolution(android.util.Size(1280, 720))`，CameraX 1.3+ 这两个 API 互斥，构造阶段即抛 `IllegalArgumentException`，`bindToLifecycle` 失败，预览区显示"摄像头不可用"，ML Kit 永远收不到任何帧。叠加国产机相机 HAL 碎片化，1280x720 在很多设备上压根不被支持，崩溃概率极高 | 改用 CameraX 1.3+ 官方推荐 `ResolutionSelector` 写法：`AspectRatioStrategy.RATIO_16_9_FALLBACK_AUTO_STRATEGY`（16:9 优先，硬件不支持时自动回退 4:3）+ `ResolutionStrategy.HIGHEST_AVAILABLE_STRATEGY`（最高可用分辨率，国产机多摄自动适配）。同时清掉死代码 `import androidx.camera.core.AspectRatio`。回归：`tests/verify_bug_2026_08_09_002_camerax_resolution_selector.py` T1-T4（3 个 ResolutionSelector import 在场、死 AspectRatio import 已清、`.setTargetAspectRatio()`/`.setTargetResolution()` 调用清零、`ResolutionSelector.Builder()` 链式配置 16:9 + 最高可用并接入 ImageAnalysis），旧代码反向验证 8 项断言全数生效 |
| BUG-2026-08-09-003 | "识物盘点"页点"拍照"按钮 app 自动退出：`AiScreens.kt` 的 `DocumentOcrScreen`/`ObjectRecognizeScreen`/`StocktakeRecognizeScreen` 三个屏幕的"拍照"按钮直接 `cameraLauncher.launch(null)` 启动 `ActivityResultContracts.TakePicturePreview()`，但①未请求 Android 6+ 运行时 `CAMERA` 危险权限，未授权时系统相机会立即抛 `SecurityException` 直接闪退（manifest 仅声明 `<uses-permission>` 无任何运行时 grant 入口）；②拍照回调里 `MediaStore.Images.Media.insertImage(...)` 在 API 29+ 已废弃且因 scoped storage 受限可返回 `null`，紧接着 `android.net.Uri.parse(null)` 抛 `NullPointerException` 第二次闪退（用户拍完照片返回也会崩）。两路径叠加 → "点拍照自动退出 app" | ①封装 `LaunchCameraWithPermission` Composable helper（`AiScreens.kt` 内部 `private` 函数），先通过 `ActivityResultContracts.RequestPermission()` 请求 `Manifest.permission.CAMERA`，授权后才 `cameraLauncher.launch()`；拒绝时通过 `Snackbar` 提示"请授予相机权限后重试"并附"去设置"跳转 `Settings.ACTION_APPLICATION_DETAILS_SETTINGS`；②回调里改写 `saveBitmapToCacheAndGetUri(bitmap, prefix)`：先把 `Bitmap` 用 `Bitmap.compress(JPEG, 90)` 写入 `context.cacheDir/camera/{prefix}_{ts}.jpg`，再 `FileProvider.getUriForFile(...)`（新增 `file_paths.xml` 仅暴露 `cache-path`）暴露成 `Uri`，`Uri.parse(null)` 路径彻底消除；③三个 `cameraLauncher` 全部走同一 helper，删除 3 处重复实现。回归：`tests/verify_bug_2026_08_09_003_takepicture_permission_and_path.py` T1-T7（FileProvider provider/authority 在 manifest、`file_paths.xml` 含 `cache-path`、3 个 launcher 全部经 helper、`cameraLauncher.launch(null)` 裸调用清零、`Uri.parse(insertImage(...))` 旧写法清零、`LaunchCameraWithPermission` helper 存在并用 `RequestPermission`、回调用 `saveBitmapToCacheAndGetUri`），旧代码反向验证 7 项断言全数生效 |
| BUG-2026-08-09-004 | "识别单据"页打开即报错 "网络错误: Failed to connect to /127.0.0.1:5000"：`AiScreens.kt::DocumentOcrScreen` 的 `LaunchedEffect(Unit) { viewModel.loadWarehouses() }` 在页面挂载瞬间即触发 `getWarehouses()` 请求，而仓库下拉仅在 OCR 识别完成展示物料行后才需要；尚未识别单据、未选仓库前提前发请求，未配置 baseUrl 时（默认 fallback `http://127.0.0.1:5000/`）立即 ConnectException，错误经 `LoadWarehouses onFailure` → `_uiState.error` → Snackbar 弹给用户，体感"页面一打开就报错"（截图：snackbar 文字"网络错误: Failed to connect to /127.0.0.1:5000"，页面状态为初始空态而非 OCR 流程中） | **已修复**。把 `loadWarehouses()` 调用从 `LaunchedEffect(Unit)` 整体后移到 `LaunchedEffect(uiState.ocrResult != null) { if (uiState.ocrResult != null) viewModel.loadWarehouses() }`，仅在 OCR 成功且 `ocrResult.items` 非空（即确认生成草稿的仓库下拉要展示）时才请求仓库列表，未识别阶段不再发请求也不会再展示误导性网络错误。`loadWarehouses()` 自身的 `if (warehouses.isNotEmpty() \|\| isLoading) return` 守卫保留，幂等性不变。同类屏幕核查：`ObjectRecognizeScreen`/`StocktakeRecognizeScreen` 仅在 `LaunchedEffect(uiState.error)` 监听错误，无 `loadWarehouses()` 调用，不受此 BUG 影响。回归：`tests/verify_bug_2026_08_09_004_loadwarehouses_after_ocr.py` T1-T4 4/4 PASSED（初始挂载 `LaunchedEffect(Unit)` 内不再裸调 `loadWarehouses`、新的 `LaunchedEffect(uiState.ocrResult != null)` 内条件调用、其他 AI 屏幕无裸 `loadWarehouses` 引入、旧写法反向验证 4 项断言全数生效）。commit `83eee4e0` |
| BUG-2026-08-09-005 | "识物盘点"页 `submitStocktake()` 缺仓库必填校验：`ScanViewModel.kt::submitStocktake` 读取 `state.scanLines` 直接提交，未检查 `state.selectedWarehouse`，用户未选仓库时仍可发起请求；后端会因缺 warehouse 拒绝但前端无任何拦截提示，体感"点了没反应" | `ScanViewModel.kt::submitStocktake` 入口先取 `state.selectedWarehouse`；若为 `null`，写 `_uiState.value.copy(error="请选择仓库")` 并 `return@launch`；同时 `StocktakeRequest` 数据类增加 `warehouse: String?` 与 `@SerializedName("warehouse_code") warehouseCode: String?` 两个字段，请求体里同步带上 `warehouse.code` 兜底服务端。`WmsRepository.submitStocktake` 透传新字段，`WmsApiService.submitStocktake` `@POST` `@Body StocktakeRequest` 适配新结构。回归：`tests/verify_p1_a_stocktake_warehouse_required.py` T1-T4 4/4 PASSED（前置 `selectedWarehouse == null` 守卫存在、错误消息"请选择仓库"、StocktakeRequest 含 warehouse/warehouseCode 字段、scanLines 空守卫"请先扫描盘点物料"）。commit `5307e791` |
| BUG-2026-08-09-006 | `DocumentOcrResult` 模型自我引用：`WmsApiService.kt::DocumentOcrResult` 把 `extracted` 字段类型直接定义成 `DocumentOcrResult?` 自身（`val extracted: DocumentOcrResult?`），Gson 解析任何带 `extracted` 字段的 OCR 响应会陷入无限递归 `new DocumentOcrResult(new DocumentOcrResult(...))`，栈溢出崩溃 `StackOverflowError`，整个 AI 识别单据 OCR 接口全挂 | 新建独立 `ExtractedDocument` 数据类承接 `extracted` 字段（含 `documentType/supplier/orderNo/purchaseOrderNo/date/items/remarks`）；`DocumentOcrResult.extracted` 改为 `ExtractedDocument?`；`ScanRequests.kt` 同步删除（合并到 `WmsApiService.kt`）；其余 `document_type/supplier/order_no/date/items/remarks/reply/matches/match_count` 顶层字段语义不变。回归：`tests/verify_p1_c_document_ocr_extracted_type.py` T1-T4 4/4 PASSED（DocumentOcrResult.extracted 类型为 ExtractedDocument 而非 self、新 ExtractedDocument 类独立存在、ExtractedDocument 字段映射正确、构建合法 DocumentOcrResult 不爆栈）。commit `6d49d096` |
| BUG-2026-08-09-007 | Android 拍照临时文件长期驻留 cacheDir：`WmsApplication` 仅在每次拍照时写 `cacheDir/camera/{prefix}_{ts}.jpg`，但没有任何清理机制；BUG-2026-08-09-003 修复后 FileProvider 改为写入 cacheDir，使用越久 cacheDir 越大，可触发 `IOException: No space left on device` 导致后续拍照失败，且旧的临时 JPEG 文件长期堆积存在隐私残留（图片未及时清理） | `WmsApplication.onCreate()` 在 `super.onCreate()` 后调用 `cleanupStaleCameraCache()`：遍历 `cacheDir/camera/` 目录所有 `isFile`，按 `lastModified() < now - 24h` 判定为陈旧文件，调用 `f.delete()` 删除并累计 `cleanedCount/cleanedBytes`；`Log.i` 输出清理摘要便于现场排查；整个清理流程包在 `try/catch` 内，删除失败/目录不存在/IO 异常均 `Log.w` 警告而不阻塞应用启动。回归：`tests/verify_p2_a_camera_cache_cleanup.py` T1-T5 5/5 PASSED（WmsApplication.onCreate 调 cleanupStaleCameraCache、cutoff = 24 小时、只删文件不递归子目录、累加 cleanedCount/cleanedBytes、try/catch 不抛异常）。commit `816aad25` |
| BUG-2026-08-09-008 | `ApiEnvelope.displayMessage()` 直接吐后端 `message/msg` 字段无任何过滤：①未限制长度，后端 `msg` 字段可塞入 5000+ 字符堆栈或大段 HTML 导致 OOM/UI 错位；②未做 HTML 标签过滤，存储型 XSS 风险——后端报错信息若被攻击者构造包含 `<script>alert(1)</script>`，前端 Snackbar/Dialog 用 `Text(...)` 渲染虽不会执行，但用 `Html.fromHtml` 渲染的对话框或部分文本组件会被注入；③多种 entity 编码（`&lt;`/`&gt;`/`&amp;`/`&quot;`）未做反转，错误信息里出现的 `<`/`>` 显示为字面量，用户可读性差 | `ApiEnvelope.kt::displayMessage` 改为调内部 helper `sanitizeMessage(raw)`：①`trim()` 后 `Regex("<[^>]+>")` 去除所有 HTML 标签（包括 `<script>`/`<img onerror=...>` 等）；②反转实体编码 `&lt;`→`<`、`&gt;`→`>`、`&quot;`→`"`、`&#39;`→`'`、`&apos;`→`'`、`&amp;`→`&`；③反转后再 `Regex("<[^>]+>")` 二次过滤防 `&lt;script&gt;` 嵌套绕过；④长度超过 `MAX_DISPLAY_MESSAGE_LENGTH=200` 则 `substring(0,200)+"..."` 截断；空消息兜底为"操作成功"/"操作失败"。companion object 暴露 `MAX_DISPLAY_MESSAGE_LENGTH` 常量供测试断言。回归：`tests/verify_p2_d_displaymessage_sanitize.py` T1-T6 6/6 PASSED（`<script>` 标签去除、`&lt;` 反转、长度 200+ 截断、companion 常量 200、空消息兜底、displayMessage 调 sanitizeMessage）。commit `19252289` |
| BUG-2026-08-09-009 | 登出后加密 SharedPreferences 异步清空导致 token 残留窗口：`WmsRepository.kt::logout()` 使用 `encryptedPrefs.edit().clear().apply()`，而 `apply()` 是异步写盘；点击登出到下一次 process kill 之间若立即有请求，token 仍可被读取；更严重的是 `apply()` 在低内存/快速退出场景下可能未落盘就被回收，下次启动 token 仍在加密 prefs 中，相当于"登出失败"；普通 `dataStore.edit { it.clear() }` 同步但与 EncryptedSharedPreferences 不同步，导致 token/普通偏好之间状态不一致 | `WmsRepository.kt::logout` 第一步先 `try/catch` 包 `encryptedPrefs.edit().clear().commit()`（同步阻塞写盘，保证 token 立即清除并落盘）；catch 分支 `Log.w("WmsRepo", "清空加密 prefs 失败: ${e.message}")` 记录失败原因但不再向上抛（避免登出流程半途中断）；再走 `context.dataStore.edit { it.clear() }` 清空普通偏好；最后 `RetrofitClient.setToken(null)` 通知 OkHttp 拦截器移除内存中的 token。三步顺序固定：加密 prefs → DataStore → 内存 token。回归：`tests/verify_p2_e_logout_sync_commit.py` T1-T5 5/5 PASSED（encryptedPrefs.edit().clear() 用 commit 同步、`dataStore.edit` 清空、RetrofitClient.setToken(null) 通知、commit 在 try/catch 内防抛、顺序正确）。commit `8ca4eaf1` |
| BUG-2026-08-09-010 | 手机 WMS 语音"正在聆听"仍卡死到兜底超时：BUG-2026-08-09-003 的 8s 兜底 + sherpa 离线引擎在默认构建（`-Pwms.sherpa` 未开，模型未打包）下不可用，而国内设备无 Google 服务时 `AndroidVoiceSttEngine`（`SpeechRecognizer`）既不回调 `onPartial` 也不回调 `onError`，用户点麦克风后只能干等 8s"识别超时"，语音指令名存实亡 | 选择"接云服务器 + 走后端中转"方案：后端新增 `/${mobile/api/asr}` 路由（`app/routes/mobile.py`，读 `TENCENTCLOUD_SECRET_ID/TENCENTCLOUD_SECRET_KEY/TENCENTCLOUD_REGION` 环境变量，`app/tencent_asr.py` 手工 TC3-HMAC-SHA256 签名调腾讯云一句话识别 16k_zh，音频格式/大小校验，错误归类 400/500/502）；Android 端新增 `CloudAsrVoiceSttEngine`（AudioRecord 16kHz mono PCM16 录音 → WAV 封装 → `WmsApiService.asrAudio` multipart 上传 → 解析 `{status,text}` → 回调 `onResult`），`WmsApiService` 增 `asrAudio` + `AsrResult` 模型（扁平结构非 ApiEnvelope），`VoiceSttEngineRegistry.defaultSelector` 改为"云引擎优先 → sherpa → Android 系统识别"。关键点：`CloudAsrVoiceSttEngine.destroy()` **不取消上传协程、不清空 listener**，因 ViewModel 在 `stop()` 后立即 `destroy()`，异步上传结果需在 destroy 后仍能回调给共享 `engineListener`。回归：`tests/verify_tencent_asr_helper.py`（8 用例）与 `tests/verify_mobile_asr_route.py`（8 用例）全绿。Android 端 `assembleDebug` 需在有 Android SDK + JDK17 的构建机执行（本沙箱无 SDK 且 JDK25 与 AGP8.x 不兼容，无法本地编译验证）。commit 见本次提交 |
| BUG-2026-08-10-001 | 移动端"识别单据"页调用 `POST /api/ai/document_ocr` 报 `请求已过期或缺少安全令牌，请刷新页面后重试`（CSRF 400）：该 AI 端点未纳入移动端 CSRF 豁免名单，与 `/api/ai/*` 其他端点不一致；移动端原生 APK 用 Bearer Token 鉴权但仍要走 Flask-WTF CSRF 校验（POST 表单必带 token），导致"识别单据"功能在未带 token 时必 400 | `app/app.py:CSRFProtect.exempt(...)` 新增 `/api/ai/document_ocr` 加入移动端 CSRF 豁免名单；与 `api_chat_stream`/`api_chat_message` 等其他 `/api/ai/*` 移动端端点策略一致。回归：`tests/verify_ai_chat_stream_endpoint.py` CSRF 豁免名单断言补充 `api_document_ocr` |
| BUG-2026-08-10-002 | 移动端"物料档案"页 `mobile_material_archive_images` / `mobile_material_archive_upload` 端点 `OperationalError: no such table: material_image` 500：material_image 表在 `WMS_SKIP_STARTUP_DB_UPGRADE=1` 或 `WMS_NO_DB_TOUCH=1` 启动场景下不会被 `db.create_all()` 自动创建，提交时也无 `_ensure_material_image_table()` 兜底；唯一有 try/except 兜底的 `_archive_material_payload` 不会被这两个专用端点复用 | `app/routes/mobile.py` 抽出 3 个 helper：`_has_material_image_table()`（`sqlalchemy.inspect` 探测）、`_safe_material_image_count(id)` / `_safe_material_image_list(id)`（缺表回退 0/[]，与 `_archive_material_payload` 原行为一致）、`_ensure_material_image_table_inline()`（缺表时用 `MaterialImage.__table__.create(db.engine, checkfirst=True)` 跨方言补建，失败 `current_app.logger.warning`）。`mobile_material_archive_images` 改调 `_safe_material_image_list(id)`；`mobile_material_archive_upload` 先调 `_has_material_image_table()` 缺表则 `_ensure_material_image_table_inline()`，仍失败返 500 `"物料档案表未就绪，请稍后重试或联系管理员"`，成功路径用 `_safe_material_image_count(id)` 拿当前数量。回归：`tests/verify_mobile_material_archive_api.py` 12/12 PASSED（含 `test_search_when_material_image_table_missing` 表缺失搜索 200/0、T3-T6 upload/list/delete 正常、T1 endpoint 注册、T7-T8 401/Bearer 鉴权、T9-T10 主图同步/回退） |
| BUG-2026-08-11-001 | 手机 app 登录 `/api/login` 报 502：`start_wms_auto.bat` / `start_wms_offline.bat` 均设 `WMS_NO_DB_TOUCH=1`，导致 `auto_migrate_database()` 和 `db.create_all()` 全被跳过，老数据库可能缺 user 表登录安全列（`login_lock_ip`/`login_ip_failed_count`/`login_ip_locked_until`/`must_change_password`/`last_login_at`/`last_login_ip` 等）、`api_token` 表或 `login_log` 表，`/api/login` 内无兜底 try/except，任何 `OperationalError` 直接 500 → nginx 502 | `app/routes/native_api.py` 新增 `_ensure_login_schema()` 函数：在 `/api/login` 路由开头检查并补建缺失的 `api_token`/`login_log` 表及 user 表登录安全列、`api_token.last_used_at` 列、`login_log.fail_reason` 列；整个 login 函数体包 try/except 兜底，异常时 `db.session.rollback()` + 记日志 + 返回 JSON 500 而非裸 500。回归：`tests/verify_app_py_split_native_api.py` 6/6 PASSED（含新增 `test_login_when_tables_missing`：DROP api_token+login_log 表后登录仍 200 返回 token） |
| BUG-2026-08-11-002 | 手机 app 拍照识物崩溃 `kotlin.Unit cannot be cast to com.factory.wms.data.api.RecognizeMaterialResult`：后端 `/mobile/api/recognize_material` 把 `reply/extracted/matches/match_count` 平铺在响应顶层，而 Android 端 `ApiEnvelope<RecognizeMaterialResult>` 期望这些字段包在 `data` 内；Gson 反序列化时 `data=null`，`handleResponse` 的 `Unit as T` 兜底把 `Unit` 强转成 `RecognizeMaterialResult` 抛 ClassCastException | `app/routes/mobile.py` 识物接口响应改为标准信封：`data: {reply, extracted, matches, match_count}`，与 `WmsApiService.recognizeMaterial` 的 `ApiEnvelope<RecognizeMaterialResult>` 契约对齐。commit `b5183f67`（2026-08-11 补登台账） |
| BUG-2026-08-11-003 | RetrofitClient 两项隐患（2026-08-11 APK 全量审计发现）：①`buildRetrofit()` 在 baseUrl 为空时静默 fallback 到硬编码域名 `https://gd2026.top/`，baseUrl 未配置时 API 请求（含 Authorization token）会被发往该默认服务器；②object 单例的 `baseUrl`/`authToken`/`retrofit` 可变字段无并发保护，`setBaseUrl` 重建 retrofit 期间其他线程可能读到半更新中间态 | ①`buildRetrofit(url)` 空 url 改用不可达占位 `http://127.0.0.1:9/`（discard 端口），`apiService` getter 在 baseUrl 为空时 `check()` 抛错快速失败，占位地址永远收不到请求更不携带 token；②三个可变字段全部 `@Volatile`，`setBaseUrl` 在 `synchronized(lock)` 内先构建新 retrofit 再发布引用（先构建后发布）。回归：`tests/verify_bug_2026_08_11_003_retrofit_client_hardening.py` T1-T6 6/6 PASSED（无 gd2026.top 残留、空 url 占位、getter 空值抛错、@Volatile 覆盖、synchronized、先构建后发布） |
| BUG-2026-08-11-004 | `WmsRepository.submitOpeningStock` 成功分支 `val msg = submitResult?.let { "期初库存已保存" } ?: "期初库存已保存"` 两分支返回相同字符串，`?.let` 永走非空分支属无意义冗余，且对非空类型做可空解包有误导性（2026-08-11 APK 全量审计发现） | 删除冗余 `msg` 变量，成功分支直接 `Result.success("期初库存已保存")`；`opening_stock` 操作日志写入逻辑不变。回归：`tests/verify_bug_2026_08_11_004_opening_stock_msg_cleanup.py` T1-T3 3/3 PASSED（无冗余解包、固定提示、日志保留） |
| BUG-2026-08-11-006 | 手机 app 拍照识物崩溃 `kotlin.Unit cannot be cast to com.factory.wms.data.api.RecognizeMaterialResult`（用户截图报错）：`WmsRepository.handleResponse<T>` 在 envelope success 但 `data==null` 时**无条件**执行 `Result.success(Unit as T)`；reified 泛型下 `Unit as T` 是真实 checkcast，T=RecognizeMaterialResult 时抛 ClassCastException，被外层 catch 包装成 "网络错误: kotlin.Unit cannot be cast..."。触发条件为服务端 success 响应缺 data（旧版本服务端未含 BUG-2026-08-11-002 修复、代理丢 body 等）。与 BUG-2026-08-11-002（服务端响应补 data）是同一症状的两端：002 修服务端，本条修 App 端兜底 | `handleResponse` 的 `Unit as T` 兜底加 `T::class == Unit::class` 守卫：仅删除类接口（`ApiEnvelope<Unit>`）允许 success 无 data 回填 Unit；其他类型 data 缺失时返回 `Result.failure(Exception(envelope.displayMessage()))`，UI 展示服务端 msg 而非 ClassCastException 文本。回归：`tests/verify_bug_2026_08_11_006_handle_response_unit_cast.py` T1-T4 4/4 PASSED（Unit 守卫存在且在 cast 之前、非 Unit 兜底返回 failure 不含 success、failure 透出 displayMessage、后端 recognize_material success 响应含 data 四字段防服务端回归） |
| BUG-2026-08-11-007 | 前端界面大面积英文文案，中文用户看不懂：①AI Agent 任务页把 `warehouse_patrol`/`pending`/`completed` 等英文枚举码和 Agent 硬编码英文整句（"Warehouse daily patrol: ..."、"Negative stock: 2; low stock: 5." 等目标/步骤/数据范围/结果摘要/操作标签）直接渲染；②AI 文档任务页 `ocr_upload`/`pending_confirmation`/`matched`/`helpful`/`ocr_error` 等枚举码裸露；③AI 运维看板灰度范围 `allowlist`、运行状态、工具名英文；④AI 验收页结论 `go/no_go`、灰度角色英文；⑤AI 业务质量页"Schema版本"；⑥10+ 单据详情页（入库/出库/销售/委外/采购申请/售后/审批/操作审计/首页）状态枚举 `{% else %}` 兜底分支直接输出英文原值；⑦打印页 `RECEIPT`/`MATERIAL REQUISITION`、报表中心 `Report Center`、登录页 `WMS Cloud`、物料页 `ALL` 等英文标题残留；⑧base.html 面包屑 `aria-label="breadcrumb"` 等无障碍标签英文 | 双管齐下：新数据源头中文化 + 历史英文数据渲染层翻译。①`app/app.py` 三个 Agent 函数（warehouse_patrol/purchase_followup/sales_followup）改为直接以中文写入任务目标、步骤名、数据范围、结果摘要、操作标签；②`app/app.py` 新增 `ai_agent_text`（固定整句映射 + 含数字正则句型 + 片段替换）、`ai_agent_label`、`ai_doc_label` 三个 Jinja 过滤器及配套映射表（agent_type/task_status/step_status/risk/tool、source/doc_type/job_status/match_status/rating/error_type/run_status/rollout_mode）；③12 个 AI 模板（ai_agent_tasks/ai_agent_task_detail/ai_document_jobs/ai_document_job_detail/ai_ops_dashboard/ai_acceptance/ai_business_quality/ai_prelaunch 等）应用过滤器；④10+ 详情页状态兜底统一显示"未知"；⑤打印/报表/登录/物料页英文标题改中文；⑥base.html aria-label 中文化；⑦`_disabled_unused_20260506/bom_detail.html` aria-label="Close"→"关闭"。回归：`tests/verify_bug_2026_08_11_007_ui_chinese.py` T1-T10 10/10 PASSED（过滤器注册、Agent 中文写入、12 模板过滤器应用、验收页通过/不通过、结构版本、状态兜底"未知"、运行时过滤器行为、全模板英文残留静态扫描 0 残留） |
| BUG-2026-08-11-008 | 微信分享直推模式必然 403：`_wechat_share_send_image` POST 到助手 `/send` 时未携带 `X-Wechat-Helper-Token` 请求头，而助手端 `_check_auth` 强制校验该 token（token 未配置也一律拒绝），导致默认配置（helper_url=http://127.0.0.1:8765/send）下每次分享都被助手 403 拒绝、状态记为 failed；同时 helper_url 可配置成任意外网地址，一旦携带 token 会明文泄露给第三方主机 | `app/app.py` 新增 `_wechat_share_helper_url_allowed()`（仅允许 http/https + 127.0.0.1/localhost/::1 回环地址）；`_wechat_share_send_image` 直推时携带 `X-Wechat-Helper-Token: app.config['WECHAT_HELPER_TOKEN']`，token 未配置时拒绝直推并提示，非回环地址直接拒绝不发请求；`app/routes/wechat_share.py` 保存配置路由同步校验 helper_url 回环，非法地址尽早报错。回归：`tests/verify_bug_2026_08_11_008_wechat_push_token.py` 6/6 PASSED（T1 直推携带 token、T2 非回环拒绝且零请求、T3 token 缺失拒绝且零请求、T4 保存路由校验、回环/非法地址单测） |
| BUG-2026-08-11-009 | 微信发送助手并发不安全且无发送环境校验：①ThreadingHTTPServer 每请求一线程，但剪贴板/键盘输入/前台焦点是全局资源，并发 /send 互相抢焦点、串剪贴板，会发错图发错人；②锁屏/远程桌面断开/弹窗抢焦点时 SendInput 把 Ctrl+V/回车打进别的窗口；③失败只返回自由文本 msg，WMS 侧只能靠关键词匹配判定状态，脆弱；④先写剪贴板后校验接收人，无谓副作用 | `app/wechat_helper.py`：新增全局 `SEND_LOCK`，`send_image_task` 全程持锁串行化；新增 `_SendError` 机器可读错误码体系（no_receiver/clipboard_failed/wechat_window_not_found/open_contact_failed/paste_failed/send_key_failed/focus_lost），`do_POST` 与 poll 回报均携带 `code` 字段；新增 `_ensure_foreground()` 在激活后/打开会话后/回车发送前三处校验微信主窗口仍在前台，失焦立即中止；接收人校验提前到写剪贴板之前，失败零 UI 副作用。回归：`tests/verify_bug_2026_08_11_009_helper_robustness.py` 5/5 PASSED（T1 持锁、T2 三处前台校验、T3 错误码体系、T4 校验顺序、T5 调用方三元组解包；Windows-only 模块按仓库惯例静态验证）；另补 `tests/test_wechat_helper_send_image_task.py` 6/6 PASSED（A9 命名合规单测：ready/sent/error 三路径、接收人回退链、SEND_LOCK 串行化），commit `cf640084` |
| BUG-2026-08-11-010 | WMS 侧微信分享结果判定靠关键词匹配，脆弱且无重试策略：`_wechat_share_send_image` 只返回 (sent, message)，丢弃助手端机器可读 code，`_wechat_share_status_from_send_result` 用"失败/错误/http"等关键词猜测 failed/pending——新错误文案漏配关键词即误判为 pending；且连接级错误（助手重启窗口期 ConnectionError）一次失败即记 failed 无任何重试，而读超时若盲目重试又会因请求已被助手执行而重复粘贴/发送 | `app/app.py`：`_wechat_share_send_image` 改返回结构化三元组 `(status, code, message)`，status ∈ sent/pending/failed 直接落库，code 涵盖 ok/helper_not_configured/invalid_helper_url/token_not_configured/helper_offline/helper_timeout/auth_failed/send_error/http_<n> 及助手端透传码；仅 ConnectionError（请求未到达助手、零副作用）自动重试 1 次，Timeout 明确不重试防重复发送；HTTP 403 映射 auth_failed；删除关键词匹配函数 `_wechat_share_status_from_send_result`，`_wechat_share_order` 与 `routes/wechat_share.py` resend 路由同步改造，failed 时 message 附错误码便于排查。回归：`tests/verify_bug_2026_08_11_010_wms_result_mapping.py` 8/8 PASSED（code 透传、ready→pending、连接重试恰好 2 次、超时不重试仅 1 次、403→auth_failed、持续离线 helper_offline、未配置零请求、落库状态+错误码）；008/拆分回归同步全绿 |
| BUG-2026-08-11-011 | 微信分享定时任务可重复发送：`run_due_wechat_share_jobs` 调 `run_wechat_share_for_today(force=True)` 绕过"今日已有 pending/sent 记录即跳过"的去重检查，scheduler 每分钟触发一次，同一分钟内 misfire 补跑或服务重启会对同一入库单重复生成图片并重复直推微信（接收人收到多张相同分享图）；且每次触发无条件写 in_order_daily marker，重复触发产生多条 marker | `app/app.py`：定时任务改用 `force=False` 尊重去重语义；执行前检查今日 scheduled marker，已存在则该 config 整日跳过，每日最多执行一次、marker 恰好一条。回归：`tests/verify_bug_2026_08_11_011_scheduled_dedup.py` 4/4 PASSED（已有 pending 跳过且零文件生成、已有 marker 整体跳过、同分钟双触发仅 1 条 marker、源码 force=False 静态断言） |
| BUG-2026-08-11-012 | 分享页每次打开都同步 GET 助手 `/health`（timeout=1.5s），助手离线时每次页面加载被卡住约 1.5s，连续刷新/多标签打开体验差 | `app/app.py` 新增模块级 `_WECHAT_SHARE_HEALTH_CACHE`：按 health_url 缓存健康检查结果 30s，30s 内重复调用直接命中缓存不再发起 HTTP 请求；地址未配置时同步清空缓存避免换配置后命中旧结果。回归：`tests/verify_bug_2026_08_11_012_helper_health_cache.py` 4/4 PASSED（30s 内仅 1 次请求、超 30s 过期重查、换地址不命中、未配置零请求且清缓存） |
| BUG-2026-08-11-013 | 微信分享图片每天生成且永久累积在 `output/wechat_share/`，无任何清理机制导致目录无限膨胀；超期图片对应日志仍保留 image_path，列表页出现必然 404 的下载链接 | `app/app.py` 新增 `_wechat_share_cleanup_old_images(retention_days=30)`：仅删除符合分享命名规则（`YYYYMMDD_HHMMSS_*.png`）且 mtime 超期的文件，其它文件不动，并同步清空对应 `WechatShareLog.image_path/image_size`；新增 `_wechat_share_cleanup_old_images_daily()` 每日守卫，挂入 `run_due_wechat_share_jobs`（每分钟 scheduler）每日执行一次。回归：`tests/verify_bug_2026_08_11_013_image_retention.py` 5/5 PASSED（超期删除+日志解绑、30 天内保留、非分享命名文件不动、每日守卫同日仅一次、scheduler 挂接静态断言） |
| BUG-2026-08-11-014 | 重发微信分享用当前配置接收人而非分享时冻结接收人：列表"接收人"列展示的是 `log.receiver_*`（分享时冻结），`resend_wechat_share_log` 却用 `config.receiver_*` 直推，配置中途修改后点重发会悄悄发给另一个人，与页面展示不一致 | `app/routes/wechat_share.py` resend 路由：日志存在冻结接收人时构造 `SimpleNamespace` 发送快照覆盖 `receiver_name/receiver_wechat_id/receiver_search_key`（search key 取冻结值），message 标注"按历史接收人重发"；无冻结接收人时回退当前配置。回归：`tests/verify_bug_2026_08_11_014_resend_frozen_receiver.py` 3/3 PASSED（冻结接收人优先、无冻结回退配置、message 标注提示） |
| BUG-2026-08-11-015 | 微信分享页缺少三处界面引导：①用户不清楚「直推」（WMS 生成图片后立即 POST 助手）与「轮询」（助手 `WMS_WECHAT_HELPER_POLL=1` 定时拉取）两种发送模式的区别，助手地址字段无任何说明；②`auto_send`（自动点击发送）勾选框无风险提示，开启后助手粘贴图片直接点微信「发送」，全程无人工确认、消息发出无法撤回，用户不了解风险即开启；③「待发送」记录（图片已生成未进微信）无消化路径引导，助手未开轮询时 pending 一直堆积用户不知如何处理 | `app/templates/wechat_share.html`（纯模板）：①助手地址字段下 form-text 说明直推/轮询两模式及回环地址限制；②新增 `#autoSendRiskHint` 警示条（无人工确认/无法撤回/确认窗口停留目标聊天），auto_send 未勾选默认隐藏、勾选时 JS change 监听实时显示；③`log_counts.pending > 0` 时状态卡片显示消化引导（轮询自动拉取或列表逐条重发），并按助手在线/轮询状态追加「不会被自动消费」或「请先启动助手」提示。回归：`tests/verify_bug_2026_08_11_015_share_page_guidance.py` 4/4 PASSED（模式文案存在、警示条显隐随配置切换、pending 引导按需显示、在线未轮询/离线两种提示分支） |
| BUG-2026-08-11-016 | 物料管理图片无法打开：默认上传目录为工作目录下的 `uploads/material_images`，数据库保存的相对路径却由 Flask `/static` 路由解析到 `app/static/uploads/material_images`，导致新上传图片和已有 39 张历史图片均返回 404 | `app/config.py` 将默认 `UPLOAD_FOLDER` 指向 `app/static/uploads`；`app/utils.py` 启动时幂等复制旧 `uploads/material_images` 中目标不存在的文件，不覆盖静态目录已有文件；回归：`tests/test_material_image_static_path.py` 验证新上传和历史图片均通过静态路由返回 200，相关测试 14 passed，A1-A10 0 违规 |
| BUG-2026-08-11-017 | 手机 APP 物料档案图片无法打开只显示灰块：Android 端 Coil 图片下载未复用 API 的 OkHttp Bearer 认证客户端（反向代理保护静态资源时图片请求缺少认证），且 `AsyncImage` 无错误状态，401/404/网络错误均静默呈现灰色块无法诊断；首版修复提交还漏声明 `imageUrl`/`painter` 变量导致编译错误，且误用 Coil 3.x 专属的 `io.coil-kt:coil-network-okhttp` 构件（Coil 2.7.0 下不存在）导致 CI `checkDebugAarMetadata` 依赖解析失败 | `RetrofitClient` 导出 `sharedOkHttpClient()`；`WmsApplication` 实现 `ImageLoaderFactory`，用 Coil 2.x 内置的 `ImageLoader.Builder.callFactory { sharedOkHttpClient() }` 注入统一客户端（不新增依赖）；`ArchiveImageCard` 声明 `imageUrl`/`painter`，空 URL 与 Coil Error 状态显示"图片加载失败"而非灰块。回归：`tests/verify_bug_2026_08_11_017_mobile_material_image_loader.py` T1-T5 5/5 PASSED（共享客户端、callFactory 注入、无 Coil3 专属 API/构件、可见错误状态、空 URL 分支），stash 反向验证重新 RED |
| BUG-2026-08-11-018 | 物料管理列表"图片"列缩略图无点击事件，点击图片无反应，无法查看大图 | `app/templates/material.html` 缩略图绑定 `onclick="previewMaterialImage(this.src)"` 并显示 zoom-in 光标；新增 `imagePreviewModal` 大图预览弹窗（modal-lg、最大高度 75vh）与 `previewMaterialImage()` 函数（赋值 src 后 `bootstrap.Modal` 显示）。回归：`tests/verify_bug_2026_08_11_018_material_image_preview.py` T1-T3 3/3 PASSED，stash 反向验证重新 RED |
| BUG-2026-08-11-019 | 手机 APP 物料档案图片点击无法放大查看：Android 档案图片列表缩略图无点击事件，用户无法查看大图 | `MaterialArchiveScreens.kt`：`ArchiveImageCard` 新增 `onPreview` 回调，缩略图区域 `.clickable` 触发；`MaterialArchiveDetailScreen` 持有 `previewImageUrl` 状态，非空时弹出全屏 `Dialog`，黑底 + `AsyncImage`（`ContentScale.Fit`）完整展示大图，点击任意处或返回键关闭；预览走同一 Coil ImageLoader（含 Bearer 认证）。回归：`tests/verify_bug_2026_08_11_019_mobile_image_preview.py` T1-T3 3/3 PASSED，stash 反向验证重新 RED |
| BUG-2026-08-11-020 | 手机 APP 档案图片全屏预览不支持手势缩放，铭牌小字无法局部放大查看 | `MaterialArchiveScreens.kt` 预览 Dialog 的 `AsyncImage` 增加 `pointerInput` + `detectTransformGestures`：`scale` 双指捏合缩放（1x-5x，缩回 1x 自动复位偏移）、`offset` 拖动平移，`graphicsLayer` 应用变换；单击关闭、返回键关闭行为不变。回归：`tests/verify_bug_2026_08_11_019_mobile_image_preview.py` T4 手势契约 PASSED（T1-T4 4/4），stash 反向验证 T4 重新 RED |
| BUG-2026-08-11-021 | 扫码盘点没有盘点仓库：Android 扫码盘点页无仓库选择 UI，`/api/stocktake` 后端完全忽略 payload 中的 warehouse 字段，`InventoryCheckScan` 模型无 warehouse 列，盘点单及其自动生成的库存调整草稿都没有仓库，违反 AGENTS.md 仓库必填规则 | 后端：`app.py` `InventoryCheckScan` 增加 `warehouse` 列 + `auto_migrate_database` 补列并对存量数据回填默认仓库名；`routes/native_api.py` `/api/stocktake` 校验仓库必填，未传时自动带入默认仓库，无默认仓库返回 400「请选择盘点仓库」。Android：`StocktakeScreen` 增加盘点仓库选择卡片（`WarehouseSelectorCard` + `WarehousePickerDialog`，进入页面自动加载仓库列表并预选第一项），提交确认弹窗显示所选仓库且未选仓库时禁用确认按钮；`ScanViewModel.submitStocktake()` 已有未选仓库拦截。回归：`tests/verify_bug_2026_08_11_021_stocktake_warehouse_required.py` T1-T4 4/4 PASSED（无默认仓拒绝、默认仓自动带入并写入调整草稿、显式仓库落库、表结构含 warehouse 列） |
| BUG-2026-08-12-001 | AI 知识库与工具描述仍宣称"采购入库必须关联采购订单"，与 AGENTS.md"采购订单仅作为可选来源"规则冲突（提示词/知识层；后端强制校验已由 `purchase_in_order_requires_order()` 开关守卫并默认停用，见 BUG-2026-08-02-019） | `app/ai/knowledge.py` `purchase_receive_sop.rule` 改为"采购订单是采购入库的可选来源，采购入库允许手工新增、编辑、保存和完成；有关联采购订单时必须保留来源、数量与执行进度跟踪。送货单识别只能生成草稿或确认页，不能直接完成入库。"；`app/ai/tools/registry.py` `purchase_receive_draft` 工具描述改为"A purchase order is only an optional source; when linked, keep source, quantity and progress tracking."。回归：`tests/test_bug_2026_08_12_001_purchase_optional_prompt.py` 3/3 PASSED（stash 反向验证旧代码 3 failed） |
| BUG-2026-08-12-002 | v2 调试接口 `/api/ai/v2/llm/chat` 与 `/api/ai/v2/llm/intent` 用 `payload.get('system_prompt', 默认值)` 直接取客户端提示词，任何登录用户（含 user/production 低权限角色）都可覆盖 system_prompt，绕过 WMS 角色与业务边界约束 | `app/ai/v2_routes.py` 新增 `_resolve_debug_system_prompt(payload, default)`：非 admin 一律忽略 payload 中的 system_prompt 回落服务端默认；admin 允许自定义（调试用）但截断到 2000 字符并 `logger.warning` 审计（user id、prompt 长度）；两条调试路由均改用该 helper。回归：`tests/test_bug_2026_08_12_002_v2_system_prompt_guard.py` 6/6 PASSED（非 admin 忽略、production 忽略、admin 生效+审计日志、2000 截断、空值回落、路由必须走 helper），stash 反向验证旧代码 6 failed |
| BUG-2026-08-12-003 | 统一系统提示词（`app/ai/prompts.py` legacy-v1）仅一句英文泛化描述，未包含任何 WMS 业务红线；`app/app.py` 两条聊天提示词链路（`_ai_call_llm_chat` 与流式聊天路由）硬编码且缺少"送货通知→入库草稿（禁止采购申请）""仓库必填""密码不可重置"等红线 | `app/ai/prompts.py` legacy-v1 在保留 `Never auto-submit` 英文安全边界的前提下追加 6 条中文核心业务红线（只建草稿/送货通知禁转采购申请/采购订单可选来源/仓库库位必填/数据实时查询不得编造/禁改密码）；`app/app.py` 两条聊天链路均以 `get_prompt_spec().system_prompt` 为基础约束再拼接页面上下文。回归：`tests/test_bug_2026_08_12_003_system_prompt_redlines.py` 4/4 PASSED（英文边界保留、8 个红线关键词、两条链路均接入），stash 反向验证旧代码 3 failed；`verify_ai_platform_foundations.py` PASS |
| BUG-2026-08-12-004 | 移动端 8 个读取接口（dashboard/stock query/alert list/in_order list+detail/out_order list+detail/opening_stock GET）无仓库解析与过滤：dashboard 跨仓统计今日进出/待办、stock query 与 alert list 直接读全局 `Material.stock`、单据列表/详情不按仓库隔离、期初库存未传参时返回跨仓记录，违反 AGENTS.md 仓库必填规则（P1，WMS FULL 审计 AUDIT-2026-08-12-001） | `app/app.py` 新增 `resolve_request_warehouse(params)`（warehouse_id/code/name 显式解析 + 存在且 active 校验 + 默认仓库回退，无默认仓库返回「请选择仓库」）与 `get_warehouse_stock_quantities(warehouse)`（库位管理开启时按 LocationInventory 仓库名汇总，关闭时按 StockTransaction.location 流水净额汇总，绝不回退全局 Material.stock）；`app/routes/native_api.py` 8 个读取接口统一接入：解析失败 400、列表/dashboard/期初按仓库过滤、单据详情跨仓 404、库存/告警用仓库级数量。回归：`tests/test_bug_2026_08_12_004_mobile_warehouse_scope.py` 14/14 PASSED（stash 反向验证旧代码 14 failed）；`verify_app_py_split_native_api.py`（`_seed_base` 补默认仓库契约）与 `verify_mobile_opening_stock_api.py` 全绿；`verify_opening_stock_multi_warehouse.py` / `verify_in_order_state_machine.py` / `verify_out_order_state_machine.py` PASS；lint A1-A10 与裸 fetch 扫描 0 违规 |
| BUG-2026-08-12-005 | 旧版报表导出 `/report/inout/print`、`/report/inout/export`、`/report/stock/print` 绕过仓库必填规则：inout 仅按日期导出全部仓库 InOrder/OutOrder，stock print 直接导出全局 `Material.stock` 跨仓总数，`stock_query.html` 暴露无仓库参数的 `/report/stock/print` 直连入口（P1，WMS FULL 审计 AUDIT-2026-08-12-002） | `app/routes/report.py` 三个旧导出统一接入 `resolve_request_warehouse(request.args)`（显式 warehouse_id/code/name 校验 + 默认仓库回退，无默认 400「请选择仓库」）：inout print/export 按 `InOrder.warehouse`/`OutOrder.warehouse` 精确过滤（双 sheet 结构、文件名、Content-Type、日期筛选保持不变）；stock print 改用 `get_warehouse_stock_quantities(warehouse)` 输出仓库级库存并按仓库数量判定预警状态，不再读取全局 `Material.stock`；`stock_query.html` 打印链接携带当前选中 `warehouse_id`。回归：`tests/test_bug_2026_08_12_005_report_export_warehouse_scope.py` 9/9 PASSED（A/B 仓显式过滤、默认仓回退、无默认 400、export 与 print 一致、仓库级库存 5/95 而非全局 100、模板链接带参），stash 反向验证旧代码 9 failed；`verify_bug_2026_08_02_018_report.py` 9/9 与 BUG-004 测试 14/14 全绿；lint A1-A10 与裸 fetch 扫描 0 违规；`verify_wms_bugs.py` 仅剩 AI-AGENTS-001/AI-STAGE5-OPS-001 两项既有脚本漂移（FIX-3/FIX-4 处理） |
| BUG-2026-08-12-006 | `scripts/verify_ai_agents.py` 断言详情页渲染 HTML 含旧英文内部文案（`'Stock risk scan'`、`'Low-stock replenishment scan'`、裸 `'draft'`），但生产代码已直接将中文步骤名写入 `AIAgentStep.name`，历史英文数据经 `ai_agent_text`/`ai_agent_label` 过滤器在渲染层中文化，验证脚本契约漂移导致误 FAIL（P2，WMS FULL 审计 AUDIT-2026-08-12-003） | 仅修改验证脚本（UI 中文化与过滤器保持不变）：数据库层直接断言结构化内部语义——warehouse/purchase task completed、每类 task ≥4 步且 step_no 自 1 连续、全部步骤 status='completed'、warehouse 步骤含 `tool_name='warehouse_insights'`、purchase 有且仅有一条 `risk_level='draft'` 且其 `tool_name='purchase_request_draft'`；渲染层断言数据库中文步骤名逐条出现在详情页 HTML、旧英文 `'Stock risk scan'`/`'Low-stock replenishment scan'` 不出现、`risk_level='draft'` 经 `ai_agent_label('risk')` 渲染为中文「草稿」单元格；并直接断言过滤器输出 `ai_agent_text('Stock risk scan')=='库存风险扫描'` 等中文化契约。回归：`verify_ai_agents.py` PASS（修改前 AssertionError RED 确认）；`verify_ai_all.py --level core` 56/58（仅剩 FIX-4 `verify_ai_stage5_ops.py` 与 FIX-5 `verify_ai_business_quality_dashboard.py` 两项既有失败）；`verify_wms_bugs.py` AI-AGENTS-001 转 PASS，仅剩 AI-STAGE5-OPS-001（FIX-4 处理） |
| BUG-2026-08-12-007 | `scripts/verify_ai_stage5_ops.py` 断言 `/ai/ops` 运维看板渲染 HTML 含内部 capability key `'warehouse_insights'`，但模板 `ai_ops_dashboard.html` 已正确使用 `name|ai_agent_label('tool')` 将工具名中文化为「仓库洞察」，内部 key 不应作为 UI 文案出现，验证脚本契约漂移导致误 FAIL（P2，WMS FULL 审计 AUDIT-2026-08-12-004） | 仅修改验证脚本（模板中文化与内部 key 存储保持不变）：数据库层断言 `AIToolCall.tool_name=='warehouse_insights'` 且 `capability=='warehouse_insights'`（内部 key 持久化不变）；模板源码层断言 `ai_ops_dashboard.html` 含 `ai_agent_label('tool')` 过滤器调用；渲染层断言页面含中文标签「仓库洞察」且不含内部 key `warehouse_insights`。保留既有验证范围：admin 可访问、warehouse 角色被拒（302/403）、降级开关 `_ai_llm_enabled`、drafts/agents 开关对 capability 的影响、rollout admin_only 限制、全局关闭后聊天返回「管理员关闭」提示。回归：`verify_ai_stage5_ops.py` PASS（修改前第 86 行 AssertionError RED 确认）；`verify_ai_all.py --level core` 57/58（仅剩 FIX-5 `verify_ai_business_quality_dashboard.py`）；`verify_wms_bugs.py` 全绿 |
| BUG-2026-08-12-008 | `scripts/verify_ai_business_quality_dashboard.py` 测试6 断言模板源码含 `'schema_version': 'Schema版本'`，但模板 `ai_business_quality.html` 已正确提供 `schema_version` 维度且用户可见标签使用中文化「结构版本」，功能存在、浏览器 E2E 通过，失败源于测试错误绑定历史显示文案（P2，WMS FULL 审计 AUDIT-2026-08-12-005） | 仅修改验证脚本（模板与中文标签保持不变）：保留 role/source/model 既有断言；`schema_version` 改为契约式断言——正则提取 `'schema_version': '<label>'` 映射必须存在，label 非空、不等于原始 key、必须含中文且不得混入英文字母（当前「结构版本」及未来等价中文标签均可通过）；新增 `renderDimensionTable` 函数体实际使用 `dimLabels[dim]` 映射渲染维度列的断言；保留维度表 ID、by_dimension 数据引用及测试7版本对比、测试8下钻等既有范围。回归：`verify_ai_business_quality_dashboard.py` 8/8 PASSED（修改前测试6「缺少Schema版本维度标签」RED 确认）；`verify_ai_business_quality.py` 8/8 与 `verify_ai_browser_e2e.py` 8/8 全绿；`verify_ai_all.py --level core` 58/58 全绿；`verify_wms_bugs.py` 回归检查通过 |
| BUG-2026-08-13-001 | 移动端物料图片上传/删除仅验证登录不验证角色，普通用户可修改任意物料图片（与 Web 端 material.py:341 强制仓库角色不一致） | `app/routes/mobile.py` 新增 `_web_or_api_role_required` 装饰器（Web 会话或 Bearer Token 任一 + 业务角色），对 `/mobile/api/material_archive/<id>/images` POST/DELETE 应用 `@_web_or_api_role_required('warehouse')`；同步在 `scripts/lint_wms_rules.py` A2 规则 KNOWN_DECORATOR_HINTS 追加 `@_web_or_api_role_required` 白名单。回归：`tests/verify_bug_2026_08_13_001_mobile_image_role.py` 7/7 PASSED（warehouse Bearer 上传/删除通过、user Bearer 上传/删除 403、无认证 401、admin 会话回归 PASS、源码静态校验）；lint A1-A10 0 违规 |
| BUG-2026-08-13-002 | `/mobile/api/scan_submit` 用 `@require_role('warehouse')+@login_required`，两者只认 Flask-Login `current_user`（Web 会话），原生 App 用 Bearer Token 提交入库/出库/盘点一律 401，无法完成扫码提交（功能阻断） | `app/routes/mobile.py` scan_submit 改用 `@_web_or_api_role_required('warehouse')`；handler 内新增 `actor = current_user if current_user.is_authenticated else get_bearer_user()`，`operator_id` 与角色判定统一用 `actor`；导入列表追加 `get_bearer_user`。回归：`tests/verify_bug_2026_08_13_002_scan_submit_bearer.py` 7/7 PASSED（warehouse Bearer 入/出/盘/查询成功且 operator_id 落库、user Bearer 403、无认证 401、Web 会话 admin 回归）；lint A1-A10 0 违规 |
| BUG-2026-08-13-003 | `_save_material_image_from_url` 调用 `requests.get(url, ..., max_redirects=5)`，但 `requests.get` 不接受 `max_redirects`（它是 `Session.max_redirects` 属性），每次必抛 `TypeError: request() got an unexpected keyword argument 'max_redirects'`，移动端/物料档案「网上找图」功能完全不可用（功能阻断） | `app/app.py` `_save_material_image_from_url` 改用 `allow_redirects=False` 手动逐跳跟随重定向上限 5 跳，删除非法的 `max_redirects=5` 参数。回归：`tests/verify_bug_2026_08_13_003_004_remote_image_ssrf.py` T1/T4/T5/T6/T8 PASSED |
| BUG-2026-08-13-004 | `_save_material_image_from_url` 旧实现 `allow_redirects=True` 只对初始 URL 主机调用 `_is_private_or_loopback_host`，不对 302 目标重新校验；首跳合法外网、后续 302 可跳到 `127.0.0.1`/`169.254.169.254` 等内网地址（SSRF），修复 BUG-003 后该路径重新可达使风险暴露 | `app/app.py` 改为手动逐跳跟随：每跳 urljoin 解析 Location → 校验 scheme ∈ (http,https) → 对新主机重新调用 `_is_private_or_loopback_host` → 命中内网即拒绝「不允许访问内网地址」；重定向上限 5 跳、Location 缺失拒绝、非 http(s) scheme 拒绝。回归：`tests/verify_bug_2026_08_13_003_004_remote_image_ssrf.py` T2/T3/T7 PASSED |
| BUG-2026-08-13-005 | `get_bearer_user` 每次 Bearer 请求都执行 `db.session.commit()`（写 `last_used_at` + `expires_at`），SQLite 下加剧写锁竞争；同时 `expires_at = now + 7d` 无条件顺延，活跃 token 永不过期（无限续期），失去 token 有效期约束意义 | `app/app.py` 改为节流写库 + 阈值续期：新增 `LAST_USED_FLUSH_SECONDS=300`/`RENEW_THRESHOLD_SECONDS=86400`/`TOKEN_TTL_DAYS=7`；`last_used_at` 仅当从未记录或距上次 >=5min 才刷新；`expires_at` 仅当剩余 <1d 才顺延 7d；否则跳过 commit。回归：`tests/verify_bug_2026_08_13_005_token_write_amplification.py` 7/7 PASSED |
| BUG-2026-08-13-006 | `app/auto_update.py` 启动自动更新直接 `git fetch origin main` + `git pull --ff-only` + `pip install -r requirements.txt`，无固定提交/标签校验、无签名或哈希验证、无引入提交审计；远端仓库或 GitHub 账号被篡改后，下次重启即自动拉入任意代码并安装任意依赖（供应链风险，该功能默认关闭但开启后无防护） | `app/auto_update.py`：①新增 `WMS_GIT_PIN`，fetch 后 `resolve_sha` 取远端 tip SHA，`verify_pin` 校验匹配（支持 SHA 前缀匹配 / git tag 经 rev-parse），不匹配拒绝 pull；②`main()` 记录更新前/后 HEAD SHA 及 `log_pulled_commits`（subject+author+date 审计清单）；③新增 `WMS_PIP_REQUIRE_HASHES=1` 追加 `--require-hashes`，依赖更新成功后 `log_pip_versions` 记录已安装包版本。回归：`tests/verify_bug_2026_08_13_006_auto_update_supply_chain.py` 11/11 PASSED |
| BUG-2026-08-15-002 | 反编译 APK 副产物 `apk_source/` 被版本控制，包含 17,916 个文件和约 208 MB 数据，导致代码库同步阶段文件枚举与传输显著变慢并可能超时 | 修复进行中：移除 `apk_source/` 并加入 `.gitignore`，回归测试验证目录不存在、Git 不再跟踪且忽略规则存在 |
| BUG-2026-08-13-007 | 部门删除拦截器 `_department_delete_blockers` 只检查领料单和采购申请，遗漏员工档案（`Employee.department_id`）引用；有员工的部门可被直接删除，导致员工档案外键悬空、部门信息丢失（基础资料完整性缺陷） | `app/app.py` `_department_delete_blockers` 新增 `Employee.query.filter(Employee.department_id == department.id).count()` 检查，与领料单检查并列；单删路由（`/department/<id>/delete`）和批量删除路由（`/department/delete`）共用同一拦截函数，两条路径同步生效。回归：`tests/verify_app_py_split_department.py` 新增 `test_delete_department_blocks_employee_reference`（单删拦截）+ `test_batch_delete_department_blocks_employee_reference`（批量删除混合场景：有员工的部门保留、无员工的部门删除成功），9/9 PASSED |
| INV-AUDIT-001 | 真实库位库存无法正确汇总到仓库库存：`get_warehouse_stock_quantities(warehouse)` 用 location 字符串匹配仓库名汇总，库位字符串与仓库名不一致或库位启用前后命名漂移时，仓库库存与总库存长期不一致 | `app/app.py` 新增 `resolve_inventory_warehouse_id(value)` 统一解析仓库 ID（None/实例/ID/名称/编码）；`update_location_inventory` / `add_location_inventory_atomic` / `deduct_location_inventory_atomic` 新增 `warehouse` / `warehouse_id` 参数并写入 `LocationInventory.warehouse_id`；`get_warehouse_stock_quantities` 改为按 `warehouse_id` 汇总，旧 location 字符串数据无 `warehouse_id` 时回退按 location 字符串匹配仓库名（兼容历史数据，不自动归入默认仓库）。回归：`tests/test_inv_audit_001_002_location_inventory_warehouse_isolation.py` 18/18 PASSED（resolve_inventory_warehouse_id 多形态解析、跨仓同名库位隔离、仓库级汇总正确、历史 location 字符串回退）。commit `d7fc6599` + `13c8a392` |
| INV-AUDIT-002 | 库位库存缺少仓库维度，`LocationInventory` 唯一约束为 `(material_id, location)`，不同仓库下同名库位被合并成一行库存；跨仓调拨同名库位相互覆盖 | `LocationInventory` 唯一约束改为 `(material_id, warehouse_id, location)`；新增 Alembic 迁移 `8b17c4d90a2e_location_inventory_warehouse_compat` 添加 `warehouse_id` 列并重建唯一约束；`auto_migrate_database` 增加 SQLite 表重建逻辑（SQLite 不支持 `ALTER TABLE DROP CONSTRAINT`，用 batch_alter_table 重建表结构 + 数据回填）；历史数据无法确定仓库归属的保留为未归属，不自动归入默认仓库。回归：`tests/test_inv_audit_001_002_location_inventory_warehouse_isolation.py::TestCrossWarehouseSameLocationNotMerged` 验证 A 仓 A1=10 与 B 仓 A1=5 不被合并；`tests/test_location_inventory_warehouse_migration.py` 验证迁移逻辑。commit `d7fc6599` |
| INV-AUDIT-003 | 移动扫码入库/出库/盘点未统一执行仓库必填规则，且库存校验直接读取全局 `Material.stock` 跨仓数量，可凭其他仓库库存扣减本仓库 | `app/routes/mobile.py` `scan_submit` 接入 `resolve_request_warehouse` 强制仓库必填（未提供或无效时 400），库存校验改用 `get_warehouse_stock_quantities(warehouse)` 返回仓库级库存；`update_location_inventory` 调用传入 `warehouse` 参数确保 `warehouse_id` 写入；库位字段独立解析不再与仓库名混淆。回归：`tests/test_inv_audit_003_mobile_scan_warehouse_scope.py` 7/7 PASSED（仓库必填拒绝、仓库级库存校验、跨仓库存隔离、入/出/盘均落 warehouse_id）。commit `2b47d8e7` |
| INV-AUDIT-004 | 旧物料模糊查询接口 `POST /api/query/search` 未校验仓库参数，直接返回全局 `Material.stock`，违反 AGENTS.md「库存查询仓库必填筛选」规则 | `app/routes/stock_query.py` `api_query_search` 接入 `resolve_request_warehouse` 强制仓库必填（无默认仓库时返回 400「请选择仓库」），`stock` 字段改用 `get_warehouse_stock_quantities(warehouse)` 返回仓库级库存，不再回退全局 `Material.stock`。回归：`tests/test_inv_audit_004_query_search_warehouse_scope.py` 7/7 PASSED（仓库必填校验、仓库级库存返回、跨仓库存不泄漏 A 仓查不到 B 仓库存、默认仓库回退）。commit `9b1d779c` |
| INV-AUDIT-005 | 调拨/盘点/调整/普通出库/移动端 stocktake 的仓库存在性及 active 状态校验不一致：部分路由只校验非空不校验存在，部分路由不校验 active，可对已停用仓库或不存在仓库名生成单据 | `app/app.py` 新增 `resolve_active_inventory_warehouse(value, warehouse_id)` 与 `validate_inventory_warehouse(value, warehouse_id)` 辅助函数，按 ID/名称/编码统一解析并校验 `status='active'`；`app/routes/transfer.py`（from_warehouse/to_warehouse）、`app/routes/check.py`（warehouse）、`app/routes/adjustment.py`（warehouse）、`app/routes/out_order.py`（领料/其他出库 warehouse）、`app/routes/native_api.py`（stocktake warehouse）全部接入 `validate_inventory_warehouse`，校验失败返回 400，校验通过用规范化仓库名回写单据字段。回归：`tests/test_inv_audit_005_warehouse_active_validation.py` 23/23 PASSED（辅助函数多形态解析 + active 过滤、各路由对不存在/已停用仓库拒绝、对 active 仓库通过且规范化回写）。commit `da22cc81` |
| BUG-2026-08-14-001 | PUR-AUDIT-001：多采购订单选单生成采购入库时，`assert_warehouse_active()` 返回 `(ok, msg)` 二元组被直接赋给 `wh_err`，非空元组恒为真（含 `(True, '')`），导致合法仓库也被拒绝，选单下推功能完全不可用 | `app/routes/purchase_order.py` `create_in_order_from_purchase_order_selection` 路由改为正确解构 `wh_ok, wh_msg = assert_warehouse_active(warehouse, allow_empty=False)`，仅在 `not wh_ok` 时返回 `api_error(wh_msg)`；保留默认仓库回退和多来源 `source_purchase_order_id=None`/明细 `source_purchase_order_item_id` 写入逻辑不变。回归：`tests/test_pur_audit_001_selection_warehouse.py` 4/4 PASSED（同供应商多订单选单成功+来源完整、不存在仓库拒绝、停用仓库拒绝、不同供应商拒绝） |
| BUG-2026-08-14-002 | PUR-AUDIT-002：多订单选单生成入库时 `InOrder.source_purchase_order_id=None`，真实来源仅保存在 `InOrderItem.source_purchase_order_item_id`，但采购订单删除只检查表头 `source_purchase_order_id`，可删除仍被入库明细引用的采购订单，破坏来源追溯和 `received_quantity` 执行进度 | `app/app.py` 新增 `has_inbound_reference(purchase_order_id)` 辅助函数，同时检查表头级 `InOrder.source_purchase_order_id` 和行级 `InOrderItem.source_purchase_order_item_id`（JOIN `PurchaseOrderItem`，参数化查询）；`app/routes/purchase_order.py` `delete_purchase_order` 和 `batch_delete_purchase_order` 均改用 `has_inbound_reference`，批量删除返回被阻断清单。回归：`tests/test_pur_audit_002_delete_protection.py` 8/8 PASSED（辅助函数契约、单来源草稿阻断、多来源草稿/已完成双阻断、删除草稿释放引用后可删、批量删除返回阻断清单） |
| BUG-2026-08-14-003 | PUR-AUDIT-003：入库完成与批量完成在写锁后、写库存前未重新校验仓库 active 状态，草稿保存后仓库被停用仍可完成入库，导致新库存归属停用仓库 | `app/routes/in_order.py` `complete_in_order` 和 `batch_complete_in_order` 在 `_acquire_order_write_lock` 成功后、`add_stock` 之前调用 `assert_warehouse_active(order.warehouse, allow_empty=False)`；单张完成回滚并返回 400，批量完成将该单据加入 skipped 并继续处理其他合法单据。回归：`tests/test_pur_audit_003_complete_warehouse_active.py` 4/4 PASSED（停用仓库单张完成拒绝+库存不变、不存在仓库拒绝、批量完成跳过停用仓+其他正常、启用仓库正常完成）。`verify_in_order_state_machine.py` PASS、`verify_inbound_push.py` PASS |
| BUG-2026-08-14-004 | PUR-AUDIT-004：用户需求——采购入库明细表、领料单明细表、导出采购入库明细表、导出领料单明细表必须有合同单号、工程名称字段。4 个导出函数（`batch_export_in_order`/`export_single_in_order`/`export_out_order`/`export_single_out_order`）此前未输出 `item.contract_no`/`item.project_name`，明细表模板已含字段但导出 Excel 缺失，导出文件与页面明细不一致 | `app/routes/in_order.py` `batch_export_in_order`、`export_single_in_order` 在表头与每条数据行追加「合同单号」「工程名称」两列，值取 `item.contract_no`/`item.project_name`；`app/routes/out_order.py` `export_out_order`、`export_single_out_order` 同步追加两列；列序与明细表模板一致。回归：`tests/test_pur_audit_004_export_contract_project_fields.py` 4/4 PASSED（采购入库单张/批量、领料单单张/批量导出均含两列且行级值与 item 一致）。lint A1-A10 与裸 fetch 扫描 0 违规 |
| BUG-2026-08-14-005 | SALES-AUDIT-001（P0）：`recalculate_sales_order` 在出库完成回写 `shipped_quantity` 时不检查销售订单 `status`，已 `cancelled` 的订单被出库完成静默"复活"为 `confirmed`/`closed`，导致取消语义被破坏、库存扣减与取消决策不一致 | `app/app.py` `recalculate_sales_order` 在重算前增加 `cancelled` 状态守卫：若 `order.status == 'cancelled'` 直接返回，不覆盖状态、不更新 `shipped_quantity`，保留取消决策痕迹。回归：`tests/test_sales_audit_001_cancelled_revive_guard.py` 验证取消订单的出库完成不会改写状态、库存仍按既有扣减逻辑但订单保持 cancelled |
| BUG-2026-08-14-006 | SALES-AUDIT-002（P1）：`cancel_sales_order` 不检查已存在的 `pending` 销售出库草稿，取消后这些草稿仍可被完成，绕过 SALES-AUDIT-001 守卫的并发窗口 | `app/routes/sales.py` `cancel_sales_order` 在状态变更前查询该订单关联的 `OutOrder.business_type=='销售出库' and status=='pending'`，存在则拒绝取消并提示先处理草稿。回归：`tests/test_sales_audit_002_cancel_blocks_pending_outbound.py` 验证有 pending 草稿时取消被拒、无草稿或草稿已完成时取消成功 |
| BUG-2026-08-14-007 | SALES-AUDIT-003+004+006（P1 并发与超量一组）：①`create_sales_outbound_from_selection`/`batch_create_sales_outbound` 锁 `SalesOrderItem` 不锁 `SalesOrder`，并发下推重复草稿；②`sync_sales_order_shipment` 用 `+= quantity` 回写 `shipped_quantity` 非原子，并发完成丢失更新；③`complete_out_order` 不校验出库数量 ≤ 未发货数量，叠加编辑路径（SALES-AUDIT-005）允许"小数量草稿→编辑改大→完成"超量出库 | ①`app/routes/sales.py` 下推草稿路由调用 `_acquire_order_write_lock(SalesOrder, ...)` 加写锁后二次校验状态；批量下推循环内每张订单独立提交，避免前序草稿丢失。②`sync_sales_order_shipment` 改用 `sa_update` 原子累加 `shipped_quantity`。③`complete_out_order` 加锁后、扣库存前调用新增的 `sales_outbound_remaining_check` 校验每条有来源明细不超过 `sales_item.quantity - sales_item.shipped_quantity`，超量 rollback 返回错误。回归：`tests/test_sales_audit_003_004_concurrency_and_atomic.py`、`tests/test_sales_audit_006_complete_remaining_check.py` 验证加锁/原子/超量场景 |
| BUG-2026-08-14-008 | SALES-AUDIT-005（P1）：`/out_order/add` 编辑销售出库草稿时重建明细未保留 `source_sales_order_item_id`，导致发货进度无法正确回写到原销售订单行 | `app/routes/out_order.py` 编辑路径重建 `OutOrderItem` 时从既有明细携带 `source_sales_order_item_id`，确保 `sync_sales_order_shipment` 能正确按来源行回写 `shipped_quantity`。回归：`tests/test_sales_audit_005_edit_preserve_source_item.py` 验证编辑后明细仍保留来源关联 |
| BUG-2026-08-14-009 | SALES-AUDIT-007（P1）：销售报表/导出未将仓库作为必填条件，违反 AGENTS.md「库存查询、出入库报表、库存台账的查询入口必须将仓库作为必填条件」 | `app/app.py` 新增 `_require_report_warehouse()` 辅助函数，按 `warehouse_id` → `warehouse` 名称 → 默认仓库顺序解析，无则返回 400；`app/routes/sales.py` 10 个报表/导出路由（`export_sales_orders`/`export_sales_report`/`export_sales_execution_report`/`export_sales_outflow_report`/`export_sales_outbound`/`sales_reconciliation_report`/`sales_trend_report`/`sales_price_analysis`/`export_sales_price_analysis`/`export_sales_execution_report`）全部接入该门禁。回归：`tests/test_sales_audit_007_report_warehouse_required.py` 验证无仓库参数返回 400/空结果、有默认仓库时回退成功。**补充修复（2026-08-15，CI 首次跑通暴露）**：`scripts/full_smoke_test.py` 导出用例此前无参数调用销售报表导出端点，门禁生效后 6 个销售导出（`/sales/export`、`/sales/outbound/export`、`/sales/reconciliation/export`、`/sales/report/export`、`/sales/trend_report/export`、`/sales/outflow_report/export`）返回 400，CI 冒烟 114/120 红。修复：脚本新增 `get_warehouse_id()`（从 `/api/warehouses` 取仓库 id，无则经 `/warehouse/add` 创建），销售导出端点统一附加 `?warehouse_id=` 参数。回归：冒烟 121/121 PASSED，WMS CI 转绿。commit `6667135` |
| BUG-2026-08-14-010 | SALES-AUDIT-008（P1）：`complete_out_order` 在草稿保存后才校验仓库，草稿创建时仓库可能仍 active，但保存后仓库被停用，完成流程此前不再复核，导致已停用仓库仍可完成出库 | `app/routes/out_order.py` `complete_out_order` 加锁后、扣库存前对 `business_type=='销售出库'` 的单据调用 `validate_sales_outbound_warehouse(order)` 复核 active 状态（对照 PUR-AUDIT-003 的 `in_order.py:1389` 修复模式），不通过则 rollback 返回错误。回归：`tests/test_sales_audit_008_warehouse_active_recheck.py` 3/3 PASSED（active 仓库完成成功、停用仓库完成被拒+状态保持 pending、非销售出库不受影响） |
| BUG-2026-08-14-011 | SALES-AUDIT-009（P1）：销售订单导出/报表/打印缺合同单号与工程名称字段。`SalesOrder` 模型已有 `contract_no`/`project_name`/`project_no` 字段，列表页与详情页已展示，但 4 个 P1 导出/打印入口（`export_sales_orders`/`export_sales_report`/`export_sales_execution_report`/`print_sales_order`）遗漏，2 个 P2 出库导出（`export_sales_outflow_report`/`export_sales_outbound`）已关联 `SalesOrder` 但未输出合同/工程 | `app/routes/sales.py` 6 个导出/打印入口在表头与数据行（或打印 info 列表）追加合同单号/合同编号、工程名称两列，值取 `order.contract_no or ''`/`order.project_name or ''`（出库导出来自关联 `source_sales_order`）；修复运算符优先级陷阱 `(obj.field or '') if obj else ''` 避免 AttributeError。回归：`tests/test_sales_audit_009_export_contract_project_fields.py` 6/6 PASSED（4 个 P1 + 2 个 P2 导出/打印均含两列且行级值与订单一致） |
| BUG-2026-08-14-012 | SYS-AUDIT-001（P1）：`execute_init_business_data` 成功消息谎称"SystemSetting 已保留"，实际 `INIT_LOG_TABLES` 含 SystemSetting 被全量删除，导致 AI API Key、LLM 接口地址等配置丢失到下次服务器重启 | `app/routes/system_settings.py` init 清理完成后立即调用 `ensure_default_system_settings()` 恢复默认值；响应消息改为"User 账号已保留，系统参数已重置为默认值"；修正 `_init_business_data_keep_users_and_settings` docstring 明确 SystemSetting 会被删除。回归：`tests/test_system_settings.py::TestExecuteInitBusinessDataRestoresSettings` 验证 init 后 SystemSetting 表非空 |
| BUG-2026-08-14-013 | SYS-AUDIT-002（P1）：唯一测试文件 `verify_app_py_split_system_settings.py` 以 `verify_` 前缀命名，pytest 默认不收集，覆盖也仅限 endpoint 注册/页面渲染/空表单保存/preview 访问，未覆盖高风险的 init 失败路径、字段校验、secret 保留逻辑 | 删除 `verify_` 文件，新建 `test_system_settings.py` 使 pytest 自动收集；补充 7 个高风险路径测试（int 越界/select 非法值/secret 留空保留/init 错短语/缺密码/错密码/init 成功后恢复默认值）。回归：13/13 PASSED |
| BUG-2026-08-14-014 | SYS-AUDIT-003（P2）：`execute_init_business_data` 用豁免注释绕过 A8 pydantic 校验，3 个固定参数手工 strip 和比较，无类型契约 | 引入 `InitBusinessDataExecuteForm(BaseModel)`，声明 `admin_password`/`confirm_phrase`（min_length=1）和 `include_master_data`（field_validator 限制 0/1），移除豁免注释。`save_system_settings`/`test_ai_llm_settings` 参数由 SYSTEM_SETTING_DEFINITIONS 动态驱动，保留豁免注释。回归：13/13 PASSED；A8 0 违规 |
| BUG-2026-08-14-015 | SYS-AUDIT-004+005（P2）：①主表单 `<form>` 缺 `method="post"` 属性，A1 lint 规则无法扫描 CSRF；②5 处 `window.csrfFetch \|\| fetch` 回退模式，若 csrfFetch 未定义会回退到裸 fetch | ①表单加 `method="post"` 属性，表单内已有 csrf_token 隐含字段；②5 处改为直接调用 `csrfFetch`（base.html 全局定义），消除裸 fetch 回退。回归：13/13 PASSED；A1 lint 0 违规；裸 fetch 扫描通过 |
| BUG-2026-08-14-016 | SYS-AUDIT-006（P2）：init 成功后清空全部历史 OperationAudit（除本次 preview+done 外），永久销毁此前所有操作审计轨迹，合规风险（等保/SOX 要求审计日志不可销毁） | 清空前导出一份 OperationAudit 全量 JSON 备份到 `output/audit_backup/audit_backup_YYYYMMDD_HHMMSS.json`，响应消息附带备份路径。导出失败不阻断 init 主流程（仅日志告警）。回归：13/13 PASSED |
| BUG-2026-08-14-017 | SYS-AUDIT-007+008（P2）：①5 个路由装饰器顺序为 `@require_role` 在 `@login_required` 之上，非常规写法易误导；②3 处异常处理将 `str(exc)` 拼入返回给浏览器的 JSON 消息，可能暴露 LLM URL/API Key/数据库表名 | ①统一为 `@login_required` 在上、`@require_role('admin')` 在下；②3 处改为对外返回通用消息"请查看服务器日志获取详情"，详细错误仍通过 `app.logger.exception()` 记录。回归：13/13 PASSED |
| BUG-2026-08-14-018 | SYS-AUDIT-009（P2）：前端确认按钮 JS 硬编码 `fd.append('include_master_data', '1')`，用户无法选择保留主数据，每次 init 都会清空全部主数据 | 模态框新增 checkbox"同时清空主数据"，默认勾选；JS 从 checkbox 读取状态替代硬编码 '1'，取消勾选则传 '0'。回归：13/13 PASSED |
| BUG-2026-08-14-019 | SYS-AUDIT-011（P2）：`set_system_setting` 用 `filter_by(key=key).first()` 查询后更新，无 `with_for_update()` 行锁，两个 admin 并发保存时后提交者覆盖前者部分变更 | 查询加 `.with_for_update()` 行锁。SQLite 忽略 with_for_update（no-op），MySQL/PostgreSQL 生效。回归：`tests/test_system_settings.py::TestSetSystemSettingWriteLock` 验证加锁后正常写入 |
| BUG-2026-08-14-020 | AUDIT-2026-08-14-001（P1 | FIX-001）：移动端 `POST /api/inbound`、`POST /api/outbound` 在关闭库位管理且无默认仓库时，不传 warehouse/warehouse_code/warehouse_id 仍返回 200 并创建 status=completed 空仓库单据、改库存，违反「仓库无论库位管理是否开启都必填」「无默认仓库必须拒绝」规则，与 Web 端仓库规则不一致 | `app/routes/native_api.py` `inbound`/`outbound` 在 `parse_api_lines()` 成功后、创建 `InOrder`/`OutOrder` 前调用 `resolve_request_warehouse(payload)` 解析并校验仓库存在且 active，失败返回 400「请选择仓库」/「不存在」/「已停用」；成功用规范化仓库名写回单据；开启库位管理路径继续独立校验库位；保留 Bearer Token 认证、`api_role_required` 角色校验与 `mobile_api_idempotent` 幂等。回归：`tests/test_bug_2026_08_12_004_mobile_warehouse_scope.py::TestMobileWarehouseWriteScope` 21/21 PASSED（无默认仓库入/出库 400 且无单据库存不变、默认仓库回退成功、停用仓库 400、库位管理开启缺库位 400）。commit `9460f68c` |
| BUG-2026-08-14-021 | AUDIT-2026-08-14-002（P1 | FIX-002）：生产环境可通过 `WMS_DISABLE_CSRF=1/true/yes` 关闭全局 CSRF，`ProductionConfig.WTF_CSRF_ENABLED=False`，应用仅记日志仍继续启动，存在 CSRF 防护整体失效风险 | `app/config.py` 新增 `validate_production_security_config()`，生产启动时若检测到 `WMS_DISABLE_CSRF` 直接 `raise RuntimeError` fail-fast 拒绝启动；`ProductionConfig.WTF_CSRF_ENABLED` 硬编码为 `True`；`app/app.py` 在 `app.config.from_object` 前调用该校验函数。开发/测试环境隔离开关不受影响；移动端 `/api/*` Bearer 认证与 `csrf.exempt` 端点保持不变。错误信息不含变量完整值/密钥/Token。回归：`tests/test_production_security_config.py` 6/6 PASSED（production+未设置开启、production+`1`/`true` 均失败且信息明确、测试环境开关生效）。commit `1f557e63` |
| BUG-2026-08-14-022 | AUDIT-2026-08-14-003（P2 | FIX-003）：路由拆分迁移到 `app/routes/` 后，验证脚本仍静态读取 `app/app.py` 查找已迁移函数，导致静态 C 段误报失败（如 `verify_bug_2026_08_02_001.py` 读 in_order 函数、`verify_bug_2026_08_02_002.py` 读出库函数、`verify_opening_stock_multi_warehouse.py` 读期初路由）；`verify_offline_wheelhouse.py` 按宿主 OS(Linux) 解析 Windows wheelhouse 误报 cryptography 缺失 | `scripts/verify_bug_2026_08_02_001.py` 改读 `app/routes/in_order.py`；`verify_bug_2026_08_02_002.py` 改读 `app/routes/out_order.py` + `after_sale_out.py`；`verify_opening_stock_multi_warehouse.py` 改读 `app/routes/opening_stock.py`，正则按 4 空格缩进匹配迁移函数；`verify_offline_wheelhouse.py` 改用 `--platform win_amd64 --python-version 3.11 --implementation cp --abi cp311 --only-binary=:all:` 跨平台解析 Windows wheelhouse。回归：3 脚本全部 PASS + `verify_stability_gate.py` 门禁通过。commit `36331e7b` + `5facb057` |
| BUG-2026-08-14-023 | AUDIT-2026-08-14-004（P2 | FIX-004）：`verify_transfer_state_machine.py` 种子未配置仓库维度且库位库存用旧 `warehouse_id=NULL` 结构；`verify_adjustment_state_machine.py` 创建 `AdjustmentOrder` 无 `warehouse`；`verify_check_state_machine.py` 创建 `InventoryCheck` 无 `warehouse`，当前完成路由按仓库必填规则拒绝，专项脚本失败 | 仅修测试种子，不降低生产仓库必填规则：`verify_transfer_state_machine.py` 显式开启 `location_management_enabled=1` 并构造带仓库/库位的余额转移前置；`verify_adjustment_state_machine.py` 为 `AdjustmentOrder` 赋 `warehouse="默认测试仓"`；`verify_check_state_machine.py` 为 `InventoryCheck` 赋 `warehouse="默认测试仓"`。回归：3 状态机脚本 PASS + `pytest tests/` 400/400 PASSED。commit `36331e7b` |
| BUG-2026-08-15-001 | 采购订单列表 `/purchase_order` 与物料列表 `/material` 列表路由仅 `@login_required`，任何已登录用户（production / viewer 等受限角色）可用 URL 直访越权进入采购/基础资料页面，虽侧边栏已按角色隐藏菜单但其防护只在渲染层，服务端无角色门禁（与侧边栏可见性不一致） | `app/routes/purchase_order.py` `purchase_order_list` 与 `app/routes/material.py` `material_list` 均加 `@require_role('warehouse')`（require_role 内置放行 admin），与侧边栏「采购管理 admin/warehouse」「基础资料 admin」可见性对齐；production/viewer 直访被 302 跳回首页。回归：`tests/verify_bug_2026_08_15_001_purchase_material_list_role.py` 7/7 PASSED（production @ /purchase_order、production @ /material、viewer @ /material、viewer @ /purchase_order 均被拒；warehouse @ 两路由 200；admin @ 两路由 200）。浏览器 E2E 复跑 127/128 PASSED（唯一未过为测试环境缺 APK 的 `/mobile/app` 404，非代码缺陷）。commit 见本次提交。**补充修复（CI 首次跑通暴露）**：`purchase_insights` 能力声明允许 `purchase` 角色，但其业务端点 `/purchase_order` 列表仅 `@require_role('warehouse')`，交集为空导致 AI 采购洞察对采购角色被拒（`verify_ai_permission_matrix.py` FAIL）。修复 `purchase_order_list` 改为 `@require_role('warehouse','purchase')`，采购角色可访问采购订单列表支撑 AI 采购洞察；`/material` 保持 warehouse（与 `master_data_insights` 声明一致）。回归：`verify_ai_permission_matrix.py` PASS + BUG-001 测试扩为 8/8 PASSED + `pytest tests/` 456/456 PASSED。commit 见本次提交 |

## 已确认误报

| 编号 | 原问题 | 误报原因 |
|------|--------|----------|
| BUG-003 | 委外收货数量双倍计算 | 源码已有 autoflush 处理说明，sum 查询没有额外加本次数量 |
| BUG-008 | 全局确认框变量竞态 | 当前实现已使用确认队列，不存在 resolver 覆盖 |
| SCAN-2026-08-07-001 | `scan_wms_risks.py` 报 4 个 `\|safe` 候选：`alert.html:237`、`sales_reconciliation_report.html:147`、`print_out_with_html.html:63`、`print_in_with_html.html:67` | 逐个人工判真均为误报：①`alert.html` 全文件无 `\|safe`（行号漂移误匹配）；②`sales_reconciliation_report.html:147` 是 `chart_data\|tojson\|safe`，即 VULN-005 确立的标准安全写法；③两个打印模板的 `rendered_content` 由 `_render_html_print_content()` 生成，已经 `SandboxedEnvironment(autoescape=True)` 沙箱渲染 + `sanitize_print_html()` 白名单净化双重防护（VULN-001 已登记） |
| SCAN-2026-08-11-001 | 2026-08-11 全系统增量审计（app.py 28k 行 + routes/ 23k 行 + ai/ 21k 行 + JS 5k 行 + Android 增量）报 5 类疑似项 | 逐个人工判真均为误报：①"routes 未检查原子函数返回值"（transfer.py L427-438 / after_sale_out.py L390-406/L444-463）——源码逐条 `if not ok: rollback + return api_error`，证据齐全；②"providers.py 重试风暴/API key 泄漏"——`call_llm` 有界重试 `max_retries=2` + 指数退避 + 熔断器 `breaker`，日志只打异常对象不含 header/key；③"streaming.py 连接泄漏"——`stream_response_payload` 是纯字符串切片的有限生成器，Flask 响应结束自然关闭；④"app.py pickle RCE / 硬编码密钥 / 未参数化 SQL"——全仓 grep 无 `pickle/eval/exec/__import__`、无硬编码 password/secret/api_key，f-string SQL 仅存在于 `auto_migrate_database` 内部 DDL（表名/列名全部为代码内常量，非用户输入）；⑤"AI 路由缺权限"——`ai/routes.py` 17 条、`ai/v2_routes.py` 19 条全部 `@login_required`，能力校验在 tools 层 `AI_CAPABILITY_ROLES`（AI-AUTH-001 已登记）。审计同时确认：全部 `requests.*` 调用带 timeout；`subprocess` 仅存在于 restart.py/auto_update.py 固定参数部署脚本；无 `send_file/send_from_directory` 用户输入路径遍历；上传有 `MAX_CONTENT_LENGTH=50MB` + `ALLOWED_EXTENSIONS` + Excel 5MB 三层限制；lint A1-A10 0 违规、无裸非 GET fetch；`verify_ai_all.py --level smoke` 7/7 PASS（唯一失败项为沙箱缺 `requests` 依赖的环境问题，装依赖后 PASS，非代码问题） |

## 降级或暂缓项

| 编号 | 状态 | 说明 |
|------|------|------|
| BUG-007 | 低风险 | 原问题主要是死代码/防御性分支，不是核心业务矛盾 |
| VULN-002 | 低风险 | `|safe` 使用前已有 sanitize 保护，继续保持净化入口即可 |
| CONF-003 | 低风险 | 生产配置已修正，环境部署时仍需确认 HTTPS/Cookie 配置 |
| SQLite 并发能力 | 已修复单机并发边界 | 已启用 WAL、60 秒 busy timeout、外键、原子库存更新；启动迁移使用排他事务串行执行。高并发生产环境仍建议迁移 MySQL/PostgreSQL |
| BUG-NEW-002 | 暂不按 BUG | `update_completed_in_order()` 的库位增减路径支持正负 `qty_diff`，新增、删除、数量变更均已有库位同步 |
| BUG-NEW-004 | 已修复 | 保留 `received_quantity` 作为防重复下推的占用量；采购订单状态改为仅汇总 `completed` 入库单，草稿和待提交入库不再提前把订单标记为已入库 |
| BUG-NEW-007 | 已修正字段语义 | `api_material_payload()` 不再把 `warehouse_code` 直接复制给 `location_code`，库位启用时取库位库存记录 |
| BUG-NEW-010 | 低风险体验项 | `makeKey()` 已会规范化 `embedded` 参数；未确认恢复错配。仅提高恢复标签数量上限 |
| BUG-NEW-012 | 低风险体验项 | 登录页占位链接/勾选框不影响核心业务；初始密码展示必须与 BUG-NEW-005 和 `AGENTS.md` 一致 |
| BUG-NEW-016 | 已修复 | 自动迁移在检查字段前执行 SQLite `BEGIN EXCLUSIVE` 并等待最多 60 秒；迁移失败将阻止进程继续启动，避免多个 worker 重复 DDL 或带不完整结构运行 |
| BUG-NEW-017 | 误报 | 报告描述为“日志不包含具体 SQL，不利排查”，不是“敏感 SQL 泄露” |
| BUG-NEW2-005 | 低风险设计项 | `log_operation()` 是独立的 best-effort 日志提交，当前多在业务提交后调用；不按核心数据 BUG 处理 |
| JS-NEW2-001 | 低风险兜底 | `confirmDialog()` 缺 DOM 时降级 `window.confirm()` 是可用性兜底，不影响数据正确性 |
| JS-NEW2-002 | 低风险兜底 | `toast()` 缺容器时降级 `alert()` 是可用性兜底，不影响核心业务 |
| SEC-NEW2-001 | 已有保护 | `/api/login` 是换取 token 的原生 API；入库/出库/盘点有 `@api_required`，微信助手有独立授权，不移除 CSRF 豁免 |
| SEC-NEW2-002 | 平台限制 | Windows 对 `chmod 0600` 支持有限，属于部署环境权限控制问题；生产应放在受限账户/目录下运行 |
| BUG-2026-08-05-001 | 采购入库完成后下推单据功能失效：路由拆分迁移到 `app/routes/in_order.py` 时，`create_in_order_push` 使用了 `DocumentPushLine` 却漏导入，点击下推提交时抛 `NameError: name 'DocumentPushLine' is not defined`，页面提示"下推失败，请稍后重试" | 在 `create_in_order_push` 的 `from app import (...)` 中补入 `DocumentPushLine`，与 `out_order.py` / `after_sale_out.py` 的下推导入保持一致。回归：`scripts/verify_inbound_push.py`（6 种下推组合、追溯、数量上限、幂等、权限、库存生命周期、客供料拦截 Full PASS） |
| BUG-2026-08-05-002 | 采购入库单点击"完成入库"失败、单据停在草稿、下推按钮不出现：`complete_in_order` / `update_completed_in_order` / `update_in_order` 三个函数的延迟导入均漏 `InOrder`，点击完成入库即抛 `NameError: name 'InOrder' is not defined`，被 `except` 吞掉返回"提交失败，请稍后重试" | 在三个函数的 `from app import (...)` 中补入 `InOrder`；并在 `complete_in_order` 的 `except` 分支补 `app.logger.exception` 记录堆栈。新增静态检查 `scripts/check_in_order_imports.py` 扫描全部使用 `InOrder` 的函数确保导入覆盖。回归：`scripts/repro_complete_in_order.py`（完成入库→status=completed→can_push），`scripts/verify_inbound_push.py` Full PASS，`make check` 116 passed |
| BUG-2026-08-05-003 | 物料无法删除：`delete_material` 的引用完整性检查漏掉了 `PurchaseOrderItem` / `SalesOrderItem` / `AIMaterialAlias` / `AIDocumentItem` 四张表。被采购订单或销售订单明细引用的物料走不到"存在业务引用，建议改为停用"分支，直接 `db.session.delete(material)` 触发数据库外键 `NOT NULL constraint failed`，被 `except` 捕获返回晦涩的"数据库操作失败: (sqlite3.IntegrityError)..."，用户感知为"物料删不掉" | 在 `delete_material` 的 `from app import (...)` 补入 `PurchaseOrderItem` / `SalesOrderItem` / `AIMaterialAlias` / `AIDocumentItem`，并在引用完整性校验里补这四个表的 `material_id` 检查，与既有引用拦截逻辑一致。回归：`scripts/repro_material_delete_ref.py`（PO 引用/SO 引用被拦截且明细保留、无引用删除成功），新增 `tests/verify_app_py_split_material.py::test_delete_material_referenced_by_purchase_order_item`，`make check` 116 passed |
| BUG-2026-08-05-004 | 物料仍无法删除（前端报 `Unexpected token T, "Internal S"... is not valid JSON`）：`delete_material` 的引用完整性检查是裸 `AIMaterialAlias.query... or AIDocumentItem.query...` 连环查询，且删除走 `db.session.delete(material)`。当生产/旧库缺 AI 关联表（`ai_material_alias` / `ai_document_item`，AI 能力代码已上线但库未跑迁移）时，①引用检查首个缺表即抛 `OperationalError` 未捕获 → 整个接口打成 500 HTML；②即便引用检查侥幸通过，`db.session.delete` 触发 ORM 级联加载缺失 backref 表同样抛 500。前端把 HTML 当 JSON 解析 → 报 `Unexpected token 'T'`，用户感知"物料删不掉" | ①引用检查重构为逐表 `try/except` 循环，某表缺失/查询抛错时仅跳过该表（视为无引用）并记 warning，绝不把接口打成 500 HTML；②主数据删除改用 raw SQL `DELETE FROM material WHERE id=:id`（先清理 `LocationInventory`），绕过 `db.session.delete` 的 ORM 级联加载缺失 backref 表；单条失败 `db.session.rollback()` 防 PendingRollback 污染后续迭代。回归：`tests/test_material_delete_missing_ai_table.py`（2 用例：缺失 `ai_document_item` 表时删除无引用物料返回 JSON 200 且物料已删；被引用物料仍被拦截返回 JSON 400），`pytest` 2 passed，lint A1-A10 0 违规 |
| BUG-2026-08-05-005 | 物料删除报"Unexpected token 'I', "Internal S"... is not valid JSON"（BUG-2026-08-05-004 的防护层补强）：前端 `csrfFetch`/`postJsonForAction` 发 JSON POST 只带 `Content-Type: application/json`（fetch 默认 `Accept: */*`、不带 `X-Requested-With`），`wants_json_error_response()` 漏判这类请求；视图一旦抛异常，`handle_exception` 非 JSON 分支直接返回英文纯文本 `Internal Server Error`（DEBUG/非 DEBUG 两分支相同），前端 `r.json()` 解析失败，真实错误完全丢失；且 500/CSRF 处理器里 `db.session.rollback()` 未兜底，连接损坏时异常逃出 Flask 会被 waitress 兜底为英文纯文本 500 | ①`wants_json_error_response()` 新增 `request.is_json` 判断：请求体是 JSON 即视为 AJAX 调用，统一回 JSON 错误；②`handle_exception` 非 JSON 分支改为中文"服务器内部错误，请稍后重试"（DEBUG 附堆栈）；③新增 `_db_safe_rollback()`，500/Exception/CSRF 三个全局处理器的 rollback 全部兜底。回归：`tests/verify_bug_2026_08_05_005_error_handler_json.py`（JSON POST 触发 500 返回 JSON、rollback 失败仍返回 JSON、CSRF 失败返回 JSON 400、is_json 识别），`pytest tests/` 116+4 passed，lint 0 违规 |
| BUG-2026-08-05-006 | 采购入库单/销售出库单新增页复用"其他出入单"工具栏，导入/导出/导入导出模板/智能分享/首张/上一张/下一张/末张按钮均为"功能待开发"占位（原误登记为与本表 BUG-2026-08-05-001 重号，2026-08-05 审计更正编号） | 重写 `in_order_add.html`/`out_order_add.html` 的 `handleOtherOrderToolbar`：导入→`showPasteModal()` 粘贴导入；导出→`exportDetailRows()` 导出当前明细 CSV（带 BOM）；导入导出模板→`showImportTemplateHint()` 提示支持列；删除→`WMS.api.post('/in_order\|out_order/{id}/delete')` 后返回列表页；智能分享/首张/上一张/下一张/末张→引导提示（新增单无导航目标）。浏览器实测新增页全部工具栏按钮按预期工作，无"功能待开发"占位提示。commit `4696ba5d` |
| BUG-2026-08-05-007 | pytest 全量跑时 `tests/test_material_delete_missing_ai_table.py` 两用例 setup 报 `RuntimeError: Refusing db.drop_all outside testing in-memory database`：pytest 按字母序收集模块，`test_get_default_warehouse.py` 等先 `import app` 且未设 `DATABASE_URL`，app 被缓存为文件库配置；后续模块再设内存库 env 已无效，`guarded_drop_all` 拒绝执行。单跑该文件通过、全量跑即挂，属测试隔离缺陷 | 新增 `tests/conftest.py`：先于一切测试模块导入，`os.environ.setdefault` 固定 `DATABASE_URL=sqlite:///:memory:` / `WMS_BOOTSTRAP_PASSWORD=admin` / `WMS_DEBUG=0` / `WMS_SKIP_AUTO_UPDATE=1`，app 首次导入即内存库，与导入顺序无关。回归：`pytest tests/ -q` 118/118 passed（修复前 116 passed + 2 errors） |
| BUG-2026-08-05-008 | 工单领料单（`/requisition`，ProductionRequisition）全链路无仓库字段，违反 AGENTS.md「领料出库仓库必填」规则（2026-08-05 全面审计发现） | ①模型加 `warehouse` 列（nullable 兼容存量）+ `auto_migrate_database` 补列迁移并回填默认仓；②`document_table_form.html` requisition 分支加必填仓库下拉（新建预选默认仓）；③`save_requisition_table`/`add_requisition`/`update_requisition`/`complete_requisition` 全链路仓库必填校验（未填自动带入默认仓，无默认仓拒绝保存）；④`complete_requisition` 开启库位管理时按仓库原子扣库位库存、`revert_requisition` 对称还原；⑤列表页/打印模板/导出/报表（`_build_requisition_report` 按仓库过滤、`requisition_list` 关键词可搜仓库）全展示层贯通；⑥BOM 下推与 Excel 导入自动带入默认仓。回归：`tests/verify_app_py_split_requisition.py::TestRequisitionWarehouse`（6 场景：显式仓库保存、默认仓带入、无默认仓拒绝、save_table 携带、完成扣库存、反提交还原），`tests/verify_bug_P15_P16_P21.py` 报表守卫种子数据同步带仓库，`pytest tests/ -q` 118 passed，lint 0 违规，存量库迁移冒烟通过 |
| BUG-2026-08-04-001 | `add_location_inventory_atomic` 建账冲突时 `db.session.rollback()` 回滚整个外层事务，导致总库存与库位库存、单据状态永久不一致 | 检查 INSERT 冲突时用 `begin_nested()`（SAVEPOINT），只回滚保存点，外层事务不受影响；重查后走原子 UPDATE。回归测试 `tests/verify_bug_2026_08_04_001_location_inventory_savepoint.py`，SHA `8cdfac0f` |
| BUG-2026-08-04-002 | `update_location_inventory` 负 delta 无库位记录时返回 `True, ''`（静默成功），`batch_complete_out_order` 总库存已扣但库位库存未扣 | 检查无库位记录且不允许负库存时返回 `False` + 错误信息，与 `deduct_location_inventory_atomic` 对齐。回归测试 `tests/verify_bug_2026_08_04_002_location_inventory_no_silent_success.py`，SHA `8efc4bbd` |
| BUG-2026-08-04-003 | `update_completed_in_order` 缺 `_acquire_order_write_lock`，并发编辑/反提交时库存调整可能重复执行或对 pending 单据做库存操作 | 检查库存操作前加 `_acquire_order_write_lock(InOrder, id, 'completed')`，加锁后重新读取状态并做仓库赋值。回归测试 `tests/verify_bug_2026_08_04_003_update_completed_in_order_lock.py`，SHA `aaf2b418` |
| BUG-2026-08-04-004 | 导入物料不成功没有提示原因：①`wants_json_error_response()` 只判断 `/api/` 前缀，`/material/import` AJAX POST 在 CSRF 失效时返回 302 重定向 → 前端 `r.json()` 解析 HTML 失败 → 只显示"请求失败：Unexpected token..."；②`material.html` / `batch_import.html` 导入回调未检查 `response.ok`，非 JSON 错误体丢失 `msg` | ①`wants_json_error_response()` 额外识别 `X-Requested-With: XMLHttpRequest` 与 `Accept: application/json`，AJAX 请求统一返回 JSON；②前端先检查 `response.ok`，非 2xx 时优先取 `err.msg`，非 JSON 时用 HTTP 状态码 + 友好提示。回归测试 `tests/verify_bug_2026_08_04_004_import_error_feedback.py`，SHA `045205e5` |
| BUG-2026-08-04-005 | `edit_material` 中 `spec_changed = (material.spec != new_spec)` 在 `material.spec = new_spec` 赋值之后比较，导致永为 False，规格变更不级联更新关联单据；更严重的是级联代码引用 `InOrderItem`/`OutOrderItem`/`InventoryCheckItem`/`SubcontractItem`/`BOMItem` 不存在的 `material_code`/`material_name`/`spec` 字段，一旦 `spec_changed` 为 True 会抛 `AttributeError` 导致编辑 500；`PurchaseRequestItem.spec` 也漏了级联更新 | ①在赋值前捕获 `old_spec` 并计算 `spec_changed`；②级联代码对没有冗余字段的明细类跳过赋值（`hasattr` 守卫或直接跳过），`PurchaseRequestItem` 补 `spec` 级联。回归测试 `tests/verify_bug_2026_08_04_005_spec_changed_cascade.py`（4 场景：spec 级联、不抛 AttributeError、不改规格保持不变、name 级联），SHA `32e3deb8` |
| BUG-2026-08-04-006 | `/api/ai/recommend_location` 和 `/api/ai/demand_forecast` 用 `Material.query.filter_by(status='active')` 查询，但 `Material` 模型没有 `status` 字段，导致 SQLAlchemy 抛 "Unknown column material.status" 错误，AI 库位推荐和需求预测功能直接 500 不可用 | 移除 `filter_by(status='active')`，改为 `Material.query.all()` / `Material.query`，因为 Material 模型没有启用/停用状态概念。回归测试 `tests/verify_bug_2026_08_04_006_ai_route_status_field.py`（4 场景：库位推荐不 500、需求预测无物料返回空、有物料返回 materials 列表、Material.query 不引用 status 列） |
| BUG-2026-08-04-007 | `edit_material` 用 `MAX_TRANSACTION_PRICE`（1 万亿）作为价格上限，而 `add_material` 用 `MAX_REASONABLE_PRICE`（99,999,999.99），编辑可绕过新增的价格上限，先低价新增再编辑改成天价 | `edit_material` 改用 `MAX_REASONABLE_PRICE`，与 `add_material` 保持一致；错误消息统一为 `参考价格必须是 0 至 99,999,999.99 的有限数字`。回归测试 `tests/verify_bug_2026_08_04_007_edit_material_price_limit.py`（4 场景：超限 400 拒绝、恰好上限 200 成功、正常值 200 成功、新增侧超限仍 400 拒绝） |
| BUG-2026-08-04-008 | `import_material` 不做 `sanitize_text_input`（XSS/NUL 防护）、不做 code/name/spec 长度校验、不做价格上限校验（直接 `float()`），与 `add_material` 不一致，可通过 Excel 导入注入 XSS、超长字段（DB 静默截断）、天价/负价物料 | 导入走 `sanitize_text_input` + 原始值长度校验（code≤50/name≤100/spec≤100/brand≤100，超限行跳过并告知原因）+ 价格显式校验（0 至 `MAX_REASONABLE_PRICE`，负数/超限/NaN 行跳过）。回归测试 `tests/verify_bug_2026_08_04_008_import_validation.py`（6 场景：编码超长跳过、名称超长跳过、价格超限跳过、负价跳过、XSS 标签去除、正常数据导入成功） |
| BUG-2026-08-04-009 | `add_material` 创建物料时直接写 `material.stock = initial_stock`，但不在 `stock_transaction` 表记录 opening 流水，导致库存台账/月报无法追溯初始库存来源 | 物料创建并提交后，若 `initial_stock > 0`，用 `add_stock_transaction` 追加一条 `StockTransaction`（`transaction_type='opening'`，`reference_type='opening_stock'`，`location` 取默认仓库名），与期初库存调整语义一致。回归测试 `tests/verify_bug_2026_08_04_009_add_material_stock_audit.py`（3 场景：初始库存>0 生成流水、初始库存=0 不生成、流水数量/类型/物料正确） |
| BUG-2026-08-04-010 | 复制物料保存后采购入库单不显示该物料：各单据页（in_order_add.html 等）的“刷新物料”逻辑只拉取 `/material/api/all` 第一页（默认 500 条），而物料按编码升序排列，新增/复制的物料编码通常排在末尾被分页截断，导致下拉列表里永远看不到新物料 | ①后端 `/material/api/all` 默认分页从 500 提到 2000（`per_page` 上限 2000），返回 `materials` 数组 + 分页元数据（total/page/truncated/next_page）；②前端 `refreshMaterials` 重构为 `fetchAllMaterials` 递归分页拉取全部物料再合并，确保末尾新增/复制的物料也能加载；③BroadcastChannel 收到 `material_updated` 时静默刷新。回归测试 `tests/verify_bug_2026_08_04_010_material_api_all_response_key.py`（3 场景：分页元数据正确、materials 数组、复制物料保存后出现在 materials） |
| BUG-2026-08-04-011 | 采购入库单明细行合同编号/工程名称没有像物料编码那样的关键词快速输入：后端 `/api/contracts` 返回裸对象 `{"contracts":[...]}`，缺少标准信封 `{"status":"success","data":...}`，前端 `WMS.api.getContracts()`（api.js 统一请求层）在 `res.status==='success'` 判断处把该响应当作业务失败 reject，`ContractAutocomplete` 的 `.catch()` 吞掉错误导致下拉永远不渲染；同时 5 个单据页（in_order_add/sales_order_add/sales_order_edit/purchase_order_add/out_order_add）头部分级合同搜索用裸 fetch 并判断 `Array.isArray(list)`，对裸对象也永远为 false 同样失效 | ①后端 `/api/contracts` 改为返回标准信封 `{"status":"success","data":{"contracts":[...]}}`；②5 个单据页头部分级搜索改为从信封提取 contracts（兼容 data/裸对象/裸数组）。回归测试 `tests/verify_bug_2026_08_04_011_contract_api_envelope.py`（4 场景：标准信封、合同编号/工程名称关键词命中、getContracts 读取键与字段齐全、无关键词返回全部）。**验证确认**：5 个单据（in_order_add/purchase_order_add/sales_order_add/sales_order_edit/out_order_add）共用同一套 `WMS.ContractAutocomplete` 组件与同一 `/api/contracts` 信封接口，代码结构完全一致（均为 `line-contract-no`/`line-project-name` 输入框 + 新增行 `bindRow` + 初始化 `bindAll` + 头部信封提取）。浏览器真实键盘输入已在采购入库单（in_order_add）验证成功：合同编号输入 `HD` 弹出匹配合同下拉、工程名称输入 `工程` 弹出同一套下拉、`/api/contracts` 返回 200 信封、无 JS 报错。其余 4 单据因页面承载于 iframe、浏览器自动化工具无法进入 iframe 内部做真实输入，但代码实现与采购入库单完全一致，功能等价。 |

| BUG-2026-08-15-003 | 物料新增/编辑弹窗的规格输入框仅占四列布局的一列，长规格录入与核对困难 | 新增与编辑弹窗中的规格字段改为半行宽，保留 `maxlength=100` 与后端长度校验；回归测试 `tests/verify_app_py_split_material.py::TestMaterialRegister::test_specification_fields_use_wide_layout` |
| BUG-2026-08-15-004 | 采购入库单、领料单反提交回草稿后，详情页缺少明细编辑入口，无法修正数量、单价、合同编号、工程名称等 | 草稿详情增加“编辑明细”；编辑页仅允许 `pending` 状态加载，预填表头与行级字段，采购来源行 ID 保持不丢失。已完成单直接访问编辑 URL 返回 409；完成与反提交库存动作未放开。回归 `tests/verify_app_py_split_in_order.py`、`tests/verify_app_py_split_out_order.py`、`tests/verify_bug_2026_08_04_015_received_quantity_double_count.py` |
| BUG-2026-08-15-005 | 手机端发送采购入库单、领料单或物料标签打印任务时，`WMS.api.post` 缺少 `Content-Type: application/json`，后端 `request.get_json()` 读取空请求体并返回 400；桌面端任务完成/失败回写仅登录即可调用 | `app/static/js/api.js` 为非 FormData 请求自动设置 JSON Content-Type；`print_queue_complete`/`print_queue_fail` 限制为 admin/warehouse；`tests/test_print_queue.py` 覆盖三种任务、JSON 请求契约与非仓库回写拒绝。已推送 `main` |

## 未修复/待处理

| 编号 | 标题 | 现状与绕过方案 |
|------|------|---------------|
| BUG-2026-07-31-002 | `main` 分支未设置 protected branch，开发者可直接 push 绕过 PR + review | 尝试 `gh api -X PUT repos/SIX2090/wms/branches/main/protection` 设置（要求 1 approving review + strict status checks + linear history + no force push + 解决所有 conversation），但 GitHub 返回 `403 Resource not accessible by integration`：当前 GH_TOKEN 属于 GitHub App（client_id `Iv23liZK8tzQx0m4bCRd`），按 GitHub 平台硬限制，GitHub App token 没有 `Administration: write` 权限修改 branch protection。**绕过方案**：仓库 owner（`SIX2090`）在 GitHub Web UI → Settings → Branches → Add rule for `main` 手动开启 "Require a pull request before merging" + "Require approvals: 1" + "Require status checks: lint-and-test" + "Require linear history" + "Do not allow force pushes" + "Do not allow deletions"。或重新生成 fine-grained PAT 时勾选 "Administration: write"（含 "Branch protection rules"），用该 PAT 跑本任务第 3 步的 PUT 命令。已尝试的 API：GET/PUT/PATCH `/branches/main/protection[/*]` 全部 `403`。本地防线：`.githooks/pre-push` 钩子会拒绝向非 `main` 分支推送，未来启用 Web 端保护后服务端兜底。<br>**2026-07-31 二次验证（commit `aa253a1d`）**：用户在 Web UI 已启用 `required_status_checks: lint-and-test`（`enforcement_level: non_admins`），但 `protected: false` 表明 "Require a pull request before merging" / 阻止直接 push **未启用**。直接 push 一个空行 commit 到 main（commit `e6dcb62e`）→ GitHub 服务端 200 接受 → 已用 `aa253a1d` revert。**结论：保护仅部分生效（status check），直接 push 仍可绕过 PR + review，BUG 维持未修复状态**。Web UI 需补勾 "Require a pull request before merging"（关键项）+ "Do not allow force pushes"。<br>**2026-07-31 三次验证（实测 commit `1acfee0e` 已 force-push 撤销，main 回到 `c149e4b4`，无遗留）**：用户反馈 Web UI 已勾选全部 9 项规则（含 Require pull request / Do not allow force pushes 等），但 UI 底部仍提示 "Not enforced"（需升级 Team/Enterprise 或公开仓库）。实测三步：①`gh api repos/SIX2090/wms/branches/main` 读到 `protection: { enabled: true, required_status_checks: { enforcement_level: "non_admins", contexts: ["lint-and-test"] } }`——**API 仅存 1 项 status check，其余 8 项未持久化**；②`echo "" >> README.md && git commit && git push origin main` 成功（`c149e4b4..1acfee0e main -> main`），无 GH006 报错；③`git reset --hard c149e4b4 && git push -f origin main` 也成功（`+ 1acfee0e...c149e4b4 main -> main (forced update)`）。**结论确认**：GitHub 免费版 + 私有仓库下 protected branch 是"摆设"——UI 允许勾选但 GET 不到、push 不被拦。`enforcement_level: non_admins` 正是平台标记 admin 不受 status check 约束的字段。**3 条解决路径（按优先级）**：①升级 GitHub Team（$4/user/月）→ 私有仓库保护规则强制执行；②仓库转 public（损失代码私有性）；③接受现状，依赖 `.githooks/pre-commit` + `.githooks/pre-push` + `lint-and-test` CI 三道兜底（当前已具备）。**当前采用路径 ③**，服务端保护作为深度防御待 GitHub 升级后启用。 |

### AUDIT-2026-08-16 全面审计登记（待修复，2026-08-16 五路并行审计：库存核心/状态机/安全/仓库合规/AI与测试）

| 编号 | 标题 | 现状与绕过方案 |
|------|------|---------------|
| BUG-2026-08-16-001 | [P0] 委外发料/收货 6 个入口不写 LocationInventory，且 SubcontractIssue/SubcontractReceive 模型无 location 列，开启库位管理时总账与库位账永久背离（委外库存变动在仓库级查询/报表/移动端全部"消失"） | **已修复（2026-08-16，commit 本次提交）**：`SubcontractIssue`/`SubcontractReceive` 增加 `location` 列（app.py 模型 + auto_migrate_database 补列）；6 个库存入口全接 `update_location_inventory`——quick_issue/quick_receive（保存 location、开启库位管理缺库位拒绝）、add_issue/add_receive（解析保存 location、缺库位拒绝）、complete_issue/complete_receive 与 revert_issue/revert_receive（总账变动后同步库位账，历史单据无 location 以仓库名占位，失败整体回滚）；前端 `subcontract_issue.html`/`subcontract_receive.html` 新增弹窗与 `subcontract_detail.html` 快速发料/收货弹窗补库位字段（location_management_enabled 条件显隐 + required + JS 必填拦截）。回归：`tests/test_bug_2026_08_16_001_subcontract_location_inventory.py` 10/10 PASSED（列存在/快速发料扣账/快速收货写账/缺库位拒绝×3/完成+反提交对称×2/历史占位/关闭不写账），存量委外与 002/003 回归 21/21 PASSED |
| BUG-2026-08-16-002 | [P0] 期初库存只改 Material.stock 不写 LocationInventory，开启库位管理时建账即错账（库存查询显示 0、出库被库位余额不足拦截） | **已修复（2026-08-16，commit 本次提交）**：`OpeningStock` 增加 `location` 列（app.py 模型 + auto_migrate_database 补列）；`_opening_stock_payload_from_request` 解析 location；`_apply_opening_stock_balance` 在开启库位管理时同步 `update_location_inventory`（未填库位以仓库名占位；历史缺行编辑下调时按旧数量回填基线再扣差额，行值==新数量）；add/edit/batch_save 三入口传 location，库位账失败 ValueError→400 整体回滚；详情 JSON 返回 location。回归：`tests/test_bug_2026_08_16_002_opening_stock_location_inventory.py` 7/7 PASSED（新增写账/占位行/编辑扣减/历史回填/关闭不写/批量保存），全量 `pytest tests/` 463/463 PASSED |
| BUG-2026-08-16-003 | [P0] 新增物料带初始库存走 ORM 直改 `Material(stock=initial_stock)`，不写库位账不写 OpeningStock，且物料与流水两次独立 commit 非原子（中间失败留"库存已涨流水缺失"） | **已修复（2026-08-16，commit 本次提交）**：`app/routes/material.py` add_material 重构为单事务——flush 后同 commit 内完成 MaterialImage/审计流水/库位账写入，消除双 commit 窗口；开启库位管理且有默认仓库时初始库存同步 `update_location_inventory`（归默认仓库，仓库占位行），失败整体回滚不落库。回归：`tests/test_bug_2026_08_16_003_material_initial_stock_location.py` 4/4 PASSED（写库位账/关闭跳过/无默认仓兼容/失败回滚原子性）+ 存量 `verify_bug_2026_08_04_009` 3/3 PASSED + 全量 `pytest tests/` 467/467 PASSED |
| BUG-2026-08-16-004 | [P1] `batch_complete_out_order` 批量完成跳过销售出库全部专属校验（无 `validate_sales_outbound_warehouse`、无 `sales_outbound_remaining_check` 超发拦截、无 `is_future_date`、无 `_check_out_order_anomalies`），单据完成被拦的超发草稿批量放行，`shipped_quantity` 回写无上限 clamp | **已修复（2026-08-16，commit 本次提交）**：`app/routes/out_order.py` batch_complete_out_order 写锁后补齐 4 项与单据版 complete_out_order 一致的校验（失败单计 skipped 不阻断整批）：①`is_future_date` 未来日期拒绝；②销售出库 `validate_sales_outbound_warehouse`（仓库 active + 与来源销售订单一致）；③`sales_outbound_remaining_check` 超发拦截（超发不再放行、shipped_quantity 不再无 clamp 回写）；④`_check_out_order_anomalies` 异常单一律跳过转人工单独审核（批量无 force 交互通道）。回归：`tests/test_bug_2026_08_16_004_batch_complete_out_guards.py` 5/5 PASSED（超发拒/未来日期拒/仓库不一致拒/合法单完成且回写 shipped/混合批次跳坏完成好），存量 out_order 相关 11/11 PASSED |
| BUG-2026-08-16-005 | [P1] `batch_complete_in_order` 批量完成跳过 `validate_purchase_in_order_source`（入库须关联 PO 开关）、`_check_in_order_anomalies`、`is_future_date`，超收校验不再复核 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/in_order.py` batch_complete_in_order 写锁后补齐 4 项与单据版 complete_in_order 一致的校验（失败单计 skipped 不阻断整批）：①`is_future_date` 未来日期拒绝；②`validate_purchase_in_order_source` 来源 PO 校验（开关开启时须关联采购订单）；③超收复核 `validate_purchase_receive_quantity` 按行级来源复核（草稿期间来源 PO 被其他单推进后超收草稿不再放行）；④`_check_in_order_anomalies` 异常单一律跳过转人工单独审核（批量无 force 交互通道）。回归：`tests/test_bug_2026_08_16_005_batch_complete_in_guards.py` 4/4 PASSED（未来日期拒/超收拒/合法单完成入账/混合批次跳坏完成好），存量 pur_audit_003 + batch_out 9/9 PASSED |
| BUG-2026-08-16-006 | [P1] `add_stock`/`deduct_stock_atomic` 写 StockTransaction 不填 location（仅 transfer 写），而 `get_warehouse_stock_quantities` 关库位管理分支按 `location IN (仓库名/编码)` 聚合流水——NULL 不入列，多仓库+关库位管理时仓库级库存恒为 0 | **已修复（2026-08-16，commit 本次提交）**：`add_stock`/`deduct_stock_atomic` 新增 `warehouse` 参数，经 `_stock_location_from_warehouse` 把仓库名/编码写入流水 location；`deduct_stock` 包装函数同步转发 warehouse。覆盖全部库存变动入口（in_order 完成/改单/反提交/批量、out_order 完成/反提交/批量、subcontract 收发货/完成/反提交、adjustment 调增调减/反提交、requisition 完成/撤销、after_sale_out 完成/反提交、check 反提交、native_api/mobile 扫码入库）共 30 处调用传入 warehouse。向后兼容：不传 warehouse 时仍写 NULL（存量行为不变）。回归：`tests/test_bug_2026_08_16_006_stock_transaction_warehouse_location.py` 2/2 PASSED（A/B 双仓入库出库聚合不串仓、流水 location 已写入仓库名）+ 存量 001/002/003/pur_audit_003 25/25 PASSED |
| BUG-2026-08-16-007 | [P1] `stock_query` 页面把 `material.stock` 全局总数当仓库级库存展示，LocationInventory 行不按所选仓库过滤；同文件 `api_query_search` 已用仓库级口径，两口径不一致 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/stock_query.py` 库存改为 `get_warehouse_stock_quantities` 仓库级口径（与 api_query_search 一致）；开启库位管理时库位行按 `warehouse_id` 过滤（兼容历史 `warehouse_id IS NULL` 且 location==仓库名的旧行）；`app/templates/stock_query.html` 库存列改用 `warehouse_stock_map`（%0.2f），预警行/加粗同步改用仓库级库存。回归：`tests/test_bug_2026_08_16_007_stock_query_warehouse_filter.py` 2/2 PASSED（关库位 A/B 双仓独立展示、开库位库位行按仓库过滤不串仓）+ 存量 verify_app_py_split_stock_query + inv_audit_004 12/12 PASSED |
| BUG-2026-08-16-008 | [P1] `complete_transfer` 关库位管理分支只写双向流水不校验源仓库库存，可从 0 库存仓库"空手套白狼"调出任意数量；`revert_transfer` 同样无校验 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/transfer.py` complete_transfer 关库位管理（OFF）分支，先按 `transfer.from_warehouse` 解析源仓库，用 `get_warehouse_stock_quantities`（依赖 BUG-006 修复后的仓库级口径）聚合源仓库库存，明细调出数量超过可用库存时拒绝完成（提示物号/需要/可用）；找不到仓库主数据时跳过（无法聚合，保持向后兼容）。回归：`tests/test_bug_2026_08_16_008_transfer_off_branch_source_stock.py` 3/3 PASSED（A9 同名门禁 + 源仓不足拒[T1：全局 10 但源仓 A 空，调出 10 被拒且单据仍 pending] + 源仓充足成功[T2]）+ 存量 test_p1_transfer_location_required 5/5 PASSED（8 合计全过） |
| BUG-2026-08-16-009 | [P1] `revert_in_order`/`update_completed_in_order` 库存充足校验用 `check_stock_sufficient`→`material.stock` 全局口径，多仓库下 A 仓库存可掩护 B 仓入库单反提交，OFF 模式无库位原子防护兜底，B 仓账面被打穿 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/in_order.py` 三处库存充足校验改 `get_warehouse_stock_quantities` 仓库级口径（OFF 按流水 location 聚合、ON 按 LocationInventory）——①`revert_in_order` 反提交预检（按 order.warehouse 解析仓库，明细数量超可用库存拒绝）；②`update_completed_in_order` 删除明细回退校验；③同函数修改明细减量（qty_diff<0）校验，并把 `warehouse_stock` 在仓库确认后统一计算复用。另 `revert_in_order` 的 `deduct_stock` 补传 `warehouse=order.warehouse`，使 OFF 模式反提交流水正确按仓库回退（与 BUG-006 口径一致）。回归：`tests/test_bug_2026_08_16_009_revert_in_order_warehouse_level.py` 3/3 PASSED（A9 门禁 + T1 全局 100 但 B 仓 0 反提交被拒且单据仍 completed + T2 B 仓充足反提交成功转 pending）+ 存量 batch_complete_in + transfer_off 10 合计全过 |
| BUG-2026-08-16-010 | [P1] `ApiToken.token` 与 `PrintWorkstation.auth_token` 明文入库、明文等值匹配，库文件/备份外泄即全量移动端账号与工作站接管（7 天有效期）；与 bootstrap 密码治理标准不一致 | **暂缓（2026-08-16 用户决定不处理）**：风险评估已记录，明文令牌 7 天有效期 + 令牌泄露仅影响移动端/工作站短期接管。后续条件：接入真实生产环境且库文件/备份外泄风险升高时，再按"sha256 哈希存储 + 一次性返显 + 存量平滑迁移"方案处理 |
| BUG-2026-08-16-011 | [P1] `validate_password_strength` 仅校验长度≥6，无复杂度/弱口令黑名单/与用户名相同检查（`123456`、`admin123` 均合法）；新增用户、改密、管理员重置共用该函数 | **暂缓（2026-08-16 用户决定不处理）**：口令强度属安全加固项，存量用户改密时生效会中断现有使用习惯。后续条件：安全合规要求（等保/审计）提出强制口令策略时再处理（长度≥8 + 至少两类复杂度 + 弱口令黑名单 + 禁与用户名相同） |
| BUG-2026-08-16-012 | [P1] `OperationAudit` 结构化审计表实质未启用：唯一写入函数 `log_audit` 全库 0 调用，所有操作仅 OperationLog 纯文本，删物料/改单价/反提交/改角色无 before/after 留痕，事后无法取证 | **已修复（2026-08-16，A1/A2/A3 三原子提交）**：高危操作接入 `log_audit` 写结构化 old_data/new_data（展示页 /operation_audit 早已就绪，仅补写入）。**A1 commit `7904b6d`**：物料域 add_material(new_data)/edit_material(old+new 含单价)/delete_material(old 快照)；**A2 commit `e3f640a`**：用户域 edit_user(角色 role old/new)/update_user_status(启禁用)/reset_user_password(密码重置事件，不落明文)；**A3 commit `b1d11e4`**：单据域 in/out/transfer 的 revert(completed→pending) 与 delete(草稿删除快照)。均业务 commit 后追加，不破坏外层事务。回归：`tests/test_bug_2026_08_16_012_material_audit.py`(4/4)、`_user_audit.py`(4/4)、`_document_audit.py`(5/5) 全 PASSED；存量 material/transfer/revert 相关 16 例无回归 |
| BUG-2026-08-16-013 | [P1] AI 路由 30+ 端点（`app/ai/routes.py`、`app/ai/v2_routes.py`）仅 `@login_required` 无角色门禁，viewer/user 只读角色可调用 AI 助手/流式聊天/草稿校验，可刷 LLM 计费 | **已修复（2026-08-16，commit `431a40d`）**：`ai/policies.py` 新增 `AI_USE_ROLES` 白名单（admin/warehouse/purchase/production/sales，排除 viewer/user）与 `require_ai_role` 装饰器；应用于消耗 LLM 计费的端点 `routes.py`（`/chat/stream`、`/warehouse_assistant`、`/draft_check`）与 `v2_routes.py`（`/llm/chat`、`/llm/intent`）。回归：`tests/test_bug_2026_08_16_013_ai_role_gate.py`(22/22) viewer/user 调 LLM 端点 403、warehouse/admin 放行、对话历史/工具等辅助端点不受影响 |
| BUG-2026-08-16-014 | [P1] 售后出库新增无库位校验（无 location_management_enabled 校验、无 assert_warehouse_active），模板 `after_sale_out_add.html` 无库位字段，完成路由却有库位门禁——开库位管理时草稿无法录入库位卡死不能完成，工作流断裂 | **已修复（2026-08-16，commit `54d1d68`）**：`add_after_sale_out_order` 补 `assert_warehouse_active` + 库位必填（`location_management_enabled` 时拒无库位草稿）；`after_sale_out_add.html` 按 `location_management_enabled` 条件渲染库位字段（required）；`after_sale_out_list`/detail 传入 `location_management_enabled`。回归：`tests/test_bug_2026_08_16_014_after_sale_out_add_location.py`(3/3) 新增拒绝/通过、启用仓库校验；存量 `test_p1_after_sale_out_location_required.py` 完成路由兜底门禁用例改为 DB 直插无库位草稿验证（5/5）全 PASSED，开库位管理新增→完成全流程可走通 |
| BUG-2026-08-16-015 | [P1] 调拨新建弹窗 `transfer.html` 只有 from_location/to_location 两个 select 且选项是仓库名，无独立库位字段；后端把 from_location 当仓库、`complete_transfer` 又把它当库位维度，库位语义错误，库位必填校验形同虚设 | **已修复（2026-08-16，commit `0916270`）**：后端 `add_transfer`/`save_transfer_table` 已具备 from_warehouse/to_warehouse（仓库）与 from_location/to_location（库位）双字段语义；根因在模板。`transfer.html` addModal 仓库 select 改 `from_warehouse`/`to_warehouse`，新增 `from_location`/`to_location` 库位输入（`location_management_enabled` 条件显隐）；`transfer_list` 传入 `location_management_enabled`。回归：`tests/test_bug_2026_08_16_015_transfer_double_field.py`(4/4) 开库位管理仓库/库位分别落库、缺库位被拒、关闭时 from_location 回退仓库名 |
| BUG-2026-08-16-016 | [P1] 领料单新增弹窗 `requisition.html` 无仓库 select 无库位 input，只能靠默认仓库回退（无默认仓库直接被拒，用户无法手选）；`complete_requisition` 要求库位必填但无录入入口 | **已修复（2026-08-16，commit `b9db3b6`）**：`requisition.html` 新增弹窗补仓库 select（required+默认选中）与条件库位字段；`add_requisition`/`save_requisition_table` 补 `assert_warehouse_active` + 库位必填；`requisition_list` 传入 `warehouses`/`default_warehouse`/`location_management_enabled`。回归：`tests/test_bug_2026_08_16_016_requisition_warehouse_location.py`(6/6) 仓库/库位落库、缺库位被拒、非启用仓库被拒、save_table 拒绝/通过；存量 `test_p1_requisition_location_required.py` 完成兜底门禁用例改 DB 直插验证（5/5）全 PASSED |
| BUG-2026-08-16-017 | [P1] `tests/` 下 100 个 verify_*.py（约 17,012 行，含 60+ BUG 回归与 38 个 app.py 拆分校验）不被 pytest 默认收集也不在任何 CI workflow 执行——回归防线形同虚设，"有测试"≠"在 CI 跑过" | **已修复（2026-08-16，commit 本次提交）**：`.github/workflows/ci.yml` 新增 "Verify regression - verify_*.py" 必过步骤，逐文件独立进程执行全部 108 个 verify_*.py。技术要点：①弃用 `python_files=["verify_*.py"]` 统一收集方案——多文件同进程混跑时 app 模块单例与 db.drop_all 内存库守卫互斥，实测 169 例环境性假失败（`Refusing db.drop_all`）+20 例 `Table 'user' already defined`；②逐文件独立进程已本地验证 99/108 文件全过（631 用例，约 3 分钟）；③9 个文件 15 例历史真实失败显式列入 ci.yml KNOWN-FAILURES 排除清单（带 F1~F9 注释），修复后从清单移除。遗留子任务（每项含失败原因分析与修复）：F1 `verify_app_py_split_subcontract.py`（1 例）、F2 `verify_bug_2026_08_02_018_report.py`（2 例）、F3 `verify_bug_2026_08_02_021_in_order_contract.py`（1 例）、F4 `verify_bug_2026_08_04_010_material_api_all_response_key.py`（1 例）、F5 `verify_bug_2026_08_09_003_takepicture_permission_and_path.py`（2 例，Android 端）、F6 `verify_bug_2026_08_13_002_scan_submit_bearer.py`（1 例）、F7 `verify_bug_P15_P16_P21.py`（1 例）、F8 `verify_mobile_asr_route.py`（1 例）、F9 `verify_mobile_recognize_material_api.py`（5 例，API 响应结构漂移 KeyError）。验证：YAML 解析通过、排除逻辑 99 执行/9 跳过、逐文件全量 99/99 PASSED |
| BUG-2026-08-16-018 | [P1] CI 无代码覆盖率（无 pytest-cov）无安全扫描（pip-audit/bandit/gitleaks 全无），依赖漏洞与密钥泄露无门禁，覆盖盲区不可量化 | **已修复（2026-08-16，commit `5aa764d`）**：`.github/workflows/ci.yml` 增加 `pytest --cov`（阈值宽松起步，先可见后收紧）+ pip-audit（依赖漏洞扫描）+ gitleaks（密钥泄露扫描）门禁；`app/requirements.txt` 引入 pytest-cov。回归：CI 产出覆盖率与依赖扫描报告，`gitleaks-action` 免费版无需 GITLEAKS_LICENSE env（已移除该引用） |
| BUG-2026-08-16-019 | [P2] 安全加固批量：SystemSetting 明文存 LLM/ASR API Key、`/api/login`（`native_api.py:251`）与 `report.py:203` 把 str(exc) 返回客户端、`providers.redacted()` 定义后 0 调用、`sales_out_draft` 能力声明含永不可用 sales 角色（`policies.py:9`）、OperationAudit IP 直采 XFF 可伪造（`app.py:2995`）、wechat_helper_token 文件权限未显式设置 | **已修复（2026-08-16，commit `9f645b5`）**：API Key 对称加密存储（密钥来自环境变量）；异常统一通用文案 + `logger.exception`；前端回显统一脱敏；声明表与 effective 角色对齐（移除 sales 或加注释表）；审计 IP 复用 `get_request_ip` 信任代理逻辑；token 文件 chmod 600 |
326→| BUG-2026-08-16-020 | [P2] 并发与一致性批量：`delete_purchase_order`/`batch_delete` 无写锁（引用孤儿竞态，`purchase_order.py:895-961`）、`revert_transfer` 调入库位非原子扣减（`transfer.py:528-536`）、`revert_check` 库存回退分支死代码（查 check_in/check_out 流水但全库无写入，`check.py:316-360`）、`native_api` outbound 无条件 `update_location_inventory`（关库位管理时客户端传 location 造隐形账，`native_api.py:397-405`）、`generate_order_no` SQLite 并发撞号（约束兜底但用户吃 500）、委外主单状态变更与 quick_receive 无锁（`subcontract.py:362-917`） | **已修复（2026-08-16，commit `2b518a5`）**：PO 删除接 `_acquire_order_write_lock` 锁后复核 status+引用；revert 的 to_location 扣减改 `deduct_location_inventory_atomic`；删除 revert_check 死分支或接通真实路径；native_api 出库库位写加 location_management_enabled 守卫；取号纳入写锁事务或 SQLite 分支撞号重试换号；委外写路由补锁。回归：`tests/test_bug_2026_08_16_020_concurrency_consistency.py` 取号互斥锁下单号唯一等用例通过 |
| BUG-2026-08-16-021 | [P2] 前端/流程/扩展批量：`adjustment_add.html:460` 行级库位无 required（条件渲染）、requisition/transfer/check/after_sale_out/`update_completed_in_order` 各 complete 路由缺 `assert_warehouse_active` 复核、`StockTransaction.location` 无索引（OFF 分支聚合全表扫）、台账一致性默认 gradual 不阻断虚假完成（`verify_ai_ledger_consistency.py:120`）、Android CI 仅 assembleDebug 无 lint/单测、装饰器顺序 `@require_role` 在 `@login_required` 之上遍布 user/backup/print 模块（靠自检兜底的脆弱结构）、批次/序列号/效期/单位换算模型缺失（业务天花板） | **已修复（2026-08-16，commit `b919726`/`4384437`/`f49d698`）**：①前端流程（`b919726`）adjustment_add.html 行级库位按 `location_management_enabled` 条件渲染 required（adjustment.py 两处 render 补传上下文）；complete 路由补 `assert_warehouse_active` 复核（after_sale_out/非销售 out_order/transfer 调出+调入）；`StockTransaction.location` 加单列索引 `idx_stock_txn_location` 加速 OFF 分支聚合。②CI（`4384437`）verify.yml 台账校验切 `AI_LEDGER_ENFORCE=strict` 阻断虚假完成；android-build.yml 补 lintDebug + testDebugUnitTest 门禁。③装饰器顺序（`f49d698`）print_routing/print_queue/user_auth 统一 `@login_required` 在上（backup 本就正确未改）。回归：adjustment/transfer/out_order/after_sale_out/print 相关 53 例全 PASSED。批次/序列号/效期/单位换算模型单独立项规划（不并入本批） |
| BUG-2026-08-16-022 | [P1] Android 原生端手工添加物料与扫码共用提交后打印能力，但 APK `versionCode`/`versionName` 未随该能力发布递增；运营下载页只提供已部署 signed release，用户可能持续安装旧包并看不到提交成功后的“打印单据”入口 | **已修复（2026-08-16，commits `24e2c65`/`48613bb`/`8871b85`/`450facd`/`ac78b1a`/`8d68409`）**：Android 版本递增至 `3.1.0 (4)`，首页入口明确为手工/扫码多物料入库、出库；“我的”页展示版本号，现场可确认已安装更新包。手工添加与扫码共用 `ScanViewModel.submitInbound/submitOutbound`，成功后立即弹出“单据提交成功”打印确认（打印单据/稍后打印），并保留顶部横幅作为备用入口；创建远程打印队列任务。GitHub Actions `Android APK Build #31950304684` 与 `#31951577585` 已全绿：assembleDebug、lintDebug、testDebugUnitTest、上传 `app-debug` artifact 均通过；CameraX Image/Camera2 实验 API opt-in 与打印确认类型导入已修复。回归：`test_print_auto_after_scan.py` + `test_print_queue.py` 34/34 PASSED。发布边界：本次 GitHub 输出为 debug APK artifact；部署环境仍须使用现有 keystore 构建签名 release APK 并替换 `/mobile/app` 所服务的 APK |
| BUG-2026-08-16-023 | [P1] `.testvenv/` 本地 Python 虚拟环境的 7,815 个文件被误提交，GitHub `actions/setup-python` 的 pip 缓存扫描 `.testvenv/bin/python` 时在 runner 上触发 `EACCES`，导致 WMS CI 在依赖安装前失败；另有安全加密后测试仍断言密钥明文、调拨验证夹具缺少调出仓库库存、gitleaks 对测试假值误报、售后出库新增页缺 `location_management_enabled` 导入导致 500 | **已修复（2026-08-16，commits `cd9002e`/`afd0d1a`/`36824ac`/`1c3a015`/`42edeff`）**：从 Git 索引移除 `.testvenv/` 并加入 `.gitignore`（本地目录保留）；测试改为验证密钥 `enc:` 加密存储；调拨夹具写入材料仓库存流水；gitleaks 仅精确排除 9 个已核验的前端存储键/测试假值文件；补齐售后出库新增页配置函数导入并新增 GET 页面回归。回归：打印链路 34/34、售后出库 17/17、调拨 7/7 PASSED；GitHub WMS CI `#31953443966` 全绿，涵盖 Lint、全量单测与覆盖率、pip-audit、gitleaks、verify_*.py、121 项 smoke、hooks 可复现检查 |
| BUG-2026-08-17-001 | 部署兜底脚本 `fix_db_columns.py` 缺少 `in_order`/`out_order` 的 `location` 与 `in_order.auto_push_requisition` 补列；`WMS_NO_DB_TOUCH=1` 时 `app.auto_migrate_database()` 被跳过，旧库 `in_order` 永远缺 `location` 列，首页 `index()` 报 `no such column: in_order.location`，服务不可用 | `app/fix_db_columns.py` 新增 `_table_exists` 守卫，补 `in_order.location`/`in_order.auto_push_requisition`/`out_order.location` 三列（与 `app/app.py auto_migrate_database()` 的 ALTER 语句一致，幂等）；`tests/test_fix_db_columns.py` 新增对应回归用例，`start_wms_offline.bat` 重启即自动补列 |
| BUG-2026-08-17-002 | ①关库位管理+多仓库时，所有仓库级报表（库存报表、库存台账、仓库月报）使用 `Material.stock` 全局数而非按仓库聚合，导致"有库存查不出来"；`add_stock`/`deduct_stock_atomic` 不写 `StockTransaction.location`，`get_warehouse_stock_quantities` 关库位管理分支按 `location` 聚合时 NULL 不入列；另有 5 处调用 `deduct_stock` 未传 `warehouse` 参数。②老数据（入库单 `warehouse` 为空或仓库解析不到）单张反提交/编辑已完成单删明细时，`get_warehouse_stock_quantities(wh_obj)` 取不到值，`warehouse_stock={}` 恒判库存不足，即使全局有库存也报"库存不足，不能反提交"，单据卡在 completed 无法反提交 | **已修复（2026-08-17）**：①`get_warehouse_stock_quantities` 新增单仓库全局回退；`_collect_inventory_rows`/`_build_warehouse_monthly_report` 改用 `get_warehouse_stock_quantities`；`_build_report_filters` 补充 `warehouse_code`；`_collect_ledger_rows`/`_build_warehouse_monthly_report` 过滤器匹配仓库名/编码；`add_stock`/`deduct_stock_atomic` 通过 `_stock_location_from_warehouse` 写入 `location`；补 5 处 `deduct_stock` 漏传 `warehouse`；`scripts/backfill_stock_txn_location.py` 回填 800 条历史流水；`tests/test_bug_2026_08_16_006_stock_transaction_warehouse_location.py` 5/5 PASSED；新增 `tests/test_bug_2026_08_17_000_warehouse_report_fallback.py` 2/2 PASSED；真实库验证 4 仓库全局 vs 仓库汇总一致（0 差异）。②`app/routes/in_order.py` `revert_in_order` 与 `update_completed_in_order` 库存充足校验：`wh_obj` 解析失败时回退全局 `Material.stock` 口径（与 `batch_revert_in_order`/`deduct_stock` 一致），有仓库时仍走 BUG-2026-08-16-009 仓库级口径；`tests/test_bug_2026_08_17_002_revert_in_order_without_warehouse.py` 回归通过 |
| BUG-2026-08-17-003 | 采购订单执行、供应商采购汇总、物料采购汇总、采购价格分析和委外加工报表虽然要求选择仓库，但采购查询未按入库仓库归属过滤，委外查询未按 `SubcontractOrder.warehouse` 过滤，导致多仓库报表串仓 | **已修复（2026-08-17）**：采购订单明细以关联采购入库单仓库为归属口径，兼容 `InOrderItem.source_purchase_order_item_id` 与历史主表 `InOrder.source_purchase_order_id` 两种来源；尚未入库、无法确认仓库归属的采购订单不返回。委外加工报表按 `SubcontractOrder.warehouse` 严格过滤。新增 `tests/test_bug_2026_08_17_003_purchase_subcontract_warehouse_reports.py`，验证采购明细与委外主单在两仓间互不串数据（2/2 PASSED）。 |
| BUG-2026-08-17-004 | 数据仪表盘未提供仓库筛选，入库、出库、库存、库存金额、趋势、月度金额和库存排行全部使用全局数据，多仓库环境下报表口径串仓 | **已修复（2026-08-17）**：`/report/dashboard` 增加仓库选择，未选择且无默认仓库时不展示全局统计；`build_report_dashboard_context(warehouse)` 对入库/出库单、库存流水、库存排行、分类和金额趋势统一按仓库聚合；AI 解读及快捷入口携带当前仓库参数。新增 `tests/test_bug_2026_08_17_004_report_dashboard_warehouse_filter.py`，验证两仓的核心指标、库存金额和排行互不串仓（1/1 PASSED）。 |
| BUG-2026-08-22-001 | 打印物料标签的条码/二维码模糊难扫描：①`/api/qrcode` 生成二维码后用 `Image.LANCZOS` 重采样缩放，黑白模块边缘被抗锯齿抹灰，热敏打印出来发虚；②`/api/barcode` 默认 300 DPI + module_width=0.3mm 输出，短编码仅约 300px 宽，前端 CSS 拉伸到几十毫米宽打印时被放大 2~3 倍，条边模糊；③浏览器缩放 `<img>` 默认平滑插值进一步软化边缘 | **已修复（2026-08-22）**：`app/routes/label_barcode.py`——①`/api/qrcode` 改整数 box_size 输出（尺寸 = (modules+2*border) × box_size 整数倍、不小于请求 size），完全去掉 LANCZOS 重采样，全图仅纯黑/纯白；②`/api/barcode` 提升 600 DPI 输出（短编码 624×561px）。前端 4 个模板（print_label.html / print_batch_labels.html / label_preview.html / label_template_detail.html）条码/二维码图片加 `image-rendering: pixelated` 阻止浏览器平滑插值。回归：`tests/test_bug_2026_08_22_001_label_barcode_clarity.py`（5 场景：无灰色像素、尺寸模块整数倍、长内容仍锐利、条码高分辨率、接口 200）5/5 PASSED + 存量 `verify_app_py_split_label_barcode.py` 5/5 PASSED。注意：生产模式 Jinja 模板有缓存，需重启 WMS 服务后生效 |
| BUG-2026-08-23-001 | 手机 APP 扫码入库卡片只显示物料编码+数量，缺名称/品牌/规格。根因：卡片物料详情展示与 `addScanLine` 接口补全能力在 commit `17df8a1c`（2026-08-20）才合入 main，现场 APK 是 3.1.0 (versionCode 4，2026-08-16 构建) 不含该能力；且能力合入后未递增版本号（违反 BUG-2026-08-16-022 立的规矩），部署包永远不会更新到手机。排查确认扫码出库/手工入库/手工出库共用 ScanScreenBase + addScanLine，同一问题同一修复链路 | **已修复（2026-08-23）**：①`ScanScreenBase.kt`/`OpeningStockScreen.kt` 详情行字段顺序统一为 名称/品牌/规格，卡片构成 编码/名称/品牌/规格/数量 五要素；②`build.gradle.kts` versionCode 4→5、versionName 3.1.0→3.2.0，确保新 APK 可作为更新安装。回归：`tests/test_bug_2026_08_23_001_mobile_scan_card_material_details.py`（5 场景：五字段渲染、字段顺序、addScanLine 补全回填、四入口共用 addScanLine、版本号递增）5/5 PASSED + 存量 `verify_android_scan_viewmodel_isolation.py` 1/1 PASSED。**发布边界**：GitHub Actions 产出 debug APK artifact；部署环境仍须用现有 keystore 构建签名 release APK 并替换 `/mobile/app` 所服务的 APK，用户手机重新下载安装后生效 |
| BUG-2026-08-23-002 | 采购入库单「下推领料单」链路卡顿。实测「提交+自动下推领料单」（100 明细）一次请求发 835 条 SQL、耗时 350~407ms，其中 **202 条（24%）是对 `system_setting` 的重复查询**：`complete_in_order` 的入库明细循环内每条调 `location_management_enabled()`，自动下推循环内每条经 `deduct_stock_atomic → allow_negative_stock()` 各查一次开关。`get_system_setting` 无缓存，纯函数级开关每次调用都查库；生产机（杀软扫盘/低配 CPU/SQLite 写锁排队 busy_timeout 30~60s）上放大为秒级卡顿，且写事务持锁时间越长并发排队越明显。附带发现：`operation_log` 无 `(target_type, target_id)` 索引，所有单据详情页（含下推入口/落地页）全表扫描 | **已修复（2026-08-23）**：①`app/app.py` `get_system_setting` 增加请求级缓存（`flask.g`，随请求销毁无跨请求陈旧；非请求线程维持原行为），`set_system_setting` 同步更新缓存保证同请求写后读一致——缓存部分随打印代理提交 `bcacf3ab` 一并合入（当时工作区未提交改动被该提交整体带上，特此说明）；②`app/routes/in_order.py` `complete_in_order` 把 `location_management_enabled()` 提升到明细循环外（双保险）。回归：`tests/test_bug_2026_08_23_002_push_chain_settings_cache.py`（3 场景：同请求重复读同 key 只查一次库、set 后 get 缓存一致、提交+自动下推全流程 system_setting 查询从 2N+2≈42 降到 ≤6）3/3 PASSED；实测 100 明细提交+自动下推耗时 407ms→~290ms（-24%）。遗留：`operation_log` 目标索引另立任务跟进 |
| BUG-2026-08-23-003 | 内置打印代理在数据库 schema 未就绪（缺表/缺列：库文件丢失、路径错配或被外部重建）时，`_agent_loop` 兜底每 3 秒 `log.exception` 刷完整 traceback，启动窗口满屏红字被用户误报为"启动报错"；主服务实际不受影响，但刷屏掩盖真实日志且体验极差 | 已修复（2026-08-23）：`app/local_print_agent.py` 新增 `_is_schema_not_ready()` 识别 schema 等待态（`no such table`/`no such column`，大小写不敏感），`_agent_loop` 对该类异常改为：首条单行警告 → 转 `SCHEMA_WAIT_INTERVAL`(60s) 低频静默重试（不再刷 traceback）→ schema 就绪后自动恢复并打一条恢复日志；其他异常维持原行为。回归：`tests/test_local_print_agent.py` 新增 2 项（关键字识别与误判防护；两轮等待仅 1 条警告/0 traceback/恢复日志 1 条/sleep 间隔正确），套件 17/17 PASSED；沙箱复现「inventory.db 丢失 + WMS_NO_DB_TOUCH=1 启动」验证修复前每 3s 刷 traceback、修复后 0 traceback 仅 1 条警告且 /login 200 正常 |
| BUG-2026-08-23-004 | 端口 8080 被旧 WMS 实例占用时（现场最高频启动失败原因，Windows 报 WinError 10048），`run_server.py` 直接抛英文 `OSError: [Errno 98] Address already in use` 完整 traceback，操作员看不懂、不知道要先停旧实例或换端口 | 已修复（2026-08-23）：`app/run_server.py` 新增 `_is_addr_in_use_error()`（识别 Linux Errno 98 / Windows WinError 10048 / macOS 48 / 消息兜底匹配），`serve()` 捕获端口占用后输出中文指引（先跑 stop_wms_offline.bat 停旧实例，或 set WMS_PORT=18080 换端口）并以退出码 1 结束，不再抛裸 traceback；其他异常维持原行为。回归：`tests/test_run_server_port_hint.py`（6 场景识别/误判防护）PASSED；沙箱实测占用 8080 后启动输出中文指引 |

## 每日使用方式

```powershell
.\scripts\python.cmd scripts\verify_wms_bugs.py
.\scripts\python.cmd scripts\scan_wms_risks.py
```

规则：`verify_wms_bugs.py` 失败才需要立即处理；`scan_wms_risks.py` 输出的是候选风险，必须人工判真后才能进入 BUG 修复。
