# WMS BUG 基线

更新时间：2026-09-14（持续滚动更新；累计 402 条：2026-07 共 42 条，2026-08 共 241 条，2026-09 共 78 条，最新 BUG-2026-09-14-035；另含新增能力条目 WECOM-BOT-001 等）

用途：把已经核验过的问题固定下来，避免不同 AI 模型每天重复报告同一批“疑似 BUG”。后续扫描结果必须先对照本文件：已修复项看回归，误报项不重复报，暂缓项只在风险条件变化时重新评估。新 BUG 登记前先 grep 本文件查同根因历史（AGENTS.md 防反复规则 R6），同模式复发必须同时修复全部同类消费点。

| BUG-2026-09-13-012 | [P1] Windows 下 SQLite 相对路径返回反斜杠，导致迁移路径与测试/部署路径语义不一致 | 已修复（2026-09-13）：_resolve_sqlite_db_path 对相对路径规范化并统一返回正斜杠，避免 Windows 路径比较和跨平台迁移失败。R6：已 grep 路径解析与数据库迁移消费点，未发现同类未修复实现。验证：tests/test_auto_migrate_db_path.py 5 passed，静态门禁通过。 |

| BUG-2026-09-13-013 | [P2] Windows 启动回归及验证脚本子进程编码不一致，readerthread 解码失败使 stdout=None | 已修复并验证（2026-09-13）：7 个启动回归与 scripts/verify_wms_bugs.py 的 19 个 Python 子进程调用明确设置子进程 PYTHONIOENCODING=utf-8、父进程 encoding=utf-8，保留严格解码和原有数据库/日志断言。R6：同类点已排查；与 BUG-2026-08-07-002 的导入隔离根因不同。专项 pytest 29 passed；verify_wms_bugs.py exit=0 且无 UnicodeDecodeError/readerthread；两项静态门禁通过。仅修测试验证链路，不改生产启动或业务数据。 |

| BUG-2026-09-13-014 | [P2] 图片回归测试未关闭静态响应，Windows 删除失败；固定迁移文件名被旧运行污染 | 已修复并验证（2026-09-13）：两项测试使用 tmp_path 独立静态/上传/旧图目录，monkeypatch 恢复配置与工作目录；通过响应上下文关闭文件句柄，再执行原有删除清理。保留默认上传根目录、HTTP 200、PNG 类型和逐字节内容断言；迁移加强为首次精确复制 1 个、再次复制 0 个。R6：同类点已排查该文件两处静态响应和迁移实现；不修改生产上传/迁移逻辑，不删除旧运行或生产图片。专项连续两轮各 2 passed；全量 pytest tests -q --tb=short --disable-warnings：1632 passed、85 skipped、1934 warnings、0 failed（251 秒）。 |

| BUG-2026-09-13-015 | [P1] 销售看板与异常页仍按全局库存判断缺货，其他仓有货掩盖本仓不足 | 已修复并验证（2026-09-13）：两个页面通过共享 _sales_analysis_stock 按仓库 ID 优先、历史名称/编码兼容解析，使用 get_warehouse_stock_quantities；按请求缓存同仓库存，不回退全局或默认仓。无效/空/停用仓库明确在页面提示库存待人工确认，不伪造缺货结论。R6：与 BUG-2026-09-13-010 同根因的非 AI 页面遗漏；sales.py 全部库存读取点已排查，剩余下推两处原已使用仓库口径，现无 material.stock 裸用。新增测试 tests/test_bug_2026_09_13_015_sales_page_stock.py：初始 8 项失败，修复扩充后 13 passed，覆盖双仓隔离、ID 优先、历史编码流水、部分发货余量、无效/停用仓库、缓存、真实模板渲染及只读不改库存。全量 pytest tests -q --tb=short --disable-warnings：1645 passed、85 skipped、1934 warnings、0 failed；verify_wms_bugs.py 与双静态门禁通过。未运行会创建业务记录的 full_smoke_test.py，使用隔离数据库真实页面 GET 渲染回归替代。生产需重启 WMS 服务生效。 |

| BUG-2026-09-13-016 | [P1] 批量出库漏停用仓库校验、缺失物料触发异常，单张/批量未统一累计需求预检 | 已修复并验证（2026-09-13）：共享 _out_order_stock_error，单张及批量写锁内解析有效启用仓库、拒绝不存在物料及非正数量、累计同物料需求后按仓库库存预检；删除批量全局库存读取，保留逐项原子扣减及整单回滚。R6：复查 BUG-2026-08-16-004/021 与 BUG-2026-09-13-002，同类点已排查两完成入口。初始 3 failed/2 passed，扩充后专项 8 passed；覆盖停用仓、历史编码库存、重复明细、重复完成、坏单后继续好单、模拟第二次扣减失败整单回滚。全量 pytest tests -q --tb=short --disable-warnings：1653 passed、85 skipped、0 failed（257 秒）。不执行生产单据，使用隔离 SQLite 路由回归替代写生产的 smoke 脚本。生产需重启 WMS 服务生效。 |

| BUG-2026-09-13-017 | [P1] 盘点单行新增无效仓库回退全局库存，漏区域/冻结与写锁边界 | 已修复并验证（2026-09-13）：写锁内重检状态及物料，空单可用默认仓，空仓历史已有明细/冻结时拒绝擅自归属；仓库必须有效启用，按仓库库存取账面，保存 area/location，启用库位时区域必填，按物料+区域判重并首次设置 frozen_at。R6：对照 BUG-2026-09-13-003/004 和 BUG-2026-09-02-004，表格保存/导入/单行更新已检查，补齐单行新增消费点。初始 7 failed；新增 9 项回归覆盖双仓历史编码、默认仓、停用/无效仓、分区重复、库位必填、冻结不漂移、写锁状态变更、旧空仓有明细拒绝归属；全量 pytest tests -q --tb=short --disable-warnings：1662 passed、85 skipped、0 failed（264 秒）。隔离 SQLite HTTP 回归替代生产写入 smoke，未操作生产单据。生产需重启 WMS 服务生效。 |

| BUG-2026-09-13-018 | [P2] 手机扫码累加缺少明确数量反馈、移除按钮直接删除且按列表索引操作 | 源码已修改，Android 编译/真机验收待完成（2026-09-13）：addScanLine 独立 scanFeedback 回显本次与累计数量，页面及相机弹窗可见、不借用提交成功状态；删除增加编码/名称/规格/区域/数量确认，按物料+区域重新定位并复核数量，提交期间禁加行/删除。R6：三类扫码页共用基类和 ViewModel；保留盘点替换确认、900ms 节流、原声音震动及开关；期初独立页面不改，多单位明确不做。新增 3 项源码接线测试先红后绿，与既有物料回显共 8 passed；全量 Python pytest 1683 passed、85 skipped、0 failed，不代表 Kotlin 编译通过。gradlew.bat --offline :app:compileDebugKotlin 实际失败：JAVA_HOME 未设置且找不到 java；本机未发现 Android SDK，未生成 APK，未做真机测试，不能标记上线完成。验收需重新构建安装 APK，检查取消/确认删除、数量变更拒绝误删、连续重复扫描与盘点替换，手机库存预检/草稿恢复/库位/单据附件另批推进。 |

| BUG-2026-09-13-019 | [P1] Android 出库提交前按全局库存逐行预检，未累计同物料需求 | 后端已验证，App 源码已接入、编译/真机验收待完成（2026-09-13）：对照 BUG-2026-09-13-016、BUG-2026-09-03-004，同根因的新消费点 native_api_outbound；最终原子扣减已有防护，不能据此断言可超发。新增 Bearer 角色保护、pydantic 校验的只读 /api/outbound/preflight；共享仓库级累计预检供实际提交复核，提示本仓可用量与本次累计量。保留最终扣减、负库存配置和离线幂等；预检业务拒绝不入离线队列，网络失败沿用暂存。库位明细校验仍由最终扣减兜底，下一批库位选择收口；多单位不改。R6：同类点已排查 Web 单张/批量、Android 展示与提交。新增测试初始 8 failed，扩充后专项及部门/合同/网页联动 36 passed；全量 pytest tests -q --tb=short --disable-warnings：1694 passed、85 skipped、0 failed；lint_wms_rules --staged 0 违规。只读测试确认不新建单据/流水/幂等记录，覆盖历史空 ID 编码库存、双仓隔离、累计需求、停用仓、负库存策略、认证与非法输入。本机缺 Java/Android SDK，未构建安装 APK；后端服务重启后接口生效，App 需构建发版。 |

| BUG-2026-09-13-020 | [P1] 手机入出库未提交清单仅在内存，杀进程丢失 | 源码已修改，Android 编译/真机验收待完成（2026-09-13）：与 BUG-2026-09-03-003 断点续盘不同，离线队列只保存已点击提交的作业。新增 DataStore 编辑草稿，服务器+账号+作业类型隔离，恢复清单/名称规格品牌/仓库/部门/领料人/合同；StateFlow 监听、Mutex 串行保存，删空删除草稿，保存失败常驻提示。提交前落盘幂等键，进程中断恢复后禁止修改原请求，只允许复用原键核实；出库核实不重复执行只读预检，以免已成功扣库后预检不足阻碍幂等回放。业务明确拒绝允许修改，认证/服务端异常保留原键；成功或离线确实入队后清空。R6：同类点已排查入出库与既有盘点恢复，保留盘点专用实现，不改多单位。新增 3 项源码接线测试先红后绿，关联 17 passed；全量 Python 1697 passed、85 skipped、0 failed；后续保护调整专项仍 17 passed。实际 gradlew.bat --offline :app:compileDebugKotlin 因无 JAVA_HOME/java 失败，Python 测试不代表 Android 编译或杀进程验收通过。需在具备 SDK 的环境构建，真机验证杀进程恢复、换账号/服务器隔离、删空不复活、弱网入队与提交中断原键核实后再上线。 |

| BUG-2026-09-13-021 | [P1] Android 入出库扫码行没有库位选择，启用库位管理时只能提交空库位 | 源码已修改，Android 编译/真机验收待完成（2026-09-13）：对照仓库/库位必填规则与 native_api_outbound/native_api_inbound 最终校验；出库页新增库位编码输入并写入每个扫码/手工行，提交沿用后端库位必填拒绝。当前输入支持手工编码/粘贴库位标签文本，尚未实现独立库位列表/扫码库位标签解析，下一阶段可增强。多单位不改。R6：复查入库、出库、盘点，盘点已有区域字段；新增源码接线测试。 |

| BUG-2026-09-13-022 | [P0] 手机按行写库位但未保存单据表头库位，后续反提交无法准确定位；关闭库位时入库仍写隐藏库位账 | 已修复并验证（2026-09-13）：R6 排查入出库两路，Web 单据反提交采用 order.location；现有单据仅支持单个表头库位，因此移动端一单强制同库位并持久化，跨库位必须分单，不伪装支持多库位单据。关闭库位管理时忽略来端库位且不写 LocationInventory；库位预检按所选仓库+库位累计，不拿其他库位余额兜底。历史已完成单据不自动修改，旧空表头归属待人工核对。新增隔离数据库测试 7 项先红后绿，预检关联 18 passed；全量 pytest tests -q --tb=short --disable-warnings：1707 passed、85 skipped、0 failed。后端服务需重启；Android 未编译/真机测试，不代表已上线。 |

### BUG-2026-09-13-021 补充修复记录（2026-09-13）

- 原输入框只接通出库，入库没有入口，不能算库位选择完成。本次改为入出库共用 ScanLocationSelector，支持分页取全本仓已有库位、手工输入、相机扫描标签；启用状态未确认时阻止新提交，关闭时隐藏控件并清除行库位。
- 换仓清空旧库位与候选，忽略旧仓迟到响应；库位选择应用到本单全部行并随编辑草稿恢复。既有单据只能一个表头库位，跨库位分单，配合 BUG-2026-09-13-022 避免反提交回退错位。
- 全仓查无默认库位配置字段，接口如实返回 default_location=null，不将仓库物理地址或库存最多库位冒充默认。后续配置默认库位属于独立主数据需求。
- 新增 tests/test_native_api_location_options.py 验证双仓隔离、分页全集、认证、无效仓及关闭模式；源码回归检查共享入口与标签扫描。全量初次 1708 passed、85 skipped、1 failed，为旧源码测试绑定 addScanLine 形参名；已调整该断言并保留物料补全行为检查。Android 缺 Java/SDK，未编译/安装，真机验收仍待完成。

### MOBILE-DOCUMENT-EVIDENCE-001（2026-09-13，新增能力，非重复BUG）

- 单据拍照取证独立于物料图片，使用 DocumentEvidence 关联入库/出库单；每单最多3张JPEG，长边1600像素、单张300KB上限，与单据同一事务提交，失败整单回滚。
- 仅人工提交携带照片；图片随原请求离线暂存。读取接口按单据创建人/管理员授权，不公开静态图片地址；后端解码验证并重编码清理元数据。
- 后端隔离测试 5 passed，Python 语法检查通过；Android 已接入相机权限入口、JPEG 压缩、每单最多3张、随请求发送并进入编辑草稿恢复。照片仍只在人工点击提交后写库，业务拒绝不进离线队列。Android 编译/真机验收仍待具备 Java/SDK 的环境完成；需真机验证权限拒绝、照片大小、离线暂存与服务端查看。

### MOBILE-PERMISSION-001（2026-09-13，权限体验修复）

- Android 安装包只能声明危险权限，不能绕过系统静默授予相机/麦克风；原应用仅在扫码或语音功能进入后才分别请求，设备若已将权限设为“禁止”时现场才发现不可用。
- 首次实现的 Compose 权限门禁在现场版本出现启动即退出现象，无法取得设备 logcat，按最小风险回退为 Activity 原生 `requestPermissions`：业务导航正常启动，同时在 `onCreate` 请求 `CAMERA` 与 `RECORD_AUDIO`；拒绝时 Toast 提示，扫码/语音功能继续由各自入口做权限兜底。已授权设备不会重复弹窗。
- 回归：`tests/test_android_startup_permissions.py` 与扫码直开测试共 5 passed；本机无 Java/Android SDK，未完成 APK 编译和真机权限弹窗验收，需安装新 APK 后确认不再崩溃。
- `ad39926` 的 Android APK Build #474 曾显示红叉，但该运行日志接口返回 GitHub 403，无法取得具体编译错误；本地同样因无 Java/SDK 无法重现。当前源码已改为 Activity 原生请求，后续新提交的 CI 结果作为实际构建依据。
- `76c8fea` 触发的新构建仍在同一 Build Release 步骤失败；本次进一步去掉 `ActivityCompat` 依赖，直接调用 Activity 原生 `requestPermissions`，减少厂商/依赖兼容面。

### BUG-2026-09-13-023（2026-09-13，Android 启动崩溃：本地持久化未兜底）

- **现场证据**：用户截图 `Screenshot_20260913_235020_com.factory.wms.jpg`，系统弹「WMS扫码屡次停止运行」，状态栏「取货中」。`WMS扫码` 即 `com.factory.wms` 的 `android:label`，`MainActivity` 为唯一 Activity（Compose 单 Activity 架构），故为**主进程启动期崩溃**，非单功能失效。
- **根因（代码实证，非推测）**：崩溃点在**数据持久化层**，不在权限层。`AppNavGraph` 组合期一次性创建 11 个 ViewModel（`NavGraph.kt:81-91`），每个都构造 `WmsRepository`；而 `WmsRepository` 构造期同步执行 `AppDatabase.getDatabase(context)`（Room 建库），且 `encryptedPrefs` 用 `EncryptedSharedPreferences`（依赖 Android Keystore）。`AuthViewModel.init` 的协程**直接读取**两者且**无 try/catch**：任一步失败（Keystore 失效 / 库文件损坏 / 磁盘满）都会让协程抛未捕获异常 → 杀进程。佐证：同文件 `logout()` 早已有 try/catch，**`getSavedToken()` 没有**——不对称。
- **为何前 5 次修复无效**：`MOBILE-PERMISSION-001` 的 `90a0ef9`/`ad39926`/`b9516a9`/`e950815` 全部围绕权限门禁，方向本身偏离根因；且该缺陷登记已写明「无法取得设备 logcat」，属**盲改**。权限门禁只需从主路径移开即可（现版本已正确），但数据层未兜底，崩溃条件依然存在。
- **修复（三层兜底，均降级不中断启动）**：
  ① `WmsRepository.kt`：`db` 改 `AppDatabase?`（构造期建库失败降级为无本地缓存）；`encryptedPrefs` 改可空（创建失败置 null 并删除损坏 prefs 文件）；`getSavedToken()` 自身 try/catch；`saveLoginInfo` 写失败不阻断登录；`logout()` 的 DataStore 清理补 try/catch；新增 `safeCache()` / `logOperation()` 统一入口，本地写失败只记 WARN；`offlineQueue` 改可空（本地库不可用时降级为直连提交，入队前判空且**不谎报"已暂存"**）。
  ② `AppDatabase.kt`：`getDatabase()` 建库失败先 `deleteDatabase` 再重建一次（本地库为可重建缓存，不值得牺牲启动）；抽出 `buildDatabase()` 并提常量 `DB_NAME`。
  ③ `AuthViewModel.kt`：会话还原整体 try/catch，失败降级为未登录（`isLoggedIn=false`），绝不因本地缓存问题导致 App 打不开；401 登出链路同样兜底。
  连带适配：`ScanViewModel` 订阅离线计数改可空安全调用。
- **R6 同根因排查**：全量 grep 可空引用连锁，发现并修复 `recordOperationLog` 内 **3 处** `operationLogDao.insert` 未改安全调用（`db` 可空化后会编译失败），另行核对 `WmsApplication.warmUpOfflineQueue` 已用 `runCatching` 无需改。
- **验证**：新增 `tests/test_android_startup_crash_resilience.py` **7/7 passed**（锁死三层兜底断言，防后续重构删掉 try/catch 导致复发）；相关安卓/移动端回归 **177 passed / 4 skipped**；**全量 pytest 1724 passed / 85 skipped / 0 failed**；`lint_wms_rules.py --staged` **0 违规**。
- **未完成项（重要）**：沙箱无 Java/Android SDK（历史工具链已随沙箱重建丢失，重新下载 kotlinc 仅得 2.9MB 残缺包），**未能执行 `./gradlew assembleRelease`**。按 `BUG-2026-09-12-006` 已确立规则「Kotlin 改动须以 CI `assembleRelease` 结果为唯一验收依据，未确认 CI 转绿不得声称完成」，**本次修复的编译验收仍待 CI 确认**。
- **阻塞线索（需现场核对）**：台账 `MOBILE-PERMISSION-001` 已记 `Android APK Build` 自 **#424** 起每次在 `Build Release APK (R8 瘦身)` 步骤失败（**#423 为最后一个绿版**）。若该状态持续，则 **APK 产物从未更新**，现场设备安装的始终是 #423 之前的旧包——这可能是「装了新包仍是同样问题」的直接原因。需在 Actions 页面确认最新构建是否转绿。
- **补充修复（2026-09-14，`b6497c7` 推送后 CI 反馈）**：推送后 CI 实际跑出 **Android APK Build #481 = failure**，失败步骤正是 `Build Release APK (R8 瘦身)`，报错唯一一条：
  ```
  e: .../ui/viewmodel/scan/ScanViewModel.kt:303:48 'return' is prohibited here.
  > Task :app:compileReleaseKotlin FAILED
  ```
  **根因**：本次修复给 `ScanViewModel.init` 写了 `val queue = repository.offlineQueue ?: return`。Kotlin **不允许在 `init` 块内使用 `return`**（没有可返回的函数体）——这是语法级硬错误，属本次改动新引入。**修复**：改为显式判空包裹 `if (queue != null) { ... }`。同时回查全仓库 Kotlin 源码，确认无第二处「init 块内 return」（其余 `?: return` 均在普通函数体内，合法）。
- **连带修复（同一 CI 运行暴露）**：**`WMS CI #970 = failure`**，`Verify regression - verify_*.py` 步骤 **6 个测试失败**，全部是**字符串/正则硬编码了修复前的旧写法**，代码语义本身正确：
  | 测试 | 失败原因 | 处置 |
  |---|---|---|
  | `verify_p2_e_logout_sync_commit::t1/t2` | 断言 `encryptedPrefs.edit().clear().commit()` 无安全调用；实际改成了 `prefs.edit()...` | **代码同时存在真 bug**：原 `encryptedPrefs?.edit()?.clear()?.commit()` 安全调用链在 prefs 为 null 时整链短路，`commit()` 是否执行不可知、返回值被 `?.` 吞掉，违背 P2-E「必须真正同步落盘」。已改为 `val prefs = encryptedPrefs; if (prefs != null) { if (!prefs.edit().clear().commit()) 记 WARN }`，测试断言同步放宽并补「不得用 apply()」断言 |
  | `verify_bug_2026_08_12_009::t4` | 断言 `encryptedPrefs.getString(KEY_TOKEN`，实际为安全调用 `?.getString` | 测试断言放宽为 `encryptedPrefs\?*\.\s*getString(` |
  | `verify_bug_2026_09_12_008::test_009` | 断言 `offlineQueue.enqueue(`，实际为 `queue != null && queue.enqueue(` | 断言改为 `queue\.enqueue\(` + 校验先判空 |
  | `verify_android_offline_hint::t3` | 正则要求 `materialDao.getByCode`，实际为 `runCatching { materialDao?.getByCode }` | 正则放宽，仍强制 `fromCache`/`cachedAtMillis` 语义 |
  | `verify_bug_2026_08_11_004::t3` | 断言 `submitOpeningStock` 内直调 `operationLogDao.insert`，实际已统一走 `logOperation()` | 断言改为必须调用 `logOperation()` + 全文件校验落库链路仍在 |
- **补充验证（2026-09-14）**：上述 6 个测试 + 本 BUG 新增回归 **40 passed**；**全量 pytest 1724 passed / 85 skipped / 0 failed**（286s）；`lint_wms_rules.py --staged` **0 违规**。
- **APK 分发链核验（2026-09-14）——含一处自我更正**：
  - 经 `gh-proxy.com` 打通 GitHub API（`api.github.com` 直连 000、`ghproxy.net` 对 API 返 403），可读 Actions 状态与日志。
  - `#481`（`b6497c7`）`failure`；`#970` WMS CI 同批 `failure`。二者根因已定位并修复（见上两条）。
  - 修复推送 `1c8c67d` 后：**`#482 Android APK Build` = success**（#423 以来首次全绿，13 步含 assembleRelease / lintRelease / testReleaseUnitTest / 上传 APK / 发布 Release 全通过）、**`#971 WMS CI` = success**、`#1266 WMS AI Verification` = success。
  - **⚠ 自我更正**：先前据 AXML 字符串池顺序误判「Release APK 的 `versionCode=15`，全历史不存在，属幽灵包」。用 `pyaxmlparser` 精确解析后确认真实值为 **`versionCode=14` / `versionName=3.8.0`**，与源码 `build.gradle.kts` **完全一致**；字符串池中的 `'15'` 实为 `minSdkVersion` 的残留，属解析误读。**「版本号幽灵包」结论作废。**
  - **但「现场装的是旧包」这一核心判断仍然成立，且有更强证据**：对 Release 资产做 dex 常量池逐项比对——
    | 修复标志串（本次新增） | 旧包（#481 前，SHA `92a46c14…`） | 新包（#482 后，SHA `2fb3e7dc…`） |
    |---|---|---|
    | `本地数据库不可用` | 无 | **有** |
    | `加密存储不可用` | 无 | **有** |
    | `降级为无本地缓存` | 无 | **有** |
    | `降级为待登录状态` | 无 | **有** |
    | `读取 token 失败，按未登录处理` | 无 | **有** |
    | `会话还原失败，降级为未登录` | 无 | **有** |
    | `建库失败，尝试删除本地库重建` | 无 | **有** |
    | 对照组 `wms_database` / `wms_secure_prefs` / `WmsRepo` / `离线队列预热失败` | 有 | 有 |
  - **结论**：CI 自 #424 起持续失败期间，Release 资产确实停在 #423 时代的旧代码（无任何本次修复标志串），这解释了「现场重装仍是老问题」。**现已修复：`#482` 转绿并成功发布新包，新包经 dex 级验证确认包含全部三层兜底修复。**
  - **生效条件**：用户需**卸载旧包后重新安装**新版（`app-release.apk` / Release 直链 `wms-mobile-scan.apk`）。因 `versionCode` 仍为 14 未递增，覆盖安装可能被系统拒（同版本号覆盖），**卸载重装是稳妥路径**；建议后续提交统一递增 `versionCode`。

- **第二轮修复（2026-09-14 08:15，现场复测仍崩溃 → 定位到真正的第二根因）**：
  - **新现场证据**：用户装新包后复测，截图显示 `WMS扫码屡次停止运行` 与系统弹框「**是否允许"WMS扫码"访问相机？1/2**」**同屏出现**。崩溃时刻从「冷启动」变为「首次进入扫码页、请求权限时」，且这是全新安装后的首次授权路径。
  - **根因（权限线路）**：`MainActivity.onCreate` 中调用了 `requestPermissions(arrayOf(CAMERA, RECORD_AUDIO), ...)`，且位置在 **`setContent{}` 之前**。`Activity.requestPermissions()` 是**同步打断式**调用——它让 Activity 立即进入 PAUSED 去显示系统弹框；此时 `setContent` 尚未执行、Compose View 树尚未建立，用户响应弹框后 Activity 恢复，回调与组合撞在未初始化的宿主上 → **进程被杀**。
  - **为何前 5 次修复全部无效**：`MOBILE-PERMISSION-001` 的 5 个提交（`90a0ef9` 加权限门禁 / `ad39926` 改 Activity 原生请求 / `b9516a9` 换 API / `e950815` 简化）**全部在更换「用什么 API 申请」，从未质疑「在 `onCreate` 里、`setContent` 之前申请」这个调用位置本身**；`e950815` 还把 `onRequestPermissionsResult` 一并删除，导致权限回调**无人处理**。台账早已记「无法取得设备 logcat」，属盲改——本次现场截图首次给出了「弹框与崩溃同屏」这一决定性线索。
  - **修复**：**彻底移除 `MainActivity` 的启动期权限申请**（连带删除 `REQUEST_STARTUP_PERMISSIONS` 常量与 `Manifest` import）。依据：两个权限**都已具备**按需申请的 Compose 机制，启动期申请纯属冗余——
    | 权限 | 现有按需申请位置 |
    |---|---|
    | CAMERA | `ui/components/ScannerDialog.kt:145-161`（`rememberLauncherForActivityResult` + `LaunchedEffect`，进扫码弹窗时请求） |
    | RECORD_AUDIO | `ui/components/VoiceAssistant.kt:80-137`（同机制，启用语音时请求） |
  - **R6 同根因排查**：全量 grep `requestPermissions` —— 移除后全项目**零调用**，`WmsApplication` 亦无权限申请，确认无第二处启动期申请。
  - **反向回归**：重写 `tests/test_android_startup_permissions.py`。**原文件正是锁死错误行为的元凶**——它断言 `MainActivity` 必须包含 `requestPermissions(`（`test_startup_requests_both_permissions_before_app_navigation`），使该写法无法被修正。新版改为 5 项反向断言：MainActivity **不得**出现 `requestPermissions(`（去注释后校验）与 `REQUEST_STARTUP_PERMISSIONS`；两个权限必须由 Compose 组件按需申请；`setContent`/`AppNavGraph` 组合完整。
  - **验证**：`test_android_startup_permissions.py` + `test_android_startup_crash_resilience.py` **12 passed**；**全量 pytest 1726 passed / 85 skipped / 0 failed**；`lint_wms_rules.py --staged` **0 违规**。
  - **CI 验收（`b99a586`）**：`#484 Android APK Build` **success**、`#973 WMS CI` **success**、`#1268 WMS AI Verification` **success**。
  - **字节码级验证（androguard 反汇编 Release APK）**：第三代包（SHA256 `1074141f…`）的 `Lcom/factory/wms/MainActivity;.onCreate(Landroid/os/Bundle;)V` 共 **42 个方法调用，权限调用数 = 0**，全部为 `setContent` / `enableEdgeToEdge` / Window 组合逻辑；对照第二代包（`2fb3e7dc…`）该方法内仍含 `requestPermissions`。**确证启动期权限申请已从产物中消失。**
  - 三代产物标志串总表（dex 常量池）：
    | 标志串 | #481前(旧) | #482后 | #484后 |
    |---|---|---|---|
    | `本地数据库不可用` | 无 | 有 | 有 |
    | `加密存储不可用` | 无 | 有 | 有 |
    | `降级为无本地缓存` | 无 | 有 | 有 |
    | `降级为待登录状态` | 无 | 有 | 有 |
    | `读取 token 失败，按未登录处理` | 无 | 有 | 有 |
    | `会话还原失败，降级为未登录` | 无 | 有 | 有 |
    | `建库失败，尝试删除本地库重建` | 无 | 有 | 有 |
    | 对照 `wms_database` / `WmsRepo` | 有 | 有 | 有 |
  - **教训**：改动 Android 启动路径时，**不能在 `onCreate` 中做任何会打断 Activity 生命周期的同步操作**（权限申请、`startActivityForResult` 等）——必须让 `setContent` 先完成。测试若硬编码实现细节（如"必须调用某 API"），会把错误锁死，应断言**行为契约**而非**实现手段**。

### BUG-2026-09-14-026（2026-09-14，加固：Gson 非空契约破坏入口与冷启动会话兜底）

- **来源**：`BUG-2026-09-13-025` 交付时自列的遗留项，本轮动手修。
- **性质**：**预防性加固**，非用户已触发的线上故障。按 `BUG-2026-08-24-007` 已确立的故障模式
  （Gson 走 `Unsafe.allocateInstance` 绕过 Kotlin 构造器 → 非空字段被静默置 null → UI 层裸调 → 主线程 NPE → 整 App 崩溃）全量排查剩余入口后，定位两处：

  **① `WmsRepository.loadEditDraft()` —— 唯一没有 try/catch 的反序列化入口**

  | 对比项 | 状态 |
  |---|---|
  | `loadStocktakeDraft()` | 有 try/catch（既有正确范式） |
  | `loadEditDraft()` | **无** try/catch，异常直接抛到冷启动链路 |

  `ScanEditDraft` 的 `lines: List<DraftScanLine>`、`contractNo: String`、
  `inboundBusinessType: String` 均为 Kotlin 非空声明，Gson 反序列化时可被置 null。
  脏值经 `ScanViewModel.restoreEditDraft()` 的 `_uiState.copy(contractNo = draft.contractNo)`
  （平台类型赋值，编译器**不插** `checkNotNull`）静默进入 UI 状态，随后在 UI 层裸调即崩。

  **② `WmsRepository.getWarehouses()` —— 缺 `ensureSession()`**

  | 方法 | `ensureSession()` |
  |---|---|
  | `getDepartments()` | ✅ |
  | `getEmployees()` | ✅ |
  | `getWarehouses()` | ❌ **缺失** |

  该方法在冷启动首页（`HomeViewModel.init → loadDashboard`）即可能被调用，早于
  `AuthViewModel` 的异步会话还原（`BUG-2026-08-24-006` 描述的同一竞态）。
  `safeCall` 有兜底故**不崩**，但会出现「首个请求不带 baseUrl/token」的功能瑕疵。

- **修复**（`WmsRepository.kt`，+46 行）：

  ```kotlin
  suspend fun loadEditDraft(key: String): ScanEditDraft? {
      val json = context.dataStore.data.first()[stringPreferencesKey(key)] ?: return null
      return try {
          Gson().fromJson(json, ScanEditDraft::class.java)?.let { sanitizeEditDraft(it) }
      } catch (e: Exception) {
          android.util.Log.w("WmsRepo", "读取改单草稿失败，按无草稿处理: ${e.message}")
          null
      }
  }

  // 表达式体 + runCatching：任一处因脏数据抛异常，整体退化为"无草稿"
  private fun sanitizeEditDraft(draft: ScanEditDraft): ScanEditDraft? = runCatching {
      val safeLines = draft.lines.mapNotNull { entry ->
          val code = entry.line.material_code
          if (code.isBlank()) null else entry   // material_code 空的脏行无法定位物料，丢弃
      }
      draft.copy(lines = safeLines)
  }.getOrNull()
  ```
  另在 `getWarehouses()` 首行补 `ensureSession()`。

- **为什么选 `runCatching` 而不是逐字段 `?: 默认值`**：
  逐字段判空会因 Kotlin 认为字段非空而触发 `USELESS_ELVIS` / `SENSELESS_COMPARISON`
  等告警，需成片 `@Suppress`（且本地无 Kotlin 编译器可验证，风险不可控——
  上一次编译错误就导致 #486 整包未产出）。`runCatching` 包裹是**零告警、零语法歧义**
  的写法，语义等价：脏字段导致任一处抛 NPE → 整体退化为 null（"无草稿"）→
  引导用户重扫，而不是把 null 带去 UI。能安全修补的坏行（`material_code` 为空）
  在 `runCatching` 内用 `mapNotNull` 剔除，属于正常路径。

- **验收（可复现）**：

  | 项 | 结果 |
  |---|---|
  | 新增回归测试 | `tests/verify_bug_2026_09_14_026_gson_nonnull_and_session.py`，**10 项** |
  | 全量测试 | **1726 passed / 85 skipped / 0 failed** |
  | 预提交门禁 | 0 处违规 |
  | CI | `#490 Android APK Build` **success**、`#1274 AI Verification` success |
  | 反向验证 | **三种破坏场景全部被精准捕获**（去 runCatching+坏行过滤 → 2 项失败；去 `ensureSession` → 2 项失败；去 try/catch → 1 项失败） |
  | 字节码级验收 | 新包字符串池含本轮新增日志 `"读取改单草稿失败，按无草稿处理: "` → 改动确已编译进 APK；`MainActivity.onCreate` 权限调用数仍为 **0** |

  > 注：#490 编译成功意义特殊——这是本地无 Kotlin 编译器可用的情况下，
  > 通过**选择零告警写法**而非"事后修编译错误"来规避 `#486` 那类事故的第一次实践。

- **测试自身的两处缺陷（本轮自查发现并修正，值得记录）**：
  1. **函数体截取用了近似匹配**：原以 `find("\n    private fun ", 1)` 找下一个成员，
     但 `sanitizeEditDraft` 后面紧跟的是 `suspend fun`（缩进不同）→ 匹配失败 →
     body 吞掉整个文件余下部分 → 断言在噪声里"碰巧"命中 → **假绿**。
     改为**大括号配对**精确截取（表达式体函数的 `.getOrNull()` 在括号外，单独处理）。
  2. **同名测试函数重复定义**：改稿时旧版未删，Python 后定义覆盖前者，
     实际跑的是宽松的旧断言。已删除重复项。
  > 教训：**断言写完全绿不算数，必须反向破坏验证能红**。第一次反向验证时测试全绿，
  > 才发现测试本身是假的——如果跳过这一步，就会带着两个假测试提交。
- **未修（已确认安全，不动）**：`OfflineQueueManager.replay()` 的三个 `fromJson`
  调用点外层已有 `catch (e: Exception)` 兜底（`doSync` 内），异常会被转成
  `markFailure` 并保留中文原因，**不会崩溃**。按"只改必要处"原则保持原样，
  并加了一条回归断言锁住该兜底不被移除。

### BUG-2026-09-14-027（2026-09-14，「WMS扫码屡次停止运行」复现：versionCode 冻结致分发陷阱）

- **现场**：用户再次反馈系统弹「WMS扫码屡次停止运行」，与 BUG-2026-09-13-023 截图同症状。
- **R6 同根因排查（先查历史，未盲改）**：命中 BUG-2026-09-13-023。该 BUG 已实证并修复两个根因——①启动期数据持久化三层兜底（WmsRepository/AppDatabase/AuthViewModel）；②onCreate 里 setContent 之前申请权限。本次在 main HEAD（8f8c17d）逐项复核：`MainActivity.onCreate` 无 `requestPermissions`（仅留根因注释），`WmsRepository.kt:42/85/169`、`AppDatabase.kt:47`、`AuthViewModel.kt:64` 兜底标志串齐全——**两修复均在源码，无回退**；台账今日已对官方 Release APK（#488/#490）做字节码级验证，含全部修复标志串。**故非代码回归。**
- **真正根因（分发陷阱）**：`app/build.gradle.kts` 的 `versionCode` 自 14 起被**冻结**，`versionName` 停 3.8.0。Android 按 versionCode 判新旧：同号覆盖安装不替换旧代码、用户在「应用信息」里永远看到 3.8.0 无法分辨新旧包 → 现场设备长期停留在修复前旧包，崩溃"屡次复现"。BUG-2026-09-13-023 早已建议「统一递增 versionCode」但一直未执行，本轮落实。
- **修复（独立 atomic action，仅改构建脚本版本号与注释，不动业务代码）**：`versionCode` 14→15、`versionName` 3.8.0→3.8.1，并补「每次发版必须递增 versionCode」注释。
- **回归**：新增 `tests/verify_bug_2026_09_14_027_versioncode_increment.py` 3 项（versionCode≥15 防冻结/倒退、versionName 同步递增、防复发注释在位），连同既有版本契约测试（均用 `>=` 断言）共 **26 passed**（沙箱无 Java/Android SDK，静态契约测试，本地无法编译 APK）。
- **生效条件与验收（重要）**：本改动需 CI `Android APK Build` 转绿产出 versionCode=15 新包后，用户**卸载旧包（连同可能损坏的本地库/Keystore）再重装** 3.8.1；此后覆盖安装可正常替换、可在「应用信息」核对版本号。**若装完 3.8.1 仍崩溃，则为未发现的第三根因，须现场抓 logcat 诊断，禁止再盲改**（BUG-2026-09-13-023 五次盲改教训）。
- **CI 验收（已确认，commit `694bbcb4`）**：`Android APK Build` / `WMS CI` / `WMS AI Verification` **三工作流全部 success**（含本次新增的 verify 回归测试）；Release 资产 `wms-mobile-scan.apk`（25,095,683 字节）已于 2026-09-14T07:53:09Z 更新为 versionCode=15 / 3.8.1 新包，固定直链即刻生效。生产/用户侧生效仍须**卸载旧包后重装 3.8.1**（覆盖安装此后亦可正常替换）。
- **用户侧终验（2026-09-14，结案）**：用户依次卸载重装 3.8.1 → 3.8.2 → 3.8.3 全部成功安装（3.8.2 崩溃报告页自报版本「3.8.2 (16)」证明 versionCode 链路正常递增、覆盖安装正常替换），分发陷阱解除。

### BUG-2026-09-14-028（2026-09-14，3.8.1 仍冷启动闪退 → 加应用内崩溃报告页捕获第三根因）

- **现场**：用户确认已装 3.8.1（versionCode=15，含 BUG-2026-09-13-023 两修复 + BUG-2026-09-14-027 版本解冻），**打开应用仍立即闪退**。这坐实存在**未被发现的第三根因**——此前两个根因均已修复且字节码级验证，却仍复发。
- **关键判断（不再盲改）**：历轮修复全程**没有拿到过一次 logcat/堆栈**，修的都是"看起来会崩的点"。本次先复核冷启动全链路代码：`WmsApplication.onCreate` 4 处调用（崩溃日志器/相机缓存清理/ScanFeedback 预热/离线队列预热）均有兜底；`MainActivity` 仅 `enableEdgeToEdge`+`setContent`；组合期 14 个 ViewModel 的 `init` 块全部有 try/catch 或为空；R8 16 条 keep 规则覆盖 model/api/room/security/camera/mlkit/ui。**静态看无裸露崩溃点 → 真凶只能在运行期（设备/R8/数据相关），必须拿到真实堆栈才能定位。**
- **障碍**：`WmsApplication` 的崩溃日志器（BUG-2026-09-13-001）已把堆栈写入 `filesDir/crash/last_crash.txt`，但 **release 包在非 root 手机上无法用 adb 读取该私有目录**（release 非 debuggable，`run-as` 不可用），文件形同虚设；用户不熟电脑，adb 路径不可行。
- **修复（诊断桩，非业务改动、不改崩溃行为）**：`MainActivity.setContent` 内加门控——检测到 `crash/last_crash.txt` 存在时，**先渲染零依赖静态页 `CrashReportScreen`（不加载会崩的 AppNavGraph/不建任何 ViewModel/不触网/不读库）**，把堆栈直接显示在屏幕上，附「复制崩溃信息」（写剪贴板）与「我已记录，继续使用」（删文件后进主界面）两入口。用户截图/粘贴回传即可定位真凶。即使主界面必崩，报告页也能稳定渲染——这是"崩了还能打开看到错误"的关键。
- **回归**：新增 `tests/verify_bug_2026_09_14_028_crash_report_screen.py` 5 项（读 filesDir 崩溃文件、有崩溃先报告页不进 AppNavGraph、报告页零依赖不建 ViewModel、有复制+继续两入口、dismiss 删文件），连同 BUG-2026-09-14-027 共 **8 passed**。版本号 15→16 / 3.8.1→3.8.2 便于用户核对装的是带报告页的包。
- **生效条件与下一步**：CI 转绿产出 3.8.2 后，用户卸载重装 → 让 App 崩一次 → 再打开即见报告页 → 截图/复制堆栈回传。**拿到真实堆栈前不再做任何"修复"**（BUG-2026-09-13-023 五次盲改教训）。
- **CI 验收（已确认，commit `76adfb4`）**：`Android APK Build` #494 success，Release 资产已更新为 3.8.2（versionCode=16）。
- **用户侧终验（2026-09-14，使命达成）**：真机 3.8.2 冷启动崩溃后再开，报告页稳定渲染，用户复制回传完整堆栈——**正是该堆栈定位出 BUG-2026-09-14-029 真凶**。诊断桩按设计完成使命，作为常驻安全网保留：今后任何崩溃用户都能自助取证回传，不再依赖 adb（release 包非 debuggable 无法 run-as 读私有目录的死角就此补上）。

### BUG-2026-09-14-029（2026-09-14，「WMS扫码屡次停止运行」真实根因：离线队列构造期急切解析 api）

- **现场证据（首次拿到真实堆栈）**：BUG-2026-09-14-028 的应用内崩溃报告页在真机 3.8.2 上捕获 `last_crash.txt`：
  ```
  RuntimeException: Cannot create an instance of class ScanViewModel
    at ...NavGraphKt.AppNavGraph
  Caused by: IllegalStateException: 服务器地址未配置，请先登录并填写服务器地址
    at RetrofitClient.getApiService
    at ScanViewModel.<init>
  ```
  设备 HUAWEI LIO-AN00 / sdk 31 / version 3.8.2(16)。**这是历轮首次拿到决定性堆栈——印证"无 logcat 盲改必失败"。**
- **根因（代码实证）**：`AppNavGraph` 组合期创建 `ScanViewModel` → 其 `init`（`ScanViewModel.kt:304`）**同步**访问 `repository.offlineQueue`（**不在 safeCall 内**）→ `offlineQueue` 的 lazy 构造 `OfflineQueueManager` 时**急切**解析 `api`（`WmsRepository.api` getter = `RetrofitClient.apiService`，`RetrofitClient.kt:73` 在 `baseUrl` 空时 `check` 抛 `IllegalStateException`）→ fresh install / 清除数据 / 未登录时 baseUrl 为空 → 冷启动组合期抛异常 → 闪退。
- **与前两轮"修复"的关系（为何一直不好）**：BUG-2026-09-13-023 修的数据持久化（Room/Keystore/AuthViewModel）与权限路径**都不是本次真凶**；本轮崩溃在**网络层 api 的构造期急切解析**。`apiService` 的 `check` 是**故意安全守卫**（防 token 发往未配置服务器），不能删；真正错误是 `OfflineQueueManager` 构造期就解析 api（它仅在 `replay()` 同步时才用 api）。
- **R6 同根因排查**：协程内 api 访问（getDashboard/getWarehouses 等）全走 `safeCall`（`WmsRepository.kt:907`，catch Exception 兜底）→ 安全；`.offlineQueue` 全项目仅 3 处：`WmsApplication:114`（已 runCatching）、`ScanViewModel:304`（init 同步，本次崩点）、`ScanViewModel:321`（用户触发）——修复后三处均安全。`OfflineQueueManager.getInstance` 全项目仅 `WmsRepository:60` 一处调用。
- **修复**：`OfflineQueueManager` 构造改收 `apiProvider: () -> WmsApiService`，内部 `by lazy { apiProvider() }` 延迟到 `replay()` 首用时才解析 api（彼时已登录、baseUrl 已配置；即便仍为空也由 `doSync` 的 try/catch 兜为失败，不崩）；`getInstance` 同步改签名；`WmsRepository.offlineQueue` 改传惰性 `{ api }`。**构造期彻底不碰网络层。**
- **回归**：新增 `tests/verify_bug_2026_09_14_029_offlinequeue_lazy_api.py` 4 项（构造收 provider 非 eager api、内部 by lazy 解析、getInstance 收 provider、repository 传 { api } 非裸 api），连同 027/028 共 **12 passed**。版本 16→17 / 3.8.2→3.8.3。
- **生效条件**：CI 转绿产出 3.8.3 后用户卸载重装，fresh install 冷启动不再闪退，可正常进入登录页。
- **CI 验收（已确认，commit `e1aea42`）**：`Android APK Build` #495 / `WMS CI` / `WMS AI Verification` 三工作流全部 success；Release 资产于 2026-09-14T09:34:52Z 更新为 3.8.3（versionCode=17）新包（沙箱代理通道下载 APK 屡被截断无法字节级校验，但 #495 构建自本提交 + 资产更新时间紧随其后 + 用户装此包后闪退消失——此前 3.8.2 上 100% 复现——行为变化即含修复的实证）。
- **用户侧终验（2026-09-14，已修复结案）**：真机 HUAWEI LIO-AN00 卸载重装 3.8.3，fresh install 冷启动**不再闪退**，正常进入登录页并**登录成功**。期间一次「登录失败(502)」经外网实测排查为服务器端后端进程停止（见 BUG-2026-09-14-030），与 App 无关；服务器恢复后登录即成功，反证 App 冷启动与网络链路均正常。**至此「WMS扫码屡次停止运行」三根因（023 数据持久化+权限、027 versionCode 冻结、029 离线队列构造期急切解析 api）全部闭环结案。**

### BUG-2026-09-14-035（2026-09-14，手机端物料档案只显示前 50 条：搜索接口 `.limit(50)` 截断 + 进入不加载）

- **发现方式**：用户要求「手机端物料档案能展示所有物料」。
- **根因（代码实证）**：①后端 `/mobile/api/material_archive/search` 硬编码 `.limit(50)`——空关键字浏览也只返回前 50（R1：默认上限被当成全量）；②`MaterialArchiveSearchScreen` 为纯搜索屏，进入不自动加载，空关键字显示「输入关键字搜索物料」。
- **连带隐患**：`_archive_material_payload` 的 `image_count` 逐物料 `COUNT`、`unit`/`category` 惰性加载——若直接取全会放大为 N+1（数千次 SQL）。
- **R6 同根因排查**：与 BUG-2026-09-07-003/004（报表截断）、BUG-2026-09-10-009（日报逐页翻页 R1）同 R1 模式；grep 移动端列表接口，本 search 为唯一硬编码小 limit 的物料主数据浏览接口，其余（库存/订单/预警）均已分页带 `total_pages`。
- **修复**：①后端返回全部匹配物料（主数据量可控），附 `total`/`truncated` 元数据，安全上限 `_MATERIAL_ARCHIVE_BROWSE_MAX=5000`；`image_count` 改一次 GROUP BY 批量预取（`_safe_material_image_counts`），`unit`/`category` `joinedload` 预加载——取全收敛为常量次 SQL；②Android 进入即 `LaunchedEffect(Unit){ viewModel.search() }` 自动加载全部，空态文案改为「暂无物料档案」。
- **回归**：新增 `tests/verify_bug_2026_09_14_035_material_archive_show_all.py` 6 项（空关键字返回全部 60 条+total、N+1 SQL≤12、超限 truncated+真实 total、关键字过滤、批量 image_count 正确、Android 自动加载静态断言+无硬编码 limit）；既有 `verify_mobile_material_archive_api.py` 12 项同步全绿；`lint_wms_rules` / `lint_no_raw_post_fetch` 0 违规。
- **生效条件**：后端改动拉取后**重启 WMS 服务生效**（R3，接口可先 `?keyword=` 自查）；**Android 改动需 CI `assembleRelease` 产出新 APK 后重装**（本地沙箱无 Java/SDK，编译验收以 CI 为准，BUG-2026-09-12-006 规则）。

### WECOM-BOT-001（2026-09-14，新增能力：微信分享接入「企业微信群机器人」通道，非重复BUG）

- **背景**：本机助手 UI 自动化存在「微信窗口必须常开 + 不能切窗口」的**结构性**限制（BUG-2026-09-14-034 已修系统代理劫持回环，但窗口/焦点前提无法靠加固消除）。方案评估：无企业微信时第三方推送服务（WxPusher/PushPlus）均**只能发链接且需上传第三方图床**（隐私 + 有效期问题），故采用**免费企业微信群机器人 webhook**——唯一能同时满足「原生图 + 不开窗口 + 图片不出内网」的通道。
- **机制**：`POST https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=XXX`，`image`（base64+md5，≤2MB）原生图直发 + `markdown` 摘要；国内 endpoint **直连**（`proxies={'http': None, 'https': None}`，复用 BUG-2026-09-14-034 思路，防代理错误路由出境被拒）；图片内联**不经过第三方图床**；每机器人限 20 条/分，批量推送单与单 `sleep(3)` 节流。
- **实现**：配置存 `system_setting['wechat_share_wecom_webhook']`（**免数据库迁移**）；新增 `_wechat_share_wecom_webhook` / `_wechat_share_wecom_webhook_allowed`（白名单仅 qyapi 官方 send 端点，防 SSRF/key 泄露）/ `_wechat_share_wecom_image_bytes`（>2MB Pillow 重编码）/ `_wechat_share_send_wecom` / `_wechat_share_deliver` **调度器**——配 webhook 走企业微信、未配回退本机助手（**完全向后兼容**）；`_wechat_share_order` 组装 markdown 摘要（单号/供应商/明细数/合计），重发路由同步分流；设置页新增 webhook 输入框（FormData + base.html 全局 fetch 拦截器，**免改 JS**）。
- **回归**：新增 `tests/verify_wecom_bot_001_channel.py` **8 项**（调度切换、image 载荷 base64+md5+proxies 直连、errcode 映射、超限重编码 JPEG、webhook 白名单、caption 先于图）；连同微信分享既有用例全绿；`lint_wms_rules` / `lint_no_raw_post_fetch` 0 违规。
- **生效条件**：**生产需重启 WMS 服务**（模板缓存刷新，R3）。真实联调需用户免费注册企业微信→建群加机器人→把 webhook 填入「微信分享」设置页→点「立即执行」验证（单元测试以 mock webhook 完成，未触达真实腾讯接口）。

### BUG-2026-09-14-034（2026-09-14，微信分享批量「HTTP 502」+ 健康检查 10090 超时：系统代理劫持本机回环请求）

- **发现方式**：现场报障。用户截图：微信分享记录批量「失败：微信发送助手返回错误：HTTP 502（错误码：http_502）」，当前状态面板「发送助手 不可用」，报错 `HTTPConnectionPool(host='127.0.0.1', port=10090): Read timed out. (read timeout=1.5)`，待发送/失败积压 160 条。
- **根因（代码 + 现场证据，非推测）**：服务器为拉取 GitHub 代码常驻代理软件。代理开启「系统代理」后写入 Windows 注册表/环境变量代理（监听 `127.0.0.1:10090`）。WMS 侧 `_wechat_share_send_image`（POST /send）、`_wechat_share_get_helper_health`（GET /health）与助手侧 `poll_once` 均用 Python `requests` 默认行为调用本机回环地址（127.0.0.1:8765 / 8080）；requests 读取系统代理后把回环请求路由到 10090，代理无法处理回环目标 → 回 HTTP 502（POST /send）或 read timeout（GET /health）。**关键佐证**：健康检查报错端口是 **10090（代理端口）而非 8765（助手端口）**——证明请求被发去了代理。与 BUG-2026-09-14-030 的 502（服务器后端进程停止）根因不同。
- **R6 同根因排查**：grep 全仓对回环 helper 的 requests 调用，确认消费点共 6 处——WMS 侧 2 处（app.py /send、/health）+ 助手侧 4 处（wechat_helper.py `poll_once` 的 GET tasks / GET image / POST report ×2），全部同一根因，本次一并修复；未发现其它对回环 helper 的 requests 调用点。
- **修复**：新增模块常量 `_LOOPBACK_NO_PROXY = {'http': None, 'https': None}`（app.py 与 wechat_helper.py 各一），6 处回环调用统一显式传 `proxies=_LOOPBACK_NO_PROXY`。原理：requests 合并环境代理用 `setdefault`，显式 `None` 优先生效，`select_proxy` 返回 `None` → 直连本机，与代理开关状态无关，用户无需「不用就关代理」。
- **回归**：新增 `tests/verify_bug_2026_09_14_034_loopback_no_proxy.py` 4 项——T1 /send 携带 no-proxy、T2 /health 携带 no-proxy、T3 机制证明（设 HTTP(S)_PROXY 且清 NO_PROXY 后，`merge_environment_settings`+`select_proxy` 仍返回 None 直连；对照组不传 no-proxy 时代理确被选中）、T4 助手源码静态断言（Windows-only 无法 import，按仓库惯例）；并为 008/010/012 现有测试桩补 `proxies` 形参适配新关键字。相关用例 **27 passed**（含 008/009/010/012 全绿）。
- **生效条件**：仅改 WMS 主服务与微信助手进程对本机回环的 HTTP 调用方式，不改业务数据、不改助手 UI 自动化/剪贴板/前台校验逻辑；**生产需重启 WMS 服务（及微信助手进程）生效**。

### BUG-2026-09-14-033（2026-09-14，Android 单元测试门禁自建起一直空跑：无测试目录、无测试依赖）

- **发现方式**：移动端工程质量走查（非现场报障）。属**流程性缺陷**——门禁显示绿色但从未拦住任何东西。
- **缺陷事实（两条并存，共同导致 `testReleaseUnitTest` 形同虚设）**：
  1. `.github/workflows/android-build.yml` 自 **BUG-2026-08-16-021** 起就包含步骤 `./gradlew testReleaseUnitTest`，但 **`app/src/test` 目录根本不存在**；
  2. `app/build.gradle.kts` 的 `dependencies` 里**没有任何测试依赖**（junit / robolectric / coroutines-test / androidx.test 全无）——即便写了测试也无法编译。
- **为何一直没被发现**：Gradle 对"零测试源"的 `testReleaseUnitTest` 任务会**以 NO-SOURCE 成功退出**，CI 显示绿色。门禁"看起来有"，实际从未运行过一行断言。
- **后果（这才是关键）**：项目所谓 55 个"Android 测试"**全部是 Python 正则匹配 Kotlin 源码字符串**——能防"某行守卫被删"，但**测不出任何运行时行为**。这正是 **BUG-2026-09-14-029（冷启动闪退）要修 5 轮才定位到真凶**的根因：每轮改完只能靠真机人肉验证，而沙箱/CI 又编译不了 APK，形成"盲改 → 真机复现 → 再盲改"的循环。
- **修复（补基础设施 + 首批真实测试）**：
  1. `build.gradle.kts` 新增 `testOptions.unitTests { isIncludeAndroidResources = true; isReturnDefaultValues = true }`（Robolectric 读资源与 Manifest 所必需）；
  2. 新增测试依赖：`junit:junit:4.13.2`、`org.jetbrains.kotlinx:kotlinx-coroutines-test:1.8.1`、`org.robolectric:robolectric:4.14.1`、`androidx.test:core:1.6.1`、`androidx.test.ext:junit:1.2.1`、`com.squareup.okhttp3:mockwebserver:4.12.0`、`androidx.arch.core:core-testing:2.2.0`；
  3. 新建 `app/src/test/java/com/factory/wms/` 及**首批真实可执行测试**（Robolectric，`@Config(sdk=[31])` 对齐现场机型 HUAWEI LIO-AN00）：
     - `RetrofitClientSessionTest.kt`（8 例）：锁死 `apiService` 在 baseUrl 未配置时**必须抛 IllegalStateException** 且消息可读（BUG-2026-09-14-029 的安全守卫语义——防后人为"不崩"而删守卫致 token 泄漏到占位地址）；尾斜杠容错；登出后守卫恢复；token 生命周期；共享 OkHttpClient 实例稳定（Coil 复用连接池）；`setBaseUrl` 并发读写不产生"新 baseUrl + 旧 retrofit"中间态。
     - `OfflineQueueStateMachineTest.kt`（8 例）：用**真实 Room 内存库**验证状态机——①`syncing` 记录确实取不到（**这就是静默丢数据的机制**）②`resetStuckSyncing` 后必须重新可见（BUG-2026-09-12-008 核心）③达上限记录落 `failed` 且被 `countFailed` 统计（用户必须看得见）④**出口完整性**：pending∪failed 覆盖全部非成功路径，任何记录不得停留在 syncing ⑤`resetToPending` 清零次数与原因（否则刚重置就被下次失败打回）⑥`upsert` 按 requestId 幂等（弱网重复点击不产生重复单据的本地保障）⑦补传按入队时间升序（防同物料先出后入被颠倒）。
- **回归**：新增 `tests/verify_bug_2026_09_14_033_android_unit_test_infra.py` 10 项——测试目录存在、含真实 `@Test`、3 个必需依赖已声明、Robolectric 资源开关已开、CI 仍保留 `testReleaseUnitTest`、**覆盖三个真实崩溃根因的契约**（baseUrl 守卫 / syncing 非终态 / 失败可见）、**反向断言禁止占位断言**（`assertTrue(true)` 之类糊弄门禁）、BUG 已登记基线。
- **验证局限性（如实记录）**：沙箱无 Kotlin 工具链且 Maven Central / GitHub Releases 下载均被网络策略阻断（kotlinc 13.7MB 处截断、repo1.maven.org 不可达），**本地无法编译运行该 Kotlin 测试**。已改用**逐项 API 签名一致性核对**代替：核对 6 个 `RetrofitClient` 成员、11 个 DAO 方法、7 个 Entity 属性、5 个常量、`data class`（`copy()` 依赖）、`AppDatabase` 抽象类声明——**全部与源码一致**。
- **CI 验收（已确认，含门禁有效性对照实验）**：
  - `Android APK Build` **#499**（`a9e4b132`，常规构建）：**全步骤 success**，含 `Unit tests (BUG-2026-08-16-021)` success；
  - **门禁有效性对照实验**：临时提交一个**必然失败**的测试探针（`assertEquals(1, 2)`），CI **#500**（`c100c254`）结果 `Unit tests` 步骤 **failure**，而 `Build Release APK (R8 瘦身)` 与 `Lint` **均 success**。该对照一次性证明三件事：①Kotlin 测试源**真实参与编译**（若测试有语法/依赖错误，编译与 Lint 阶段就会红，不会走到测试步骤）②Robolectric 及各测试依赖**正确解析** ③**门禁真实生效**——故意失败的断言被成功拦截。探针随后已移除，远端 main 重置回 `a9e4b132`（净变更零）。
  - **结论**：门禁从「`app/src/test` 不存在 → Gradle NO-SOURCE 空跑却显示绿色」变为「**有真实测试且能拦截失败**」，BUG-2026-09-14-033 完成闭环。
- **生效条件**：Android 改动；**不影响 APK 产物行为**（仅新增测试代码与 `testImplementation` 依赖，`implementation` 未变），无需重装。

### BUG-2026-09-14-032（2026-09-14，NavGraph 组合根饿汉创建全部 ViewModel：崩溃放大 + 启动开销 + 竞态温床）

- **发现方式**：移动端工程质量走查（非现场报障）。属**结构性缺陷**——无直接崩溃复现，但为既有崩溃的**放大器**，且是 BUG-2026-08-24-006 的**未根治根因**。
- **根因**：`AppNavGraph()` 函数体开头一次性创建 **14 个 ViewModel**：

  ```kotlin
  val authViewModel = viewModel()
  val inboundScanViewModel = viewModel(key = "inbound_scan")
  ... （共 14 个，含 4 个 ScanViewModel 副本）
  val overviewListViewModel = viewModel(key = "overview_list")
  ```

  `AppNavGraph` 是**全 App 的组合根**，在 `MainActivity.setContent` 第一帧即被调用——故这 14 行等价于「App 一启动就构造所有页面的 ViewModel」。

- **三个具体危害**：
  1. **崩溃放大（最严重）**：任一 VM 构造期抛异常 = 整个进程闪退。BUG-2026-09-14-029 的 `ScanViewModel.<init>` 抛 `IllegalStateException`（baseUrl 未配置）正是**被这里放大**成"打开应用即闪退"——若按需创建，该 VM 只在用户进入扫码页时才构造，最多是一个页面打不开，不会全 App 崩溃。
  2. **启动开销与内存常驻**：14 个 VM 及其依赖的 `WmsRepository` / Room DAO / 协程作用域全部常驻，其中语音/AI/期初/报表等页面用户可能从不打开。
  3. **竞态温床**：VM 在"会话尚未还原"（`AuthViewModel.init` 异步读 DataStore + EncryptedSharedPreferences，耗时数百毫秒）时已被构造，其 init 中的网络请求必然读到空 baseUrl。
- **R6 同根因排查（关键）**：命中 **BUG-2026-08-24-006**——其描述就是「`AppNavGraph` 组合阶段即创建 `ReportViewModel` 并在 init 立即 load()，与会话还原竞态 → 报表页报『服务器地址未配置』」。当时的修复是①`ReportViewModel.init` 改为不自动加载②`WmsRepository` 加 `ensureSession()` 兜底。**两处都是对症补丁，饿汉创建这个根因从未消除**——证据：`StocktakeRecordViewModel.init` 与 `ReportViewModel.init` 里都留着「BUG-2026-08-24-006：不能在 init 拉数据」的注释，说明**同一个根因已被迫用「禁止在 init 做事」这种约束反复绕行**，而 VM 本身仍在启动期被无谓构造。本次从根上消除，使这类约束不再必要。
- **修复（结构收敛，不改任何业务逻辑）**：
  - `AppNavGraph` **顶层只保留 `authViewModel`**（`startDestination` 依赖其 `isLoggedIn`，且必须在导航建立前存在）；
  - 其余 **13 个 VM 全部下沉到各自 `composable(...)` 路由内**按需创建（18 个创建点，含跨路由共用项）；
  - 语音悬浮层两个 VM 移入 `if (authState.isLoggedIn)` 分支内创建——未登录时完全不构造。
- **实例语义保持不变（关键论证）**：路由内 `viewModel()` 的宿主是 **Activity 级 `ViewModelStore`**，同一 key 跨路由**复用同一实例**，故：
  - 4 个 `ScanViewModel` 用不同 key（`inbound_scan`/`outbound_scan`/`stock_query`/`stocktake`）→ 各自独立，与修复前一致（否则入库明细会串到出库页）；
  - `AiViewModel` 在 `DocumentOcr`/`ObjectRecognize`/`StocktakeRecognize` 三处均用默认 key（按类型）→ **同一实例**，与修复前共享语义一致；
  - `VoiceOutDraftViewModel` 在语音悬浮层与 `Outbound` 路由两处同类型 → **同一实例**，语音建单草稿与出库页读写同一份状态；
  - 物料档案列表页与详情页共用 `MaterialArchiveViewModel` → 同一实例，选中态不丢。
- **用户可见影响**：启动时构造的 VM 由 14 个降至 **1 个**（登录页场景）；首屏后按实际访问逐个构造。启动更快、内存更低、**单页 VM 故障不再拖垮全 App**。
- **回归**：新增 `tests/verify_bug_2026_09_14_032_navgraph_lazy_viewmodels.py` 11 项——① 组合根顶层无饿汉创建（仅允许 authViewModel）② 反向保证 authViewModel 仍在顶层（防误下沉致启动落错页）③ 5 个带 key 的 VM 必须在路由内且 key 保留（防串页）④ 每个创建点必须位于 composable 路由或已登录分支内 ⑤ 语音 VM 受 isLoggedIn 守卫 ⑥ 同一 key 不得赋予不同 VM 类型 ⑦ BUG 已登记基线。**全绿**（预跑时基线未登记项按预期失败，证明测试有效）。
- **生效条件**：Android 改动，需产出新 APK 后重装；无需后端重启。

### BUG-2026-09-14-031（2026-09-14，Compose 列表 key 塌缩为常量致页面崩溃隐患：4 处 items key 兜底缺失）

- **发现方式**：移动端工程质量走查（非现场报障）。属**潜在缺陷**——当前后端 id 均为自增主键故未触发，但契约上未设防。
- **根因（形式）**：`items(list, key = { it.id ?: 0 })`。模型 id 声明为可空 `Int?`（`MaterialDto`/`AlertItemDto`/`MobileOrderDto` 均如此）。一旦后端下发 **2 条以上 id 为 null** 的数据（异常响应 / 分页拼接 / 未落库临时行），所有 key 塌缩为同一个 `0`；Compose `LazyColumn` **要求 key 唯一**，重复即抛 `IllegalArgumentException` → 页面崩溃。
- **关键设计原则（为何不能用常量兜底）**：`key` 的用途是「跨重组的稳定身份」。用常量兜底等于**主动放弃身份唯一性**——它不是"无害的降级"，而是把「数据异常」放大成「应用崩溃」。正确兜底必须逐级取**仍具区分度**的字段。
- **修复（4 处消费点，统一为「id 有效 → 业务唯一码 → hashCode」三级兜底）**：

  | 文件:行 | 修复前 | 修复后 |
  |---|---|---|
  | `ScanScreens.kt`（查库存列表） | `key = { it.id ?: 0 }` | `key = { it.id ?: it.code ?: it.hashCode() }` |
  | `OverviewListScreen.kt`（库存告警） | `key = { it.id ?: 0 }` | `key = { it.id ?: it.code ?: it.hashCode() }` |
  | `OverviewListScreen.kt`（单据下钻） | `key = { it.id ?: 0 }` | `key = { it.id ?: it.orderNo ?: it.hashCode() }` |
  | `StocktakeRecordScreens.kt`（盘点记录） | `key = { it.id }` | `key = { if (it.id != 0L) it.id else it.checkNo ?: it.hashCode() }` |

- **第 4 处为 R6 扩展命中**：`StocktakeRecordDto.id` 是**非空 `Long = 0`**，`key = { it.id }` 静态看"安全"，但该默认值使其与前者**同根因**——Gson 反序列化遇缺失字段落 0，多条即冲突。既然在修同一根因，一并收敛（R6：同根因全量排查，不修一半）。
- **R6 同根因排查**：全项目 `items(...key=...)` 共 6 处，逐一核验——`MaterialArchiveScreens.kt:224`（三材料三级兜底）与 `:552`（图片 `id ?: hashCode`）**原本就是正确范式**，本次修复即以其为参照标准；其余 4 处为本次修复对象。修复后全项目**零**常量塌缩 key。
- **回归**：新增 `tests/verify_bug_2026_09_14_031_list_key_uniqueness.py` 7 项——①~③ 三处消费点 key 必含 `hashCode()` 兜底 ④ 盘点记录 key 必判 `id != 0L` 且含 `checkNo`/`hashCode` ⑤ **全项目反向断言**（正则扫描所有 `key = { }`，禁止 `?: 0` / `?: ""` 常量塌缩——根因形式的机器可检约束）⑥ 既有正确范式（MaterialArchiveScreens）不得被误伤 ⑦ BUG 已登记基线。**11 passed**（含修复前预跑：基线未登记项按预期失败，证明测试有效）。
- **生效条件**：Android 改动，需产出新 APK 后重装；**当前无现场触发路径，属预防性修复**，不影响既有已装版本正常使用。

### BUG-2026-09-14-030（2026-09-14，手机登录「502」：服务器后端进程停止，非 App/代码缺陷）

- **现场**：用户装 3.8.3 后 App 正常打开进入登录页（029 闪退已愈），点登录报「登录失败(502)」。
- **R6 同根因排查**：命中 BUG-2026-08-11-001（登录 502）——但该次是**后端在跑、`WMS_NO_DB_TOUCH=1` 缺表 → `/api/login` 未捕获 OperationalError → 500**，仅登录接口异常。本次外网实测 `https://gd2026.top` **首页、登录接口全部端点 502**，响应头 `Server: nginx/1.30.0` —— nginx 网关存活、上游 WMS 后端（Waitress `0.0.0.0:8080`）整体不可达。**不同根因：后端进程停了，不是后端报错。**
- **判定**：`start_wms_auto.bat` 以前台控制台进程运行后端（窗口打印 "Press Ctrl+C to stop"），**窗口被关闭 / 服务器重启或休眠即后端停止** → nginx 对全部请求回 502。非代码缺陷，不改代码。
- **处置与恢复验证（外网实测）**：服务器侧重跑启动脚本后，首页 302→`/login` 200（登录页正常渲染）、`POST /api/login` 对测试账号返回 401「账号或密码错误」（正常业务响应，证明 nginx→Waitress→数据库全链路健康），用户随后真机登录成功。
- **遗留风险与建议（未实施，待用户确认）**：生产后端依赖前台窗口进程，关窗/重启/休眠 = 全员断联且无自愈。建议注册为 Windows 服务或任务计划程序（开机自启 + 崩溃自动重启 + 无窗口常驻），一次性配置彻底消除该风险。

### BUG-2026-09-13-025（2026-09-14，分发链核验：用户"解析包时出现问题"= 下载截断，非代码缺陷）

- **现场**：用户装包后截图系统提示「解析包时出现问题」，无法安装。
- **判定**：**不是代码缺陷**。安卓在解析阶段即拒收，说明 APK 文件头尾不齐——下载中途断流。正常包 `25,095,687` 字节；截断包无 EOCD 结尾，非合法 ZIP。
- **核验动作**：从固定 Release（tag `android-apk`）重新拉取，逐项校验并留证：

  | 校验项 | 结果 |
  |---|---|
  | 文件大小 | 25,095,687 字节（与 CI 产物一致） |
  | ZIP 结构（EOCD） | 通过，`testzip()` 无损坏项 |
  | 条目数 | 424 |
  | 包名 / 版本 | `com.factory.wms` / versionCode **14**、versionName **3.8.0** |
  | MD5 | `bf2bdbcb548dc61b04df857813bec865` |
  | SHA256 | `6c51f8ebb324fa1157c8b53926f8e7e3…` |

- **CI 全绿（本轮）**：`#488 Android APK Build` success（`e5eff9c`）、`#1272 AI Verification` success、`#977 WMS CI` success。对照 `#486` 为 failure（即 `BUG-2026-09-13-024` 的那次回归，该次**无包产出**）。
- **字节码级验收（androguard，两代包对照）**：

  | 验证项 | 线上固定 Release 包 | 工作区旧包 |
  |---|---|---|
  | `MainActivity.onCreate` 权限相关调用数 | **0** | **0** |

  两代包均确认启动期权限申请已移除（对应 `BUG-2026-09-13-023` 第二根因修复）。
  注：`encryptedPrefs` / `getSavedToken` 等私有成员名经 R8 混淆后无法在字符串池直接检索，故改以**方法调用序列**为验收口径——`MainActivity.onCreate` 的指令级调用数是不可伪造的硬证据。
- **全量测试**：`1726 passed / 85 skipped / 0 failed`。
- **分发结论**：固定 Release 直链
  `https://github.com/SIX2090/wms/releases/download/android-apk/wms-mobile-scan.apk`
  每次推送后由 CI 自动覆盖，永远为最新包。工作区 `/workspace/wms-mobile-scan.apk` 已替换为 #488 产物（MD5 同上）。
- **教训**：**"装不上"与"装上了崩"是两条完全不同的路径**，前两轮把「解析包失败」并入「启动崩溃」一起排查，属于归因错误。分发链完整性应作为独立环节先验，再谈代码。

### BUG-2026-09-13-024（2026-09-14，我造成的回归：陈旧基线导致 6 个公共 API 与 3 个源文件被删，APK 构建 40 处编译错误）

- **现场**：`Android APK Build #486`（远端 `7d2f81c6aa`）`step 8 Build Release APK (R8 瘦身)` = failure，卡在 `:app:compileReleaseKotlin`，**40 处错误全部集中在 `ScanViewModel.kt`**：
  ```
  e: ScanViewModel.kt:151:34 Unresolved reference 'editDraftKey'
  e: ScanViewModel.kt:190:24 Unresolved reference 'saveEditDraft'
  e: ScanViewModel.kt:265:37 Unresolved reference 'getLocationOptions'
  e: ScanViewModel.kt:1041:60 Too many arguments for
      'suspend fun submitInbound(request: InboundRequest): Result<SubmitResult>'
  e: ScanViewModel.kt:1109:72 No parameter with name 'replay' found
  ```
  因该包未产出新 APK，**用户手机上仍是旧包**——这正是"修了一百遍还是修不好"的直接原因：改动根本没到用户手上。
- **根因**：上一提交 `bda6623` 修启动崩溃时**基线取错**——编辑的是本地工作树中一份陈旧副本，而非远端最新版。该提交把远端 `WmsRepository.kt` 从 **933 行裁到 863 行**，删除 6 个已被 `ScanViewModel` 依赖的成员（`editDraftKey` / `saveEditDraft` / `loadEditDraft` / `getLocationOptions` / `submitInbound(requestId)` / `submitOutbound(requestId, replay)`），并连带删除 3 个源文件（`data/model/LocationOptions.kt`、`data/model/ScanEditDraft.kt`、`ui/components/ScanLocationSelector.kt`）。
- **为何本地与 AI 自查都没拦住（本次最重要的方法论教训）**：
  ① 沙箱无 Compose/AGP，本地编译只能做「基线 vs HEAD 错误集**差集**」。而当时对比的两侧**都带这个缺陷**，差集为空 → 误判为"干净"。**差集法只能发现「本次新增」，发现不了「本来就不该被删的」。**
  ② 更早一轮还误判过「本地 main 与远端同源」：实际 `git merge-base HEAD origin/main` **为空**（零共同提交），本地是一条与远端完全无关的历史。在该前提下做的所有"基线对比"都不可信。
  ③ CI 日志一度取不到（`actions/jobs/*/logs` → 302 到 `productionresultssaN.blob.core.windows.net`，沙箱 DNS 把它解析到伪 IP `198.18.0.33` 导致 TLS 失败；`WebFetch` 又在 Android SDK 许可协议大段文本处被截断）。**本次打通方法**：用 `223.5.5.5` 的 DoH 拿真实 IP，再 `curl --resolve <host>:443:<真实IP>` 直连，成功取得 191,908 字节完整日志——**这是拿到真实编译错误的唯一路径，后续勿再走弯路**。
- **修复**：将 `WmsRepository.kt` / `AuthViewModel.kt` 逐字节恢复到 `cc73fb0`（#485 全绿）版本。核对确认远端**已有等价且更完整的启动崩溃防护**（即 BUG-2026-09-13-023：`encryptedPrefs` 可空降级 + `resetSecurePrefsFile` 自愈 + `AuthViewModel.init` 整体 try/catch），故 `bda6623` 的这部分改动本身即为**重复实现**，回退无功能损失。
- **保留项**：`bda6623` 中唯一确有增量价值的一项——`WmsApplication` 的**进程级崩溃落盘器**（`setDefaultUncaughtExceptionHandler` → `filesDir/crash/last_crash.txt`）。现场此前只有"屡次停止运行"一句系统弹窗、拿不到任何堆栈，该装置让下次崩溃可直接定位；刻意**不吞异常**（原样交回系统默认处理），避免把崩溃变成静默数据错误。
- **验证**：
  - 本地 kotlinc 全树编译（排除 Compose 包）**0 error**；对照修复前同一条命令报 **40 error，与远端 CI 日志逐条一致**。
  - `WmsRepository.kt` / `AuthViewModel.kt` 与 `cc73fb0` **逐字节一致**（`git hash-object` 相同）；`WmsApplication.kt` 仅新增 `installCrashLogger()` 定义 + `onCreate` 内一行调用。
  - `tests/verify_android_startup_crash_safety.py` **6/6 passed**；**逐条破坏性测试复验**：去掉 try/catch、去掉处理器安装、让处理器吞异常——三种注入均被捕获。其中"只定义不调用"一项经复验确认为**空跑**（只断言 `installCrashLogger` 字符串存在，函数定义本身就能满足），已改为按花括号配平取 `onCreate` 体、校验内部确有调用。
  - 关联启动/编译门禁 **26 passed**；**全量 pytest 1727 passed / 84 skipped / 0 failed**（较修复前 1563 passed 多出 164 项——即被误删文件所携带的测试已回归）。
  - 远端 `66cd2c0f9c`：**`Android APK Build #487` = success（13 步全绿，含 assembleRelease / lintRelease / testReleaseUnitTest / 上传 / 发布 Release）**、`WMS AI Verification #1271` = success、APK 资产已更新至 `2026-09-14T03:02:03Z`（25,095,687 字节）。
- **R6 固化的规则**：
  ① 改任何文件前，先确认本地基线即远端最新（`git merge-base --is-ancestor HEAD origin/main` 非空且 `git diff origin/main -- <file>` 为空），**不得凭工作树现状假设基线正确**；
  ② Android 编译验收以 CI `assembleRelease` 为准，差集法不得作为"无新增错误"的充分证据；
  ③ 取 CI 日志用 DoH 真实 IP + `--resolve` 直连，不因 302/截断就放弃取证。

## 判定规则

| BUG-2026-09-13-008 | [P0] 微信助手编码损坏及字面反引号换行导致 SyntaxError，模块无法加载且全量 pytest 收集中断 | **已修复并验证（2026-09-13），发布以本次 Git 记录为准**：根因为 f46e472/27b330a 的错误文本写入；依据原始 51ddf0c 恢复完整 UTF-8 内容和被吞并的代码行，保留预期的中文安全错误提示，改为只记录异常类型的 warning，禁止回传异常原文或 traceback 刷屏。R6：复查 BUG-2026-08-11-009 与 08-16-019；恢复既有发送串行锁、焦点校验、无 token 拒绝逻辑，不触发实际微信发送。新增模拟 /send 解析异常的回归，相关 pytest 12 passed；py_compile 通过。全量 pytest tests -q --tb=short：1626 passed、85 skipped、4 failed（253 秒），四项均为既有 Windows 路径和图片文件占用问题：test_auto_migrate_db_path 两项、test_material_image_static_path 两项；收集阻断已解除，未进行真实微信发送。 |
| BUG-2026-09-13-011 | [P1] AI ???????/?????? `Material.stock`?????????????????????? | **????2026-09-13?**???? `order.warehouse`???? `from_warehouse` ??????????? `get_warehouse_stock_quantities` ????????????????????????R6???? `_ai_check_draft` ?????????py_compile?????????????????? WMS ????? |\n| BUG-2026-09-13-010 | [P1] AI-SALES-F02 shortage analysis used global Material.stock and a 50-row query limit, causing cross-warehouse false shortage results and incomplete counts | Fixed 2026-09-13: both consumers now validate SalesOrder warehouse and use get_warehouse_stock_quantities; invalid/empty historical warehouses are skipped with a warning; count scans the full result set while display remains capped at 5. R6 checked _ai_sf_query_short_stock and _ai_sf_query_customer_followup_list. Verification: py_compile, static gates, and 8 regression tests passed. Production requires WMS service restart. |
| BUG-2026-09-13-009 | [P1] 委外发料快速发料、新增明细、完成入口使用全局 `Material.stock` 校验，跨仓库会误判库存；完成入口同物料多行未累计需求可绕过库存限制 | **已修复（2026-09-13）**：三处入口统一按单据仓库解析 `validate_inventory_warehouse` + `get_warehouse_stock_quantities` 校验；新增明细和完成入口累计同物料需求；保留原子扣减、负库存开关、历史仓库兼容和人工完成边界。R6：同类点已排查并一次覆盖快速发料/新增明细/完成三处。验证：委外库存与删除流水回归 26 passed；py_compile、git diff --check、静态门禁待提交前执行。生产需重启 WMS 服务生效。 |

| BUG-2026-09-13-007 | [P1] 领料草稿保存缺少仓库函数导入导致保存失败；新增无库存检查、修改和批量未累计同物料需求 | **已修复并验证（2026-09-13），发布以本次 Git 记录为准**：补齐保存导入，保存/新增/修改/批量统一按单据仓库校验累计需求，修改排除自身旧数量、遵守负库存设置，错误消息使用仓库库存。整单失败主动回滚，批量保持部分成功语义。R6：对照 BUG-2026-08-16-016、09-13-002/006，领料同类点已排查；完成仍走带仓库的原子扣减，导入草稿由完成兜底，不改变人工提交边界。新测试 tests/test_bug_2026_09_13_007_requisition_stock.py 修复前 9 failed/12 passed，修复后 21 passed；加上库存、库位必填、删除锁、领料人及自动推单回归共 68 passed。双仓、历史编码流水、明细累计和草稿不改库存均验证。全量 pytest 因旧 wechat_helper.py 语法错误在收集期中断；排除该文件的尝试在 240 秒超时，不能报告全量通过。 |

| BUG-2026-09-13-006 | [P1] 调拨草稿新增、修改、批量添加仍使用全局库存；表格保存与明细入口未累计重复物料需求 | **已修复并本地验证（2026-09-13），推送以本次 Git 记录为准**：四个草稿入口按来源仓库校验，单行修改排除自身原数量，新增与批量加入既有行需求；表格整单保存累计请求数量，超额回滚。批量保持原有部分成功语义，只累计实际接受行；负库存开关一致生效。新增 16 项真实 SQLite 路由回归，覆盖空源仓/其他仓充足、历史 NULL warehouse_id 编码流水、重复行超额、失败保留原明细、草稿不写库存及总账等于两仓合计。相关四文件测试 57 passed。修正两个原回归夹具：为库位必填测试提供真实仓库库存；完成校验测试直接构造历史草稿，不再依赖空仓通过保存。R6：对照 BUG-2026-08-16-008 与 BUG-2026-09-13-002，同类点已排查，transfer.py 中剩余全局库存校验均清除；完成/反提交沿用现有累计校验并回归通过。Excel 导入属于草稿录入，不以库存充足作为导入承诺，最终完成仍有库存门禁。跨模块领料、委外、销售与 AI 草稿问题不在本次修复完成范围，仍待处理。验证命令：python -m pytest tests/test_bug_2026_09_13_002_warehouse_deductions.py tests/test_bug_2026_08_16_008_transfer_off_branch_source_stock.py tests/test_inv_audit_005_warehouse_active_validation.py tests/test_p1_transfer_location_required.py -q。 |

| BUG-2026-09-13-005 | [P1] 分区盘点在实盘汇总前跳过零差异区域，导致实盘漏计、盘盈被算成盘亏，并绕过多库位合并限制 | **本地修复并验证，远程发布待核验（2026-09-13）**：有区域行无论行级差异是否为零都参与合计与库位边界校验，最终净差异为零才不生成调整。R6：同类点已排查，扫码独立调整走差异求和而非分区实盘减单份账面，零差异过滤不影响其和，未修改其语义。新增 6 项真实 SQLite 路由回归（盘盈+2、合计无差异、盘亏、单区零差异、全部零库存、多库位包含零差异区域时拒绝），与区域保存/复制回归共 13/13。全量 1588 passed/85 skipped/4 failed；4 项在原始 SHA 51ddf0c 上同样失败，均为 Windows 路径/图片文件占用既有测试问题；静态回归通过。生产需重启 WMS 服务生效。 |

| BUG-2026-09-13-004 | [P1] PC 盘点保存忽略区域且只按物料匹配，重复新行重复计差异、多区域互相覆盖；Excel 同类入口漏防重复区域 | **本地修复并验证，远程发布待核验（2026-09-13）**：表格按物料+区域 upsert，拒绝请求内重复键、历史重复行显式要求处理；库位启用时区域必填。页面新增区域录入、复制、提交和回显；Excel 接收区域并跳过重复键/缺库位行。R6：复查 09-02-004、09-06-005；同类点已排查，扫码批次已有物料+区域键，复制已保留区域，单行添加已有物料判重。`pytest tests/test_bug_2026_09_13_004_check_row_identity.py tests/test_bug_2026_09_13_003_check_warehouse_freeze.py tests/test_inv_batch_001b_check_freeze_upsert.py tests/verify_bug_2026_09_03_001_check_import_warehouse.py -q` 20/20；涵盖重复拒绝无部分写入、多区域往返/删除/冻结、库位必填、人工完成后仅生成调整草稿、Excel 判重。**生产需重启 WMS 服务生效**。 |

| BUG-2026-09-13-003 | [P1] 已冻结盘点单换仓后沿用旧仓账面，导致调整错误归属新仓库 | **本地修复并验证，远程发布待核验（2026-09-13）**：保存写锁内按仓库 ID 校验冻结身份，有明细禁止换仓；同仓名称/编码等价，空单可选新仓。R6：复查 BUG-2026-09-02-004 与 09-03-001，同类点已排查，其他盘点更新不修改仓库、导入已拒绝同单跨仓。`pytest tests/test_bug_2026_09_13_003_check_warehouse_freeze.py tests/test_inv_batch_001b_check_freeze_upsert.py -q` 9/9（换仓拒绝且原行不变、编码别名、空单换仓、原冻结回归）。生产需重启 WMS 服务生效。 |

| BUG-2026-09-13-002 | [P1] 关库位模式下售后出库、负向库存调整、正向调整反提交仍只校验全局总账；调拨完成未累计重复物料，反提交未校验调入仓余额，导致串仓和负库存 | **本地修复并验证，远程发布待核验（2026-09-13）**：`deduct_stock_atomic` 在物料写锁内按仓库检查余额，覆盖所有携带仓库的扣减消费点；保留全不可归属历史反提交兼容。调拨完成与反提交累计同物料数量并在写流水前拒绝超额。R6：复查 BUG-2026-08-16-008/009 与 08-18-002，同类点已排查，统一扣减入口同时覆盖委外发料等遗漏点。回归 `tests/test_bug_2026_09_13_002_warehouse_deductions.py` 10/10（三类扣减失败回滚及成功、历史编码流水、多行调拨双向扣减、总账=各仓合计）；库存相关扩大回归 372 passed/10 skipped/1 Windows 启动测试失败待基线核验；`scripts/verify_wms_bugs.py` 通过。审计旧夹具补齐真实库存，禁止用空仓成功反提交作为预期。生产需重启 WMS 服务生效；提交/推送结果以 Git 记录和交付报告为准。 |

| BUG-2026-09-13-001 | [P2·资源错配] **Waitress 线程数写死默认 16，与部署机实际核数脱钩**：`run_server.py` 原为 `threads = int(os.environ.get("WMS_THREADS", "16"))`——该值是 2026-08 为缓解生产日志 queue depth 12 由 8 上调而来，但**当时未与 CPU 核数挂钩**。生产实际为 2 vCPU / 2GB 腾讯云低配机（任务管理器实测内存常驻 70%）：Python GIL 下同进程纯计算无法多核并行，16 线程在 2 核机上**不增吞吐**，只徒增内存占用（每线程栈 + 每请求 Flask 上下文）与上下文切换开销，内存压力反过来加剧慢请求与 `waitress.queue` 排队 | **已修复（2026-09-13）**：新增 `_default_waitress_threads()`（**4 起步、每核 4、封顶 16**——2 核→8、4 核及以上→16 保持既有行为不变）与 `_resolve_waitress_threads(env)`（`WMS_THREADS` 显式设置且非空白时优先，空串回落自适应默认，非法值维持 int() 启动期快速失败）；`main()` 改用 `_resolve_waitress_threads(os.environ)`。R6：已 grep 台账无"线程数默认值"同类历史（8→16 注释为唯一相关记录，本次在其语义上扩展而非回退）；同类消费点已排查——`app.py`/`start_qa_server.py` 的 `threads=8`/`threads=4` 为 CI/QA 专用入口，不受影响。回归：`tests/test_bug_2026_09_13_001_adaptive_threads.py` 6/6（核数缩放 / 封顶 16 / cpu_count=None 兜底 / 环境变量优先 / 空串回落 / 反向断言源码不再含写死 "16"）+ 既有 `test_run_server_port_hint.py` 1/1。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**；需保留 16 线程的现场可设 `WMS_THREADS=16` 显式固定 |
| BUG-2026-09-10-011 | [P2] 手机端错误提示吞掉服务端原因：`WmsRepository` 有 **20 处** `catch (e: Exception) { Result.failure(Exception("网络错误: ${e.message}")) }`，而 `handleResponse` 本身已正确透传服务端 `msg`（如「请选择进行中的盘点单」「请选择仓库」）——外层 catch 把它覆盖成「网络错误: ...」，现场看到"网络错误"却反复重试（实为业务前置条件未满足，网络完全正常）；`getMaterialInfo` 的缓存兜底分支同样吞原因 | **已修复（2026-09-10）**：新增 `WmsRepository.BusinessException`（companion object 内，标记"服务端已给出明确原因"）+ 私有 `safeCall { }` 统一包装——先捕获 `BusinessException` 原样放行，仅兜底 `catch (Exception)` 才标注「网络错误」；`handleResponse` 三条失败路径（success 无 data / envelope 非 ok / HTTP 非 2xx）统一产出 `BusinessException`；**20 处调用点全部收口**（含 3 个 submit 级联 catch、日报逐页翻页、期初库存保存、物料缓存兜底）。R6：`grep 网络错误` 复查无存量未收口写法。回归：`tests/verify_bug_2026_09_10_011_repository_error_message.py` 6/6（BusinessException 位置 / safeCall 捕获顺序 / handleResponse 产出类型 / **无遗留直包写法** / 关键接口走 safeCall / 级联 catch 放行）。**生效条件**：Android 改动，需产出新 APK 后重装 |
| BUG-2026-09-12-008 | [P0·静默丢数据] **离线队列补传失败不回写状态，记录永久卡死 `SYNCING`（AI-MOB-OFFLINE-01 自身缺陷，且属 R6 复发）**：`OfflineQueueManager.doSync()` 中 `replay(op)` 返回 `false`（"未知作业类型"这类确定性失败）时走 `else { failCount++ }`，**未调用 `markFailure` 回写状态**；而该行此前已被 `dao.markSyncing()` 置为 `syncing`。致命点在于**三处消费点全部按状态过滤**——`listPending()` 只取 `status='pending'`（补传再也取不到它）、`countPending()` 只算 pending、`countFailed()` 只算 failed——于是这条作业在**补传队列、待同步角标、失败告警三个界面上同时消失，UI 完全无感知**，唯一出路是 App 进程重启触发 `resetStuckSyncing()`。讽刺之处：`PendingOperationDao.kt:55-56` 的注释白纸黑字写着「不复位就永远补传不出去——**静默丢数据，正是本任务要根治的问题**」，即该功能正是为消灭静默丢数据而建，却在错误分支里把同一问题重造了一遍。R6：已 grep 全仓 `markSyncing` 写入点（仅 1 处）、`STATUS_SYNCING` 引用点（仅实体/DAO/本类），确认同类消费点仅此一处 | **已修复（2026-09-12）**：`else` 分支补 `markFailure(op, "未知作业类型 \`${op.operationType}\`，无法补传", forceFail = true)` 并加 `Log.w`；因"未知类型"重试一万次也不会变，故 `forceFail=true` 直接落 `failed` 交人工处理（而非回 pending 空转）。同时把 `replay()` 的 KDoc 改为显式声明「**false 表示确定性失败，调用方必须回写状态，绝不可直接丢弃返回值**」，把契约钉在签名文档上，防止后续新增 `return false` 路径再犯。<br>**附带修正（同根因文案）**：`buildSummary()` 全失败分支原文案为「N 条同步失败，**请检查网络**后重试」——但失败原因可能是业务拒绝（盘点单已结束）或未知类型，与网络无关，会让作业员反复切网络却始终无效（与 BUG-2026-09-10-011「把非网络原因说成网络原因」同模式）。改为「N 条同步失败，已保留在本机待处理（详见失败列表）」。<br>**验证**：`tests/verify_bug_2026_09_12_008_offline_queue_state_machine.py` 10/10。其中 `test_008_syncing_not_terminal_in_behavior_model` 是**可执行状态机模型**——把 Kotlin 逻辑等价翻译为 Python 真实跑一遍"补传返回 false"路径，断言①无任何记录停留在 syncing ②未知类型记录落 failed 且能被 `countFailed` 统计 ③成功记录被删除；并内置**对照组**复现修复前行为（老实现下该行停在 syncing 且 `countPending()==0 && countFailed()==0`），证明缺陷真实存在而非臆测。`test_no_syncing_written_without_writeback_paths` 额外断言 `markSyncing` 全类仅 1 处调用且失败出口 ≥2 处，锁住"状态机出口完整性"。<br>**同类点已排查**：`markSyncing` 前的 `break`（中途断网）发生在置状态**之前**，当前条仍为 pending，不存在残留路径——已核对控制流顺序。**生效条件**：Android 改动，需产出新 APK 后重装；无需后端重启 |
| BUG-2026-09-12-009 | [P1·信任性缺陷] **`enqueue()` 非 suspend，落库前就返回 true，向用户谎报"已暂存"**：`OfflineQueueManager.enqueue()` 签名为非 suspend 的 `fun ... : Boolean`，内部 `scope.launch { dao.upsert(...) }` 后**立即 `return true`**；调用方 `WmsRepository.submitWithOfflineFallback` 拿到 `true` 即抛 `OfflineQueuedException`，UI 显示「网络不可用，XXX 已暂存，联网后自动提交」。但此刻写库**可能尚未发生、甚至可能失败**（磁盘满 / DB 损坏 / 序列化异常）——数据并不在本地，用户却被引导放心离开。此缺陷比 008 更隐蔽：**只在失败时反向暴露**（正常路径行为正确），属"善意 bug"（函数内确有仓库必填硬校验，看着严谨）。离线暂存这条链路的全部价值就建立在"暂存一定成功"上，返回值却与真实结果无关 | **已修复（2026-09-12）**：①`enqueue` 改为 `suspend fun`，直接 `return runCatching { dao.upsert(...); refreshCounts(); true }.getOrElse { Log.e(...); false }`——**落库完成才返回**，失败明确返回 false；②调用方 `submitWithOfflineFallback` 本就在 suspend 上下文，直接 await 无需改语法；③**同时修正失败分支文案**：原为 `Exception("网络错误: ${e.message}")`，统一把原因说成网络（同 BUG-2026-09-10-011 模式，实际可能是本地存储异常，会误导排查方向），改为 `Exception("提交失败且本地暂存未成功，请保持本页重试（原因：${e.message}）")`，明确告知"数据没保住"，让用户自己决定重扫或重试。<br>**验证**：`test_009_enqueue_is_suspend`（含**反向断言**：函数体内不得再出现 `scope.launch` 异步落库后立即 return 的旧写法）、`test_009_enqueue_failure_returns_false`（失败分支必须返回 false）、`test_009_repository_does_not_lie_when_queue_fails`（入队失败不得复用"已暂存"措辞）。**同类点已排查**：全仓 `grep "\.enqueue("` 仅 1 个调用点；`grep "return true"` 确认无其他 fire-and-forget 假成功。**生效条件**：Android 改动，需产出新 APK 后重装 |
| BUG-2026-09-12-010 | [P1·重复扣库存] **三个提交入口无 `isLoading` 守卫，弱网重复点按产生重复单据**：`ScanViewModel` 的 `submitInbound` / `submitOutbound` / `submitStocktake` **在置 `isLoading=true` 前后均无并发守卫**（`grep "if (_uiState.value.isLoading)"` 计数为 **0**），UI 三个确认按钮的 `enabled` 也不含 `isLoading`。当前仅靠 `onClick` 中先 `showSubmitDialog = false` 关掉弹窗、按钮随对话框消失来遮蔽——**纯时序侥幸**：一旦弹窗改为不关闭、或用户极速双击/手势穿透、或后续加"重试"按钮，即触发重复提交。而每次提交都会 `newRequestId()`（`UUID.randomUUID()`）生成**全新幂等键**，后端 `mobile_api_idempotent` 只能对**同一** key 去重，**无法识别两次不同 key 的相同业务**→ 两张单据、库存扣两次。这是"幂等键只防重试不防重复提交"的语义盲区 | **已修复（2026-09-12）**：**双层防护**——①ViewModel 三个入口各加 `if (_uiState.value.isLoading) return@launch`，置于 `val state = _uiState.value` 之前（源码守卫，与 UI 无关）；②UI 三个确认按钮 `enabled` 追加 `!uiState.isLoading`。盘点按钮因原已带「仓库+盘点单必选」前置校验，改为**与运算追加**而非覆盖（`enabled = selectedWarehouse != null && selectedCheckOrder != null && !isLoading`），保住原有校验不被削弱。<br>**验证**：`test_010_all_submits_have_isloading_guard`（三个方法都有守卫，且断言**守卫位置必须在置 `isLoading=true` 之前**，否则会自己挡住自己）、`test_010_ui_buttons_disabled_while_loading`（三个按钮的 `enabled` 表达式块均含 `!uiState.isLoading`，并断言盘点按钮的两项前置校验仍在）。<br>**回归影响（已修）**：本次改动打坏了既有测试 `tests/verify_android_stocktake_check_order.py::test_screen_ui_and_confirm_gate`——该测试断言按钮 `enabled` 为**单行字面量** `"uiState.selectedWarehouse != null && uiState.selectedCheckOrder != null"`，被多行写法打破。已将该断言改为**语义断言**（从 `viewModel.submitStocktake()` 调用点定位按钮块，断言块内含盘点单校验 + isLoading 守卫），既保留原意又不再依赖格式；该文件 6/6 通过。**生效条件**：Android 改动，需产出新 APK 后重装 |
| BUG-2026-09-12-011 | [P1·承诺未兑现] **离线队列靠扫码页懒加载初始化，"联网自动补传"对部分用户路径完全不生效**：`WmsApplication.onCreate()` **没有任何离线队列预热**（`grep "syncPending\|offlineQueue"` 计数为 **0**），而 `WmsRepository.offlineQueue` 是 `by lazy`，其 `init` 块承载两件必须发生的事——① `dao.resetStuckSyncing()` 复位进程被杀残留的 syncing 记录；② 注册 `networkMonitor.onlineChanges` 网络恢复监听。首次访问发生在 `ScanViewModel`（扫码页渲染时才创建），**因此：用户断网入库→提交成功暂存→此后再未打开扫码页→网络恢复→队列既不复位也不补传，什么都不会发生**。数据其实在库里，但产品对用户承诺的"联网后自动提交"落空；若恰有 syncing 残留，还会叠加 BUG-2026-09-12-008 的卡死。根因是**初始化时机绑定在页面生命周期上，而职责属于进程级**——队列是全局基础设施，不该由某个页面的打开与否来决定其是否启动 | **已修复（2026-09-12）**：`WmsApplication` 新增进程级 `appScope = CoroutineScope(SupervisorJob() + Dispatchers.IO)` 与 `repository: WmsRepository by lazy { WmsRepository(this) }`，`onCreate()` 末尾调用 `warmUpOfflineQueue()` —— 在后台协程里 `runCatching { repository.offlineQueue }` 触发实例化，从而让 `init` 的复位与网络监听与用户操作路径**无关**地生效。要点：①**必须后台执行**，不得阻塞主线程（`onCreate` 是关键路径）；②`SupervisorJob` 隔离失败，预热异常不得影响启动或崩溃；③懒加载 `repository` 避免给启动增加无谓开销，其内部 DB/Retrofit 均为单例，多建实例不产生重复资源；④`getInstance` 的 `synchronized` 双检锁保证幂等，重复调用无副作用。顺带把文件内日志 TAG 由字面量 `"WmsApp"` 收敛为 `private companion object` 常量（原 2 处硬编码）。<br>**验证**：`test_011_application_warms_up_offline_queue`（断言 `onCreate` 确实调用预热、预热体走 `appScope.launch` 不阻塞主线程、引用了 `offlineQueue`）。**生效条件**：Android 改动，需产出新 APK 后重装；无需后端重启 |
| BUG-2026-09-12-006 | [P0·CI 阻断] **AI-MOB-OFFLINE-01 提交后 Android CI 全红（`./gradlew assembleRelease` 编译失败），两处 Kotlin 编译错误**——用户先发现「修不了 BUG 反而增加 BUG」：`Android APK Build` 工作流自 `#424` 起每次都在 `Build Release APK (R8 瘦身)` 步骤失败（`#423` 为最后一个绿版，之间恰为本次 5 个提交）。根因两处，**均非逻辑错误，而是 Kotlin 类型可见性/初始化顺序写错**：<br>①**`DatabaseMigrations.kt` 属性初始化顺序**：`object DatabaseMigrations` 内 `val ALL: Array<Migration> = arrayOf(MIGRATION_1_2)` 写在 `private val MIGRATION_1_2` **之前**。Kotlin `object` 属性初始化严格按源码顺序执行，初始化 `ALL` 时 `MIGRATION_1_2` 尚未赋值，编译器直接报 `variable 'MIGRATION_1_2' must be initialized`（硬编译错误，非警告）。<br>②**companion 嵌套类跨文件引用不可解析**：`OfflineQueueManager.kt` 与 `ScanViewModel.kt` 分别写 `WmsRepository.BusinessException`（2 处）与 `WmsRepository.OfflineQueuedException`（3 处）。这两个类定义在 `WmsRepository` 的 `companion object` 内，**Kotlin 不支持 `Outer.NestedClass` 这种跨文件点号访问 companion 的嵌套类**，必须 `import ...WmsRepository.Companion.X` 后直接用短名。此前基线中 `BusinessException` 只在 `WmsRepository.kt` 同文件内使用（同文件内 `BusinessException` 短名可见），所以从未暴露；`OfflineQueueManager.kt` 是**仓库内首个跨文件引用该嵌套类的文件**，首次触发。R6：同根因在 2 个文件、5 个引用点同时存在，一并修复 | **已修复（2026-09-12）**：①`ALL` 移至 `MIGRATION_1_2` 之后，并补注释说明「新增迁移须追加到 `ALL` 之前、切勿上移该行」；②两文件改为 `import com.factory.wms.data.repository.WmsRepository.Companion.BusinessException` / `...Companion.OfflineQueuedException`，5 处引用改短名（KDoc 里的全限定写法保留，用于指明确切来源）；③顺带修正 `OfflineQueueManager.kt` 的 `ApiEnvelope` import——原写 `data.api.ApiEnvelope`（该包无此类）实为 `data.model.ApiEnvelope`，同样导致 `unresolved reference`。<br>**验证方式（可复现）**：沙箱内自建真实 Kotlin 工具链——kotlinc 1.9.24 + **真实 Android SDK platform-35 `android.jar`**（`dl.google.com` 经 `--resolve 220.181.174.97` 可达）+ Room/Retrofit/OkHttp/Gson/coroutines/DataStore/Security/lifecycle 的**真实 AAR/JAR**。对照编译 `#423`（绿版基线与 HEAD，逐条 diff 错误集合：基线 6303 条、修复后 HEAD 6347 条，**「相比基线多出的错误」已归零**（唯一残留 1 条 `ScanViewModel collect` 经证实为隔离编译缺失 AndroidX 的共模噪声——基线在 `VoiceAssistant.kt`/`NavGraph.kt`/`AuthViewModel.kt` 同类位置已有 3 条同款）。核心文件**零错误产出 182 个 class**；`DatabaseMigrations.kt` 修复前复现 `must be initialized`、修复后消失，形成「改前复现→改后消失」闭环证据。<br>回归：全量 Python **1535 passed、85 skipped、6 failed**（6 项为条码/Excel 打印，`git stash` 比对确认改动前即失败，系沙箱缺条码渲染依赖）；lint 双门禁 **0 违规**。**生效条件**：Android 改动，CI 出包后重装；**无需后端重启**。**教训（写入规则）**：Android/Kotlin 改动**不能只靠「语法检查」或「单文件编译」判定通过**——本次两处均为语法合法、单文件看不出、仅全项目编译才暴露的错误。后续 Kotlin 改动须以 CI `assembleRelease` 结果为唯一验收依据，未确认 CI 转绿不得声称完成 |
| BUG-2026-09-12-003 | [P1] 手机端查库存列表模式看不到库位、品牌（BUG-2026-09-10-003 的遗留未覆盖面，专家走查+实测坐实）：`/api/mobile/stock/query` 响应字典只下发 id/code/name/spec/unit/category/supplier/stock/price/min_stock/reorder_point，**漏下发 `brand` 与 `locations`**；而扫码路径（`mobile_material_payload`）两者均下发。根因是时间线错位——列表模式 `AI-MOB-STOCK-F01` 于 **2026-08-09** 引入，而补 `locations` 的 `BUG-2026-09-10-003` 于 **2026-09-10** 才修，**当时只覆盖了扫码路径**（其测试甚至明确断言"所有消费点都带 locations"），列表接口被漏掉。后果：同一物料、同一仓库、库位数据真实存在时，**扫码能看到「货在货架A-03 50 件」，列表里看不到**——作业员必须逐个点进去扫才能知道货在哪，而"查库存看到总量却不知道在哪、还得挨个扫"正是列表模式本该解决的问题；`brand` 则是 `Material` 表真实存在的列、`StockListRow` 已渲染但数据永远为空 | **已修复（2026-09-12，commit 见下）**：①`mobile_api_stock_query` 响应补齐 `brand`（`m.brand or ''`）与 `locations`；②**复用** `build_material_locations_map`（BUG-2026-09-10-004 为消除 N+1 引入的批量预取函数）而非逐条查询，实测 50 条物料逐条查 = **50 条 SQL**、批量 IN = **1 条**（快 5.8x），并继承其与 `/api/material/search`、`/api/material/all` 完全一致的口径（非零、数量降序、库位升序、IN 分批 500），避免同根因在多消费点分叉（R6）；③为该函数新增可选参数 `legacy_location_names`——传入时额外纳入 `warehouse_id IS NULL` 且 `location` 等于本仓名/编码的历史行，**不传则保持原精确匹配行为**（search/all 两处调用点零改动）。做此扩展的原因经实测：不兼容历史行会出现「`get_warehouse_stock_quantities` 汇总 70 但库位明细只列 50」的不一致，违反 **R2 第 3 条（汇总=明细）**。回归：`tests/test_bug_2026_09_12_003_android_stock_query_locations.py` 8/8（brand+locations 下发 / 零库存库位不列 / **R2-1 多仓隔离 A 仓不串 B 仓** / **R2-2 历史 NULL warehouse_id 兼容** / 关库位管理为空 / **防 N+1 退化**（本页 20 条物料 location_inventory SQL 为常量级）/ 第 2 页同样带字段 / **R2-3 汇总=明细** + 共用函数向后兼容）；既有 `test_bug_2026_09_10_004_material_search_n_plus_one.py` 6/6 与 `09_10_002`/`09_10_003`/`08_16_007` 共 14 项零回归；全量 **1530 passed、85 skipped**（另 6 项条码/Excel 打印失败经 `git stash` 比对确认**改动前即失败**，系沙箱缺条码渲染依赖，与本次无关）；lint 双门禁 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**（R3）；Android 侧 `MaterialDto` 早有 `brand`/`locations` 字段与渲染逻辑，**无需改代码、无需重装 APK 即可显示库位分布** |
| BUG-2026-09-10-010 | [P1] 手机首页概览只统计系统默认仓（与 009 日报同根因，用户走查发现）：`/api/mobile/dashboard` 不带仓库参数、服务端一律回退 `get_default_warehouse()`，首页四张卡（今日入库量/出库量/待处理/库存告警）**只反映默认仓**；4 仓以上用户在非默认仓作业时首页数字明显偏小甚至为 0，与日报跨仓盲区并存 | **已修复（2026-09-10）**：①后端支持 `warehouse_id / warehouse_code / warehouse` 指定仓库，显式 `all` = 全部仓库汇总（口径与 009 日报严格一致，缺省仍回退默认仓、非法仓仍 400）；告警在 all 模式下**按物料去重**（任一仓低于最低库存计一次，不被仓库数放大）；响应新增 `warehouse` / `warehouse_id` / `all_warehouses`；②Android `getDashboard(warehouseId)` + `HomeViewModel`（warehouses / selectedWarehouseId / loadWarehouses / selectWarehouse）+ 首页概览条标题行加仓库切换；③R6 同类收敛：把日报页的 `WarehouseSelector` **提取为共享组件** `ui/components/WarehouseSelector.kt`，报表页改用共享组件，消除两份实现漂移风险。回归：`tests/test_bug_2026_09_10_010_dashboard_warehouse_scope.py` 9/9（指定仓隔离 / **全部合计 = 各仓之和 R2** / 跨仓待处理累加 / 缺省回退默认仓 / 响应标记 / 非法仓仍 400 / **告警去重** / 数量合计 / 昨日不串今日） + `tests/verify_bug_2026_09_10_010_home_warehouse_selector.py` 7/7（API 参数 / Repository 透传 / ViewModel 状态 / 页面接入 / 共享组件与报表页复用 / DTO 字段 / 进入即加载仓库）。**生效条件**：后端改动拉取后**重启 WMS 服务生效**（R3）；App 下拉需产出新 APK 后重装 |
| BUG-2026-09-10-009 | [P1] 手机每日报表固定只查系统默认仓，多仓用户「查不到今天的记录」：手机报表页请求 `/api/mobile/report/daily_detail` 不带任何仓库参数，服务端 `resolve_request_warehouse` 一律回退 `get_default_warehouse()`，录在其他仓的单据**一条都进不了日报**（首页今日入库/出库同源同口径，同样只算默认仓）。用户在用仓库 4 个以上，当天单据录在非默认仓即表现为「今天查不到」 | **已修复（2026-09-10，commit 本次提交）**：①后端支持 `warehouse_id / warehouse_code / warehouse` 指定仓库；显式传 `all` = 全部仓库汇总（用户主动跨仓，不是「未指定仓库」，不违反 AGENTS.md 仓库必填；缺省仍回退默认仓、非法仓仍 400）；明细行新增 `warehouse` 字段，响应新增 `warehouse_id` / `all_warehouses`，诊断统计（pending_orders / other_type_orders）同步跨仓；②Android 顶部新增 `WarehouseSelector`（默认仓库 / 各仓 / 全部仓库汇总），进入页面先拉仓库列表再查报表，`dailyReportDetail` 与逐页翻页均携带 warehouse_id（R1），全部仓库模式明细行显示来源仓，`DailyReportItem.warehouse` 可空兼容旧版后端。回归：`tests/test_bug_2026_09_10_009_daily_report_warehouse_scope.py` 7/7（指定仓隔离 / **全部合计 = 各仓之和 R2** / all 模式明细带仓名 / 缺省仍回退默认仓 / 响应标记 / 非法仓仍 400 / 跨仓 pending 累加） + `tests/verify_bug_2026_09_10_009_report_warehouse_selector.py` 6/6（API 参数 / Repository 逐页透传 / ViewModel 状态 / 选择器含全部仓与默认仓 / 字段可空 / 加载顺序）。**生效条件**：后端拉取后**重启 WMS 服务生效**（R3），接口侧可先手工用 `&warehouse_id=N` 自查；App 下拉需 GitHub Actions 产出新 APK 后重装 |
| BUG-2026-09-10-008 | [P2] 手机每日报表空态不说明「为什么查不到」：报表只统计「已完成」单据，而 PC 端入库/出库保存后默认 `pending`（需人工点完成），现场只能看到一句「当日暂无领料单明细」，无法区分「没单 / 单没完成 / 业务类型不对 / 查错仓库」，只能反复反馈「今天的记录查不到」 | **已修复（2026-09-10，commit 本次提交）**：`ReportModels.DailyReportData` 新增可空 `warehouse / serverToday / diagnostics`（`DailyReportDiagnostics`：business_types / pending_orders / other_type_orders，全部可空——旧版后端无该节点时 Gson 置 null），`ReportScreens` 空态文案由固定句改为 `emptyStateHint()`：①待完成单据数>0 → 提示「今日该仓还有 N 张单据未完成（电脑端录入后需点『完成』才会计入报表）」；②否则有其他类型已完成单 → 列出「产品入库 3 单」等；③否则回退基础文案；日期条在日期下方显示实际查询的仓库。回归：`tests/verify_bug_2026_09_10_008_report_empty_hint.py` 6/6 PASSED（模型字段齐全且可空 / diagnostics 模型存在 / 空态走动态函数 / **hint 对 diagnostics 与 pendingOrders 判空**（BUG-2026-08-24-007 同模式：Gson 绕过构造器默认值）/ 日期条显示仓库 / 两类原因均覆盖）。**生效条件**：Android 改动，需 GitHub Actions 产出新 APK 后重装；不重装时旧版 App 只是拿不到诊断字段，空态仍显示原文案（后端已兼容） |
| BUG-2026-09-12-005 | [P1] PC/移动端物料搜索带仓库时「库存总数」与「库位分布」口径分裂（BUG-2026-09-12-003 修复过程中同根因排查发现，实测坐实）：`/api/material/search?warehouse=X` 的响应里，`stock` 走 `get_warehouse_stock_quantities`（**含**历史 NULL warehouse_id 兼容），而 `locations` 走 `build_material_locations_map`（默认**仅** `warehouse_id` 精确匹配）——同一响应出现 **`stock=70` 但库位明细只列 `50`**（实测复现）。后果：作业员看到库存 70 件、按库位分布去货架找货，**有 20 件永远找不到**（货在历史行上）。R6：与 BUG-2026-09-12-003 同根因（口径在多个消费点分叉），属该函数被复用时未同步兼容逻辑所致 | **已修复（2026-09-12，commit 见下）**：`material_search_api` 在带仓库上下文时构造 `legacy_location_names = [wh.name] + [wh.code]`（名称与编码，去重）传入 `build_material_locations_map`，使其与 `get_warehouse_stock_quantities` 严格同口径；无仓上下文时保持 `None`（全局视图不按仓过滤，行为不变）。**同时核对 `/api/material/all`：该接口 `wh_obj=None`、本就是全局口径（stock 走 `Material.stock`、locations 不过滤），不存在分叉，无需改动**——避免了盲目套用修复。回归：`tests/test_bug_2026_09_12_005_material_search_location_consistency.py` 5/5（**带仓搜索 stock == locations 明细之和 R2-3** / 历史行按仓库**编码**写入时同样纳入 / **R2-1 B 仓历史行不串入 A 仓** / 无仓上下文行为不变 / 防 N+1 退化）；既有 `09-10-004`/`09-12-003`/`09-10-003`/`09-10-002` 共 23 项零回归；lint 双门禁 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**（R3）；无数据库迁移 |
| BUG-2026-09-10-007 | [P2] 手机每日报表日期跨天不刷新：`ReportViewModel` 在 App 启动（AppNavGraph 组合）时创建，`date` 只在 `init` 取一次 `Date()`；Android 进程常在后台存活数日，用户隔天打开报表页仍按启动日查询，顶部日期显示的也是旧日期——表现为「今天的记录查不到」（点「回到今天」可临时绕过） | **已修复（2026-09-10，commit 本次提交）**：`ReportUiState` 新增 `dateIsToday`（默认 true）；`load()` 在今天模式下先把 `date` 校正为系统当天再查询（进入报表页时触发，ViewModel 复用也不过期）；`shiftDay()` 手动翻天置 `dateIsToday=false`（避免用户翻到的日期被自动校正覆盖），`resetToday()` 恢复 true。回归：`tests/verify_bug_2026_09_10_007_report_date_refresh.py` 4/4 PASSED（UiState 有标记 / load 校正 / 翻天脱离 / 回到今天恢复）。**生效条件**：Android 改动，需产出新 APK 后重装 |
| BUG-2026-09-10-006 | [P1] 手机原生端出库业务类型与 PC 领料单不是同一个：`POST /api/outbound` 固定写 `business_type='Android扫码出库'`，而 PC 领料单是 `'领料单'`——后果①每日报表 requisition 严格等值 `'领料单'`，**手机扫的库在日报里一条都不出现**（首页「今日出库」却显示有数，因为 dashboard 不区分业务类型）；②PC 领料单列表默认口径 `business_type == '领料单' or is None`，**手机出的库在 PC 上也看不到**。R6：与 09-10-005 同根因（业务类型口径分裂），两处一并修 | **已修复（2026-09-10，commit 本次提交）**：`/api/outbound` 出库统一写 `business_type='领料单'`（手机端出库即仓库领料），来源仍由 `purpose`（领用部门/「Android原生端提交」）与 `remark` 区分；`ScanRequests.OutboundRequest` 默认 `business_type` 同步为「领料单」（契约一致）。R6 同类点排查：全仓 `grep 'Android扫码出库'` 仅此两处生产点，历史单据由 09-10-005 的报表口径兼容命中，无需数据迁移。回归：`tests/test_bug_2026_09_10_006_outbound_business_type.py` 4/4 PASSED（出库写领料单 / **当日即出现在日报** / 出现在 PC 领料列表口径 / Android 契约一致）；既有 `test_mobile_outbound_contract.py`、`test_print_auto_after_scan.py` 零回归。**生效条件**：后端改动，拉取后**重启 WMS 服务生效**（R3） |
| BUG-2026-09-10-005 | [P1] 手机每日报表「查不到今天的记录」，且无任何可诊断线索：`/api/mobile/report/daily_detail` 的 requisition 严格等值 `business_type='领料单'`，而手机原生端出库写的是 `'Android扫码出库'` → 手机出的库**永远进不了日报**；同时报表只统计 `status='completed'`，PC 端入库/出库保存后默认 `pending`（需人工点完成）→ 当天录的单查不到；响应里既没有仓库名也没有待完成数，空结果无法区分原因。用户实测反馈「手机 app 每日报表查不到今天的记录」（09-10） | **已修复（2026-09-10，commit 本次提交）**：①requisition 口径改为业务类型集合 `('领料单', 'Android扫码出库')` + 空类型兜底（`business_type IS NULL` 按领料计，与 PC 领料单列表 `out_order_list` 口径一致），销售出库/其他出库仍排除（口径不扩大）；②响应新增 `warehouse`（实际查询仓库）、`server_today`、`diagnostics{business_types, pending_orders, other_type_orders}`——待完成单据数与「当天该仓已完成但类型不在本口径内」的分布，供手机端空态给出明确原因。回归：`tests/test_bug_2026_09_10_005_daily_report_scope.py` 8/8 PASSED（手机扫码出库进日报 / 空类型按领料 / 销售·其他出库排除 / diagnostics 字段 / pending 只报不进明细 / 其他类型计数 / **R2 多仓隔离**含显式 warehouse_id 切仓 / 昨日不串今日）；既有 `test_mobile_report_daily_detail.py` 12/12 零回归。**生效条件**：后端改动，拉取后**重启 WMS 服务生效**（R3）；无数据库迁移 |
| BUG-2026-09-10-004 | [P2] 手机端查库存带仓库搜索 N+1 性能缺陷（09-10-002 遗留、专家走查确认）：`/api/material/search` 带 warehouse 时 `api_material_payload` **逐物料**调 `get_warehouse_stock_quantities`（每次都是全仓聚合查询）+ 逐物料查 `LocationInventory` 库位分布，命中上限 100 条时高达 200+ 次查询，手机端搜索明显卡顿；`/api/material/all` 库位分布同样逐物料查询（1000 条上限即 1000 次） | **已修复（2026-09-10）**：`api_material_payload` 签名扩展 `warehouse_stock_map`/`locations_map` 预取参数（不传保持原逐物料行为，单物料 `/api/material/info` 不变）；新增 `build_material_locations_map`（一次 IN 分组查询替代 N 次单查，IN 分批 500 兼容变量受限数据库，过滤/排序口径与逐物料路径严格一致——非零、数量降序、库位升序、带仓按 warehouse_id 过滤 R2）；search 接口固定为 1 次全仓聚合 + 1 次库位批量查询（与命中数无关），all 接口库位分布同步批量化。回归：`tests/test_bug_2026_09_10_004_material_search_n_plus_one.py` 6/6（带仓搜索聚合固定 1 次/库位查询固定 2 次且命中数变化不变/all 接口库位批量化/批量路径与单物料 info 路径 stock+locations+min_stock 等字段严格一致/无仓上下文 0 次聚合+全局口径不变/预取函数空入参与分批边界）；存量 09-10-002/003 物料 API 15 项全过；全量 **1436 passed、84 skipped、0 failed**；lint 双门禁 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**（Android 无需改代码、无需重装 APK）。**遗留说明**：手机端查库存离线容忍（Room 缓存陈旧度标注）属架构级特性另行任务评估 |
| BUG-2026-09-10-003 | [P1] 手机端查库存两个误导性缺陷（专家走查实测）：①结果卡「库存充足/不足」徽标按 `stock > minStock` 判断，但 `api_material_payload` **不下发 min_stock**——DTO 字段恒 null→0，退化为"大于 0 即充足"，现场按徽标拣货会误判（结果卡"最低库存/再订货点"两个 InfoChip 同理恒显 0）；②现场找货第二高频问题「货在哪个库位」无答案——payload 仅在开启库位管理时返回数量最多的单个 location_code，无各库位数量分布 | **已修复（2026-09-10）**：①`api_material_payload` 新增 `min_stock`/`reorder_point` 真实下发（Material 模型早有字段，纯新增向后兼容，Android 徽标与 InfoChip 无需改代码即变真值）；②payload 新增 `locations` 数组——开启库位管理时返回该物料全部非零库位及数量（按数量降序、有仓库上下文按 warehouse_id 过滤与该仓账面口径一致 R2、未开启为空列表），`location_code` 保持"数量最多库位"原语义不变；Android `MaterialDto` 增加 `locations`/`MaterialLocationDto`（默认 null 保持 Entity 映射构造兼容），查库存结果卡新增「库位分布」区块逐行展示库位+数量。回归：`tests/test_bug_2026_09_10_003_material_payload_min_stock_locations.py` 5/5（min_stock/reorder_point 真值下发/库位分布非零+降序+location_code 原语义/带仓上下文 B 仓 999 不串入 R2/关库位管理为空/search 同步携带）+ `tests/test_bug_2026_09_10_003_android_stock_locations_display.py` 3/3（DTO 字段与默认兼容/徽标按 minStock 比较+InfoChip 锚点/库位分布区块锚点）；存量物料 API/位置相关 161 项零回归；Kotlin 括号平衡校验通过；lint 双门禁 0 违规。**生效条件**：后端改动拉取后**重启 WMS 服务生效**（徽标与 InfoChip 立即变真值）；「库位分布」展示需 GitHub Actions 产出新 APK 后用户重装。**说明**：price 字段是否随手机端下发按角色裁剪（专家走查 P3 项）与带仓搜索 N+1 性能（09-10-002 遗留）均另行任务 |
| BUG-2026-09-10-002 | [P1] Android 查库存输「电线」点查询只弹「物料不存在」，且模糊候选只显示前 8 条、名称/规格/品牌挤一行省略号截断（用户实测要求：候选列出包含关键词的所有物料、名称规格品牌显示齐全）。三层原因：①查询按钮/键盘搜索键走 `/api/material/info` 精确编码查询，非编码关键词必报「物料不存在」（后端 `/api/material/search` 早已支持模糊，按钮路径没接上）；②`ScanScreens.kt` 候选 `take(8)` 截断；③候选项 subtitle 单行 `maxLines=1 + Ellipsis`，长名称/规格/品牌被截去 | **已修复（2026-09-10）**：①`ScanViewModel.kt` 新增 `queryMaterialByKeyword`——先 `getMaterialInfo` 精确查编码，未命中回退 `searchMaterial` 模糊列表（全部命中写入 materialSuggestions 交页面展示；模糊也无命中不写 error，页面空态提示「未找到包含「kw」的物料」，避免 toast+空态双提示；不触碰 scannedCode 防 LaunchedEffect 重复查询），放大镜按钮与新增键盘 IME Search 键统一走该入口；②`ScanScreens.kt` 候选去掉 `take(8)`，列出全部命中（`heightIn(max=360.dp)` + `verticalScroll` 内部滚动）；③候选项改三行完整展示（编码加粗 / 名称整行 / 「规格: x 品牌: y」行），候选区块内无 `maxLines=1`/Ellipsis 截断；④后端 `/api/material/search` 配套 R1 显式化——上限魔法数 100 提为常量 `MATERIAL_SEARCH_API_LIMIT`，响应新增 `total`/`truncated`（命中超上限时调用方可知"全量 N 条、本次前 100 条"，字段新增向后兼容，Android 仅读 data 不受影响）。回归：`tests/test_bug_2026_09_10_002_material_search_total.py` 4/4（名称/规格/品牌三处模糊命中全返回且字段齐全/超上限 total=真实全量+truncated=True/无关键词 total 正确/info 精确未命中契约不变）+ `tests/test_bug_2026_09_10_002_android_stock_query_display.py` 4/4（按钮与 IME 走回退入口/无 take(N) 截断+可滚动/候选项名称规格品牌齐全无省略号/空态提示锚点）；存量 09-03-004 物料 API 21 项、search 相关 36 项全过；Kotlin 编辑区括号平衡校验通过；`scripts/lint_wms_rules.py --staged` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：后端改动拉取后**重启 WMS 服务生效**；Android 侧 GitHub Actions（android-build.yml）产出新 APK 后用户手机重新下载安装生效。**遗留说明**：带 warehouse 搜索时 `api_material_payload` 逐物料调 `get_warehouse_stock_quantities`（N+1 聚合，100 条≈100 次仓库级汇总查询），属性能项非本次显示缺陷根因，另行任务优化 |
| BUG-2026-09-10-001 | [P1] 反提交+删除草稿后库存台账仍显示已删除单据（用户实测领料单 out_order-111/out_order-169 残留）：BUG-2026-08-17-006 只修了采购入库单删除清流水，**R6 同类消费点排查不全复发**——领料单/工单领料单/调拨单/库存调整单/售后出库单/委外发料单/委外收货单 7 类单据的删除与批量删除路由均不写 `StockTransaction` 清理，反提交产生的反向流水（revert_out/revert_requisition/transfer 反向/revert_adjustment/revert_after_sale_out/revert_issue/revert_receive）与完成流水一起成为指向已删除单据的悬挂引用，台账 `_collect_ledger_rows` 全量直读流水故照常显示 | **已修复（2026-09-10，R6 全消费点一次补齐）**：6 个路由文件 12 处删除端点（`out_order.py`/`requisition.py`/`transfer.py`/`adjustment.py`/`after_sale_out.py`/`subcontract.py` 各自的单删+批删）在 `_acquire_order_write_lock` 写锁内同步清理 `reference_type`+`reference_id` 匹配的 StockTransaction，与 `delete_in_order` 对齐；历史悬挂流水用 `scripts/cleanup_orphan_document_transactions.py`（默认预演、`--confirm-delete` 执行、删除时按类型回连主表二次确认防误删重建单据，含历史 `reference_type='check'` 兜底）。**R6 同类点全量排查结论**：`in_order` 已由 17-006 覆盖；`opening_stock` 流水由 delete_material 引用完整性守卫拦截（有流水物料禁删）；现版本盘点经调整单写流水（随 adjustment 覆盖），历史 'check' 流水由清理脚本兜底；台账查询层不加读时过滤（8 张单据表存在性回连代价高，删除点清理+脚本与 17-006 先例一致）。回归：`tests/test_bug_2026_09_10_001_delete_document_ledger_cleanup.py` 16/16（7 类单据单删+批删清流水/反提交→删除全流程/不误删他单流水/无流水草稿空操作）+ `tests/test_cleanup_orphan_document_transactions.py` 3/3（预演不写库/确认删除/重建单据不误删）；存量 17-006 回归 3/3、删除/反提交相关 89 项全过；全量 **1411 passed、84 skipped、0 failed**；`scripts/lint_wms_rules.py --staged` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**；存量台账中的历史悬挂流水需手工执行清理脚本（先预演确认，再 `--confirm-delete`） |
| BUG-2026-09-06-005 | [P1] 启用库位管理时盘点差异调整明细缺少库位，且同物料跨多个区域合并时无法可靠归属 | **已修复（2026-09-06）**：差异调整写入盘点区域作为库位；缺失区域或多区域合并时阻止生成草稿，避免错误记账。回归：`tests/test_bug_2026_09_06_004_check_copy_and_location.py`。**生效条件**：后端改动，拉取后重启 WMS |
| BUG-2026-09-06-006 | [P1] Android 盘点提交未携带库位/区域，启用库位管理且有差异时无法生成可过账调整单 | **已修复（2026-09-06）**：Android 盘点页增加盘点库位/区域输入，按行提交 area；后端兼容 location/location_code，并在库位管理开启且产生差异时强制校验。相同物料可按不同库位分别记录。回归：`tests/test_bug_2026_09_06_006_android_stocktake_location.py`。**生效条件**：后端重启并重新构建发布 Android APK |
| BUG-2026-09-06-004 | [P1] 复制盘点单丢失仓库和区域语义，并复制旧实盘/差异结果，形成假已盘数据 | **已修复（2026-09-06）**：复制时保留并校验仓库，按当前仓库账面重新冻结；实际库存重置为账面、差异清零、盘点归属不复制，保留区域。回归：`tests/test_bug_2026_09_06_004_check_copy_and_location.py`。**生效条件**：后端改动，拉取后重启 WMS |

| BUG-2026-09-15-007 | [P1] 期初库存无法分单、导入单据不能改日期、首/上/下/末 功能缺失（用户原话：「期初库存单据，我要做几张怎么新增，因为有很多仓库很多物料很多仓管人员，导入的期初库存单据怎么修改日期，首 上下末功能要有」）。根因是数据模型不支持：`opening_stock` 是一张平铺台账，唯一约束 `uix_opening_stock_material_warehouse` 钉死在 `(material_id, warehouse_id)` 上，同物料同仓只能存在一行，"按仓库/物料/建账人分几张单"在模型层就做不到；没有单据头、没有单号，所以既无法定位"某一张单"来改日期，`DOCUMENT_NAVIGATION_MODULES` 里也无从注册导航。Excel 导入把每行直接 upsert 进台账，用户导入完看不到"这是哪一批"，更无从改日期。改造前 4 个导航键是硬编码 disabled 的死按钮（BUG-2026-09-15-005 曾因"无可导航单据"把它们删除） | **已修复（2026-09-15，ARCH-OS-DOC-01）**：引入单据头 `opening_stock_doc`（`doc_no` QS+YYMM+4位、`date` 建账日期、`warehouse_id` 可空、`status`、`remark`、`operator_id`），`opening_stock` 加 `doc_id` 外键，唯一键收窄为 `(material_id, warehouse_id, doc_id)`——**单内不重复、跨单据可重复**，同物料同仓多张单成为合法状态。期初库存口径随之定义为「某 (物料,仓库) 在所有单据中明细行数量之和」（见 `INVENTORY_TRUTH.md` §2.1.2）。新增单据级 CRUD：`POST /opening_stock/save`（无 doc_id 取号建单；有则改单头+逐行 upsert+差集删行并回冲）、`GET /opening_stock/add`、`GET /opening_stock/<id>`（单据详情页）、`POST /opening_stock/<id>/delete`（物理删除+全额回冲）、`POST /opening_stock/<id>/date`（改期同步该单全部明细行、不动账）、`POST /opening_stock/<id>/line/<line_id>/delete`；原 JSON 明细接口迁至 `/opening_stock/line/<id>`（endpoint 名不变）。Excel 导入整批归入一张新单据并回传 `doc_id`/`doc_no`/`detail_url`（行匹配加 `doc_id` 过滤，防跨单误改；全错行不留空单）；二次导入生成新单而非覆盖。前端注册 `opening_stock` 进 `WMS_ACTION_MODULES`（`navigator: true`），工具栏 4 个"首/上/下/末张"由死按钮改为真实跳转（`/api/document_navigation/opening_stock`），详情页注入 `window.__OPENING_DOC__` 回填明细，保存/粘贴导入统一走 `/opening_stock/save` 并带 `doc_id`（原先打 batch_save 会永远新建兼容单，编辑已有单据完全失效）。**开发中修复的两个真实缺陷**：①`_apply_opening_stock_balance` 的 `doc_id` 曾由调用方对返回值赋值，而新建明细行时函数内部创建对象、调用方变量仍为 None，赋值静默丢失 → 明细行不归属任何单据、导航永远找不到它（表面"保存成功"数据是错的），改为 `doc_id` 参数传入；②`/opening_stock/<id>/date` 500（`name 'datetime' is not defined`，延迟导入漏项）。**R6 兜底**：`doc_id` 起初只写在 `auto_migrate_database()`，生产批处理默认 `WMS_NO_DB_TOUCH=1` 会整体跳过迁移，存量库重启后一查即 500（`no such column: opening_stock.doc_id`）——新增 `ensure_opening_stock_doc_columns()` 无条件幂等补列并在启动期注册，使 `test_r6_startup_migration_column_guard` 保持 0 缺口（补兜底而非登记欠账）。迁移在 `auto_migrate_database()` 内幂等执行：建单据头表 + `ALTER TABLE opening_stock ADD COLUMN doc_id` + 检测到遗留 `UNIQUE(material_id, warehouse_id)` 时 12 步重建表（备份 `opening_stock_backup_mw001` + 行数一致性校验）+ 按 `(date, warehouse_id)` 回填历史单据。**回归**：新增 `tests/test_opening_stock_multi_doc.py` 33 项（同物料同仓多单据/单内去重/明细行必带 doc_id/差额入账不重复累加/单头日期覆盖明细/删行回冲/删单物理删除+全额回冲/改日期同步不动账/404/R6 校验不放松含 pydantic 中文提示/单号格式/兼容路径/Excel 整批归单/前端接线/ensure 兜底）、`tests/test_opening_stock_migration.py` 11 项、`tests/test_opening_stock_doc_no.py` 6 项、`tests/test_opening_stock_doc_navigation.py` 9 项；`tests/test_bug_2026_09_15_005` T1/T5 按新契约收窄（导航键由"必须不存在"反转为"必须存在且接通"）；`tests/verify_app_py_split_opening_stock.py` 两处断言随 JSON 明细接口路径更新。相关全量 **191 passed、6 skipped、0 failed**；`scripts/lint_wms_rules.py` 0 违规。**生效条件**：后端+模板+JS 改动，拉取后**重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存。**注意**：写路由按 A8 补 pydantic 输入模型（`OpeningStockDocRequest`/`OpeningStockDateRequest`），pydantic 英文校验错误已本地化为中文（"第 1 行仓库为必填项"） |

| BUG-2026-09-15-008 | [P1] 期初库存"删除不了"已建账/已导入记录（用户原话：「我要删除已导入的全部期初但删除不了」）。根因：台账页没有任何删除"已建账"记录的入口——工具栏「删除」经 BUG-2026-09-15-005 重定义为 `clearEntryRows()`，只清空**未保存**的录入行，对已入账记录完全无效；已建账/已导入数据只能钻进对应单据页逐张点"删除本单"，批量导入的一批想清空重来无从下手；台账面板也无逐行删/删全部 | **已修复（2026-09-15）**：新增 `POST /opening_stock/delete_all`——物理删除全部 `OpeningStock` 明细行 + 全部 `OpeningStockDoc` 单据头，逐行 `_reverse_opening_stock_line` 回冲库存（含 `doc_id` 为 NULL 的历史直连台账行），与"删除本单"同规则（回冲后库存变负照常删除、仅中文提示不阻断）；按 A8 用 pydantic `confirm: bool` 做服务端二次确认，缺/非 true 返回 400 数据不动。前端工具栏新增「删除全部」危险按钮（双确认 + `WMS.api`，禁原生 fetch），并把易混淆的「删除」改名「清空录入」以区分"清未保存"与"删已建账"。**注意**：测试夹具手动 push 常驻 app_context 会把登录态泄漏给后续 test_client（生产真实 HTTP 无此问题，干净上下文实测匿名 302 拦截），匿名用例需先显式 logout 再断言。回归 `tests/test_opening_stock_delete_all.py` 7/7（全删回冲归零/confirm 必填/空记录幂等/孤儿行回冲/未登录拦截/负向流水/前端按钮静态断言），期初库存全量 103 passed，`lint_wms_rules.py` 0 违规。后端+模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |

| BUG-2026-09-15-009 | [P1] 期初库存页把"台账查询 + 录入网格"堆在同一页，"单据"概念被淹没（用户原话：「重新写期初库存模块，我要的是一个单据而不是什乱七八糟的东西」，并选定形态「单据列表+单据编辑分离」）。根因：`/opening_stock` 列表页既是只读物料级台账、又内嵌可编辑录入网格并预填全部记录；新建页 `/add` 也把台账全部记录预填进网格——一页三合一，用户找不到"一张单据"在哪 | **已修复（2026-09-15）**：拆分为单据列表 + 单据编辑两页。`/opening_stock` 改渲染新模板 `opening_stock_list.html`——只列**单据**（单据号/日期/仓库/明细行数/合计金额/备注，操作列：打开进 `/opening_stock/<id>`、删除），含「新增单据」「批量导入」「删除全部」（BUG-2026-09-15-008 入口随迁）按钮，查询按单据号/备注/仓库（`warehouse.has`）、仓库筛选（表头或任一明细行 `lines.any`）、分页；路由 `opening_stock_list` 由查明细行 `OpeningStock` 改为查单据头 `OpeningStockDoc` 并按单聚合行数/金额/仓库展示（多仓标"多仓库"）。编辑器 `opening_stock.html`（`/<id>` 与 `/add`）移除台账查询面板成为纯单据编辑页，新建页不再预填全部记录（`existingRows` 非编辑分支置空，从空白单据开始），工具栏加「返回列表」，「删除全部」入口移到列表页；保留旧锚点 `#opening-query-panel`（随面板迁到列表页查询卡片，供深链兼容）。物料级期初余额汇总仍可在单据编辑页"汇总"页签或库存报表查看。回归：新增 `tests/test_opening_stock_doc_list.py` 9 项（列表只列单据/编辑与新建页无台账面板/仓库筛选/按号搜索/行数金额/前后端静态断言）；`test_bug_2026_09_15_005` T8 按新契约收窄（锚点迁列表页）；`test_opening_stock_delete_all` T7 改指列表模板；期初库存全量 **111 passed、0 failed**，`lint_wms_rules.py` 0 违规。后端+模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |
| BUG-2026-09-16-001 | [P2] 期初库存明细行没有「勾选批量删除」——只能逐行点删除图标，几十行的导入单要按几十次（用户原话：「期初库存有无删除整张单据功能？有无选择行删除功能？」→ 确认后指示：「把批量删行做出来」）。根因：两条既有删行路径都不满足批量诉求——①每行右侧删除图标 `deleteRow(index)` 只做 `rows.splice()`，删的是**前端数组**，必须再点「保存」才由后端差集回冲落库（用户删完直接离开页面则行不消失，且界面无任何提示）；②后端 `POST /opening_stock/<id>/line/<line_id>/delete` 单行接口自 ARCH-OS-DOC-01 起就**从未被前端接线**（死接口，A9 无覆盖），且一次仅删一行。整表亦无 `checkbox`/全选，全仓库 grep 零命中 | **已修复（2026-09-16）**：新增 `POST /opening_stock/<id>/lines/delete`（`batch_delete_opening_stock_lines`）——一次删多行、逐行 `_reverse_opening_stock_line` 回冲库存后 `db.session.delete`，回冲后库存为负照常删除、仅返回中文提示不阻断（与 `delete_opening_stock_doc` 同规则，避免同类点规则漂移）。**入参**按 A8 走 pydantic `OpeningStockLineIdsRequest(line_ids: list[int], min_length=1)`，缺失/空列表/非整数/非正数一律 400 且数据不动；`line_ids` 去重保序，同一 id 传多次只删一次、只回冲一次（防库存被重复扣减，T7 覆盖）。**越权防护**：`OpeningStock.id.in_(line_ids) AND doc_id == id` 查回后必须 `len(lines) == len(line_ids)`，混入他单的 line_id 即整批 400、一行不删、库存不动（不能只校验第一条就放行）。**原子性**：任一行回冲失败整体 `rollback`，保证「要么全删全回冲，要么原样保留」，不留半残状态。前端 `opening_stock.html` 明细表加勾选列（表头全选 `#openingCheckAll` + 每行 `.opening-row-check`），实现 `batchDeleteSelectedRows()`：**已保存行**（有 `line_id`）走新批量接口立即落库+回冲，**未保存行**仅从录入网格移除，混选时确认框分别报出两类数量，并新增「删除选中行」按钮与「已勾选 N 行」计数提示。**关键实现点**：勾选态用与 `rows` 同下标的 `selectedFlags` 数组存在**数据层**而非读 DOM——`renderRows()` 会整体重建 tbody，DOM 勾选态随之下线，改数量/加行/插入行后必然丢失；`insertRowAfter` 同步 `splice` 勾选位避免勾到相邻行，`DOMContentLoaded` 先 `syncSelectedFlags` 再首次渲染。全选框含 `indeterminate` 半选态。**回归**：新增 `tests/test_opening_stock_batch_delete_lines.py` 8 项（批删按行回冲且未选行不串账/越权整批拒绝/入参缺失空表非正数拒绝/单据不存在 404/未登录拦截/负向流水/重复 id 幂等/前端勾选列与端点静态断言）；**变异测试验证 T2 有效性**（临时关闭越权校验后 T2 如期 FAIL，确认测试非空跑）；期初库存全量 **123 passed、1 skipped、0 failed**（原 111 + 新增 8），`scripts/lint_wms_rules.py` 0 违规、`lint_no_raw_post_fetch.py` 通过；`scripts/verify_wms_bugs.py` 无新增 FAIL（4 个 AI 模块 FAIL 为改动前既有基线，已 stash 对比确认）。**真实浏览器 E2E 已实测通过**：Chromium 直连 `?embedded=1` 内容页，19/19 断言全过、零 `pageerror`——勾选 2 行→提示「已勾选 2 行」→全选/取消→未勾选时点删除被拦截→批量删除后剩 M003 一行→库存 M001 90→80、M002 140→120 按各自数量精确回冲、未删的 M003 200→200 不串账、合计金额 ¥600→¥300 正确重算。后端+模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存。**E2E 踩坑记录（供后续复用）**：tab-shell 会复用已打开标签页（同 URL 不重载，曾出现 doc5 页面 `__OPENING_DOC__.id` 仍是 4 的假象），且外层壳对 Playwright 报 zero-size 致 `wait_for_selector` 可见性等待超时；绕行方案是直连 `?embedded=1` 并用 `state="attached"` / `click(force=True)` |
| BUG-2026-09-16-002 | [P3] 期初库存单行删除接口 `/opening_stock/<id>/line/<line_id>/delete` 是**死接口**：自 ARCH-OS-DOC-01 引入起全仓库零调用点（前端 `opening_stock.html`、`app/static/js` 业务代码、Android App、`tests/` 均无引用），且无 A9 测试覆盖；一次仅删一行，用户诉求的"勾选多行一次删掉"用不上它 | **已废弃移除（2026-09-16）**：判定为 BUG-2026-09-16-001 批量接口的**能力重复实现**，按 R6"同一能力不留两处实现、避免规则各自漂移"直接删除路由（非仅标 deprecated 注释——留着仍会被后人误接成第三条删行路径）。**移除前已全仓库确认零消费方**：`grep` 模板 / 静态 JS / Java / Kotlin / Dart / tests / scripts 全部无调用。**替代方案**：删单行请调 `POST /opening_stock/<id>/lines/delete` 传单个 `line_id`——语义、回冲规则、越权防护与返回体完全一致，且该接口额外具备 A8 入参校验与去重幂等。原实现以 tombstone 注释完整留档在 `app/routes/opening_stock.py`（含 404 文案、越权校验、回冲理由串），供日后追溯，并写明"不要恢复本接口"的原因。**回归**：新增 `TestDeprecatedSingleLineDeleteRoute` 2 项锁死结局——①断言路由表不再注册该路径（并断言 `/lines/delete` 仍在，确保删的是死接口而非把删行能力整体删掉）；②手工拼老 URL 只能得 404。**变异测试验证有效性**：临时恢复该路由后两项如期 FAIL。期初库存全量 **125 passed、1 skipped、0 failed**（上一轮 123 + 新增 2），`scripts/lint_wms_rules.py` 0 违规，`verify_wms_bugs.py` 无新增 FAIL（4 个 AI 模块 FAIL 为既有基线）。后端路由删除，**生产需重启 WMS 服务生效（R3）** |
| BUG-2026-09-16-004 | [P2] 期初库存**导出**功能不可用（用户原话：「期初单据导出功能也有问题」，未给现象，实测复现出三处叠加缺陷，任一处都足以让文件不可用）：①**换行符写错 → 整个文件塌成一行**——`new Blob([...lines.join('\\n')])` 写在单引号字符串里，`'\\n'` 是「反斜杠+n」两个字面字符而非换行符，导出内容成了 `"仓库编码","物料编码",…"备注"\n"WH001",…`（其中的 \n 是可见文本），Excel/WPS 打开只有第 1 行有内容、其余全挤在一个单元格；同仓库 `in_order_add.html` / `out_order_add.html` 用的都是正确的 `'\n'`，只有本模板写错，属 R6 意义上的"同能力他处正确、此处漂移"。②**导出列序与导入列序不一致 → 导出的文件无法回导**——导出原为 9 列（仓库,物料编码,物料名称,规格,单位,数量,单价,期初余额,备注），而粘贴导入 `parsePasteLines` 按 6 列取值（仓库,编码,数量,单价,备注,日期），即"导出的第 3 列是名称、导入却当成数量"，用户「导出→改价格→导回」的常规闭环必然失败。③**导入解析不认引号 → 带引号的 CSV 每列都解错**——原用 `line.split(/Tab 或逗号/)` 裸切，不剥引号也不识别引号内逗号，`"M001"` 解析出带引号的 `"M001"`（物料匹配失败），`"含逗号,的备注"` 还会被从中间切开导致整体错列。node 实证：导出行 `"WH001","M001","轴承",…` 回导后得到 仓库=`"WH001"`、物料编码=`"M001"`、数量=`"轴承"`——全列错位 | **已修复（2026-09-16）**：①换行改为真实 `'\n'`（与 `in_order_add.html`/`out_order_add.html` 同口径），前置 BOM 保证 Excel 正确识别 UTF-8 中文；②导出列序统一到后端 `/opening_stock/import/template` 官方模板的 9 列（仓库编码,物料编码,物料名称,规格,单位,数量,单价,备注,日期），表头行与数据行统一走 `csvCell()` 加引号（原表头是裸 `join(',')` 与数据行格式不一致），新增 `warehouseCodeById` 映射取真实仓库编码，日期取真实存在的 `headerDocDate`（原引用的 `docDate` 元素在模板中并不存在，会导致日期列恒空）；③导入侧新增 `splitCsvLine()`——支持双引号包裹、`""` 转义、引号内逗号，同时兼容原有裸 Tab/逗号文本；`parsePasteLines` 自动跳过表头行、按 `parts.length >= 9` 识别导出格式列序，保证「导出→改→粘贴导入」闭环；④顺带修两处体验缺陷：空单据点导出原静默下载一个只有表头的文件，现提示「没有可导出的明细行」并 return；导出文件名改为带单据号（`opening_stock_QS26090001.csv`，新建单据为 `_new`），不再多单互相覆盖。**回归**：新增 `tests/test_opening_stock_export_csv.py` 12 项——T1 换行必须真实且不得含字面量反斜杠n、T1b 静态兜底扫非注释代码、T2 表头与后端官方模板列序逐字一致（单一事实来源交叉校验）、T3 导出→回导字段逐项一致（含带逗号备注不裂列）、T4 `splitCsvLine` 引号/转义/引号内逗号/裸 Tab、T5 表头行自动跳过、T6 空单据提示不下载、T6b 非空单据文件名带单据号、T7 原有宽松粘贴格式（Tab/裸逗号/缺日期列）行为不变、T8-T10 静态契约。**测试设计要点**：测试不重写一份 JS 逻辑做"模拟"，而是从模板**抽取真实函数**在 node 中执行（`exportRows` 真实跑，仅桩掉 Blob/URL/createElement 截获产出），否则实现与测试各写一套、改了实现测试仍绿。**三组变异测试验证用例有效性**：①换行还原成 `'\\n'` → T1/T1b/T3 如期 FAIL；②列序回退为旧 9 列 → T2 如期 FAIL；③删掉空单据守卫 → T6 如期 FAIL。**真实浏览器 E2E 通过（16/16 断言、零 pageerror）**：Chromium 登录 → 新建单据灌 3 行 → 点导出实际下载文件 → 校验含真实换行（4 非空行）、无字面量反斜杠n、表头列序与官方模板一致、3 数据行、带逗号备注未裂列 → 把导出的 CSV 原文喂回 `parsePasteLines` 得 3 行零错误。模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |
| BUG-2026-09-16-005 | [P2] 期初库存明细行**行内删除图标静默失效**：`deleteRow(index)` 只做 `rows.splice()`，删的是**前端数组**——已建账的行必须再点一次「保存」才会由后端差集回冲落库，而界面**没有任何提示**。用户点删除图标看见行消失了、以为删成功就直接离开页面，下次打开该行又回来了（库存也没回冲），属"看着生效、实际没落库"的静默失效。此为 BUG-2026-09-16-001 批量删行排查时一并发现的同根因遗留点（当时用户未拍板，本轮按指令「全部都要修」处理） | **已修复（2026-09-16）**：`deleteRow` 按行是否已建账分流，与 `batchDeleteSelectedRows` **完全同一口径**（避免同类点规则漂移 R6）——①**已建账行**（有 `line_id`）→ 弹确认框（文案说明「已建账，将立即从库存中扣回，此操作不可恢复」）→ 确认后调 `POST /opening_stock/<id>/lines/delete` 传单个 id，立即物理删除 + 库存回冲，成功后本地摘行并提示；**接口失败时不摘行**，避免界面显示已删而后端其实还在；②**未保存行** → 仅从录入网格移除（不发任何请求），但明确提示「已移除「XXX」（尚未保存，点「保存」后才生效）」，不再让用户误以为已落库；③无 `OPENING_DOC` 时兜底本地移除，不误打 `/opening_stock/null/lines/delete`。删除图标 tooltip 也按状态区分（已建账显示「删除本行（已建账，将立即回冲库存）」，未保存显示「删除本行（尚未保存）」）。**回归**：新增 `tests/test_opening_stock_delete_row_persist.py` 8 项——T1 已建账行走批量接口（断言 URL 与 payload 为单 id）且成功后摘行、T2 确认框须提及库存回冲且用户取消则不发请求行不动、T3 接口失败时行必须保留、T4 未保存行不发请求且提示含「尚未保存」、T5 无单据不误打 null id、T6-T8 静态契约（不得退回"只 splice 不落库不提示"的三无实现）。**测试方式同 BUG-2026-09-16-004**：抽取模板真实 `deleteRow` 在 node 中执行，桩掉 confirm/WMS.api/toast 记录调用序列。**变异测试验证有效性**：把 `deleteRow` 还原成旧的静默 splice 实现后，6 项如期 FAIL。**真实浏览器 E2E 通过**：点第 2 行（M002）删除图标 → 本地立即摘除 → **刷新页面确认该行不再复活**（证明真正落库，而不只是从 DOM 消失）→ 未保存行删除后弹出「尚未保存」提示；服务端核对一致：单据明细由 3 行变 2 行、`Material` M002 stock 由 5 回冲为 **0**、`StockTransaction` 新增 `qty=-5.0 / remark=期初单据删行回冲` 负向流水。模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |
| BUG-2026-08-25-002-补 | [P2] `batch_import.html` 遗漏一处记录级链接新窗口规则（**既有基线失败，非本次改动引入**）：`tests/test_bug_2026_08_25_002_doc_links_new_tab.py::test_t1` 在 `main` 上即 FAIL，报 `batch_import.html: <a class="btn btn-sm btn-outline-primary mt-1" href="' + res.detail_url + '">`。根因：BUG-2026-08-25-002 当时统一了 36 个模板 77+ 处记录级链接加 `target="_blank" rel="noopener"`，但 `batch_import.html` 里这处**由导入结果动态拼接**的「查看单据 X（可改日期）」链接漏加——该链接指向 ARCH-OS-DOC-01 整批导入生成的期初库存单据，属"进入某一项"的记录级链接，却在本页跳转，导入结果页（含成功/告警/错误明细）被顶掉。**归属确认**：已用 `git stash -u` 在干净 `main` 基线复跑，失败信息逐字一致，确认与本轮改动无关，属历史遗漏 | **已修复（2026-09-16）**：为该动态链接补 `target="_blank" rel="noopener"`，与其余记录级链接口径统一，保留导入结果页便于对照。**回归**：`tests/test_bug_2026_08_25_002_doc_links_new_tab.py` **3 passed**（原为 1 failed 2 passed）。另全仓库重新扫描一遍记录级链接（`url_for('*_detail')` / `detail_url` / `_edit_page` / `safeJumpUrl(`），除该处外仅剩 7 处未加新窗口——逐条核对**全部是测试文件 EXCLUSIONS 中显式登记的排除项**（详情页同记录「编辑」模式切换与「返回」回退导航），按规则本就应保持本页跳转，无误伤。模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |

| BUG-2026-09-16-006 | [P1] 生产打开期初库存单据列表即 500：`sqlite3.OperationalError: no such table: opening_stock_doc`（2026-09-16 生产日志实测，栈顶 `routes/opening_stock.py:54 opening_stock_list` 的 `query.paginate`；启动日志同时显示 `[Auto-Fix] opening_stock.doc_id 已补缺列` + `Startup database initialization skipped by environment`）。根因：ARCH-OS-DOC-01 引入的单据头表 `opening_stock_doc` **建表**只发生在 `db.create_all()` / `auto_migrate_database()`（Step 1），历史行归单只在 `auto_migrate_database()`（Step 5）；而 `start_wms_offline.bat` / `start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1` 把迁移整体跳过，BUG-2026-09-15-007 的 `ensure_opening_stock_doc_columns()` 只补了 `opening_stock.doc_id` **列**、没建**表**——「功能上线前建的存量库 + WMS_NO_DB_TOUCH=1 重启」叠加态下表根本不存在，单据列表/首上下末导航/新增/编辑/删除**所有消费点**齐 500。**R6 同根因历史已查**：BUG-2026-08-22-001（缺 `excel_print_template` 表）、BUG-2026-09-12（缺 `department` 表）均为"建表只在迁移里、迁移被开关跳过"，当时各修各的消费点，本次为第 3 次复发 | **已修复（2026-09-16）**：新增 `ensure_opening_stock_doc_table()`，仿 `ensure_excel_print_template_table` 独立 sqlite 连接、独立于迁移开关无条件执行、幂等（WAL + `busy_timeout=60000`，失败仅记日志不阻断启动，锁冲突下次启动自愈，R4），启动期注册在 `ensure_opening_stock_doc_columns()` 之后。**防 R6 漂移**：建表 DDL 与历史回填从 `auto_migrate_database()` 抽出公共函数 `_create_opening_stock_doc_table()` / `_backfill_opening_stock_docs()`，迁移与兜底**两处共用同一实现**（静态测试锁死 DDL/INSERT 全仓库各只有一份）。**必须回填而非只建空表**：只建空表会让单据列表"能打开但空空如也"，用户历史期初在界面上等同消失（数据没丢也会误认丢失而重复录入）；回填口径与迁移一致——按 (建账日期, 仓库) 归单、单号 QS+YYMM+4位、`date` 为 NULL 的行保守不归单、只加归属不改账（quantity/price/amount 与 Material.stock 不动）。**职责边界**：老库 `UNIQUE(material_id, warehouse_id)` 的 12 步重建与 `uix_opening_stock_line_doc` 唯一索引仍属 `auto_migrate_database()`（极端脏数据下建唯一索引失败会连累建表回滚，兜底路径不碰）；`doc_id` 列顺带兜底使函数与注册顺序无关。**回归**：新增 `tests/test_ensure_opening_stock_doc_table.py` 8 项——缺表补建（列/索引/归单分组/单号递增/NULL日期不归单/不改账）、原 500 分页查询恢复、幂等重复执行、已有表与人工改动不动、全新库建空表、库文件不存在静默返回、迁移与兜底共用公共函数静态断言、**子进程 `import app` + `WMS_NO_DB_TOUCH=1` 模拟生产启动完整复现**（缺表被补建、历史归单 3 张单）。期初库存/迁移/ensure 相关 **193 passed、1 skipped、0 failed**；`lint_wms_rules.py --staged` 0 违规、`lint_no_raw_post_fetch.py` 通过。后端改动，**生产需重启 WMS 服务生效（R3）**——重启后兜底自动建表归单，无需人工操作数据库 |

| BUG-2026-09-16-007 | [P2] 期初库存单据删除后台账仍残留流水（用户原话：「期初库存被我删除了怎么还有单据，搞得乱七八糟的」→ 明确口径「我的要求是删除了就没有流水」）。根因：`_reverse_opening_stock_line` 删单/删行时**追加一条 quantity=-N 回冲流水**（remark=期初单据删除回冲/删行回冲/批量删行回冲/全量删除回冲），明细行物理删除后，其建账 +N 与回冲 -N 两条流水都留在 `stock_transaction`，库存台账继续显示单据编号 `opening_stock-<id>` 指向已不存在明细行的记录（悬挂引用）。且与本仓库既有惯例不一致——入库单/出库单/调整单/售后出库单删除时都是**同步删除自身流水**（in_order.py:2029 / out_order.py:1072 / adjustment.py:576 / after_sale_out.py:538，注释明写「避免库存台账保留指向已删除单据的悬挂引用」），期初库存是唯一走"加回冲流水"的岔路（R6 同能力两处口径漂移） | **已修复（2026-09-16）**：①**删除路径改口径**：`_reverse_opening_stock_line` 不再追加 -N 回冲流水，改为**物理删除该明细行的全部库存流水**（建账 +N 与历次编辑差额；数量为 0 的行也先清流水再早退，覆盖"先建 5 再改 0"残留的 +5/-5 编辑流水）；库存余额回冲（Material.stock / 库位账）与负库存中文提示口径不变，`reason` 参数保留仅兼容既有调用点。5 处删除消费点（整单删除/保存差集删行×2/批量删行/删除全部）共用同一函数，一次改动全部收口。②**历史数据清理**：新增 `cleanup_dangling_opening_stock_transactions()`，启动期独立于迁移开关无条件幂等执行（busy_timeout=60000，失败仅记日志不阻断，R4）——物理删除「reference_type='opening_stock' 且 reference_id 已不在 opening_stock 表」的悬挂流水；**误删防护**：material.py「新增物料初始库存」流水复用同 reference_type 但 reference_id 是 **material.id**（remark='新增物料初始库存'），按 remark 排除（id 域会撞号，仅靠 id 无法区分）。**回归**：新增 `tests/test_cleanup_dangling_opening_stock_transactions.py` 5 项（悬挂 +N/-N/NULL-remark 清理、存世行与 material.id 域与他类型流水保留、幂等、缺表/缺文件静默、整单删除集成、启动接线与删除语义静态断言）；改写 `test_opening_stock_batch_delete_lines.py` T6 与 `test_opening_stock_delete_all.py` T6 为新契约（流水物理移除、无新增负向流水、未删行不串账、库存回冲不变）。**测试基建踩坑**：新测试类初版逐表 wipe 在全量 pytest 共享内存库下撞 `foreign_keys=ON`（app.py:6062 引擎级强制）残留行级联出 3 failed+1 error；改为 `drop_all+create_all`（test_material_delete_missing_ai_table 同款）后，选择集（72 文件）**289 passed、0 failed**，与干净 main 基线（284 passed）差值恰为新增 5 项，54 个 subprocess 型 ERROR 为改动前既有基线。`lint_wms_rules.py --staged` 0 违规、`lint_no_raw_post_fetch.py` 通过。后端改动，**生产需重启 WMS 服务生效（R3）**——重启时自动清理历史悬挂流水，台账即刻干净 |

| BUG-2026-09-16-008 | [P2] 从未使用打印却每天半夜收到「打印任务滞留」告警（用户原话：「我没有打印为什么有这个消息」，截图两条告警时间 2026-09-15 00:00:35 / 2026-09-16 00:00:00，同一任务 ID 9 / 出库单 ID 169）。根因链路：①手机扫码/移动端提交入出库成功后 `enqueue_auto_print_job` **无条件**自动创建打印任务（docstring 明写"任务始终创建"），用户没点打印、提交动作本身就建了任务；②无任何在线打印工作站，任务永远 pending；③`check_print_health` 每分钟巡检按「同一任务**当日**只告警一次」去重——任务跨天仍 pending，每天零点过后第一次巡检即重新告警；④打印工作台只有完成/失败接口，**没有取消任务能力**，滞留任务用户删不掉，骚扰无解 | **已修复（2026-09-16，按用户拍板「加自动打印总开关」）**：①新增系统设置 `auto_print_on_submit`「提交后自动打印」（默认**开**，保持既有行为不影响依赖自动打印的场景；系统设置 → 打印参数可关）。②关闭后 `enqueue_auto_print_job` 入口直接返回 None 不建任务——6 个自动调用点（mobile 扫码提交×2/草稿确认×2、native App 入出库×2）共用同一函数一次收口（R6），调用点均不使用返回值，业务单据照常提交，手动打印（手机端"打印单据"按钮走 `/print_queue/jobs`，`source_event='manual'`）不受影响。③`check_print_health` 巡检在开关关闭时把滞留的**自动来源** pending 任务逐批作废（`status='cancelled'` + 中文作废原因），手动任务与阈值内新任务不动；作废在告警开关判断**之前**执行，队列卫生与告警解耦，作废后自然不再产生滞留告警——用户关掉开关后**无需重启**，下一分钟巡检即清净。新增 `AUTO_PRINT_SOURCE_EVENTS` 常量登记全部自动来源事件（静态测试锁定调用点字面量 ⊆ 常量，新增调用点漏登记即报红）。**回归**：新增 `tests/test_auto_print_on_submit_switch.py` 5 项（默认开建任务/关闭不建/关闭巡检只作废滞留自动任务且不再告警/开启巡检不作废/静态接线）；`tests/test_print_alerts.py::test_check_print_health` 适配 stats 新增 `auto_cancelled` 键。打印/移动端/原生接口相关 **488 passed、0 failed**；`lint_wms_rules.py --staged` 0 违规、`lint_no_raw_post_fetch.py` 通过。后端改动，**生产需重启 WMS 服务生效（R3）**；重启后到「系统设置 → 打印参数」关闭「提交后自动打印」，约 1 分钟内滞留任务自动作废、告警停止 |

| BUG-2026-09-16-009 | [P1] 手机端（Android）提交的期初库存**不归任何单据**：`native_api.py` 的 `native_api_opening_stock_submit` 调 `_apply_opening_stock_balance` 时**漏传 `doc_id`**，明细行 `doc_id=NULL`。后果（跨端黑洞）：手机上建的期初在 PC 端 `/opening_stock` 单据列表**永远看不到**、不能按单打开/改日期/删除，只剩「删除全部」一条路；`_opening_stock_compat_doc` 的 docstring 明写兼容单就是为「粘贴导入、移动端」等无单调用方准备的，但移动端原生 API 这条接线被漏接。此为 ARCH-OS-DOC-01 改造时的同类漏接点（batch_save 兼容分支已接，native_api 漏接），与 BUG-2026-09-15-007 开发期缺陷①（`doc_id` 赋值静默丢失）同根因族 | **已修复（2026-09-16）**：`native_api_opening_stock_submit` 取 `_opening_stock_compat_doc(payload.get('date'), generate_order_no)` 并把 `doc_id=compat_doc.id` 传入入账函数——与 batch_save 兼容分支**同一口径**：无单写入统一归 QSLEGACY+YYMM 兼容单（幂等复用，不会每提交一次建一张单把列表刷爆），「同 (物料,仓库) 单行 upsert」语义不变。**自愈历史数据**：`_apply_opening_stock_balance` 既有逻辑在 `doc_id` 非空时会把历史 NULL 悬空行收养进该单，手机端下次提交同 (物料,仓库) 即自动归单，无需人工修数。**回归**：`tests/verify_mobile_opening_stock_api.py` 新增 3 项——S6 提交行 doc_id 非空且归 QSLEGACY 单 + PC 端 `/opening_stock` 列表页 HTML 可见该单号（跨端可见性闭环）、S7 历史 doc_id=NULL 悬空行被收养且数量按差额更新、S8 静态契约锁死「提交链路必须传 doc_id」防重构再漏。期初库存/原生 API 相关 **158 passed、1 skipped、0 failed**（原 155 + 新增 3）；`lint_wms_rules.py --staged` 0 违规、`lint_no_raw_post_fetch.py` 通过。后端改动，**生产需重启 WMS 服务生效（R3）** |

| BUG-2026-09-16-010 | [P1] 手机端期初建账两处操作硬伤（仓库管理专家评审 2026-09-16，用户拍板"先做 P0"）：①**连续扫码同一条码数量被覆盖成 1**——`OpeningStockViewModel.addLine` 对同编码行做 `existing.copy(quantity = quantity)` **覆盖**合并，扫码回调每次又只传 manualQty（默认 1），仓管扫 100 个轴承提交数量恒为 1，AI-MOB-CONTINUOUS-SCAN-01 的连续扫描在期初场景名存实亡（扫码场景按"件"计数，合并必须累加；同仓库入库扫码 `ScanViewModel` 即累加口径，此处为 R6 同能力漂移）；②**已录入行不能改数量**——行卡片只有删除按钮，数量录错只能删行重扫 | **已修复（2026-09-16）**：①`addLine` 同编码合并改为**累加**（`existing.quantity + quantity`），连续扫 N 次即 N 件，手动添加同编码亦为累加语义；②新增 `updateLineQuantity(index, quantity)`（确切值、负数拦截）+ 行卡片 `clickable` 打开编辑弹窗（数字键盘 + ±1 步进 + 「删除该行」「取消」），"扫码/添加负责加、点行负责改成对的数"两条路径互补。弹窗索引防御：行已被移除时自动关闭不崩溃。**回归**：新增 `tests/verify_android_opening_qty_merge.py` 4 项静态契约——T1 累加语义锁死（`existing.quantity + quantity` 必须在、`copy(quantity = quantity)` 出现即红）、T2 ViewModel 声明与非负校验、T3 行卡片 clickable + 弹窗接线、T4 连续扫描加行链路未断。Android 相关契约测试 **35 passed、0 failed**（含 continuous_scan / vm_method_pairing / keyword_search / scan_feedback / qty_keyboard 无回归）。纯 Kotlin App 改动，**随下次 APK 构建发布生效**，服务端无需重启 |

| BUG-2026-09-16-011 | [P1] 期初库存单据网格**没有库位列**（仓库管理专家评审 2026-09-16，用户拍板"先做 P0"）：开启库位管理后按 AGENTS.md 规则二「仓库和库位均为必填」，后端 `_apply_opening_stock_balance` 与 `/opening_stock/save` 早已接收 location 入参、编辑页 `edit_rows` 也带 location 字段，但前端网格没有任何库位录入入口——期初永远落不到真实库位，只能以仓库名作占位行进库位账（BUG-2026-08-16-002 的占位兜底被当成了常态）。入库单 `in_order_add.html` 已有「开关传模板 + 必填红星」的成熟模式，期初为漏接的同类点 | **已修复（2026-09-16，与入库单同口径）**：①`/opening_stock/add` 与 `/opening_stock/<id>` 两个路由把 `location_management_enabled()` 传入模板；②开启时网格在「仓库」列后渲染「库位」列（红星必填标识，`LOCATION_ENABLED`/`GRID_COLSPAN` 两个 JS 常量随开关注入，空态 colspan 由写死 12 改为常量避免列数错位）；③`existingRows`/`emptyRow`/`collectItems` 全链路携带 location——编辑已有单据行库位正确回填，保存时随 items 传到后端（后端原有支持，零改动）；④保存前必填校验：开启库位管理后非空行缺库位即拦截并中文提示行号（仓库兜底解析之后再校验），关闭时行为完全不变。**范围说明**：粘贴导入/Excel 模板/导出 CSV 的库位列与手机端库位选择另行任务，本轮只补网格录入主路径。**回归**：新增 `tests/test_opening_stock_location_column.py` 5 项——开/关两种开关态渲染、编辑页回填、保存落库且**库位账落到真实库位而非占位行**（端到端）、静态契约（collectItems/emptyRow/单元格守卫/GRID_COLSPAN/必填提示）。**测试基建踩坑**：夹具 `_wipe` 漏清 `LocationInventory` 导致整文件运行时 T3 残留库位账 100 + T4 再入 100 = 200 与断言不符（单跑 T4 必过、整跑必挂），已补清表。期初库存/库位相关 **319 passed、2 skipped、0 failed**；`lint_wms_rules.py --staged` 0 违规、`lint_no_raw_post_fetch.py` 通过。后端+模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |

| BUG-2026-09-16-012 | [P1] 期初库存编辑页**整库内嵌物料**（仓库管理专家评审 2026-09-16，用户拍板"先做 P0"）：`/opening_stock/add` 与 `/opening_stock/<id>` 两个路由都执行 `Material.query.all()` 把全部物料序列化成 `materialData` 塞进页面，前端联想再全量 filter——物料上千时首屏 HTML 数 MB、联想卡顿，且属 R1 点名的"把全量当默认"模式。手机端 `/api/material/search` 早有服务端搜索能力，PC 期初页是漏改的同类点 | **已修复（2026-09-16）**：①两个编辑页路由删除整库查询与 `material_options` 内嵌（性能落点）；②联想改走既有 `GET /api/material/search?keyword=`（web 会话可用），防抖 200ms + 序号防竞态（后发先至丢弃旧结果），命中写入本地缓存 `materialCache`/`materialCodeIndex`；空关键字不再展示"全部物料前 30 条"（整库已不内嵌），改为提示输入关键字；③粘贴导入一次性几十~几百个编码，逐个调搜索会打满请求——新增 `POST /opening_stock/materials/lookup` 按编码**批量**预热缓存：A8 pydantic 校验（1~500 个、空白剔除、去重保序），**大小写不敏感匹配**（与原 `materialData.find` 的 toLowerCase 比较同口径，粘贴来源大小写差异不再误报"物料不存在"），payload 复用 `api_material_payload`（与移动端搜索同一构造器，R6 防字段漂移），返回 items + missing 清单；④`selectMaterial`/`parsePasteLines` 一律查缓存，`confirmPasteImport` 先 `extractPasteCodes` 批量预热（缓存已覆盖时零额外请求）再走原同步解析，行级校验逻辑零改动。**回归**：新增 `tests/test_opening_stock_material_search.py` 5 项（页面无内嵌+路由无整库查询静态锁、lookup 命中/missing/payload 字段齐、入参与未登录校验、同一 payload 构造器静态防漂移、模板契约）；`test_opening_stock_export_csv.py` harness 同步改缓存 Map + 真实抽取 `cacheMaterial`/`findMaterialByCode`（仍执行模板真实源码，非重写模拟），导出→回导闭环 12 项全过。**测试基建踩坑×2**：①静态断言须先剥注释/docstring，否则说明文字里的 `Material.query.all()` 字样误报；②夹具常驻 app_context 泄漏登录态，匿名用例须先显式 logout（同 BUG-2026-09-15-008 记录）。**基线对账**：大选择集（opening_stock or material，253 项）运行——干净基线 248 passed + 59 errors（既有共享内存库污染类），修复后 **254 passed + 59 errors**（ERROR 零新增；新测试文件改用 drop_all+create_all 免疫模式，未给基线添错）。`lint_wms_rules.py --staged` 0 违规、`lint_no_raw_post_fetch.py` 通过。后端+模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |

| 类型 | 进入条件 | 处理方式 |
|------|----------|----------|
| 真实 BUG | 有代码证据、可触发路径、错误结果 | 修复并加入回归验证 |
| 风险项 | 理论可触发，但依赖配置或边界条件 | 记录缓解措施，必要时加监控 |
| 配置项 | 生产环境需要强制配置或关闭调试 | 固化配置检查 |
| 误报 | 代码已有保护，或不符合实际业务流程 | 记录原因，不重复报 |
| 暂缓 | 技术限制或迁移成本较高 | 明确风险和后续条件 |

## 已修复并纳入回归

## 已修复并纳入回归

| BUG-2026-09-08-002 | [P1] 物料管理页全局工具栏「设置没反应、打印是网页残渣」（用户实测）：①点全局「设置」面板秒关——`openSettings` 同步点击页面「列设置」按钮把面板打开后，用户的原始点击事件继续冒泡到 document，触发页面「点击面板外部关闭」监听，面板在同一次点击内被立即关掉（Playwright 实证：点全局设置后 panel.show=False，直接点页面列设置按钮=True）；②点全局「打印」裸打整张网页——物料模块未配 printListUrl/detailPrintUrl，`printCurrent` 兜底 `window.print()`，分类树/搜索框/列设置面板等全部上纸（Playwright+pdftotext 实证 PDF 含「分类树/搜索物料编码/选择显示字段」），且 visibility 隐藏法会被祖先 overflow 裁掉长表与长名称 | **已修复（2026-09-08）**：①`app/static/js/app.js` `openSettings(module, evt)` 委托点击前 `evt.stopPropagation()`（根因层修复，全模块带「点外部关闭」面板的页面同受益）；②`app/templates/material.html` 新增物料清单打印样式：按区块 display:none 功能区（工具栏/页头/搜索卡/分类树/弹窗/列设置面板），解除表格祖先滚动/裁剪约束（overflow/max-height），单元格放开换行防长名称被裁，勾选列/排序图标/拖拽手柄不上纸；③`app/static/css/custom.css` 全局打印规则追加隐藏 `.cb-actionbar/.modal/.dropdown-menu`（所有列表页 window.print() 兜底不再把页面 chrome 打上纸）。回归：`tests/test_bug_2026_09_08_002_toolbar_settings_print.py` 4/4（stopPropagation 先于委托点击/事件对象传入/打印样式契约/全局规则契约/node --check app.js）；Playwright 浏览器实证：修复后点设置面板正常展开且点外部仍正常关闭，打印 PDF 仅含物料表格（3 行全、长名称 BREAKER 完整、无分类树/搜索框/面板残渣）；`scripts/lint_wms_rules.py --staged` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：含模板改动（material.html），拉取代码后**生产需重启 WMS 服务生效**（R3）；app.js/custom.css 为静态文件，强刷浏览器即可。**R6 同类点排查**：设置秒关的根因修复在 app.js 通用层、全模块生效；其余未配 printListUrl 的列表页（售后出库/采购申请/委外/调拨/调整/盘点/领料等）仍走 window.print() 兜底，通用规则已消除工具栏/弹窗/下拉残渣，各页专属表格打印样式按需按报告逐页处理（每页布局结构不同，不做臆测性批量改动） |

| BUG-2026-09-08-001 | [P1] 物料标签打印字段静默丢失（用户实测「物料名称打印不出来」：模板设计器里 [条码]/[物料名称]/[规格] 齐全，批量打印页只出条码+规格）。根因是同一缺陷类的三个消费点（R6 一并修复）：①Excel 风格设计器（`material.html`）删除行/列后**不重排**剩余单元格的 `data-row`/`data-col`，序列化出的坐标可越出声明的 rows×cols 网格（如残留 row=3 而 rows=3）；②批量打印页（`print_batch_labels.html`）渲染时仅按声明 rows×cols 迭代 cellMap，**越界坐标的单元格被静默丢弃**——字段不打印且无任何提示（node 实证：残留 row=3 的名称单元格渲染结果为「空,barcode,spec」，name 丢失）；③`_normalize_label_template_layout` 发现 "行-列" 键控条目（旧设计器 `label_template_detail.html` 的 `save_layout` 格式）时**整体丢弃 cells 列表**（新设计器格式），模板被两个设计器先后编辑后只剩键控条目里的字段 | **已修复（2026-09-08）**：①`material.html` 新增 `reindexLabelCells()`（按 DOM 实际行/列含 colSpan 重写 data-row/data-col），`saveTemplate()` 与 `saveTemplateToStorage()` 序列化前强制调用——删除行/列链路（deleteRow/deleteSelectedRow/deleteCol）均经 saveTemplateToStorage 收尾自动覆盖，存下去的坐标恒等于用户所见布局；②`print_batch_labels.html` 渲染网格改为 `max(声明 rows/cols, 单元格实际最大坐标+1)`，越界单元格全部渲染（存量损坏模板无需重画即恢复）；③`_normalize_label_template_layout` 混合格式改合并：cells 列表全保留（同坐标列表优先），键控条目仅补充未占用坐标并合成占位 text（`[规格]`）。回归：`tests/test_bug_2026_09_08_001_label_cell_coords.py` 4/4（混合合并含 name 保留+同坐标列表优先+键控补入合成 text/纯键控原行为/打印页网格覆盖契约/设计器序列化前重排契约）；E2E 实证：越界损坏模板与混合格式模板经 `/label_template/api/<id>/detail` 均返回全部字段，从打印页源码提取的真实网格逻辑渲染出 barcode,spec,name；修改区 4 个 JS 函数 node --check 通过；标签/打印相关既有测试 40/40 PASSED；`scripts/lint_wms_rules.py --staged` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：含模板改动（material.html / print_batch_labels.html），拉取代码后**生产需重启 WMS 服务生效**（R3，生产模式模板有缓存）；存量已损坏的模板无需手工修复，重启后打印自动恢复全部字段 |
| BUG-2026-09-07-021 | [P1] 报表中心缺经营视角：只有流水类（出入库明细/台账）与静态库存列表，无按物料维度的库存状态预警、ABC 资金占用分类与呆滞识别——采购/财务无法从报表直接回答「哪些物料该补货、钱压在哪、哪些压了多久」三问。原计划 021（骨架+缺货状态）/022（ABC+呆滞）/023（库龄+周转）三步拆分，按用户指令「021 022 023不做」合并为**单原子动作**：骨架 + ABC + 呆滞一次完成；库龄/周转不做（v1 裁剪） | **已修复（2026-09-08）**：新增 `inventory_analysis`（库存经营分析）报表并注册进 `SQL_PAGED_REPORT_BUILDERS`（019/020 分批导出/模板打印按协议自动接入）——①物料维度 16 列宽表（行集=筛选后物料，行数可控，与库存报表 015 同判定不适合 SQL 下沉：Python 全量构建 + 切片分页，表头 sort_field 走全量 `_sort_rows` 回退后切片）；预计算层每次调用独立重算，**不在 g 缓存库存映射**（盘点等路由先写流水再读，缓存会返回脏数据）；②库存状态口径：`min_stock>0` 才预警——stock≤0 缺货 / stock≤min_stock 低于安全库存 / 其余正常；建议补货量 = 补至 `max_stock`（未设则补至 `min_stock`），不预警为 0；③ABC 口径：库存金额（仓库级净库存×单价）降序累计占比**不含自身** <70%→A / <90%→B / 其余 C，同金额按物料 id 升序确定，金额合计≤0 全 C（首项豁免：单物料归 A 而非 C）；④呆滞口径：stock>0 且 idle_days ≥ `stock_idle_days` 系统设置（默认 90），idle_days = today − 最后出库日期（无出库按物料建档日期起算）；出库方向判定与 `_is_inbound_transaction` 严格同源（显式出库类型白名单 OR 数量<0 且类型未知/NULL；**期初类型既非入也非出不参与**）；⑤summary（count=物料数/quantity=净库存和/amount=金额和）对筛选后全量计算、与分页解耦（R2）；hide_zero 过滤零库存行与库存报表口径一致；⑥`REPORT_BUILDERS` 同步登记（payload 门禁 + 3 元组包装 `_build_inventory_analysis_report`，实际分页/导出/打印均走 SQL_PAGED 分支）。回归：`tests/test_bug_2026_09_07_021_inventory_analysis.py` 7/7（宽表行=物料级+两仓库存净额隔离（B 仓 M1 净 2 金额 200、M2 无流水显示 0，0 库存行保留是缺货预警行集语义）/库存状态三态+min_stock≤0 不预警+补货量补至 max/min/ABC 集成梯度 AABCC+纯函数边界（首项豁免单物料 A、total=0 全 C、同金额 id 序）/呆滞 90 天边界（=90 呆滞、89 不标记、stock=0 不标记）+从未出库按建档 d-100 起算+期初流水不被误判出库/分页元数据+全量排序回退+020 分批导出（批 3 两批全量 5 行无提示）+模板打印接入/hide_zero 剔除 M3/summary count=5 quantity=18 amount=1208）；全量回归 **1347 passed、84 skipped、0 failed**；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移。**v1 裁剪声明**：呆滞为全局物料维度（多仓下偏保守）；库龄分布/周转率属原 023 范围，按用户指令未实现 |
| BUG-2026-09-07-020 | [P1] SQL 分页报表导出受 `REPORT_ROW_LIMIT`(50000) 单次截断，「按当前筛选导出全部」未实现（评测报告 0.4 后半）：019 只补了文件内提示行，超限导出仍缺行——大仓长周期明细（>5 万行）导出/模板打印永远不完整；且 019 修复后 print_excel 的全量路径同样受 5 万上限 | **已修复（2026-09-08）**：分批流式导出——①新增 `_iter_report_export_batches`：SQL 分页报表循环 builder 的**分页分支**逐批取回（每批 `REPORT_EXPORT_BATCH_SIZE`=5000，批间行序由 builder 内排序含表头 sort_field 全量排序回退保证连续，退出条件 `page*batch >= total`，多批拼接 = 筛选全集、无重复无遗漏）；内存路径报表（台账/月报，结存/期末倒推为运行期值无法切片）单次全量构建、仍受 `LEDGER_ROW_LIMIT` 等截断；②新增 `_build_report_excel_download`：openpyxl **write_only** Workbook 逐批写、内存峰值恒为单批，截断时文件尾提示行（019 机制迁移），**替代并删除 `_build_excel_response`**；`report_api_query` 导出分支改走 download；③新增 `_collect_report_export_rows`：print_excel 改走分批收集（不再经过 `_build_report_payload` 与 019 的 export 强制置位），模板打印同步突破 5 万上限；④`_report_export_truncation` 统一截断判定（SQL 分页=不截断；内存路径读 flask.g 读后即清）。回归：`tests/test_bug_2026_09_07_020_export_all_rows.py` 5/5（REPORT_ROW_LIMIT=10 时导出仍全量 25 行无提示/批 7 四批拼接行序与单批一致每单恰 5 行/模板打印同步突破/空结果仅表头/导出行数=页面 total）；BUG-2026-09-07-019 T3 随架构演进改为内存路径台账截断提示（SQL 分页导出截断由 020 取代）6/6；导出/打印相关 32 项全过；全量回归 **1340 passed、84 skipped、0 failed**；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-019 | [P1] 模板 Excel 打印对 10 类 SQL 分页报表只输出第一页（数据完整性回归）：前端「模板打印」（templatePrintBtn → `/report/<type>/print_excel`）构造参数时**显式 delete('export')**，请求不带 export=excel；而 SQL 分页 builder（BUG-2026-09-07-004 起下沉的 10 类）只在 `export == 'excel'` 时返回全量行、否则返回当页——print_excel 用 payload 的 rows 渲染模板，导致模板打印从全量退化为第一页（默认 20 行），用户以为打印了全部、实际静默缺行。内存路径报表（台账/月报等）的 all_rows 一直为全量，故该回归仅在 SQL 分页报表出现；且导出超 `REPORT_ROW_LIMIT`(50000) 时截断提示只存在于网页 JSON，**下载后的 Excel 文件内无任何缺行标识**，会计/审计基于缺行文件对账即成事故 | **已修复（2026-09-08）**：①`routes/report.py` `report_print_excel` 在 `_build_report_filters()` 后强制 `filters['export'] = 'excel'`（打印语义 = 当前筛选全量，与页面分页无关；内存路径 all_rows 本就全量，置位行为不变），truncated 时记 warning 日志；②`_build_excel_response` 新增 truncated/raw_total 参数，超限时在 Excel 文件尾部追加空行 + 截断提示行（真实行数 / 单次上限 / 已含行数 / 建议缩小范围分批导出），`report_api_query` 导出分支透传 payload 的 truncated/raw_total——文件级 0.4「导出不静默丢行」补全。**020 演进**：BUG-2026-09-07-020 起导出/打印改走分批流式（SQL 分页报表不再截断），print_excel 改用 `_collect_report_export_rows`、`_build_excel_response` 由 `_build_report_excel_download` 替代删除，本条文件内提示机制随迁移保留于内存路径报表（019 T3 已随之演进）。回归：`tests/test_bug_2026_09_07_019_print_excel_full_rows.py` 6/6（print_excel 全量 25 行（修复前仅 20）/export=excel 全量且未超限无提示行/内存路径台账 LEDGER_ROW_LIMIT=10 时文件仅含 10 流水+1 合计行 + 尾部提示行含真实 12 与已含 11/页面分页不受导出上限影响/无默认仓 print_excel 400/打印首行为默认日期倒序最新单 IN-E）；全量回归 **1335 passed、84 skipped、0 failed**（新增 waitress 依赖解锁 test_run_server_port_hint 1 项）；lint 双门禁 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-018 | [P1] 委外加工报表每次查询/翻页都全量物化再内存求和：行集 = 委外单、无 `REPORT_ROW_LIMIT` 截断（无 R2 失真），但原内存路径每次查询/翻页都把委外单连同 需求明细 + 发料单及其明细 + 收货单及其明细 **全部物化** 再逐单 Python 匹配求和——发料/收货两套子表使其成为最重的单表报表：实测（8000 单 / 40000 明细行，内存 SQLite）单次查询 **2.52s / 81 条 SQL**，翻 3 页 7.24s、单页均摊 2.41s | **已修复（2026-09-07）**：新增 `_sql_paged_subcontract_report` 并注册进 `SQL_PAGED_REPORT_BUILDERS`——①发料/收货明细分别先做「按委外单分组聚合」命中子查询（`SubcontractIssueItem.issue_id→SubcontractIssue.subcontract_order_id`、`SubcontractReceiveItem.receive_id→SubcontractReceive.subcontract_order_id`；物料关键词时仅命中物料行参与，与内存逐表 `matched_items` 口径一致；无关键词时全部明细，含物料缺失行）；②主查询 = 委外单 LEFT JOIN 两个聚合子查询 + 加工厂商，WHERE（仓库/日期/供应商/状态）与排序下沉 SQL，默认路径 ORDER BY 日期倒序 + ID 倒序确定兜底并只取当页 LIMIT/OFFSET；③汇总由 SQL 对筛选后全集聚合、与分页解耦（count=委外单数、quantity=发料数量总和、amount=收货数量总和，与内存 summary 口径一致）；④无发料/收货的委外单 LEFT JOIN 保留（数量 0，与内存一致）；⑤**关键词保留判定 = 需求/发料/收货任一表存在命中行**（`db.or_` 三个 EXISTS/聚合存在行），与内存「三集合联合判空后 continue」语义一致，各列只累计各自命中行；⑥表头排序（sort_field）回退「全量取回 + Python `_sort_rows` + 切片」——行数=委外单数、无嵌套子表 ORM 加载，语义与内存一致。回归：`tests/test_bug_2026_09_07_018_subcontract_sql_pagination.py` 8/8（默认日期倒序/分页互不重叠/汇总解耦且各页加总==汇总/无子单保留计 0/关键词只累计命中行且无命中单排除/仓库必填+多仓隔离（B 仓 777 不串入）/API 元数据/双路径一致含关键词路径/表头收货数量降序首行=6）；全量回归 1328 passed、84 skipped、0 failed；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-017 | [P1] 工单领料报表每次查询/翻页都全量物化再内存切片：行集 = 领料单、无 `REPORT_ROW_LIMIT` 截断（无 R2 失真），但原内存路径每次查询都全量物化领料单 + `selectinload` 全部明细行再逐单 Python 求和——实测（15000 单 / 30000 明细，内存 SQLite）单次查询 **1.85s / 31 条 SQL**，翻 3 页 4.99s、单页均摊 1.66s；**每翻一页/每改一次筛选都重算一遍全量**，单据量大时报表卡顿 | **已修复（2026-09-07）**：新增 `_sql_paged_requisition_report` 并注册进 `SQL_PAGED_REPORT_BUILDERS`——①明细行先做「按领料单分组聚合」命中子查询（物料关键词时仅命中物料行参与，与内存 `matched_items` 口径一致；关键词命中判定复用 `_material_filter_clause`——token 按空格拆分后无「跨字段串词」问题，与 `_material_matches` 语义等价）；②主查询 = 领料单 LEFT JOIN 聚合子查询 + BOM + 操作人，WHERE（仓库/日期/状态）与排序下沉 SQL，**默认路径 ORDER BY 日期倒序 + ID 倒序确定兜底并只取当页 LIMIT/OFFSET**；③汇总（单数/数量/金额）由 SQL 对筛选后全集聚合、与分页解耦；④无明细领料单 LEFT JOIN 保留（数量 0、计入单数，与内存一致）；⑤物料关键词时用「聚合子查询存在行」过滤（与内存 continue 排除无命中单一致）；⑥**表头排序（sort_field）回退「全量取回 + Python `_sort_rows` + 切片」**——行数=领料单数、无明细 ORM 加载，语义与内存完全一致；⑦每行展示值（状态标签/金额四舍五入/日期格式化/详情链接）逐项对齐内存。回归：`tests/test_bug_2026_09_07_017_requisition_sql_pagination.py` 8/8（默认排序日期倒序+同日 ID 倒序/分页互不重叠/汇总解耦且各页加总==汇总/无明细单保留且计 0/关键词只算命中行且无命中单排除/仓库必填+多仓隔离（B 仓 777 不串入）/API 元数据/双路径一致（含关键词路径）/表头 amount 降序首行=200）；全量回归 1320 passed、84 skipped、0 failed；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-016 | [P1] 采购价格分析报表仍走「全量物化再内存分桶」老路：与 BUG-2026-09-07-013/014 同源——它与采购订单执行、供应商采购汇总、物料采购汇总**共用**采集器 `_collect_purchase_order_execution_rows`，受 `REPORT_ROW_LIMIT`(5 万) 截断；原内存路径先物化全部采购明细行再在 Python 里按（物料 × 供应商）分桶，汇总卡片（采购数量/金额）正是对**截断后的分桶结果**求和——长周期查询出现「采购金额比实际小」，违反 R2「汇总 = 明细全集」。这是**共用该采集器的最后一张报表**，本修复后整条采集器风险线关闭 | **已修复（2026-09-07）**：新增 `_sql_paged_purchase_price_analysis_report` 并注册进 `SQL_PAGED_REPORT_BUILDERS`——①分桶与聚合下沉 SQL `GROUP BY`（物料编码/名称/规格/供应商），只产出「组」行（行数=物料×供应商组数），不再物化明细行；②汇总（数量/金额）由 SQL 全量聚合，与分桶/分页/排序彻底解耦（R2 汇总=明细全集）；③复用 `_purchase_order_item_query` 作为 WHERE 口径单一事实源；④warehouse 分支 outerjoin 入库单会展开同一采购行，先按所需列 `DISTINCT` 子查询再聚合，防数量/金额被展开放大（同款防护）；⑤min/max/avg/price_range 与 `_safe_float(price)` 口径一致（price 可空默认 0 → SQL 侧 coalesce 按 0 参与，与内存一致）；⑥**last_price/last_order_no/url 复刻内存路径「最新一行」语义**：内存用 `row_date >= last`（**含等号**）逐行覆写而明细按 (日期,单号,明细ID) 倒序 → 同一最大日期内**单号最小、明细ID最小**者胜出（与 009-07-014 物料汇总严格 `>` 取最大单号**相反**，两条路径语义已被 T8 专项锁定）；不引入窗口函数，用**两次 MIN 回连**复刻（最大日期 → 该日期内最小单号 → 该单号内最小明细ID → 按明细ID取行）；⑦无供应商行聚合在空名组、展示为「未指定供应商」（与内存一致）；⑧默认排序显式化为最近采购日期倒序（同日期按物料编码/供应商升序稳定兜底）。回归：`tests/test_bug_2026_09_07_016_purchase_price_sql_pagination.py` 8/8（分页语义/汇总解耦且各页加总==汇总 R2/默认最近采购日倒序/金额降序/仓库必填+多仓隔离（B 仓 777 不串入 A 仓）/API 元数据 total_pages 且 truncated=False/双路径分桶一致（含 5 组全字段复核）/同日期取最小单号专项）；全量回归 1312 passed、84 skipped、0 failed；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**收口声明**：采购执行+三类采购汇总+价格分析 5 张共用该采集器的报表现已全部 SQL 分页化，采集器 5 万截断风险线整体关闭。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-015 | [P1] 库存报表每次查询/翻页/改排序都重复做全量计算，其中两处根因可消除且均为实测证据：①`_collect_inventory_rows` 对 material 的 category/unit/supplier 三个 many-to-one 关系靠懒加载——物料分属不同供应商时逐行触发查询（实测 2000 物料各配不同供应商 → **2008 条 SQL / 0.380s**，同供应商仅 9 条 / 0.159s）；②`get_warehouse_stock_quantities` 的空 location 回补把「`warehouse_id IS NULL` 且 location 为空」的**全部**历史流水加载进 Python（实测 22330 行 / 0.476s，占库存报表单次查询 0.51s 的约 70%），再逐行解析来源单据归属——其中大量行**无来源单据**（reference_type/reference_id 为空），在 `_ledger_source_warehouse_map` 中必然被跳过（`_filter_txn_list_by_warehouse_scope` 三条保留条件对它们全为假），永远不可能归属任何仓库，纯属白扫。注：本报表**没有** REPORT_ROW_LIMIT 截断（无 `_report_check_row_limit`），不属于 R2「汇总失真」，也不适用 009-07-012/013/014 的「SQL 分页下沉」套路（行集=物料、行数可控，真正开销在仓库库存聚合），故**不做 SQL 分页伪优化** | **已修复（2026-09-07）**：①`_collect_inventory_rows` 统一 `selectinload(Material.category/unit/supplier)` 预加载（用 selectinload 而非 joinedload，避免与 supplier 关键词筛选的 `join(Material.supplier)` 产生连接冲突），实测 N+1 由 2008 条 SQL 降至个位数；②回补扫描在 SQL 侧剪枝 `reference_type IS NOT NULL AND reference_type != '' AND reference_id IS NOT NULL`——改前先用真实来源单据做**强等价性验证**：空 location 流水按「有来源单据属本仓（计入）/属他仓（不串入）/无来源单据（剪掉）」三组构造，断言剪枝前后保留集合**非空且完全一致**、库存数值精确匹配；实测该扫描由 22330 行 / 0.476s 降至 11971 行 / 0.081s（**-83%**）。回归：`tests/test_bug_2026_09_07_015_inventory_report_perf.py` 6/6（预加载不改口径/300 物料各配不同供应商时 SQL 语句 ≤ 20/无来源单据行不计入库存/回补归属精确且 B 仓 999 不串入 A 仓/库存报表多仓隔离/API 回归）；全量回归 1304 passed、84 skipped、0 failed；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移。**同类受益方**：`get_warehouse_stock_quantities` 为库存查询页/盘点/入库等共享函数，剪枝对所有调用方生效（语义等价，已证明） |
| BUG-2026-09-07-014 | [P1] 物料采购汇总报表仍走「全量物化再内存分桶」老路：与 BUG-2026-09-07-013 同源——它与采购订单执行、供应商采购汇总、采购价格分析**共用**采集器 `_collect_purchase_order_execution_rows`，受 `REPORT_ROW_LIMIT`(5 万) 截断；原内存路径先物化全部采购明细行再在 Python 里按物料（编码/名称/规格）分桶，汇总卡片（采购数量/金额）正是对**截断后的分桶结果**求和——长周期查询出现「采购金额比实际小」，违反 R2「汇总 = 明细全集」；默认排序还依赖明细行序的「首次出现顺序」，语义不确定 | **已修复（2026-09-07）**：新增 `_sql_paged_material_purchase_report` 并注册进 `SQL_PAGED_REPORT_BUILDERS`——①分桶与聚合下沉 SQL `GROUP BY` 物料（编码/名称/规格），只产出「物料桶」行（行数=物料数），不再物化明细行；②汇总（数量/金额）改由 SQL 全量聚合，与分桶/分页/排序彻底解耦（R2 汇总=明细全集）；③复用 `_purchase_order_item_query` 作为 WHERE 口径单一事实源；④warehouse 分支 outerjoin 入库单会展开同一采购行，先按所需列 `DISTINCT` 子查询再聚合，防数量/金额被展开放大（与 013 同款防护）；⑤`last_supplier` 需「分组内取最新一行」（内存路径靠明细按 日期/单号/明细ID 倒序的首次出现顺序隐式得到），**不引入窗口函数**（避免绑定 SQLite 版本、为后续迁移留余地），改用**三次分组回连**逐层收敛：分组最大日期 → 该日期内最大单号 → 该单号内最大明细ID → 按明细ID取供应商，各层用行值 `IN` 精确定位（已验证 SQLite 3.45 行值 IN 可用）；⑥`supplier_count`/`order_count` 按非空去重，口径对齐内存路径 `if row['supplier']` / `if row['order_url']`；⑦默认排序显式化为**最近采购日期倒序**（等价于内存路径首次出现顺序），同日期按物料编码升序稳定兜底；⑧单位为分桶键外取值，取分组内最小值以保证确定性（正常数据物料编码唯一、单位恒定；仅「重复编码但单位不同」脏数据下与内存路径首行取值可能不同，已在代码注释标注）。回归：`tests/test_bug_2026_09_07_014_material_purchase_sql_pagination.py` 8/8（分页语义/汇总解耦且各页明细加总==汇总 R2/默认最近采购日倒序/金额降序/仓库必填+多仓隔离（B 仓 777 不串入 A 仓）/API 元数据 total_pages 且 truncated=False/双路径分桶一致/**last_supplier 同日两单按单号倒序取大者**）；全量回归 1298 passed、84 skipped、0 failed；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**同类消费点（R6 已登记、不重复报）**：`purchase_price_analysis` 仍共用同一采集器、存在同一缺陷，已排入本轮迁移后续动作（需 min/max/最新价，与物料/供应商分桶模板不同，不并入本条）。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-013 | [P1] 供应商采购汇总报表仍走「全量物化再内存分桶」老路：它与采购订单执行、物料采购汇总、采购价格分析**共用**采集器 `_collect_purchase_order_execution_rows`，该采集器受 `REPORT_ROW_LIMIT`(5 万) 截断；供应商汇总先物化全部采购明细行再在 Python 里按供应商分桶，汇总卡片（采购数量/金额）正是对这份**截断后的分桶结果**求和——长周期或多供应商查询会出现「采购金额比实际小」，违反 R2「汇总 = 明细全集」。另：默认排序依赖内存路径「按订单日期倒序的首次出现顺序」，语义不确定 | **已修复（2026-09-07）**：新增 `_sql_paged_supplier_purchase_report` 并注册进 `SQL_PAGED_REPORT_BUILDERS`——①分桶与聚合下沉 SQL `GROUP BY 供应商`，只产出「供应商桶」行（行数=供应商数），不再物化明细行；②汇总改由 SQL 全量聚合（count/quantity/amount），与分桶/分页/排序彻底解耦（R2 汇总=明细全集）；③复用既有 `_purchase_order_item_query` 作为 WHERE 口径单一事实源；④因采购订单无仓库字段、靠关联入库单归属（仓库分支 outerjoin 会展开同一采购行），先对所需列做 `DISTINCT` 子查询再聚合，防止数量/金额被入库行数放大（与 009-07-011 采购执行同款防护）；⑤`remaining`/`amount`/`overdue` 口径与 `_purchase_execution_row` 逐条对齐（剩余量单行下限 0、金额为 0/NULL 回退「数量×单价」、逾期需 未收完且未 completed/closed 且已过预计到货日）；⑥供应商编码沿用内存路径「按名称查 Supplier 表」语义；⑦默认排序由「首次出现顺序」改为**最近采购日期倒序**（语义等价且确定）。回归：`tests/test_bug_2026_09_07_013_supplier_purchase_sql_pagination.py` 7/7（分页语义/汇总解耦且各页明细加总==汇总 R2/默认最近采购日倒序/金额降序/仓库必填+多仓隔离（B 仓 777 不串入 A 仓、汇总不被入库单展开放大）/API 元数据 total_pages 且 truncated=False/双路径分桶一致）；全量回归 1290 passed、84 skipped（标签条码域 6 项失败经核实为沙箱缺 `python-barcode`/`qrcode` 依赖，装回后 20/20 通过，与本次改动无关）；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**同类消费点（R6 已登记、不重复报）**：`material_purchase_summary` 共用同一采集器、同一缺陷，已由 **BUG-2026-09-07-014** 修复（其 `last_supplier` 需「分组内取最新一行」，语义风险高于本条，故未并入本条一并改动）；`purchase_price_analysis` 仍存在同一缺陷，已排入本轮迁移后续动作（需 min/max/最新价，与物料/供应商分桶模板不同）。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-012 | [P1] 出入库汇总报表仍走「全量物化再内存分桶」老路：BUG-2026-09-07-004/011 只下沉了入库/出库/盘点/采购执行四类明细报表，`summary` 仍把入库明细与出库明细**各**物化最多 5 万行再在 Python 里按日期分桶——它是唯一同时消费两条明细流的报表，**最先触达 REPORT_ROW_LIMIT 截断**，且汇总卡片基于截断后的分桶结果计算（违反 R2「汇总 = 明细全集」）；默认排序还把「仅出库日期」追加在所有入库日期之后，并非全局有序 | **已修复（2026-09-07）**：新增 `_sql_paged_summary_report` 并注册进 `SQL_PAGED_REPORT_BUILDERS`——①分桶下沉为 SQL `GROUP BY date`，只产出「日期桶」行（行数=期间天数），不再物化明细行；②汇总改由 SQL 聚合筛选后全集（count/quantity/amount），与分页/分桶解耦（R2 汇总=明细全集）；③复用既有 `_filtered_in_detail_query`/`_filtered_out_detail_query`，保证 WHERE 口径单一事实源，并保留「选定供应商时出库侧不参与」原语义（不引入行为漂移）；④`net_quantity`/`net_amount` 为 in−out 计算列，在日期桶这个极小结果集上用 Python 排序（SQL 侧排序需子查询包裹，成本不划算）；⑤默认排序由「首次出现顺序」改为**日期倒序**（原顺序非全局有序，一并按期修正）。回归：`tests/test_bug_2026_09_07_012_summary_sql_pagination.py` 7/7（分页语义/汇总解耦且各页明细加总==汇总 R2/默认日期倒序/净变动排序/仓库必填+多仓隔离/API 元数据 total_pages/双路径分桶一致）；报表域 108 项、库存台账月报多仓 348 项全过；`scripts/lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 均 0 违规。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-011 | [P1] 盘点/采购订单执行报表仍走「全量物化再内存切片」老路：BUG-2026-09-07-004 只下沉了入库/出库明细，盘点明细与采购订单执行每次查询/翻页/排序仍全量物化（最多 5 万行）再内存切片，翻一次页重算一次全量；采购执行汇总若直接 sum 还会被 warehouse 过滤分支的入库单 outerjoin 按入库行数放大（金额/数量失真） | **已修复（2026-09-07）**：①行映射提炼 `_check_row`/`_purchase_execution_row`（内存路径与 SQL 路径单一映射源，防两份映射漂移）；②盘点新增 `_filtered_check_query` 共用 WHERE 构造 + `_CHECK_SORT_MAP` 12 列排序映射；③`_purchase_order_item_query` 统一 outerjoin Supplier/Unit/PurchaseRequest（supplier 关键词筛选由 INNER JOIN 改 outerjoin+filter，语义等价），排序移交调用方，`_PURCHASE_EXECUTION_SORT_MAP` 20 列排序映射（overdue_days 与 expected_date 同序：逾期越久日期越小）；④`_sql_paged_check_report`/`_sql_paged_purchase_execution_report`：SQL count 真实总数、with_entities 聚合 summary 与分页解耦（R2 汇总=明细）、offset/limit 只取当页；采购执行汇总基于 distinct 子查询（id/quantity/price/amount），不被入库单展开行放大；⑤注册进 `SQL_PAGED_REPORT_BUILDERS`，导出仍走 5 万上限 + 截断透传。回归：`tests/test_bug_2026_09_07_011_check_purchase_sql_pagination.py` 8/8（盘点分页/汇总解耦+仓库必填/排序下沉、采购执行分页+R2 隔离/汇总不放大/排序+supplier 关键词、双路径行级一致×2、API 元数据）；报表域 257 项全过。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-010 | [P3] 采购报表页把「缺失报表差距清单」公示给最终用户：/purchase_report 列出 6 项「缺少报表 / 当前差距 / 优先级」（自曝其短），产品路线图应在开发台账跟踪而非业务页面展示 | **已修复（2026-09-07）**：模板撤下差距清单区块，路由不再构造/传 missing_reports；可用报表卡片不受影响。回归：`tests/test_bug_2026_09_07_010_purchase_report_no_gap_list.py` 3/3（清单移除/可用卡片保留/路由锚点）。**生效条件**：后端 + 模板改动，拉取后**重启 WMS 服务生效**（R3），浏览器 Ctrl+F5 强刷 |
| BUG-2026-09-07-009 | [P2] 报表中心卡片「导出」裸链接：不带 warehouse_id——有默认仓库时用户无感知导出"默认仓库全量"；无默认仓库时后端按仓库必填返回 400 JSON，浏览器直接下载一个错误页 | **已修复（2026-09-07）**：/report 路由传 default_warehouse；卡片导出显式带 warehouse_id=默认仓（title 注明「按默认仓库全量导出，条件导出请进预览页」）；无默认仓库时按钮禁用并提示「请先进预览页选择仓库后导出」，不再下载 JSON 错误页。回归：`tests/test_bug_2026_09_07_009_report_center_export_button.py` 3/3（有默认仓链接带 id/无默认仓禁用提示/路由传参锚点）。**生效条件**：后端 + 模板改动，拉取后**重启 WMS 服务生效**（R3），浏览器 Ctrl+F5 强刷 |
| BUG-2026-09-07-008 | [P1] 报表「打印」只打当前页：打印按钮原是 window.print() 直接打印屏幕，只含当前页 20/50/100 行，用户要打印完整查询结果只能逐页打印或先导出 Excel 再打印（/report/print 路由也一直返回"未实现"） | **已修复（2026-09-07）**：打印按钮改为——①按 R1 规则（显式 page_size=500 + total_pages 循环）拉取当前筛选的**全部**数据；②渲染到屏幕隐藏的 #printArea（报表标题/打印时间/筛选条件/汇总/全量表格）；③再 window.print()，@media print 只显示打印区（隐藏筛选面板/汇总卡/报表区）；④超 MAX_PRINT_ROWS(10000) 截断并提示改用导出 Excel，防浏览器卡死；⑤错误提示走页面 alert 条不用 alert（A4）。回归：`tests/test_bug_2026_09_07_008_report_print_full_rows.py` 4/4（容器/全量拉取/打印 CSS/旧绑定移除）。**生效条件**：模板改动，拉取后**重启 WMS 服务生效**（R3），浏览器 Ctrl+F5 强刷 |
| BUG-2026-09-07-007 | [P2] 入库明细报表「业务类型」筛选前端无控件：后端自 BUG-2026-08-18-004 起支持 business_type 筛选（采购入库/产品入库/其他入库），report_view.html 却一直没渲染对应控件，用户想"只看产品入库"只能手改 URL | **已修复（2026-09-07）**：report_view.html 筛选表单在 report_type == 'in_detail' 时新增「业务类型」下拉（全部/采购入库/产品入库/其他入库），随 FormData 提交，与既有后端过滤直接打通；出库明细等其他报表不渲染。回归：`tests/test_bug_2026_09_07_007_in_detail_business_type_filter.py` 4/4（模板锚点/入库页渲染/出库页不渲染/API 端到端筛选）；存量 business_type 后端测试零回归。**生效条件**：模板改动，拉取后**重启 WMS 服务生效**（R3），浏览器 Ctrl+F5 强刷 |
| BUG-2026-09-07-006 | [P2] 库存台账无期初/期末对账行：按日期范围查询时，start_date 前流水虽计入结存（跳过但累计），但「期初是多少、本期入/出合计多少、期末多少」均不可见，对账只能手工加减流水列 | **已修复（2026-09-07）**：`_collect_ledger_rows` 新增标记行——①「期初结存」行（仅指定 start_date 时）：期初 = start_date 前累计余额，date=start_date+最小时间 + _txn_id=-2，默认日期排序下排同物料最前；②「本期合计」行：期初 + 本期入 − 本期出 = 期末结存，date=end_date+最大时间 + _txn_id=-1，排同物料最后；两行带 material_id，summary 期末结存口径不变。R6 同类点排查：全 tests 直接消费台账行的 8 个文件全部适配（求和/行数断言排除标记行，合计行参与求和会翻倍）。回归：`tests/test_bug_2026_09_07_006_ledger_opening_closing_rows.py` 4/4（范围期初/合计位置与数值、无 start_date 无期初行、summary 不回归、多物料分组位置）；台账/月报/报表域 53 项全过。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-005 | [P2] 仓库月报金额失真：in_amount/out_amount 一律「数量 × 物料当前单价」估算（代码自述"金额按物料当前单价估算"），历史月份单价变动后金额不可信——例：上月采购价 10 元入库 100 件，本月物料调价 99 元后，上月月报入库金额被显示为 9900 而非 1000 | **已修复（2026-09-07）**：新增 `_monthly_txn_price_map()` 批量解析流水实际单价——按 reference 反查来源单据明细加权单价（sum(amount)/sum(quantity)，in_order / out_order / after_sale_out_order 三类，每类一次分组聚合查询避免 N+1）；月报聚合循环按流水单价累计 in_amount/out_amount；无单据价的类型（transfer/adjustment/check/opening/requisition 等）与解析不到的行用物料当前价兜底；期末库存金额仍按当前价估值（库存估值口径，行 remark 已更新为「出入库金额按来源单据实际单价，期末库存金额按当前单价估值」）。回归：`tests/test_bug_2026_09_07_005_monthly_report_actual_price.py` 5/5（入/出/售后按单据价、调整兜底、期末估值+remark 口径）；月报与仓库报表域回归 22/22 全过。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-004 | [P1] 入库/出库明细报表「翻页 = 全量重算」：原路径每次查询/翻页/排序都全量物化（最多 5 万行）再内存切片，数据量大时每点一次翻页都重新全表扫描 + 构造数万 dict；汇总卡片与列表共享同一份截断数据，超限时汇总失真 | **已修复（2026-09-07）**：count/聚合/排序/分页全部下沉 SQL——①`_collect_in/out_detail_rows` 拆出共用件：行映射 `_in_detail_row`/`_out_detail_row`（内存路径与 SQL 路径单一映射源，防漂移）+ WHERE 构造 `_filtered_in/out_detail_query`（出库客户/部门关键词筛选由 Python 循环下沉为 Department.name/OutOrder.customer/OutOrder.purpose 三字段 ilike，语义等价）；②新增 `_sql_paged_in/out_detail_report`：SQL count 真实总数（翻页不再截断失真）、with_entities 聚合 summary（与分页解耦，R2 汇总=明细全集）、`_IN/OUT_DETAIL_SORT_MAP` 19+14 列排序映射 + 行 id tiebreaker、offset/limit 只取当页；③`SQL_PAGED_REPORT_BUILDERS` 注册表 + `_build_report_payload` SQL 分支（total=真实总数、total_pages 按真实总数、导出仍走 5 万上限 + 截断透传）。盘点/采购执行等其余报表下一批下沉。回归：`tests/test_bug_2026_09_07_004_report_sql_pagination.py` 7/7（分页不重叠/汇总跨页一致/降序/客户三通道/仓库必填+多仓隔离 R2/API 元数据/SQL 与内存路径行级一致）；BUG-2026-09-07-003 T1/T3 随架构演进（展示无截断、截断只剩导出与未下沉报表）4/4；报表域 134 项全过。**生效条件**：纯后端改动，拉取后**重启 WMS 服务生效**，不涉及模板与数据库迁移 |
| BUG-2026-09-07-003 | [P1] 报表查询结果超上限被静默截断：入库/出库/盘点/采购执行明细超 REPORT_ROW_LIMIT(50000)、台账流水超 LEDGER_ROW_LIMIT 时仅写后端日志，页面 total 显示的是截断后行数、汇总卡片也按截断数据统计，用户不知道数据不完整（违反 R2「汇总 = 明细」）；报表 API 同时缺 total_pages 分页元数据（违反 R1 分页元数据完整性） | **已修复（2026-09-07）**：①新增 `_report_check_row_limit()` 统一截断检查——先 count 真实行数，超限记 warning 并把真实行数写入 flask.g（多次截断取最大值），5 处截断点（入库/出库/盘点/采购执行/台账流水）全部收拢同一口径；②`_build_report_payload` 透传 truncated / raw_total / total_pages（读后即清 g 防同一上下文串值），`/report/api/<type>` 响应同步携带；③report_view.html 截断时表格上方显示警告条「查询结果共 M 条，超过单次处理上限，仅显示前 N 条，上方汇总数据也按前 N 条统计。请缩小日期、仓库或物料范围」，错误分支自动隐藏。R6 同根因排查：全仓 limit 截断点仅此 5 处已统一，其余报表（库存/月报/出入库汇总/委外/工单领料）无 limit 截断、不涉及。回归：`tests/test_bug_2026_09_07_003_report_truncation_notice.py` 4/4；`test_bug_2026_08_29_002` T2/T3 锚点随实现收拢演进（行为不变）5/5；报表域回归 44 项 + 筛选/导出/移动报表 84 项全过。**生效条件**：后端 + 模板改动，拉取后**重启 WMS 服务生效**（R3），浏览器 Ctrl+F5 强刷 |
| BUG-2026-09-07-002 | [P1] 手机 APP 语音指令同音误识别无约束：用户实测说「领料」被识别成「饮料」、说「入库」被识别成「欲哭」，弹窗报「未识别到可执行指令」。根因三层：①服务端 `/mobile/api/asr` 热词表只有入库/出库权重 100，领料/盘点/查库存等核心指令词仅权重 5~11，同音替换约束力不足；②识别文本没有任何领域词纠正，腾讯云/系统识别/sherpa 的同音误识别原文直接进指令解析；③`parseCommand` 没有「领料」分支——即使识别正确也无法执行 | **已修复（2026-09-07）**：三层防御，识别内容只允许收敛到仓库领域词——①服务端热词表核心指令词全部提至权重 100（同音增强替换）并扩充词表（领料/盘点/查库存/库存/识物/识别/期初/送货单/退货/调拨）；②`app/tencent_asr.py` 新增 `correct_voice_asr_text()` 识别后按领域词表强制纠正（饮料→领料、欲哭→入库、玉库/入裤/如库/乳库→入库、出裤/初库/楚库→出库、裤存/酷存→库存、盘店/潘点/判点→盘点、食物→识物、起初→期初、调播→调拨、扫马→扫码），`mobile_asr` 路由双保险应用；③Android `VoiceCommandViewModel.kt` 同款词表在 `onResult` 先纠正再解析（覆盖系统识别/sherpa 本地路径），并新增 领料→出库 指令分支。安全边界：不收「推出→退出」（退出登录破坏性操作，防口误误触发登出）。回归：`tests/test_bug_2026_09_07_002_voice_asr_domain_correction.py` 3/3（纠正函数全集+路由纠正接线+热词权重全 100）、`tests/verify_bug_2026_09_07_002_voice_domain_correction.py` 6/6（含双端词表一致性门禁，改词表必须双边同步）；存量 `verify_mobile_asr_route.py`+`verify_tencent_asr_helper.py` 18/18 零回归。**发布边界**：服务端改动拉取重启 WMS 生效；Android 侧 GitHub Actions 产出新 APK 后用户手机重新下载安装生效 |
| BUG-2026-09-07-001 | [P2] 手机 APP 语音指令聆听窗口 15 秒过长：用户要求改为 8 秒。此前 AI-MOB-APK-003（2026-08-30）曾因现场反馈「一直超时」把兜底阈值 8s→15s，但彼时云引擎尚无 VAD；现 `CloudAsrVoiceSttEngine` 已具备 VAD-lite（说话停顿 1.2 秒即自动结束录音并立即上传识别），正常指令 2~4 秒说完即出结果，15 秒窗口只让"忘记停顿"的用户干等 | **已修复（2026-09-07）**：`VoiceCommandViewModel.kt` `VOICE_LISTEN_TIMEOUT_MS` 15_000L→8_000L（用户明确要求，取代 AI-MOB-APK-003），超时提示文案同步改「请在点按麦克风后 8 秒内说出指令」；倒计时 UI（剩余 N 秒）随常量自动变为 8 秒起倒数。回归：`tests/verify_bug_2026_08_09_003_voice_listen_timeout.py` 断言阈值更新为 8000 并重写 T2b 门禁（固定 ==8000，注明取代关系），10/10 PASSED。**发布边界**：GitHub Actions 产出新 APK 后用户手机重新下载安装生效 |
| BUG-2026-09-06-003 | [P1] 盘点完成路径在生产模块布局下必然 500：BUG-2026-09-06-002 引入的 `_check_period_movement_alerts`（app/routes/check.py）写 `from app.utils import normalize_stock_quantity`。本仓库 **app/ 目录即包根**（app.py 为顶层模块、无 __init__.py），真实启动布局（python app.py / run_server.py，app 目录在 sys.path[0]）下 `app` 是普通模块而非包，`from app.utils import` 必然 ModuleNotFoundError → **每次无 force 的「完成盘点」（凡无未盘行走到动销校验，如空单+手机扫码全盘完的抽盘主流程）生产 500**。既有测试未抓出根因：pytest 把仓库根插进 sys.path（tests 是包→rootdir 插入父目录），`app` 被当命名空间包，`app.utils` 恰好可导——**测试与生产的导入布局不一致，错误被掩盖**（交叉冲突检验按生产布局直跑现形） | **已修复（2026-09-06，commit 本次提交）**：改仓库统一写法 `from app import StockTransaction, normalize_stock_quantity`（app.py 顶层 `from utils import (...)` 已转导出，与全仓其余消费点一致）。R6 同模式排查：全仓 grep `from app.<子模块> import` 仅 2 处——本处 + `app/ai/tools/navigation.py` skill_catalog 内`from app.ai.tools.registry import get_all_tools`（AI 技能清单路由 v2_skill_catalog 调用时同样生产炸，ai 是真实包 `app/ai/__init__.py`，改为 `from ai.tools.registry import`，任何布局可解析），两处同 commit 修复。回归：`tests/test_bug_2026_09_06_003_period_movement_import_layout.py` 2/2 PASSED（T1 全仓静态契约禁止 `from app.<子模块> import` 防同类复发；T2 子进程按**生产模块布局**（cwd=app 目录直跑）完成空单+手机全盘→无 force 必须 success——修复前 500；T3 同布局下动销校验仍正确命中 period_movement）；既有 `test_bug_2026_09_06_002_check_period_movement.py` 7/7 零回归。**生效条件**：纯代码改动，拉取重启 WMS 服务即生效 |
| BUG-2026-09-06-002 | [P1] 完成盘点不校验期间动销，实盘数过期后按冻结口径过账导致账实不符：INV-BATCH-001-B 引入冻结快照语义后，调整量 = 实盘 − 冻结账面（`inventory_check_item.system_stock`）。设冻结时点账面 F、期间净出库 Δ、实盘 P，则过账后库存 = (F − Δ) + (P − F) = **P − Δ**，只要 Δ≠0 库存就不等于实盘数 P（少调 Δ）。精确风险窗口是**该行 `counted_at` 之后**的出入库——实盘后又被领走/入库，行内实盘数已过期；而 `complete_check` 既不拦截盘点期间的出入库，也不扫描此窗口，更无任何提示。分类建单（BUG-2026-09-05-003）一次性预生成上千行冻结口径行，与手机补盘组合时最易踩中 | **已修复**（2026-09-06，commit ea8c78d/6bddfdca + ec24121/52dcb142）：`_check_period_movement_alerts(check)` 按物料 SUM 本盘点单 frozen_at→now 区间内、本仓库 `warehouse_id` 命中的流水净变动（NULL reference 安全：拆 OR 不走 NOT AND 三值逻辑；跨仓库 / 历史 NULL warehouse 不计入；frozen_at 为 NULL 放行历史脏数据；自身 reference 防御），任一 |net| > 0 即 `status='confirm' code='period_movement'` 软拦截 + samples（物料编码 + 净变动数，前端绿/红着色），`force=1` 放行。R6 同类点排查：冻结口径仅盘点单使用（`InventoryCheckItem.system_stock`），扫码行走即时口径不受影响，无其他消费点。回归 7 项 `tests/test_bug_2026_09_06_002_check_period_movement.py` |
| BUG-2026-09-06-001 | [P1] 完成盘点无漏盘校验，「没盘」与「盘了无差异」在系统里是同一状态：分类建单（BUG-2026-09-05-003）预生成的待盘行 `actual_stock = system_stock`、`difference = 0`，`counted_by/counted_at` 为空；`complete_check`（`app/routes/check.py`）只校验「明细非空 + 有仓库」，对 `counted_at` 为空的行既不拦截也不提示，这些行因差异恒 0 不生成任何调整。结果是：一单 500 行只盘了 50 行也能直接「完成」，系统无任何提示，`_build_check_batch_meta` 也只统计 row_count / material_count / diff_row_count，**没有已盘/未盘计数**，主管只能逐行肉眼看「盘点人/时间」列核对漏盘。漏盘是盘点最大风险源，盘点单可在「一半物料没盘」状态下被标记为已完成并生成调整单，事后无法与真实无差异区分 | **已修复**（2026-09-06，commit e7e67d1 + 81e6f56）：`_check_uncounted_alerts(check)` 判定 `counted_at` 与 `counted_by` 至少其一为空即视为未盘（手机扫码与 `/check/<id>/item/<item_id>` 均写这两个字段，纯 PC 录入视为未盘是预期的"用户主动确认"路径）；`complete_check` 前置校验未盘行数 > 0 返回 `status='confirm' code='uncounted'` + samples（物料编码+名称），前端弹窗列出，支持「仍要完成」带 `force=1` 放行（软拦截，不硬阻断：账面 0 无实物等合法未盘场景仍可完成）。连带：`_create_adjustment_drafts_from_check` 在 PC 路径上也补上 INV-GUARD-001 同仓库同物料重复盘点护栏（BUG-2026-09-02-002 旧实现只覆盖 scan 路径）。R6 同类点排查：未盘语义仅由 `counted_at/counted_by` 表达，消费点 `complete_check`（完成）已覆盖；`_create_adjustment_drafts_from_check` 调整生成路径同步收紧。回归 6 项 `tests/test_bug_2026_09_06_001_check_uncounted_guard.py` + 2 老测试适配 `test_inv_batch_001b_check_freeze_upsert.py::test_t4` 与 `test_check_add_category_generate.py::test_t5` |
| BUG-2026-09-05-008 | [P2] 手机盘点不能填行级差异原因：明细行有 reason 字段、PC 表格与 Excel 导入都能填，但手机扫码只有「盘点区域」，盘出差异想当场备注（破损/串货/待查）只能回 PC 补填 | **已修复（2026-09-05，commit 本次提交，台账子修复 ID FEATURE-2026-09-05-004）**：①`mobile.py` scan_submit(check) 读取 payload `reason` 并透传 `_apply_scan_to_batch(..., reason=reason)`；②`_apply_scan_to_batch`：PC 预置行被补盘时写 `row.reason`（**空值不覆盖** PC 已填原因）；扫码新建行保留「手机扫码盘点」哨兵前缀、原因以「：」追加（`手机扫码盘点：破损3件`）；③**关键联动**：`_void_check_scan` 原按 `row.reason == '手机扫码盘点'` 精确相等识别本次新建行，带了原因会误判为 PC 预置行、撤销时只重置不删除 → 留下孤儿未盘行；改为前缀判定 `str(row.reason or '').startswith('手机扫码盘点')`（纯哨兵仍匹配，向后兼容）；④`mobile_scan.html` 盘点模式加「差异原因（可选）」输入框，随每笔扫码清空（原因只属当次差异）。回归：`tests/test_bug_2026_09_05_008_mobile_check_reason.py` 5/5 PASSED（预置行写原因 / 空值不覆盖 PC 原因且撤销重盘后覆盖 / 新建行哨兵+原因 / **带原因的扫码行撤销仍整行删除而非重置** / 模板契约）；既有 `test_inv_revert_001_check_scan_void.py` 8/8、`test_inv_batch_001c_scan_hooks_batch.py` 7/7 零回归。**生效条件**：后端 + 模板改动，拉取代码后**重启 WMS 服务生效**（R3），手机浏览器强刷 |
| BUG-2026-09-05-007 | [P1] 手机盘点看不到"还剩哪些没盘"：盘点人只拿手机时，选完盘点单后只有扫码入口，无进度与待盘清单——漏盘只能回 PC 详情页核对，多人分区盘点时极易漏盘；选单下拉也只有明细条数、无进度 | **已修复（2026-09-05，commit 本次提交，台账子修复 ID FEATURE-2026-09-05-003）**：①`app.py::_list_pending_check_orders` 增 `counted_count`（counted_at 非空行数，Android/H5 列表共用），H5 选单下拉选项直接显示「已盘 X/Y」；②新增 `GET /mobile/api/check_orders/<int:check_id>/items`（`_web_or_api_role_required('warehouse')`，盘点单不存在 404）：返回 total/counted 与明细清单（未盘在前、已盘在后，按编码+区域序）。**盲盘设计**：接口不下发各行 `system_stock`——盘点人看到账面会照着填，盘点失去意义；已盘行回传实盘与盘点人/时间供自查，未盘行只给物料识别信息（`actual_stock=null`）；③`mobile_scan.html` 选单后显示「已盘 X/共 Y」进度条（盘完加"已全部盘完"）与折叠式待盘清单（前 30 项 + 其余计数），扫码提交成功与撤销成功后自动刷新。回归：`tests/test_bug_2026_09_05_007_mobile_check_progress.py` 6/6 PASSED（列表 counted_count / 进度+盲盘不下发账面+已盘行带人时间 / 未盘排前 / 不存在 404 / 扫码后计数递进且手机新增行同样带盘点归属 / 模板契约含刷新与盲盘注释）；既有 `test_inv_batch_001e_mobile_check_required.py` 6/6 零回归。**生效条件**：后端 + 模板改动，拉取代码后**重启 WMS 服务生效**（R3），手机浏览器强刷（H5 页面缓存） |
| BUG-2026-09-05-006 | [P2] 盘点单导出无法核对漏盘：详情页早有行级盘点归属回查（INV-BATCH-001-D，盘点人/时间列），`/check/<id>/export` 导出却只有账面/实盘/差异——Excel 里筛不出"未盘行"；且末列表头写「备注」实际写入的一直是 `item.reason`（差异原因），表头与内容不符 | **已修复（2026-09-05，commit 本次提交）**：`app/routes/check.py` `export_single_check` 表头补「盘点人」「盘点时间」（已盘行写 counted_by 用户名 + counted_at，未盘行留空——Excel 直接筛空值即得漏盘清单），末列正名「差异原因」（与导入别名 `差异原因/原因` 对齐，导出文件可直接回导，额外列被导入忽略无副作用）。回归：`tests/test_bug_2026_09_05_006_check_export_counted_cols.py` PASSED（12 列表头无「备注」/ 已盘行导出人与时间 / 未盘行两列空 / 差异原因内容正确）。**生效条件**：纯代码改动，拉取重启 WMS 服务即生效，不涉及数据库迁移 |
| BUG-2026-09-05-005 | [P1] 整仓大盘点无一键建单：FEATURE-2026-09-05-001 只支持"按一个分类"生成待盘明细，季度/年度整仓全盘需逐分类各建一单（手机端来回换单）或退回 Excel 导入老路 | **已修复（2026-09-05，commit 本次提交，台账子修复 ID FEATURE-2026-09-05-002）**：①`check.html` 新建弹窗「盘点分类（可选）」升级为「盘点范围（可选）」三选——不预生成（空单，手机扫码自动加行）/ 整仓全部物料（全盘）/ 按分类生成（含子分类），选按分类才显示分类选择器、切走自动清空已选分类；②`add_check` 接收 `scope=all`：为**全部物料**（含无分类）按编码序生成"未盘"明细行，行语义与按分类完全一致（system=该仓仓库级账面、actual=system、diff=0、counted_at 空、建行即冻结 frozen_at）；scope 与 category_id 同时给出时 scope 优先；整仓/分类统一走 `import_max_rows()` 上限保护（超限提示按分类分批建单，不落单）；系统无物料时建空单并明确提示。回归：`tests/test_check_add_scope_all.py` 5/5 PASSED（全部物料建行含无分类+仓库级账面+未盘语义+冻结 / 超上限 400 不落单 / 无物料空单提示 / scope 优先于 category_id / 模板契约含三选项与联动清空 JS）；既有 `test_check_add_category_generate.py` 7/7 零回归。**生效条件**：后端 + 模板改动，拉取代码后**重启 WMS 服务生效**（R3），浏览器 Ctrl+F5 强刷 |
| BUG-2026-09-05-004 | [P1] 物料导出忽略分类筛选：物料列表页左侧分类树选「电线」后点「导出」（app.js `buildCurrentFilteredUrl` 会把 `category_id` 拼进导出 URL），`export_material` 却只读 search/stock_filter/sort/order，**从不读 category_id**——导出得到全量物料而非所选分类，页面与导出结果不一致；用户想"导出某类物料建盘点表"被静默误导（R6：与 BUG-2026-08-29-003「页面筛选条件未被导出接口接收」同根因在物料域的消费点） | **已修复（2026-09-05，commit 本次提交）**：`app/routes/material.py` `export_material` 读取 `category_id` 并**逐字镜像 `material_list` 的分类子树展开**（`build_category_tree_rows` + descendants 反推，`Material.category_id.in_(子树 ids)`），选父分类导出含全部子分类物料、选叶子只含叶子，页导口径一致；导出超限闪回 `material_list` 的重定向同步携带 `category_id` 不丢筛选。R6 同类点排查：全仓仅物料页有分类树筛选（category/unit/supplier 等基础资料导出无分类维度），无其他消费点。回归：`tests/test_bug_2026_09_05_004_material_export_category.py` 4/4 PASSED（带分类仅该类含子分类 / 不带全量 / 分类+关键词叠加 / 叶子分类不含父类物料）。**生效条件**：纯代码改动，拉取重启 WMS 服务即生效，不涉及数据库迁移 |
| BUG-2026-09-05-003 | [P1] 盘点单无法按分类生成待盘明细，"只盘某一类"无系统路径：自 BUG-2026-09-04-005 手机盘点强制挂 PC 盘点单后，想"只盘电线这一类"只能①Excel 导出物料手工整理再导入（踩 BUG-2026-09-05-004 的坑且单号/实盘列填错即每行一张单或全盘亏）②逐行加明细——且**没有待盘清单就没有漏盘核对**（手机扫码只加行，漏盘一种系统不提醒）。用户原话「为什么不在新建盘点单支持选分类？」 | **已修复（2026-09-05，commit 本次提交，台账子修复 ID FEATURE-2026-09-05-001）**：①`app/templates/check.html` 新建盘点单弹窗新增「盘点分类（可选）」——复用 quick-select（`data-ks="category"`，候选接口 `/api/options/category` 现成），选中写 hidden `category_id` 随 FormData 提交，创建成功 toast 展示后端返回的生成行数；②`app/routes/check.py` `add_check` 接收 `category_id`：分类不存在 400；模块级新增 `_expand_check_category_ids()`（BFS 展开含子分类，seen 集合防 parent_id 脏数据成环死循环）；为分类下**全部物料**（含账面 0——账面 0 实物有货正是要盘出的盘盈，业务决策已经用户确认）预生成"未盘"明细行：`system_stock`=该仓库仓库级账面（`get_warehouse_stock_quantities`，与导入/表格同口径）、`actual_stock`=system、`difference`=0、`counted_by/counted_at` 空——完成盘点时未盘行差异恒 0 不生成调整，详情页按「盘点人/时间」列一眼核对漏盘；建行即写 `frozen_at` 冻结账面（与 save_check_table 首次写入语义一致）；物料数超 `import_max_rows()`（默认 3000）报错提示改用更细分类，不落单；不选分类保持原行为（空单，手机扫码自动加行）。使用路径：新建盘点单选「电线」→ 手机 `/mobile/scan?mode=check` 选该单逐个扫 → PC 完成盘点生成调整草稿 → 人工审核提交。回归：`tests/test_check_add_category_generate.py` 7/7 PASSED（不选分类原行为 / 含子分类+账面 0 建行+冻结+未盘语义 / 分类不存在 400 不落单 / 超上限 400 / 未盘行无调整且手机盘一行后完成只调该行 / 模板契约 / 分类成环不死循环）；既有 `test_inv_batch_001e_mobile_check_required.py` 等盘点域测试零回归。**生效条件**：后端 + 模板改动，拉取代码后**重启 WMS 服务生效**（R3），浏览器 Ctrl+F5 强刷 |
| BUG-2026-09-05-002 | [P2] 从「库存台账」点单据编号进入入库单，台账页被带离应用标签工作区：BUG-2026-08-25-002 把报表记录级链接统一改为 `target="_blank"` 新浏览器标签页，原报表页虽不被替换，但点击后**跳出应用**、新开浏览器标签整壳重载（侧边栏/标签栏重新初始化），且新标签的 `WmsTabs.restore()` 会改写 `wms_open_tabs`；用户实测反馈「从库存台账进入库单，库存台账这个页面不要有变化」——期望单据在**应用标签栏**新开标签页、台账标签页（筛选条件/查询结果/页码）纹丝不动 | **已修复（2026-09-05，commit 本次提交）**：`app/templates/report_view.html`（库存台账 / 入库明细 / 领料明细 / 盘点 / 采购明细 / 委外 / 工单领料等全部 `/report/view/<type>` 通用报表共用模板）①新增 `bindReportDocLinks()`：报表页运行在标签工作区 iframe（父窗口存在 `WmsTabs`）时，拦截 `.report-doc-link` 点击改用 `window.parent.WmsTabs.open(href, title)` 在应用标签栏新开标签页，原报表页所在标签完全不跳转；②单据链接渲染新增 `data-doc-title`（台账行优先取 `reference_type` 单据类型标签，如「入库单 IN26080009」，其他报表回退列标题）作为应用标签页名；③兜底不回退：Ctrl/Cmd/Shift+点击、中键、独立浏览器（无父级 WmsTabs）仍按 `target="_blank" rel="noopener"` 新开浏览器标签页（沿用 BUG-2026-08-25-002 行为）。R6 同类点排查：本模板覆盖全部通用报表单据链接（同一渲染函数 `renderTable` 单点生效）；其余列表模板记录级链接保持 BUG-2026-08-25-002 的 _blank 行为不变（本次仅按用户点名的库存台账/通用报表链路改造）。回归：`tests/test_bug_2026_09_05_002_report_doc_links_app_tab.py` 4/4 PASSED（_blank 兜底保留 / WmsTabs 绑定且渲染后调用 / 修饰键放行序 / data-doc-title 命名）；既有 `tests/test_bug_2026_08_25_002_doc_links_new_tab.py` 3/3 PASSED 零回归；`scripts/lint_wms_rules.py` 0 违规、模板内联脚本 node --check 通过。**生效条件**：模板改动，拉取代码后**重启 WMS 服务生效**（R3） |
| BUG-2026-09-05-001 | [P1] 物料编辑保存 500：`sqlite3.OperationalError: no such column: inventory_check_item.counted_by`（2026-09-05 08:52:25 / 08:53:00 连续两次同参数复现，`app/routes/material.py` edit_material 第 5 步「盘点单明细」级联统计 `InventoryCheckItem.query.filter_by(material_id=43)`）。根因：INV-BATCH-001-A / INV-BATCH-002 引入的盘点域迁移列**只在 `auto_migrate_database()` 里 ADD COLUMN**，而 `start_wms_offline.bat` 等启动脚本默认 `WMS_NO_DB_TOUCH=1` 会整体跳过迁移函数、`fix_db_columns.py` 又完全不含盘点表补列——存量生产库重启也补不上，缺列长期存在；改个物料名称即 500（R6 同根因**第 4 次复发**：print_job / stock_transaction / excel_print_template / 本处） | **已修复（2026-09-05，commit `b89dc310`，台账子修复 ID INV-BATCH-001-F）**：①`app/app.py` 新增 `ensure_inventory_check_columns()`（仿 `ensure_print_job_columns`：独立 sqlite 连接、独立于迁移开关无条件执行、PRAGMA table_info 幂等判断），**一次性覆盖盘点域全部新增列**——`inventory_check.frozen_at`、`inventory_check_item.counted_by / counted_at / area`、`inventory_check_scan.check_id`、`inventory_check_scan_item.area`，列片段与 `auto_migrate_database()` 逐字一致；启动序列在 `ensure_stock_transaction_warehouse_id_column()` 之后调用。②`app/routes/material.py` 编辑保存的 5 项「仅统计条数、无冗余字段需同步」查询（`InOrderItem` / `OutOrderItem` / `InventoryCheckItem` / `SubcontractItem` / `BOMItem`）改为可降级 `_append_related_count()`：查询失败只记 WARNING 并跳过该项提示，不阻断物料保存（R6 消费点兜底，避免下次缺列再打成 500）。R6 同类点排查：`delete_material` 引用检查已逐表 try/except 降级，无需改动。回归：`tests/test_ensure_inventory_check_columns.py` 5/5 PASSED（6 列全补齐 / 幂等重复执行 / 列齐全不重复 ALTER / 空库缺表跳过 / 子进程 `WMS_NO_DB_TOUCH=1` 启动接线补列）、`tests/test_bug_2026_09_05_001_material_edit_cascade_degrade.py` 3/3 PASSED（盘点查询抛缺列时编辑仍返回 success 且改名落库 / 提示剔除降级项 / 正常路径仍统计「盘点单 N 条」）；配套止血脚本 commit `f02b7ab`（`app/fix_inventory_check_columns.py` + `fix_inventory_check_columns.bat`：不便重启时双击即补列、自动定位数据库、回读 PRAGMA 校验，测试 10/10）；bat 兜底层对齐 commit `eaceaf1`（`app/fix_db_columns.py` 新增 `_ensure_inventory_check_columns()`，启动脚本强制先跑层也覆盖盘点表——双保险，测试 14/14，三处清单防漂移断言）；`scripts/lint_wms_rules.py --staged` 0 违规。**生效条件**：后端 + 数据库改动，拉取代码后**重启 WMS 服务**即自动补列（无需手工改库，补列只 ADD 可空列、不动存量数据）；不便立刻重启时可先双击 `fix_inventory_check_columns.bat` 止血，再择机重启。**线上验证（2026-09-05 09:32:21 生产启动日志实证）**：`[DB] 盘点域已补缺列（BUG-2026-09-05-001）: inventory_check.frozen_at, inventory_check_item.counted_by, inventory_check_item.counted_at, inventory_check_item.area, inventory_check_scan.check_id, inventory_check_scan_item.area` 六列一次补齐；同一份日志同时出现 `Startup database initialization skipped by environment.`——坐实根因推断①：该机启动脚本带 `WMS_NO_DB_TOUCH=1`，`auto_migrate_database()` 被跳过而自愈函数独立生效，正反两面均验证 |
| BUG-2026-09-04-005 | [P1] 移动盘点未强制选盘点单，盘点结果分散：Android /api/stocktake 与 H5 /mobile/api/scan_submit（check 模式）不要求选择 PC 端盘点单——自动挂钩"最新 pending 批次"，无批次时**独立建 CS 单并立即生成调整草稿**，与 PC 盘点单（批次）流程割裂，同一仓库可并存多张独立 CS/草稿，盘点结果无法统一记录到一张盘点单上，账务归集与审计口径混乱 | **已修复（2026-09-04，INV-BATCH-001-E，commit 本次提交）**：移动盘点强制选单——①`app/app.py` 新增 `_list_pending_check_orders(warehouse=None)`（Android/H5 列表共用）；新增 GET `/api/stocktake/check_orders`（native）与 `/mobile/api/check_orders`（H5）两个盘点单列表端点（仅 status='pending'，可按仓库名称/编码过滤）；②`/api/stocktake` 与 `scan_submit`(check) 强制校验 `check_id`：缺省 400「请选择进行中的盘点单…」、不存在/已完结/仓库与所选盘点请求不一致均 400，**删除独立建单旁路**，明细统一 `_apply_scan_to_batch` upsert 进所选批次，返回 `batch_no`/`inventory_check_id`；③`_void_check_scan` 补批次行回退（BUG-2026-09-04-005 附随）：批次已完成拒绝作废（提示先反提交批次调整单），批次 pending 时本 CS 新建批次行（reason=='手机扫码盘点' 且 counted_by==本 CS 操作人）删除、PC 预置行被本 CS 补盘的（counted_by==本 CS 操作人）重置回"待盘"（counted_by/counted_at=NULL）、批次被清空时撤销 frozen_at——扫错作废后同物料可重盘且不产生脏草稿；④`scripts/verify_wms_bugs.py` BUG-NEW2-004 断言更新为强制挂批次形态（不再要求独立草稿调用）。**业务决策（用户确认）**：所有盘点结果统一记录到一张 PC 盘点单上；无进行中盘点单时手机端提示先到电脑端建单；旧版 App 3.5.1（不带 check_id）将被拒绝，须发新版。回归：`tests/test_inv_batch_001e_mobile_check_required.py` 6/6 PASSED；重构 `test_inv_batch_001c_scan_hooks_batch.py` 7/7、`test_inv_guard_001_stocktake_duplicate_adjustment_guard.py` 5/5、`test_inv_revert_001_check_scan_void.py` 8/8 PASSED；`tests/` 全量 1149 passed + 涉盘点 verify 全绿 + `scripts/verify_wms_bugs.py` PASS。**生效条件**：后端改动，拉取重启 WMS 生效；Android 需发新版 APK（AA③）；H5 页面另见 AA② |
| BUG-2026-09-04-003 | [P1] Android CI 每次构建签名都不同，新旧 APK 无法覆盖安装（用户实测 #285 产物装不上，提示「应用未安装」）。根因：release 变体未配置 WMS_STORE_FILE 时回退 debug 签名，而 runner 的 debug.keystore 是构建时临时生成——实测 #285 产物 v2 证书有效期起点 = 本次构建时刻（2026-09-04 03:26:16 UTC），`build.gradle.kts`（AI-MOB-APK-001）「runner 镜像预置 debug.keystore 跨构建稳定」的注释假设不成立；APK 本体经沙箱全套校验完好（ZIP 424 条目 CRC 通过 / v2 签名块有效 / manifest 解析正常） | **已修复（2026-09-04，commit 本次提交）**：方案B——`.github/workflows/android-build.yml` 构建前新增 `Restore fixed debug keystore` 步骤：把仓库 Secret `WMS_FIXED_DEBUG_KEYSTORE_B64`（固定 debug.keystore 的 base64，PKCS12，alias androiddebugkey/密码 android，仅用于内部测试签名）还原到 `~/.android/debug.keystore`，AGP 每次用同一签名；Secret 未配置时 `::warning::` 降级不阻塞构建。Secret 需仓库管理员在 Settings→Secrets and variables→Actions 手动配置一次（API 连接器 token 无 secrets 写权限）。**部署边界**：配好 Secret 后 CI 重新构建的 APK 签名固定为 cert SHA256 `93:5C:F6:47:24:BE:FC:C3:FE:EA:68:9D:0E:9E:26:85:CF:91:BC:19:02:6F:D6:B8:F7:AE:F0:DD:CD:01:A8:E1`；因签名切换，已装任意旧版本仍需**最后卸载重装一次**，此后更新可互相覆盖安装。回归：`tests/test_bug_2026_09_04_003_fixed_debug_keystore.py` 4/4 PASSED（还原步骤存在且位于构建前/Secret 名与解码路径/warning 降级/workflow YAML 合法性） |
| BUG-2026-09-04-002 | [P1] 内置打印代理心跳写库遇 `database is locked` 裸抛整段 SQLAlchemy traceback 刷屏（2026-09-03 15:30:29 日志实证：`_heartbeat` 内 `db.session.commit()` 更新 `print_workstation.last_heartbeat` 时锁冲突，异常冒泡到 `_agent_loop` 走 `log.exception` 完整打印）。违反 AGENTS.md R4「busy_timeout + 低频重试 + 降级静默，禁止裸抛 traceback 刷屏」；锁窗口内心跳丢失且日志被污染（台账锁类 17 条同族） | **已修复（2026-09-04，commit 本次提交）**：`app/local_print_agent.py` ①新增 `_is_db_locked(exc)`——`str(exc)` 与 `exc.orig` 双通道精确匹配 `database is locked`，兼容 SQLAlchemy ORM 包装形态；②`_heartbeat` 的 commit 包守卫：遇锁 → rollback + 单行节流 WARNING（`lock_warned` 状态位）+ 返回 False（外层按 30s 低频重试），非锁异常保持原样 re-raise；③`_agent_loop` 异常分支对锁同样单行节流告警并继续轮询，不再刷 traceback。回归：`tests/test_bug_2026_09_04_002_print_agent_lock_degrade.py` 8/8 PASSED（锁识别正反例/心跳遇锁返回 False 不抛/锁恢复后正常返回并清标记/非锁异常仍上抛/锚点防回退）；既有 `test_local_print_agent.py`+`test_print_agent_api.py`+PRAGMA gating 相关 43/43 零回归。**生效条件**：纯代码改动，拉取重启 WMS 服务即生效，无需数据库迁移 |
| BUG-2026-09-04-001 | [P1] AI-R14-F01 每日 02:00 自动清理预览保存 `ai_cleanup_log` 必失败：`sqlite3.IntegrityError FOREIGN KEY constraint failed`（2026-09-03/2026-09-04 连续两天同参数复现，INSERT 参数 `executed_by=0`）。根因：系统自动任务无真实登录用户，`notifications.py` 写 `executed_by=0`，而 `ai_cleanup_log.executed_by` 是 `user.id` 的非空外键，users 表无 id=0 行 → 外键必违反；审计记录（R5 人工确认边界依据）落库断链 | **已修复（2026-09-04，commit 本次提交）**：①`app/app.py` 新增 `_ai_dr_resolve_system_executor_id()`——优先 `WMS_BOOTSTRAP_USERNAME`/admin 账号，无 admin 取最小 id 用户，无用户返回 None；②`_ai_dr_save_log` 兜底：入参 executed_by<=0 或用户不存在时解析系统归属账号，无账号可归时跳过落库并 `warning`（不再外键刷日志）；③`notifications.py` 每日预览改传解析结果。R6 同根因排查：全仓 grep `executed_by=0`/`user_id=0` 仅此一处消费点，已覆盖。回归：`tests/test_bug_2026_09_04_001_ai_cleanup_log_system_executor.py` 10/10 PASSED（admin 优先/Bootstrap 环境变量覆盖/无 admin 回退最小 id/无用户返 None/0 兜底落 admin/不存在用户兜底/真实用户直通/无用户优雅跳过/负数兜底/notifications 锚点禁写 0）；既有 `test_ai_data_retention_config.py` 等 43/43 零回归。**生效条件**：纯代码改动，拉取重启 WMS 服务即生效，无需数据库迁移；历史失败日志无残留数据需清理 |
| BUG-2026-08-29-006 | [P2] WMS CI #735 新暴露 2 个失败：verify_bug_2026_08_11_017::test_t3/test_t4 用字面字符串断言（期望 `Text("图片加载失败")` 与独立 `if (imageUrl.isBlank())` 分支），与 MaterialArchiveScreens.kt 现行实现（`Icon(... "图片加载失败")` 视觉化提示 + `val loadFailed = imageUrl.isBlank() || painter.state is AsyncImagePainter.State.Error` 守卫合并）形式上不一致；现行 Kotlin 写法等价且更优（Icon 视觉、布尔合并更紧凑），但契约测试字面断言过严导致 CI 红 | **已修复（2026-08-29）**：放宽测试断言到业务意图层——T3 检查「图片加载失败」文案与 AsyncImagePainter.State.Error 识别存在（Icon/Text 形态不限）；T4 检查 imageUrl.isBlank() 守卫存在、空 URL 未透传给 painter（合并到 loadFailed 或显式 if 分支都允许）。回归：verify_bug_2026_08_11_017_mobile_material_image_loader.py 5/5 PASSED，推送后 WMS CI 应全绿。**生效条件**：纯测试文件改动，Kotlin 行为不变 |
| BUG-2026-08-29-005 | [P2] WMS CI 自 2026-08-28 23:04 起持续红（3 failed, 964 passed）：①test_bug_2026_08_16_004 T4/T5——出库新增仓库级库存校验（2262e039）后，测试种子仍只有全局 Material.stock=100、主仓无任何流水，多仓库系统按流水净额算主仓可用 0.00，「合法草稿」被新守卫按「库存不足」正确拦截，completed==1 断言失败——属测试种子未跟上新守卫，非业务缺陷；②test_bug_2026_08_23_001::test_apk_version_bumped_for_release 硬编码 versionName=="3.3.0"，AI-MOB-APK-001 升版 3.5.0 后每次发版必挂 | **已修复（2026-08-29）**：①_seed() 补主仓期初入库流水（StockTransaction material_id=1 transaction_type=in quantity=100 warehouse_id=1 location=主仓），库存校验语义保持不变，超发/未来日期/仓库不一致等守卫用例不受影响；②版本断言改为逐段数值比较 tuple(int) >= (3,3,0) 基线，升版不再需要改测试。回归：tests/test_bug_2026_08_16_004_batch_complete_out_guards.py 5/5、tests/test_bug_2026_08_23_001_mobile_scan_card_material_details.py 全量 PASSED，推送后 WMS CI 应转绿。**生效条件**：纯测试文件改动，不影响生产代码与数据 |
| BUG-2026-08-29-004 | [P2] 采购入库明细表工程名称筛选框无候选提示：供应商输入框已有「输入即弹候选」自动补全（supplierFilterDropdown），工程名称框（name=project_name）却是纯文本，输入「东莞」命中多个工程时用户看不到候选列表、只能凭记忆输入全名，筛选效率低易输错 | **已修复（2026-08-29）**：in_order.html 工程名称框复用供应商下拉模式加自动补全——数据源复用现成接口 /api/contracts?keyword=（contract_no/project_name ilike 模糊匹配，标准信封 {status,data:{contracts}}，限 50 条，后端零改动），200ms 防抖请求+按关键词缓存，候选行显示「工程名称 + 合同编号」，键盘上下/回车/Escape/mousedown 选择回填完整工程名；不选择直接提交仍按 %kw% 模糊命中（后端 _apply_header_or_item_contract_filters 已兜底）。回归：tests/test_bug_2026_08_29_004_project_name_suggest.py T1-T6 PASSED。**生效条件**：纯前端模板改动，拉取重启+浏览器 Ctrl+F5 强刷即可，不涉及数据库迁移 |
| BUG-2026-08-29-003 | [P1] 采购入库明细表供应商手工输入筛选失效：/in_order?type=purchase_in 筛选区供应商输入框占位写「输入名称快速匹配」，但 JS（in_order.html showDropdown→findSupplierByName）只做全名/全编码精确匹配回填 supplier_id，手工输入部分名称（如「欧姆」）不点选下拉时 hidden.supplier_id 为空，提交后供应商条件整体失效；导出链路只认 supplier_id，文本筛选后导出结果与页面不一致 | **已修复（2026-08-29）**：app.py 新增共享 helper `_apply_order_partner_text_filter`（往来单位名称/编码 contains 大小写不敏感匹配，supplier 与 customer 都参与）；in_order_list 与 export_in_order 在 supplier_id 为空时回退 supplier_name 模糊匹配（id 优先），页面与导出一致；in_order.html 输入框补 name="supplier_name"，分页/导出/清除链接回填该参数。回归：tests/test_bug_2026_08_29_003_supplier_text_filter.py T1-T6 PASSED。**生效条件**：纯代码改动，拉取重启即可，不涉及模板参数与数据库迁移 |
| BUG-2026-08-29-002 | [P1] 库存台账末行结存排序错乱 + 明细静默截断：`_collect_ledger_rows` 按 `(日期, 物料代码, 单号)` 排序，单据号字典序不等于发生时间顺序，同一天同一物料多笔流水时，翻到该物料最后一行看到的 `balance_quantity` 并非最终结存（实测 6 个物料受影响，如 201033 实际结存 0 但页面末行显示 800）；明细查询写死 `.limit(5000)` 且按 `material_id` 升序截断，流水超上限后物料 ID 较大的流水整段丢失，结存静默归零且无任何提示 | **已修复（2026-08-29，commit 本次提交）**：①排序键改为 `(日期, 物料代码, 发生时间 _ts, 流水 ID _txn_id)`，按真实发生时间而非单号字典序排序，返回前 `pop` 掉两个临时字段，`rows` 结构与改动前完全一致、不泄漏给调用方与前端；②新增模块级常量 `LEDGER_ROW_LIMIT = 50000` 取代硬编码 `5000`，超限前先 `query.count()`，超限时 `app.logger.warning('[LEDGER] ...')` 并提示缩小查询范围（仓库/物料/日期），不再静默截断。回归：`tests/test_bug_2026_08_29_002_ledger_closing_sort_limit.py` 5/5 PASSED（常量=50000 / 函数内无硬编码 limit(5000) / 超限告警四要素 / 排序键含时间与流水 ID 且清理临时字段 / 无仓库返回空不回归）；线上实测受影响物料由 6 个降为 0，台账与报表结存逐仓逐物料对比 0 差异。**生效条件**：纯代码改动，拉取重启即可，不涉及模板与数据库迁移 |
| BUG-2026-08-29-001 | [P0] 库位账静默分裂 + 出库缺仓库级库存校验：`update_location_inventory` 库位为空时直接 `return (True, '')`，手机端扫码（mobile.py）与原生接口（native_api.py）据此判定成功，导致总账已扣减而库位账未动，账实静默分裂且无任何报错；`complete_out_order` / `batch_complete_out_order` 只按全局库存校验，未按出库仓库维度校验，A 仓库存不足时可用 B 仓库存蒙混过关 | **已修复（2026-08-29，commit 本次提交）**：①空库位分支改为——启用库位管理时显式失败并返回「物料 X 未指定库位，无法更新库位库存」，未启用时保持旧行为，`quantity_delta=0`（无变动）仍返回成功不影响既有调用方；②两个出库完成路由扣减前新增仓库级预检：按 `order.warehouse` 解析仓库对象，取 `get_warehouse_stock_quantities(wh_obj)` 逐行比对需求量，不足时先 `db.session.rollback()` 再 `api_error('出库仓库 X 库存不足：物料编码')`，避免残留半成品事务。回归：`tests/test_bug_2026_08_29_001_location_inventory_split.py` 5/5 PASSED（启用时空库位失败 / 未启用不回归 / 零变动仍成功 / 空白库位 strip 后失败 / 两处预检锚点）；既有 `test_bug_2026_08_27_001_ledger_location_filter.py` 3/3 不回归。**生效条件**：纯代码改动，拉取重启即可，不涉及模板与数据库迁移 |
| BUG-2026-08-28-005 | 手机 App（安卓原生）物料档案列表规格显示不全：`MaterialArchiveRow` 中规格文本 `maxLines = 1` + 省略号截断，长规格（如 ZB-BVR-450/750V-1*25）只显示「规格: ZB-BVR-450/7...」，仓库人员无法区分同系列不同线径物料 | 已修复（2026-08-28）：`MaterialArchiveScreens.kt` 列表行规格改 `maxLines = 2` 折行显示（超出两行才省略兜底），详情页规格本就不限行不受影响；回归 `scripts/verify_wms_bugs.py` 新增 `check_mobile_archive_spec_wrap`（BUG-2026-08-28-005），Android 编译由 CI assembleDebug 校验（本地沙箱无 SDK） |
| BUG-2026-08-28-004 | [P0] 库存台账/仓库月报/库存汇总按仓库查询整片 500，同时采购入库提交失败。根因：BUG-2026-08-27-005 把仓库过滤切换到 `StockTransaction.warehouse_id` 外键精确命中，列只在 `auto_migrate_database()` 里 ADD COLUMN；但 `start_wms_offline.bat`/`start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1`，`auto_migrate_database()` 与 `backfill_stock_txn_warehouse_id()` 都被 `startup_db_upgrade_disabled()` 跳过——存量生产库重启也无法补列，SQL 使用 `warehouse_id` 时报 `sqlite3.OperationalError: no such column: stock_transaction.warehouse_id`。`ensure_print_job_columns`/`ensure_excel_print_template_table` 已用同一模式兜底，但 stock_transaction.warehouse_id 列没补，这是 BUG-2026-08-27-005 的遗留盲区 | **已修复（2026-08-28，commit 本次提交）**：`app/app.py` 新增 `ensure_stock_transaction_warehouse_id_column()`——仿 `ensure_print_job_columns` 用独立 sqlite 连接、独立于迁移开关无条件执行、幂等（PRAGMA table_info 判存在则不 ALTER），列片段与 `auto_migrate_database()` 的 ALTER 一致；启动接线处（`ensure_print_job_columns()` 之后）无条件调用。配套：把 `backfill_stock_txn_warehouse_id()` 调用从 `startup_db_upgrade_disabled()` 守卫里取出，列被 ensure 补好后无条件执行（仍幂等、仅处理 NULL 行）。回归：`tests/test_ensure_stock_transaction_warehouse_id_column.py` 7/7 PASSED（缺列补列 / 幂等 / 索引必建 / 表不存在 no-op / 库文件不存在 no-op / 接线位置 / backfill 无守卫）；既有相关回归 73/73 PASSED（含 ledger 001/002/003/004/005/006/007、错误处理、仓库必填、revert、库存 fallback 等）。**部署边界**：用户需要拉取最新 main 并重启 WMS 服务，启动时列自动补齐 + 历史数据自动回填，台账查询与采购入库提交同时恢复 |
| BUG-2026-08-28-003 | [P1] 库存台账点击"查询"显示 `Unexpected token '服'，服务器内部错误，请稍后重试' is not valid JSON` 且看不到任何数据/真实错误。根因：报表查询走 `GET /report/api/<type>`，浏览器 fetch 默认不带 `X-Requested-With` 与 `application/json Accept`，视图抛错后 `wants_json_error_response()` 既不识别 `/api/` 也不识别 `/mobile/api/`，`handle_exception` 回落到纯文本 `'服务器内部错误，请稍后重试'`；前端 `await response.json()` 抛 `SyntaxError: Unexpected token '服'`，根因被吞、用户看到表格行渲染浏览器抛错消息 | **已修复（2026-08-28，commit 本次提交）**：`wants_json_error_response()` 显式识别 `/report/api/` 前缀，与 `/api/`、`/mobile/api/` 同列为 AJAX JSON 调用，错误处理器据此回 JSON；非 /api/ 路径（/report/view/<type>、/report/print、/report/<type>/print_excel 等）保持原 HTML/纯文本行为不变，避免把报表 HTML/下载意外 JSON 化。回归：`tests/test_bug_2026_08_28_003_report_api_json_error.py` 5/5 PASSED（5xx 必 JSON / 前缀识别 / 非 API 路径不回归 / 浏览器默认头命中 / rollback 失败仍回 JSON）；相关回归 `verify_bug_2026_08_05_005_error_handler_json.py` 4/4 PASSED。**生效条件**：仅需拉取代码重启 WMS（代码层改动，不涉及模板/静态资源/数据库迁移），与 BUG-2026-08-28-004 配套部署后用户能看到真实错误（如果有）并恢复台账查询 |
| BUG-2026-08-27-003 | 多仓库下库存台账查不到历史空 location 流水：历史流水 `StockTransaction.location` 为 NULL/空（2026-08-16 前不写仓库名），多仓库时无法按 location 归属到具体仓库，被台账仓库过滤排除——用户"多仓库、关库位管理、选了日期+仓库+物料仍查不到"。仓库自带 `backfill_empty_warehouse_documents` 又把空 location 一律归到**默认仓库**且只覆盖入库/领料两类，既不准确也不完整 | 已修复：`app/app.py` 新增 `_ledger_source_warehouse_getters`/`_ledger_source_warehouse_map`，多仓库按仓库查询时把空 location 流水一并取出，再按**来源单据的仓库**归属过滤（in/out/期初/调整/盘点/领料/销售/委外取单据 warehouse 字段，期初经 warehouse 关系，调拨按数量正负归 from/to）；归属所选仓库的保留，否则剔除、不串仓；location 非空流水不受影响。回归测试 `tests/test_bug_2026_08_27_003_ledger_source_warehouse.py`（4 passed：空location按来源归属可见 / 他仓不串仓 / 非空location不回归 / 调拨按方向归属） |
| BUG-2026-08-27-001 | [P2] 嵌入（Tab iframe）模式下单据详情页出现两个「删除」、两个「打印」：`insertGlobalActionBar()` 注入的全局工具栏（新增/保存/删除/设置/打印/导入/导出/模板/分享）与各单据详情页头部自带的状态感知按钮（草稿「删除」/已完成「反提交」/打印/发送打印）叠加重复；更严重的是已完成单据上页面只显示「反提交」，全局工具栏仍显示通用「删除」，点击必被后端拒绝（"只有待处理的单据可以删除"），造成"反提交/删除有问题"的错觉（涉及领料单/入库单/采购订单/销售订单/采购申请/售后出库/委外全部单据详情页） | **已修复（2026-08-27，commit 本次提交）**：`app/static/js/app.js` `insertGlobalActionBar()` 增加详情页去重——URL 以 `/数字` 结尾的详情页上，检测页面自带删除/反提交（onclick 含 delete/revert）与打印（onclick 含 printOrder( 或 href 以 /print 结尾）动作，页面已提供时跳过全局重复「删除」/「打印」按钮并清理多余分隔线；列表页/新增页不受影响，页面未提供对应动作的（如委外详情无打印、采购申请详情无删除）全局按钮保留，功能不缺失。已完成单不再显示必失败的「删除」，只剩「反提交」入口，与"已完成须先反提交再删草稿"规则一致。回归：`tests/test_bug_2026_08_27_001_detail_actionbar_dedupe.py` 5/5 PASSED（去重逻辑锚点/按钮过滤与分隔线清理/7 个详情模板自带动作齐全/侧边栏打印导航不误判/行级删除 class 委托不干扰）；node 抽取真实代码块 6 场景功能验证（草稿详情去重/已完成详情去重/列表页保留/委外仅去删除/采购申请仅去打印/行级按钮与导航不误判）6/6 PASSED；`node --check app.js` 语法通过。生效条件：前端静态文件，强制刷新（Ctrl+F5）即可 |
| BUG-2026-08-27-004 | [P1] 多仓库下仓库月报与库存汇总漏掉历史空 location 流水，与库存台账数字对不上：BUG-2026-08-27-003 修复台账空 location 流水按来源单据归属时，`_build_warehouse_monthly_report`（月报）与 `get_warehouse_stock_quantities` 多仓库+关库位分支未同步——仍仅 `location.in_(loc_names)` 把空 location 历史流水整体排除，同一批数据「台账有数、月报/库存查询无数」。这是同一仓库口径逻辑被复制成多份、修复漏改一处导致反复出 BUG 的直接案例（诊断详见 220 个 BUG 中 102 个为库存/仓库/台账类） | **已修复（2026-08-27，commit 本次提交）**：提炼共用口径助手 `_warehouse_scoped_txn_condition`（多仓库 IN 仓库名/编码/库位名并放行空 location；单仓库不过滤）与 `_filter_txn_list_by_warehouse_scope`（空 location 流水按来源单据仓库归属过滤），台账 `_collect_ledger_rows`、月报 `_build_warehouse_monthly_report`（月内流水+未来流水期末倒推）、库存汇总 `get_warehouse_stock_quantities`（多仓库+关库位分支）三处统一调用，今后改口径只改一处。附带修复月报期末口径漏掉库位名流水问题（与台账一致）。回归：`tests/test_bug_2026_08_27_004_monthly_report_source_warehouse.py` 6/6 PASSED（月报含空location归属流水/不串仓/仓库名不回归/**台账↔月报收入发出期末三项一致性断言**/库存汇总含归属流水/调拨按正负归属）；既有回归：台账 001/002/003 三件套 17/17、库存查询与仓库报表相关 31/31 全部 PASSED，无行为回退。 |
| BUG-2026-08-27-005 | [P1] stock_transaction 无 warehouse_id 列，仓库归属全靠 location 字符串（历史承载仓库名/编码、库位名、空值三类语义），仓库级报表只能靠字符串匹配重建归属且口径分散——这是 102/220 个库存/仓库/台账类 BUG 反复出现的结构性根因（BUG-004 只修了报表口径，未动数据模型） | **已修复（2026-08-27，commit 本次提交）**：B 治本。①模型加 `warehouse_id` 外键列+索引；②双通道迁移：SQLite 走 `auto_migrate_database` 补列、MySQL/PG 走 alembic `c1d2e3f4a5b6`（含外键与唯一匹配回填）；③启动幂等回填 `backfill_stock_txn_warehouse_id()`：location 唯一匹配仓库名/编码→该仓库，库位名唯一归属（LocationInventory.warehouse_id）→该仓库，空 location→按来源单据推断（复用台账归属逻辑，调拨按数量正负归 to/from），歧义保留 NULL 不猜；④写入端统一：`add_stock`/`deduct_stock_atomic`/`add_stock_transaction`/期初调整 4 处全部落 warehouse_id，调拨完成/反提交、物料初始库存 5 个调用方传仓库。今后仓库过滤可走外键精确匹配，不再猜字符串。回归：新增 11/11（列存在/4 写入端/回填 6 场景/幂等）；相关全量 81/81（含台账 001-004、调拨、期初、物料审计、库存查询、仓库报表）零回归。 |
| BUG-2026-08-27-006 | [P1] 仓库级报表读取路径仍以 location 字符串推理为主：B 已加 warehouse_id 外键并回填历史，但台账/月报/库存汇总的 SQL 过滤仍走 `location.in_(仓库名/编码/库位名)`+Python 归属——字符串口径与精确外键并存，新数据与回填数据没有被优先用外键过滤，切换不彻底则治本打折 | **已修复（2026-08-27，commit 本次提交）**：C 断根。①`_warehouse_scoped_txn_condition` 升级为三元组：多仓库 condition 改为 `warehouse_id == 所选仓库 OR (warehouse_id IS NULL AND location 兜底)`——外键精确命中优先、未回填行走字符串兜底，仓库解析失败整体降级 location 口径；②`_filter_txn_list_by_warehouse_scope` 加 warehouse_id 参数，外键命中（含空 location 新数据）直接保留；③台账/月报/库存汇总三处调用方全部传 wid；④`get_warehouse_stock_quantities` 多仓库关库位分支改 `warehouse_id` 精确聚合 + NULL 行走 location + 空 location 未回填行按来源单据归属；⑤`_material_stock_unattributed` 关库位分支改以 `warehouse_id IS NOT NULL` 判归属。回归：新增 7/7（新数据台账/月报/库存汇总精确命中与仓库隔离、未回填兜底不回归、**四方对账：台账期末==月报期末==库存查询==库位账实际库存**、_material_stock_unattributed 双向）；相关全量 90/90 零回归。 |
| BUG-2026-08-27-007 | [P1] C 读取路径切换引入两个边界漏洞（独立验证发现）：①`_filter_txn_list_by_warehouse_scope` 用 `t.warehouse_id == warehouse_id` 判断精确命中，降级场景（filters 传不存在的仓库名，loc_names 非空但 wid=None）下 Python `None == None` 为 True——全部 NULL 行（含其他仓库空 location 流水）绕过归属过滤被保留，报表串仓（A 版本无此问题，C 引入回归）；②`_material_stock_unattributed` 关库位分支改为仅 `warehouse_id IS NOT NULL` 判归属，未回填的非空 location 行（跨仓同名库位歧义保留 NULL 的行）被误判不可归属 → 回退全局 Material.stock 口径，削弱 BUG-2026-08-18-002 防 A 仓掩护 B 仓的仓库级严格校验 | **已修复（2026-08-27，commit 本次提交）**：①精确命中判断加 `warehouse_id is not None and` 前置守卫，降级场景不再误保留 NULL 行；②归属判定改为 `warehouse_id IS NOT NULL OR location 非空`——外键为精确标志、非空 location 为字符串线索，维持严格校验。回归：新增对抗性测试 tests/test_bug_2026_08_27_007 3/3（降级不串仓/字符串口径不回归/未回填行仍算归属），先红后绿验证；A/B/C 相关 40/40；全套件 930 通过（7 个失败为 barcode 库缺失与 APK 版本测试滞后，与本改动零交集，改动范围 git diff 确认）。 |
| BUG-2026-08-27-008 | [P1] 手机 APP 下载地址（/mobile/app）必然 404：APK 二进制从未提交进 git 仓库，`ANDROID_APK_PATHS` 两个候选路径（仓库根 app-release.apk、Android 构建输出 release 目录）在部署机上都不存在，CI 只构建 debug 上传 GitHub Actions artifact 不落部署机——用户点"下载扫码APP"（base.html 菜单 + mobile_connect 页）必见裸 404 页，即"下载地址不正确" | **已修复（2026-08-27，commit 本次提交）**：下载路由改三级兜底——①环境变量 `WMS_ANDROID_APK_URL`（管理员配置内网文件服务器/GitHub Releases 地址）→ 302 跳转；②本地 APK 文件 → 直接发送（原逻辑）；③均无 → 渲染友好说明页（200，新增 `mobile_app_download_fallback.html`），含网页版扫码入口（/mobile/scan/query，手机浏览器免安装直接用）与管理员三种部署方式指引，不再裸 404。回归：新增 tests/test_bug_2026_08_27_008 4/4（说明页/配置302/本地文件发送/配置优先），既有 verify_app_py_split_mobile 7/7。 |
| BUG-2026-08-27-002 | 库存台账（及仓库月报）在单仓库 + 关库位管理下查不到历史流水：2026-08-16 之前 `add_stock`/`deduct_stock_atomic` 不写仓库名，历史流水 `StockTransaction.location` 为 NULL；而台账按 `location` 匹配仓库名/编码过滤（报表仓库必填后加的），升级后这些老流水被排除——用户"以前能查、现在查不到"，且出现"库存查询有数（单仓库 `get_warehouse_stock_quantities` 有全局兜底）、库存台账却查不到"的口径不一致 | 已修复：`app/app.py` `_collect_ledger_rows` 与 `_build_warehouse_monthly_report` 在 `Warehouse.query.count()==1`（单仓库）时不按 `location` 过滤——单仓库所有流水必属该仓库，与 `get_warehouse_stock_quantities` 单仓库全局口径一致；多仓库仍按 `location` 精确过滤（仓库名/编码/库位，配合 BUG-2026-08-27-001），无法归属的 NULL 流水保持隐藏防串仓。回归测试 `tests/test_bug_2026_08_27_002_ledger_single_warehouse_null_location.py`（4 passed：单仓库 NULL 流水可见 / 单仓库仓库名编号流水不回归 / 多仓库 NULL 流水仍隐藏 / 月报同根因）；关联 BUG-2026-08-27-001 测试 3 passed 不回归 |
| BUG-2026-08-27-001 | 库存台账（及仓库月报）按仓库查询查不到调拨/委外等流水：`StockTransaction.location` 字段混用「仓库名/编码」与「库位」，而库存台账是唯一依赖该字段按仓库过滤的报表，旧逻辑只匹配仓库名/编码，导致开启库位管理时库位型流水（transfer/subcontract/mobile 走 `add_stock_transaction` 直接写库位）按仓库查不到，用户看到“库存台账查不到东西” | 已修复：`app/app.py` 新增 `_warehouse_location_filter_values(warehouse_id, name, code)`，把仓库名、仓库编码、以及该仓库名下所有库位名一并纳入 `location` 过滤；`_collect_ledger_rows` 与 `_build_warehouse_monthly_report` 共用此逻辑（同根因，AGENTS.md 仓库≠库位，但台账需按仓库聚合全部变动）。关闭库位管理时 `LocationInventory` 无行，仅匹配仓库名/编码，行为不变；回归测试 `tests/test_bug_2026_08_27_001_ledger_location_filter.py`（3 passed：开启库位管理调拨流水按仓聚合且不串仓 / 关闭时行为不变 / 月报同根因） |
| BUG-2026-08-26-001 | Excel 打印模板编辑器顶部「占位符速插」区 22 个字段全部直接显示纯英文模板变量（`{order.order_no}`、`{item.material.code}` 等），面板标题仅"占位符速插："无用法说明，仓库一线人员看不懂含义、不知道该插入什么字段 | 已修复（2026-08-26）：`print_template_editor.html` 的 `PLACEHOLDERS` 改为 `{ label, value }` 结构——chip 显示中文标签（单号/日期/供应商/物料编码/数量/金额…22 项），悬停提示显示英文变量+用法，点击仍插入英文 value（模板引擎取值键绝对不动，改动量仅前端显示层）；面板标题补中文使用说明（点击字段插入到当前单元格，打印时自动替换为实际内容）。回归测试 `tests/test_bug_2026_08_26_001_placeholder_chips_chinese.py`（4 passed：每条目 label 必含中文 / value 保持英文模板键格式不得中文化 / chip 显示取 label 插入取 value 防改回 / 面板标题含用法说明） |
| BUG-2026-08-25-002 | 单据/报表/列表点击记录项在本页跳转、原列表上下文丢失：库存台账点领料单、入库明细点单号等都在当前标签页打开详情，返回后筛选/页码状态易丢。f28b921 只修了 report_view.html 通用报表，其余 36 个模板的记录级链接仍为本页跳转 | 已修复（2026-08-25）：36 个模板 77+ 处记录级链接（单据编号、行内 查看/编辑 按钮、详情页 来源单 交叉引用、销售看板/报表订单号、AI 工作台 jump_url）统一加 `target="_blank" rel="noopener"`；显式排除菜单/分页/新增/导出/打印/返回等页面内导航与详情页同记录"编辑"切换按钮；回归测试 `tests/test_bug_2026_08_25_002_doc_links_new_tab.py`（3 passed：全模板扫描无遗漏 / 17 个关键列表页点名验证 / 排除项防误改），浏览器实测点击入库单号新标签页打开详情、原列表页保留 |
| BUG-2026-08-25-001 | 其他入库单「先开单、后建客户」选不到新客户：页面级往来单位数组只在开单时渲染一次，跨页新建客户后下拉里没有新客户。根因双重：①`in_order_add.html` 主数据广播监听只处理 `supplier_updated`，客户页新建客户广播的是 `customer_updated`，入库单页（客户模式）收到后不刷新；②`refreshSuppliers()` 固定请求 `/api/suppliers`（供应商接口），客户模式下一旦刷新会把客户列表整体覆盖成供应商数据 | 已修复（2026-08-25）：`in_order_add.html` 广播监听按 `partyField` 分流（客户模式响应 `customer_updated`、供应商模式响应 `supplier_updated`），`refreshSuppliers()` 按 `partyField` 选择 `/api/customers` 或 `/api/suppliers`、提示语改用 `partyLabel`；回归测试 `tests/test_bug_2026_08_25_001_other_in_customer_refresh.py`（3 passed：客户模式含 customer_updated 监听 / refreshSuppliers 含 /api/customers 分流 / 采购入库供应商模式不回退）；浏览器端到端实测：A 标签页开单、B 标签页建客户并广播，不刷新页面即可搜到并选中新客户（hidden customer_id 正确写入） |
| BUG-2026-08-24-007 | [P0] 手机端（Android App）一打开「每日报表」整 App 崩溃（系统弹「WMS扫码 屡次停止运行」非接口报错）。根因：`ReportModels.DailyReportItem.contractNo`（commit `5cca484` 新增）声明为非空 `String = ""`，但该类多数构造参数无默认值 → Kotlin 不生成无参构造器 → Gson 走 `Unsafe.allocateInstance` 实例化（绕过构造器，`= ""` 默认值不生效）；当服务端响应缺 `contract_no` 字段（手机连的还是未部署 `5cca484`/`554eafb` 的旧版后端）或字段显式 null（`Material.spec` 列 nullable）时，非空字段运行时被置 null 破坏 Kotlin 非空契约，`ReportScreens` 渲染明细行裸调 `item.contractNo.isNotBlank()` 抛 NPE，异常发生在 Compose 主线程渲染路径未捕获 → 整 App 崩溃。网络请求本身是成功的（旧后端正常返回数据），所以表现为「加载完一渲染就崩」而非报错 toast。扫码出库页不崩是因为 `ContractDto` 全字段可空且 UI 用 `orEmpty()`/`isNullOrBlank()` | 已修复（2026-08-24）：①`ReportModels.DailyReportItem` 的 `spec`/`contractNo` 改可空 `String?`（对齐 `supplier`/`department` 既有模式，Gson 缺字段/显式 null 均不再破坏契约）；②`ReportScreens.DailyReportItemRow` 改 `!item.spec.isNullOrBlank()`/`!item.contractNo.isNullOrBlank()`；③后端 `daily_detail` 的 `spec` 输出加 `or ''` 兜底（`material.spec` 列可空，禁止下发显式 null）。回归：`tests/verify_bug_2026_08_24_007_report_contract_npe.py` T1-T6 6/6 PASSED（两字段可空声明 / 两处判空写法 / 后端 spec 兜底 / 出库页 ContractDto 用法 null 安全无同类 NPE）+ `tests/test_mobile_report_daily_detail.py` 12/12 PASSED（新增 spec=None 物料输出空字符串用例）+ 存量 `verify_daily_report_contract_ui.py` 5/5 PASSED。**部署边界**：手机端需重新构建安装新 APK；同时服务器必须拉取最新 main 重启（仅代码更新，`contract_no` 等列早已存在无需 DB 迁移），否则报表不崩但合同编号列不显示（旧后端不下发该字段） |
| BUG-2026-08-24-006 | [P1] 手机端（Android App）首次打开「每日报表」报「网络错误: 服务器地址未配置，请先登录并填写服务器地址」且页面停留空态：`AppNavGraph` 组合阶段（App 启动时）即创建 `ReportViewModel` 并在 init 立即 `load()`，而 `AuthViewModel` 异步还原 `RetrofitClient` 的 baseUrl/token 依赖 DataStore + EncryptedSharedPreferences 首轮读取（数百毫秒），两者竞态——报表请求先于会话还原时 `apiService` 的 `check(baseUrl.isNotBlank())` 抛 IllegalStateException，被 `getDailyReport` 外层 catch 包装成上述报错；错误态滞留 `uiState.error`，等用户首次进入报表页时弹出，且 init 加载失败后 `report==null` 页面停留「当日暂无明细」空态不重试 | 已修复（2026-08-24）：`WmsRepository` 新增 `ensureSession()`——请求前若内存 baseUrl 为空则从持久化存储同步还原 token（先注入，避免首请求 401 误触发强制登出）与 baseUrl，已还原时零开销 no-op；`getDailyReport`/`getDashboard`（HomeViewModel.init 同款竞态）请求前调用；`ReportViewModel.init` 不再自动加载（启动时可能尚未登录，提前加载只会留下过期错误态），加载改由 `DailyReportScreen` 进入时 `LaunchedEffect(Unit)` 触发，每次进入按当前日期/类型刷新。回归 `tests/verify_bug_2026_08_24_006_daily_report_session_race.py` T1-T5 5/5 PASSED（ensureSession 先 token 后 baseUrl / 两处请求前调用 / init 旧自动加载清零 / 页面进入触发加载）。验证：pre-commit lint A1-A10 0 违规；本地 `8e0fcf2` → 常规 HTTPS 推送远程 `8e0fcf2`（本沙箱无 Android SDK，assembleDebug 需构建机执行） |
| BUG-2026-08-24-005 | [P1] 仓库本地电脑跑独立打印代理（wms_print_agent.py）时，本机枚举失败（Print Spooler 停止/WMI 异常）经 `/print_queue/api/v1/heartbeat` 上报空 `printers` 列表，服务端 `sync_workstation_printers(ws, [])` 把已有打印机全部误标 offline、打印路由瘫痪（云端部署 + 本地打印机形态的真实风险） | 已修复（2026-08-24）：服务端 `AgentHeartbeatRequest.printers` 改 Optional——`None`（未上报/枚举失败）只保活工作站、跳过打印机同步，空列表 `[]` 仍按「真无打印机」置 offline；独立代理 `enumerate_printers` 三态 list/[]/None（新增 `_run_powershell_status` 区分「命令失败/成功但无打印机」，` _run_powershell` 对外行为不变、与内置代理同步），`send_heartbeat` 枚举失败改上报 `printers=None` 并本地告警节流（失败/恢复各一条），`--list-printers` 枚举失败给排查指引并返回非 0。回归 `tests/test_print_agent_api.py`（null 保活/空列表置 offline）+ `tests/test_print_agent_script.py`（三态枚举 + 心跳 null/节流/恢复）。验证：lint A1-A10 0 违规 + verify_wms_bugs + pytest 781 passed；本地 `4eecbd5` → 受限网络 Git Data API 重放远程 `054ca49`。部署提示：云端无打印机时设 `WMS_LOCAL_PRINT_AGENT=0` 关内置代理，仓库本地电脑跑独立代理并在该机启动 Print Spooler |
| BUG-2026-08-24-004 | [P1] 内置打印代理在 Print Spooler 服务停止/WMI 异常时每 60s 刷 3 条 PowerShell stderr，且把本机打印机全部误标 offline：`enumerate_local_printers` 枚举失败（三条 Get-CimInstance/Get-WmiObject Win32_Printer 命令全部退出码非 0）返回 `[]`，与「本机真无打印机」不可区分，`_heartbeat` 据此调用 `sync_workstation_printers(ws, [])` 把已有打印机全部置 offline、打印路由瘫痪；且每条失败命令都 WARNING，每 60 秒一轮刷屏 | 已修复（2026-08-24）：app/local_print_agent.py 新增 `_run_powershell_status` 返回 `(ok, stdout)` 以区分「命令失败」与「成功但无打印机」（`_run_powershell` 对外行为不变、与独立代理同步）；`enumerate_local_printers` 改三态返回 list/[]（真无打印机）/None（枚举失败）；`_heartbeat` 收到 None 时只保活工作站、不调用 sync（不误标 offline），并按状态翻转节流告警（进入失败一条 WARNING、恢复一条 INFO）。回归 `tests/test_local_print_agent.py`（18 passed：三态枚举 + 心跳误标/连续失败节流/恢复重同步）。验证：lint A1-A10 0 违规 + verify_wms_bugs + pytest 776 passed；本地 `d843a6e` → 受限网络 Git Data API 重放远程 `0c21432`。遗留（后续单独处理）：独立代理 `tools/print_agent/wms_print_agent.py` 经 HTTP 心跳的同类「空上报误标 offline」需服务端心跳契约配合 |
| BUG-2026-08-24-003 | [P1] 打印心跳写库间歇 500「database is locked」：connect 期 PRAGMA（WAL/synchronous/busy_timeout=30000/foreign_keys）注册放在 `initialize_database()` 的 `startup_db_upgrade_disabled()` 早退之后，生产 `start_wms_offline.bat`/`start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1` 时 `initialize_database()` 提前 return，监听器从不注册，ORM 连接退回 sqlite3 默认 5s busy_timeout，心跳等高频写与其他写事务相撞即抛 `database is locked`（2026-08-24 15:31 日志实录心跳 500） | 已修复（2026-08-24）：app.py 把 PRAGMA 注册拆为 `_register_sqlite_pragma_listener()`（幂等），在早退之前无条件调用——连接行为与 schema 迁移解耦，`WMS_NO_DB_TOUCH=1` 跳过迁移时连接仍拿到 `busy_timeout=30000`；回归 `tests/test_bug_2026_08_24_003_sqlite_pragma_gating.py`（2 passed：早退路径仍注册 / connect 处理器 busy_timeout=30000、foreign_keys=ON 生效且幂等）。验证：lint A1-A10 0 违规 + verify_wms_bugs 86 项（CONF-004 PASS）+ pytest 776 passed；本地 `fd09ecd` → 受限网络 Git Data API 重放远程 `366fa25` |
| BUG-2026-08-24-002 | [P2] 打印 ptoken 未绑定具体单据 ID：`generate_print_token` 签发的 payload 仅含 `{jid, ws}`，`print_token_or_login_required` 只验 token 有效性与 10 分钟时效，不校验 URL 路径中的单据 `id` 与 `jid` 的对应关系——一个泄露的有效 ptoken 可在 10 分钟内对 `/in_order/<任意id>/print`、`/out_order/<任意id>/print`、`/label/batch_print?ids=任意`、`/material_archive/<任意id>/print` 4 条打印路由重放，免登录批量读取供应商/价格/数量/客户等商业数据（只读，无法写） | 已修复（2026-08-24，AUDIT-2026-08-24 P2-001，commit `367474d`）：`print_token_or_login_required` 新增 `job_type` 参数，ptoken 路径强制校验载荷 jid 对应 PrintJob 的 job_type 与目标资源 ID 同路由一致（in_order/out_order/material_archive 按 `target_id` 对路径 id、batch_print 按 `target_ids` 对查询 ids 集合，不匹配即走未授权；Web 会话路径不变）；4 条路由分别声明 in_order/out_order/material_archive/material_archive_batch 绑定。回归：新增 `tests/test_bug_2026_08_24_002_ptoken_resource_binding.py`（8 passed：错类型/错目标/无 Job 拒访 302、绑定正确放行 200、会话路径不受影响）；修正 `test_print_agent_api.py` 中固化旧漏洞行为的契约用例。验证：`make check`（lint A1-A10 0 违规 + verify_wms_bugs 86 项 + pytest 773 passed）；`run_smoke_in_ci.py` 全新库 121/121 通过 |
| BUG-2026-08-24-001 | [P1] app/requirements.txt 钉死的 9 个包含已知 CVE（pip-audit 计 51 个漏洞、OSV 计 83 条记录）：Pillow 12.2.0、Werkzeug 3.0.1、cryptography 42.0.4、urllib3 2.0.7、Jinja2 3.1.2、requests 2.31.0、Flask 3.0.3、PyMySQL 1.1.0、click 8.1.7；CI pip-audit 步骤为 advisory（`continue-on-error: true`）未收紧 | 已修复（2026-08-24，AUDIT-2026-08-24 P1-001）：按栈分 3 个原子提交升级至 OSV 清零版本——Web 栈（commit `2e2b4d3`：Flask 3.1.3 / Werkzeug 3.1.6 / Jinja2 3.1.6 / itsdangerous 2.2.0 / click 8.3.3）、加密与 HTTP 栈（commit `d76dd74`：cryptography 50.0.0 / requests 2.33.0 / urllib3 2.7.0 / PyMySQL 1.1.1）、图像栈（commit `2b5d391`：Pillow 12.3.0）；CI pip-audit 门禁由 advisory 收紧为阻塞（commit `53963dc`，并约定未来无修复版漏洞须显式 `--ignore-vuln` 注明理由）。验证：每组升级后 `make check`（lint A1-A10 0 违规 + verify_wms_bugs 86 项 + pytest 765 passed）×3；`run_smoke_in_ci.py` 全新库 121/121 通过 ×3；OSV 逐包复核 10 个变更钉包 0 漏洞；venv 全量依赖闭包（84 包含传递依赖）`pip-audit --vulnerability-service osv` 报 No known vulnerabilities found；升级前基线实测 Found 83 known vulnerabilities in 9 packages（与审计报告 51 漏洞/9 包同一事实，OSV 服务对 GHSA/PYSEC 双计） |
| BUG-2026-08-22-002 | 内置「系统默认入库单模板/领料单模板」下载不了：两个内置模板只有 html_template_content、excel_template_path 为空，模板管理页下载/在线编辑按钮被 `{% if excel_template_path %}` 隐藏、「下载当前默认打印模板」入口不出现、Excel 填充打印只能回退内置版式 | 已修复（2026-08-22）：app.py 新增 `_ensure_builtin_excel_template_file()`，把 static/templates 示例 xlsx 复制为 uploads/print_templates 内置副本（在线编辑改写副本不动原件），启动同步（`_ensure_default_print_templates_unconditional` 与 ORM `ensure_default_print_templates` 双路径）在内置模板缺 Excel 文件时幂等回填 excel_template_path，用户已自定义路径不覆盖；回归 `tests/test_builtin_print_template_excel.py`（3 passed：回填且文件可填充 / 幂等且不覆盖用户文件 / 下载路由 200 返回 xlsx） |
| BUG-2026-08-22-001 | 「Excel打印模板中心」（/print_templates）打开即报「服务器内部错误，请稍后重试」。根因：`ExcelPrintTemplate` 模型（表 `excel_print_template`，commit 5db1a091 新增）只挂在 `db.create_all()` 里，而 `start_wms_offline.bat`/`start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1` 会跳过 `initialize_database()`/`auto_migrate_database()`，强制运行的 `fix_db_columns.py` 又不含该表，导致功能上线前创建的存量库重启也建不出表，查询抛 `no such table: excel_print_template` → 500 | 已修复（2026-08-22）：app.py 新增 `ensure_excel_print_template_table()`，仿照 `ensure_print_job_columns` 用独立 sqlite 连接、独立于迁移开关无条件执行、幂等建表（`CREATE TABLE/INDEX IF NOT EXISTS excel_print_template`，列定义与模型一致，含 target_type/target_code 两索引；库文件不存在时交给 create_all；表已存在时结构与数据一律不动），并在启动接线处（`ensure_print_job_columns()` 之后）无条件调用；回归测试 `tests/test_ensure_excel_print_template_table.py`（4 passed：缺表补建且列/索引齐全 / 幂等且数据保留 / 已有表结构与数据不动 / 子进程 WMS_NO_DB_TOUCH=1 启动接线建表）；复现脚本模拟「存量老库 + WMS_NO_DB_TOUCH=1」验证修复前 500、修复后 200 |
| BUG-2026-08-21-003 | 采购入库明细表（/in_order?type=purchase_in）同时输入「供应商 + 合同编号」就报「内部服务器错误」。根因：`_apply_header_or_item_contract_filters` 用相关子查询 `EXISTS` 判断「表单头或任一明细匹配合同编号/工程名称」，而入库单列表外层查询已 `outerjoin` 明细表（`db.session.query(InOrder, InOrderItem).outerjoin(...)`），SQLAlchemy 自动相关把子查询里的明细表也关联到外层 FROM，导致子查询无 FROM 子句，编译抛 `Select statement ... returned no FROM clauses due to auto-correlation` → 500。领料明细表（out_order）、销售订单（sales）、采购订单（purchase_order）、入库导出（export）同类调用同样隐患 | 已修复：`_apply_header_or_item_contract_filters` 两个 `EXISTS` 子查询显式 `.correlate(header_model)`，只关联表头表、明细表保留为子查询自身 FROM；回归测试 `tests/test_bug_2026_08_21_003_in_order_list_contract_filter.py`（4 passed：供应商+合同 200 / 供应商+不匹配合同空结果 / 仅合同 200 / helper 编译过滤） |
| BUG-2026-08-21-002 | 打印模板修改未生效：「系统默认入库单模板」「系统默认领料单模板」`template_type` 一直停在 `html`、`html_template_content` 仍是旧样式（含 RECEIPT 副标题、序号/备注列、合计行、四格签名行），改的 Excel 默认模板线上永不生效。根因：`ensure_default_print_templates()` 只挂在 `initialize_database()` 里，而 `start_wms_offline.bat`/`start_wms_auto.bat` 默认 `WMS_NO_DB_TOUCH=1` 会跳过 `initialize_database()`，模板同步从不执行 | 已修复：app.py 新增 `_ensure_default_print_templates_unconditional()`，仿照 `backfill_empty_warehouse_documents` 用独立 sqlite 连接、独立于迁移开关无条件幂等同步（`template_type`→`excel`、`html_template_content`→新常量，并保证各自表内 `is_default=1`），启动接线处无条件调用；回归测试 `tests/test_print_template_sync.py`（4 passed：入库单同步 / 领料单同步 / 幂等 / 无表跳过） |
| BUG-2026-08-21-001 | 上传打印模板无法保存：`create_print_template` 调用 `save_print_template_file(excel_file, prefix)` 只传 2 参数，而函数签名 `save_print_template_file(file_storage, prefix, static_folder)` 需 3 参数（缺 `static_folder`），上传 Excel 模板即抛 `TypeError: missing 1 required positional argument: 'static_folder'`，模板既无法写盘也无法落库 | 已修复：调用处补传 `app.static_folder`（`save_print_template_file(excel_file, prefix, app.static_folder)`）；回归测试 `tests/test_print_template_upload.py`（3 passed：Excel 上传落库且文件写盘 / 缺文件 400 明确报错 / HTML 模板成功） |
| BUG-2026-08-20-012 | H5 扫码页停止扫码后摄像头未真正关闭：`stopBarcodeScanner()` 只调 ZXing `reset()` 停止解码，未停掉 `<video>` 上的媒体轨道，导致摄像头指示灯常亮、扫描框关闭后仍录制，浪费电量并有隐私风险 | 已修复：`stopBarcodeScanner()` 在 `reset()` 后遍历 `barcodeVideo.srcObject.getTracks()` 逐个 `track.stop()` 并清空 `srcObject`；回归验证由现有扫码流程测试与手动验收覆盖 |
| BUG-2026-08-20-011 | 批量草稿（/mobile/api/scan_batch_draft）库位/仓库名不一致：`scan_submit` 已做「未开库位管理时仓库编号→仓库名」规范化，但批量草稿未做；手机端把仓库编号当作 location 传入时，草稿 location 存的是编号而非仓库名，与流水/后续确认口径不一致 | 已修复：`scan_batch_draft` 位置字段复用与 `scan_submit` 相同规范化（location 为空回退仓库名；未开库位管理且 location==仓库 code 时替换为仓库名）；回归测试 `tests/test_mobile_scan_draft_flow.py::TestBatchDraftLocationNameConsistency` |
| BUG-2026-08-20-010 | H5 扫码页仓库字段在未开启库位管理时整块隐藏，导致入库/出库/盘点无法选择仓库，只能依赖后端默认仓兜底；且「待确认」页固定只查默认仓，其它仓的草稿看不到 | 已修复：[mobile_scan.html](app/templates/mobile_scan.html) 把「仓库/库位」拆成两个字段——「仓库」仅在非 in/out 模式隐藏（始终必选，AGENTS 仓库必填），「库位」仅开启库位管理时显示；扫码提交 payload 拆分传 `warehouse` 与 `location`；「待确认」页新增仓库下拉，切换仓库即重载，按所选仓库查询待确认草稿；回归测试 `tests/test_mobile_scan_draft_flow.py::TestConfirmWarehouseIsolation::test_pending_list_filtered_by_warehouse` |
| BUG-2026-08-20-009 | H5 扫码页「连续批量扫码」在盘点(check)等非出入库模式下仍显示，提交后端 `/mobile/api/scan_batch_draft` 时 `_norm_mode` 把非 `in/out` 的 mode 静默归一为 `in`，导致盘点扫码批量提交被误当成「入库」生成待确认草稿 | 已修复：后端 `BatchDraftRequest._norm_mode` 改为校验失败（非 `in/out` 直接 400，不再静默转 in）；前端仅在 in/out 模式渲染批量开关；回归测试 `tests/test_mobile_scan_draft_flow.py::test_batch_draft_rejects_check_mode` |
| BUG-2026-08-20-001 | 线上 print_job 缺 `printing_started_at` 等迁移列，打印代理 claim 永久报 `no such column`：`start_wms_offline.bat` / `start_wms_auto.bat` 默认设置 `WMS_NO_DB_TOUCH=1` 跳过 `auto_migrate_database()`，而强制运行的 `fix_db_columns.py` 又不含 print_job 补列，叠加导致重启也补不上 | 已修复：app.py 新增 `ensure_print_job_columns()`，仿照 `backfill_empty_warehouse_documents` 用独立 sqlite 连接、独立于迁移开关无条件执行、幂等补齐 print_job 的 `workstation_id`/`printer_id`/`route_rule_id`/`source_event`/`printing_started_at` 缺列（与 `auto_migrate_database` ALTER 一致），并在启动接线处（`backfill_empty_warehouse_documents()` 之后）无条件调用；回归测试 `tests/test_ensure_print_job_columns.py`（4 passed：缺列补齐 / 幂等 / 无表跳过 / 子进程模拟 `WMS_NO_DB_TOUCH=1` 启动接线补列） |
| BUG-2026-08-20-002 | 打印代理部署包 run.bat/start.bat 启动即报 3 次「系统找不到文件 'dir」：生成批处理的 Python 里，3 条「常见安装目录搜 Python」的 `for /f` 行误用 `r'...'` 原始字符串，`\'` 不会被折叠成 `'`，写出到 bat 后变成 `in (\'dir ... \')`，cmd 无法识别 `for /f` 的单引号命令形式、把 `'dir` 当文件执行（`where` 行是普通字符串、`\'` 正常折叠成 `'`，故未报错）；连累 3 条 dir 目录回退全部失效，只能靠 `py.exe` 启动 | 已修复：`print_routing.py` 生成 py_detect 的 3 条 `dir` 行从 raw 字符串改为普通字符串（`\'`→`'`、`\\`→`\`），写出即 `in ('dir /b /s "%LocalAppData%\\...\\python.exe" 2^>nul')` 正确形式，cmd 正常把 dir 当内部命令执行；Python 侧验证：`py_compile` 通过、3 条 dir 行均无 `(\'dir` 残留。用户需重新下载代理部署包并换成新 run.bat 生效 |
| BUG-2026-08-19-001 | 字段设置（栏目设置）弹窗字段多时列表溢出弹窗、无滚动条，底部按钮遮挡行且看不全 | 已修复：`custom.css` 字段设置内容区/面板改 `grid-template-rows:minmax(0,1fr)` + 表格容器 `overflow:auto;min-height:0`（去掉 `min-height:260px` 撑破约束），`app.js` 明细面板移除 `display:contents` 内联样式使行约束生效；`scripts/verify_wms_bugs.py` 新增 `check_field_settings_scroll` 回归（BUG-2026-08-19-001） |
| BUG-2026-08-19-002 | 采购入库明细表拖动列宽部分列拖不动：`setupResizableTable` 的 colgroup 按初始列序静态创建，字段设置重排/隐藏列后 `<col>` 索引错位（fixed 布局下 col 宽度优先于 th），拖动某列实际改到别的列或无效果 | 已修复：`app.js applyWidths` 改为按当前可见表头顺序（data-column-key）重建 colgroup 并跳过 `wms-field-column-hidden` 列；新增 thead MutationObserver 在重排/隐藏后自动重建；`scripts/verify_wms_bugs.py` 新增 `check_col_resize_sync` 回归（BUG-2026-08-19-002） |
| BUG-2026-08-19-003 | 打印工作台路由（/print_routing）老库缺 print 系列表（print_workstation/print_device/print_route_rule/print_job）时首条查询抛 `OperationalError: no such table`，`require_role` 路由直接 500，页面提示"服务器内部错误，请稍后重试" | 已修复：`print_routing_page` 按代码库既有模式（缺失表交给 `db.create_all()` 处理）捕获 `OperationalError` 时惰性 `db.create_all()` 补齐缺表后重试，页面正常渲染；回归测试 `tests/test_print_routing_admin.py::test_print_routing_page_selfheals_missing_tables`（删表后 GET 返回 200 且 print 表被重建） |
| BUG-2026-08-19-004 | 打印工作台"新增打印机"等按钮点击无反应：模板把 `{{ ws.code \| tojson }}` 嵌进双引号 onclick 属性，tojson 输出的双引号把属性截断成 `onclick="openAddPrinter(1, "1")"`，点击即 JS 语法错误；同类受影响：新增/编辑/删除打印机、删除工作站、删除路由规则共 5 处 | 已修复：含 `tojson` 的 onclick 属性定界符统一改单引号（tojson 已把内容中的单引号转义为 `\u0027`，安全），渲染后 `onclick='openAddPrinter(1, "1")'`；渲染级验证 19 个 onclick 全部为合法 JS；`scripts/verify_wms_bugs.py` 新增 `check_onclick_tojson_quotes` 全模板回归（BUG-2026-08-19-004） |
| BUG-2026-08-19-006 | 打印代理在 Win7（PowerShell 2.0）上枚举打印机永远为空：`enumerate_printers` 只用 `Get-CimInstance + ConvertTo-Json`（均为 PS 3.0+ 命令），PS 2.0 下命令不存在退出码非 0，`_run_powershell` 静默吞掉 stderr 返回空串 → 心跳上报空打印机列表 → 管理页打印机全部"离线"，`--list-printers` 输出空无任何提示 | 已修复：`enumerate_printers` 改三级回退链 CimInstance+Json → WmiObject+Json → WmiObject+ConvertTo-Csv（PS 2.0 自带），新增 `_parse_printer_output`（JSON/CSV 双格式）与 `_to_bool/_to_int` 字符串转换；`_run_powershell` 失败时记 stderr 日志不再静默；`--list-printers` 空结果时输出三步排查指引；回归测试 `tests/test_print_agent_script.py`（单对象/多对象/两级回退/CSV 解析） |
| BUG-2026-08-19-007 | 打印代理定向打印后原默认打印机永不恢复：`get_default_printer` 仅用 `Get-CimInstance`（PS 3.0+，Win7 PS 2.0 返回空串→`process_job` 误判读取失败）；`set_default_printer` 用 `bool(stdout)` 判成功，但 `SetDefaultPrinter` 成功时无任何输出恒为 False；打印机名含单引号未转义会截断/注入命令 | 已修复：`get_default_printer` 回退 `Get-WmiObject`（PS 2.0 可用）；`set_default_printer` 命令末尾按 `$?` 显式回写 OK 成功标记；新增 `_ps_quote` 单引号翻倍转义；回归测试 `tests/test_print_agent_script.py::TestDefaultPrinter`（WMI 回退/成功标记/单引号转义 3 项） |
| BUG-2026-08-19-008 | 代理带 `autoprint=1` 打开任何打印页都是 500：Jinja 模板作用域没有 Python 内建名 `int`（只有 `\|int` 过滤器），原 `type=int` 渲染即抛 `UndefinedError` | 已修复：`_autoprint_script.html` 改用 `\|int` 过滤器并做 1-99 边界钳制；回归测试 `tests/test_print_queue.py`（autoprint 页面渲染 200 且含份数脚本） |
| BUG-2026-08-19-009 | autoprint 打印完成后页面不自关，代理等浏览器进程退出才算完成→等满 `print_timeout`（默认 120s）强杀进程并把已出纸任务上报为 failed | 已修复：autoprint 脚本打完全部份数后 `setTimeout(window.close, 800)`（--app 模式生效；人工打开的标签页浏览器忽略自关不受影响）；回归测试 `tests/test_print_queue.py` |
| BUG-2026-08-19-010 | 僵尸任务回收两缺陷：v1 claim 无任何回收（代理崩溃后其任务永久卡 printing）；legacy next 按 `created_at` 判超时→队列积压 >5min 的 pending 任务一旦认领置 printing，下次轮询立即按 created_at<回收线 重置回 pending，无限循环永远打不出 | 已修复：`PrintJob` 新增 `printing_started_at` 字段（含启动迁移），统一 `_recover_zombie_printing_jobs` 按认领时间判定（存量 NULL 退回 created_at 近似，未达 MAX_ATTEMPTS 重置 pending、达到置 failed），三条认领路径（next/工作站 next/v1 claim）均记录认领时间且认领前先回收本工作站僵尸；回归测试 `tests/test_print_agent_api.py`（认领回收/次数耗尽/跨站不回收）+ `tests/test_print_queue.py`（积压任务认领后不被误回收） |
| BUG-2026-08-19-011 | 编辑打印机不能改 `system_name`，手填错系统名的打印机只能删除重建（且被路由规则引用时删除也被拒绝） | 已修复：`PrinterEditRequest` 新增 `system_name`（留空=保持不变，同工作站重名 400，>200 字符 422），前端编辑弹窗新增系统名称字段复用 `knownPrinterList` datalist 下拉；回归测试 `tests/test_print_routing_admin.py::test_printer_edit_system_name`（修正/重名 400/留空保持 3 场景） |
| BUG-2026-08-19-012 | 打印代理部署包内 run.bat/start.bat 裸调 `python`/`pythonw`：Python 未加入 PATH 的电脑（如 Win7 手动装 3.8 没勾 Add Python to PATH）双击 bat 即报「'python' 不是内部或外部命令，也不是可运行的程序或批处理文件」，代理无法启动 | 已修复：bat 改为自动定位 Python——PATH 上的 python.exe（`findstr /v WindowsApps` 排除 Win10 商店假 python）→ `%LocalAppData%\Programs\Python\Python3*` / `%ProgramFiles%\Python3*` / `C:\Python3*` 常见安装目录 → `py.exe` 启动器兜底；找不到时输出中文安装指引并退出；start.bat 优先用同目录 pythonw.exe 后台运行、取不到回退 python；bat 以 GBK 编码写入（zh-CN 默认 cp936 下中文可读）、CRLF 行尾；回归测试 `tests/test_print_routing_admin.py::test_download_agent_bats_autolocate_python` |
| BUG-2026-08-18-004 | 入库明细/汇总报表只查「采购入库」，手机端「产品入库」「其他入库」永远查不出；仓库只按名称匹配，手机端存仓库编号（WH001）的历史单据也查不出 | 已修复：`_collect_in_detail_rows` 移除硬编码 `business_type=='采购入库'`，改为可选筛选（空=全部类型）；仓库过滤 `db.or_(name, code)`；`_build_report_filters` 透传 `business_type/warehouse_code`；仪表盘 8 处统计与 `report_inout_print` 导出同样兼容仓库编号；回归测试 `tests/test_report_in_detail_business_type_warehouse.py`，2 passed |

| BUG-2026-08-18-001 | 入库 Excel 导入、其他入库编辑/导出/采购报表及批量反提交与仓库和业务类型口径不一致 | 修复导入字段保真、列表筛选条件传递、采购报表业务类型隔离、批量反提交仓库级库存校验；回归测试覆盖导入、导出、报表和多仓反提交 |

| 编号 | 问题 | 回归检查 |
|------|------|----------|
| BUG-2026-08-18-002 | 多仓库+关库位管理下，历史 NULL-location 流水无法按仓库聚合，单张/批量反提交误报“库存不足”，单据卡在 completed 无法删除 | 已修复：`revert_in_order` / `batch_revert_in_order` / `update_completed_in_order` 在仓库级校验失败且该物料库存全部为未归属流水（`_material_stock_unattributed`）时回退全局 `Material.stock` 口径；存在可归属流水仍保持仓库级严格校验（防 A 仓掩护 B 仓）；回归测试 `tests/test_bug_2026_08_18_002_revert_legacy_unattributed_stock.py`，专项 3 passed |
| BUG-2026-08-18-005 | 系统早期采购入库单/领料单未强制选择仓库，单据 warehouse 为空字符串，报表按仓库过滤后这些历史单据“有数据也查不出来” | 已修复：新增 `scripts/backfill_document_warehouse.py` 把 warehouse/location 为空的采购入库单（in_order）、领料单（production_requisition）及其关联 `stock_transaction.location` 回填为默认仓库（warehouse.is_default=1 且 active），默认 dry-run、须显式 `--apply` 写库、幂等可重跑；回归测试 `tests/test_backfill_document_warehouse.py`（5 passed：dry-run 不写库 / apply 写库 / 无默认仓库拒绝 / 幂等 / 已归属数据不变），提交 `cb74962`；并新增启动期自动回填 `backfill_empty_warehouse_documents`（app.py 紧跟 auto_migrate_database 调用，存在默认仓库即自动回填、幂等、无默认仓库跳过，用户无需手工执行命令），回归测试 `tests/test_backfill_empty_warehouse_documents.py`（4 passed），提交 `26996aa`；后修复接线缺陷：`start_wms_offline.bat` 默认设置 `WMS_NO_DB_TOUCH=1` 会跳过 `auto_migrate_database`，原实现把回填放进同一 else 分支导致生产重启永不生效，已改为独立于迁移开关无条件执行，新增接线回归 T5（子进程 `import app` + `WMS_NO_DB_TOUCH=1` 校验 DB 被回填），专项 5 passed、全量 596 passed，提交 `19b94cb`；再修复日志不可见缺陷：回填调用原在日志配置之前执行，生产环境回填日志行丢失（启动输出只剩 `Flask config loaded`，看不到回填结果，用户误以为没生效），已把 `backfill_empty_warehouse_documents()` 移到日志配置之后调用，并给 T5 增加断言「启动输出必须包含『历史单据仓库回填完成』」，专项 5 passed、全量 596 passed、lint 0 违规，提交 `bc98db2` |
| BUG-2026-08-18-006 | `revert_in_order` 的 BUG-2026-08-18-002-fix 分支（仓库级库存不足但全局充足时把未归属流水修正到本单仓库）引用 `StockTransaction` 但函数内 import 列表遗漏该符号，触发时 NameError→500，接口返回 HTML 而非 JSON，反提交无提示 | 已修复：`revert_in_order` 局部 import 补入 `StockTransaction`；回归 `tests/test_bug_2026_08_16_009_revert_in_order_warehouse_level.py`（3 passed）+ `tests/test_bug_2026_08_17_002_revert_in_order_without_warehouse.py`（3 passed），提交 `365a099` |
| BUG-2026-08-17-006 | 采购入库单反提交后删除，库存流水未随物理删除清理，库存台账显示已删除单据 | 已修复：单张/批量删除均清理该入库单关联流水；历史遗留数据使用 `scripts/cleanup_orphan_in_order_transactions.py`，默认预演、须 `--confirm-delete` 执行；回归测试覆盖预演与删除安全边界 |
| BUG-2026-08-17-005 | 库存查询低库存/正常库存按全局 `Material.stock` 判断，旧入库/出库/盘点/调整列表导出未按仓库过滤 | 已修复：`stock_query` 按仓库库存判断；四类列表导出按仓库隔离并补齐盘点/调整仓库列；回归测试 `tests/test_bug_2026_08_17_005_warehouse_stock_status_and_legacy_exports.py`，专项 3 passed，lint/verify/compileall 通过 |
| BUG-001 | commit 失败后仍返回 success | `scripts/verify_wms_bugs.py` 检查高风险回归模式 |
| BUG-002 | 库存扣减无事务隔离 | 检查 `deduct_stock()` 委托原子扣减，避免读改写扣减 |
| BUG-004 | 仓库/部门新增编辑删除缺异常处理 | 人工代码审查加自动高风险扫描 |
| BUG-005 | 物料复制编码硬编码 6 位 | 检查复制编码函数不再依赖固定 6 位 |
| BUG-006 | 期初库存调整无锁 | 检查使用库存原子增量更新 |
| BUG-009 | document 事件监听器泄漏 | 检查重复绑定前移除旧 handler |
| BUG-010 | ExcelTable cloneNode 清监听器副作用 | 检查不再使用 cloneNode 替换单元格 |
| BUG-011 | 重复创建导入模态框 | 检查已有模态框时直接复用 |
| BUG-012 | 导出列错位 | 检查按导出列映射数据 |
| VULN-001 | 打印 HTML 净化失败返回空 | 检查异常时返回安全错误 HTML |
| CONF-001 | 生产环境 SECRET_KEY 未强制 | 检查 production 必须配置 SECRET_KEY 或显式允许自动密钥 |
| CONF-002 | 开发配置默认 SQL_ECHO | 检查默认不启用 SQL 日志 |
| CONF-004 | SQLite 并发限制 | 检查启用 WAL、busy_timeout、外键 |
| VULN-003 | 部分 POST 表单缺 CSRF 字段 | 检查模板中普通 POST 表单含 `csrf_token` |
| VULN-004 | 密码强度不一致 | 检查新增用户和重置密码复用同一强度校验 |
| BUG-NEW-001-FIX-01 | 已完成入库单可被详情页或接口直接物理删除 | 后端只允许删除 `pending`；已完成单必须先反提交回退库存，详情页不显示完成单删除按钮 |
| BUG-NEW-003 | 采购入库来源规则不一致 | 采购入库允许手工录入，采购订单仅为可选来源；有关联订单时继续校验来源和数量 |
| BUG-NEW-005 | 默认密码策略不一致 | 已统一为优先使用 `WMS_BOOTSTRAP_PASSWORD`，未设置且首次创建管理员时使用 `admin`；安装和启动不得重置已有密码 |
| BUG-NEW-008 | 微信分享默认配置硬编码私人信息 | 检查源码不含私人姓名/微信号默认值 |
| BUG-NEW-009 | Excel 导入组件列名拼入 HTML 存在 XSS 风险 | 检查必填列通过 `textContent` 写入 |
| BUG-NEW-013 | 入库单下推缺少来源和数量控制 | 采购入库、其他入库可下推领料、其他出库和售后出库草稿；必须校验完成状态、可下推数量、幂等及客供阻断 |
| BUG-NEW-014 | 批量完成出库单失败回滚后计数和事务不一致 | 检查每张出库单独立提交，失败单独回滚 |
| BUG-NEW-015 | 单号生成固定截取末尾 4 位 | 检查按前缀+年月后的完整数字后缀递增 |
| BUG-NEW2-001 | 入库路径未检查 `add_stock()` 返回值 | 检查 Android 入库、手机扫码入库等核心路径失败即回滚 |
| BUG-NEW2-002 | 入库路径未检查库位库存更新返回值 | 检查 Android 入库和手机扫码入库库位失败即回滚 |
| BUG-NEW2-003 | 期初库存并发更新读取旧值缺少锁 | 检查期初库存新增/编辑/批量保存读取时使用 `with_for_update()`，库存更新仍用原子增量 |
| BUG-NEW2-004 | 盘点完成直接改库存或不生成调整单 | 检查普通盘点和扫码盘点完成后生成库存调整草稿，库存变化由调整单提交执行 |
| BUG-NEW3-001 | `add_stock()` 返回值未检查导致流水和库存可能不一致 | 检查所有 `add_stock()` 调用必须接收并处理返回值 |
| BUG-NEW3-005 | `batch_complete_in_order` 缺单据写锁、循环外 commit 致并发重复审核/部分失败整体回滚 | 每单 `_acquire_order_write_lock(InOrder,id,'pending')`、循环内独立 `commit`、失败仅回滚自身，SHA `7d2272a4` |
| BUG-NEW3-006 | `batch_complete_out_order` 缺单据写锁致并发重复扣库存/重复推进销售发货进度 | 每单 `_acquire_order_write_lock(OutOrder,id,'pending')`、循环内独立 `commit`，SHA `9d6a2ea1` |
| BUG-NEW3-007 | `batch_revert_in_order` 缺锁+`deduct_stock` 读改写竞态+循环外 commit | 每单加锁+改 `deduct_stock_atomic`+循环内独立 `commit`，SHA `60f365b4` |
| BUG-NEW3-008 | 售后出库完成/反提交未同步库位库存，启用库位管理后总库存与库位库存长期不一致 | `complete_after_sale_out_order` 和 `revert_after_sale_out_order` 在原子扣减/恢复后调用 `update_location_inventory`，失败回滚，SHA `0b56db5d` |
| AI-AUTH-001 | AI 草稿和敏感分析权限校验分散，存在能力扩展后越权风险 | 检查 AI 草稿、文档确认和敏感分析统一通过 `AI_CAPABILITY_ROLES` 校验 |
| AI-AUTH-002 | 销售 AI 能力 `sales_insights` / `sales_followup_agent` 上线后未纳入权限矩阵自动化覆盖 | 检查 `scripts/verify_ai_permission_matrix.py` 的 `EXPECTED` 含上述能力，且 `ROLES` 含 `sales` |
| AI-IDEMPOTENCY-001 | AI 重复点击、网络重试或 SSE 重连可能重复生成草稿 | 检查普通响应和 SSE 使用持久化 `request_id`，重复请求只执行一次并重放结果 |
| AI-ENCODING-001 | AI 采购入库业务类型和确认框存在历史乱码 | 检查已知乱码常量不得重新出现 |
| AI-AUDIT-001 | AI 请求和能力授权缺少持久化审计，无法复盘模型、耗时和权限结果 | 检查每个首次请求创建 `AIRun`，能力校验写入 `AIToolCall`，幂等重放不重复创建运行记录 |
| BUG-SALES-001 | `SalesOrder` 缺 `customer_id` 外键，依赖 customer 字符串文本，无法关联客户主数据 | 检查 `SalesOrder.customer_id` 为 `nullable=False` 外键到 `customer.id` |
| BUG-SALES-002 | `SalesOrder` 缺 `warehouse_id` 外键，依赖 warehouse 字符串，报表仓库筛选无法关联 Warehouse 表 | 检查 `SalesOrder.warehouse_id` 为外键到 `warehouse.id`，历史数据已回填 |
| BUG-SALES-003 | `OutOrderItem` 缺行级来源外键，无法追溯到销售订单明细 | 检查 `OutOrderItem.source_sales_order_item_id` 为外键到 `sales_order_item.id` |
| BUG-SALES-004 | `OutOrder` 缺头级来源外键，依赖 `purpose` 字符串解析 | 检查 `OutOrder.source_sales_order_id` 为外键到 `sales_order.id`，`source_sales_order` relationship 已建立 |
| BUG-SALES-005 | 销售出库缺跨仓库边界校验，可能扣错仓库库存 | 检查出库时 `material.stock` 减扣后不为负，且按 `SalesOrder.warehouse_id` 校验仓库一致性 |
| BUG-SALES-006 | 销售选单接口 `/api/sales_order/selectable` 无并发保护，高并发可能重复生成出库草稿 | 检查选单接口使用 `BEGIN IMMEDIATE` 串行化 |
| BUG-SALES-007 | `SalesOrder` 金额字段使用 `Float` 类型，精度不足导致对账偏差 | 检查 `SalesOrder.total_amount`/`untaxed_amount`/`tax_amount`/`shipped_amount`/`remaining_amount` 均为 `Numeric(18,2)` |
| BUG-SALES-008 | `/sales/<id>/copy` 和 `/sales/batch_delete` 缺 `@require_role`，仅 `@login_required` 越权风险 | 检查 12 个 `/sales/*` POST 路由全部含 `@require_role('warehouse','purchase','sales')`（SM-P6-FIX-01） |
| BUG-SALES-009 | `sales_order_detail.html` 两处 fetch 调用缺 `X-CSRFToken` 头，存在 CSRF 漏洞 | 检查模板 fetch 调用使用 `csrfPost` helper 或继承 `base.html` 全局 `window.fetch` wrapper（SM-P6-FIX-01） |
| BUG-SALES-010 | `sales_out_draft` AI 工具语义错配：工具名"销售出库草稿"但描述/端点均为"售后出库" | 检查 `sales_out_draft` 标记为 deprecated alias，新增 `after_sale_out_draft`（端点 `add_after_sale_out_order`）+ `sales_outbound_draft`（端点 `create_sales_outbound_draft`）（AI-SALES-F01-FIX-02） |
| BUG-SALES-011 | `VALID_ROLES` 缺 `'sales'` 角色，导致 `sales_out_draft` 工具合规检查失败 | 检查 `VALID_ROLES` 包含 `'sales'`（AI-BUG-F02 / `b374565`） |
| BUG-SALES-012 | 销售模块无 AI 异常分析与单据联查能力，对比采购侧 `out_order_detail.html` 缺失 | 检查 `sales_order_detail.html` 含 AI 异常分析按钮 + `/api/ai/sales_order/<id>/anomaly_analysis` 只读路由 + 售后单联查面板（AI-SALES-F01-FIX-02） |
| BUG-SALES-013 | 销售模块无 AI 履约跟进工作台，对比采购侧 `AI-R11-F01` 7 队列结构缺失 | 检查 `ai_sales_workbench.html` + `sales_followup_workbench.py` 7 队列 + `sales_followup` Agent + `sales_insights` 工具 + 3 路由（AI-SALES-F02） |
| BUG-SALES-014 | 销售模板散用 `confirm()`/`alert()` 原生 API，与系统级 `showConfirm`/`showToast` 不一致 | 检查 5 个销售模板（`sales_outbound_selection.html`/`customer.html`/`after_sale_out.html`/`after_sale_out_add.html`/`after_sale_out_detail.html`）不再含 `confirm(`/`alert(` 调用（SM-P6-02） |
| BUG-SALES-015 | `sales_order.html` 工具栏 + 行内写按钮无角色权限感知隐藏，`user`/`production` 角色可见写操作入口 | 检查写按钮包裹 `{% if current_user.role in ['admin','warehouse','purchase','sales'] %}`，后端 `@require_role` 仍二次校验（SM-P6-02） |
| BUG-SALES-016 | `customer.html` 完全无客户导入入口，与 `supplier.html` 结构不一致 | 检查 `customer.html` 含 `importModal` 模态框 + AJAX 提交 + `csrf_token` + `notifyMasterDataChanged('customer_updated')` 广播（SM-P6-02） |
| LOGIN-CSRF-001 | Web `/login` 被 `@csrf.exempt` 豁免，无 CSRF token 的 POST 仍可建立会话 | 检查 `app.login` 不在 `csrf._exempt_views`，`app.native_api_login` 仍豁免，`login.html` 含 `csrf_token`，无 token POST `/login` 返回 400 |
| BUG-INSPECT-2026-07-27-001 | `verify_ai_tool_schemas.py` 的 `VALID_PAYLOADS` 未覆盖 AI-SALES-F02 新增的 `sales_followup_agent` 和 `sales_insights`，导致 `verify_ai_all.py --level smoke` 6/7 失败 | 在 `scripts/verify_ai_tool_schemas.py` `VALID_PAYLOADS` 中补齐这两条 valid payload（参考 `app/ai/tools/registry.py:242-259` 的 `SALES_INSIGHTS_SCHEMA`/`SALES_FOLLOWUP_SCHEMA`），`verify_ai_all.py` 升至 7/7 |
| BUG-MOBILE-2026-07-27-001 | 375/414 窄屏下出入库列表/详情页工具栏按钮可能并排挤压、触摸目标不足、长单号+状态徽章溢出可视区、模态框超出宽度（巡检报告 P2 遗留项） | 在 `app/static/css/custom.css` 末尾新增 `@media (max-width: 414px)` 块：`.wms-entry-toolbar` 改单列网格+按钮全宽+`min-height:40px`；`.order-title/.order-meta` 允许换行；`.modal-dialog` 限制 `max-width:calc(100vw-16px)`；`.table-responsive-wrapper` 不遮挡按钮；`.pagination` 换行居中。`scripts/verify_mobile_buttons.py` 静态校验通过；Playwright 运行时验证 375px 下 20 个按钮全部 `visible=True overflow=False` 高度 40px；截图存档 `qa_screenshots/mobile_375_*.png`、`qa_screenshots/mobile_414_*.png` |
| BUG-DATE-2026-07-27-001 | `InOrder.date` / `OutOrder.date` 默认 `date.today()` 但无未来日期强校验，巡检报告 C 维度遗留：凭证日期可被提前到未来账期 | 新增 `app/app.py:is_future_date(order_date, today=None)` helper（`order_date > date.today()`）；在 `add_in_order`/`update_in_order`/`complete_in_order` 三路由入口校验后返回 400 + `入库日期不能晚于今天`；在 `add_out_order`/`complete_out_order` 两路由入口校验后返回 400 + `出库日期不能晚于今天`；`in_order_detail`/`in_order_add_page`/`out_order_add_page` 三个 `render_template` 注入 `today=date.today()`；`in_order_add.html`/`out_order_add.html`/`in_order_detail.html` 三个日期 input 增加 `max="{{ today }}"`。`scripts/verify_future_date_guard.py` 25 项检查全绿（含 Flask test_client 未来日期被 400 拒、今天日期通过） |
| BUG-BATCH-2026-07-27-001 | `/in_order/batch_delete`、`/in_order/batch_complete`、`/out_order/batch_delete`、`/out_order/batch_complete` 四个批量接口无单次条数上限，超长事务可能阻塞数据库（巡检报告 P3 遗留项） | 四个 batch 路由入口在 `ids` 解析后加 `if len(ids) > 100: return jsonify({'status':'error','msg':'单次批量操作不能超过 100 条，请分批处理'}), 400`；前端 `batchInOrderAction`/`batchOutOrderAction` 在提交前加 `if (ids.length > 100) { showToast(...); return; }`。`scripts/verify_batch_limit.py` 26 项检查全绿（静态 4 后端+2 前端+动态 101 条被 400 拒×4+50 条不被拦截×4） |
| BUG-2026-07-29-001 | 全新空库上 `auto_migrate_database()` 启动失败 | `app/app.py:auto_migrate_database()` 新增 `_table_exists()` 守卫；目标表 `out_order` 不存在时 commit + close + return，DDL 交给 `db.create_all()` 处理。`scripts/verify_bug_2026_07_29_001.py` 覆盖「空库启动→HTTP 200」+「已有库迁移正常」双场景（commit `61d077e`/`ca271e2`） |
| BUG-2026-07-29-002 | 物料/供应商/客户 `name` 字段 XSS 风险 | `app/app.py` 新增 `sanitize_text_input(value, max_len)`：去 NUL/控制字符/HTML 尖括号/`javascript:` 协议；`add_material`/`add_supplier`/`add_customer` 9 字段同步接入（commit `64bbaea`） |
| BUG-2026-07-29-003 | POST 校验错误统一返回 HTTP 400 | `app/app.py` 新增 `api_error(msg, code=400)` helper；用脚本批量替换 745 处 `return jsonify({'status':'error',...})` 为 `return api_error(...)`；空表单 POST 现在返回 400 而非 200（commit `ac3d4ce`） |
| BUG-2026-07-29-004 | CSRF token 可重放：8 小时过长 | `app/config.py` `WTF_CSRF_TIME_LIMIT` 从 28800 缩短至 1800（30 分钟）（commit `7f2ed4f`） |
| BUG-2026-07-29-005 | 物料 `stock=999999999999`（12 位）被接受 | `app/app.py` 新增 `MAX_REASONABLE_STOCK = 99_999_999.99`；`add_material` 中 `parse_bounded_number` 显式传 `maximum=MAX_REASONABLE_STOCK`（commit `82ec4e9`） |
| BUG-2026-07-29-006 | `/material/print_label` `/stock_query/print` `/report/print` 三个未实现路由 404 行为模糊 | 显式注册三个 stub handler 显式返回 `api_error(..., code=404)` 带中文说明；全量 grep 确认 `app/templates/` 没有任何 `url_for`/`href` 引用这三个 URL（commit `c1d6235`） |
| BUG-2026-07-29-007 | 5000 字符 URL 参数被接受，无截断/告警 | `app/app.py` 新增 `QUERY_STRING_MAX_LENGTH = 2048` 与 `@app.before_request limit_query_string_length()`：超长返回 HTTP 414 + 中文提示（commit `0d8e966`） |
| BUG-2026-07-29-008 | admin `must_change_password=True` 时访问 `/in_order/<id>/print` 被强制重定向到 `change_password` | `enforce_initial_password_change` 中将所有 `print_*` 端点加入白名单；admin 即使未改初始密码也能打印单据（commit `a0d2a14`） |
| BUG-2026-07-29-009 | NUL 字节 `\x00` 被静默吞掉 | 复用 `sanitize_text_input` 的 NUL 字节移除逻辑，物料 `code`/`name`/`spec` 同时受益（commit `64bbaea`，随 BUG-002 一并提交） |
| BUG-2026-07-29-010 | admin 锁定后 GET `/login` 仍正常渲染无前端倒计时 | `app/app.py:login()` GET 分支显式探测 `User.locked_until` 与 `login_ip_locked_until`，把 `locked` / `lock_remaining_seconds` 注入 `login.html` 模板；前端 `lockHint` + `lockCountdown` JS 倒计时在锁定时可见（commit `cb21e32`） |
| BUG-2026-07-28-001 | 404 错误页空白：`GET /this_does_not_exist` 返回纯文本 `('页面不存在', 404)`，无模板 | `app.py` 新增 `render_template('404.html', path=request.path)`；`app/templates/404.html` 新建（Bootstrap 5 + 404 卡片 + 返回首页/返回上一页 + 路径显示）。`scripts/verify_bug_001.py` 静态+动态全绿 |
| BUG-2026-07-28-002 | 405 错误页空白：缺 `@app.errorhandler(405)`，GET `/supplier/add` 等被 405 走默认 Flask 空白页 | `app.py` 新增 `method_not_allowed()` handler + `render_template('405.html')`；`app/templates/405.html` 新建（与 404 同布局，状态色琥珀）。`scripts/verify_bug_001.py` 同时覆盖 405 卡片断言 |
| BUG-2026-07-28-003 | `/purchase_order` 默认 302 → `/purchase_order/add`，用户期望默认显示列表 | `app.py:34910` 改 `view in ('add','new')` 才跳新增；默认走 `pagination + orders` 渲染列表。`scripts/verify_bug_003.py` 断言默认 200 + `?view=add` 302 |
| BUG-2026-07-28-004 | admin 在 `/user` 列表可自助点击「重置密码」按钮，无任何二次校验，违反 AGENTS.md R-1 强规则 | `app.py:reset_user_password` 增加 `if user_id == current_user.id → 403`；admin 目标（`role=='admin'` 或 `username=='admin'`）要求 `bootstrap_pwd == WMS_BOOTSTRAP_PASSWORD`；`user.html:103-110` 自身行按钮 `disabled`+tooltip；`user.html` `resetPassword()` JS 加 admin 二次确认。`scripts/verify_bug_004.py` 覆盖所有失败路径 |
| BUG-2026-07-28-005 | 入/出库单保存不校验仓库/供应商/明细必填，可生成空单 | `app.py` 新增 `_validate_order_business_required(form, kind)` 统一 helper；`add_in_order`/`add_out_order`/`add_adjustment`/`add_transfer`/`add_check` 5 路由入口调用，未通过返回 400 + 中文 msg。`scripts/verify_bug_005.py` 覆盖空仓库/空物料/正常保存 |
| BUG-2026-07-28-006 | `/supplier` `/customer` 等列表页表头首列（checkbox 列）显示 `COLU...` 截断 | `app.js columnsOf` 识别 checkbox-only 列不设 label（避免回退到 `key`）；`custom.css` 新增 `.cb-check-col { width: 50px; min-width: 50px; max-width: 50px; }` 列宽兜底。`scripts/verify_bug_006.py` 抓首列 th 文本 ≠ `'COLU...'`、width==50 |
| BUG-2026-07-28-007 | `/purchase_request` `/purchase_order?view=list` `/out_order` `/check` `/requisition` 直接访问时同时存在 list 自身工具栏 + 嵌入工具栏 | `app.js insertGlobalActionBar` 增加 `isWmsEmbeddedPage()` 守卫（直接访问模式 return）；`custom.css` 新增 `body:not(.embedded-page) #cbGlobalActionBar { display: none !important; }` 兜底；嵌入模式（`?embedded=1`）保留双工具栏。`scripts/verify_bug_007.py` 浏览器访问对比 |
| BUG-2026-07-28-008 | `/material` 空库时分页区「共 0 条」+ 表格「暂无数据」并存 | `_list_macros.html:pager()` 在 `pagination.total==0` 时返回空字符串；空库列表统一包 `{% if pagination.total > 0 %}{...}{{pager(...)}}{% else %}{...}暂无数据...{% endif %}` 互斥。`scripts/verify_bug_008.py` 抓 HTML 断言互斥 |
| BUG-2026-07-28-009 | `/requisition` 分页区写「共 0 单」与其他列表「共 0 条」单复数不一致 | `requisition.html` 手工分页 div 改为 `{{ pager(pagination, 'requisition_list') }}` 宏；`subcontract.html`/`sales_order.html`/`check.html`/`transfer.html`/`adjustment.html` 同步切换。`scripts/verify_bug_009.py` 抓 HTML 断言统一「共 N 条」 |
| BUG-2026-07-28-010 | `GET /supplier/add` 被 BUG-002 修复为 405 卡片，业务上希望 GET 该路径直接弹出新增 modal | `app.py:supplier_add` 路由 `methods=['POST']` 改 `methods=['GET','POST']`；GET 重定向 `/supplier?showAddModal=1`；列表页 `showAddModal=1` query 自动弹 modal。`scripts/verify_bug_010.py` 覆盖 302+列表页 modal 触发 |
| BUG-2026-07-28-011 | 登录页输错 1 次提示「还可尝试 2 次」但无锁定 UI、无倒计时、无 IP 累计提示 | `app.py:login()` 锁定分支返回 `lock_remaining` 秒数 + `locked:True`（HTTP 423）；`login.html` 新增 `lockHint` 倒计时 span + 按钮置灰 + IP 累计警告条；`showLockCountdown` JS 每秒 tick 减秒。`scripts/verify_bug_011.py` 5 次失败 → 第 5 次 423 + 倒计时 |
| BUG-2026-07-28-012 | `/operation_audit` 「旧日志 0 / 变更审计 0」与系统术语不一致 | `operation_audit.html` 统一为「历史审计 / 实时审计」+ tooltip 解释对应 `OperationLog`/`OperationAudit` 两表。`scripts/verify_bugs_012_to_020.py` 抓页面断言「历史审计」「实时审计」存在 |
| BUG-2026-07-28-013 | `/admin/console` 「验收快照 0 / 证据包 0」零值卡片无引导 | `admin_console.html` 零值卡改为 `.clickable-card[data-href]`，验收快照 → `/ai_prelaunch`、证据包 → `/backup`；非零值维持原数字。`scripts/verify_bugs_012_to_020.py` 抓 admin_console 断言 data-href 存在 |
| BUG-2026-07-28-014 | 9+ 新增单据页（入库/采购/出库/盘点/领料/调拨/调整/销售/委外）仅有「保存」「返回」 | `in_order_add.html` 等 9 模板 footer 加「保存并新建」按钮（`saveAndNew` JS fetch + `keep=1` 提交）；后端 `*_add_page` 接收 `keep=1` query 保留 `supplier_id/customer_id/warehouse` 表头字段。`scripts/verify_bugs_012_to_020.py` 抓 9 模板断言「保存并新建」存在 |
| BUG-2026-07-28-015 | 连续打开 16+ 页面后 Tab 栏无限累积，浏览器卡顿 | `app/static/js/app.js` `WmsTabs.MAX=15`，`open()` 超过 MAX 自动关闭最早 tab；新增 `closeOthers/closeAll` + 右键菜单「关闭当前/关闭其他/全部关闭」+ `localStorage` 持久化。`scripts/verify_bugs_015_020.py` 静态断言 MAX_TABS + closeOthers/closeAll/contextmenu |
| BUG-2026-07-28-016 | 右下角 AI 助手浮窗在窄屏或按钮密集处遮挡底部按钮 | `base.html` 新增 `#aiAssistantHideBtn`×关闭按钮 + `hideAiAssistantFloating()` JS（`localStorage.wms_ai_hide_floating=1` 记忆）；scroll 监听：滚到底部 80px 内 `transform:translateY(120%); opacity:.3` 半隐藏。`scripts/verify_bugs_015_020.py` 抓 base.html 断言 |
| BUG-2026-07-28-017 | `/in_order` 「入库明细」与 `/in_order/add`「新增采购入库单」标题口径不一致 | `in_order.html` title 改「入库单」+ 列表 h2 改「入库单」；`in_order_add.html` title 改「新增入库单」+ h2 改「新增入库单」。`scripts/verify_bugs_015_020.py` 抓 HTML 断言 |
| BUG-2026-07-28-018 | `/supplier` `/customer` placeholder 顿号不一致（电话/地址无顿号） | `supplier.html` `customer.html` placeholder 全部统一为「搜索供应商/客户编号、名称、联系人、电话、地址」全顿号。`scripts/verify_bugs_015_020.py` 抓 HTML 断言全顿号 |
| BUG-2026-07-28-019 | `/category` 所有分类都显示「1 级」（`row.level` 永远 0） | `app.py:category_list` 引入 `build_category_tree_rows()` 按 `parent_id` 递归计算 `level` + 树枝 `├─/└─` 缩进 + 防环（visited 集合）；`category.html` 改为 `<span class="category-level-badge lv{{row.level+1}}">{{row.level+1}} 级</span>` + 5 套分级色（lv1 靛蓝/lv2 青色/lv3 绿色/lv4 琥珀/lv5+ 红色）。`scripts/verify_bugs_015_020.py` + `dbg_check_levels2.py` 抓 HTML 断言 lv1-lv4 CSS+徽标 class+title |
| BUG-2026-07-28-020 | `/stock_query` 「打印模板」按钮一直常驻，空数据时无意义 | `stock_query.html` 按钮加 `{% if not stock_rows or stock_rows\|length == 0 %}disabled title="请先查询数据" aria-disabled="true"{% else %}onclick="printTemplate()"{% endif %}`；无数据徽标 + 「暂无数据」占位。`scripts/verify_bugs_015_020.py` 抓 HTML 断言 `bi-printer` + `disabled` + `无数据` |
| BUG-F02-01 | 基础资料列表默认按 `created_at desc` 排序，与查字典习惯相反 | `app.py` `_get_master_list_filters` 默认 `sort_by='code'`/`order='asc'`，5 个 list 路由同步；模板统一用 `sort_th` 宏。`audit_screenshots/verify_f02_01_sort.py` 33/33 |
| BUG-F02-02 | 物料/供应商/客户 6 个 add/edit 路由对 code/name/spec/purpose/remark/contact/phone/address 不做长度校验，DB 静默截断 | `app.py` 6 路由入口加 11 字段长度校验（code 50/name 100/spec 100/purpose 200/remark 500/contact 50/phone 20/address 200）；超限返 400 + 中文 msg。`audit_screenshots/verify_f02_02_truncate.py` 30/30 |
| BUG-F02-03 | 标签模板设计页点"保存布局"后端 404，前端 `data.status === 'success'` 假阳性，误报保存成功 | `app.py` 新增 `save_label_template_layout`（`@require_role admin/warehouse` + layout 非空/类型校验 + 审计 log）；`label_template_detail.html` `saveLayout()` 加 `response.ok` 检查 + `_savingLayout` 守卫 + `spinner-border` 反馈 + `console.error` 详细错误。`audit_screenshots/verify_f02_03_label_save.py` 16/16 |
| BUG-F02-04 | 仓库被停用/删除后，入库单仍可选用，导致库存归属不明 | `app.py` 新增 `is_warehouse_active`/`assert_warehouse_active` helper；`add_in_order`/`update_in_order` 入口校验；不通过返 400 + 中文 msg。`audit_screenshots/verify_f02_04_warehouse.py` 12/12 |
| BUG-F02-05 | `location_management_enabled=False` 时入库单前端仍把仓库字段渲染为必填 | `in_order_add.html` 仓库字段包 `{% if location_management_enabled() %}` 条件渲染；JS 同步用 `locationManagementEnabled` 控制必填；`add_in_order` 后端同步允许空仓库。`audit_screenshots/verify_f02_05_location_off.py` 13/13 |
| BUG-F02-06 | 普通用户没有自助改自己资料（电话/邮箱/备注）的入口；admin 改他人审计缺 `last_modified_by` | `app.py` 新增 `edit_my_profile`（仅改 email/phone/bio，邮箱/电话格式校验，长度限制 200/30/500，不可改 username/role/status/password）+ User 模型加 `email/phone/bio` 列 + 迁移；`edit_user` `log_operation` 显式带 `last_modified_by=current_user.username`；`my_profile.html` + 侧边栏入口。`audit_screenshots/verify_f02_06_profile.py` 17/17 |
| BUG-F02-07 | 主数据列表分页 `per_page` 无上限校验，URL 不记忆每页大小 | `app.py` 5 list 路由统一 `per_page` 白名单 [10,20,50,100,200]，默认 20；`_list_macros.html` 新增 `per_page_select` 宏；`base.html` 自动绑定 `.per-page-select` 切换 URL。`audit_screenshots/verify_f02_07_pagination.py` 17/17 |
| BUG-F02-08 | purchase/sales 等非授权角色可访问 `/label_template/<id>` 设计页，点保存才 403 | `app.py` `label_template_detail` 加 `@require_role('admin','warehouse')`。`audit_screenshots/verify_f02_08_template_perm.py` 4/4 |
| BUG-F02-09 | `material.html` 标签模板设计器 16 处调用从未定义的 `saveTemplateToStorage()`，任何编辑交互即抛 `ReferenceError`，设计结果无法持久化 | `material.html` 新增 `saveTemplateToStorage()`（序列化当前编辑器状态到 `localStorage.labelTemplateDraft`）+ `restoreTemplateDraft()`（无已保存模板时自动恢复草稿）+ `saveTemplate()` 成功后清除草稿。`audit_screenshots/verify_f02_09_10_frontend.py` 14/15（浏览器实测项因本机无浏览器跳过） |
| BUG-F02-10 | `warehouse.html`/`department.html`/`employee.html` 的 GET 筛选表单内含 `csrf_token` 隐藏域，筛选后 token 明文出现在地址栏 URL | 三个模板 GET 表单删除 `csrf_token` 隐藏域（POST 模态框表单保留）。`audit_screenshots/verify_f02_09_10_frontend.py` 静态+线上 HTTP 双重验证通过 |
| BUG-2026-07-29-001 | 全新空库上 `auto_migrate_database()` 启动失败，删除 `instance/inventory.db` 后直接 `python3 app/run_server.py` 服务退出，HTTP=000 | `app/app.py:auto_migrate_database()` 新增 `_table_exists()` helper；目标表 `out_order` 不存在时 commit + close + return，DDL 交给 `db.create_all()` 处理。`scripts/verify_bug_2026_07_29_001.py` 覆盖「空库启动→HTTP 200」+「已有库迁移正常」双场景 |
| BUG-2026-07-29-002 | 物料/供应商/客户 name 字段接受未净化的 `<script>` 标签，存储型 XSS 风险 | `app/app.py` 新增 `sanitize_text_input()` helper（NUL/控制字符/HTML 尖括号净化+截断）；`add_material`/`add_supplier`/`add_customer` 等主数据路由同步接入。`scripts/verify_bug_2026_07_29_002.py` 断言尖括号与 NUL 字节均被过滤 |
| BUG-2026-07-29-003 | POST 表单校验错误统一返回 HTTP 200 + `{"status":"error",...}`，违反 REST 规范 | `app/app.py` 全局 `grep -n 'return jsonify({\"status\": \"error\"'` 排查，新增 `api_error(msg, code=400)` helper，30+ 路由统一改 `return api_error(...)`。`scripts/verify_bug_2026_07_29_003.py` 覆盖空表单/缺字段/正常提交三路径 |
| BUG-2026-07-29-004 | CSRF token 8 小时过期过长，截获一次可无限滥用 | `app/config.py` `WTF_CSRF_TIME_LIMIT` 从 28800 改 1800（30 分钟）。`scripts/verify_bug_2026_07_29_004.py` 覆盖 token 短过期 |
| BUG-2026-07-29-005 | 物料 stock=999999999999（12 位）被接受，超业务合理边界 | `app/app.py` `add_material`/数量路由 `MAX_REASONABLE_STOCK=99999999.99`/`MAX_REASONABLE_PRICE=99999999.99` 收紧上限；超限返回 400。`scripts/verify_bug_2026_07_29_005.py` 断言 12 位数被拒 |
| BUG-2026-07-29-006 | `/material/print_label`、`/stock_query/print`、`/report/print` 等打印/导出路由 404 | `app/app.py` 排查后端 `@app.route` 残留；缺实现路由按 `print_in_order` 模板补齐或前端按钮切换为 `window.print()`。`scripts/verify_bug_2026_07_29_006.py` 覆盖前端模板引用→后端实现一致 |
| BUG-2026-07-29-007 | 5000 字符 URL 参数被接受，无截断/告警 | `app/app.py` 入口 `@before_request _limit_query_string` 拦截 >2048 字节 query string 返回 414。`scripts/verify_bug_2026_07_29_007.py` 覆盖 5000 字符拒绝 |
| BUG-2026-07-29-008 | `/in_order/{id}/print` 已登录 admin 仍 302 | `app/app.py:print_in_order` 补齐 `@require_role('admin','warehouse','purchase')` admin 误伤修复。`scripts/verify_bug_2026_07_29_008.py` 覆盖 admin/warehouse/purchase 三角色 |
| BUG-2026-07-29-009 | NUL 字节 `\x00` 被静默吞掉 | `sanitize_text_input()` 同时去除 NUL 字节（随 BUG-002 一并提交）。回归测试覆盖 code/name 全字段 |
| BUG-2026-07-29-010 | 锁定后 `/login` GET 仍正常渲染，无前端倒计时 | `app/app.py:login()` GET 分支检测 admin 锁定 → 传 `lock_remaining`/`locked_account` 模板；`login.html` 新增 `lockHint` 倒计时 + JS 每秒 tick。`scripts/verify_bug_2026_07_29_010.py` 覆盖 5 次错误后 GET 页面含倒计时 span |
| BUG-2026-07-31-001 | 长会话 CSRF token 过期（30 分钟寿命），停留 30 分钟后所有非 GET 请求失败 | `app/app.py` 新增 `POST /api/csrf_refresh` 端点（`@csrf.exempt`，返回新 token）；`app/templates/base.html` 每 25 分钟（寿命 30 分钟，提前 5 分钟保险）调用一次 + 页面 visibilitychange 切回前台时立即调用，更新 `<meta name="csrf-token">` content。根治用户停留 30+ 分钟的所有 CSRF 失败场景。 |
| BUG-2026-08-09-003 | 手机 WMS 语音功能卡在"正在聆听，请说出指令…"无法退出 | `VoiceCommandViewModel.startListening()` 启动 8 秒 `viewModelScope` 超时 Job；`stopListening`/`dispatchResults`/`onError` 三处取消该 Job；超时后自动 destroy recognizer 并写入 `error="识别超时，请重试"`。`onError` 新增 `ERROR_NETWORK/ERROR_SERVER/ERROR_CLIENT` 三个细分提示。`scripts/verify_bug_2026_08_09_003.py` 静态覆盖：超时 Job 存在 + 8 秒阈值 + 三处 cancel + 三个新增 error 分支 |
| BUG-2026-08-02-001 | 未启用库位管理时入库单仓库被误设为可选，导致可保存无仓库入库单；仓库与库位概念混淆 | `app/app.py` 新增 `prefer_default_warehouse()`/`get_default_warehouse()` helper；`add_in_order`/`update_in_order`/`complete_in_order`/`update_completed_in_order`/`batch_complete_in_order` 均强制仓库必填，未填写时自动取默认仓库，无默认仓库时拒绝保存/完成；`in_order_add.html`/`in_order_detail.html` 仓库字段加 `required` 与默认仓库选中，移除 `locationManagementEnabled` 控制仓库必填的旧逻辑。`scripts/verify_bug_2026_08_02_001.py` 静态+动态全量覆盖 |
| BUG-2026-08-02-002 | `add_out_order` 领料/其他出库仓库被位置管理开关误设为可选，与 AGENTS.md 仓库必填规则冲突 | `app/app.py:add_out_order` 非销售出库分支移除 `location_management_enabled()` 条件，改为：未填仓库时优先 `get_default_warehouse()` 自动带入，无默认仓库返回 400 `请选择仓库`（commit `3b9aba5a`）。`scripts/verify_bug_2026_08_02_002.py` D1/D2 静态+动态覆盖 |
| BUG-2026-08-02-003 | `complete_out_order` 仓库校验依赖位置管理开关；首版修复把 `order.warehouse = default_wh.name` 放在 `_acquire_order_write_lock` 之前，被其 SQLite 分支 `db.session.rollback()` 丢弃，导致存量无仓库 pending 单据完成时仓库仍为空 | `app/app.py:complete_out_order` 改为：锁前只做 fast-path 读校验（`if not order.warehouse and not get_default_warehouse(): return api_error('请选择仓库')`），实际赋值与必填校验移到加锁后 `order = locked` 之后再执行，确保赋值随 commit 落库（commit `324ef4d3` + 修复 commit 见本次提交）。`scripts/verify_bug_2026_08_02_002.py` D3 验证完成时 `order.warehouse == 默认仓库` |
| BUG-2026-08-02-004 | `batch_complete_out_order` 缺仓库校验，存量无仓库 pending 单据批量完成时跳过校验直扣库存 | `app/app.py:batch_complete_out_order` 加锁后 `if not order.warehouse: default_wh = get_default_warehouse(); ...; if not order.warehouse: skipped.append(f'{order.order_no}(未填写仓库)'); rollback; continue`，与单据版一致，跳过本单不阻断整批（commit `504f923f`）。`scripts/verify_bug_2026_08_02_002.py` D4 验证默认仓库自动带入 |
| BUG-2026-08-02-005 | `add_after_sale_out_order` 售后出库 `warehouse` 字段模型已存在但闲置，从未读取/校验 | `app/app.py:add_after_sale_out_order` 读取 `data.get('warehouse')`，未填时 `get_default_warehouse()` 自动带入，无默认仓库返回 400 `请选择仓库`，并显式 `order.warehouse = warehouse` 落库（commit `6917860b`）。`scripts/verify_bug_2026_08_02_002.py` D5/D6 覆盖 |
| BUG-2026-08-02-006 | `complete_after_sale_out_order` 完成时不校验仓库，存量无仓库 pending 单据完成时扣库存无仓库归属 | `app/app.py:complete_after_sale_out_order` 在 `_acquire_order_write_lock` 之后做仓库自动带入与必填校验，赋值随 commit 落库（commit `fbc9c3a9`）。`scripts/verify_bug_2026_08_02_002.py` D7 验证 |
| BUG-2026-08-02-007 | `after_sale_out_add.html` 仓库字段从未渲染，前端无法选择仓库 | `app/templates/after_sale_out_add.html` 新增仓库 `<select name="warehouse" required>` 字段，循环 `warehouses` 选项并按 `default_warehouse`/`order.warehouse` 选中；JS `submitForm()` 增加仓库非空前端校验（commit `ea8df15b`）。`scripts/verify_bug_2026_08_02_002.py` B1/B2 静态断言 |
| BUG-2026-08-02-008 | `out_order_add.html` 仓库字段未带 `required`，未预选默认仓库；前端 JS 无仓库非空校验 | `app/templates/out_order_add.html` 仓库 `<select>` 加 `required` 属性，循环选项按 `prefill.warehouse`/`default_warehouse` 选中；JS `submitForm()` 增加 `if (!warehouseValue) { alert('仓库不能为空，请选择仓库'); return; }` 前端校验（commit `31ea13e9`）。`scripts/verify_bug_2026_08_02_002.py` A1/A2 静态断言 |
| BUG-2026-08-02-009 | `complete_in_order` 仓库赋值写在 `_acquire_order_write_lock` 之前，被其 SQLite 分支 `db.session.rollback()` 丢弃，导致存量无仓库 pending 入库单完成时以 `warehouse=NULL` 落库 + 库位库存不同步 | `app/app.py:complete_in_order` 改为锁前只做 fast-path 读校验（`if not order.warehouse and not get_default_warehouse(): return api_error('入库单必须填写仓库')`），实际赋值与必填校验移到加锁后 `order = locked` 之后再执行，与 `complete_out_order` 实现对齐（commit `2d52e240`）。`scripts/verify_bug_2026_08_02_001.py` D5 验证锁后赋值正确落库 |
| BUG-2026-08-02-011 | `batch_delete_in_order` 删除循环未回退 `source_purchase_order_item.received_quantity`、未调 `update_purchase_order_status`、未加写锁，与单条 `delete_in_order` 逻辑不对称；并发完成后误删风险 | `app/app.py:batch_delete_in_order` 改为：逐条 `_acquire_order_write_lock(InOrder, id, 'pending', selectinload(InOrder.items))`；删除前回退 `source_purchase_order_item.received_quantity` 并收集 `affected_purchase_order_ids`；删除后调 `update_purchase_order_status`；补 `_source_has_active_push` 下推占用校验；每张单据独立 commit，单点失败仅回滚自身（commit `436d92cf`）。`scripts/verify_bug_2026_08_02_004.py` 9 项全绿（含 received_quantity 回退、已完成单拒绝、混合批量删除） |
| BUG-2026-08-02-010 | `complete_adjustment`/`revert_adjustment` 只调 `add_stock`/`deduct_stock_atomic` 改 `Material.stock` 总库存，不调 `update_location_inventory`，`AdjustmentOrderItem.location` 字段闲置；开启库位管理后总库存与库位库存之和长期偏差，且无法对称回退 | `app/app.py:complete_adjustment` 在 `add_stock`/`deduct_stock_atomic` 之后补 `if location_management_enabled() and quantity: update_location_inventory(item.material, item.location, quantity)`；`revert_adjustment` 对称回退 `update_location_inventory(..., -quantity)`；loc_key 暂用 `item.location`，TODO(P1-1) 切换为 `adjustment.warehouse`（commit `a3b9448f`）。`scripts/verify_bug_2026_08_02_003.py` 15 项全绿（正向/负向调整 complete+revert 的总库存与库位库存一致性），`verify_adjustment_state_machine.py` 未回归 |
| P2-1 | `after_sale_out_detail.html` 详情页基本信息卡片未显示仓库字段，单据详情无法核对实物入哪个仓 | `app/templates/after_sale_out_detail.html` 在客户名称下方补 `<p><strong>仓库：</strong>{{ order.warehouse or '-' }}</p>`（commit `61452e84`） |
| BUG-2026-08-02-012 | `TransferOrder`/`InventoryCheck`/`AdjustmentOrder` 三个模型无 `warehouse` 字段，违反 AGENTS.md 仓库必填规则；`TransferOrder` 用 `from_location`/`to_location` 存仓库名，仓库与库位概念混淆 | 模型层：`AdjustmentOrder`/`InventoryCheck` 加 `warehouse` 列；`TransferOrder` 加 `from_warehouse`/`to_warehouse` 列（保留 `from_location`/`to_location` 作库位字段）；`auto_migrate_database` 含三表 ALTER + 存量数据回填默认仓库名（commit `738728dc`）。路由层：`add_adjustment`/`save_check_table`/`add_check` 仓库必填+默认仓库带入；`save_transfer_table`/`add_transfer` 调出/调入仓库必填，同时写 `from_warehouse`/`to_warehouse` 和 `from_location`/`to_location`（库位字段历史兼容）（commit `2dfe08ee`）。`scripts/verify_bug_2026_08_02_005.py` 27 项全绿（含 S1-S8 模型/迁移静态、D1-D8 字段落库、S9 路由静态、D9-D14 路由动态必填+默认带入） |
| BUG-2026-08-02-013 | `complete_transfer`/`revert_transfer` 无条件调 `deduct_location_inventory_atomic`/`update_location_inventory`，未开启库位管理时也写 `LocationInventory`，与入库/出库的 `if location_management_enabled():` 模式不一致；`_create_adjustment_drafts_from_check`/`_scan` 生成的调整草稿不带 warehouse，导致 `complete_adjustment` 的 loc_key 回退到 `item.location` | `complete_transfer`/`revert_transfer` 加 `use_location = location_management_enabled()` 守卫，库位库存操作包在 `if use_location:` 内，流水记录（`add_stock_transaction`）保留在守卫外（未开启也记审计流水）；`_create_adjustment_drafts_from_check`/`_scan` 调整草稿 `warehouse=getattr(check, 'warehouse', None)`；`complete_adjustment`/`revert_adjustment` 守卫 P0-2 已补（commit `b2b9a94d`）。`scripts/verify_bug_2026_08_02_007.py` 9 项全绿（含 D1 库位关闭时 complete_transfer 不写 LocationInventory 但记流水、D2 盘点草稿带 warehouse），`verify_bug_2026_08_02_003.py`/`verify_adjustment_state_machine.py` 未回归 |
| BUG-2026-08-02-014 | 盘点单 `check.html` 新建弹窗和 `document_table_form.html` 编辑表单无仓库字段，违反 AGENTS.md 仓库必填规则 | `check_list` 路由补传 `warehouses`/`default_warehouse`；`_render_check_form` 补传 `warehouses`/`default_warehouse` 并在 header 带 `warehouse`；`check.html` 新建弹窗加 `<select name="warehouse" required>` 默认预选；`document_table_form.html` check 分支加仓库下拉+required+默认预选；`collectHeader()` check 分支带 warehouse；`refreshWarehouses()` 支持 check（commit `e6b6b772`） |
| BUG-2026-08-02-015 | `adjustment_add.html` 表单无仓库字段，`adjustment_add_page`/`adjustment_detail` 路由未传 `default_warehouse`，且 warehouses 查询未过滤启用状态 | `adjustment_add_page`/`adjustment_detail` 改用 `get_active_warehouses()` 并传 `default_warehouse=get_default_warehouse()`；模板在操作人旁补 `<select name="warehouse" required>` 编辑回填 `adjustment.warehouse`、新建预选默认仓库；`submitForm()` 增加仓库非空前端校验并写入 data.warehouse（commit `479bfcc7`） |
| BUG-2026-08-02-016 | `transfer.html` 新建弹窗和 `document_table_form.html` 调拨表单的调出/调入仓库下拉无 `required`、无默认预选；`_render_transfer_form`/`transfer_list` 未传 `default_warehouse` | `_render_transfer_form`/`transfer_list` 补传 `default_warehouse=get_default_warehouse()`；`transfer.html` 弹窗调出仓库预选默认仓库、调出/调入加 `*` 必填标记；`document_table_form.html` 调拨分支调出/调入 `<select>` 加 `required`、调出仓库新建时预选默认仓库（commit `fc60ddf8`） |
| BUG-2026-08-02-017 | `opening_stock.html` 新增期初库存单的仓库下拉已有 `required` 但未预选默认仓库；`opening_stock_list` 路由未传 `default_warehouse` | `opening_stock_list` 补传 `default_warehouse=get_default_warehouse()`；`opening_stock.html` 仓库 `<option>` 按 `default_warehouse.id == w.id` 预选（commit `5f4f95f9`） |
| BUG-2026-08-02-019 | P1-5 采购入库被设计为必须关联采购订单，与 AGENTS.md"采购订单仅作为可选来源"规则冲突 | 新增/保存/完成 `采购入库` 路径不强制校验 source_purchase_order_id；手工单可直接保存并完成入库；有关联 PO 时仍保留来源/数量/执行进度跟踪。回归 `tests/verify_bug_P15_P16_P21.py` TestBugP15PurchaseInOptionalPurchaseOrder 组 |
| BUG-2026-08-02-020 | P1-6 已完成入库单可通过详情页/列表/批量接口直接删除，违反"人工反提交→草稿→删除"规则 | `delete_in_order` 路由仅允许 pending 单删除，completed/partially_completed 返回 409 `已完成入库单禁止直接删除，请先反提交回草稿状态`；`batch_delete_in_order` 对已完成 ID 整批拒绝。详情页、列表页后端、接口统一规则。回归 `tests/verify_bug_P15_P16_P21.py` TestBugP16CompletedInOrderCannotDeleteDirectly 组 |
| BUG-2026-08-03-001 | 表头列向下填充按钮（`WmsFillDown.fillDown`，app.js）不跳过 `material_code` 空行，把合同编号/工程名称填到所有 15 行（含空行），与"向下填充行数应与物料明细行数一致"规则冲突；昨天 BUG-2026-08-02-021 只修了 Ctrl+D（`setupColumnFillDown`），表头按钮入口未修 | `app/static/js/app.js` `fillDown()` 在遍历源行之后的行时，先检查 `cellFor(row,'material_code')` 是否有值，空行 `skipped++` 跳过不填充，提示含"跳过 N 个空行"；仅在有 `material_code` 列的表格生效，不影响其他表格。回归 `tests/verify_bug_2026_08_03_001_filldown_skip_empty.py` |
| BUG-2026-08-03-002 | Ctrl+D 向下填充（`setupColumnFillDown`）与表头按钮（`WmsFillDown.fillDown`）是两套独立实现：① Ctrl+D 仅采购入库有，销售订单/采购订单/出库单无 Ctrl+D；② Ctrl+D 填数量/单价后不联动金额（`isAmount` 死代码，`calcAmount` 永不触发），按钮会联动；③ 两套实现易出现"修一处漏一处"的回归（正是 BUG-2026-08-02-021→001 的根因） | 将 Ctrl+D 统一到 `app/static/js/app.js` 的 `WmsFillDown` IIFE：新增 keydown 监听复用 `fillDown(table,key)`，所有页面自动获得 Ctrl+D；移除 `in_order_add.html` 的 `setupColumnFillDown`（避免双重触发）。两入口共用同一函数，行为永远一致（跳过空行、派发 input/change 联动金额、提示文案相同）。回归 `tests/verify_bug_2026_08_02_021_in_order_contract.py` T1-T2、`tests/verify_bug_2026_08_03_001_filldown_skip_empty.py` |
| BUG-2026-08-04-012 | 采购入库单详情页"下推"菜单三个链接（下推领料单/其他出库/售后出库）是普通 `<a href>` 锚点，点击后在当前标签页跳转到下推页，原采购入库单详情页被替换（界面关闭），用户无法在查看原单的同时进入领料单/出库单界面 | 给 `in_order_detail.html` 三个下推链接添加 `target="_blank" rel="noopener"`，在新标签页打开下推页，原采购入库单详情页保持打开。回归 `tests/verify_bug_2026_08_04_012_push_open_new_tab.py` T1-T3 |
| BUG-2026-08-04-013 | 复制物料时自动生成的物料编码只基于原物料编码递增，未按"原物料分类 + 流水号"生成 | `generate_material_copy_code()` 参数改为接收 `Material` 对象，优先取 `source.category.code` 作为前缀生成 `{分类编码}{三位流水号}`（补零到 3 位，如分类编码 `101` 已有最大编号 `101009` → 复制生成 `101010`），取该前缀下最大流水号 +1；原物料无分类时回退到原编码递增逻辑保持兼容。回归 `tests/verify_bug_2026_08_04_013_copy_material_category_code.py` T1-T5 |
| BUG-2026-08-04-014 | 采购订单新增页面保存使用原生非 GET `fetch` 提交，违反"所有非 GET 请求必须走 `WMS.api.*`"规则（预提交钩子、CSRF一致性可能不一致） | 迁移原生 `fetch` 为 `WMS.api.post`，复用统一错误处理和CSRF逻辑，保持业务功能不变。lint 规则 `scripts/lint_no_raw_post_fetch.py` 验证通过 |
| BUG-2026-08-04-016 | `/api/material/search` 返回的物料载荷缺少 `brand` 与 `price` 字段：`in_order_detail.html` 用该接口做物料快速搜索，`fillAddMaterialInfo()` 读取 `material.brand`/`material.price`，导致入库单详情页新增物料时品牌恒为空、单价恒为 0.00，覆盖了物料档案里已维护的品牌与价格 | `api_material_payload()` 补充返回 `brand` 与 `price` 字段（对 `/api/material/search` 与 `/api/material/info` 等所有调用方均为新增字段，向后兼容）。回归测试 `tests/verify_bug_2026_08_04_016_material_search_brand_price.py`（2 场景：搜索结果含 brand/price、info 含 brand/price） |

| BUG-2026-08-06-001 | `auto_migrate_database()` 在 Flask config 加载之前执行，且用硬编码路径 `app/instance/inventory.db` 检查数据库，与 SQLAlchemy 实际使用的数据库（由 `SQLALCHEMY_DATABASE_URI` 决定）不一致，导致老库启动时领料单 `out_order.picker` 等字段未被添加，访问领料单直接报 `no such column: out_order.picker` | `app/app.py:auto_migrate_database()` 将迁移调用移动到 config 加载之后，并新增纯函数 `_resolve_sqlite_db_path()` 从 `app.config['SQLALCHEMY_DATABASE_URI']` 解析真实 sqlite 路径（绝对/相对拼接/内存库/非 sqlite 均正确处理）。回归：`tests/test_auto_migrate_db_path.py` 5 用例全绿（绝对路径原样、相对路径拼接 instance_path、内存库返回 None、非 sqlite 返回 None、config 回退）。commit 见本次提交 |
| BUG-2026-08-07-001 | 7 个 AI 相关模板把后端/AI 返回字段直接拼入 `innerHTML`，存在存储型 XSS：`ai_sales_workbench.html`/`ai_warehouse_workbench.html`/`ai_purchase_workbench.html`（section.title/count/metric_scope、item.title/subtitle/detail/jump_url）、`sales_order_detail.html`（AI 异常分析 kind/message/summary/order_no/status）、`ai_business_quality.html`（指标 label、维度值、样本字段、版本对比）、`ai_supplier_evaluation.html`（ai_analysis、msg、supplier_code/name/contact 等）、`in_order_detail.html`（异常提醒 a.msg/a.ai_suggestion，更早一批修复） | 所有动态文本统一经 base.html 全局 `escapeHtml()` 转义；三个工作台跳转链接统一走 `safeJumpUrl()` 白名单（仅允许 `/` 或 `http(s)://` 开头，其余回退 `#`）。回归：`tests/verify_bug_2026_08_07_001_ai_templates_xss_escape.py` T1-T6（静态扫描 7 模板转义覆盖 + 未转义危险拼接模式清零） |
| BUG-2026-08-07-002 | pytest 全量跑时 `test_document_confirmation_golden.py`/`test_fix_db_columns.py`/`test_material_governance_golden.py` 3 个 golden 测试收集报 `ModuleNotFoundError: No module named 'app.ai'; 'app' is not a package`：其他测试模块先 `sys.path.insert(APP_DIR)` + `import app`（命中 app/app.py 模块，无 `__path__`），此后 `from app.fix_db_columns import ...` / `from app.ai... import ...` 包语义导入失败；单跑时 /workspace 在 sys.path，app 以 namespace 包加载故可过——结果依赖导入顺序，属测试隔离缺陷（与 BUG-2026-08-05-007 同类不同因） | `tests/conftest.py` 抢先以模块语义导入 app（与所有业务测试一致），再补 `__path__` 指向 APP_DIR，使 `import app.ai.xxx` / `import app.fix_db_columns` 沿该路径解析，模块/包两种语义共存。回归：`pytest tests/ -q` 139 passed 0 errors（修复前 3 collection errors） |
| BUG-2026-08-08-001 | 库存台账报表页 `report_view.html` 的物料搜索框是纯文本输入，无自动补全，用户输入编码/名称/规格关键词时无法快速选择物料，只能盲打后查询 | `report_view.html` 给物料搜索框新增 client 端自动补全下拉：加载 `/material/api/all`（分页拉全量）到 `allMaterialsForSearch`，`input` 事件按 编码/名称/规格 关键词过滤（`escapeHtml` 转义防 XSS），点选后回填物料编码并关闭下拉；不可用（加载失败）时静默降级为普通输入框。回归：`render_template('report_view.html', report_type='ledger', ...)` 断言 `materialDropdown`/`materialList`/`autocomplete="off"`/`/material/api/all?page=` 渲染均存在 |
| BUG-2026-08-08-002 | 报表页 `report_view.html` 的"客户 / 部门"输入框是纯文本输入，同页物料已有下拉、供应商有 datalist，唯独客户无补全，输入客户名称时无法快速选择，体验不一致 | `report_view.html` 给客户输入框新增 client 端自动补全下拉：加载 `/api/customers` 到 `allCustomersForSearch`，`input` 事件按 名称/编码 关键词过滤（`escapeHtml` 转义防 XSS），点选后回填客户名称并关闭下拉；无匹配时提示"可直接输入部门"并允许自由输入；加载失败静默降级。回归：`tests/verify_bug_2026_08_08_002_report_customer_autocomplete.py` T1-T4（`customerDropdown`/`customerList` 容器渲染、`autocomplete="off"`、`/api/customers` 加载、`loadCustomersForSearch`/`customerSearchTimeout` 过滤逻辑均存在） |
| BUG-2026-08-08-003 | 领料/其他出库单详情页 `out_order_detail.html` 的添加物料模态框 `#code` 与行内新增行 `.material-code-field` 仅支持精确编码盲查（`/api/material/info`），无下拉候选列表，输错或记不全编码时无法选择物料（入库单详情页 `in_order_detail.html` 早已有关键词下拉，两页体验不一致） | `out_order_detail.html` 移植 in_order_detail 的 quick-dropdown 方案：模态框 `#code` 与行内 `.material-code-field` 统一走 `/api/material/search?kw=` 关键词搜索（编码/名称/规格），渲染候选下拉（`escapeHtml` 转义），支持鼠标点选、↑↓ 键盘导航、Enter 选中、Esc/点外关闭；精确命中时自动回填名称/规格/单位/库存/价格。回归：`tests/verify_bug_2026_08_08_003_out_order_detail_material_quick_dropdown.py` T1-T4（下拉容器渲染、`/api/material/search?kw=` 调用、`renderMaterialQuickDropdown`/`selectAddMaterial`/`bindMaterialQuickInput` 函数、键盘导航与点外关闭逻辑均存在） |
| BUG-2026-08-08-004 | 委外单详情页 `subcontract_detail.html` 三个模态框编码输入框（添加产品 `#productCode`、发料 `#issueCode`、收货 `#receiveCode`）完全无 JS 绑定：无下拉、无回填，发料/收货模态框的"搜索"按钮（`openSearchBtn`/`openReceiveSearchBtn`）是死按钮，`issueName`/`issueSpec`/`issueStock`/`receiveName` 禁用字段永远不会被填充，用户只能盲打完整编码 | 复用模板已注入的 `subcontractMaterials` 全量数据做客户端关键词下拉：新增 `filterSubcontractMaterials()`（编码/名称/规格模糊匹配）与通用 `bindScMaterialInput()`（点选、↑↓/Enter/Esc 键盘导航、blur 关闭、`escapeHtml` 转义）；选中后产品码聚焦数量、发料回填名称/规格/库存、收货回填名称/单价；两个搜索按钮绑定 `show()` 展示候选。回归：`tests/verify_bug_2026_08_08_004_subcontract_detail_material_dropdown.py` T1-T5（三个下拉容器渲染、`autocomplete="off"`、过滤/绑定函数、选中回填、搜索按钮绑定均存在） |
| BUG-2026-08-08-005 | 老库仍报 `no such column: production_requisition.warehouse`（BUG-2026-08-05-008 已在模型 + `auto_migrate_database()` 补列，但个别未跑迁移的存量库在启动依赖 `fix_db_columns.py` 的路径下仍缺列，属 BUG-2026-08-05-008 的启动独立性兜底缺失） | `app/fix_db_columns.py` 的 `fix_columns()` 新增 `production_requisition.warehouse` 列迁移，并回填存量数据为默认仓库名（`warehouse.is_default=1 AND status='active'` 第一条，无默认仓则留空）；幂等（已存在则跳过）。回归：`tests/test_fix_db_columns.py` 3 个新用例（`test_fix_columns_adds_warehouse_to_production_requisition`/`test_fix_columns` 断言 `warehouse in pr_cols`/幂等重跑不报错），`pytest tests/test_fix_db_columns.py -q` 6 passed |
| BUG-2026-08-09-001 | Android App 扫码无法识别条码：`ScannerDialog.kt` 的 ML Kit 成功回调中 `scannedFlag.compareAndSet(false, true)` 无条件置位——ML Kit 对未识别到条码的帧同样回调成功（`barcodes` 为空列表），相机首帧（用户尚未对准，几乎必为空结果）即把 `scannedFlag` 置为 true；analyzer 入口 `if (scannedFlag.get()) { imageProxy.close() }` 随后丢弃所有后续帧，ML Kit 从此不再分析任何画面，相机预览与扫描线动画正常但永远识别不出条码 | `ScannerDialog.kt` 成功回调改为仅在 `!rawValue.isNullOrEmpty()` 时才 `compareAndSet` 置位并回调 `onBarcodeScanned`，空结果继续分析后续帧；补充 `addOnFailureListener` 记录识别失败日志（`android.util.Log.w`）便于现场诊断。回归：`tests/verify_bug_2026_08_09_001_scanner_dialog_flag.py` T1-T4（修复模式存在、旧无条件置位模式清零、rawValue 非空守卫、failure listener 存在），并用 `git show HEAD` 旧代码反向验证该测试必然失败 |
| BUG-2026-08-09-002 | 父单 BUG-2026-08-09-001 修复后扫码仍报"摄像头不可用 / Cannot use both setTargetResolution and setTargetAspectRatio on the same config."：`ScannerDialog.kt` 的 `ImageAnalysis.Builder()` 同时调用 `setTargetAspectRatio(AspectRatio.RATIO_16_9)` 和 `setTargetResolution(android.util.Size(1280, 720))`，CameraX 1.3+ 这两个 API 互斥，构造阶段即抛 `IllegalArgumentException`，`bindToLifecycle` 失败，预览区显示"摄像头不可用"，ML Kit 永远收不到任何帧。叠加国产机相机 HAL 碎片化，1280x720 在很多设备上压根不被支持，崩溃概率极高 | 改用 CameraX 1.3+ 官方推荐 `ResolutionSelector` 写法：`AspectRatioStrategy.RATIO_16_9_FALLBACK_AUTO_STRATEGY`（16:9 优先，硬件不支持时自动回退 4:3）+ `ResolutionStrategy.HIGHEST_AVAILABLE_STRATEGY`（最高可用分辨率，国产机多摄自动适配）。同时清掉死代码 `import androidx.camera.core.AspectRatio`。回归：`tests/verify_bug_2026_08_09_002_camerax_resolution_selector.py` T1-T4（3 个 ResolutionSelector import 在场、死 AspectRatio import 已清、`.setTargetAspectRatio()`/`.setTargetResolution()` 调用清零、`ResolutionSelector.Builder()` 链式配置 16:9 + 最高可用并接入 ImageAnalysis），旧代码反向验证 8 项断言全数生效 |
| BUG-2026-08-09-003 | "识物盘点"页点"拍照"按钮 app 自动退出：`AiScreens.kt` 的 `DocumentOcrScreen`/`ObjectRecognizeScreen`/`StocktakeRecognizeScreen` 三个屏幕的"拍照"按钮直接 `cameraLauncher.launch(null)` 启动 `ActivityResultContracts.TakePicturePreview()`，但①未请求 Android 6+ 运行时 `CAMERA` 危险权限，未授权时系统相机会立即抛 `SecurityException` 直接闪退（manifest 仅声明 `<uses-permission>` 无任何运行时 grant 入口）；②拍照回调里 `MediaStore.Images.Media.insertImage(...)` 在 API 29+ 已废弃且因 scoped storage 受限可返回 `null`，紧接着 `android.net.Uri.parse(null)` 抛 `NullPointerException` 第二次闪退（用户拍完照片返回也会崩）。两路径叠加 → "点拍照自动退出 app" | ①封装 `LaunchCameraWithPermission` Composable helper（`AiScreens.kt` 内部 `private` 函数），先通过 `ActivityResultContracts.RequestPermission()` 请求 `Manifest.permission.CAMERA`，授权后才 `cameraLauncher.launch()`；拒绝时通过 `Snackbar` 提示"请授予相机权限后重试"并附"去设置"跳转 `Settings.ACTION_APPLICATION_DETAILS_SETTINGS`；②回调里改写 `saveBitmapToCacheAndGetUri(bitmap, prefix)`：先把 `Bitmap` 用 `Bitmap.compress(JPEG, 90)` 写入 `context.cacheDir/camera/{prefix}_{ts}.jpg`，再 `FileProvider.getUriForFile(...)`（新增 `file_paths.xml` 仅暴露 `cache-path`）暴露成 `Uri`，`Uri.parse(null)` 路径彻底消除；③三个 `cameraLauncher` 全部走同一 helper，删除 3 处重复实现。回归：`tests/verify_bug_2026_08_09_003_takepicture_permission_and_path.py` T1-T7（FileProvider provider/authority 在 manifest、`file_paths.xml` 含 `cache-path`、3 个 launcher 全部经 helper、`cameraLauncher.launch(null)` 裸调用清零、`Uri.parse(insertImage(...))` 旧写法清零、`LaunchCameraWithPermission` helper 存在并用 `RequestPermission`、回调用 `saveBitmapToCacheAndGetUri`），旧代码反向验证 7 项断言全数生效 |
| BUG-2026-08-09-004 | "识别单据"页打开即报错 "网络错误: Failed to connect to /127.0.0.1:5000"：`AiScreens.kt::DocumentOcrScreen` 的 `LaunchedEffect(Unit) { viewModel.loadWarehouses() }` 在页面挂载瞬间即触发 `getWarehouses()` 请求，而仓库下拉仅在 OCR 识别完成展示物料行后才需要；尚未识别单据、未选仓库前提前发请求，未配置 baseUrl 时（默认 fallback `http://127.0.0.1:5000/`）立即 ConnectException，错误经 `LoadWarehouses onFailure` → `_uiState.error` → Snackbar 弹给用户，体感"页面一打开就报错"（截图：snackbar 文字"网络错误: Failed to connect to /127.0.0.1:5000"，页面状态为初始空态而非 OCR 流程中） | **已修复**。把 `loadWarehouses()` 调用从 `LaunchedEffect(Unit)` 整体后移到 `LaunchedEffect(uiState.ocrResult != null) { if (uiState.ocrResult != null) viewModel.loadWarehouses() }`，仅在 OCR 成功且 `ocrResult.items` 非空（即确认生成草稿的仓库下拉要展示）时才请求仓库列表，未识别阶段不再发请求也不会再展示误导性网络错误。`loadWarehouses()` 自身的 `if (warehouses.isNotEmpty() \|\| isLoading) return` 守卫保留，幂等性不变。同类屏幕核查：`ObjectRecognizeScreen`/`StocktakeRecognizeScreen` 仅在 `LaunchedEffect(uiState.error)` 监听错误，无 `loadWarehouses()` 调用，不受此 BUG 影响。回归：`tests/verify_bug_2026_08_09_004_loadwarehouses_after_ocr.py` T1-T4 4/4 PASSED（初始挂载 `LaunchedEffect(Unit)` 内不再裸调 `loadWarehouses`、新的 `LaunchedEffect(uiState.ocrResult != null)` 内条件调用、其他 AI 屏幕无裸 `loadWarehouses` 引入、旧写法反向验证 4 项断言全数生效）。commit `83eee4e0` |
| BUG-2026-08-09-005 | "识物盘点"页 `submitStocktake()` 缺仓库必填校验：`ScanViewModel.kt::submitStocktake` 读取 `state.scanLines` 直接提交，未检查 `state.selectedWarehouse`，用户未选仓库时仍可发起请求；后端会因缺 warehouse 拒绝但前端无任何拦截提示，体感"点了没反应" | `ScanViewModel.kt::submitStocktake` 入口先取 `state.selectedWarehouse`；若为 `null`，写 `_uiState.value.copy(error="请选择仓库")` 并 `return@launch`；同时 `StocktakeRequest` 数据类增加 `warehouse: String?` 与 `@SerializedName("warehouse_code") warehouseCode: String?` 两个字段，请求体里同步带上 `warehouse.code` 兜底服务端。`WmsRepository.submitStocktake` 透传新字段，`WmsApiService.submitStocktake` `@POST` `@Body StocktakeRequest` 适配新结构。回归：`tests/verify_p1_a_stocktake_warehouse_required.py` T1-T4 4/4 PASSED（前置 `selectedWarehouse == null` 守卫存在、错误消息"请选择仓库"、StocktakeRequest 含 warehouse/warehouseCode 字段、scanLines 空守卫"请先扫描盘点物料"）。commit `5307e791` |
| BUG-2026-08-09-006 | `DocumentOcrResult` 模型自我引用：`WmsApiService.kt::DocumentOcrResult` 把 `extracted` 字段类型直接定义成 `DocumentOcrResult?` 自身（`val extracted: DocumentOcrResult?`），Gson 解析任何带 `extracted` 字段的 OCR 响应会陷入无限递归 `new DocumentOcrResult(new DocumentOcrResult(...))`，栈溢出崩溃 `StackOverflowError`，整个 AI 识别单据 OCR 接口全挂 | 新建独立 `ExtractedDocument` 数据类承接 `extracted` 字段（含 `documentType/supplier/orderNo/purchaseOrderNo/date/items/remarks`）；`DocumentOcrResult.extracted` 改为 `ExtractedDocument?`；`ScanRequests.kt` 同步删除（合并到 `WmsApiService.kt`）；其余 `document_type/supplier/order_no/date/items/remarks/reply/matches/match_count` 顶层字段语义不变。回归：`tests/verify_p1_c_document_ocr_extracted_type.py` T1-T4 4/4 PASSED（DocumentOcrResult.extracted 类型为 ExtractedDocument 而非 self、新 ExtractedDocument 类独立存在、ExtractedDocument 字段映射正确、构建合法 DocumentOcrResult 不爆栈）。commit `6d49d096` |
| BUG-2026-08-09-007 | Android 拍照临时文件长期驻留 cacheDir：`WmsApplication` 仅在每次拍照时写 `cacheDir/camera/{prefix}_{ts}.jpg`，但没有任何清理机制；BUG-2026-08-09-003 修复后 FileProvider 改为写入 cacheDir，使用越久 cacheDir 越大，可触发 `IOException: No space left on device` 导致后续拍照失败，且旧的临时 JPEG 文件长期堆积存在隐私残留（图片未及时清理） | `WmsApplication.onCreate()` 在 `super.onCreate()` 后调用 `cleanupStaleCameraCache()`：遍历 `cacheDir/camera/` 目录所有 `isFile`，按 `lastModified() < now - 24h` 判定为陈旧文件，调用 `f.delete()` 删除并累计 `cleanedCount/cleanedBytes`；`Log.i` 输出清理摘要便于现场排查；整个清理流程包在 `try/catch` 内，删除失败/目录不存在/IO 异常均 `Log.w` 警告而不阻塞应用启动。回归：`tests/verify_p2_a_camera_cache_cleanup.py` T1-T5 5/5 PASSED（WmsApplication.onCreate 调 cleanupStaleCameraCache、cutoff = 24 小时、只删文件不递归子目录、累加 cleanedCount/cleanedBytes、try/catch 不抛异常）。commit `816aad25` |
| BUG-2026-08-09-008 | `ApiEnvelope.displayMessage()` 直接吐后端 `message/msg` 字段无任何过滤：①未限制长度，后端 `msg` 字段可塞入 5000+ 字符堆栈或大段 HTML 导致 OOM/UI 错位；②未做 HTML 标签过滤，存储型 XSS 风险——后端报错信息若被攻击者构造包含 `<script>alert(1)</script>`，前端 Snackbar/Dialog 用 `Text(...)` 渲染虽不会执行，但用 `Html.fromHtml` 渲染的对话框或部分文本组件会被注入；③多种 entity 编码（`&lt;`/`&gt;`/`&amp;`/`&quot;`）未做反转，错误信息里出现的 `<`/`>` 显示为字面量，用户可读性差 | `ApiEnvelope.kt::displayMessage` 改为调内部 helper `sanitizeMessage(raw)`：①`trim()` 后 `Regex("<[^>]+>")` 去除所有 HTML 标签（包括 `<script>`/`<img onerror=...>` 等）；②反转实体编码 `&lt;`→`<`、`&gt;`→`>`、`&quot;`→`"`、`&#39;`→`'`、`&apos;`→`'`、`&amp;`→`&`；③反转后再 `Regex("<[^>]+>")` 二次过滤防 `&lt;script&gt;` 嵌套绕过；④长度超过 `MAX_DISPLAY_MESSAGE_LENGTH=200` 则 `substring(0,200)+"..."` 截断；空消息兜底为"操作成功"/"操作失败"。companion object 暴露 `MAX_DISPLAY_MESSAGE_LENGTH` 常量供测试断言。回归：`tests/verify_p2_d_displaymessage_sanitize.py` T1-T6 6/6 PASSED（`<script>` 标签去除、`&lt;` 反转、长度 200+ 截断、companion 常量 200、空消息兜底、displayMessage 调 sanitizeMessage）。commit `19252289` |
| BUG-2026-08-09-009 | 登出后加密 SharedPreferences 异步清空导致 token 残留窗口：`WmsRepository.kt::logout()` 使用 `encryptedPrefs.edit().clear().apply()`，而 `apply()` 是异步写盘；点击登出到下一次 process kill 之间若立即有请求，token 仍可被读取；更严重的是 `apply()` 在低内存/快速退出场景下可能未落盘就被回收，下次启动 token 仍在加密 prefs 中，相当于"登出失败"；普通 `dataStore.edit { it.clear() }` 同步但与 EncryptedSharedPreferences 不同步，导致 token/普通偏好之间状态不一致 | `WmsRepository.kt::logout` 第一步先 `try/catch` 包 `encryptedPrefs.edit().clear().commit()`（同步阻塞写盘，保证 token 立即清除并落盘）；catch 分支 `Log.w("WmsRepo", "清空加密 prefs 失败: ${e.message}")` 记录失败原因但不再向上抛（避免登出流程半途中断）；再走 `context.dataStore.edit { it.clear() }` 清空普通偏好；最后 `RetrofitClient.setToken(null)` 通知 OkHttp 拦截器移除内存中的 token。三步顺序固定：加密 prefs → DataStore → 内存 token。回归：`tests/verify_p2_e_logout_sync_commit.py` T1-T5 5/5 PASSED（encryptedPrefs.edit().clear() 用 commit 同步、`dataStore.edit` 清空、RetrofitClient.setToken(null) 通知、commit 在 try/catch 内防抛、顺序正确）。commit `8ca4eaf1` |
| BUG-2026-08-09-010 | 手机 WMS 语音"正在聆听"仍卡死到兜底超时：BUG-2026-08-09-003 的 8s 兜底 + sherpa 离线引擎在默认构建（`-Pwms.sherpa` 未开，模型未打包）下不可用，而国内设备无 Google 服务时 `AndroidVoiceSttEngine`（`SpeechRecognizer`）既不回调 `onPartial` 也不回调 `onError`，用户点麦克风后只能干等 8s"识别超时"，语音指令名存实亡 | 选择"接云服务器 + 走后端中转"方案：后端新增 `/${mobile/api/asr}` 路由（`app/routes/mobile.py`，读 `TENCENTCLOUD_SECRET_ID/TENCENTCLOUD_SECRET_KEY/TENCENTCLOUD_REGION` 环境变量，`app/tencent_asr.py` 手工 TC3-HMAC-SHA256 签名调腾讯云一句话识别 16k_zh，音频格式/大小校验，错误归类 400/500/502）；Android 端新增 `CloudAsrVoiceSttEngine`（AudioRecord 16kHz mono PCM16 录音 → WAV 封装 → `WmsApiService.asrAudio` multipart 上传 → 解析 `{status,text}` → 回调 `onResult`），`WmsApiService` 增 `asrAudio` + `AsrResult` 模型（扁平结构非 ApiEnvelope），`VoiceSttEngineRegistry.defaultSelector` 改为"云引擎优先 → sherpa → Android 系统识别"。关键点：`CloudAsrVoiceSttEngine.destroy()` **不取消上传协程、不清空 listener**，因 ViewModel 在 `stop()` 后立即 `destroy()`，异步上传结果需在 destroy 后仍能回调给共享 `engineListener`。回归：`tests/verify_tencent_asr_helper.py`（8 用例）与 `tests/verify_mobile_asr_route.py`（8 用例）全绿。Android 端 `assembleDebug` 需在有 Android SDK + JDK17 的构建机执行（本沙箱无 SDK 且 JDK25 与 AGP8.x 不兼容，无法本地编译验证）。commit 见本次提交 |
| BUG-2026-08-10-001 | 移动端"识别单据"页调用 `POST /api/ai/document_ocr` 报 `请求已过期或缺少安全令牌，请刷新页面后重试`（CSRF 400）：该 AI 端点未纳入移动端 CSRF 豁免名单，与 `/api/ai/*` 其他端点不一致；移动端原生 APK 用 Bearer Token 鉴权但仍要走 Flask-WTF CSRF 校验（POST 表单必带 token），导致"识别单据"功能在未带 token 时必 400 | `app/app.py:CSRFProtect.exempt(...)` 新增 `/api/ai/document_ocr` 加入移动端 CSRF 豁免名单；与 `api_chat_stream`/`api_chat_message` 等其他 `/api/ai/*` 移动端端点策略一致。回归：`tests/verify_ai_chat_stream_endpoint.py` CSRF 豁免名单断言补充 `api_document_ocr` |
| BUG-2026-08-10-002 | 移动端"物料档案"页 `mobile_material_archive_images` / `mobile_material_archive_upload` 端点 `OperationalError: no such table: material_image` 500：material_image 表在 `WMS_SKIP_STARTUP_DB_UPGRADE=1` 或 `WMS_NO_DB_TOUCH=1` 启动场景下不会被 `db.create_all()` 自动创建，提交时也无 `_ensure_material_image_table()` 兜底；唯一有 try/except 兜底的 `_archive_material_payload` 不会被这两个专用端点复用 | `app/routes/mobile.py` 抽出 3 个 helper：`_has_material_image_table()`（`sqlalchemy.inspect` 探测）、`_safe_material_image_count(id)` / `_safe_material_image_list(id)`（缺表回退 0/[]，与 `_archive_material_payload` 原行为一致）、`_ensure_material_image_table_inline()`（缺表时用 `MaterialImage.__table__.create(db.engine, checkfirst=True)` 跨方言补建，失败 `current_app.logger.warning`）。`mobile_material_archive_images` 改调 `_safe_material_image_list(id)`；`mobile_material_archive_upload` 先调 `_has_material_image_table()` 缺表则 `_ensure_material_image_table_inline()`，仍失败返 500 `"物料档案表未就绪，请稍后重试或联系管理员"`，成功路径用 `_safe_material_image_count(id)` 拿当前数量。回归：`tests/verify_mobile_material_archive_api.py` 12/12 PASSED（含 `test_search_when_material_image_table_missing` 表缺失搜索 200/0、T3-T6 upload/list/delete 正常、T1 endpoint 注册、T7-T8 401/Bearer 鉴权、T9-T10 主图同步/回退） |
| BUG-2026-08-11-001 | 手机 app 登录 `/api/login` 报 502：`start_wms_auto.bat` / `start_wms_offline.bat` 均设 `WMS_NO_DB_TOUCH=1`，导致 `auto_migrate_database()` 和 `db.create_all()` 全被跳过，老数据库可能缺 user 表登录安全列（`login_lock_ip`/`login_ip_failed_count`/`login_ip_locked_until`/`must_change_password`/`last_login_at`/`last_login_ip` 等）、`api_token` 表或 `login_log` 表，`/api/login` 内无兜底 try/except，任何 `OperationalError` 直接 500 → nginx 502 | `app/routes/native_api.py` 新增 `_ensure_login_schema()` 函数：在 `/api/login` 路由开头检查并补建缺失的 `api_token`/`login_log` 表及 user 表登录安全列、`api_token.last_used_at` 列、`login_log.fail_reason` 列；整个 login 函数体包 try/except 兜底，异常时 `db.session.rollback()` + 记日志 + 返回 JSON 500 而非裸 500。回归：`tests/verify_app_py_split_native_api.py` 6/6 PASSED（含新增 `test_login_when_tables_missing`：DROP api_token+login_log 表后登录仍 200 返回 token） |
| BUG-2026-08-11-002 | 手机 app 拍照识物崩溃 `kotlin.Unit cannot be cast to com.factory.wms.data.api.RecognizeMaterialResult`：后端 `/mobile/api/recognize_material` 把 `reply/extracted/matches/match_count` 平铺在响应顶层，而 Android 端 `ApiEnvelope<RecognizeMaterialResult>` 期望这些字段包在 `data` 内；Gson 反序列化时 `data=null`，`handleResponse` 的 `Unit as T` 兜底把 `Unit` 强转成 `RecognizeMaterialResult` 抛 ClassCastException | `app/routes/mobile.py` 识物接口响应改为标准信封：`data: {reply, extracted, matches, match_count}`，与 `WmsApiService.recognizeMaterial` 的 `ApiEnvelope<RecognizeMaterialResult>` 契约对齐。commit `b5183f67`（2026-08-11 补登台账） |
| BUG-2026-08-11-003 | RetrofitClient 两项隐患（2026-08-11 APK 全量审计发现）：①`buildRetrofit()` 在 baseUrl 为空时静默 fallback 到硬编码域名 `https://gd2026.top/`，baseUrl 未配置时 API 请求（含 Authorization token）会被发往该默认服务器；②object 单例的 `baseUrl`/`authToken`/`retrofit` 可变字段无并发保护，`setBaseUrl` 重建 retrofit 期间其他线程可能读到半更新中间态 | ①`buildRetrofit(url)` 空 url 改用不可达占位 `http://127.0.0.1:9/`（discard 端口），`apiService` getter 在 baseUrl 为空时 `check()` 抛错快速失败，占位地址永远收不到请求更不携带 token；②三个可变字段全部 `@Volatile`，`setBaseUrl` 在 `synchronized(lock)` 内先构建新 retrofit 再发布引用（先构建后发布）。回归：`tests/verify_bug_2026_08_11_003_retrofit_client_hardening.py` T1-T6 6/6 PASSED（无 gd2026.top 残留、空 url 占位、getter 空值抛错、@Volatile 覆盖、synchronized、先构建后发布） |
| BUG-2026-08-11-004 | `WmsRepository.submitOpeningStock` 成功分支 `val msg = submitResult?.let { "期初库存已保存" } ?: "期初库存已保存"` 两分支返回相同字符串，`?.let` 永走非空分支属无意义冗余，且对非空类型做可空解包有误导性（2026-08-11 APK 全量审计发现） | 删除冗余 `msg` 变量，成功分支直接 `Result.success("期初库存已保存")`；`opening_stock` 操作日志写入逻辑不变。回归：`tests/verify_bug_2026_08_11_004_opening_stock_msg_cleanup.py` T1-T3 3/3 PASSED（无冗余解包、固定提示、日志保留） |
| BUG-2026-08-11-006 | 手机 app 拍照识物崩溃 `kotlin.Unit cannot be cast to com.factory.wms.data.api.RecognizeMaterialResult`（用户截图报错）：`WmsRepository.handleResponse<T>` 在 envelope success 但 `data==null` 时**无条件**执行 `Result.success(Unit as T)`；reified 泛型下 `Unit as T` 是真实 checkcast，T=RecognizeMaterialResult 时抛 ClassCastException，被外层 catch 包装成 "网络错误: kotlin.Unit cannot be cast..."。触发条件为服务端 success 响应缺 data（旧版本服务端未含 BUG-2026-08-11-002 修复、代理丢 body 等）。与 BUG-2026-08-11-002（服务端响应补 data）是同一症状的两端：002 修服务端，本条修 App 端兜底 | `handleResponse` 的 `Unit as T` 兜底加 `T::class == Unit::class` 守卫：仅删除类接口（`ApiEnvelope<Unit>`）允许 success 无 data 回填 Unit；其他类型 data 缺失时返回 `Result.failure(Exception(envelope.displayMessage()))`，UI 展示服务端 msg 而非 ClassCastException 文本。回归：`tests/verify_bug_2026_08_11_006_handle_response_unit_cast.py` T1-T4 4/4 PASSED（Unit 守卫存在且在 cast 之前、非 Unit 兜底返回 failure 不含 success、failure 透出 displayMessage、后端 recognize_material success 响应含 data 四字段防服务端回归） |
| BUG-2026-08-11-007 | 前端界面大面积英文文案，中文用户看不懂：①AI Agent 任务页把 `warehouse_patrol`/`pending`/`completed` 等英文枚举码和 Agent 硬编码英文整句（"Warehouse daily patrol: ..."、"Negative stock: 2; low stock: 5." 等目标/步骤/数据范围/结果摘要/操作标签）直接渲染；②AI 文档任务页 `ocr_upload`/`pending_confirmation`/`matched`/`helpful`/`ocr_error` 等枚举码裸露；③AI 运维看板灰度范围 `allowlist`、运行状态、工具名英文；④AI 验收页结论 `go/no_go`、灰度角色英文；⑤AI 业务质量页"Schema版本"；⑥10+ 单据详情页（入库/出库/销售/委外/采购申请/售后/审批/操作审计/首页）状态枚举 `{% else %}` 兜底分支直接输出英文原值；⑦打印页 `RECEIPT`/`MATERIAL REQUISITION`、报表中心 `Report Center`、登录页 `WMS Cloud`、物料页 `ALL` 等英文标题残留；⑧base.html 面包屑 `aria-label="breadcrumb"` 等无障碍标签英文 | 双管齐下：新数据源头中文化 + 历史英文数据渲染层翻译。①`app/app.py` 三个 Agent 函数（warehouse_patrol/purchase_followup/sales_followup）改为直接以中文写入任务目标、步骤名、数据范围、结果摘要、操作标签；②`app/app.py` 新增 `ai_agent_text`（固定整句映射 + 含数字正则句型 + 片段替换）、`ai_agent_label`、`ai_doc_label` 三个 Jinja 过滤器及配套映射表（agent_type/task_status/step_status/risk/tool、source/doc_type/job_status/match_status/rating/error_type/run_status/rollout_mode）；③12 个 AI 模板（ai_agent_tasks/ai_agent_task_detail/ai_document_jobs/ai_document_job_detail/ai_ops_dashboard/ai_acceptance/ai_business_quality/ai_prelaunch 等）应用过滤器；④10+ 详情页状态兜底统一显示"未知"；⑤打印/报表/登录/物料页英文标题改中文；⑥base.html aria-label 中文化；⑦`_disabled_unused_20260506/bom_detail.html` aria-label="Close"→"关闭"。回归：`tests/verify_bug_2026_08_11_007_ui_chinese.py` T1-T10 10/10 PASSED（过滤器注册、Agent 中文写入、12 模板过滤器应用、验收页通过/不通过、结构版本、状态兜底"未知"、运行时过滤器行为、全模板英文残留静态扫描 0 残留） |
| BUG-2026-08-11-008 | 微信分享直推模式必然 403：`_wechat_share_send_image` POST 到助手 `/send` 时未携带 `X-Wechat-Helper-Token` 请求头，而助手端 `_check_auth` 强制校验该 token（token 未配置也一律拒绝），导致默认配置（helper_url=http://127.0.0.1:8765/send）下每次分享都被助手 403 拒绝、状态记为 failed；同时 helper_url 可配置成任意外网地址，一旦携带 token 会明文泄露给第三方主机 | `app/app.py` 新增 `_wechat_share_helper_url_allowed()`（仅允许 http/https + 127.0.0.1/localhost/::1 回环地址）；`_wechat_share_send_image` 直推时携带 `X-Wechat-Helper-Token: app.config['WECHAT_HELPER_TOKEN']`，token 未配置时拒绝直推并提示，非回环地址直接拒绝不发请求；`app/routes/wechat_share.py` 保存配置路由同步校验 helper_url 回环，非法地址尽早报错。回归：`tests/verify_bug_2026_08_11_008_wechat_push_token.py` 6/6 PASSED（T1 直推携带 token、T2 非回环拒绝且零请求、T3 token 缺失拒绝且零请求、T4 保存路由校验、回环/非法地址单测） |
| BUG-2026-08-11-009 | 微信发送助手并发不安全且无发送环境校验：①ThreadingHTTPServer 每请求一线程，但剪贴板/键盘输入/前台焦点是全局资源，并发 /send 互相抢焦点、串剪贴板，会发错图发错人；②锁屏/远程桌面断开/弹窗抢焦点时 SendInput 把 Ctrl+V/回车打进别的窗口；③失败只返回自由文本 msg，WMS 侧只能靠关键词匹配判定状态，脆弱；④先写剪贴板后校验接收人，无谓副作用 | `app/wechat_helper.py`：新增全局 `SEND_LOCK`，`send_image_task` 全程持锁串行化；新增 `_SendError` 机器可读错误码体系（no_receiver/clipboard_failed/wechat_window_not_found/open_contact_failed/paste_failed/send_key_failed/focus_lost），`do_POST` 与 poll 回报均携带 `code` 字段；新增 `_ensure_foreground()` 在激活后/打开会话后/回车发送前三处校验微信主窗口仍在前台，失焦立即中止；接收人校验提前到写剪贴板之前，失败零 UI 副作用。回归：`tests/verify_bug_2026_08_11_009_helper_robustness.py` 5/5 PASSED（T1 持锁、T2 三处前台校验、T3 错误码体系、T4 校验顺序、T5 调用方三元组解包；Windows-only 模块按仓库惯例静态验证）；另补 `tests/test_wechat_helper_send_image_task.py` 6/6 PASSED（A9 命名合规单测：ready/sent/error 三路径、接收人回退链、SEND_LOCK 串行化），commit `cf640084` |
| BUG-2026-08-11-010 | WMS 侧微信分享结果判定靠关键词匹配，脆弱且无重试策略：`_wechat_share_send_image` 只返回 (sent, message)，丢弃助手端机器可读 code，`_wechat_share_status_from_send_result` 用"失败/错误/http"等关键词猜测 failed/pending——新错误文案漏配关键词即误判为 pending；且连接级错误（助手重启窗口期 ConnectionError）一次失败即记 failed 无任何重试，而读超时若盲目重试又会因请求已被助手执行而重复粘贴/发送 | `app/app.py`：`_wechat_share_send_image` 改返回结构化三元组 `(status, code, message)`，status ∈ sent/pending/failed 直接落库，code 涵盖 ok/helper_not_configured/invalid_helper_url/token_not_configured/helper_offline/helper_timeout/auth_failed/send_error/http_<n> 及助手端透传码；仅 ConnectionError（请求未到达助手、零副作用）自动重试 1 次，Timeout 明确不重试防重复发送；HTTP 403 映射 auth_failed；删除关键词匹配函数 `_wechat_share_status_from_send_result`，`_wechat_share_order` 与 `routes/wechat_share.py` resend 路由同步改造，failed 时 message 附错误码便于排查。回归：`tests/verify_bug_2026_08_11_010_wms_result_mapping.py` 8/8 PASSED（code 透传、ready→pending、连接重试恰好 2 次、超时不重试仅 1 次、403→auth_failed、持续离线 helper_offline、未配置零请求、落库状态+错误码）；008/拆分回归同步全绿 |
| BUG-2026-08-11-011 | 微信分享定时任务可重复发送：`run_due_wechat_share_jobs` 调 `run_wechat_share_for_today(force=True)` 绕过"今日已有 pending/sent 记录即跳过"的去重检查，scheduler 每分钟触发一次，同一分钟内 misfire 补跑或服务重启会对同一入库单重复生成图片并重复直推微信（接收人收到多张相同分享图）；且每次触发无条件写 in_order_daily marker，重复触发产生多条 marker | `app/app.py`：定时任务改用 `force=False` 尊重去重语义；执行前检查今日 scheduled marker，已存在则该 config 整日跳过，每日最多执行一次、marker 恰好一条。回归：`tests/verify_bug_2026_08_11_011_scheduled_dedup.py` 4/4 PASSED（已有 pending 跳过且零文件生成、已有 marker 整体跳过、同分钟双触发仅 1 条 marker、源码 force=False 静态断言） |
| BUG-2026-08-11-012 | 分享页每次打开都同步 GET 助手 `/health`（timeout=1.5s），助手离线时每次页面加载被卡住约 1.5s，连续刷新/多标签打开体验差 | `app/app.py` 新增模块级 `_WECHAT_SHARE_HEALTH_CACHE`：按 health_url 缓存健康检查结果 30s，30s 内重复调用直接命中缓存不再发起 HTTP 请求；地址未配置时同步清空缓存避免换配置后命中旧结果。回归：`tests/verify_bug_2026_08_11_012_helper_health_cache.py` 4/4 PASSED（30s 内仅 1 次请求、超 30s 过期重查、换地址不命中、未配置零请求且清缓存） |
| BUG-2026-08-11-013 | 微信分享图片每天生成且永久累积在 `output/wechat_share/`，无任何清理机制导致目录无限膨胀；超期图片对应日志仍保留 image_path，列表页出现必然 404 的下载链接 | `app/app.py` 新增 `_wechat_share_cleanup_old_images(retention_days=30)`：仅删除符合分享命名规则（`YYYYMMDD_HHMMSS_*.png`）且 mtime 超期的文件，其它文件不动，并同步清空对应 `WechatShareLog.image_path/image_size`；新增 `_wechat_share_cleanup_old_images_daily()` 每日守卫，挂入 `run_due_wechat_share_jobs`（每分钟 scheduler）每日执行一次。回归：`tests/verify_bug_2026_08_11_013_image_retention.py` 5/5 PASSED（超期删除+日志解绑、30 天内保留、非分享命名文件不动、每日守卫同日仅一次、scheduler 挂接静态断言） |
| BUG-2026-08-11-014 | 重发微信分享用当前配置接收人而非分享时冻结接收人：列表"接收人"列展示的是 `log.receiver_*`（分享时冻结），`resend_wechat_share_log` 却用 `config.receiver_*` 直推，配置中途修改后点重发会悄悄发给另一个人，与页面展示不一致 | `app/routes/wechat_share.py` resend 路由：日志存在冻结接收人时构造 `SimpleNamespace` 发送快照覆盖 `receiver_name/receiver_wechat_id/receiver_search_key`（search key 取冻结值），message 标注"按历史接收人重发"；无冻结接收人时回退当前配置。回归：`tests/verify_bug_2026_08_11_014_resend_frozen_receiver.py` 3/3 PASSED（冻结接收人优先、无冻结回退配置、message 标注提示） |
| BUG-2026-08-11-015 | 微信分享页缺少三处界面引导：①用户不清楚「直推」（WMS 生成图片后立即 POST 助手）与「轮询」（助手 `WMS_WECHAT_HELPER_POLL=1` 定时拉取）两种发送模式的区别，助手地址字段无任何说明；②`auto_send`（自动点击发送）勾选框无风险提示，开启后助手粘贴图片直接点微信「发送」，全程无人工确认、消息发出无法撤回，用户不了解风险即开启；③「待发送」记录（图片已生成未进微信）无消化路径引导，助手未开轮询时 pending 一直堆积用户不知如何处理 | `app/templates/wechat_share.html`（纯模板）：①助手地址字段下 form-text 说明直推/轮询两模式及回环地址限制；②新增 `#autoSendRiskHint` 警示条（无人工确认/无法撤回/确认窗口停留目标聊天），auto_send 未勾选默认隐藏、勾选时 JS change 监听实时显示；③`log_counts.pending > 0` 时状态卡片显示消化引导（轮询自动拉取或列表逐条重发），并按助手在线/轮询状态追加「不会被自动消费」或「请先启动助手」提示。回归：`tests/verify_bug_2026_08_11_015_share_page_guidance.py` 4/4 PASSED（模式文案存在、警示条显隐随配置切换、pending 引导按需显示、在线未轮询/离线两种提示分支） |
| BUG-2026-08-11-016 | 物料管理图片无法打开：默认上传目录为工作目录下的 `uploads/material_images`，数据库保存的相对路径却由 Flask `/static` 路由解析到 `app/static/uploads/material_images`，导致新上传图片和已有 39 张历史图片均返回 404 | `app/config.py` 将默认 `UPLOAD_FOLDER` 指向 `app/static/uploads`；`app/utils.py` 启动时幂等复制旧 `uploads/material_images` 中目标不存在的文件，不覆盖静态目录已有文件；回归：`tests/test_material_image_static_path.py` 验证新上传和历史图片均通过静态路由返回 200，相关测试 14 passed，A1-A10 0 违规 |
| BUG-2026-08-11-017 | 手机 APP 物料档案图片无法打开只显示灰块：Android 端 Coil 图片下载未复用 API 的 OkHttp Bearer 认证客户端（反向代理保护静态资源时图片请求缺少认证），且 `AsyncImage` 无错误状态，401/404/网络错误均静默呈现灰色块无法诊断；首版修复提交还漏声明 `imageUrl`/`painter` 变量导致编译错误，且误用 Coil 3.x 专属的 `io.coil-kt:coil-network-okhttp` 构件（Coil 2.7.0 下不存在）导致 CI `checkDebugAarMetadata` 依赖解析失败 | `RetrofitClient` 导出 `sharedOkHttpClient()`；`WmsApplication` 实现 `ImageLoaderFactory`，用 Coil 2.x 内置的 `ImageLoader.Builder.callFactory { sharedOkHttpClient() }` 注入统一客户端（不新增依赖）；`ArchiveImageCard` 声明 `imageUrl`/`painter`，空 URL 与 Coil Error 状态显示"图片加载失败"而非灰块。回归：`tests/verify_bug_2026_08_11_017_mobile_material_image_loader.py` T1-T5 5/5 PASSED（共享客户端、callFactory 注入、无 Coil3 专属 API/构件、可见错误状态、空 URL 分支），stash 反向验证重新 RED |
| BUG-2026-08-11-018 | 物料管理列表"图片"列缩略图无点击事件，点击图片无反应，无法查看大图 | `app/templates/material.html` 缩略图绑定 `onclick="previewMaterialImage(this.src)"` 并显示 zoom-in 光标；新增 `imagePreviewModal` 大图预览弹窗（modal-lg、最大高度 75vh）与 `previewMaterialImage()` 函数（赋值 src 后 `bootstrap.Modal` 显示）。回归：`tests/verify_bug_2026_08_11_018_material_image_preview.py` T1-T3 3/3 PASSED，stash 反向验证重新 RED |
| BUG-2026-08-11-019 | 手机 APP 物料档案图片点击无法放大查看：Android 档案图片列表缩略图无点击事件，用户无法查看大图 | `MaterialArchiveScreens.kt`：`ArchiveImageCard` 新增 `onPreview` 回调，缩略图区域 `.clickable` 触发；`MaterialArchiveDetailScreen` 持有 `previewImageUrl` 状态，非空时弹出全屏 `Dialog`，黑底 + `AsyncImage`（`ContentScale.Fit`）完整展示大图，点击任意处或返回键关闭；预览走同一 Coil ImageLoader（含 Bearer 认证）。回归：`tests/verify_bug_2026_08_11_019_mobile_image_preview.py` T1-T3 3/3 PASSED，stash 反向验证重新 RED |
| BUG-2026-08-11-020 | 手机 APP 档案图片全屏预览不支持手势缩放，铭牌小字无法局部放大查看 | `MaterialArchiveScreens.kt` 预览 Dialog 的 `AsyncImage` 增加 `pointerInput` + `detectTransformGestures`：`scale` 双指捏合缩放（1x-5x，缩回 1x 自动复位偏移）、`offset` 拖动平移，`graphicsLayer` 应用变换；单击关闭、返回键关闭行为不变。回归：`tests/verify_bug_2026_08_11_019_mobile_image_preview.py` T4 手势契约 PASSED（T1-T4 4/4），stash 反向验证 T4 重新 RED |
| BUG-2026-08-11-021 | 扫码盘点没有盘点仓库：Android 扫码盘点页无仓库选择 UI，`/api/stocktake` 后端完全忽略 payload 中的 warehouse 字段，`InventoryCheckScan` 模型无 warehouse 列，盘点单及其自动生成的库存调整草稿都没有仓库，违反 AGENTS.md 仓库必填规则 | 后端：`app.py` `InventoryCheckScan` 增加 `warehouse` 列 + `auto_migrate_database` 补列并对存量数据回填默认仓库名；`routes/native_api.py` `/api/stocktake` 校验仓库必填，未传时自动带入默认仓库，无默认仓库返回 400「请选择盘点仓库」。Android：`StocktakeScreen` 增加盘点仓库选择卡片（`WarehouseSelectorCard` + `WarehousePickerDialog`，进入页面自动加载仓库列表并预选第一项），提交确认弹窗显示所选仓库且未选仓库时禁用确认按钮；`ScanViewModel.submitStocktake()` 已有未选仓库拦截。回归：`tests/verify_bug_2026_08_11_021_stocktake_warehouse_required.py` T1-T4 4/4 PASSED（无默认仓拒绝、默认仓自动带入并写入调整草稿、显式仓库落库、表结构含 warehouse 列） |
| BUG-2026-08-12-001 | AI 知识库与工具描述仍宣称"采购入库必须关联采购订单"，与 AGENTS.md"采购订单仅作为可选来源"规则冲突（提示词/知识层；后端强制校验已由 `purchase_in_order_requires_order()` 开关守卫并默认停用，见 BUG-2026-08-02-019） | `app/ai/knowledge.py` `purchase_receive_sop.rule` 改为"采购订单是采购入库的可选来源，采购入库允许手工新增、编辑、保存和完成；有关联采购订单时必须保留来源、数量与执行进度跟踪。送货单识别只能生成草稿或确认页，不能直接完成入库。"；`app/ai/tools/registry.py` `purchase_receive_draft` 工具描述改为"A purchase order is only an optional source; when linked, keep source, quantity and progress tracking."。回归：`tests/test_bug_2026_08_12_001_purchase_optional_prompt.py` 3/3 PASSED（stash 反向验证旧代码 3 failed） |
| BUG-2026-08-12-002 | v2 调试接口 `/api/ai/v2/llm/chat` 与 `/api/ai/v2/llm/intent` 用 `payload.get('system_prompt', 默认值)` 直接取客户端提示词，任何登录用户（含 user/production 低权限角色）都可覆盖 system_prompt，绕过 WMS 角色与业务边界约束 | `app/ai/v2_routes.py` 新增 `_resolve_debug_system_prompt(payload, default)`：非 admin 一律忽略 payload 中的 system_prompt 回落服务端默认；admin 允许自定义（调试用）但截断到 2000 字符并 `logger.warning` 审计（user id、prompt 长度）；两条调试路由均改用该 helper。回归：`tests/test_bug_2026_08_12_002_v2_system_prompt_guard.py` 6/6 PASSED（非 admin 忽略、production 忽略、admin 生效+审计日志、2000 截断、空值回落、路由必须走 helper），stash 反向验证旧代码 6 failed |
| BUG-2026-08-12-003 | 统一系统提示词（`app/ai/prompts.py` legacy-v1）仅一句英文泛化描述，未包含任何 WMS 业务红线；`app/app.py` 两条聊天提示词链路（`_ai_call_llm_chat` 与流式聊天路由）硬编码且缺少"送货通知→入库草稿（禁止采购申请）""仓库必填""密码不可重置"等红线 | `app/ai/prompts.py` legacy-v1 在保留 `Never auto-submit` 英文安全边界的前提下追加 6 条中文核心业务红线（只建草稿/送货通知禁转采购申请/采购订单可选来源/仓库库位必填/数据实时查询不得编造/禁改密码）；`app/app.py` 两条聊天链路均以 `get_prompt_spec().system_prompt` 为基础约束再拼接页面上下文。回归：`tests/test_bug_2026_08_12_003_system_prompt_redlines.py` 4/4 PASSED（英文边界保留、8 个红线关键词、两条链路均接入），stash 反向验证旧代码 3 failed；`verify_ai_platform_foundations.py` PASS |
| BUG-2026-08-12-004 | 移动端 8 个读取接口（dashboard/stock query/alert list/in_order list+detail/out_order list+detail/opening_stock GET）无仓库解析与过滤：dashboard 跨仓统计今日进出/待办、stock query 与 alert list 直接读全局 `Material.stock`、单据列表/详情不按仓库隔离、期初库存未传参时返回跨仓记录，违反 AGENTS.md 仓库必填规则（P1，WMS FULL 审计 AUDIT-2026-08-12-001） | `app/app.py` 新增 `resolve_request_warehouse(params)`（warehouse_id/code/name 显式解析 + 存在且 active 校验 + 默认仓库回退，无默认仓库返回「请选择仓库」）与 `get_warehouse_stock_quantities(warehouse)`（库位管理开启时按 LocationInventory 仓库名汇总，关闭时按 StockTransaction.location 流水净额汇总，绝不回退全局 Material.stock）；`app/routes/native_api.py` 8 个读取接口统一接入：解析失败 400、列表/dashboard/期初按仓库过滤、单据详情跨仓 404、库存/告警用仓库级数量。回归：`tests/test_bug_2026_08_12_004_mobile_warehouse_scope.py` 14/14 PASSED（stash 反向验证旧代码 14 failed）；`verify_app_py_split_native_api.py`（`_seed_base` 补默认仓库契约）与 `verify_mobile_opening_stock_api.py` 全绿；`verify_opening_stock_multi_warehouse.py` / `verify_in_order_state_machine.py` / `verify_out_order_state_machine.py` PASS；lint A1-A10 与裸 fetch 扫描 0 违规 |
| BUG-2026-08-12-005 | 旧版报表导出 `/report/inout/print`、`/report/inout/export`、`/report/stock/print` 绕过仓库必填规则：inout 仅按日期导出全部仓库 InOrder/OutOrder，stock print 直接导出全局 `Material.stock` 跨仓总数，`stock_query.html` 暴露无仓库参数的 `/report/stock/print` 直连入口（P1，WMS FULL 审计 AUDIT-2026-08-12-002） | `app/routes/report.py` 三个旧导出统一接入 `resolve_request_warehouse(request.args)`（显式 warehouse_id/code/name 校验 + 默认仓库回退，无默认 400「请选择仓库」）：inout print/export 按 `InOrder.warehouse`/`OutOrder.warehouse` 精确过滤（双 sheet 结构、文件名、Content-Type、日期筛选保持不变）；stock print 改用 `get_warehouse_stock_quantities(warehouse)` 输出仓库级库存并按仓库数量判定预警状态，不再读取全局 `Material.stock`；`stock_query.html` 打印链接携带当前选中 `warehouse_id`。回归：`tests/test_bug_2026_08_12_005_report_export_warehouse_scope.py` 9/9 PASSED（A/B 仓显式过滤、默认仓回退、无默认 400、export 与 print 一致、仓库级库存 5/95 而非全局 100、模板链接带参），stash 反向验证旧代码 9 failed；`verify_bug_2026_08_02_018_report.py` 9/9 与 BUG-004 测试 14/14 全绿；lint A1-A10 与裸 fetch 扫描 0 违规；`verify_wms_bugs.py` 仅剩 AI-AGENTS-001/AI-STAGE5-OPS-001 两项既有脚本漂移（FIX-3/FIX-4 处理） |
| BUG-2026-08-12-006 | `scripts/verify_ai_agents.py` 断言详情页渲染 HTML 含旧英文内部文案（`'Stock risk scan'`、`'Low-stock replenishment scan'`、裸 `'draft'`），但生产代码已直接将中文步骤名写入 `AIAgentStep.name`，历史英文数据经 `ai_agent_text`/`ai_agent_label` 过滤器在渲染层中文化，验证脚本契约漂移导致误 FAIL（P2，WMS FULL 审计 AUDIT-2026-08-12-003） | 仅修改验证脚本（UI 中文化与过滤器保持不变）：数据库层直接断言结构化内部语义——warehouse/purchase task completed、每类 task ≥4 步且 step_no 自 1 连续、全部步骤 status='completed'、warehouse 步骤含 `tool_name='warehouse_insights'`、purchase 有且仅有一条 `risk_level='draft'` 且其 `tool_name='purchase_request_draft'`；渲染层断言数据库中文步骤名逐条出现在详情页 HTML、旧英文 `'Stock risk scan'`/`'Low-stock replenishment scan'` 不出现、`risk_level='draft'` 经 `ai_agent_label('risk')` 渲染为中文「草稿」单元格；并直接断言过滤器输出 `ai_agent_text('Stock risk scan')=='库存风险扫描'` 等中文化契约。回归：`verify_ai_agents.py` PASS（修改前 AssertionError RED 确认）；`verify_ai_all.py --level core` 56/58（仅剩 FIX-4 `verify_ai_stage5_ops.py` 与 FIX-5 `verify_ai_business_quality_dashboard.py` 两项既有失败）；`verify_wms_bugs.py` AI-AGENTS-001 转 PASS，仅剩 AI-STAGE5-OPS-001（FIX-4 处理） |
| BUG-2026-08-12-007 | `scripts/verify_ai_stage5_ops.py` 断言 `/ai/ops` 运维看板渲染 HTML 含内部 capability key `'warehouse_insights'`，但模板 `ai_ops_dashboard.html` 已正确使用 `name|ai_agent_label('tool')` 将工具名中文化为「仓库洞察」，内部 key 不应作为 UI 文案出现，验证脚本契约漂移导致误 FAIL（P2，WMS FULL 审计 AUDIT-2026-08-12-004） | 仅修改验证脚本（模板中文化与内部 key 存储保持不变）：数据库层断言 `AIToolCall.tool_name=='warehouse_insights'` 且 `capability=='warehouse_insights'`（内部 key 持久化不变）；模板源码层断言 `ai_ops_dashboard.html` 含 `ai_agent_label('tool')` 过滤器调用；渲染层断言页面含中文标签「仓库洞察」且不含内部 key `warehouse_insights`。保留既有验证范围：admin 可访问、warehouse 角色被拒（302/403）、降级开关 `_ai_llm_enabled`、drafts/agents 开关对 capability 的影响、rollout admin_only 限制、全局关闭后聊天返回「管理员关闭」提示。回归：`verify_ai_stage5_ops.py` PASS（修改前第 86 行 AssertionError RED 确认）；`verify_ai_all.py --level core` 57/58（仅剩 FIX-5 `verify_ai_business_quality_dashboard.py`）；`verify_wms_bugs.py` 全绿 |
| BUG-2026-08-12-008 | `scripts/verify_ai_business_quality_dashboard.py` 测试6 断言模板源码含 `'schema_version': 'Schema版本'`，但模板 `ai_business_quality.html` 已正确提供 `schema_version` 维度且用户可见标签使用中文化「结构版本」，功能存在、浏览器 E2E 通过，失败源于测试错误绑定历史显示文案（P2，WMS FULL 审计 AUDIT-2026-08-12-005） | 仅修改验证脚本（模板与中文标签保持不变）：保留 role/source/model 既有断言；`schema_version` 改为契约式断言——正则提取 `'schema_version': '<label>'` 映射必须存在，label 非空、不等于原始 key、必须含中文且不得混入英文字母（当前「结构版本」及未来等价中文标签均可通过）；新增 `renderDimensionTable` 函数体实际使用 `dimLabels[dim]` 映射渲染维度列的断言；保留维度表 ID、by_dimension 数据引用及测试7版本对比、测试8下钻等既有范围。回归：`verify_ai_business_quality_dashboard.py` 8/8 PASSED（修改前测试6「缺少Schema版本维度标签」RED 确认）；`verify_ai_business_quality.py` 8/8 与 `verify_ai_browser_e2e.py` 8/8 全绿；`verify_ai_all.py --level core` 58/58 全绿；`verify_wms_bugs.py` 回归检查通过 |
| BUG-2026-08-13-001 | 移动端物料图片上传/删除仅验证登录不验证角色，普通用户可修改任意物料图片（与 Web 端 material.py:341 强制仓库角色不一致） | `app/routes/mobile.py` 新增 `_web_or_api_role_required` 装饰器（Web 会话或 Bearer Token 任一 + 业务角色），对 `/mobile/api/material_archive/<id>/images` POST/DELETE 应用 `@_web_or_api_role_required('warehouse')`；同步在 `scripts/lint_wms_rules.py` A2 规则 KNOWN_DECORATOR_HINTS 追加 `@_web_or_api_role_required` 白名单。回归：`tests/verify_bug_2026_08_13_001_mobile_image_role.py` 7/7 PASSED（warehouse Bearer 上传/删除通过、user Bearer 上传/删除 403、无认证 401、admin 会话回归 PASS、源码静态校验）；lint A1-A10 0 违规 |
| BUG-2026-08-13-002 | `/mobile/api/scan_submit` 用 `@require_role('warehouse')+@login_required`，两者只认 Flask-Login `current_user`（Web 会话），原生 App 用 Bearer Token 提交入库/出库/盘点一律 401，无法完成扫码提交（功能阻断） | `app/routes/mobile.py` scan_submit 改用 `@_web_or_api_role_required('warehouse')`；handler 内新增 `actor = current_user if current_user.is_authenticated else get_bearer_user()`，`operator_id` 与角色判定统一用 `actor`；导入列表追加 `get_bearer_user`。回归：`tests/verify_bug_2026_08_13_002_scan_submit_bearer.py` 7/7 PASSED（warehouse Bearer 入/出/盘/查询成功且 operator_id 落库、user Bearer 403、无认证 401、Web 会话 admin 回归）；lint A1-A10 0 违规 |
| BUG-2026-08-13-003 | `_save_material_image_from_url` 调用 `requests.get(url, ..., max_redirects=5)`，但 `requests.get` 不接受 `max_redirects`（它是 `Session.max_redirects` 属性），每次必抛 `TypeError: request() got an unexpected keyword argument 'max_redirects'`，移动端/物料档案「网上找图」功能完全不可用（功能阻断） | `app/app.py` `_save_material_image_from_url` 改用 `allow_redirects=False` 手动逐跳跟随重定向上限 5 跳，删除非法的 `max_redirects=5` 参数。回归：`tests/verify_bug_2026_08_13_003_004_remote_image_ssrf.py` T1/T4/T5/T6/T8 PASSED |
| BUG-2026-08-13-004 | `_save_material_image_from_url` 旧实现 `allow_redirects=True` 只对初始 URL 主机调用 `_is_private_or_loopback_host`，不对 302 目标重新校验；首跳合法外网、后续 302 可跳到 `127.0.0.1`/`169.254.169.254` 等内网地址（SSRF），修复 BUG-003 后该路径重新可达使风险暴露 | `app/app.py` 改为手动逐跳跟随：每跳 urljoin 解析 Location → 校验 scheme ∈ (http,https) → 对新主机重新调用 `_is_private_or_loopback_host` → 命中内网即拒绝「不允许访问内网地址」；重定向上限 5 跳、Location 缺失拒绝、非 http(s) scheme 拒绝。回归：`tests/verify_bug_2026_08_13_003_004_remote_image_ssrf.py` T2/T3/T7 PASSED |
| BUG-2026-08-13-005 | `get_bearer_user` 每次 Bearer 请求都执行 `db.session.commit()`（写 `last_used_at` + `expires_at`），SQLite 下加剧写锁竞争；同时 `expires_at = now + 7d` 无条件顺延，活跃 token 永不过期（无限续期），失去 token 有效期约束意义 | `app/app.py` 改为节流写库 + 阈值续期：新增 `LAST_USED_FLUSH_SECONDS=300`/`RENEW_THRESHOLD_SECONDS=86400`/`TOKEN_TTL_DAYS=7`；`last_used_at` 仅当从未记录或距上次 >=5min 才刷新；`expires_at` 仅当剩余 <1d 才顺延 7d；否则跳过 commit。回归：`tests/verify_bug_2026_08_13_005_token_write_amplification.py` 7/7 PASSED |
| BUG-2026-08-13-006 | `app/auto_update.py` 启动自动更新直接 `git fetch origin main` + `git pull --ff-only` + `pip install -r requirements.txt`，无固定提交/标签校验、无签名或哈希验证、无引入提交审计；远端仓库或 GitHub 账号被篡改后，下次重启即自动拉入任意代码并安装任意依赖（供应链风险，该功能默认关闭但开启后无防护） | `app/auto_update.py`：①新增 `WMS_GIT_PIN`，fetch 后 `resolve_sha` 取远端 tip SHA，`verify_pin` 校验匹配（支持 SHA 前缀匹配 / git tag 经 rev-parse），不匹配拒绝 pull；②`main()` 记录更新前/后 HEAD SHA 及 `log_pulled_commits`（subject+author+date 审计清单）；③新增 `WMS_PIP_REQUIRE_HASHES=1` 追加 `--require-hashes`，依赖更新成功后 `log_pip_versions` 记录已安装包版本。回归：`tests/verify_bug_2026_08_13_006_auto_update_supply_chain.py` 11/11 PASSED |
| BUG-2026-08-15-002 | 反编译 APK 副产物 `apk_source/` 被版本控制，包含 17,916 个文件和约 208 MB 数据，导致代码库同步阶段文件枚举与传输显著变慢并可能超时 | 修复进行中：移除 `apk_source/` 并加入 `.gitignore`，回归测试验证目录不存在、Git 不再跟踪且忽略规则存在 |
| BUG-2026-08-13-007 | 部门删除拦截器 `_department_delete_blockers` 只检查领料单和采购申请，遗漏员工档案（`Employee.department_id`）引用；有员工的部门可被直接删除，导致员工档案外键悬空、部门信息丢失（基础资料完整性缺陷） | `app/app.py` `_department_delete_blockers` 新增 `Employee.query.filter(Employee.department_id == department.id).count()` 检查，与领料单检查并列；单删路由（`/department/<id>/delete`）和批量删除路由（`/department/delete`）共用同一拦截函数，两条路径同步生效。回归：`tests/verify_app_py_split_department.py` 新增 `test_delete_department_blocks_employee_reference`（单删拦截）+ `test_batch_delete_department_blocks_employee_reference`（批量删除混合场景：有员工的部门保留、无员工的部门删除成功），9/9 PASSED |
| INV-AUDIT-001 | 真实库位库存无法正确汇总到仓库库存：`get_warehouse_stock_quantities(warehouse)` 用 location 字符串匹配仓库名汇总，库位字符串与仓库名不一致或库位启用前后命名漂移时，仓库库存与总库存长期不一致 | `app/app.py` 新增 `resolve_inventory_warehouse_id(value)` 统一解析仓库 ID（None/实例/ID/名称/编码）；`update_location_inventory` / `add_location_inventory_atomic` / `deduct_location_inventory_atomic` 新增 `warehouse` / `warehouse_id` 参数并写入 `LocationInventory.warehouse_id`；`get_warehouse_stock_quantities` 改为按 `warehouse_id` 汇总，旧 location 字符串数据无 `warehouse_id` 时回退按 location 字符串匹配仓库名（兼容历史数据，不自动归入默认仓库）。回归：`tests/test_inv_audit_001_002_location_inventory_warehouse_isolation.py` 18/18 PASSED（resolve_inventory_warehouse_id 多形态解析、跨仓同名库位隔离、仓库级汇总正确、历史 location 字符串回退）。commit `d7fc6599` + `13c8a392` |
| INV-AUDIT-002 | 库位库存缺少仓库维度，`LocationInventory` 唯一约束为 `(material_id, location)`，不同仓库下同名库位被合并成一行库存；跨仓调拨同名库位相互覆盖 | `LocationInventory` 唯一约束改为 `(material_id, warehouse_id, location)`；新增 Alembic 迁移 `8b17c4d90a2e_location_inventory_warehouse_compat` 添加 `warehouse_id` 列并重建唯一约束；`auto_migrate_database` 增加 SQLite 表重建逻辑（SQLite 不支持 `ALTER TABLE DROP CONSTRAINT`，用 batch_alter_table 重建表结构 + 数据回填）；历史数据无法确定仓库归属的保留为未归属，不自动归入默认仓库。回归：`tests/test_inv_audit_001_002_location_inventory_warehouse_isolation.py::TestCrossWarehouseSameLocationNotMerged` 验证 A 仓 A1=10 与 B 仓 A1=5 不被合并；`tests/test_location_inventory_warehouse_migration.py` 验证迁移逻辑。commit `d7fc6599` |
| INV-AUDIT-003 | 移动扫码入库/出库/盘点未统一执行仓库必填规则，且库存校验直接读取全局 `Material.stock` 跨仓数量，可凭其他仓库库存扣减本仓库 | `app/routes/mobile.py` `scan_submit` 接入 `resolve_request_warehouse` 强制仓库必填（未提供或无效时 400），库存校验改用 `get_warehouse_stock_quantities(warehouse)` 返回仓库级库存；`update_location_inventory` 调用传入 `warehouse` 参数确保 `warehouse_id` 写入；库位字段独立解析不再与仓库名混淆。回归：`tests/test_inv_audit_003_mobile_scan_warehouse_scope.py` 7/7 PASSED（仓库必填拒绝、仓库级库存校验、跨仓库存隔离、入/出/盘均落 warehouse_id）。commit `2b47d8e7` |
| INV-AUDIT-004 | 旧物料模糊查询接口 `POST /api/query/search` 未校验仓库参数，直接返回全局 `Material.stock`，违反 AGENTS.md「库存查询仓库必填筛选」规则 | `app/routes/stock_query.py` `api_query_search` 接入 `resolve_request_warehouse` 强制仓库必填（无默认仓库时返回 400「请选择仓库」），`stock` 字段改用 `get_warehouse_stock_quantities(warehouse)` 返回仓库级库存，不再回退全局 `Material.stock`。回归：`tests/test_inv_audit_004_query_search_warehouse_scope.py` 7/7 PASSED（仓库必填校验、仓库级库存返回、跨仓库存不泄漏 A 仓查不到 B 仓库存、默认仓库回退）。commit `9b1d779c` |
| INV-AUDIT-005 | 调拨/盘点/调整/普通出库/移动端 stocktake 的仓库存在性及 active 状态校验不一致：部分路由只校验非空不校验存在，部分路由不校验 active，可对已停用仓库或不存在仓库名生成单据 | `app/app.py` 新增 `resolve_active_inventory_warehouse(value, warehouse_id)` 与 `validate_inventory_warehouse(value, warehouse_id)` 辅助函数，按 ID/名称/编码统一解析并校验 `status='active'`；`app/routes/transfer.py`（from_warehouse/to_warehouse）、`app/routes/check.py`（warehouse）、`app/routes/adjustment.py`（warehouse）、`app/routes/out_order.py`（领料/其他出库 warehouse）、`app/routes/native_api.py`（stocktake warehouse）全部接入 `validate_inventory_warehouse`，校验失败返回 400，校验通过用规范化仓库名回写单据字段。回归：`tests/test_inv_audit_005_warehouse_active_validation.py` 23/23 PASSED（辅助函数多形态解析 + active 过滤、各路由对不存在/已停用仓库拒绝、对 active 仓库通过且规范化回写）。commit `da22cc81` |
| BUG-2026-08-14-001 | PUR-AUDIT-001：多采购订单选单生成采购入库时，`assert_warehouse_active()` 返回 `(ok, msg)` 二元组被直接赋给 `wh_err`，非空元组恒为真（含 `(True, '')`），导致合法仓库也被拒绝，选单下推功能完全不可用 | `app/routes/purchase_order.py` `create_in_order_from_purchase_order_selection` 路由改为正确解构 `wh_ok, wh_msg = assert_warehouse_active(warehouse, allow_empty=False)`，仅在 `not wh_ok` 时返回 `api_error(wh_msg)`；保留默认仓库回退和多来源 `source_purchase_order_id=None`/明细 `source_purchase_order_item_id` 写入逻辑不变。回归：`tests/test_pur_audit_001_selection_warehouse.py` 4/4 PASSED（同供应商多订单选单成功+来源完整、不存在仓库拒绝、停用仓库拒绝、不同供应商拒绝） |
| BUG-2026-08-14-002 | PUR-AUDIT-002：多订单选单生成入库时 `InOrder.source_purchase_order_id=None`，真实来源仅保存在 `InOrderItem.source_purchase_order_item_id`，但采购订单删除只检查表头 `source_purchase_order_id`，可删除仍被入库明细引用的采购订单，破坏来源追溯和 `received_quantity` 执行进度 | `app/app.py` 新增 `has_inbound_reference(purchase_order_id)` 辅助函数，同时检查表头级 `InOrder.source_purchase_order_id` 和行级 `InOrderItem.source_purchase_order_item_id`（JOIN `PurchaseOrderItem`，参数化查询）；`app/routes/purchase_order.py` `delete_purchase_order` 和 `batch_delete_purchase_order` 均改用 `has_inbound_reference`，批量删除返回被阻断清单。回归：`tests/test_pur_audit_002_delete_protection.py` 8/8 PASSED（辅助函数契约、单来源草稿阻断、多来源草稿/已完成双阻断、删除草稿释放引用后可删、批量删除返回阻断清单） |
| BUG-2026-08-14-003 | PUR-AUDIT-003：入库完成与批量完成在写锁后、写库存前未重新校验仓库 active 状态，草稿保存后仓库被停用仍可完成入库，导致新库存归属停用仓库 | `app/routes/in_order.py` `complete_in_order` 和 `batch_complete_in_order` 在 `_acquire_order_write_lock` 成功后、`add_stock` 之前调用 `assert_warehouse_active(order.warehouse, allow_empty=False)`；单张完成回滚并返回 400，批量完成将该单据加入 skipped 并继续处理其他合法单据。回归：`tests/test_pur_audit_003_complete_warehouse_active.py` 4/4 PASSED（停用仓库单张完成拒绝+库存不变、不存在仓库拒绝、批量完成跳过停用仓+其他正常、启用仓库正常完成）。`verify_in_order_state_machine.py` PASS、`verify_inbound_push.py` PASS |
| BUG-2026-08-14-004 | PUR-AUDIT-004：用户需求——采购入库明细表、领料单明细表、导出采购入库明细表、导出领料单明细表必须有合同单号、工程名称字段。4 个导出函数（`batch_export_in_order`/`export_single_in_order`/`export_out_order`/`export_single_out_order`）此前未输出 `item.contract_no`/`item.project_name`，明细表模板已含字段但导出 Excel 缺失，导出文件与页面明细不一致 | `app/routes/in_order.py` `batch_export_in_order`、`export_single_in_order` 在表头与每条数据行追加「合同单号」「工程名称」两列，值取 `item.contract_no`/`item.project_name`；`app/routes/out_order.py` `export_out_order`、`export_single_out_order` 同步追加两列；列序与明细表模板一致。回归：`tests/test_pur_audit_004_export_contract_project_fields.py` 4/4 PASSED（采购入库单张/批量、领料单单张/批量导出均含两列且行级值与 item 一致）。lint A1-A10 与裸 fetch 扫描 0 违规 |
| BUG-2026-08-14-005 | SALES-AUDIT-001（P0）：`recalculate_sales_order` 在出库完成回写 `shipped_quantity` 时不检查销售订单 `status`，已 `cancelled` 的订单被出库完成静默"复活"为 `confirmed`/`closed`，导致取消语义被破坏、库存扣减与取消决策不一致 | `app/app.py` `recalculate_sales_order` 在重算前增加 `cancelled` 状态守卫：若 `order.status == 'cancelled'` 直接返回，不覆盖状态、不更新 `shipped_quantity`，保留取消决策痕迹。回归：`tests/test_sales_audit_001_cancelled_revive_guard.py` 验证取消订单的出库完成不会改写状态、库存仍按既有扣减逻辑但订单保持 cancelled |
| BUG-2026-08-14-006 | SALES-AUDIT-002（P1）：`cancel_sales_order` 不检查已存在的 `pending` 销售出库草稿，取消后这些草稿仍可被完成，绕过 SALES-AUDIT-001 守卫的并发窗口 | `app/routes/sales.py` `cancel_sales_order` 在状态变更前查询该订单关联的 `OutOrder.business_type=='销售出库' and status=='pending'`，存在则拒绝取消并提示先处理草稿。回归：`tests/test_sales_audit_002_cancel_blocks_pending_outbound.py` 验证有 pending 草稿时取消被拒、无草稿或草稿已完成时取消成功 |
| BUG-2026-08-14-007 | SALES-AUDIT-003+004+006（P1 并发与超量一组）：①`create_sales_outbound_from_selection`/`batch_create_sales_outbound` 锁 `SalesOrderItem` 不锁 `SalesOrder`，并发下推重复草稿；②`sync_sales_order_shipment` 用 `+= quantity` 回写 `shipped_quantity` 非原子，并发完成丢失更新；③`complete_out_order` 不校验出库数量 ≤ 未发货数量，叠加编辑路径（SALES-AUDIT-005）允许"小数量草稿→编辑改大→完成"超量出库 | ①`app/routes/sales.py` 下推草稿路由调用 `_acquire_order_write_lock(SalesOrder, ...)` 加写锁后二次校验状态；批量下推循环内每张订单独立提交，避免前序草稿丢失。②`sync_sales_order_shipment` 改用 `sa_update` 原子累加 `shipped_quantity`。③`complete_out_order` 加锁后、扣库存前调用新增的 `sales_outbound_remaining_check` 校验每条有来源明细不超过 `sales_item.quantity - sales_item.shipped_quantity`，超量 rollback 返回错误。回归：`tests/test_sales_audit_003_004_concurrency_and_atomic.py`、`tests/test_sales_audit_006_complete_remaining_check.py` 验证加锁/原子/超量场景 |
| BUG-2026-08-14-008 | SALES-AUDIT-005（P1）：`/out_order/add` 编辑销售出库草稿时重建明细未保留 `source_sales_order_item_id`，导致发货进度无法正确回写到原销售订单行 | `app/routes/out_order.py` 编辑路径重建 `OutOrderItem` 时从既有明细携带 `source_sales_order_item_id`，确保 `sync_sales_order_shipment` 能正确按来源行回写 `shipped_quantity`。回归：`tests/test_sales_audit_005_edit_preserve_source_item.py` 验证编辑后明细仍保留来源关联 |
| BUG-2026-08-14-009 | SALES-AUDIT-007（P1）：销售报表/导出未将仓库作为必填条件，违反 AGENTS.md「库存查询、出入库报表、库存台账的查询入口必须将仓库作为必填条件」 | `app/app.py` 新增 `_require_report_warehouse()` 辅助函数，按 `warehouse_id` → `warehouse` 名称 → 默认仓库顺序解析，无则返回 400；`app/routes/sales.py` 10 个报表/导出路由（`export_sales_orders`/`export_sales_report`/`export_sales_execution_report`/`export_sales_outflow_report`/`export_sales_outbound`/`sales_reconciliation_report`/`sales_trend_report`/`sales_price_analysis`/`export_sales_price_analysis`/`export_sales_execution_report`）全部接入该门禁。回归：`tests/test_sales_audit_007_report_warehouse_required.py` 验证无仓库参数返回 400/空结果、有默认仓库时回退成功。**补充修复（2026-08-15，CI 首次跑通暴露）**：`scripts/full_smoke_test.py` 导出用例此前无参数调用销售报表导出端点，门禁生效后 6 个销售导出（`/sales/export`、`/sales/outbound/export`、`/sales/reconciliation/export`、`/sales/report/export`、`/sales/trend_report/export`、`/sales/outflow_report/export`）返回 400，CI 冒烟 114/120 红。修复：脚本新增 `get_warehouse_id()`（从 `/api/warehouses` 取仓库 id，无则经 `/warehouse/add` 创建），销售导出端点统一附加 `?warehouse_id=` 参数。回归：冒烟 121/121 PASSED，WMS CI 转绿。commit `6667135` |
| BUG-2026-08-14-010 | SALES-AUDIT-008（P1）：`complete_out_order` 在草稿保存后才校验仓库，草稿创建时仓库可能仍 active，但保存后仓库被停用，完成流程此前不再复核，导致已停用仓库仍可完成出库 | `app/routes/out_order.py` `complete_out_order` 加锁后、扣库存前对 `business_type=='销售出库'` 的单据调用 `validate_sales_outbound_warehouse(order)` 复核 active 状态（对照 PUR-AUDIT-003 的 `in_order.py:1389` 修复模式），不通过则 rollback 返回错误。回归：`tests/test_sales_audit_008_warehouse_active_recheck.py` 3/3 PASSED（active 仓库完成成功、停用仓库完成被拒+状态保持 pending、非销售出库不受影响） |
| BUG-2026-08-14-011 | SALES-AUDIT-009（P1）：销售订单导出/报表/打印缺合同单号与工程名称字段。`SalesOrder` 模型已有 `contract_no`/`project_name`/`project_no` 字段，列表页与详情页已展示，但 4 个 P1 导出/打印入口（`export_sales_orders`/`export_sales_report`/`export_sales_execution_report`/`print_sales_order`）遗漏，2 个 P2 出库导出（`export_sales_outflow_report`/`export_sales_outbound`）已关联 `SalesOrder` 但未输出合同/工程 | `app/routes/sales.py` 6 个导出/打印入口在表头与数据行（或打印 info 列表）追加合同单号/合同编号、工程名称两列，值取 `order.contract_no or ''`/`order.project_name or ''`（出库导出来自关联 `source_sales_order`）；修复运算符优先级陷阱 `(obj.field or '') if obj else ''` 避免 AttributeError。回归：`tests/test_sales_audit_009_export_contract_project_fields.py` 6/6 PASSED（4 个 P1 + 2 个 P2 导出/打印均含两列且行级值与订单一致） |
| BUG-2026-08-14-012 | SYS-AUDIT-001（P1）：`execute_init_business_data` 成功消息谎称"SystemSetting 已保留"，实际 `INIT_LOG_TABLES` 含 SystemSetting 被全量删除，导致 AI API Key、LLM 接口地址等配置丢失到下次服务器重启 | `app/routes/system_settings.py` init 清理完成后立即调用 `ensure_default_system_settings()` 恢复默认值；响应消息改为"User 账号已保留，系统参数已重置为默认值"；修正 `_init_business_data_keep_users_and_settings` docstring 明确 SystemSetting 会被删除。回归：`tests/test_system_settings.py::TestExecuteInitBusinessDataRestoresSettings` 验证 init 后 SystemSetting 表非空 |
| BUG-2026-08-14-013 | SYS-AUDIT-002（P1）：唯一测试文件 `verify_app_py_split_system_settings.py` 以 `verify_` 前缀命名，pytest 默认不收集，覆盖也仅限 endpoint 注册/页面渲染/空表单保存/preview 访问，未覆盖高风险的 init 失败路径、字段校验、secret 保留逻辑 | 删除 `verify_` 文件，新建 `test_system_settings.py` 使 pytest 自动收集；补充 7 个高风险路径测试（int 越界/select 非法值/secret 留空保留/init 错短语/缺密码/错密码/init 成功后恢复默认值）。回归：13/13 PASSED |
| BUG-2026-08-14-014 | SYS-AUDIT-003（P2）：`execute_init_business_data` 用豁免注释绕过 A8 pydantic 校验，3 个固定参数手工 strip 和比较，无类型契约 | 引入 `InitBusinessDataExecuteForm(BaseModel)`，声明 `admin_password`/`confirm_phrase`（min_length=1）和 `include_master_data`（field_validator 限制 0/1），移除豁免注释。`save_system_settings`/`test_ai_llm_settings` 参数由 SYSTEM_SETTING_DEFINITIONS 动态驱动，保留豁免注释。回归：13/13 PASSED；A8 0 违规 |
| BUG-2026-08-14-015 | SYS-AUDIT-004+005（P2）：①主表单 `<form>` 缺 `method="post"` 属性，A1 lint 规则无法扫描 CSRF；②5 处 `window.csrfFetch \|\| fetch` 回退模式，若 csrfFetch 未定义会回退到裸 fetch | ①表单加 `method="post"` 属性，表单内已有 csrf_token 隐含字段；②5 处改为直接调用 `csrfFetch`（base.html 全局定义），消除裸 fetch 回退。回归：13/13 PASSED；A1 lint 0 违规；裸 fetch 扫描通过 |
| BUG-2026-08-14-016 | SYS-AUDIT-006（P2）：init 成功后清空全部历史 OperationAudit（除本次 preview+done 外），永久销毁此前所有操作审计轨迹，合规风险（等保/SOX 要求审计日志不可销毁） | 清空前导出一份 OperationAudit 全量 JSON 备份到 `output/audit_backup/audit_backup_YYYYMMDD_HHMMSS.json`，响应消息附带备份路径。导出失败不阻断 init 主流程（仅日志告警）。回归：13/13 PASSED |
| BUG-2026-08-14-017 | SYS-AUDIT-007+008（P2）：①5 个路由装饰器顺序为 `@require_role` 在 `@login_required` 之上，非常规写法易误导；②3 处异常处理将 `str(exc)` 拼入返回给浏览器的 JSON 消息，可能暴露 LLM URL/API Key/数据库表名 | ①统一为 `@login_required` 在上、`@require_role('admin')` 在下；②3 处改为对外返回通用消息"请查看服务器日志获取详情"，详细错误仍通过 `app.logger.exception()` 记录。回归：13/13 PASSED |
| BUG-2026-08-14-018 | SYS-AUDIT-009（P2）：前端确认按钮 JS 硬编码 `fd.append('include_master_data', '1')`，用户无法选择保留主数据，每次 init 都会清空全部主数据 | 模态框新增 checkbox"同时清空主数据"，默认勾选；JS 从 checkbox 读取状态替代硬编码 '1'，取消勾选则传 '0'。回归：13/13 PASSED |
| BUG-2026-08-14-019 | SYS-AUDIT-011（P2）：`set_system_setting` 用 `filter_by(key=key).first()` 查询后更新，无 `with_for_update()` 行锁，两个 admin 并发保存时后提交者覆盖前者部分变更 | 查询加 `.with_for_update()` 行锁。SQLite 忽略 with_for_update（no-op），MySQL/PostgreSQL 生效。回归：`tests/test_system_settings.py::TestSetSystemSettingWriteLock` 验证加锁后正常写入 |
| BUG-2026-08-14-020 | AUDIT-2026-08-14-001（P1 | FIX-001）：移动端 `POST /api/inbound`、`POST /api/outbound` 在关闭库位管理且无默认仓库时，不传 warehouse/warehouse_code/warehouse_id 仍返回 200 并创建 status=completed 空仓库单据、改库存，违反「仓库无论库位管理是否开启都必填」「无默认仓库必须拒绝」规则，与 Web 端仓库规则不一致 | `app/routes/native_api.py` `inbound`/`outbound` 在 `parse_api_lines()` 成功后、创建 `InOrder`/`OutOrder` 前调用 `resolve_request_warehouse(payload)` 解析并校验仓库存在且 active，失败返回 400「请选择仓库」/「不存在」/「已停用」；成功用规范化仓库名写回单据；开启库位管理路径继续独立校验库位；保留 Bearer Token 认证、`api_role_required` 角色校验与 `mobile_api_idempotent` 幂等。回归：`tests/test_bug_2026_08_12_004_mobile_warehouse_scope.py::TestMobileWarehouseWriteScope` 21/21 PASSED（无默认仓库入/出库 400 且无单据库存不变、默认仓库回退成功、停用仓库 400、库位管理开启缺库位 400）。commit `9460f68c` |
| BUG-2026-08-14-021 | AUDIT-2026-08-14-002（P1 | FIX-002）：生产环境可通过 `WMS_DISABLE_CSRF=1/true/yes` 关闭全局 CSRF，`ProductionConfig.WTF_CSRF_ENABLED=False`，应用仅记日志仍继续启动，存在 CSRF 防护整体失效风险 | `app/config.py` 新增 `validate_production_security_config()`，生产启动时若检测到 `WMS_DISABLE_CSRF` 直接 `raise RuntimeError` fail-fast 拒绝启动；`ProductionConfig.WTF_CSRF_ENABLED` 硬编码为 `True`；`app/app.py` 在 `app.config.from_object` 前调用该校验函数。开发/测试环境隔离开关不受影响；移动端 `/api/*` Bearer 认证与 `csrf.exempt` 端点保持不变。错误信息不含变量完整值/密钥/Token。回归：`tests/test_production_security_config.py` 6/6 PASSED（production+未设置开启、production+`1`/`true` 均失败且信息明确、测试环境开关生效）。commit `1f557e63` |
| BUG-2026-08-14-022 | AUDIT-2026-08-14-003（P2 | FIX-003）：路由拆分迁移到 `app/routes/` 后，验证脚本仍静态读取 `app/app.py` 查找已迁移函数，导致静态 C 段误报失败（如 `verify_bug_2026_08_02_001.py` 读 in_order 函数、`verify_bug_2026_08_02_002.py` 读出库函数、`verify_opening_stock_multi_warehouse.py` 读期初路由）；`verify_offline_wheelhouse.py` 按宿主 OS(Linux) 解析 Windows wheelhouse 误报 cryptography 缺失 | `scripts/verify_bug_2026_08_02_001.py` 改读 `app/routes/in_order.py`；`verify_bug_2026_08_02_002.py` 改读 `app/routes/out_order.py` + `after_sale_out.py`；`verify_opening_stock_multi_warehouse.py` 改读 `app/routes/opening_stock.py`，正则按 4 空格缩进匹配迁移函数；`verify_offline_wheelhouse.py` 改用 `--platform win_amd64 --python-version 3.11 --implementation cp --abi cp311 --only-binary=:all:` 跨平台解析 Windows wheelhouse。回归：3 脚本全部 PASS + `verify_stability_gate.py` 门禁通过。commit `36331e7b` + `5facb057` |
| BUG-2026-08-14-023 | AUDIT-2026-08-14-004（P2 | FIX-004）：`verify_transfer_state_machine.py` 种子未配置仓库维度且库位库存用旧 `warehouse_id=NULL` 结构；`verify_adjustment_state_machine.py` 创建 `AdjustmentOrder` 无 `warehouse`；`verify_check_state_machine.py` 创建 `InventoryCheck` 无 `warehouse`，当前完成路由按仓库必填规则拒绝，专项脚本失败 | 仅修测试种子，不降低生产仓库必填规则：`verify_transfer_state_machine.py` 显式开启 `location_management_enabled=1` 并构造带仓库/库位的余额转移前置；`verify_adjustment_state_machine.py` 为 `AdjustmentOrder` 赋 `warehouse="默认测试仓"`；`verify_check_state_machine.py` 为 `InventoryCheck` 赋 `warehouse="默认测试仓"`。回归：3 状态机脚本 PASS + `pytest tests/` 400/400 PASSED。commit `36331e7b` |
| BUG-2026-08-15-001 | 采购订单列表 `/purchase_order` 与物料列表 `/material` 列表路由仅 `@login_required`，任何已登录用户（production / viewer 等受限角色）可用 URL 直访越权进入采购/基础资料页面，虽侧边栏已按角色隐藏菜单但其防护只在渲染层，服务端无角色门禁（与侧边栏可见性不一致） | `app/routes/purchase_order.py` `purchase_order_list` 与 `app/routes/material.py` `material_list` 均加 `@require_role('warehouse')`（require_role 内置放行 admin），与侧边栏「采购管理 admin/warehouse」「基础资料 admin」可见性对齐；production/viewer 直访被 302 跳回首页。回归：`tests/verify_bug_2026_08_15_001_purchase_material_list_role.py` 7/7 PASSED（production @ /purchase_order、production @ /material、viewer @ /material、viewer @ /purchase_order 均被拒；warehouse @ 两路由 200；admin @ 两路由 200）。浏览器 E2E 复跑 127/128 PASSED（唯一未过为测试环境缺 APK 的 `/mobile/app` 404，非代码缺陷）。commit 见本次提交。**补充修复（CI 首次跑通暴露）**：`purchase_insights` 能力声明允许 `purchase` 角色，但其业务端点 `/purchase_order` 列表仅 `@require_role('warehouse')`，交集为空导致 AI 采购洞察对采购角色被拒（`verify_ai_permission_matrix.py` FAIL）。修复 `purchase_order_list` 改为 `@require_role('warehouse','purchase')`，采购角色可访问采购订单列表支撑 AI 采购洞察；`/material` 保持 warehouse（与 `master_data_insights` 声明一致）。回归：`verify_ai_permission_matrix.py` PASS + BUG-001 测试扩为 8/8 PASSED + `pytest tests/` 456/456 PASSED。commit 见本次提交 |

## 已确认误报

| 编号 | 原问题 | 误报原因 |
|------|--------|----------|
| BUG-003 | 委外收货数量双倍计算 | 源码已有 autoflush 处理说明，sum 查询没有额外加本次数量 |
| BUG-008 | 全局确认框变量竞态 | 当前实现已使用确认队列，不存在 resolver 覆盖 |
| SCAN-2026-08-07-001 | `scan_wms_risks.py` 报 4 个 `\|safe` 候选：`alert.html:237`、`sales_reconciliation_report.html:147`、`print_out_with_html.html:63`、`print_in_with_html.html:67` | 逐个人工判真均为误报：①`alert.html` 全文件无 `\|safe`（行号漂移误匹配）；②`sales_reconciliation_report.html:147` 是 `chart_data\|tojson\|safe`，即 VULN-005 确立的标准安全写法；③两个打印模板的 `rendered_content` 由 `_render_html_print_content()` 生成，已经 `SandboxedEnvironment(autoescape=True)` 沙箱渲染 + `sanitize_print_html()` 白名单净化双重防护（VULN-001 已登记） |
| SCAN-2026-08-11-001 | 2026-08-11 全系统增量审计（app.py 28k 行 + routes/ 23k 行 + ai/ 21k 行 + JS 5k 行 + Android 增量）报 5 类疑似项 | 逐个人工判真均为误报：①"routes 未检查原子函数返回值"（transfer.py L427-438 / after_sale_out.py L390-406/L444-463）——源码逐条 `if not ok: rollback + return api_error`，证据齐全；②"providers.py 重试风暴/API key 泄漏"——`call_llm` 有界重试 `max_retries=2` + 指数退避 + 熔断器 `breaker`，日志只打异常对象不含 header/key；③"streaming.py 连接泄漏"——`stream_response_payload` 是纯字符串切片的有限生成器，Flask 响应结束自然关闭；④"app.py pickle RCE / 硬编码密钥 / 未参数化 SQL"——全仓 grep 无 `pickle/eval/exec/__import__`、无硬编码 password/secret/api_key，f-string SQL 仅存在于 `auto_migrate_database` 内部 DDL（表名/列名全部为代码内常量，非用户输入）；⑤"AI 路由缺权限"——`ai/routes.py` 17 条、`ai/v2_routes.py` 19 条全部 `@login_required`，能力校验在 tools 层 `AI_CAPABILITY_ROLES`（AI-AUTH-001 已登记）。审计同时确认：全部 `requests.*` 调用带 timeout；`subprocess` 仅存在于 restart.py/auto_update.py 固定参数部署脚本；无 `send_file/send_from_directory` 用户输入路径遍历；上传有 `MAX_CONTENT_LENGTH=50MB` + `ALLOWED_EXTENSIONS` + Excel 5MB 三层限制；lint A1-A10 0 违规、无裸非 GET fetch；`verify_ai_all.py --level smoke` 7/7 PASS（唯一失败项为沙箱缺 `requests` 依赖的环境问题，装依赖后 PASS，非代码问题） |

## 降级或暂缓项

| 编号 | 状态 | 说明 |
|------|------|------|
| BUG-007 | 低风险 | 原问题主要是死代码/防御性分支，不是核心业务矛盾 |
| VULN-002 | 低风险 | `|safe` 使用前已有 sanitize 保护，继续保持净化入口即可 |
| CONF-003 | 低风险 | 生产配置已修正，环境部署时仍需确认 HTTPS/Cookie 配置 |
| SQLite 并发能力 | 已修复单机并发边界 | 已启用 WAL、60 秒 busy timeout、外键、原子库存更新；启动迁移使用排他事务串行执行。高并发生产环境仍建议迁移 MySQL/PostgreSQL |
| BUG-NEW-002 | 暂不按 BUG | `update_completed_in_order()` 的库位增减路径支持正负 `qty_diff`，新增、删除、数量变更均已有库位同步 |
| BUG-NEW-004 | 已修复 | 保留 `received_quantity` 作为防重复下推的占用量；采购订单状态改为仅汇总 `completed` 入库单，草稿和待提交入库不再提前把订单标记为已入库 |
| BUG-NEW-007 | 已修正字段语义 | `api_material_payload()` 不再把 `warehouse_code` 直接复制给 `location_code`，库位启用时取库位库存记录 |
| BUG-NEW-010 | 低风险体验项 | `makeKey()` 已会规范化 `embedded` 参数；未确认恢复错配。仅提高恢复标签数量上限 |
| BUG-NEW-012 | 低风险体验项 | 登录页占位链接/勾选框不影响核心业务；初始密码展示必须与 BUG-NEW-005 和 `AGENTS.md` 一致 |
| BUG-NEW-016 | 已修复 | 自动迁移在检查字段前执行 SQLite `BEGIN EXCLUSIVE` 并等待最多 60 秒；迁移失败将阻止进程继续启动，避免多个 worker 重复 DDL 或带不完整结构运行 |
| BUG-NEW-017 | 误报 | 报告描述为“日志不包含具体 SQL，不利排查”，不是“敏感 SQL 泄露” |
| BUG-NEW2-005 | 低风险设计项 | `log_operation()` 是独立的 best-effort 日志提交，当前多在业务提交后调用；不按核心数据 BUG 处理 |
| JS-NEW2-001 | 低风险兜底 | `confirmDialog()` 缺 DOM 时降级 `window.confirm()` 是可用性兜底，不影响数据正确性 |
| JS-NEW2-002 | 低风险兜底 | `toast()` 缺容器时降级 `alert()` 是可用性兜底，不影响核心业务 |
| SEC-NEW2-001 | 已有保护 | `/api/login` 是换取 token 的原生 API；入库/出库/盘点有 `@api_required`，微信助手有独立授权，不移除 CSRF 豁免 |
| SEC-NEW2-002 | 平台限制 | Windows 对 `chmod 0600` 支持有限，属于部署环境权限控制问题；生产应放在受限账户/目录下运行 |
| BUG-2026-08-05-001 | 采购入库完成后下推单据功能失效：路由拆分迁移到 `app/routes/in_order.py` 时，`create_in_order_push` 使用了 `DocumentPushLine` 却漏导入，点击下推提交时抛 `NameError: name 'DocumentPushLine' is not defined`，页面提示"下推失败，请稍后重试" | 在 `create_in_order_push` 的 `from app import (...)` 中补入 `DocumentPushLine`，与 `out_order.py` / `after_sale_out.py` 的下推导入保持一致。回归：`scripts/verify_inbound_push.py`（6 种下推组合、追溯、数量上限、幂等、权限、库存生命周期、客供料拦截 Full PASS） |
| BUG-2026-08-05-002 | 采购入库单点击"完成入库"失败、单据停在草稿、下推按钮不出现：`complete_in_order` / `update_completed_in_order` / `update_in_order` 三个函数的延迟导入均漏 `InOrder`，点击完成入库即抛 `NameError: name 'InOrder' is not defined`，被 `except` 吞掉返回"提交失败，请稍后重试" | 在三个函数的 `from app import (...)` 中补入 `InOrder`；并在 `complete_in_order` 的 `except` 分支补 `app.logger.exception` 记录堆栈。新增静态检查 `scripts/check_in_order_imports.py` 扫描全部使用 `InOrder` 的函数确保导入覆盖。回归：`scripts/repro_complete_in_order.py`（完成入库→status=completed→can_push），`scripts/verify_inbound_push.py` Full PASS，`make check` 116 passed |
| BUG-2026-08-05-003 | 物料无法删除：`delete_material` 的引用完整性检查漏掉了 `PurchaseOrderItem` / `SalesOrderItem` / `AIMaterialAlias` / `AIDocumentItem` 四张表。被采购订单或销售订单明细引用的物料走不到"存在业务引用，建议改为停用"分支，直接 `db.session.delete(material)` 触发数据库外键 `NOT NULL constraint failed`，被 `except` 捕获返回晦涩的"数据库操作失败: (sqlite3.IntegrityError)..."，用户感知为"物料删不掉" | 在 `delete_material` 的 `from app import (...)` 补入 `PurchaseOrderItem` / `SalesOrderItem` / `AIMaterialAlias` / `AIDocumentItem`，并在引用完整性校验里补这四个表的 `material_id` 检查，与既有引用拦截逻辑一致。回归：`scripts/repro_material_delete_ref.py`（PO 引用/SO 引用被拦截且明细保留、无引用删除成功），新增 `tests/verify_app_py_split_material.py::test_delete_material_referenced_by_purchase_order_item`，`make check` 116 passed |
| BUG-2026-08-05-004 | 物料仍无法删除（前端报 `Unexpected token T, "Internal S"... is not valid JSON`）：`delete_material` 的引用完整性检查是裸 `AIMaterialAlias.query... or AIDocumentItem.query...` 连环查询，且删除走 `db.session.delete(material)`。当生产/旧库缺 AI 关联表（`ai_material_alias` / `ai_document_item`，AI 能力代码已上线但库未跑迁移）时，①引用检查首个缺表即抛 `OperationalError` 未捕获 → 整个接口打成 500 HTML；②即便引用检查侥幸通过，`db.session.delete` 触发 ORM 级联加载缺失 backref 表同样抛 500。前端把 HTML 当 JSON 解析 → 报 `Unexpected token 'T'`，用户感知"物料删不掉" | ①引用检查重构为逐表 `try/except` 循环，某表缺失/查询抛错时仅跳过该表（视为无引用）并记 warning，绝不把接口打成 500 HTML；②主数据删除改用 raw SQL `DELETE FROM material WHERE id=:id`（先清理 `LocationInventory`），绕过 `db.session.delete` 的 ORM 级联加载缺失 backref 表；单条失败 `db.session.rollback()` 防 PendingRollback 污染后续迭代。回归：`tests/test_material_delete_missing_ai_table.py`（2 用例：缺失 `ai_document_item` 表时删除无引用物料返回 JSON 200 且物料已删；被引用物料仍被拦截返回 JSON 400），`pytest` 2 passed，lint A1-A10 0 违规 |
| BUG-2026-08-05-005 | 物料删除报"Unexpected token 'I', "Internal S"... is not valid JSON"（BUG-2026-08-05-004 的防护层补强）：前端 `csrfFetch`/`postJsonForAction` 发 JSON POST 只带 `Content-Type: application/json`（fetch 默认 `Accept: */*`、不带 `X-Requested-With`），`wants_json_error_response()` 漏判这类请求；视图一旦抛异常，`handle_exception` 非 JSON 分支直接返回英文纯文本 `Internal Server Error`（DEBUG/非 DEBUG 两分支相同），前端 `r.json()` 解析失败，真实错误完全丢失；且 500/CSRF 处理器里 `db.session.rollback()` 未兜底，连接损坏时异常逃出 Flask 会被 waitress 兜底为英文纯文本 500 | ①`wants_json_error_response()` 新增 `request.is_json` 判断：请求体是 JSON 即视为 AJAX 调用，统一回 JSON 错误；②`handle_exception` 非 JSON 分支改为中文"服务器内部错误，请稍后重试"（DEBUG 附堆栈）；③新增 `_db_safe_rollback()`，500/Exception/CSRF 三个全局处理器的 rollback 全部兜底。回归：`tests/verify_bug_2026_08_05_005_error_handler_json.py`（JSON POST 触发 500 返回 JSON、rollback 失败仍返回 JSON、CSRF 失败返回 JSON 400、is_json 识别），`pytest tests/` 116+4 passed，lint 0 违规 |
| BUG-2026-08-05-006 | 采购入库单/销售出库单新增页复用"其他出入单"工具栏，导入/导出/导入导出模板/智能分享/首张/上一张/下一张/末张按钮均为"功能待开发"占位（原误登记为与本表 BUG-2026-08-05-001 重号，2026-08-05 审计更正编号） | 重写 `in_order_add.html`/`out_order_add.html` 的 `handleOtherOrderToolbar`：导入→`showPasteModal()` 粘贴导入；导出→`exportDetailRows()` 导出当前明细 CSV（带 BOM）；导入导出模板→`showImportTemplateHint()` 提示支持列；删除→`WMS.api.post('/in_order\|out_order/{id}/delete')` 后返回列表页；智能分享/首张/上一张/下一张/末张→引导提示（新增单无导航目标）。浏览器实测新增页全部工具栏按钮按预期工作，无"功能待开发"占位提示。commit `4696ba5d` |
| BUG-2026-08-05-007 | pytest 全量跑时 `tests/test_material_delete_missing_ai_table.py` 两用例 setup 报 `RuntimeError: Refusing db.drop_all outside testing in-memory database`：pytest 按字母序收集模块，`test_get_default_warehouse.py` 等先 `import app` 且未设 `DATABASE_URL`，app 被缓存为文件库配置；后续模块再设内存库 env 已无效，`guarded_drop_all` 拒绝执行。单跑该文件通过、全量跑即挂，属测试隔离缺陷 | 新增 `tests/conftest.py`：先于一切测试模块导入，`os.environ.setdefault` 固定 `DATABASE_URL=sqlite:///:memory:` / `WMS_BOOTSTRAP_PASSWORD=admin` / `WMS_DEBUG=0` / `WMS_SKIP_AUTO_UPDATE=1`，app 首次导入即内存库，与导入顺序无关。回归：`pytest tests/ -q` 118/118 passed（修复前 116 passed + 2 errors） |
| BUG-2026-08-05-008 | 工单领料单（`/requisition`，ProductionRequisition）全链路无仓库字段，违反 AGENTS.md「领料出库仓库必填」规则（2026-08-05 全面审计发现） | ①模型加 `warehouse` 列（nullable 兼容存量）+ `auto_migrate_database` 补列迁移并回填默认仓；②`document_table_form.html` requisition 分支加必填仓库下拉（新建预选默认仓）；③`save_requisition_table`/`add_requisition`/`update_requisition`/`complete_requisition` 全链路仓库必填校验（未填自动带入默认仓，无默认仓拒绝保存）；④`complete_requisition` 开启库位管理时按仓库原子扣库位库存、`revert_requisition` 对称还原；⑤列表页/打印模板/导出/报表（`_build_requisition_report` 按仓库过滤、`requisition_list` 关键词可搜仓库）全展示层贯通；⑥BOM 下推与 Excel 导入自动带入默认仓。回归：`tests/verify_app_py_split_requisition.py::TestRequisitionWarehouse`（6 场景：显式仓库保存、默认仓带入、无默认仓拒绝、save_table 携带、完成扣库存、反提交还原），`tests/verify_bug_P15_P16_P21.py` 报表守卫种子数据同步带仓库，`pytest tests/ -q` 118 passed，lint 0 违规，存量库迁移冒烟通过 |
| BUG-2026-08-04-001 | `add_location_inventory_atomic` 建账冲突时 `db.session.rollback()` 回滚整个外层事务，导致总库存与库位库存、单据状态永久不一致 | 检查 INSERT 冲突时用 `begin_nested()`（SAVEPOINT），只回滚保存点，外层事务不受影响；重查后走原子 UPDATE。回归测试 `tests/verify_bug_2026_08_04_001_location_inventory_savepoint.py`，SHA `8cdfac0f` |
| BUG-2026-08-04-002 | `update_location_inventory` 负 delta 无库位记录时返回 `True, ''`（静默成功），`batch_complete_out_order` 总库存已扣但库位库存未扣 | 检查无库位记录且不允许负库存时返回 `False` + 错误信息，与 `deduct_location_inventory_atomic` 对齐。回归测试 `tests/verify_bug_2026_08_04_002_location_inventory_no_silent_success.py`，SHA `8efc4bbd` |
| BUG-2026-08-04-003 | `update_completed_in_order` 缺 `_acquire_order_write_lock`，并发编辑/反提交时库存调整可能重复执行或对 pending 单据做库存操作 | 检查库存操作前加 `_acquire_order_write_lock(InOrder, id, 'completed')`，加锁后重新读取状态并做仓库赋值。回归测试 `tests/verify_bug_2026_08_04_003_update_completed_in_order_lock.py`，SHA `aaf2b418` |
| BUG-2026-08-04-004 | 导入物料不成功没有提示原因：①`wants_json_error_response()` 只判断 `/api/` 前缀，`/material/import` AJAX POST 在 CSRF 失效时返回 302 重定向 → 前端 `r.json()` 解析 HTML 失败 → 只显示"请求失败：Unexpected token..."；②`material.html` / `batch_import.html` 导入回调未检查 `response.ok`，非 JSON 错误体丢失 `msg` | ①`wants_json_error_response()` 额外识别 `X-Requested-With: XMLHttpRequest` 与 `Accept: application/json`，AJAX 请求统一返回 JSON；②前端先检查 `response.ok`，非 2xx 时优先取 `err.msg`，非 JSON 时用 HTTP 状态码 + 友好提示。回归测试 `tests/verify_bug_2026_08_04_004_import_error_feedback.py`，SHA `045205e5` |
| BUG-2026-08-04-005 | `edit_material` 中 `spec_changed = (material.spec != new_spec)` 在 `material.spec = new_spec` 赋值之后比较，导致永为 False，规格变更不级联更新关联单据；更严重的是级联代码引用 `InOrderItem`/`OutOrderItem`/`InventoryCheckItem`/`SubcontractItem`/`BOMItem` 不存在的 `material_code`/`material_name`/`spec` 字段，一旦 `spec_changed` 为 True 会抛 `AttributeError` 导致编辑 500；`PurchaseRequestItem.spec` 也漏了级联更新 | ①在赋值前捕获 `old_spec` 并计算 `spec_changed`；②级联代码对没有冗余字段的明细类跳过赋值（`hasattr` 守卫或直接跳过），`PurchaseRequestItem` 补 `spec` 级联。回归测试 `tests/verify_bug_2026_08_04_005_spec_changed_cascade.py`（4 场景：spec 级联、不抛 AttributeError、不改规格保持不变、name 级联），SHA `32e3deb8` |
| BUG-2026-08-04-006 | `/api/ai/recommend_location` 和 `/api/ai/demand_forecast` 用 `Material.query.filter_by(status='active')` 查询，但 `Material` 模型没有 `status` 字段，导致 SQLAlchemy 抛 "Unknown column material.status" 错误，AI 库位推荐和需求预测功能直接 500 不可用 | 移除 `filter_by(status='active')`，改为 `Material.query.all()` / `Material.query`，因为 Material 模型没有启用/停用状态概念。回归测试 `tests/verify_bug_2026_08_04_006_ai_route_status_field.py`（4 场景：库位推荐不 500、需求预测无物料返回空、有物料返回 materials 列表、Material.query 不引用 status 列） |
| BUG-2026-08-04-007 | `edit_material` 用 `MAX_TRANSACTION_PRICE`（1 万亿）作为价格上限，而 `add_material` 用 `MAX_REASONABLE_PRICE`（99,999,999.99），编辑可绕过新增的价格上限，先低价新增再编辑改成天价 | `edit_material` 改用 `MAX_REASONABLE_PRICE`，与 `add_material` 保持一致；错误消息统一为 `参考价格必须是 0 至 99,999,999.99 的有限数字`。回归测试 `tests/verify_bug_2026_08_04_007_edit_material_price_limit.py`（4 场景：超限 400 拒绝、恰好上限 200 成功、正常值 200 成功、新增侧超限仍 400 拒绝） |
| BUG-2026-08-04-008 | `import_material` 不做 `sanitize_text_input`（XSS/NUL 防护）、不做 code/name/spec 长度校验、不做价格上限校验（直接 `float()`），与 `add_material` 不一致，可通过 Excel 导入注入 XSS、超长字段（DB 静默截断）、天价/负价物料 | 导入走 `sanitize_text_input` + 原始值长度校验（code≤50/name≤100/spec≤100/brand≤100，超限行跳过并告知原因）+ 价格显式校验（0 至 `MAX_REASONABLE_PRICE`，负数/超限/NaN 行跳过）。回归测试 `tests/verify_bug_2026_08_04_008_import_validation.py`（6 场景：编码超长跳过、名称超长跳过、价格超限跳过、负价跳过、XSS 标签去除、正常数据导入成功） |
| BUG-2026-08-04-009 | `add_material` 创建物料时直接写 `material.stock = initial_stock`，但不在 `stock_transaction` 表记录 opening 流水，导致库存台账/月报无法追溯初始库存来源 | 物料创建并提交后，若 `initial_stock > 0`，用 `add_stock_transaction` 追加一条 `StockTransaction`（`transaction_type='opening'`，`reference_type='opening_stock'`，`location` 取默认仓库名），与期初库存调整语义一致。回归测试 `tests/verify_bug_2026_08_04_009_add_material_stock_audit.py`（3 场景：初始库存>0 生成流水、初始库存=0 不生成、流水数量/类型/物料正确） |
| BUG-2026-08-04-010 | 复制物料保存后采购入库单不显示该物料：各单据页（in_order_add.html 等）的“刷新物料”逻辑只拉取 `/material/api/all` 第一页（默认 500 条），而物料按编码升序排列，新增/复制的物料编码通常排在末尾被分页截断，导致下拉列表里永远看不到新物料 | ①后端 `/material/api/all` 默认分页从 500 提到 2000（`per_page` 上限 2000），返回 `materials` 数组 + 分页元数据（total/page/truncated/next_page）；②前端 `refreshMaterials` 重构为 `fetchAllMaterials` 递归分页拉取全部物料再合并，确保末尾新增/复制的物料也能加载；③BroadcastChannel 收到 `material_updated` 时静默刷新。回归测试 `tests/verify_bug_2026_08_04_010_material_api_all_response_key.py`（3 场景：分页元数据正确、materials 数组、复制物料保存后出现在 materials） |
| BUG-2026-08-04-011 | 采购入库单明细行合同编号/工程名称没有像物料编码那样的关键词快速输入：后端 `/api/contracts` 返回裸对象 `{"contracts":[...]}`，缺少标准信封 `{"status":"success","data":...}`，前端 `WMS.api.getContracts()`（api.js 统一请求层）在 `res.status==='success'` 判断处把该响应当作业务失败 reject，`ContractAutocomplete` 的 `.catch()` 吞掉错误导致下拉永远不渲染；同时 5 个单据页（in_order_add/sales_order_add/sales_order_edit/purchase_order_add/out_order_add）头部分级合同搜索用裸 fetch 并判断 `Array.isArray(list)`，对裸对象也永远为 false 同样失效 | ①后端 `/api/contracts` 改为返回标准信封 `{"status":"success","data":{"contracts":[...]}}`；②5 个单据页头部分级搜索改为从信封提取 contracts（兼容 data/裸对象/裸数组）。回归测试 `tests/verify_bug_2026_08_04_011_contract_api_envelope.py`（4 场景：标准信封、合同编号/工程名称关键词命中、getContracts 读取键与字段齐全、无关键词返回全部）。**验证确认**：5 个单据（in_order_add/purchase_order_add/sales_order_add/sales_order_edit/out_order_add）共用同一套 `WMS.ContractAutocomplete` 组件与同一 `/api/contracts` 信封接口，代码结构完全一致（均为 `line-contract-no`/`line-project-name` 输入框 + 新增行 `bindRow` + 初始化 `bindAll` + 头部信封提取）。浏览器真实键盘输入已在采购入库单（in_order_add）验证成功：合同编号输入 `HD` 弹出匹配合同下拉、工程名称输入 `工程` 弹出同一套下拉、`/api/contracts` 返回 200 信封、无 JS 报错。其余 4 单据因页面承载于 iframe、浏览器自动化工具无法进入 iframe 内部做真实输入，但代码实现与采购入库单完全一致，功能等价。 |

| BUG-2026-08-15-003 | 物料新增/编辑弹窗的规格输入框仅占四列布局的一列，长规格录入与核对困难 | 新增与编辑弹窗中的规格字段改为半行宽，保留 `maxlength=100` 与后端长度校验；回归测试 `tests/verify_app_py_split_material.py::TestMaterialRegister::test_specification_fields_use_wide_layout` |
| BUG-2026-08-15-004 | 采购入库单、领料单反提交回草稿后，详情页缺少明细编辑入口，无法修正数量、单价、合同编号、工程名称等 | 草稿详情增加“编辑明细”；编辑页仅允许 `pending` 状态加载，预填表头与行级字段，采购来源行 ID 保持不丢失。已完成单直接访问编辑 URL 返回 409；完成与反提交库存动作未放开。回归 `tests/verify_app_py_split_in_order.py`、`tests/verify_app_py_split_out_order.py`、`tests/verify_bug_2026_08_04_015_received_quantity_double_count.py` |
| BUG-2026-08-15-005 | 手机端发送采购入库单、领料单或物料标签打印任务时，`WMS.api.post` 缺少 `Content-Type: application/json`，后端 `request.get_json()` 读取空请求体并返回 400；桌面端任务完成/失败回写仅登录即可调用 | `app/static/js/api.js` 为非 FormData 请求自动设置 JSON Content-Type；`print_queue_complete`/`print_queue_fail` 限制为 admin/warehouse；`tests/test_print_queue.py` 覆盖三种任务、JSON 请求契约与非仓库回写拒绝。已推送 `main` |

## 未修复/待处理

| 编号 | 标题 | 现状与绕过方案 |
|------|------|---------------|
| BUG-2026-07-31-002 | `main` 分支未设置 protected branch，开发者可直接 push 绕过 PR + review | 尝试 `gh api -X PUT repos/SIX2090/wms/branches/main/protection` 设置（要求 1 approving review + strict status checks + linear history + no force push + 解决所有 conversation），但 GitHub 返回 `403 Resource not accessible by integration`：当前 GH_TOKEN 属于 GitHub App（client_id `Iv23liZK8tzQx0m4bCRd`），按 GitHub 平台硬限制，GitHub App token 没有 `Administration: write` 权限修改 branch protection。**绕过方案**：仓库 owner（`SIX2090`）在 GitHub Web UI → Settings → Branches → Add rule for `main` 手动开启 "Require a pull request before merging" + "Require approvals: 1" + "Require status checks: lint-and-test" + "Require linear history" + "Do not allow force pushes" + "Do not allow deletions"。或重新生成 fine-grained PAT 时勾选 "Administration: write"（含 "Branch protection rules"），用该 PAT 跑本任务第 3 步的 PUT 命令。已尝试的 API：GET/PUT/PATCH `/branches/main/protection[/*]` 全部 `403`。本地防线：`.githooks/pre-push` 钩子会拒绝向非 `main` 分支推送，未来启用 Web 端保护后服务端兜底。<br>**2026-07-31 二次验证（commit `aa253a1d`）**：用户在 Web UI 已启用 `required_status_checks: lint-and-test`（`enforcement_level: non_admins`），但 `protected: false` 表明 "Require a pull request before merging" / 阻止直接 push **未启用**。直接 push 一个空行 commit 到 main（commit `e6dcb62e`）→ GitHub 服务端 200 接受 → 已用 `aa253a1d` revert。**结论：保护仅部分生效（status check），直接 push 仍可绕过 PR + review，BUG 维持未修复状态**。Web UI 需补勾 "Require a pull request before merging"（关键项）+ "Do not allow force pushes"。<br>**2026-07-31 三次验证（实测 commit `1acfee0e` 已 force-push 撤销，main 回到 `c149e4b4`，无遗留）**：用户反馈 Web UI 已勾选全部 9 项规则（含 Require pull request / Do not allow force pushes 等），但 UI 底部仍提示 "Not enforced"（需升级 Team/Enterprise 或公开仓库）。实测三步：①`gh api repos/SIX2090/wms/branches/main` 读到 `protection: { enabled: true, required_status_checks: { enforcement_level: "non_admins", contexts: ["lint-and-test"] } }`——**API 仅存 1 项 status check，其余 8 项未持久化**；②`echo "" >> README.md && git commit && git push origin main` 成功（`c149e4b4..1acfee0e main -> main`），无 GH006 报错；③`git reset --hard c149e4b4 && git push -f origin main` 也成功（`+ 1acfee0e...c149e4b4 main -> main (forced update)`）。**结论确认**：GitHub 免费版 + 私有仓库下 protected branch 是"摆设"——UI 允许勾选但 GET 不到、push 不被拦。`enforcement_level: non_admins` 正是平台标记 admin 不受 status check 约束的字段。**3 条解决路径（按优先级）**：①升级 GitHub Team（$4/user/月）→ 私有仓库保护规则强制执行；②仓库转 public（损失代码私有性）；③接受现状，依赖 `.githooks/pre-commit` + `.githooks/pre-push` + `lint-and-test` CI 三道兜底（当前已具备）。**当前采用路径 ③**，服务端保护作为深度防御待 GitHub 升级后启用。 |

### AUDIT-2026-08-16 全面审计登记（待修复，2026-08-16 五路并行审计：库存核心/状态机/安全/仓库合规/AI与测试）

| 编号 | 标题 | 现状与绕过方案 |
|------|------|---------------|
| BUG-2026-08-16-001 | [P0] 委外发料/收货 6 个入口不写 LocationInventory，且 SubcontractIssue/SubcontractReceive 模型无 location 列，开启库位管理时总账与库位账永久背离（委外库存变动在仓库级查询/报表/移动端全部"消失"） | **已修复（2026-08-16，commit 本次提交）**：`SubcontractIssue`/`SubcontractReceive` 增加 `location` 列（app.py 模型 + auto_migrate_database 补列）；6 个库存入口全接 `update_location_inventory`——quick_issue/quick_receive（保存 location、开启库位管理缺库位拒绝）、add_issue/add_receive（解析保存 location、缺库位拒绝）、complete_issue/complete_receive 与 revert_issue/revert_receive（总账变动后同步库位账，历史单据无 location 以仓库名占位，失败整体回滚）；前端 `subcontract_issue.html`/`subcontract_receive.html` 新增弹窗与 `subcontract_detail.html` 快速发料/收货弹窗补库位字段（location_management_enabled 条件显隐 + required + JS 必填拦截）。回归：`tests/test_bug_2026_08_16_001_subcontract_location_inventory.py` 10/10 PASSED（列存在/快速发料扣账/快速收货写账/缺库位拒绝×3/完成+反提交对称×2/历史占位/关闭不写账），存量委外与 002/003 回归 21/21 PASSED |
| BUG-2026-08-16-002 | [P0] 期初库存只改 Material.stock 不写 LocationInventory，开启库位管理时建账即错账（库存查询显示 0、出库被库位余额不足拦截） | **已修复（2026-08-16，commit 本次提交）**：`OpeningStock` 增加 `location` 列（app.py 模型 + auto_migrate_database 补列）；`_opening_stock_payload_from_request` 解析 location；`_apply_opening_stock_balance` 在开启库位管理时同步 `update_location_inventory`（未填库位以仓库名占位；历史缺行编辑下调时按旧数量回填基线再扣差额，行值==新数量）；add/edit/batch_save 三入口传 location，库位账失败 ValueError→400 整体回滚；详情 JSON 返回 location。回归：`tests/test_bug_2026_08_16_002_opening_stock_location_inventory.py` 7/7 PASSED（新增写账/占位行/编辑扣减/历史回填/关闭不写/批量保存），全量 `pytest tests/` 463/463 PASSED |
| BUG-2026-08-16-003 | [P0] 新增物料带初始库存走 ORM 直改 `Material(stock=initial_stock)`，不写库位账不写 OpeningStock，且物料与流水两次独立 commit 非原子（中间失败留"库存已涨流水缺失"） | **已修复（2026-08-16，commit 本次提交）**：`app/routes/material.py` add_material 重构为单事务——flush 后同 commit 内完成 MaterialImage/审计流水/库位账写入，消除双 commit 窗口；开启库位管理且有默认仓库时初始库存同步 `update_location_inventory`（归默认仓库，仓库占位行），失败整体回滚不落库。回归：`tests/test_bug_2026_08_16_003_material_initial_stock_location.py` 4/4 PASSED（写库位账/关闭跳过/无默认仓兼容/失败回滚原子性）+ 存量 `verify_bug_2026_08_04_009` 3/3 PASSED + 全量 `pytest tests/` 467/467 PASSED |
| BUG-2026-08-16-004 | [P1] `batch_complete_out_order` 批量完成跳过销售出库全部专属校验（无 `validate_sales_outbound_warehouse`、无 `sales_outbound_remaining_check` 超发拦截、无 `is_future_date`、无 `_check_out_order_anomalies`），单据完成被拦的超发草稿批量放行，`shipped_quantity` 回写无上限 clamp | **已修复（2026-08-16，commit 本次提交）**：`app/routes/out_order.py` batch_complete_out_order 写锁后补齐 4 项与单据版 complete_out_order 一致的校验（失败单计 skipped 不阻断整批）：①`is_future_date` 未来日期拒绝；②销售出库 `validate_sales_outbound_warehouse`（仓库 active + 与来源销售订单一致）；③`sales_outbound_remaining_check` 超发拦截（超发不再放行、shipped_quantity 不再无 clamp 回写）；④`_check_out_order_anomalies` 异常单一律跳过转人工单独审核（批量无 force 交互通道）。回归：`tests/test_bug_2026_08_16_004_batch_complete_out_guards.py` 5/5 PASSED（超发拒/未来日期拒/仓库不一致拒/合法单完成且回写 shipped/混合批次跳坏完成好），存量 out_order 相关 11/11 PASSED |
| BUG-2026-08-16-005 | [P1] `batch_complete_in_order` 批量完成跳过 `validate_purchase_in_order_source`（入库须关联 PO 开关）、`_check_in_order_anomalies`、`is_future_date`，超收校验不再复核 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/in_order.py` batch_complete_in_order 写锁后补齐 4 项与单据版 complete_in_order 一致的校验（失败单计 skipped 不阻断整批）：①`is_future_date` 未来日期拒绝；②`validate_purchase_in_order_source` 来源 PO 校验（开关开启时须关联采购订单）；③超收复核 `validate_purchase_receive_quantity` 按行级来源复核（草稿期间来源 PO 被其他单推进后超收草稿不再放行）；④`_check_in_order_anomalies` 异常单一律跳过转人工单独审核（批量无 force 交互通道）。回归：`tests/test_bug_2026_08_16_005_batch_complete_in_guards.py` 4/4 PASSED（未来日期拒/超收拒/合法单完成入账/混合批次跳坏完成好），存量 pur_audit_003 + batch_out 9/9 PASSED |
| BUG-2026-08-16-006 | [P1] `add_stock`/`deduct_stock_atomic` 写 StockTransaction 不填 location（仅 transfer 写），而 `get_warehouse_stock_quantities` 关库位管理分支按 `location IN (仓库名/编码)` 聚合流水——NULL 不入列，多仓库+关库位管理时仓库级库存恒为 0 | **已修复（2026-08-16，commit 本次提交）**：`add_stock`/`deduct_stock_atomic` 新增 `warehouse` 参数，经 `_stock_location_from_warehouse` 把仓库名/编码写入流水 location；`deduct_stock` 包装函数同步转发 warehouse。覆盖全部库存变动入口（in_order 完成/改单/反提交/批量、out_order 完成/反提交/批量、subcontract 收发货/完成/反提交、adjustment 调增调减/反提交、requisition 完成/撤销、after_sale_out 完成/反提交、check 反提交、native_api/mobile 扫码入库）共 30 处调用传入 warehouse。向后兼容：不传 warehouse 时仍写 NULL（存量行为不变）。回归：`tests/test_bug_2026_08_16_006_stock_transaction_warehouse_location.py` 2/2 PASSED（A/B 双仓入库出库聚合不串仓、流水 location 已写入仓库名）+ 存量 001/002/003/pur_audit_003 25/25 PASSED |
| BUG-2026-08-16-007 | [P1] `stock_query` 页面把 `material.stock` 全局总数当仓库级库存展示，LocationInventory 行不按所选仓库过滤；同文件 `api_query_search` 已用仓库级口径，两口径不一致 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/stock_query.py` 库存改为 `get_warehouse_stock_quantities` 仓库级口径（与 api_query_search 一致）；开启库位管理时库位行按 `warehouse_id` 过滤（兼容历史 `warehouse_id IS NULL` 且 location==仓库名的旧行）；`app/templates/stock_query.html` 库存列改用 `warehouse_stock_map`（%0.2f），预警行/加粗同步改用仓库级库存。回归：`tests/test_bug_2026_08_16_007_stock_query_warehouse_filter.py` 2/2 PASSED（关库位 A/B 双仓独立展示、开库位库位行按仓库过滤不串仓）+ 存量 verify_app_py_split_stock_query + inv_audit_004 12/12 PASSED |
| BUG-2026-08-16-008 | [P1] `complete_transfer` 关库位管理分支只写双向流水不校验源仓库库存，可从 0 库存仓库"空手套白狼"调出任意数量；`revert_transfer` 同样无校验 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/transfer.py` complete_transfer 关库位管理（OFF）分支，先按 `transfer.from_warehouse` 解析源仓库，用 `get_warehouse_stock_quantities`（依赖 BUG-006 修复后的仓库级口径）聚合源仓库库存，明细调出数量超过可用库存时拒绝完成（提示物号/需要/可用）；找不到仓库主数据时跳过（无法聚合，保持向后兼容）。回归：`tests/test_bug_2026_08_16_008_transfer_off_branch_source_stock.py` 3/3 PASSED（A9 同名门禁 + 源仓不足拒[T1：全局 10 但源仓 A 空，调出 10 被拒且单据仍 pending] + 源仓充足成功[T2]）+ 存量 test_p1_transfer_location_required 5/5 PASSED（8 合计全过） |
| BUG-2026-08-16-009 | [P1] `revert_in_order`/`update_completed_in_order` 库存充足校验用 `check_stock_sufficient`→`material.stock` 全局口径，多仓库下 A 仓库存可掩护 B 仓入库单反提交，OFF 模式无库位原子防护兜底，B 仓账面被打穿 | **已修复（2026-08-16，commit 本次提交）**：`app/routes/in_order.py` 三处库存充足校验改 `get_warehouse_stock_quantities` 仓库级口径（OFF 按流水 location 聚合、ON 按 LocationInventory）——①`revert_in_order` 反提交预检（按 order.warehouse 解析仓库，明细数量超可用库存拒绝）；②`update_completed_in_order` 删除明细回退校验；③同函数修改明细减量（qty_diff<0）校验，并把 `warehouse_stock` 在仓库确认后统一计算复用。另 `revert_in_order` 的 `deduct_stock` 补传 `warehouse=order.warehouse`，使 OFF 模式反提交流水正确按仓库回退（与 BUG-006 口径一致）。回归：`tests/test_bug_2026_08_16_009_revert_in_order_warehouse_level.py` 3/3 PASSED（A9 门禁 + T1 全局 100 但 B 仓 0 反提交被拒且单据仍 completed + T2 B 仓充足反提交成功转 pending）+ 存量 batch_complete_in + transfer_off 10 合计全过 |
| BUG-2026-08-16-010 | [P1] `ApiToken.token` 与 `PrintWorkstation.auth_token` 明文入库、明文等值匹配，库文件/备份外泄即全量移动端账号与工作站接管（7 天有效期）；与 bootstrap 密码治理标准不一致 | **暂缓（2026-08-16 用户决定不处理）**：风险评估已记录，明文令牌 7 天有效期 + 令牌泄露仅影响移动端/工作站短期接管。后续条件：接入真实生产环境且库文件/备份外泄风险升高时，再按"sha256 哈希存储 + 一次性返显 + 存量平滑迁移"方案处理 |
| BUG-2026-08-16-011 | [P1] `validate_password_strength` 仅校验长度≥6，无复杂度/弱口令黑名单/与用户名相同检查（`123456`、`admin123` 均合法）；新增用户、改密、管理员重置共用该函数 | **暂缓（2026-08-16 用户决定不处理）**：口令强度属安全加固项，存量用户改密时生效会中断现有使用习惯。后续条件：安全合规要求（等保/审计）提出强制口令策略时再处理（长度≥8 + 至少两类复杂度 + 弱口令黑名单 + 禁与用户名相同） |
| BUG-2026-08-16-012 | [P1] `OperationAudit` 结构化审计表实质未启用：唯一写入函数 `log_audit` 全库 0 调用，所有操作仅 OperationLog 纯文本，删物料/改单价/反提交/改角色无 before/after 留痕，事后无法取证 | **已修复（2026-08-16，A1/A2/A3 三原子提交）**：高危操作接入 `log_audit` 写结构化 old_data/new_data（展示页 /operation_audit 早已就绪，仅补写入）。**A1 commit `7904b6d`**：物料域 add_material(new_data)/edit_material(old+new 含单价)/delete_material(old 快照)；**A2 commit `e3f640a`**：用户域 edit_user(角色 role old/new)/update_user_status(启禁用)/reset_user_password(密码重置事件，不落明文)；**A3 commit `b1d11e4`**：单据域 in/out/transfer 的 revert(completed→pending) 与 delete(草稿删除快照)。均业务 commit 后追加，不破坏外层事务。回归：`tests/test_bug_2026_08_16_012_material_audit.py`(4/4)、`_user_audit.py`(4/4)、`_document_audit.py`(5/5) 全 PASSED；存量 material/transfer/revert 相关 16 例无回归 |
| BUG-2026-08-16-013 | [P1] AI 路由 30+ 端点（`app/ai/routes.py`、`app/ai/v2_routes.py`）仅 `@login_required` 无角色门禁，viewer/user 只读角色可调用 AI 助手/流式聊天/草稿校验，可刷 LLM 计费 | **已修复（2026-08-16，commit `431a40d`）**：`ai/policies.py` 新增 `AI_USE_ROLES` 白名单（admin/warehouse/purchase/production/sales，排除 viewer/user）与 `require_ai_role` 装饰器；应用于消耗 LLM 计费的端点 `routes.py`（`/chat/stream`、`/warehouse_assistant`、`/draft_check`）与 `v2_routes.py`（`/llm/chat`、`/llm/intent`）。回归：`tests/test_bug_2026_08_16_013_ai_role_gate.py`(22/22) viewer/user 调 LLM 端点 403、warehouse/admin 放行、对话历史/工具等辅助端点不受影响 |
| BUG-2026-08-16-014 | [P1] 售后出库新增无库位校验（无 location_management_enabled 校验、无 assert_warehouse_active），模板 `after_sale_out_add.html` 无库位字段，完成路由却有库位门禁——开库位管理时草稿无法录入库位卡死不能完成，工作流断裂 | **已修复（2026-08-16，commit `54d1d68`）**：`add_after_sale_out_order` 补 `assert_warehouse_active` + 库位必填（`location_management_enabled` 时拒无库位草稿）；`after_sale_out_add.html` 按 `location_management_enabled` 条件渲染库位字段（required）；`after_sale_out_list`/detail 传入 `location_management_enabled`。回归：`tests/test_bug_2026_08_16_014_after_sale_out_add_location.py`(3/3) 新增拒绝/通过、启用仓库校验；存量 `test_p1_after_sale_out_location_required.py` 完成路由兜底门禁用例改为 DB 直插无库位草稿验证（5/5）全 PASSED，开库位管理新增→完成全流程可走通 |
| BUG-2026-08-16-015 | [P1] 调拨新建弹窗 `transfer.html` 只有 from_location/to_location 两个 select 且选项是仓库名，无独立库位字段；后端把 from_location 当仓库、`complete_transfer` 又把它当库位维度，库位语义错误，库位必填校验形同虚设 | **已修复（2026-08-16，commit `0916270`）**：后端 `add_transfer`/`save_transfer_table` 已具备 from_warehouse/to_warehouse（仓库）与 from_location/to_location（库位）双字段语义；根因在模板。`transfer.html` addModal 仓库 select 改 `from_warehouse`/`to_warehouse`，新增 `from_location`/`to_location` 库位输入（`location_management_enabled` 条件显隐）；`transfer_list` 传入 `location_management_enabled`。回归：`tests/test_bug_2026_08_16_015_transfer_double_field.py`(4/4) 开库位管理仓库/库位分别落库、缺库位被拒、关闭时 from_location 回退仓库名 |
| BUG-2026-08-16-016 | [P1] 领料单新增弹窗 `requisition.html` 无仓库 select 无库位 input，只能靠默认仓库回退（无默认仓库直接被拒，用户无法手选）；`complete_requisition` 要求库位必填但无录入入口 | **已修复（2026-08-16，commit `b9db3b6`）**：`requisition.html` 新增弹窗补仓库 select（required+默认选中）与条件库位字段；`add_requisition`/`save_requisition_table` 补 `assert_warehouse_active` + 库位必填；`requisition_list` 传入 `warehouses`/`default_warehouse`/`location_management_enabled`。回归：`tests/test_bug_2026_08_16_016_requisition_warehouse_location.py`(6/6) 仓库/库位落库、缺库位被拒、非启用仓库被拒、save_table 拒绝/通过；存量 `test_p1_requisition_location_required.py` 完成兜底门禁用例改 DB 直插验证（5/5）全 PASSED |
| BUG-2026-08-16-017 | [P1] `tests/` 下 100 个 verify_*.py（约 17,012 行，含 60+ BUG 回归与 38 个 app.py 拆分校验）不被 pytest 默认收集也不在任何 CI workflow 执行——回归防线形同虚设，"有测试"≠"在 CI 跑过" | **已修复（2026-08-16，commit 本次提交）**：`.github/workflows/ci.yml` 新增 "Verify regression - verify_*.py" 必过步骤，逐文件独立进程执行全部 108 个 verify_*.py。技术要点：①弃用 `python_files=["verify_*.py"]` 统一收集方案——多文件同进程混跑时 app 模块单例与 db.drop_all 内存库守卫互斥，实测 169 例环境性假失败（`Refusing db.drop_all`）+20 例 `Table 'user' already defined`；②逐文件独立进程已本地验证 99/108 文件全过（631 用例，约 3 分钟）；③9 个文件 15 例历史真实失败显式列入 ci.yml KNOWN-FAILURES 排除清单（带 F1~F9 注释），修复后从清单移除。遗留子任务（每项含失败原因分析与修复）：F1 `verify_app_py_split_subcontract.py`（1 例）、F2 `verify_bug_2026_08_02_018_report.py`（2 例）、F3 `verify_bug_2026_08_02_021_in_order_contract.py`（1 例）、F4 `verify_bug_2026_08_04_010_material_api_all_response_key.py`（1 例）、F5 `verify_bug_2026_08_09_003_takepicture_permission_and_path.py`（2 例，Android 端）、F6 `verify_bug_2026_08_13_002_scan_submit_bearer.py`（1 例）、F7 `verify_bug_P15_P16_P21.py`（1 例）、F8 `verify_mobile_asr_route.py`（1 例）、F9 `verify_mobile_recognize_material_api.py`（5 例，API 响应结构漂移 KeyError）。验证：YAML 解析通过、排除逻辑 99 执行/9 跳过、逐文件全量 99/99 PASSED |
| BUG-2026-08-16-018 | [P1] CI 无代码覆盖率（无 pytest-cov）无安全扫描（pip-audit/bandit/gitleaks 全无），依赖漏洞与密钥泄露无门禁，覆盖盲区不可量化 | **已修复（2026-08-16，commit `5aa764d`）**：`.github/workflows/ci.yml` 增加 `pytest --cov`（阈值宽松起步，先可见后收紧）+ pip-audit（依赖漏洞扫描）+ gitleaks（密钥泄露扫描）门禁；`app/requirements.txt` 引入 pytest-cov。回归：CI 产出覆盖率与依赖扫描报告，`gitleaks-action` 免费版无需 GITLEAKS_LICENSE env（已移除该引用） |
| BUG-2026-08-16-019 | [P2] 安全加固批量：SystemSetting 明文存 LLM/ASR API Key、`/api/login`（`native_api.py:251`）与 `report.py:203` 把 str(exc) 返回客户端、`providers.redacted()` 定义后 0 调用、`sales_out_draft` 能力声明含永不可用 sales 角色（`policies.py:9`）、OperationAudit IP 直采 XFF 可伪造（`app.py:2995`）、wechat_helper_token 文件权限未显式设置 | **已修复（2026-08-16，commit `9f645b5`）**：API Key 对称加密存储（密钥来自环境变量）；异常统一通用文案 + `logger.exception`；前端回显统一脱敏；声明表与 effective 角色对齐（移除 sales 或加注释表）；审计 IP 复用 `get_request_ip` 信任代理逻辑；token 文件 chmod 600 |
326→| BUG-2026-08-16-020 | [P2] 并发与一致性批量：`delete_purchase_order`/`batch_delete` 无写锁（引用孤儿竞态，`purchase_order.py:895-961`）、`revert_transfer` 调入库位非原子扣减（`transfer.py:528-536`）、`revert_check` 库存回退分支死代码（查 check_in/check_out 流水但全库无写入，`check.py:316-360`）、`native_api` outbound 无条件 `update_location_inventory`（关库位管理时客户端传 location 造隐形账，`native_api.py:397-405`）、`generate_order_no` SQLite 并发撞号（约束兜底但用户吃 500）、委外主单状态变更与 quick_receive 无锁（`subcontract.py:362-917`） | **已修复（2026-08-16，commit `2b518a5`）**：PO 删除接 `_acquire_order_write_lock` 锁后复核 status+引用；revert 的 to_location 扣减改 `deduct_location_inventory_atomic`；删除 revert_check 死分支或接通真实路径；native_api 出库库位写加 location_management_enabled 守卫；取号纳入写锁事务或 SQLite 分支撞号重试换号；委外写路由补锁。回归：`tests/test_bug_2026_08_16_020_concurrency_consistency.py` 取号互斥锁下单号唯一等用例通过 |
| BUG-2026-08-16-021 | [P2] 前端/流程/扩展批量：`adjustment_add.html:460` 行级库位无 required（条件渲染）、requisition/transfer/check/after_sale_out/`update_completed_in_order` 各 complete 路由缺 `assert_warehouse_active` 复核、`StockTransaction.location` 无索引（OFF 分支聚合全表扫）、台账一致性默认 gradual 不阻断虚假完成（`verify_ai_ledger_consistency.py:120`）、Android CI 仅 assembleDebug 无 lint/单测、装饰器顺序 `@require_role` 在 `@login_required` 之上遍布 user/backup/print 模块（靠自检兜底的脆弱结构）、批次/序列号/效期/单位换算模型缺失（业务天花板） | **已修复（2026-08-16，commit `b919726`/`4384437`/`f49d698`）**：①前端流程（`b919726`）adjustment_add.html 行级库位按 `location_management_enabled` 条件渲染 required（adjustment.py 两处 render 补传上下文）；complete 路由补 `assert_warehouse_active` 复核（after_sale_out/非销售 out_order/transfer 调出+调入）；`StockTransaction.location` 加单列索引 `idx_stock_txn_location` 加速 OFF 分支聚合。②CI（`4384437`）verify.yml 台账校验切 `AI_LEDGER_ENFORCE=strict` 阻断虚假完成；android-build.yml 补 lintDebug + testDebugUnitTest 门禁。③装饰器顺序（`f49d698`）print_routing/print_queue/user_auth 统一 `@login_required` 在上（backup 本就正确未改）。回归：adjustment/transfer/out_order/after_sale_out/print 相关 53 例全 PASSED。批次/序列号/效期/单位换算模型单独立项规划（不并入本批） |
| BUG-2026-08-16-022 | [P1] Android 原生端手工添加物料与扫码共用提交后打印能力，但 APK `versionCode`/`versionName` 未随该能力发布递增；运营下载页只提供已部署 signed release，用户可能持续安装旧包并看不到提交成功后的“打印单据”入口 | **已修复（2026-08-16，commits `24e2c65`/`48613bb`/`8871b85`/`450facd`/`ac78b1a`/`8d68409`）**：Android 版本递增至 `3.1.0 (4)`，首页入口明确为手工/扫码多物料入库、出库；“我的”页展示版本号，现场可确认已安装更新包。手工添加与扫码共用 `ScanViewModel.submitInbound/submitOutbound`，成功后立即弹出“单据提交成功”打印确认（打印单据/稍后打印），并保留顶部横幅作为备用入口；创建远程打印队列任务。GitHub Actions `Android APK Build #31950304684` 与 `#31951577585` 已全绿：assembleDebug、lintDebug、testDebugUnitTest、上传 `app-debug` artifact 均通过；CameraX Image/Camera2 实验 API opt-in 与打印确认类型导入已修复。回归：`test_print_auto_after_scan.py` + `test_print_queue.py` 34/34 PASSED。发布边界：本次 GitHub 输出为 debug APK artifact；部署环境仍须使用现有 keystore 构建签名 release APK 并替换 `/mobile/app` 所服务的 APK |
| BUG-2026-08-16-023 | [P1] `.testvenv/` 本地 Python 虚拟环境的 7,815 个文件被误提交，GitHub `actions/setup-python` 的 pip 缓存扫描 `.testvenv/bin/python` 时在 runner 上触发 `EACCES`，导致 WMS CI 在依赖安装前失败；另有安全加密后测试仍断言密钥明文、调拨验证夹具缺少调出仓库库存、gitleaks 对测试假值误报、售后出库新增页缺 `location_management_enabled` 导入导致 500 | **已修复（2026-08-16，commits `cd9002e`/`afd0d1a`/`36824ac`/`1c3a015`/`42edeff`）**：从 Git 索引移除 `.testvenv/` 并加入 `.gitignore`（本地目录保留）；测试改为验证密钥 `enc:` 加密存储；调拨夹具写入材料仓库存流水；gitleaks 仅精确排除 9 个已核验的前端存储键/测试假值文件；补齐售后出库新增页配置函数导入并新增 GET 页面回归。回归：打印链路 34/34、售后出库 17/17、调拨 7/7 PASSED；GitHub WMS CI `#31953443966` 全绿，涵盖 Lint、全量单测与覆盖率、pip-audit、gitleaks、verify_*.py、121 项 smoke、hooks 可复现检查 |
| BUG-2026-08-17-001 | 部署兜底脚本 `fix_db_columns.py` 缺少 `in_order`/`out_order` 的 `location` 与 `in_order.auto_push_requisition` 补列；`WMS_NO_DB_TOUCH=1` 时 `app.auto_migrate_database()` 被跳过，旧库 `in_order` 永远缺 `location` 列，首页 `index()` 报 `no such column: in_order.location`，服务不可用 | `app/fix_db_columns.py` 新增 `_table_exists` 守卫，补 `in_order.location`/`in_order.auto_push_requisition`/`out_order.location` 三列（与 `app/app.py auto_migrate_database()` 的 ALTER 语句一致，幂等）；`tests/test_fix_db_columns.py` 新增对应回归用例，`start_wms_offline.bat` 重启即自动补列 |
| BUG-2026-08-17-002 | ①关库位管理+多仓库时，所有仓库级报表（库存报表、库存台账、仓库月报）使用 `Material.stock` 全局数而非按仓库聚合，导致"有库存查不出来"；`add_stock`/`deduct_stock_atomic` 不写 `StockTransaction.location`，`get_warehouse_stock_quantities` 关库位管理分支按 `location` 聚合时 NULL 不入列；另有 5 处调用 `deduct_stock` 未传 `warehouse` 参数。②老数据（入库单 `warehouse` 为空或仓库解析不到）单张反提交/编辑已完成单删明细时，`get_warehouse_stock_quantities(wh_obj)` 取不到值，`warehouse_stock={}` 恒判库存不足，即使全局有库存也报"库存不足，不能反提交"，单据卡在 completed 无法反提交 | **已修复（2026-08-17）**：①`get_warehouse_stock_quantities` 新增单仓库全局回退；`_collect_inventory_rows`/`_build_warehouse_monthly_report` 改用 `get_warehouse_stock_quantities`；`_build_report_filters` 补充 `warehouse_code`；`_collect_ledger_rows`/`_build_warehouse_monthly_report` 过滤器匹配仓库名/编码；`add_stock`/`deduct_stock_atomic` 通过 `_stock_location_from_warehouse` 写入 `location`；补 5 处 `deduct_stock` 漏传 `warehouse`；`scripts/backfill_stock_txn_location.py` 回填 800 条历史流水；`tests/test_bug_2026_08_16_006_stock_transaction_warehouse_location.py` 5/5 PASSED；新增 `tests/test_bug_2026_08_17_000_warehouse_report_fallback.py` 2/2 PASSED；真实库验证 4 仓库全局 vs 仓库汇总一致（0 差异）。②`app/routes/in_order.py` `revert_in_order` 与 `update_completed_in_order` 库存充足校验：`wh_obj` 解析失败时回退全局 `Material.stock` 口径（与 `batch_revert_in_order`/`deduct_stock` 一致），有仓库时仍走 BUG-2026-08-16-009 仓库级口径；`tests/test_bug_2026_08_17_002_revert_in_order_without_warehouse.py` 回归通过 |
| BUG-2026-08-17-003 | 采购订单执行、供应商采购汇总、物料采购汇总、采购价格分析和委外加工报表虽然要求选择仓库，但采购查询未按入库仓库归属过滤，委外查询未按 `SubcontractOrder.warehouse` 过滤，导致多仓库报表串仓 | **已修复（2026-08-17）**：采购订单明细以关联采购入库单仓库为归属口径，兼容 `InOrderItem.source_purchase_order_item_id` 与历史主表 `InOrder.source_purchase_order_id` 两种来源；尚未入库、无法确认仓库归属的采购订单不返回。委外加工报表按 `SubcontractOrder.warehouse` 严格过滤。新增 `tests/test_bug_2026_08_17_003_purchase_subcontract_warehouse_reports.py`，验证采购明细与委外主单在两仓间互不串数据（2/2 PASSED）。 |
| BUG-2026-08-17-004 | 数据仪表盘未提供仓库筛选，入库、出库、库存、库存金额、趋势、月度金额和库存排行全部使用全局数据，多仓库环境下报表口径串仓 | **已修复（2026-08-17）**：`/report/dashboard` 增加仓库选择，未选择且无默认仓库时不展示全局统计；`build_report_dashboard_context(warehouse)` 对入库/出库单、库存流水、库存排行、分类和金额趋势统一按仓库聚合；AI 解读及快捷入口携带当前仓库参数。新增 `tests/test_bug_2026_08_17_004_report_dashboard_warehouse_filter.py`，验证两仓的核心指标、库存金额和排行互不串仓（1/1 PASSED）。 |
| BUG-2026-08-22-001 | 打印物料标签的条码/二维码模糊难扫描：①`/api/qrcode` 生成二维码后用 `Image.LANCZOS` 重采样缩放，黑白模块边缘被抗锯齿抹灰，热敏打印出来发虚；②`/api/barcode` 默认 300 DPI + module_width=0.3mm 输出，短编码仅约 300px 宽，前端 CSS 拉伸到几十毫米宽打印时被放大 2~3 倍，条边模糊；③浏览器缩放 `<img>` 默认平滑插值进一步软化边缘 | **已修复（2026-08-22）**：`app/routes/label_barcode.py`——①`/api/qrcode` 改整数 box_size 输出（尺寸 = (modules+2*border) × box_size 整数倍、不小于请求 size），完全去掉 LANCZOS 重采样，全图仅纯黑/纯白；②`/api/barcode` 提升 600 DPI 输出（短编码 624×561px）。前端 4 个模板（print_label.html / print_batch_labels.html / label_preview.html / label_template_detail.html）条码/二维码图片加 `image-rendering: pixelated` 阻止浏览器平滑插值。回归：`tests/test_bug_2026_08_22_001_label_barcode_clarity.py`（5 场景：无灰色像素、尺寸模块整数倍、长内容仍锐利、条码高分辨率、接口 200）5/5 PASSED + 存量 `verify_app_py_split_label_barcode.py` 5/5 PASSED。注意：生产模式 Jinja 模板有缓存，需重启 WMS 服务后生效 |
| BUG-2026-08-23-001 | 手机 APP 扫码入库卡片只显示物料编码+数量，缺名称/品牌/规格。根因：卡片物料详情展示与 `addScanLine` 接口补全能力在 commit `17df8a1c`（2026-08-20）才合入 main，现场 APK 是 3.1.0 (versionCode 4，2026-08-16 构建) 不含该能力；且能力合入后未递增版本号（违反 BUG-2026-08-16-022 立的规矩），部署包永远不会更新到手机。排查确认扫码出库/手工入库/手工出库共用 ScanScreenBase + addScanLine，同一问题同一修复链路 | **已修复（2026-08-23）**：①`ScanScreenBase.kt`/`OpeningStockScreen.kt` 详情行字段顺序统一为 名称/品牌/规格，卡片构成 编码/名称/品牌/规格/数量 五要素；②`build.gradle.kts` versionCode 4→5、versionName 3.1.0→3.2.0，确保新 APK 可作为更新安装。回归：`tests/test_bug_2026_08_23_001_mobile_scan_card_material_details.py`（5 场景：五字段渲染、字段顺序、addScanLine 补全回填、四入口共用 addScanLine、版本号递增）5/5 PASSED + 存量 `verify_android_scan_viewmodel_isolation.py` 1/1 PASSED。**发布边界**：GitHub Actions 产出 debug APK artifact；部署环境仍须用现有 keystore 构建签名 release APK 并替换 `/mobile/app` 所服务的 APK，用户手机重新下载安装后生效 |
| BUG-2026-08-23-002 | 采购入库单「下推领料单」链路卡顿。实测「提交+自动下推领料单」（100 明细）一次请求发 835 条 SQL、耗时 350~407ms，其中 **202 条（24%）是对 `system_setting` 的重复查询**：`complete_in_order` 的入库明细循环内每条调 `location_management_enabled()`，自动下推循环内每条经 `deduct_stock_atomic → allow_negative_stock()` 各查一次开关。`get_system_setting` 无缓存，纯函数级开关每次调用都查库；生产机（杀软扫盘/低配 CPU/SQLite 写锁排队 busy_timeout 30~60s）上放大为秒级卡顿，且写事务持锁时间越长并发排队越明显。附带发现：`operation_log` 无 `(target_type, target_id)` 索引，所有单据详情页（含下推入口/落地页）全表扫描 | **已修复（2026-08-23）**：①`app/app.py` `get_system_setting` 增加请求级缓存（`flask.g`，随请求销毁无跨请求陈旧；非请求线程维持原行为），`set_system_setting` 同步更新缓存保证同请求写后读一致——缓存部分随打印代理提交 `bcacf3ab` 一并合入（当时工作区未提交改动被该提交整体带上，特此说明）；②`app/routes/in_order.py` `complete_in_order` 把 `location_management_enabled()` 提升到明细循环外（双保险）。回归：`tests/test_bug_2026_08_23_002_push_chain_settings_cache.py`（3 场景：同请求重复读同 key 只查一次库、set 后 get 缓存一致、提交+自动下推全流程 system_setting 查询从 2N+2≈42 降到 ≤6）3/3 PASSED；实测 100 明细提交+自动下推耗时 407ms→~290ms（-24%）。遗留：`operation_log` 目标索引另立任务跟进 |
| BUG-2026-08-23-003 | 内置打印代理在数据库 schema 未就绪（缺表/缺列：库文件丢失、路径错配或被外部重建）时，`_agent_loop` 兜底每 3 秒 `log.exception` 刷完整 traceback，启动窗口满屏红字被用户误报为"启动报错"；主服务实际不受影响，但刷屏掩盖真实日志且体验极差 | 已修复（2026-08-23）：`app/local_print_agent.py` 新增 `_is_schema_not_ready()` 识别 schema 等待态（`no such table`/`no such column`，大小写不敏感），`_agent_loop` 对该类异常改为：首条单行警告 → 转 `SCHEMA_WAIT_INTERVAL`(60s) 低频静默重试（不再刷 traceback）→ schema 就绪后自动恢复并打一条恢复日志；其他异常维持原行为。回归：`tests/test_local_print_agent.py` 新增 2 项（关键字识别与误判防护；两轮等待仅 1 条警告/0 traceback/恢复日志 1 条/sleep 间隔正确），套件 17/17 PASSED；沙箱复现「inventory.db 丢失 + WMS_NO_DB_TOUCH=1 启动」验证修复前每 3s 刷 traceback、修复后 0 traceback 仅 1 条警告且 /login 200 正常 |
| BUG-2026-08-23-004 | 端口 8080 被旧 WMS 实例占用时（现场最高频启动失败原因，Windows 报 WinError 10048），`run_server.py` 直接抛英文 `OSError: [Errno 98] Address already in use` 完整 traceback，操作员看不懂、不知道要先停旧实例或换端口 | 已修复（2026-08-23）：`app/run_server.py` 新增 `_is_addr_in_use_error()`（识别 Linux Errno 98 / Windows WinError 10048 / macOS 48 / 消息兜底匹配），`serve()` 捕获端口占用后输出中文指引（先跑 stop_wms_offline.bat 停旧实例，或 set WMS_PORT=18080 换端口）并以退出码 1 结束，不再抛裸 traceback；其他异常维持原行为。回归：`tests/test_run_server_port_hint.py`（6 场景识别/误判防护）PASSED；沙箱实测占用 8080 后启动输出中文指引 |
| BUG-2026-08-28-001 | 手机端「待确认」页（mobile_scan.html mode=confirm）调用 `/api/mobile/in_order/list`、`/api/mobile/out_order/list`（status=pending）时不传分页参数，后端默认只返回第 1 页 20 条（`MOBILE_API_PAGE_SIZE_DEFAULT=20`），且页面无翻页/加载更多入口——待确认的采购入库/领料草稿超过 20 条时，第 21 条起永远不可见，只能等前面的被确认掉才"顶上来"。后端接口本身已具备完整分页元数据（total/page/page_size/total_pages），属前端未消费 | **已修复（2026-08-28）**：`app/templates/mobile_scan.html` 待确认页增加分页加载——首屏仍为 20 条不变；入库/出库各自跟踪页码与 `total_pages`，超过 20 条时列表底部出现「已显示 X / Y 条，加载更多（还有 N 条）」按钮，点击后按 `&page=N` 追加取回下一页，全部加载完显示「已全部加载，共 N 条（入库 a / 出库 b）」；刷新/切换仓库/确认成功均重置回第 1 页；重复点击防抖。回归：`tests/test_bug_2026_08_28_001_mobile_confirm_pagination.py`（4 场景：入库 25 条草稿两页取回且不重叠并覆盖全部、出库 21 条两页元数据正确、模板包含 loadMore/&page=/total_pages/加载更多按钮、模板两列表统一走 fetchPendingPage 分页）4/4 PASSED + 存量 `test_mobile_scan_draft_flow.py` 22/22、`test_inv_audit_003_mobile_scan_warehouse_scope.py` 全部 PASSED。注意：生产模式 Jinja 模板有缓存，需重启 WMS 服务后生效 |

| BUG-2026-08-28-002 | 手机 APP「每日报表」（采购入库/领料单）明细列表只能看到 20 条，但汇总统计显示更多明细（如 58 明细）——汇总基于全集，明细列表被分页截断。根因：Android `WmsRepository.getDailyReport` 调 `/api/mobile/report/daily_detail` 时不传分页参数，后端默认只返回第 1 页 20 条明细，App 端不做翻页 | **已修复（2026-08-28）**：`WmsRepository.getDailyReport` 先取第 1 页，按响应 `totalPages` 逐页拉取 2..totalPages 并合并 `items`，返回全量明细（ViewModel/UI 无需改动，直接渲染合并后 items）。后端分页契约未变（仍返回 total/page/page_size/total_pages）。回归：`tests/verify_bug_2026_08_28_002_daily_report_all_pages.py`（6 场景：翻页循环、显式传页参、合并 items、copy 返回全量、后端分页元数据、API 接口页参）6/6 PASSED + 存量 3 个每日报表验证脚本 16/16 PASSED。**发布边界**：GitHub Actions 产出新 APK 并发布到 android-apk Release，用户手机重新下载安装后生效 |

| BUG-2026-09-02-001 | Android 原生端 `/api/stocktake` 扫码盘点取全局账面库存：INV-AUDIT-003 已把手机端（`mobile.py` `scan_submit`）盘点账面改成仓库级 `get_warehouse_stock_quantities()`，但 `native_api.py` 的 `/api/stocktake` 仍是 `parse_float_value(line.get('system_stock'), material.stock or 0)`，默认值回退全局 `Material.stock`。多仓库下把"全部仓库合计"当成单个仓库账面数，盘盈盘亏全部算错并生成错误调整草稿（例：A仓 60 + B仓 40 = 全局 100；盘 B仓实盘 38，正确差异 -2，实际算出 -62；盘 A仓实盘 60 本应无差异，却误判盘亏 -40 并生成调整单）。Android `ScanViewModel.kt:420` 显式传 `system_stock = null`，服务端必定落到该错误默认值 | **已修复（2026-09-02）**：`app/routes/native_api.py` `native_api_stocktake` 在循环外一次性取 `warehouse_stock_map = get_warehouse_stock_quantities(wh_obj)`（避免逐行重复聚合流水 N+1），行内默认值改为 `warehouse_stock_map.get(material.id) or 0`，对齐 `mobile.py` / INV-AUDIT-003；客户端显式传值时仍沿用（向后兼容）。回归：`tests/test_inv_audit_003_fix01_native_stocktake_warehouse_scope.py`（INV-AUDIT-003-FIX-01）3/3 PASSED（T1 多仓取 B仓账面 40、差异 -2；T2 A仓账实一致不生成调整单；T3 单仓 fallback 分支不受影响）。**发布边界**：后端改动，Android App 无需重新发版 |

| BUG-2026-09-02-002 | 扫码盘点重复调整无护栏：`_create_adjustment_drafts_from_check_scan` 幂等只按 `source_id=check.id` 判重（防同一盘点单重复生成），防不住跨盘点单对同一物料的正常重复盘点。调整草稿 pending 期间不动库存，同仓库同物料被重复扫码时每次都基于同一账面值各生成一张草稿——例：账面 40、实盘 38、差异 -2，两人先后扫码生成两张 -2 草稿，审核时都提交则库存被扣 4，全程无报错（静默双重计数），事后难追溯 | **已修复（2026-09-02）**：`app/app.py` `_create_adjustment_drafts_from_check_scan` 在幂等检查后新增跨盘点单护栏：同仓库同物料已存在未提交（pending）的扫码盘点调整草稿时拒绝生成，返回"以下物料已有待审核的扫码盘点差异（库存调整草稿）：物料(ADJ单号)。请先在「库存调整」中审核或删除这些草稿后再盘点，否则库存会被重复调整"。不误伤：不同物料、不同仓库、已提交草稿（库存已真实调整、账面已更新，再盘属合法二次盘点）均放行；历史无仓库字段的草稿按潜在冲突处理（宁拦不漏）。手机端（`mobile.py` scan_submit）与 Android（`native_api.py` /api/stocktake）共用此函数，一处拦截两端生效。回归：`tests/test_inv_guard_001_stocktake_duplicate_adjustment_guard.py` 5/5 PASSED（T1 重复盘点拒绝且提示含物料与单号/T2 不同物料放行/T3 已提交草稿放行/T4 跨仓库放行/T5 Web 手机端同受保护）。**发布边界**：后端改动，App 无需发版 |

| BUG-2026-09-02-003 | 扫码盘点单创建即 `status='completed'` 且全系统无任何作废/删除/回退端点（UI 无入口、API 无路由）：扫错物料/数量/仓库后无法纠正——要么放任错误调整草稿被审核提交（库存错调），要么在库存调整页手动删草稿（盘点单永远挂着 completed，审计无法区分哪次盘点被采纳、无法判责）。多人并行盘点下扫错概率高，纠错成本全转嫁给管理员 | **已修复（2026-09-02）**：`app/app.py` 新增 `_void_check_scan(check_id, operator_id)`（对齐 PC 盘点单 revert_check 的库存安全边界）：写锁重读状态（防并发重复作废）→ 关联调整单（source_type='check_scan'）任一已提交（completed）则拒绝作废并提示先反提交调整单（防"账已调、单作废"账实脱钩）→ 级联删除未提交（pending）调整草稿及明细（草稿不动库存，删除即恢复原状，且 INV-GUARD-001 护栏随之解除，同物料可重盘）→ 单据软标记 `status='void'` 留痕（不物理删除，保审计轨迹）→ log_operation 留痕，Bearer 请求（current_user 未认证）按 operator_id 补写 OperationLog。双端点：Web 手机端 `POST /mobile/api/check_scan/<id>/void`（会话/Bearer 双认证）+ Android `POST /api/stocktake/void`（按 check_no，Bearer）；`mobile_scan.html` 盘点历史条目新增撤销按钮（提交响应新增 `check_id` 字段）；模型 status 注释补 void 取值。回归：`tests/test_inv_revert_001_check_scan_void.py` 7/7 PASSED（T1 作废级联删草稿+留痕/T2 作废后重盘放行/T3 已提交草稿阻止作废/T4 重复作废拒绝/T5 无草稿直接作废/T6 Android 端点按 check_no 作废+404/T7 scan_submit 返回 check_id）；既有扫码盘点回归 19/19 PASSED（guard 5 + fix01 3 + mobile_scan 7 单独跑 + 仓库必填 4）；lint_wms_rules 0 违规；盘点页渲染 200 + 15 个 script 块 node 语法检查通过。**发布边界**：后端+Web 手机端页面改动，Android App 下版本接入撤销 UI 时后端已就绪 |

| BUG-2026-09-02-004 | PC 盘点单保存明细时账面基准漂移：`save_check_table` 全删重建明细（delete + 逐行 insert），且每行 `system_stock` 一律取**保存时点**的仓库级账面。多人轮流保存同一盘点单时，每次保存都把全部行的账面基准刷新到最新值——例：A 上午盘 M001（账面 60、实盘 58、差 -2），期间出库 30（账面变 30），B 下午补充一行再保存 → A 的行 `system_stock` 被刷成 30，差异从 -2 变 +28（方向都反了），差异含义被悄悄改变且无任何提示；complete 时据此生成的调整单必然错误 | **已修复（2026-09-02）**：`app/routes/check.py` `save_check_table` 行级 upsert 替换全删重建（冻结快照语义）：已有行（按物料匹配）保留 `system_stock` 冻结值、仅更新实盘/原因/差异；新增行取当前仓库级账面（该行首次录入时点基准）；提交集之外的旧行删除（全量提交语义不变）；首次写入明细时设置 `check.frozen_at`（冻结时点，一经设置不再变更）；保存路径加单据写锁（`_acquire_order_write_lock`）防并发保存互相覆盖。`update_check_item` 加单据写锁并写入 `counted_by`/`counted_at` 行级盘点归属。回归：`tests/test_inv_batch_001b_check_freeze_upsert.py` 6/6 PASSED（T1 首次保存设冻结时点/T2 期间出库后重存已有行账面冻结不变+新行取当前账面/T3 移除行删除且保留行冻结/T4 账面变化后 complete 按冻结口径生成调整单（57−60=-3 而非 57−30=+27）/T5 行级归属写入/T6 响应格式兼容+completed 单保存拒绝）；既有回归 62/62 PASSED（batch_a 3 + revert 7 + guard 5 + fix01 3 + 仓库必填 4 + warehouse_module 23 + audit_005 23 合计含重复计数，mobile_scan 7 单独跑）；lint_wms_rules 0 违规。**行为变化说明**：已有行的账面不再随重复保存刷新——这正是冻结快照的预期语义；如需以最新账面重录某行，先在表格中删除该行保存、再重新添加。**发布边界**：后端改动，App 无需发版 |
| BUG-2026-09-03-001 | Excel 导入盘点单不处理仓库（R6 排查 BUG-2026-09-02-001 同族遗漏点）：`/check/import`（`app/routes/check.py`）导入模板无"仓库"列，新建盘点单 `warehouse` 为空，`complete_check` 直接拒绝完结（"盘点单未指定仓库，无法完成"），Excel 盘点导入实际不可用；且导入明细缺省账面取全局 `Material.stock`（多仓库下把别仓库存算进本仓账面，差异错误）。注意：Web 端校验 `document_table_form.html` 添加/编辑盘点单仓库列由页面带出，此处仅导入链路缺口 | **已修复（2026-09-03）**：导入支持可选"仓库"列（仓库名或编码，`validate_inventory_warehouse` 校验存在且 active，编码自动解析为仓库名落库）；同一盘点单内多行仓库不一致时跳行并提示（防一单多仓串账）；Excel 未提供仓库列时带入系统默认仓库，无默认仓库则跳行并返回 warnings（不静默导入无主单据）；账面缺省改为该单据仓库的仓库级库存（`get_warehouse_stock_quantities`，每单缓存避免逐行重复聚合），Excel 显式填写的账面值仍以文件为准。修复后导入盘点单可正常 complete 生成调整草稿。回归 `tests/verify_bug_2026_09_03_001_check_import_warehouse.py` 6/6 PASSED（T1 带仓库列导入+可完结/T2 无仓库列带入默认仓/T3 无默认仓跳过并提示/T4 显式账面文件优先/T5 单内仓库不一致跳行/T6 仓库编码别名解析）；关联回归 71 项 PASSED（盘点批次全族 43 + 通用导入/Excel 路由 28）；lint_wms_rules 0 违规、verify_wms_bugs 静态回归通过 |
| BUG-2026-09-03-002 | 手机端物料接口"系统库存"展示固定取全局 Material.stock（R6 排查 BUG-2026-09-02-001 同族展示侧遗漏）：mobile_material_payload 的 stock 字段返回全仓合计，locations 不过滤仓库。手机盘点/查询/扫码结果页显示"当前库存"是 A+B 总和（全局 100 而 A 仓仅 10），误导盘点员判定短缺，且与后端实际使用的仓库级账面（INV-AUDIT-003-FIX-01）不一致 | **已修复（2026-09-03）**：`app/routes/mobile.py` `mobile_material_payload(material, warehouse=None)` 支持仓库上下文（对象或仓库名/编码，字符串经 validate_inventory_warehouse 解析）——传入仓库时 stock 取 get_warehouse_stock_quantities(该仓)、开启库位管理时 locations 只返回该仓库位（含历史 NULL warehouse_id 兼容行）；未传仓库维持全局展示向后兼容。接入点：`/mobile/api/material_lookup` 新增可选 warehouse 查询参数（含 matches 列表）、`/mobile/api/scan_submit` mode=query（请求带仓库时）及 in/out/check 成功响应均传当前仓库对象；`mobile_scan.html`（模板改动，**生产需重启 WMS 服务生效**） lookup 请求带当前仓库输入框值。语音识物等未选仓库场景不传（保持全局）。回归 `tests/verify_bug_2026_09_03_002_mobile_display_warehouse_stock.py` 7/7 PASSED（lookup 带仓 A=10/无仓全局 100 兼容/编码别名 WA/in 响应库存=A 仓 15 非全局 105/库位按仓过滤/无效仓回退）；mobile 相关全量子集 94 passed；渲染页 7 个内联 script node --check 全过；lint_wms_rules 0 违规 |
| BUG-2026-09-03-004 | Android 原生物料展示仍取全局库存（R6 排查 BUG-2026-09-02-001 同族展示侧遗漏之二）：Android 扫码/识别物料卡经 /api/material/info|search 拉取 api_material_payload，stock 固定返回全局 Material.stock，并按 stock 判"库存充足/不足"——多仓库下把 A+B 合计当本仓账面（A 仓仅 5 显示 100 → 误标充足）。手机网页端已于 BUG-2026-09-03-002 修复，原生走 /api/material/* 属另一消费点 | **已修复（2026-09-03）**：后端 `app/app.py` `api_material_payload(material, warehouse=None)` 支持仓库上下文（对象或仓库名/编码），命中时 stock 取 get_warehouse_stock_quantities(该仓)；`/api/material/search` 与 `/api/material/info` 接受可选 warehouse 参数透传；未传/无效仓库回退全局，`/api/material/all` 保持总库存口径不变。Android `WmsApiService` searchMaterial/materialInfo 加 @Query warehouse、`WmsRepository` 透传、`ScanViewModel` 搜索建议/扫码补全/按码查询三处带当前 selectedWarehouse.code（APP 需发版生效）。回归 `tests/verify_bug_2026_09_03_004_material_api_warehouse_stock.py` 5/5 PASSED（info 带仓 A=10/info 无仓全局/search 编码别名/all 总库存/无效仓回退）；lint 0 违规；全量 pytest 1121 passed 0 failed |
| BUG-2026-09-15-001 | 数据仪表盘「AI解读」按钮死按钮（R6 同 BUG-F02-09"调未定义函数"模式）：`report_dashboard.html` 的按钮（onclick=requestAiInsights()）无条件渲染，但函数与结果卡片 aiInsightsCard 都包在 `{% if stats %}` 内。未选仓库且无默认仓库时 stats=None——仪表盘默认落地态——按钮可见、函数缺失，点击抛 ReferenceError 无任何反应（实测 /report/dashboard 死按钮、?warehouse_id=1 正常） | **已修复（2026-09-15）**：按钮包进同一 `{% if stats %}` 条件，与函数同生共死；无数据时不渲染死按钮。回归 `tests/verify_bug_2026_09_15_001_dashboard_ai_insights_button.py` 3/3 PASSED（T1 stats=None 无按钮/T2 有仓库按钮+函数齐全/T3 按钮⟺函数不变量含无效仓库分支）。模板改动，**生产需重启 WMS 服务生效（R3）** |
| BUG-2026-09-15-002 | 采购入库单/销售出库单新增页「智能分享」按钮提示误导（文案由 BUG-2026-08-05-006 引入，R6 同根因排查）：`in_order_add.html`/`out_order_add.html` 提示"单据保存后可到列表页通过「智能分享」分享给协作伙伴"，但列表页根本没有智能分享按钮——真实能力是嵌入模式单据详情页全局操作栏的 smartShare()（需具体单据 ID 生成图片）+ 系统管理→微信分享（/wechat_share）定时自动推送配置。用户照提示去列表页找不到任何入口。R6 同类点：`opening_stock.html` 的智能分享按钮提示"期初库存已接入库存台账和仓库月报表"，与分享功能完全无关（文不对题） | **已修复（2026-09-15）**：两个新增页提示改为"单据保存后可在单据详情页使用「智能分享」生成图片；定时自动分享请到 系统管理 → 微信分享 配置"；期初库存页提示改为"微信定时自动分享请到 系统管理 → 微信分享 配置"。回归 `tests/verify_bug_2026_09_15_002_smart_share_hint.py` 5/5 PASSED（T1 误导文案移除/T2 新提示含详情页+智能分享+微信分享三要素/T3 期初库存文不对题提示修复/T4 base.html 系统管理菜单 /wechat_share 入口真实存在且分组正确/T5 全模板扫描无智能分享指列表页残留，防复发）。模板改动，**生产需重启 WMS 服务生效（R3）** |
| BUG-2026-09-15-003 | 期初库存页「导入导出模板」「粘贴导入」「批量导入」三个功能全是半成品，用户反馈"根本没法用"（R6 同根因 BUG-2026-08-05-006 的漏网消费点）：①「导入导出模板」按钮只调 `showImportHint()` 弹提示，不生成/下载模板文件；②「批量导入」跳 `/batch_import?type=opening_stock`，但批量导入页无 opening_stock 卡片，后端 `/opening_stock/import` 仅注册为"重定向 stub"（台账 P1-B 遗留），无真实导入逻辑，且仅挂 `@login_required` 缺角色校验（app.py:3446 已点名）；③「粘贴导入」用 `prompt()` 单行输入贴多行文本、纯前端 `rows.push()` 不入库、走原生 `fetch` | **已修复（2026-09-15）**：接入仓库既有集中式批量导入框架——新增 `GET /opening_stock/import/template`（openpyxl 生成含表头+示例行的 xlsx）；`/opening_stock/import` 由 stub 改为真实 Excel 导入（复用 `batch_save` 同一校验/入账路径 `_apply_opening_stock_balance`，示例行/缺列/不存在仓/重复键/停用仓正确跳过，仓库必填报错不静默回落，补 `@require_role('warehouse')`）；`batch_import.html` 新增 opening_stock 导入卡片；「粘贴导入」改多行 textarea modal（`pasteImportModal`）并经 `/opening_stock/batch_save` 统一入库；`saveDocument` 与粘贴导入均改走 `WMS.api`（消除原生 fetch）；`exportRows` CSV 加 BOM 修复 Excel 打开中文乱码。R6 同类点排查：BUG-2026-08-05-006 已修 in_order/out_order，本次补齐 opening_stock 漏网点。回归 `tests/test_opening_stock_import.py` 5/5（模板下载 xlsx/导入入账+跳过/缺列 400/仓库必填跳过/前端按钮指向真实能力）；功能冒烟（session 注入登录）模板下载/导入入账/示例行跳过/不存在仓跳过全 PASS；全量回归 **1727+5 passed、84 skipped、0 failed**；lint A1-A11 0 违规。模板+后端改动，**生产需重启 WMS 服务生效（R3）** |

| BUG-2026-09-15-004 | 新增期初库存单「日期」改不了（用户反馈"新增期初库存单为什么改不了日期？"）。根因是前端日期链路完全断裂、后端无从接收：①表头 `<input type="date" value="{{ doc_date }}">` **无 id**，任何 JS 都读不到其值；②`collectItems()` 组装 payload 时只取 `material_id/warehouse_id/quantity/price/remark`，**不含 `date`**；③`saveDocument()` 从不读取表头日期。后端 `batch_save_opening_stock` 逐行 `_parse_opening_stock_date(item.get('date'))` 恒得 `None` → 一律回落 `date.today()`，无论用户选哪天都存成"今天"。同类缺口：粘贴导入 `parsePasteLines()` 不产出 `date`；Excel 导入后端硬编码 `_parse_opening_stock_date(None)` 且模板无日期列。三个入口全部丢失日期（R6 同根因） | **已修复（2026-09-15）**：①表头日期输入补 `id="headerDocDate"`，新增 `getHeaderDocDate()`，`saveDocument()` 把表头日期注入每行 `item.date`；②粘贴导入 modal 新增建账日期输入 `id="pasteDocDate"`（打开时默认沿用表头日期），`parsePasteLines(text, defaultDate)` 支持可选第 6 列"日期"（行内优先、`YYYY-MM-DD` 校验、非法告警并回落默认），`confirmPasteImport` 传入 modal 日期；③Excel 模板新增"日期"列（示例行同步），导入解析器识别日期列 `_row_date(row)`，兼容 `datetime`/`date` 对象与 `YYYY-MM-DD`/`YYYY/MM/DD`/`YYYY.MM.DD`/`YYYYMMDD` 字符串，空值回落当天；`_apply_opening_stock_balance` 新增与更新分支均写入 `date`。回归 `tests/test_opening_stock_import.py` 新增 6 例（batch_save 带日期落库/更新换日期/缺省回落今天/模板含日期列/Excel 导入按日期建账/前端日期链路静态断言）全绿；功能冒烟 8/8 PASS。模板+后端改动，**生产需重启 WMS 服务生效（R3）** |
| BUG-2026-09-15-005 | 期初库存台账工具栏大量按钮无效/不对（用户反馈"这些工具栏很多无效或不对"）：opening_stock.html 顶部工具栏+行工具栏从单据卡片页模板套用，但本页是"期初库存台账（批量录入网格）"，非带单号可流转单据——①首/上/下/末张硬编码 disabled 无 onclick，且期初库存不在 DOCUMENT_NAVIGATION_MODULES 可导航单据清单内、页面已有自己的查询分页，4 键永久无效；②智能分享仅 toast 占位；③"导入"与"批量导入"同指向 /batch_import?type=opening_stock 重复；④"设置"跳与本页无关的 /system_settings；⑤"查找单据"跳"库存台账报表" /report/view/ledger 而非查单据；⑥"删除" clearSelectedRows() 逻辑错乱（过滤空行+pop 最后一行数据），页面无选中概念；⑦行工具栏 最近使用/存为模板/批量修改/字段设置 4 个 toast 占位 | **已修复（2026-09-15）**：精简+修正——删除无法实现的（首/上/下/末张、重复"导入"、无关"设置"）；智能分享改指向 /wechat_share 真实链接（title 保留"系统管理 → 微信分享"，兼容 BUG-2026-09-15-002 断言）；查找单据改页内查询面板锚点 #opening-query-panel（查询面板补 id）；删除重定义为 clearEntryRows()（清空当前录入明细，confirm 防误触，已建账记录不受影响）；行工具栏移除 4 个占位按钮与静态"选择模板"下拉。R6 同类点排查：首/上/下/末张导航仅 _other_order_toolbar.html / in_order_add.html 另有，但均为真实单据且按 order_id 条件启用/接线，非同款硬编码 disabled，本 BUG 仅 opening_stock 一处。回归 tests/test_bug_2026_09_15_005_opening_stock_toolbar.py 9/9 PASSED + 存量 verify_bug_2026_09_15_002_smart_share_hint.py 5/5 PASSED；test_opening_stock_import.py 前端静态断言 grep 核对无破坏；lint A1-A11 0 违规。模板改动，**生产需重启 WMS 服务生效（R3）** |
| BUG-2026-09-15-006 | 期初库存单头部三个字段是"摆设"（用户反馈"这个地方的功能有个屁用"）：opening_stock.html 新增期初库存单头部——①备注（headerRemark）有输入框但 saveDocument() 从不读取，用户填了备注保存时被直接丢弃（最不友好的一类"没用"）；②单据编号（opening_doc_no）OP+当天日期拼接的只读框，saveDocument() 不提交，本页是批量台账、单号不能查单据，纯装饰；③入库来源（期初建账）只读固定文本纯展示。saveDocument() 实际只读取 日期 与 单据仓库 两个表头字段 | **已修复（2026-09-15）**：按用户选定"备注接通+删装饰"——表头备注接通保存：saveDocument() 读取 headerRemark 作为各行明细默认备注回落（行内已填备注的行不受影响，后端 batch_save 本就按 item.remark 落库），备注输入框加 placeholder 说明新行为；删除装饰字段 单据编号、入库来源（模板）及路由 opening_doc_no 死代码（routes/opening_stock.py）。回归 tests/test_bug_2026_09_15_006_opening_stock_header_fields.py 5/5 + 关联 005 工具栏 9/9、002 智能分享 5/5、test_opening_stock_import.py 11/11 全 PASSED；lint A1-A11 0 违规。模板+后端改动，**生产需重启 WMS 服务生效（R3）** |

| BUG-2026-09-16-013 | 期初建账"摸黑录入"：全系统无任何"哪些物料还没建账"的视图（P1-A 新增能力）。期初库存只有「单据列表」和「按单编辑」两个视角，都以**已录入的数据**为中心——仓管员录到一半，无法回答"仓库里几百上千个物料，到底还漏了谁"。唯一手段是拿物料台账逐条比对期初台账人工对账，物料上千时不可行，实际结果是漏建账直到第一次出库/盘点才暴露（此时已影响库存准确性）。多仓库场景更糟：物料在某仓建了、另一仓没建，单据列表完全看不出来 | **已新增（2026-09-16）**：`GET /opening_stock/progress` 建账进度看板——行=物料、列=启用仓库的矩阵，格子显示该 (物料,仓库) 期初累计数量，空格标红「未建账」。口径与 INVENTORY_TRUTH.md §2.1.2 一致：①按 `(material_id, warehouse_id)` 对**全部单据** `SUM(quantity)` 聚合（一次分组查询拿全量，避免逐物料 N+1），绝不取单行当余额；②合计为 0 视为未建账（录 0 与没录在库存上等价）；③`warehouse_id` 为 NULL 的历史行单列「未指定仓库」栏，不猜归属、不摊派；④停用仓库不参与统计。R1：物料可上万，默认每页 50 条 + 完整分页，顶部进度统计走**全库口径**（不受当前页/筛选影响），不拿当前页行数当总量。R2：按 `warehouse_id` 精确聚合，**不走** `get_warehouse_stock_quantities` 的单仓全量回落分支（否则单仓环境下会把全局库存误标成该仓已建账，已加 T 反例守）。R4：导出 `GET /opening_stock/progress/export` 纯 CSV（utf-8-sig，Excel 中文不乱码），无物料时仍返回带表头空文件而非报错；复制按钮在非 HTTPS 环境降级为 prompt 手动复制。R6：页面与导出的 SUM 聚合 + "0 算未建账"判定**共用唯一实现** `_opening_stock_progress_matrix` / `_opening_stock_progress_cells`（app.py），不在路由里各写一套（改一处忘另一处会让页面说已建账、导出说未建账），并有测试锁死路由内不得再出现 `func.sum(OpeningStock.quantity)` 与 `abs(qty) > 1e-9`。回归 `tests/test_opening_stock_progress.py` 26/26 PASSED（多单据累加 150 而非单行 100、部分建账仍算已建账且另一仓显示未建账、0 按未建账、only_gap 开关、停用仓不出现在列头、NULL 仓库单列且不算完全未建账、全库统计不受分页影响、导出含/不含已建账、未登录拦截、防 tojson 引号截断、R2 多仓隔离 60/40 不出现合计 100、单仓反例、R6 页面与导出同口径端到端一致）；期初全族回归 188 passed 0 failed；lint A1-A11 0 违规。模板+后端改动，**生产需重启 WMS 服务生效（R3）** |

| BUG-2026-09-16-014 | 内联事件处理器用双引号包 `tojson` 导致按钮静默失效（R6 同族预防性排查，本次实装中踩到并当场修复）：`tojson` 过滤器产出**双引号** JSON（`"M-0001"`），若写成 `onclick="fn({{ x|tojson }})"`，渲染结果是 `onclick="fn("M-0001")"`——HTML 解析器在 JSON 的起始双引号处提前闭合属性，函数调用被截断，点击**无任何反应且控制台不报错**（最坏的一类静默失效，用户只会觉得"按钮坏了"）。本次建账进度页的复制按钮首版即中招，Playwright 实测 `element is not visible` + `offsetParent: null` 才暴露。全站既有正确写法（`opening_stock_list.html` `deleteDoc`、`print_routing.html` 三处）统一用**单引号包属性** | **已修复（2026-09-16）**：`opening_stock_progress.html` 复制按钮改为 `onclick='copyUnbuiltRow({{ r.code|tojson }}, {{ r.name|tojson }})'` 并补注释说明原因。R6 全模板扫描 `onclick="...[tojson` 模式，确认无其他残留（仅本页一处，已修）。回归 `tests/test_opening_stock_progress.py` 新增 2 例守住：T15 静态断言（同含 `tojson` 与 `onclick` 的行必须用单引号；`onclick='copyUnbuiltRow(` 必须存在）、T16 渲染后断言（正则可提取到完整的 `onclick='...copyUnbuiltRow...'` 属性且携带物料编码、不得退化成双引号包属性）。模板改动，**生产需重启 WMS 服务生效（R3）** |

| BUG-2026-09-16-015 | `parse_float_value(value, None)` 必然抛 TypeError，把「数量填了非数字」这类普通用户输入错误升格成 500 服务器内部错误（P1-B 预检实装中发现，属存量缺陷 R6 同族排查）。根因：`app/utils.py::parse_float_value` 的兜底分支写的是无条件 `float(default)`，调用方用 `default=None` 表达「解析不出来就返回 None，我自己报错」，于是 `float(None)` 抛 TypeError，被上层 `except Exception` 兜成 500。触发点两处：`app/app.py::_opening_stock_normalize_items`（`POST /opening_stock/batch_save` 期初网格保存）与 `app/routes/batch_import.py::opening_stock_import`（期初导入）。实测修复前数量填 `abc` / `-5` / 空串三条路径全部返回 500「服务器内部错误」。 | **已修复（2026-09-16，commit 本次提交）**：`default is None` 时直接返回 None，其余 `default` 取值行为逐字节不变（存量 `tests/test_p0_nan_quantity_guard.py` 期望全部保持）。修复后 `batch_save` 对上述三种输入统一返回 **400** 且带可读行号提示；`_opening_stock_normalize_items` 顺手删掉因该 bug 而永不可达的 `if quantity < 0` 死分支，提示语细分为「请输入数量 / 数量 [x] 无效，必须是大于等于 0 的数字」。回归：`tests/test_opening_stock_import_dry_run.py::test_t3b_invalid_quantity_skipped_not_500`（abc / -5 / 空串三例）+ 存量 `test_p0_nan_quantity_guard.py` 全过 |
| BUG-2026-09-16-016 | 期初导入「文件内重复」误判：校验失败的行也占用去重键，导致同一 (物料, 仓库) 的后一条**合法**行被当成重复丢弃（P1-B 预检实装中发现，属存量缺陷）。根因：`app/routes/batch_import.py::opening_stock_import` 把 `seen_keys.add(dedup_key)` 写在数量/单价校验**之前**，而这些校验失败时用 `continue` 跳过该行——行虽未导入，键却已登记。用户看到的是莫名其妙的「物料+仓库在文件内重复」。修复前实测只导入 1 行而应为 2 行。 | **已修复（2026-09-16，commit 本次提交）**：`seen_keys.add(dedup_key)` 下移到全部文件级校验（物料存在性 / 数量 / 单价）通过之后，并加注释说明「只有真正会被导入的行才占键」。真正的文件内重复仍然照旧拦截。回归：`tests/test_opening_stock_import_dry_run.py::test_t3c_bad_row_does_not_poison_dedup` + `test_t3d_real_duplicate_still_detected` + P1-B 全套 16/16 通过 |
| BUG-2026-09-16-017 | 手机端期初「建完就瞎」：只能建账，看不到建了什么、建错了也改不了（P1-C 新增能力）。① `GET /api/opening_stock` 写死 `.limit(200)`——**把分页上限当成了业务上限**，第 201 条之后的建账记录手机端永远看不到，且响应不带 total，用户无法知道本仓到底建了多少条（R1 违规）；② App 端 `OpeningStockScreen` 只有录入网格，无已建账列表、无编辑入口，改一个数必须回 PC 端翻单据列表。 | **已修复（2026-09-16，commit 本次提交）**：① 后端 `app/routes/native_api.py::native_api_opening_stock_list` 改走 `_mobile_paginate`，支持 page/page_size（默认 20、上限 100），响应带 total/page/page_size/total_pages；新增 built_total / built_quantity 两个汇总字段，**基于仓库全集计算**，与 keyword 和分页彻底解耦（R1：汇总不随分页缩水）；② 新增 `POST /api/opening_stock/<line_id>` 按差额编辑数量/单价——复用 `_apply_opening_stock_balance`（delta = new − old，绝不覆盖式写总账），不允许换物料/换仓库，warehouse_id 为 NULL 的历史行拒改（归属未知时不动总账），库位账同步失败返回 400 并整体回滚；③ Android 侧 `OpeningStockScreen` 拆「建账 / 已建账」双视角：已建账列表带建账概览卡、关键字筛选、滚动分页加载，点行弹窗改数量/单价（含差额预告「本仓该物料库存将减少 40」）；VM 新增 loadBuiltItems/loadMoreBuiltItems/searchBuiltItems/startEditing/cancelEditing/submitEdit，切仓库与新建账后自动刷新列表。 回归：`tests/test_opening_stock_mobile_list_edit.py` 26/26 PASSED（核心：建 205 条后翻页必须覆盖全部 205 条、第 201 条可达、汇总不随分页/关键字缩小、差额调整总账、只改单价不动库存、非法数量 abc/-5/空串全 400、跨仓编辑互不牵连、NULL 仓库行拒改、流水数量写差额而非新值、VM↔Screen 方法配对防 Kotlin 编译失败）+ 存量 opening/android/native/mobile 全域 419 passed, 5 skipped, 0 failed。 注：后端改动需重启 WMS 服务；Android 改动需重新构建 APK（CI Android APK Build 自动出包）。**模板/前端改动，生产需重启 WMS 服务生效（R3）** |
| BUG-2026-09-16-018 | 期初录入草稿「幽灵写入 / 保存后复活」（P1-D 实现过程中浏览器实测复现，属本次新增功能的自身缺陷）：把 `scheduleDraftSave()` 放在 `renderRows()` 末尾，而 `renderRows()` 并不只在用户编辑时执行 —— 页面加载、服务端行回填时同样会跑。由此产生两个连锁症状：① **幽灵草稿**：打开一张已建账的期初单，用户什么都没改，页面一加载就写出一份草稿，刷新即提示「检测到本单有未保存的录入草稿」，把「有未保存改动」这个信号彻底贬值；② **保存后复活**：手工保存成功后 `clearDraft()` 确实执行了，但紧随其后的 700ms `setTimeout` 跳转重新加载页面，`renderRows()` 又把刚被清掉的行写回 localStorage，用户下次进入该单会看到「有未保存草稿」，点恢复出来一堆**已经落库**的行，反而以为上一次保存没生效，可能重复录入造成期初虚增。修复：`renderRows()` 内**移除** `scheduleDraftSave()`（改为注释说明缘由），并把落草稿改为严格由「用户意图」驱动 —— 在 `updateField` / `addRow` / `insertRowAfter` / `copyPreviousRow` / `selectMaterial` / `deleteRow`（两个分支）/ 表头三件套的 input·change 监听 各自显式调用；`clearEntryRows` / `resetDocument` 则清草稿。同时补齐**粘贴导入**路径（`doPasteImport` 成功后 `clearDraft()`）—— 期初有手工保存与粘贴导入两条落库路径，只清前者会留下同样的复活问题。回归锁：`tests/test_opening_stock_entry_draft.py` 新增 T13（`renderRows()` 不得出现 `scheduleDraftSave()` 实际调用，剥离注释后断言）、T14（六个用户意图入口都必须落草稿，与 T13 配对防止「哪都不写」导致功能整体失效）、T15（`saveDocument` 与 `doPasteImport` 两条落库路径成功分支都必须 `clearDraft()`）；两条新断言均已做注入式变异验证（把缺陷改回去会失败）。 |
| BUG-2026-09-16-019 | 门禁脚本「断言钉死文件位置」，致 WMS CI 长期红灯失去信号价值（排查 CI 时发现的**门禁自身**缺陷，非业务代码缺陷）：ARCH-MODELS-01（2026-09-15）把 19 张 AI 表从 `app/app.py` 模型区迁到 `app/models/ai.py`（类定义逐字保留，`app.py` 侧改为 `from models.ai import (...)`）。该迁移与 AGENTS.md A10「`app.py` 不得持续膨胀」同向，是正确重构；但 `scripts/verify_wms_bugs.py` 中四项 AI 断言的判据写死为 `'class Xxx' in app_py`，迁移后全部落空：AI-DOCUMENT-JOB-MODELS-001（4 个子条件）、AI-CONTROLLED-AGENTS-001（2 个）、AI-IDEMPOTENCY-001（2 个）、AI-AUDIT-001（3 个），共 11 个子条件恒为假。后果远大于「一条检查失败」——`verify_wms_bugs` 在 CI 步骤序中位于中段，它失败后 Unit tests / Security scan / Smoke test / Verify regression 等步骤全部被 **skipped**，整条 WMS CI 自 2026-09-15 起连续十余个提交（含 17580be 基线）恒为 failure，CI 彻底丧失「看红绿判断新改动是否安全」的作用：本次期初库存 P1 的四个提交推上去后，CI 依旧全红，无法据此判断改动是否引入回归，只能手工拉基线 worktree 做 A/B 对比才自证清白。修复：在脚本中引入合并源 `ai_models_src = app_py + app/models/{ai,core,__init__}.py`，把上述 11 个**模型类**断言改为在合并源上查找；**路由 / helper / 模板等非模型断言一律保持只查 `app_py`**，不放宽任何检查强度。采用合并文本而非直接改读 models 文件，是为了兼容「模型又搬回 app.py」的情形，避免二次误报。回归锁：新增 `tests/test_verify_wms_bugs_gate.py`（8 项），覆盖①门禁整体 exit 0 且无 FAIL；②四项曾被误报的检查恒为 PASS；③模型类断言确实指向合并源、且合并源同时含 app_py 与 models/ai.py；④非模型判据仍只查 app_py（防止顺手放宽）；⑤合并源确实包含全部模型类，且这些类在纯 app_py 中确实不存在（证明「合并」是必需的）；⑥运行时校验模型可导入、9 条 AI 路由已注册、唯一约束与列齐备。其中「判据被改成恒真」这一劣化已做变异验证（注入后测试确实失败）。 |

| BUG-2026-09-17-001 | [P2] 合同/工程档案修改工程名称后，历史单据上的工程名称不同步（用户原话：「更改工程名称之前单据上的工程名称没有同步更改」，实测：HD260904 档案名已改为「淮安第三园区纯废水七期、八期安装工程」，采购入库明细表 IN26090089 仍显示旧名「广州汇侨」）。根因：单据表冗余存储 `contract_no`/`project_name` 文本快照，原模型注释明写「合同变更后历史单据保留原始值」——是设计口径而非代码笔误；`edit_contract` 与批量导入两条改名路径都没有任何传播逻辑。**R6 同根因历史已查**：合同类 9 条历史 BUG 均为筛选/导出/信封/NPE 类，无改名同步前例；冗余字段消费点全量盘点为 **9 张表**（InOrder/InOrderItem/OutOrder/OutOrderItem/AfterSaleOutOrderItem/PurchaseOrder/PurchaseOrderItem/SalesOrder/SalesOrderItem），且引用方式有两种（`contract_id` 外键 + `contract_no` 纯字符串，与 `_contract_delete_blockers` 同口径） | **已修复（2026-09-17，按用户拍板「跟随同步」改口径）**：①新增 `sync_contract_project_name(contract_id, old_no, new_no, new_name)`（app.py）——按 contract_id 外键或旧 contract_no 字符串匹配，9 张表一次收口同步 project_name（改号时连同 contract_no 一起跟随），返回 {表名: 行数}；②`edit_contract` 编辑路由：改名/改号时在同一事务内同步（失败整体回滚，不留"档案已改、单据半同步"），成功消息带「已同步更新 N 条历史单据的工程名称」；③**批量导入改名同样同步**（R6 同类消费点，导入路径是第二条改名入口，漏接会出现"手工改同步、导入改不同步"的漂移）；无变更的保存不触碰历史行；④模型注释更新为新语义（Contract docstring 与 4 处「历史单据不变」注释），防止后人按旧快照假设再写岔。**回归**：新增 `tests/test_contract_rename_sync.py` **5 项 PASSED**——T1（A9 同名）9 张表全命中+外键/字符串两种引用+对照组 HD2 隔离+无匹配安全返回；T2 编辑路由改名同步含 completed 单据且消息带条数；T3 改号后单据编号与名称一起跟随；T4 批量导入改名同步且消息带条数；T5 无变更编辑不动历史行。合同相关回归 **88 passed、2 skipped**，`lint_wms_rules.py` 0 违规。后端改动，**生产需重启 WMS 服务生效（R3）**——重启后编辑/导入改名即同步；**改名前已存在的历史不一致数据不会自动回刷**，如需把存量旧名统一刷成现名，另行说一声（可做一次性兜底脚本） |
| BUG-2026-09-18-001 | [P2] 物料档案列表（/material）行高亮仍是**单级**（只比 `min_stock`），低于安全库存（danger 档）但高于最低库存的物料在列表里**完全不被标注**——两级化（AI-CI-GREEN-005-F04/F05）统一了 /alert 页、首页、库存查询页、手机端，唯独漏了物料列表这一消费点，正是 R6 要防的「同一批物料不同页面不同答案」；且 `stock_query.py:123` 注释还误称「物料列表已对齐」，会误导后续维护把它跳过去 | **已修复（2026-09-18）**：`material.html` 行高亮与库存数字标色对齐两级判定（与 /alert 页同口径）——`low( stock<=min_stock )=红`（`table-danger`/`text-danger`）、`danger( min_stock<stock<=safety_stock )=黄`（`table-warning`/`text-warning`），`safety_stock=max(reorder_point,min_stock)`；用 Jinja 简单布尔 `set`（`is_low`/`is_danger`）计算，避开 `{% if %}` 内 `set` 的作用域陷阱，物料列表路由零改动（列表本就取全局 Material.stock 管理总览视角，与两级判定正交）。R6 同类点已随本次一并核对：库存查询页 F05 已两级、/alert 页与手机端本已两级，物料列表是最后一个单级残留点；`stock_query.py:123` 注释在本修复落地后**由错变对**，无需另改。**回归**：新增 `tests/test_bug_2026_09_18_001_material_list_two_band_highlight.py` **4 项 PASSED**——T1 danger 档标黄（旧逻辑漏报场景）、T2 low 档标红、T3 正常/未设阈值不标色、T4 只设安全库存（min=0,reorder=50）库存 30 仍标黄；两级统一测试 6 项 + 相关子集共 **24 passed**，`lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 双门禁 **0 违规**。模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存 |
| BUG-2026-09-18-002 | [P2] 「安全库存(reorder_point) 不得小于最低库存(min_stock)」这条不变量只在**批量设置**（显式冲突拒绝）与**Excel 导入**（clamp 提到 min）两处生效，而**单物料新增与编辑**两条手写路径毫无校验，可写入 reorder_point < min_stock 的脏数据——后果是 `safety_stock=max(reorder_point,min_stock)` 退化为 min_stock，用户填的安全库存被 max() **静默吞掉**、danger 档（低于安全库存）消失；四条写入路径三种行为（R6 同根因口径漂移） | **已修复（2026-09-18）**：新增公共函数 `_resolve_alert_thresholds(min_stock, reorder_point)`（routes/material.py），单物料**新增**（`/material/add`）与**编辑**（`/material/edit/<id>`）统一走它——①显式把安全库存填得比最低库存低（>0 且 <min）→ `api_error('安全库存不能低于最低库存')` 400，与批量设置文案一致，新增不落库、编辑不动原值；②安全库存未填（=0）但设了最低库存 → 自动提到最低库存（与批量 update_min-only、导入 clamp 同口径），保证 safety_stock 不小于 min_stock。校验规则**只留后端一份**（单一事实源），前端不加重复 JS——批量设置页（/alert）本就走纯后端校验，前端加 JS 反而引入第三处可漂移的阈值逻辑（R6），后端中文报错经现有 alert()/showToast 正常显示。**回归**：新增 `tests/test_bug_2026_09_18_002_material_threshold_validation.py` **5 项 PASSED**——新增拒绝显式 safety<min 且不落库 / 新增 safety 留 0 自动提到 min / 新增 safety>=min 原值保留 / 编辑拒绝显式 safety<min 且原值不变 / 编辑 safety 留 0 自动提到 min；物料域相关回归 **242 passed、3 skipped、0 failed**，`lint_wms_rules.py` 与 `lint_no_raw_post_fetch.py` 双门禁 **0 违规**。后端改动，**生产需重启 WMS 服务生效（R3）** |
| BUG-2026-09-18-003 | [P2] 手机端「两级判定」（low=破最低库存红线、danger=破安全库存预警线）在 F04 统一到了预警列表/首页/库存查询，但**扫码结果卡「库存充足/不足」徽标**与**查库存列表行数量色**仍是客户端单级重算 `stock > minStock`——低于安全库存（danger 档）但高于最低库存的物料被误显示为**绿色「库存充足」**，现场按徽标拣货会误判（R6 同类点遗漏，与 BUG-2026-09-18-001 桌面物料列表同源） | **已修复（2026-09-18，Kotlin）**：`ScanScreens.kt` 两处统一改两级，安全库存 `safetyStock = max(reorderPoint, minStock)`（与服务端 `safety_stock` 同口径）——①结果卡徽标：抽 `scanBadge` 三元组（文案/前景色/背景色），破红线=红「低于最低库存」、破预警线=黄「低于安全库存」、其余=绿「库存充足」；未设阈值的物料退回 充足/不足 二态（保持原行为，不误标「低于最低库存」）；②`StockListRow` 数量色：`stock<=minStock` 红、`stock<=safetyStock` 黄、其余绿。黄色复用主题已有 `Warning`/`WarningContainer`（Color.kt），无需新增 import（`ui.theme.*` 已导）。**同步更新既有锚点测试** `test_bug_2026_09_10_003`：其 `test_result_card_badge_compares_against_min_stock` 断言的正是被替换的旧单级字符串，改为 `test_result_card_badge_two_band_uses_safety_stock` 锁定两级（InfoChip 真值断言保留）。**回归**：新增 `tests/test_bug_2026_09_18_003_android_scan_badge_two_band.py` **6 项 PASSED**（两级徽标/未设阈值回退二态/列表行两级/旧单级字符串已清除/Warning 色板存在/Kotlin 括号平衡）；Android 相关锚点测试 **122 passed、1 skipped、0 failed**，双 lint 门禁 **0 违规**。沙箱无 Android SDK 未编译、未出 APK，按仓库先例用源码锚点+括号平衡锁定。**生效条件**：需 GitHub Actions 重新构建 APK 并重装后生效（后端无需重启） |
| BUG-2026-09-18-004 | [P2] 电脑端与手机端「安全库存预警」**口径分裂**：电脑端 /alert 页与首页一律按**全局** `Material.stock` 判定，手机端（/api/mobile/alert/list 等）按**当前仓库** `get_warehouse_stock_quantities()` 判定——多仓库环境下同一物料两端结论可能相反（全局够、本仓不够，或反之），且两端都没有任何口径说明，用户只能怀疑系统坏了（单仓时因单仓回退全局恰好一致，问题被掩盖） | **已修复（2026-09-18）**：/alert 新增**可选仓库维度**——①筛选表单加「仓库」下拉，默认「全部仓库（全局）」保持原总览口径（material.stock，行为零变化），选定仓库后按 `get_warehouse_stock_quantities(warehouse)` 仓库级判定（缺记录物料按 0、不回退全局，A11/R2），与手机端同口径；②无效仓库 id 回退全局（不报错、不空白）；③页面列表头明示当前判定口径（「按全部仓库全局总量判定」/「按仓库「X」仓库级库存判定（与手机端一致）」），直接消除两端困惑；④排序走前端 JS（`app.js` 从当前 URL 出发只改 sort/order/page），warehouse_id 自动随 URL 保留，无需额外处理；⑤「清除」按钮条件纳入 warehouse_id。仓库对象取用 `db.session.get(Warehouse, id)`（不引入 Query.get 弃用调用）。**回归**：新增 `tests/test_bug_2026_09_18_004_alert_warehouse_scope.py` **4 项 PASSED**——默认全局口径保持（全局 normal 物料不进 danger 列表）/ 选定 A 仓仓库级判定出 danger / 选定 B 仓 normal 不出现 / 无效仓库 id 回退全局；预警域相关回归 **31 passed**，双 lint 门禁 **0 违规**。后端+模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存。**说明**：首页 dashboard 的 low_stock 计数仍按全局口径（管理总览语义），与 /alert 默认全局一致、未改动；如需首页也可切仓库，另行说一声 |
| BUG-2026-09-18-005 | [P2] 两端口径分裂的**首页残留**：手机端首页 dashboard 早已能切仓库（BUG-2026-09-10-010），电脑端 /alert 页也在 BUG-2026-09-18-004 加了仓库维度，唯独**电脑端首页**那张「库存预警物料」卡片仍只按全局 `Material.stock` 计数——多仓下 A 仓已缺货、但 A+B 加总还够时，首页卡片显示 0，掩盖本仓预警（用户据此拍板「同意给电脑端首页补按仓看预警数」） | **已修复（2026-09-18）**：`index()` 的 `low_stock` 新增可选仓库维度——①默认（不传 `alert_warehouse_id`）= 全部仓库（全局 `_material_low_stock_filter`，行为零变化），选定仓库后按 `get_warehouse_stock_quantities(warehouse)` 仓库级判定（缺记录物料按 0、不回退全局，A11/R2），与手机端首页、电脑端 /alert 同口径；②无效仓库 id 回退全局；③`low_stock` 同时被 `pending_stock_tasks`（库存待处理合计）引用，仓库级预警数让合计同步反映，语义一致；④首页预警卡由纯链接改为卡片内嵌仓库选择器（GET 表单 auto-submit 到 `/`），并明示当前判定口径；⑤另两处显示 `low_stock` 的位置（库存待处理列表卡、「今日概览」表格行）同步加「（仓库名）」后缀，避免同一数字两处口径不明。仓库对象取用 `db.session.get(Warehouse, id)`。**回归**：新增 `tests/test_bug_2026_09_18_005_home_alert_warehouse_scope.py` **4 项 PASSED**——默认全局口径保持（全局 normal 物料预警数=0）/ 选定 A 仓仓库级出预警数=1 / 选定 B 仓 normal=0 / 无效仓库 id 回退全局；首页/预警相关回归 **81 passed、5 skipped、0 failed**，双 lint 门禁 **0 违规**。后端+模板改动，**生产需重启 WMS 服务生效（R3）**，浏览器 Ctrl+F5 清缓存。**说明**：本选择器只作用于「库存预警物料」计数，首页其余卡片（今日出入库、待处理单据等）仍按各自原口径；如需整页按仓过滤，属更大改动，另行评估 |
|
| BUG-2026-09-18-006 | [P1] 手机端**出库/入库页「提交」按钮被顶出屏幕且滚不回来**，单据提交不了（现场：出库→手工添加，看不到提交按钮） | **已修复（2026-09-18，Kotlin）**：根因是 `ScanScreenBase` 外层 `Column` **不可滚动**，而它平铺了 header（出库页含 仓库卡+领料部门+领料人+合同卡）/库位选择器/拍照取证/草稿错误/扫码反馈/离线横幅/打印横幅/汇总卡，只有扫码清单带 `weight(1f)`——高度不足时 Compose 把**排最后**的底部提交区直接裁掉（既非压缩也非滚动，故用户回滚也找不到）。两个放大器一并修：①`ContractInputCard` 合同建议原是无约束 `forEach` 全量展开（对比弹窗内物料候选早已 `heightIn(max=220.dp)`+`verticalScroll`），出库页输入 1 字符常命中十几条合同，单卡可达七八百 dp 足以吃光整屏；②底部区手工添加候选是无约束 `take(5)` 平铺（同类形状）。修复：顶部区改 `weight(1f)+verticalScroll`（内容再高只滚动，不挤按钮）；**底部提交区留在可滚区之外**（非 weight 子项，按测量高度先分配，必然可见，`adjustResize` 下也不会被键盘顶掉）；合同建议与手工候选各限高 200dp+内部滚动，底部区整体 `heightIn(max=420.dp)`；清单为空时去掉多余 `weight(1f)` 避免与顶部区抢空间。入库/出库/盘点三页共用基类，**一处修复三页生效（R6）**。**回归**：新增 `tests/test_bug_2026_09_18_006_scan_submit_button_offscreen.py` **6 项**（顶部区可滚/底部区在可滚区之外/合同建议限高/手工候选与底部区限高/三页共用基类/括号平衡），修复前 **4 项红 → 修复后 6 项全绿**。另引入 **ktlint 1.3.1 做真实 Kotlin 语法解析对照**（此前只有括号计数）：改动文件语法类错误数与基线**完全一致**（ScanScreenBase 1→1、ScanScreens 1→1、HomeScreen 2→2，均为仓库既有的 `chain-method-continuation` 风格告警，非本次引入），即**零新增语法错误**。沙箱无 Android SDK 未编译、未出 APK。**生效条件**：需 GitHub Actions 重新构建 APK（versionCode 20 / 3.8.6）并重装后生效（后端无需重启） |
| BUG-2026-09-18-007 | [P2] 手机端扫码页标题**写死「扫码」**，与「手动添加」入口矛盾（现场：从出库→手工添加进去，标题却是「扫码出库」，底部 Tab 又是「出库」，三套口径并存，怀疑进错页面） | **已修复（2026-09-18，Kotlin）**：根因是把**一种录入手式**写进了业务页标题——入库/出库页另有「手动添加」弹窗与语音建单（AI-VOICE-OUT-F01），盘点页另有「识物盘点」，三页还都能从首页卡片/概览下钻进入。修复为业务动作名并**一次修干净全部同根因消费点（R6）**：①三页 `title` →「入库 / 出库 / 盘点」，副标题补回"扫码或手动…"，扫码信息不丢失；②`Screen.kt` 的 `Inbound/Outbound/Stocktake.title` 同步（这是本页之外第二套标题口径）；③`HomeScreen.kt` 首页功能卡「扫码盘点」→「盘点」（第三处消费点）；④`LoginScreen.kt` 功能范围陈述「扫码入库 · 出库 · …」→「入库 · 出库 · …」（原写法会让人以为其余能力也必须扫码）；⑤**route 字符串与标题解耦，本次只动 title 不动 route**（改 route 会直接断导航）。已在 `Screen` 类上补 KDoc 说明"title 是业务动作名、不写扫码"，防止后来者改回去。**回归**：新增 `tests/test_bug_2026_09_18_007_scan_title_copy.py` **6 项**（三页标题/副标题、Screen 路由标题、Tab 与首页卡一致、登录页文案、route 未被波及、括号平衡），修复前 **4 项红 → 修复后 6 项全绿**；ktlint 语法错误数与基线一致（同 -006）。**生效条件**：同 -006，需重新构建 APK 并重装 |
| BUG-2026-09-18-008 | [P0] 手机端**入库单头字段整条链路缺失**：`/api/inbound` **一个单据头业务字段都没读**，`supplier_id` / `contract_id` / `contract_no` / `project_name` 恒为 NULL、`remark` 被常量 `'Android原生端提交'` 占满。后果不是"少个可选字段"：每日明细报表 `purchase_in` 的 `party_key='supplier'` 渲染 `party.name if party else ''`，**手机录的每一次采购入库在报表「供应商」列恒为空**，采购对账 / 按供应商归集 / 按供应商退货全部断链，且单据 `status` 落 `completed` 后无补录入口（数据不可救） | **已修复（2026-09-18，Python+Kotlin）**：根因是**同一张单据 PC 有、手机没有**的断链——`InOrder` 模型本就有 `supplier_id/customer_id/contract_id/contract_no/project_name/remark` 列，PC 入库单能填（`routes/in_order.py`），出库侧 `native_api_outbound` 早就读 `contract_no`/`department_id`/`picker` 并回填 `contract_id`/`project_name`，**唯独入库链路漏了整条**。修复：①后端 `native_api_inbound` 读取 `supplier_id`（命中 Supplier 写 `InOrder.supplier_id`；**id 无效硬拒 `请选择有效的供应商`**，对齐 PC 不静默丢弃）、`supplier`/`supplier_name`（仅无 id 时文本兜底匹配编码或名称）、`contract_no`（命中合同档案回填 `contract_id`/`project_name`，未建档保留用户原文，与 `/api/outbound` 同段实现对齐而非另写）、`remark`（写入单据备注，留空仍回落旧常量，**行为兼容**）；②新增 `GET /api/mobile/suppliers`（形状对齐 `api/departments`）作为下拉数据源；③Android `InboundRequest` 增 `supplier_id`/`supplier`/`contract_no`/`remark` 四字段并在 `ScanViewModel` 构造点接线；④入库页 header 增「供应商（选填）」`PartySelectorCard` + 「备注（选填）」`InboundRemarkCard`（备注为单行无联想，故**不需要** -006 那套限高，对照区分）；⑤两者一并进 `ScanEditDraft` 断点续传快照+恢复，避免进程回收静默丢供应商关联；⑥供应商/备注随 `payloadJson` 走离线补传，断网时选的同样保留。**回归**：新增 `tests/test_bug_2026_09_18_008_mobile_inbound_header_fields.py` **11 项**（后端读三字段/写入 InOrder/无效 id 硬拒/合同号回填/供应商接口存在/Android 请求体四字段/构造点接线/草稿持久化/UI 入口/接口声明/括号平衡），修复前 **10 项红 → 修复后 11 项全绿**（证明是真实回归测试而非同义反复）；ktlint 语法类错误数与基线一致（ScanScreens 1→1、ScanViewModel 1→1，均既有 `chain-method-continuation` 风格告警）。**另经 9 案例实测探针验证**：传 id 命中 / 传不存在 id→400 / 传名称文本 / 传编码文本 / 合同号命中回填 / 合同号未建档保留原文 / 传备注 / 都不传回归旧行为 / 供应商下拉列表，9/9 符合预期。双 lint 门禁 **0 违规**、composable 注解检查 **83 文件通过**。**生效条件**：**后端需重启 WMS 服务生效（R3）**；Android 部分需重新构建并安装 APK |
| BUG-2026-09-18-009 | [P1] 手机端扫码页基类**用「按钮文案」驱动功能开关**：`ScanScreenBase` 里 `if (submitLabel == "提交入库" \|\| submitLabel == "提交出库")` 决定了「库位选择器」和「拍照取证」两块功能是否渲染。而 `submitLabel` 是按钮**显示文案**，全仓库检索它是该变量**唯一的逻辑用途**。后果：谁把按钮文案润色成「确认入库」（纯 UI 改动），**库位选择与拍照取证会一起静默消失**——无编译错误、无测试报警，只有"启用库位管理后入库必须填库位"这条后端校验才会在提交时报错；更隐蔽的是盘点页，它的行为仰赖"自己的文案不等于提交入库/出库"这个**否定式巧合**，等于把别人的文案当自己的开关（隐式耦合：改文案 = 改功能，而改文案的人不会去读基类实现） | **已修复（2026-09-18，Kotlin）**：把隐式反推改为调用方**显式声明**——新增 `showLocationSelector: Boolean = false`（是否渲染库位选择器）与 `showEvidenceCapture: Boolean = false`（是否渲染拍照取证入口，与前者独立便于将来分别开关）两个参数，默认 `false` 使**新增页面必须主动决策而不是继承巧合**；`submitLabel` **降级为纯显示**，不再参与任何逻辑判断（且未删除，按钮文案照旧）；三处调用点各自声明真实意图——入库页/出库页 = `true`（产生单据，需要库位 + 取证），盘点页 = `false`（不产生出入库单据，故不需要，并补注释说明"原先靠文案否定式巧合"）。**回归**：新增 `tests/test_bug_2026_09_18_009_scan_base_explicit_flags.py` **8 项**（文案比对已彻底移除/两个新参数已声明且默认 false/渲染条件改用新参数且组件未被误删/入库出库显式开启/盘点显式关闭/三处调用点全部显式传参/`submitLabel` 仍用于显示未被误删/括号平衡），修复前 **6 项红 → 修复后 8 项全绿**；测试对**去注释后的代码**做判断，注释中允许引用旧写法作说明（避免把"说明注释"误判成"旧代码还在"）。ktlint 语法类错误数 `ScanScreenBase.kt` 与基线一致（1→1，既有风格告警）。三道门禁 **0 违规**、composable 注解检查通过。**生效条件**：需重新构建并安装 APK（后端无需重启） |
| BUG-2026-09-18-010 | [P1] 手机端**入库确认弹窗信息缺失且无前置校验**：四个扫码页的"提交前确认"里入库页是**信息最少的一个**——出库页显示领料部门/领料人，盘点页显示盘点仓库+盘点单且未选时明确警告「尚未选择盘点仓库」、确认按钮带 `&&` 前置校验，而**入库页只有一句「共 N 种物料，数量 X，确认提交入库？」**，仓库/供应商/合同/备注一概不显示，按钮也只看 `isLoading`，**未选仓库照样可点**。这不是美观问题：入库仓库是决定**库存记到哪个仓**的唯一依据，多仓下用户提交前看不到自己选的是哪个仓，选错仓提交是一次真实账目错误（库存进错仓），而单据提交后 `status` 落 `completed`、无补录入口，错账不可自愈。盘点页早已用弹窗解决同一类问题，入库页属同根因漏改（R6） | **已修复（2026-09-18，Kotlin）**：①弹窗正文补齐单头信息——收货仓库（未选时改为明确的「尚未选择收货仓库，请先选择仓库」警告）、供应商、合同编号、备注（后三者用 `let`/`isNotBlank()` 做存在性判断，仅在有值时显示，避免空行噪音）；②确认按钮追加「已选仓库」前置校验 `enabled = uiState.selectedWarehouse != null && !uiState.isLoading`，与盘点页同一写法——前置条件用 `&&` **追加**而非用 `isLoading` 覆盖（否则未选仓库时按钮会变可点）；③原有物料种数/总数量汇总信息保留，未因补新信息而丢失。**回归**：新增 `tests/test_bug_2026_09_18_010_inbound_submit_dialog.py` **7 项**（弹窗展示仓库含未选警告/确认按钮校验仓库且不被 isLoading 覆盖且用 && 不用 \|\|/单头信息有值时展示且做非空判断/物料汇总未丢失/与盘点弹窗既有范式一致/提交守卫未削弱/括号平衡），修复前 **4 项红 → 修复后 7 项全绿**；测试用**大括号配平**精确截取 `showSubmitDialog` 块，避免被内层 lambda 的 `}` 截断而误判。全量 `tests/` **2068 passed / 85 skipped / 0 failed**（+7 = 新增）。ktlint 语法类错误数与基线一致（ScanScreens 1→1，既有风格告警）。三道门禁 **0 违规**、composable 注解检查通过。**生效条件**：需重新构建并安装 APK（后端无需重启） |
| BUG-2026-09-18-011 | [P3] 手机端出库提交按钮文案**「提交出库」与「确认出库」两套说法并存**：同一屏里底部按钮是「提交出库」，而点开后的确认弹窗标题、主按钮都是「确认出库」，弹窗正文又是「…确认提交出库？」——三处措辞不一致，用户读到"提交"与"确认"混用，且与入库页「提交入库」口径分裂 | **已修复（2026-09-18，Kotlin）**：出库链路文案统一到「确认出库」口径——①底部按钮 `submitLabel` 「提交出库」→「确认出库」；②弹窗正文「…确认提交出库？」→「…确认出库？」（标题与主按钮原本已是「确认出库」，未动）。**本次是纯文案改动**：自 BUG-2026-09-18-009 起 `submitLabel` 已降级为纯显示值（库位选择器/拍照取证改由 `showLocationSelector` / `showEvidenceCapture` 显式控制），改文案不影响任何功能开关——已用 T4 断言锁住该约束，防止日后有人把逻辑又挂回文案上。**回归**：新增 `tests/test_bug_2026_09_18_011_outbound_confirm_outbound_label.py` **7 项**（按钮文案已改/代码中旧文案彻底移除/弹窗三处同口径/纯文案改动不影响逻辑/`-009` 测试不再硬编码文案/入库盘点文案未被波及/括号平衡），修复前 **4 项红 → 修复后 7 项全绿**。**附带修正**：`-009` 的 T7 曾把三个文案字面量硬编码进断言，导致本次合法润色会让测试变红（测试在**阻止**改动）——已改为结构性断言（每个调用点传非空文案 + 三页互异），并在 `-011` T5 加断言防止回退。该问题与 `-009` 本身同源（**把显示文案当成逻辑约束**），属 R6 同根因消费点。全量 `tests/` **2086 passed / 85 skipped / 0 failed**（+7 = 新增）。ktlint 语法类错误数与基线一致（ScanScreens 1→1，既有风格告警）。三道门禁 **0 违规**、composable 注解检查通过。**生效条件**：需重新构建并安装 APK（后端无需重启） |

## 每日使用方式

```powershell
# ⚠️ 开工前置门禁：三个工作流全绿才能开始任何任务（AGENTS.md §三）
.\scripts\python.cmd scripts\check_ci_green.py
.\scripts\python.cmd scripts\verify_wms_bugs.py
.\scripts\python.cmd scripts\scan_wms_risks.py
```

规则：
- `check_ci_green.py` 是**开工前置条件**：检查 `Android APK Build` / `WMS AI Verification` / `WMS CI` 三个工作流在 `main` 上最近一次运行是否全绿。**任一非绿即不得开工**，必须先修绿。退出码 0=全绿放行，1=阻断，2=检查本身失败（同样阻断）。
- `verify_wms_bugs.py` 失败才需要立即处理；`scan_wms_risks.py` 输出的是候选风险，必须人工判真后才能进入 BUG 修复。

三个工作流均已配置**每日定时触发**（`cron: '30 18 * * *'` = 北京时间 02:30），即使当天无人推代码也会自动跑一遍，以便发现依赖撤回 / 上游镜像失效这类**时间性故障**。

### 附：CI-GREEN-GATE-2026-09-18 门禁自身的两个「踩坑记录」

门禁工具 `scripts/check_ci_green.py` 首版有两处判断失误，均已实证修正并被回归测试锁住：

| # | 错误 | 症状 | 修正 |
|---|---|---|---|
| 1 | 用**相对路径**请求 workflow-runs 端点（`/actions/workflows/.github/workflows/ci.yml/runs`） | 三个工作流**一律 HTTP 404**。若把 404 当作"非绿"→ 门禁**永久阻断**、谁都不能开工；若当作"绿"→ **漏报放行**。两者都错 | 只用**文件名**（`os.path.basename`）。实证：完整路径 → 404；文件名 → 200；数字 ID → 200。注意 `/actions/workflows` **列表**接口返回的恰是带目录的 `path` 字段，极易照抄致错 —— 已写进代码注释与 T10/T11 测试 |
| 2 | 首版无 token 时靠匿名请求 | 私有仓库场景下必然 404，但与"工作流真的不存在"无法区分 | 明确 token 获取顺序（env → `/root/.wms_gh_token`），并把「无法确认」统一计为**阻断**而非放行 |

> 教训：**门禁工具自己必须被测试**。"检查工具坏了"有两种失效模式，都比"没有工具"更危险 —— 永久阻断会让人绕过规则，漏报放行会让人在红色基线上开工。故 T10（只传文件名 + basename 行为）、T11（**带 token 的真实端到端可达性**，无 token 时 skip）两道断言专防此类失效。
