# WMS 进销存业务与 AI 能力诊断报告

> 诊断日期：2026-09-11　｜　诊断范围：采购、销售、库存三域闭环 + AI 能力成熟度
> 方法：代码实证（模型定义、路由实现、状态机、调用链），不采用通用 ERP 建议
> 配套文档：[`INVENTORY_TRUTH.md`](./INVENTORY_TRUTH.md)（库存三口径）、[`WMS_BUSINESS_SCOPE.md`](./WMS_BUSINESS_SCOPE.md)（业务边界）

---

## 决策摘要（先看这 6 条）

1. **单据流转（采购→入库→库存→出库）主链路是完整且正确记账的**，超收校验、并发扣减、来源追溯都做了。这是资产，不要动。
2. **缺口在"两端"：前端（采购申请/销售的商务财务层）和后端（退货/在途占用/结算）**。系统把"货的移动"记全了，但"钱和权益的变化"基本没记。
3. **AI 能力的真实成色是 5.6%**：319 个 `_ai_*` 函数里只有 18 个真正调用大模型，94.4% 是 SQL 查询 + 阈值判断。9 个名字带"智能/分析"的功能**零 LLM 参与**，13 个 `ai_*` 页面同样**零 LLM**。这不是"AI 不够强"，是**命名与实质不符**。
4. **AI 最该补的不是更多功能，而是四个缺失的闭环**：反馈不回流、置信度不透明、纠错无沉淀、**存在未注册的 AI 接口**（`api_supplier_evaluation` 绕过权限矩阵）。
5. **发现一处重复实现**：补货逻辑有两套（94 行 + 136 行），分别服务两个页面入口、渲染同一模板。
6. **优先级建议**：先修"AI 名实不符 + 治理漏洞"（低成本、高信任收益）→ 再补销售/采购退货与库存占用（业务硬缺口）→ 最后才是财务结算（取决于你是否需要）。

---

## 一、采购业务诊断

### 1.1 已建成的部分（完整）

| 环节 | 实现 | 评价 |
|---|---|---|
| 采购申请 | `PurchaseRequest` + Item，状态 `pending/approved/rejected/completed` | 完整 |
| 采购订单 | `PurchaseOrder` + Item，状态 `pending/partial/completed` | 完整 |
| 收货入库 | `InOrder.source_purchase_order_item_id` 双向追溯 | 完整 |
| 收货进度 | `PurchaseOrderItem.received_quantity` 实时累加 | 完整 |
| **超收拦截** | `should_block_purchase_over_receive()` | **做得对** |
| 反提交回退 | 删除明细时回退 `received_quantity` | 完整（有历史 BUG 已修） |
| 在途计算 | `on_order` 按 `pending/partial` PO 汇总未收量 | 有 |

**采购的"实物链路"是健全的**——从申请到收货再到库存入账，每一步都有追溯，且超收有闸门。这套逻辑能支撑日常运转。

### 1.2 缺口

**(1) 采购退货完全缺失** —— 全仓库仅 1 处提及"退货"，无模型、无单据、无反向库存流。
> 后果：收错料/质量不合格只能靠"入库负数量"或调整单变通，账上看不出这是退货，供应商对账时无法追溯。

**(2) 供应商评估：有实现，但未接入任何入口**

诚实修正：`api_supplier_evaluation`（`app/app.py:32407`）**确实调用 LLM**，且要求配置 API Key 才可用；指标算得不错（交货频率、价格变异系数 CV）。

**但它不在 AI 能力注册表里**——24 项 `AI_CAPABILITY_ROLES` 无对应键，所以**绕过了权限矩阵与 rollout 开关**。对比：`supplier.py` 路由本身无评分字段。

> 这是"有功能、无治理"的状态：一个调用 LLM 的敏感分析接口，没有按 AGENTS.md 的 AI 治理要求注册能力键、风险级别、rollout 控制。

**(3) 采购在途口径不统一** —— `on_order` 在多处独立计算（`app.py:10075`、`17029`、`17188` 等），各自实现"按 `pending/partial` 汇总未收量"。这类重复计算是 `BUG-2026-09-07-013/014/016`「同源全量物化」的温床。

**(4) 无计划性采购** —— 没有 MRP 运算：不根据销售订单/BOM 反推物料需求。补货靠"低于 min_stock 就提示"的阈值触发（见 `_ai_smart_replenishment_report`，纯规则）。

**(5) 补货功能存在两套并行实现** —— `_ai_replenishment_report`（94 行）与 `_ai_smart_replenishment_report`（136 行）**功能重叠、分别服务两个页面入口**（`/ai/replenishment_live` 与 `/ai/replenishment_smart`），渲染的还是同一个模板。同一业务逻辑两份代码，改动必须改两处。

---

## 二、销售业务诊断

### 2.1 已建成的部分

`SalesOrder` 是目前**设计最完整的单据**：含税/未税/税额分离、`Numeric(18,2)` 精度、`shipped_amount`/`remaining_amount`、`shipment_status`（`pending/partial/shipped`）、业务员/项目号/币别/结算方式/账期相关字段。

> 注意：**销售订单不直接扣库存，发货才是仓库动作**（代码注释明确写了）。这个职责分离是对的——避免销售承诺直接改库存。

### 2.2 缺口

**(1) 销售退货入库不存在（严重）** —— 有 `AfterSaleOutOrder`（售后**出库**），但**没有反向的退货入库单**。

```
现状：  客户退货 → 货物回到仓库 → ❓ 系统里无单据可录
                             → 只能走"其他入库"，丢失"这是退货"的语义
```

`AfterSaleOutOrder` 有 `source_sales_order_id`、`reason`、`responsibility`，说明售后场景被设计过，但**只做了往外发的方向**。退货入库是进销存的标准闭环，缺失会导致：库存对得上但业务链断掉，退货率、质量成本无法统计。

**(2) 无销售订单占用/预占（严重）** —— 全库搜索"reserved/占用/预占 + 库存"**零命中**。

> 后果：接了销售订单不占库存，同一批货可以被两个订单同时承诺。可用量 = 账面库存 − 已承诺未发。系统只有前半段。

这是**与今天新加的 A11 规则同源的问题**：库存口径不完整。A11 管住了"仓库维度"，但"占用维度"根本还没建。

**(3) 缺发货计划与缺货预警联动** —— `sales.py:1037` 有"交期已过未发货"判断，但未见与库存可用量联动的"这张单能不能按时发"预警。

**(4) 无客户信用管理** —— `Customer` 无信用额度、无账期检查。`sales.py` 里"应收"仅 5 处提及，未成体系。

---

## 三、库存业务诊断

### 3.1 已建成的部分（这是全系统最扎实的一块）

- **三口径账本**：流水（事实）+ 库位账（仓库级）+ 总账（全局），且今天已用 `INVENTORY_TRUTH.md` 固化了权威关系与派生方向
- **原子并发控制**：`deduct_stock_atomic` 条件 UPDATE + rowcount 检查，`add_location_inventory_atomic` 用 SAVEPOINT 而非整事务回滚
- **多仓库隔离**：`warehouse_id` 外键 + 回填 + "不猜"铁律
- **盘点**：`InventoryCheck` + `InventoryCheckScan`，有漏盘软拦截、冻结期动销预警
- **调拨**：双向流水 + 源仓库存校验（防"空手套白狼"）
- **期初建账**：`OpeningStock` 同步总账与库位账

配套防护也强：A1–A11 lint、R1–R6 规则、366 个测试文件、322 条 BUG 台账。**库存域已经进入"精细维护"阶段，不是"救火"阶段。**

### 3.2 缺口

**(1) 无批次/序列号管理（已知取舍）** —— `WMS_BUSINESS_SCOPE.md` 明确列为"暂不作为目标"。**建议保持不做的决定**，但需注意：`Material.expiry_date`/`alert_days` 字段存在却无批次支撑，保质期预警只能按物料级粗判。

**(2) 无销售占用（与 §2.2 同一问题）** —— 库存的"可用量"概念不完整。

**(3) 库存成本口径是估值不是核算** —— `Material.price` 单一当前价；BOM 成本 `calculate_bom_cost_value` 直接取 `material.price`；月报 `BUG-2026-09-07-005` 已修成"出入库按单据实际单价、期末按当前价估值"。**这是明确取舍，建议在文档里写明"不提供成本核算"，避免被当财务数据。**

**(4) 客供料不拆账** —— 今天已明确为规则（CS-RULE-001），保持。

---

## 四、AI 能力诊断（重点）

### 4.1 真实成色：硬数据

```
_ai_* 函数总数              319
  含真实 LLM 调用            18   (5.6%)
  纯 SQL 查询 / 阈值判断      301   (94.4%)

命名带"智能/分析"的函数        9
  其中真实 LLM 参与            0   (0%)

ai_*_page 页面数            13
  页面函数内调用 LLM           0   (0%)  ← 全部由 _ai_* 数据函数驱动
```

**24 项 AI 能力注册在权限矩阵里**，风险分级：`draft` 11 个、`read` 12 个、`sensitive_read` 1 个。

**但至少 1 个真实调用 LLM 的接口未注册**：`api_supplier_evaluation`（见 §1.2(2)）——有功能、无治理。

### 4.2 做对的地方（不要动）

1. **AI 边界焊得很死** —— `AI_FORBIDDEN_RISK_LEVELS = {submit, audit, complete, void, delete, stock_write}`，AI 只能建 `status='pending'` 草稿，提交/审核/完成/作废/删除永远人工。
2. **降级设计完整** —— `providers.py` 有熔断器（`failure_threshold=3`）、指数退避重试、`call_llm` 返回 `None` 时回退本地规则。LLM 挂了系统不会崩。
3. **治理层齐全** —— 能力键四处注册（三个 policy dict + `AI_TOOL_REGISTRY`）、rollout 开关（off/allowlist/role/all）、24 个工具的 JSON Schema 校验。
4. **R5 要求"低置信度回退人工，禁止编造"** —— 这条规则是对的，且今天做的语音建单已落实（NOT_FOUND 态展示"听成什么+试过什么+最接近候选"）。

### 4.3 五个真实不足

#### 不足 1（最严重）：名实不符——"AI"功能实际不含 AI

**证据链**：
- `_ai_smart_replenishment_report`：`if recent_qty > prior_qty * 1.15` 判趋势，**无 LLM**
- `_ai_analysis_low_stock_report`：`Material.query.filter(low_stock).order_by(...)`，**无 LLM**
- `_ai_sf_query_short_stock`：一条 SQL join + Python 循环比较，**无 LLM**
- **13 个 `ai_*_page` 页面函数内 LLM 调用数为 0**，全部由 `_ai_*` 数据函数驱动
- 智能补货页面 UI 有"AI建议"列，内容由规则生成

> **危害**：用户看到"AI建议"会赋予它超出实际的信任。当规则给出错误结果时，损失的是**用户对系统的整体信任**，而不只是这个功能。这是比"功能弱"严重得多的问题。

> **建议（最高优先级，成本低）**：
> - 把纯规则功能的文案从"AI建议"改为"补货建议（基于库存阈值与消耗趋势）"等**如实描述**
> - 保留真正的 AI 标识给 18 个真实调用 LLM 的能力
> - 或反向：给这些规则功能真正接入 LLM 做解释层（把计算结果交给 LLM 生成"为什么这样建议"）

#### 不足 2：反馈不闭环——有反馈表，没有回流

**证据**：`AIFeedback` 表有 `rating`/`error_type`/`note` 字段，`create_feedback` 能写库，`list_feedbacks` 能查——**但全库没有任何地方读取反馈去改进系统**（调整别名、修正规则、优化 prompt 都不做）。

> 这是"有仪表盘没方向盘"。用户点了"差评"，这个信号就沉进数据库了。

> **建议**：打通最小闭环——把 `rating='bad'` + `error_type` 的样本定期汇总成"待修清单"，尤其是 **`AIMaterialAlias`（物料别名）**：用户纠正"电线→电缆"这类错误，应该**自动沉淀为别名映射**，下次就对了。这是最高性价比的 AI 改进点。

#### 不足 3：置信度不透明——用户不知道 AI 有多确定

**证据**：`_ai_*` 函数普遍返回结构化数据，但**没有统一的置信度字段**；规则类功能更没有置信度概念。

> 语音建单（今天做的）有 `score` 和"模糊匹配"徽标，这是正确的做法，但**没有推广到其他 AI 能力**。

> **建议**：凡是 AI/规则给出的建议，都带上"依据 + 可信度"，让用户知道什么时候该人工复核。这正好落实 R5。

#### 不足 4：无记忆、无个性化——每次都从零开始

**证据**：有 `history.py`（对话历史）和 `knowledge.py`（知识库），但**没有"用户偏好/仓库习惯"的记忆**。系统不知道"这个用户习惯从哪个仓领料"、"这个采购员偏好哪家供应商"。

> 对比：今天的语音建单需要用户**手工输入领料人**（因为你说"领料人由我手工输入"）——这正是缺记忆的体现。

> **建议**：先做**高频默认值记忆**（最近使用的仓库/领料人/供应商），成本低、体验提升明显，且不涉及隐私敏感。

#### 不足 5：AI 与业务闭环脱节——建议无法一键执行

**证据**：AI 能识别缺货、能建议补货，但"从建议到建单"需要用户手动重建。`replenishment_smart` 有 CSV 导出，说明**终点是导出**。

> **建议**：让 AI 建议能直接生成草稿（符合"AI 只建 pending 草稿"的边界），把"看数据→手工录单"变成"确认→改草稿"。这是 AI 在 WMS 里最大的价值释放点。

#### 不足 6：治理有漏洞——存在"未注册的 AI 接口"

**证据**：`api_supplier_evaluation` 真实调用 LLM，但**不在 24 项 `AI_CAPABILITY_ROLES` 中**，因此绕过权限矩阵、风险分级、rollout 开关。

> 这是 AGENTS.md AI 治理体系的漏网之鱼。**建议全量扫描所有调用 `call_llm*` 的端点，逐一核对是否已注册能力键**（这本可以做成一条 lint 规则）。

#### 不足 7：同一业务两套实现——补货逻辑重复

**证据**：`_ai_replenishment_report`（94 行）与 `_ai_smart_replenishment_report`（136 行）功能重叠，分别服务 `/ai/replenishment_live` 与 `/ai/replenishment_smart` 两个入口，**渲染同一个模板**。

> 与 `/ai/inventory_health_live` vs `/ai/inventory_health` 同样的模式（`_live` 后缀 + 非 `_live` 版本）。改动必须改两处，迟早不一致。

### 4.4 一个必须说的问题：94.4% 规则化不全是坏事

我要公平地说：**在 WMS 场景里，规则化往往比 LLM 更可靠**。"库存低于安全库存就预警"用 LLM 做才是错的——慢、贵、还会编。

真正的问题是**没有区分**：系统把"确定性的库存计算"和"需要理解的模糊判断"混在一个叫"AI"的筐里。正确做法是：

| 类型 | 该用什么 | 例子 |
|---|---|---|
| 确定性计算 | **规则**（现在的做法对） | 库存数量、可用天数、超收校验 |
| 模糊理解 | **LLM**（该用但用得少） | 语音/OCR 识别、语义匹配、异常归因 |
| 解释与建议 | **LLM 做解释层**（现在缺） | "为什么这个料要补货"、把数据翻译成人话 |

---

## 五、改进方案（按优先级）

### P0 — 立刻做（低成本，高信任收益）

| # | 动作 | 理由 |
|---|---|---|
| 1 | **AI 名实对齐**：纯规则功能改名/改文案，如实描述依据 | 消除"AI 名实不符"，保护用户信任 |
| 2 | **反馈闭环最小版**：差评→待修清单；物料别名纠错自动沉淀 | 让反馈真正改善系统 |
| 3 | **建议带置信度**：所有 AI/规则建议标注依据与可信度 | 落实 R5，用户知道何时复核 |
| 4 | **补齐 AI 治理漏洞**：扫描所有 `call_llm*` 端点，未注册能力键的一律登记 | `api_supplier_evaluation` 已证实漏网 |

### P1 — 补齐业务硬缺口

| # | 动作 | 理由 |
|---|---|---|
| 5 | **销售退货入库单**（新建反向单据，关联原销售单） | 标准闭环缺失，退货无法统计 |
| 6 | **销售订单库存占用**（已承诺未发量） | 可用量口径不完整，会超卖 |
| 7 | **采购退货单** | 质量退货只能变通处理 |
| 8 | **合并重复的补货实现**（两套 → 一套） | 同逻辑两处代码，迟早不一致 |

### P2 — 增强分析能力

| # | 动作 | 理由 |
|---|---|---|
| 9 | 采购在途计算**收口为单一函数** | 多处重复实现，是反复 BUG 温床 |
| 10 | 供应商履约指标（到货准时率、合格率） | 需先给 `PurchaseOrderItem` 加实际到货日期；`api_supplier_evaluation` 已有 CV 指标可复用 |
| 11 | AI 建议→一键生成草稿 | 释放 AI 在 WMS 的最大价值 |

### P3 — 按需（取决于是否要财务口径）

| # | 动作 | 理由 |
|---|---|---|
| 12 | 应收/应付/账期 | 当前基本空白，但**可能是正确的取舍**——单人记账场景未必需要 |
| 13 | 客户信用额度 | 同上 |
| 14 | 批次/序列号 | 文档已明确不做，建议保持 |

---

## 六、一句话结论

> **这个系统的"物流"是健全的，"商流"和"资金流"是缺失的；AI 的"治理框架"是优秀的，"智能实质"是被高估的——319 个 AI 函数里 94.4% 是规则，9 个叫"智能"的、13 个叫"AI"的页面，没有一个用大模型。**
>
> **最该先做的不是加功能，而是让 AI 名副其实、让反馈真正回流、补齐未注册的 AI 接口，再把销售退货和库存占用这两个标准闭环补上。**
> **规则化本身不是缺点——把规则叫成 AI 才是。**

---

## 附：本报告的自我修正记录

诊断过程中有两处初判被实证推翻，记录在此以保证可复核：

| 初判 | 实际 | 修正依据 |
|---|---|---|
| 供应商评估"零实现" | 有 LLM 驱动实现（`api_supplier_evaluation`），但未注册能力键 | 全库扫描 `call_llm` 端点时发现 |
| 两个 `_live_page` 模板缺失会 500 | 模板存在（渲染同一 HTML），是**重复路由**而非缺失 | `sed` 核对 `render_template` 实参 |

> 保留这两条是为了说明：本报告的结论都落在代码上，能被证伪也能被复核。
