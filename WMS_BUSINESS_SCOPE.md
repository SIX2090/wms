# WMS 业务口径说明（单人使用 · 精简版）

> 文档来源：业务方与开发的产品对齐结论  
> 日期：2026-07-26  
> 原则：**一个人用，不搞复杂；大白话就是登记物料出入库。**

---

## 1. 系统是干什么的

本 WMS 的核心就是：

| 动作 | 含义 |
|------|------|
| **入库** | 登记什么料、多少数量、进哪个仓；需要时带上合同编号、工程名称 |
| **出库 / 领料** | 登记什么料、多少数量、从哪个仓出；需要时带上合同编号、工程名称 |
| **库存** | 进出登记之后，还剩多少 |

**记清楚、查得到**即可。  
不为多人协作、不为严格审计、不为客户资产账另建一整套复杂体系。

---

## 2. 使用场景

- **使用人数**：一个人用  
- **目标**：日常把物料进出登记好，能按需要查  
- **不做**：大企业式库存所有权隔离、多角色审批、客供全生命周期模块、过度主数据治理

---

## 3. 客供料怎么理解、怎么做

### 3.1 业务理解

- 客供料一般是 **入多少、出多少** 的过手料，不长期当公司自有库存来经营。  
- 其他入库明细可以勾选 **客供** 并选择客户，用于登记和页面识别。
- 当前仍不建立 `owner_type` 库存所有权余额账；客供勾选不代表已经实现自购/客供库存隔离。
- 若一定要和自购料在存放上分开：**入库时选客供仓**即可。  
- 业务上真正重要的是单据上选好 **合同编号、工程名称**，方便按项目对进出。

### 3.2 日常操作约定

1. **客供到货**  
   - 做入库（如其他入库）  
   - 需要分仓时：仓库选 **客供仓**  
   - 在对应物料明细行勾选 **客供**，并选好客户、合同编号和工程名称

2. **客供领用**  
   - 做领料 / 出库  
   - 从同一客供仓出更清晰（入多少出多少）  
   - 尽量同一 **合同、工程**  

3. **自购料**  
   - 照常入材料仓、从材料仓领  
   - 合同 / 工程按项目需要填写  

### 3.3 客供料业务规则（CS-RULE-001，2026-09-11 明确）

> **本节把"客供勾选"从"半实现状态"明确为一条确定的业务规则。**
> 此前代码对客供料部分实现了语义（下推可用量恒 0 + 后端 409 拦截），
> 但文档只说"不做所有权隔离"，读者无法判断这是"没做完"还是"就该这样"。现在拍板。

**规则**：

| 项 | 结论 |
|---|---|
| 客供料能否下推为出库类单据（领料/销售等）？ | **不能**。后端硬拦截（409），前端可用量显示 0 |
| 客供料库存是否与自有料拆账？ | **不拆**。仍合并在同一仓库账内，`material.stock` 包含客供数量 |
| 是否建立 `owner_type` 所有权余额账？ | **不建立**（与 §3.3 一致） |
| 客供料怎么区分存放？ | 靠**仓别**（入库时选客供仓）+ **合同编号/工程名称** |
| 客供料怎么出库？ | 在出库单中**直接选客供仓**并关联对应合同/工程，不走"下推" |

**为什么是"不能下推"而不是"拆账"**：

- 拆账（所有权余额账）重，单人场景不划算 → 不做（见 §3.3）。
- 但不拆账 ≠ 可以随便当自有料用。客供料是**客户资产**，过手登记的本义就是
  「入多少、出多少」，转成公司自有出库会消耗客户资产却不留痕。
- 所以采取**最小干预**：只堵住"下推"这一个高危入口，其余（库存数量、查询、
  报表）保持合并口径，不引入所有权维度。**这是规则，不是欠债。**

**代码落点**（改动时须同步）：

- 展示：`app/routes/in_order.py:361` — 客供行 `available_quantity` 恒为 0
- 拦截：`app/routes/in_order.py:~458` — 下推时客供行返回 409
- 相关：`INVENTORY_TRUTH.md`（库存三口径，说明为何不拆账）

### 3.4 明确不做的方向

| 不做 | 原因 |
|------|------|
| 库存所有权隔离（自有/客供拆账、普通领料禁止扣客供） | 单人登记场景不需要；靠仓别 + 合同工程即可 |
| 独立客供业务闭环（退回客户、核销、状态机等） | 过重；当前就是出入库登记 |
| 把客供勾选宣传成完整所有权隔离 | 当前勾选只用于登记识别；仍以客户、仓库、合同和工程辅助区分 |

---

## 4. 合同编号与工程名称

- 属于 **登记与查询信息**：方便按合同、工程归集入库和领料。  
- 已在基础资料中提供 **合同/工程档案**。采购入库、其他入库、领料和其他出库的合同编号、工程名称属于明细行，同一单可对应不同合同和工程。
- **不是**库存所有者维度，也不强制与库存唯一键绑定。

---

## 5. 对外部“11 条问题”的取舍（单人口径）

此前审计类问题（客供所有权、批次全链路、库位主数据重构、生产工单闭环、审计同事务等）多针对 **多人、强审计、防误耗** 场景。

在本项目 **单人 · 只做出入库登记** 的前提下：

| 类别 | 态度 |
|------|------|
| 客供所有权 / 客供全模块 | **不做**（见第 3 节） |
| 批次/序列进仓储账、正式库位主数据大改、生产闭环、字段配置服务端化等 | **暂不作为当前目标**；有明确痛点再单项做 |
| 小修（如合同筛选与明细一致、完成单慎删等） | **有空再改**；不阻塞日常登记使用 |

**当前优先级**：保证入库、领料、库存数量登记正常；合同/工程能选能查。

---

## 6. 产品决策归档（原 3 问 → 已拍板）

| 原问题 | 结论 |
|--------|------|
| 客供库存是否按客户+合同+工程进余额唯一键？ | **否**。不做客供库存所有者维度。 |
| 历史库存如何拆分客供/自有？ | **无需按所有权回放**。历史继续当普通库存数量；新业务靠仓 + 合同工程登记。 |
| Phase A 止血 vs 完整客供模块？ | **两者都不做客供所有权方案**。保持简单出入库；客供用仓 + 合同/工程即可。 |

---

## 7. 一句话总结

> **大白话：登记物料出入库。**  
> 一个人用，选好仓库（需要时用客供仓），填好合同编号和工程名称，把进和出记清楚、查得到就行。  
> 不做复杂的客供所有权、审批流和重型仓储治理。

---

## 8. 相关已实现能力（便于对照）

- 基础资料：**合同/工程档案**（合同编号、工程名称等）  
- 单据：采购入库、其他入库、领料、其他出库等明细可带合同编号、工程名称
- 客供登记：其他入库明细可勾选客供并关联客户；客供行暂不允许下推普通出库
- 采购入库：允许手工录入，采购订单仅为可选来源；有关联订单时保留来源和数量跟踪
- 仓库主数据：可自行维护「客供仓」等仓库，业务上约定使用  

具体菜单与字段以系统当前界面为准。
