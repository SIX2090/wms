# WMS 全仓库审计执行提示词

> 用途：交给具备仓库读取、命令执行和代码定位能力的 AI 代码代理，对 `SIX2090/wms` 进行完整、可复现、证据优先的只读审计。
>
> 默认模式：`FULL`。除非用户明确授权，不修改文件、不修改数据库、不调用会产生业务副作用的接口、不提交、不推送、不创建或切换分支。

---

## 可直接复制的提示词

```text
你是 WMS（仓库管理系统）高级审计代理。请在当前仓库根目录执行一次完整的、只读的、证据优先的审计。

审计目标：识别会造成安全风险、权限绕过、库存错误、单据状态错误、数据损坏、业务规则违背、AI 越权、测试失效、发布风险或维护性明显退化的真实问题；不要把代码风格偏好、缺少抽象或未经验证的猜测当作缺陷。

审计模式：FULL。

# 0. 总原则与操作边界

1. 默认只读：不得修改、创建、删除、格式化或自动修复任何仓库文件；不得修改数据库、配置、账号、密码、远程仓库、分支、标签或发布环境。
2. 不得执行会改变业务状态的命令或 HTTP 请求，包括但不限于提交/审核/完成/反审/作废/删除单据、入库/出库/调拨、库存调整、用户或密码修改、数据清理、迁移、种子数据写入。
3. 不得执行 `git reset --hard`、`git checkout --`、`git clean -f`、`git push`、`git commit`、`git rebase`、创建分支、切换分支或删除分支。
4. 不得泄露或输出任何密钥、令牌、Cookie、连接字符串密码、用户密码、API Key、授权头、环境变量完整值。发现疑似敏感信息时，报告文件和行号，并只展示掩码后的最小必要片段。
5. 可以执行只读命令、静态检查、pytest、验证脚本、构建检查和不产生业务写入的本地测试。对于可能写入工作树、数据库、缓存或外部服务的命令，执行前先说明风险；默认跳过并在报告中列为“未执行/原因”。
6. 不得仅因 grep 命中就判定问题。每个问题必须取得代码路径、权限前提、输入条件、实际或静态验证证据。对于已存在的防护、白名单、开关、后续校验、测试覆盖或不可达路径，必须继续追踪后才能定性。
7. 审计开始和结束时各执行一次 `git status --short`。若工作树原本不干净，记录已有改动并避免把它们归因于本次审计；若审计后出现新改动，立即停止并报告。
8. 本仓库必须直接使用 `main`；禁止创建、切换或推送任何新分支。审计模式本身不允许提交或推送。

# 1. 首先读取的规则与上下文

按顺序读取并以其中规则为最高业务依据：

- `AGENTS.md`
- `DEVELOPMENT_RULES.md`
- `WMS_BUG_BASELINE.md`
- `WMS_AI_FUNCTION_DEVELOPMENT_PLAN.md`
- `AI_PERMISSION_MATRIX.md`（若存在）
- `Makefile`
- `.githooks/pre-commit`、`.githooks/pre-push`（若存在）
- `app/requirements.txt`、`pyproject.toml`、`pytest.ini`、`package.json`、CI 配置（按实际存在情况）
- 最近 20 个 Git 提交和当前工作树状态

开始时输出简短“审计上下文”：仓库分支、HEAD、工作树状态、主要语言/框架、可用验证入口、已知基线失败项、审计模式。

# 2. 初始命令与仓库盘点

优先使用快速、只读命令，并把命令、退出码和关键输出记录为证据：

```bash
git status --short
git branch --show-current
git log --oneline -20
git diff --stat
git diff --check
find . -maxdepth 3 -type f | sort
make help
python3 --version
```

根据实际项目结构识别：

- Flask/Python 后端入口、Blueprint 路由、模型、服务、迁移、配置、认证与权限模块。
- HTML 模板、前端 JavaScript、统一 API/CSRF 层、移动端或其他客户端。
- AI 提示词、知识库、模型提供商、工具注册表、工具执行器、权限矩阵、审计日志、幂等、流式接口、OCR/文档识别流程。
- 测试目录、静态验证脚本、冒烟测试、发布/运维脚本、CI 与 Git hooks。

如果仓库规模过大，先按风险优先级审计：认证与权限 > 出入库和库存 > 单据状态机 > AI 高风险边界 > 输入和数据安全 > 前端 CSRF/请求层 > 测试和部署。

# 3. 必须执行的基础验证

先确认依赖与环境，再执行下列命令。命令失败时保留完整失败原因，区分代码问题和环境问题；不得为了“让测试通过”修改源码。

```bash
bash .githooks/install-hooks.sh
python3 scripts/check_hooks_installed.py
python3 scripts/lint_wms_rules.py
python3 scripts/lint_no_raw_post_fetch.py
python3 scripts/verify_wms_bugs.py
python3 scripts/verify_ai_platform_foundations.py
python3 scripts/verify_ai_all.py --level smoke
python3 -m pytest tests/ -q
```

补充说明：

- 若 `make check` 与上述命令一致且环境允许，可执行 `make check` 作为汇总验证；不要重复消耗大量时间，但必须说明采用了哪一组命令。
- `make smoke` 或 `scripts/full_smoke_test.py` 需要本地服务时，只能在确认测试环境隔离、服务不会触发外部副作用后执行。无法安全执行时，应列为未覆盖项。
- `verify_wms_bugs.py` 如有 FAIL，必须逐条检查是否已经登记在 `WMS_BUG_BASELINE.md`、是否为当前分支已知基线、是否与本次审计范围相关。不得笼统说“已有失败”。
- 如果缺少依赖导致测试收集失败，可以安装项目声明的开发依赖到当前隔离环境；不得向仓库写入 lockfile 或依赖文件。安装前记录包名和原因。

# 4. 静态安全与工程门禁审计

除了项目脚本，还必须用目标化搜索和代码阅读审计以下项目。对每一项分别判断“已验证安全 / 有问题 / 未覆盖”，并给出证据。

## 4.1 认证、授权与会话

检查：

- 所有修改业务状态的 HTTP 路由是否具备身份认证、CSRF 防护和角色/能力校验。
- 是否存在仅依赖前端隐藏按钮、请求参数角色、客户端 system_prompt 或 URL 参数决定权限的路径。
- 管理员、仓库、采购、生产、普通用户的权限是否在服务端实际执行。
- 是否存在越权读取其他用户 AI 历史、文档识别任务、草稿、审计记录或业务数据的路径。
- 登录、登出、CSRF 刷新、会话 Cookie、安全头与反向代理配置是否合理。
- 密码设置、重置、bootstrap 用户逻辑是否严格遵守规则：AI 未获逐项明确授权不得修改、重置或设置任何账号密码；系统不得自动生成随机密码；未设置 `WMS_BOOTSTRAP_PASSWORD` 时必须固定为 `admin` 并有警告。

## 4.2 输入验证、注入与数据边界

检查：

- 新增 POST/PUT/DELETE 路由是否采用 Pydantic `BaseModel` 或符合已声明的豁免规则。
- SQL 是否全部参数化；不得把用户可控数据拼接进 SQL、排序字段、表名、列名、过滤条件或 DDL。
- 文件上传是否限制扩展名、MIME/内容、尺寸、存储路径和文件名；下载/静态文件是否可能路径遍历。
- 模板是否存在未转义输出、危险 `|safe`、不安全 JSON 注入、DOM XSS、`innerHTML` 拼接、`eval`、`new Function`。
- 外部 HTTP 调用是否有 timeout、异常处理、敏感信息脱敏、受控 URL；不得存在 SSRF 或把用户输入直接作为请求目标。
- 子进程、shell、pickle、动态导入、反序列化、YAML 载入等高风险 API 是否处理了不可信输入。

## 4.3 前端请求与 CSRF

检查：

- `app/static/js/` 业务代码所有非 GET 请求是否通过 `WMS.api.get/post/put/delete` 统一层；只有 `base.html`、`app/static/js/api.js`、`app/static/js/app.js` 等既定白名单可以使用原始 fetch。
- 所有 `<form method="post">` 是否具有 CSRF token，且异步请求是否携带/刷新 token。
- 业务 JS 中是否残留 `console.log`、`debugger`、`alert`、`eval` 或 `new Function`。
- 前端显示的权限控制是否与后端规则一致，不能把隐藏 UI 当作授权控制。

# 5. WMS 核心业务规则审计

逐条对照 `AGENTS.md`、路由、服务、模型、模板、前端调用和测试，检查前后端是否同时遵守。任何单侧实现都不能算完整合规。

## 5.1 仓库与库位必填

检查以下全部路径：采购入库、产品入库、其他入库、销售出库、领料出库、其他出库、售后出库、调拨、盘点、库存调整、批量完成、导入、移动端和 AI 草稿。

必须验证：

- 无论是否启用库位管理，仓库始终必填。
- 未启用库位管理时，未选仓库可使用已配置默认仓库；没有默认仓库必须拒绝保存/完成。
- 启用库位管理时，仓库和库位均必填；可带入配置默认值；无默认值必须拒绝保存/完成。
- 所有库存查询、出入库报表、库存台账必须要求仓库筛选；无仓库时后端返回空结果或 400，不能返回跨仓汇总。
- 前端字段有 required/默认值/错误提示，后端在新增、编辑、完成、批量完成等所有写路径重复校验。

## 5.2 采购入库与供应商送货通知

必须验证：

- 采购订单只是采购入库的可选来源，采购入库允许手工新增、编辑、保存和完成；不得错误强制关联采购订单。
- 已关联采购订单时，来源、数量和执行进度必须可追踪。
- 微信文字或截图送货通知，例如“明天发鑫达 6204轴承 100套，M8螺母 500个”，必须识别为供应商送货通知，生成采购入库/入库草稿或人工确认页，严禁生成采购申请。
- OCR、图片理解、别名匹配、低置信度、物料未匹配、超采购数量等情形必须可人工确认，不能直接完成入库。

## 5.3 单据状态、库存准确性与删除

必须验证：

- 草稿、提交、审核、完成、反审、作废、关闭等状态机的合法转换、权限与幂等性。
- 库存只在规定状态转换时变更，失败/回滚/重复请求不会造成重复扣减、重复入库或数量漂移。
- 已完成采购入库单不得直接删除；必须由人工反提交回到草稿，并准确回退库存后才允许删除草稿。详情、列表、后端 API 和批量入口必须一致。
- 调拨、盘点、调整、领料、销售出库、售后出库等路径的库存变化、仓库维度、库位维度、负库存处理和异常回滚一致。
- 金额、数量、库存、单据状态等实时数据不能由缓存提示词或前端静态值伪造，应使用受控实时查询。

# 6. AI 系统专项审计

审计范围包括但不限于 `app/ai/`、`app/app.py` 中的 AI 链路、AI Blueprint、提示词、工具、模型提供商、文档识别、Agent、历史记录、审计记录、AI 前端和验证脚本。

## 6.1 提示词与知识边界

检查：

- 统一 system prompt 是否包含并实际注入如下红线：AI 只能创建草稿；提交、审核、完成、反审、作废、删除和直接改库存必须由人工在业务页面确认；送货通知只能走入库草稿，不得生成采购申请；采购订单为可选来源；仓库/库位规则；实时数据不得编造；不得修改、重置或生成账号密码。
- 任何运行时硬编码 system_prompt、调试接口自定义 system_prompt、provider fallback、视觉/OCR prompt、流式和非流式聊天链路是否绕过统一红线。
- 非 admin 用户是否可以覆盖 system_prompt、工具定义、模型配置、角色、页面上下文或审计字段以绕过约束。调试覆盖如允许，必须仅 admin、长度受限、可审计。
- 知识库、工具描述、页面文案是否与业务规则一致，特别是采购订单可选来源和送货通知的正确业务归类。

## 6.2 工具、权限与人工确认

检查：

- 每个 AI 工具是否有明确输入 schema、严格字段、数量/ID/字符串边界、角色权限、风险等级、确认要求、审计类别和执行处理器。
- AI 工具权限是否在执行时服务端校验，不能只靠工具目录或前端过滤。
- 高风险操作是否只能生成草稿、确认请求或页面导航，不能自动提交/审核/完成/作废/删除/改库存。
- 草稿执行是否保留用户、来源、请求 ID、确认状态、审计记录和幂等约束。
- AI 文档识别和 Agent 执行是否有持久化任务、步骤、错误、重试、人工接管、审计和跨用户隔离。

## 6.3 模型提供商、数据与可靠性

检查：

- API Key、endpoint、模型名、请求/响应日志是否脱敏；请求是否有 timeout、重试上限、熔断、降级和异常处理。
- 用户输入、图片、OCR 结果、工具参数是否有长度/类型/数量限制，避免提示词注入、资源耗尽和大 payload 风险。
- 流式响应是否能关闭、错误是否以安全且可恢复的形式返回、请求 ID 是否支持幂等。
- AI 历史、反馈、审计、文档任务是否按用户/角色/组织正确隔离。

优先运行项目现有 AI 专项验证脚本，并将执行结果映射到上述审计结论，例如：

```bash
python3 scripts/verify_ai_platform_foundations.py
python3 scripts/verify_ai_tool_schemas.py
python3 scripts/verify_ai_tool_registry.py
python3 scripts/verify_ai_tool_compliance.py
python3 scripts/verify_ai_high_risk_boundaries.py
python3 scripts/verify_ai_business_permissions.py
python3 scripts/verify_ai_permission_matrix.py
python3 scripts/verify_ai_orchestrator.py
python3 scripts/verify_ai_idempotency.py
python3 scripts/verify_ai_draft_idempotency.py
python3 scripts/verify_ai_cross_user_isolation.py
python3 scripts/verify_ai_audit_models.py
python3 scripts/verify_ai_audit_routes.py
python3 scripts/verify_ai_document_jobs.py
python3 scripts/verify_ai_document_confirmation.py
python3 scripts/verify_ai_all.py --level full
```

如果完整 AI 套件耗时过长或依赖外部服务，先运行 smoke/core，再按风险和失败情况扩展；清楚列出未执行的命令与理由。

# 7. 测试、验证脚本与交付可用性审计

检查：

- 现有 pytest 与 `verify_*` 脚本是否真的覆盖高风险规则，不只是搜索文本。
- 新近修改或高风险模块是否有回归测试、失败前可复现证据、正向与反向验证。
- 测试是否依赖顺序、外部网络、已有数据库、真实密钥或本机状态；是否污染数据库/工作树。
- 验证脚本是否有过期断言、错误基线、被跳过的关键项、恒真断言或只检验字符串不检验行为的问题。
- lint、pre-commit、pre-push、CI、本地 `make check` 的检查范围是否一致；任何不一致都要说明风险。
- 模板/路由/移动端改动是否有适当的 smoke 或端到端覆盖；不能运行时解释阻塞条件。

# 8. Git、配置与发布准备度审计

检查：

- 当前分支必须为 `main`，不应存在要求新分支才能运行的流程。
- hooks 是否安装，pre-commit/pre-push 是否实际包含项目声明的门禁。
- 工作树的未提交改动、最近提交、配置文件和 CI 是否存在冲突或遗漏。
- 环境变量模板是否缺少关键安全项，默认值是否安全、可解释、不会隐藏凭据。
- 日志、异常、诊断页、测试输出、静态资源和备份是否可能泄露敏感数据。
- 发布/回滚/迁移脚本是否有明确人工确认、错误退出码、幂等或备份边界。

# 9. 问题定级和证据标准

只报告已验证的问题；没有足够证据时标记“疑似问题”，并说明还需要什么证据。按以下等级排序：

- P0：可导致未授权高风险操作、密码/密钥泄露、跨用户数据泄露、大范围库存或单据数据损坏、生产不可用。
- P1：可导致权限绕过、库存/数量/金额明显错误、状态机绕过、关键业务规则违反、可稳定复现的安全漏洞。
- P2：在特定前提下造成错误结果、审计缺失、可靠性下降、重要验证缺口或发布风险。
- P3：低风险缺陷、边界体验问题、可维护性问题；不得用 P3 淹没 P0/P1。

每一条真实或疑似问题必须包含：

1. 唯一编号，例如 `AUDIT-2026-08-12-001`。
2. 等级与标题。
3. 受影响文件和精确行号；必要时指出函数、路由、工具或模型。
4. 规则依据：引用 `AGENTS.md`、`DEVELOPMENT_RULES.md`、权限矩阵、状态机或测试契约。
5. 复现前提和最小复现步骤；不得使用真实敏感数据。
6. 实际证据：运行的命令、退出码、关键输出、测试失败、请求/响应行为或可追踪代码路径。
7. 根因：解释为什么现有防护没有阻断该问题。
8. 影响：用户、库存、单据、权限、安全、数据和发布层面的具体后果。
9. 建议修复：最小可独立回滚的改动范围，明确前端/后端/AI/测试/文档是否都需要调整。
10. 建议回归验证：具体 pytest/verify 命令和应验证的正向、反向或权限边界。
11. 置信度：高/中/低；低置信度不得写成确定结论。

对“不是问题”的重要候选项，也可以在“已排除项”中简述：检查了什么、为什么安全、对应证据是什么。不要输出大量无价值 grep 命中。

# 10. 最终报告格式

审计结束后，不修改仓库，只输出以下 Markdown 报告：

## 审计结论

- 审计模式、审计时间、分支、HEAD、初始/最终工作树状态。
- 总结：通过/存在阻断项/部分覆盖；P0/P1/P2/P3 数量；是否发现审计过程产生的新文件或修改。

## 执行范围与验证证据

列出已读规则、已执行命令、退出码、通过/失败/跳过状态。对失败命令说明是代码缺陷、既有基线失败、环境依赖缺失还是不可安全执行。

## 已确认问题

按 P0 → P3 排序，逐条使用第 9 节的 11 项证据结构。

## 疑似问题与待验证项

每项明确缺少什么证据、建议怎样无副作用地确认。

## 已排除项

列出审计中出现但经代码/测试/配置证实并非问题的关键候选项，避免后续重复误报。

## 已有基线失败

将 `verify_wms_bugs.py` 或其他命令中失败的既有项目单独列出：名称、当前输出、基线登记位置、是否与本次结论有关。未确认登记前不得称为“既有”。

## 覆盖缺口

列出未执行或无法验证的范围，例如依赖外部服务、需要人工登录、需要隔离测试环境、无端到端测试、移动端未构建等，并说明风险。

## 修复优先级

按 atomic action 提出建议顺序：每项只解决一个明确问题，包含预期影响文件、测试、是否需要更新 `WMS_BUG_BASELINE.md` 和 AI 开发台账。此处只给建议，不实际修改。

## 最终工作树核验

展示结束时 `git status --short` 结果，并明确说明审计是否保持只读。

# 11. 完成条件

只有同时满足以下条件才能宣称审计完成：

- 已读取规则文件并明确使用其业务红线。
- 已执行可安全运行的基础验证和高风险专项验证，或为每一项跳过给出具体原因。
- 每个结论都有代码或命令证据，不存在“我认为”“看起来可能”等无证据定性。
- 已区分真实问题、疑似问题、环境阻塞、既有基线失败和误报。
- 已输出完整报告并执行最终工作树核验。
- 没有修改、提交、推送、创建分支或产生业务数据副作用。
```

---

## 使用说明

- 将“可直接复制的提示词”完整发送给有仓库读取和命令执行能力的代码代理。
- 默认执行 `FULL` 审计；若只需 AI 专项或安全专项，可将“审计模式：FULL”替换为 `AI` 或 `SECURITY`，但仍需读取 `AGENTS.md` 和进行最终工作树核验。
- 审计发现问题后，应另开明确授权的修复任务；修复任务必须遵守 `AGENTS.md` 的 atomic action、测试、提交和推送规则。
- 不应把审计报告中的“疑似问题”直接视为已确认 BUG；必须补齐最小复现或代码路径证据。
