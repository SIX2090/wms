# 安全基线检查报告

> 审计依据：等保 2.0 三级要求 + Windows Server 2012 R2 安全基线
> 审计对象：WMS 绿色版部署包及配置文件
> 审计日期：2026-07-23
> 任务编号：AI-SEC-F01

## 一、凭据与密钥安全

| # | 检查项 | 合规状态 | 风险等级 | 修复建议 |
|---|---|---|---|---|
| 1 | GitHub PAT 明文存储 | 不合规 | 严重 | `deploy_cloud.bat:101` 硬编码 PAT，需立即在 GitHub 撤销，改用环境变量 `GITHUB_TOKEN` |
| 2 | `.git-credentials` 文件 ACL | 不合规 | 高 | 文件无 ACL 限制，任何用户可读；需 `icacls` 限制为 SYSTEM + Administrators |
| 3 | SECRET_KEY 存储 | 部分合规 | 中 | `app.py:638-660` 持久化到 `instance/secret_key`，`os.chmod(0o600)` ✓；但文件未加密 |
| 4 | 数据库连接字符串 | 合规 | 低 | `config.py:13` 默认 SQLite 无密码；如用 MySQL 需通过环境变量 `DATABASE_URL` 传入，不写入代码 |
| 5 | 默认管理员密码 | 部分合规 | 中 | `app.py:4452` 默认 'admin'，`must_change_password=True` ✓；但弱口令窗口期存在风险 |
| 6 | 随机密码生成 | 合规 | 低 | `说明.txt` 确认不生成随机密码，使用固定默认 'admin'（符合 AGENTS.md 密码透明性约束） |
| 7 | 微信助手令牌 | 合规 | 低 | `config.py:57` 未使用默认弱令牌，由 app.py 启动期生成并持久化 |

## 二、服务与应用池运行账户

| # | 检查项 | 合规状态 | 风险等级 | 修复建议 |
|---|---|---|---|---|
| 8 | nssm 服务运行账户 | 不合规 | 高 | `deploy_cloud.bat` 使用 LocalSystem 运行 WMS 服务，违反最小权限原则；应创建专用低权限账户 `WMS_Service` |
| 9 | 服务账户权限 | 不合规 | 高 | LocalSystem 拥有完整系统权限；专用账户仅需对 `C:\wms` 读写、8080 端口监听权限 |
| 10 | Nginx 运行账户 | 需确认 | 中 | Nginx 配置未指定运行账户，默认可能为 SYSTEM；建议指定 `nginx` 低权限账户 |

## 三、防火墙与端口

| # | 检查项 | 合规状态 | 风险等级 | 修复建议 |
|---|---|---|---|---|
| 11 | 防火墙规则配置 | 不合规 | 高 | 部署脚本未配置任何防火墙规则；需显式开放 443（HTTPS）并限制源 IP |
| 12 | 8080 端口暴露 | 不合规 | 高 | `run_server.py:101` 绑定 `0.0.0.0:8080`，公网可直接访问绕过 Nginx；应绑定 `127.0.0.1:8080` |
| 13 | 443 端口源 IP 限制 | 不合规 | 中 | 未限制源 IP；等保 2.0 要求仅开放必要来源 |
| 14 | RDP 端口暴露 | 需确认 | 中 | 未配置 RDP 源 IP 限制；建议仅允许运维网段 |

## 四、日志与审计

| # | 检查项 | 合规状态 | 风险等级 | 修复建议 |
|---|---|---|---|---|
| 15 | 应用日志 | 合规 | 低 | `config.py:27-29` 配置 RotatingFileHandler，10MB 轮转，保留 5 份 ✓ |
| 16 | 日志路径 | 部分合规 | 中 | `config.py:27` 路径 `logs/app.log` 为相对路径；应改为绝对路径 |
| 17 | 部署日志 | 不合规 | 高 | `install.bat`/`install_e_wms.bat` 无文件日志，仅控制台输出；需写入 `logs/install_*.log` |
| 18 | Windows 审计日志 | 需确认 | 中 | 未配置 Windows 安全审计策略（登录/对象访问/特权使用）；需启用 `auditpol` |
| 19 | auto_update 日志 | 合规 | 低 | `auto_update.py` 输出到 stdout，由 nssm 重定向到 `service_stdout.log`/`service_stderr.log` ✓ |
| 20 | 日志保留周期 | 部分合规 | 低 | 应用日志保留 5 份（约 50MB）；等保 2.0 三级要求日志保留 ≥ 6 个月，需配合日志归档 |

## 五、会话与 Cookie 安全

| # | 检查项 | 合规状态 | 风险等级 | 修复建议 |
|---|---|---|---|---|
| 21 | SESSION_COOKIE_SECURE | **已修复** | — | 本次已将基类和 ProductionConfig 默认改为 True（AI-SEC-F01）；TestingConfig 显式 False |
| 22 | SESSION_COOKIE_HTTPONLY | 合规 | 低 | `config.py:63` 已设为 True ✓ |
| 23 | SESSION_COOKIE_SAMESITE | 合规 | 低 | `config.py:64` 已设为 'Lax' ✓ |
| 24 | 会话超时 | 合规 | 低 | `config.py:60` 8 小时超时 ✓ |
| 25 | CSRF 保护 | 合规 | 低 | Flask-WTF CSRF 已启用；TestingConfig 显式关闭 ✓ |

## 六、组件漏洞版本

| # | 检查项 | 合规状态 | 风险等级 | 修复建议 |
|---|---|---|---|---|
| 26 | Flask CVE-2023-30861 | **已修复** | — | 原 2.3.3 → 已升级至 3.0.3（AI-SEC-F01） |
| 27 | Werkzeug CVE-2023-46136 | **已修复** | — | 原 2.3.7 → 已升级至 3.0.1（AI-SEC-F01） |
| 28 | cryptography CVE-2023-50782 | **已修复** | — | 原 41.0.4 → 已升级至 42.0.4（AI-SEC-F01） |
| 29 | urllib3 CVE-2023-43804 | **已修复** | — | 原 2.0.4 → 已升级至 2.0.7（AI-SEC-F01） |
| 30 | Windows Server 2012 R2 EOL | 不合规 | 高 | 2023-10-10 停止支持，无安全补丁；需升级至 Server 2019/2022 或购买 ESU |
| 31 | 账户锁定策略 | 不合规 | 中 | 未配置账户锁定策略（如 5 次失败锁定 30 分钟）；需通过 GPO 或 `secedit` 配置 |

## 七、本次已修复项汇总

| 修复项 | 修复内容 | 修改文件 |
|---|---|---|
| SESSION_COOKIE_SECURE | 基类 `Config` 和 `ProductionConfig` 默认改为 True；`TestingConfig` 显式 False | `app/config.py` |
| Flask CVE | 2.3.3 → 3.0.3 | `app/requirements.txt` |
| Werkzeug CVE | 2.3.7 → 3.0.1 | `app/requirements.txt` |
| cryptography CVE | 41.0.4 → 42.0.4 | `app/requirements.txt` |
| urllib3 CVE | 2.0.4 → 2.0.7 | `app/requirements.txt` |
| Golden 测试验证 | 90/90 全通过，无回归 | — |

## 八、待处置项（需人工操作）

| 优先级 | 待处置项 | 操作 |
|---|---|---|
| P0 严重 | 撤销已泄露 GitHub PAT | GitHub Settings → Developer settings → Personal access tokens → Revoke |
| P1 高 | 服务账户改用最小权限 | 创建 `WMS_Service` 低权限账户，nssm 指定该账户运行 |
| P1 高 | 8080 端口绑定 127.0.0.1 | 修改 `run_server.py:101` 或设置环境变量 `HOST=127.0.0.1` |
| P1 高 | 配置防火墙规则 | 仅开放 443 端口，限制源 IP 为运维网段 |
| P1 高 | 补充部署脚本安全加固 | 实施报告 2 的修复代码 A-F |
| P2 中 | 启用 Windows 审计策略 | `auditpol /set /category:* /success:enable /failure:enable` |
| P2 中 | 配置账户锁定策略 | GPO: 5 次失败锁定，锁定 30 分钟 |
| P2 中 | 升级 Windows Server | 迁移至 Server 2019/2022 |
