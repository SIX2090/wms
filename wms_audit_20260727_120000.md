# WMS 基础资料巡检报告

- 巡检时间：2026-07-27
- 工作目录：/workspace
- 服务状态：**离线（沙箱缺 flask 包，动态验证未执行）**；所有结论来自静态扫描 `app/app.py` + `app/templates/*.html`。
- 巡检范围：基础资料 9 大模块 + 关联矩阵 + 红线扫描。

---

## 1. 基础资料覆盖（9/9）

| 模块 | 模板 | 路由三件套 | CRUD 后端 | 导入/导出/模板 | 备注 |
|------|------|-----------|----------|----------------|------|
| 物料 | `material.html` | ✅ | ✅ 完善 | ✅（页内模态 + /batch_import） | 删除有引用校验，最完善 |
| 合同/工程 | `contract.html` | ✅ | ✅ | ⚠ 仅导出/下载模板，**无导入** | 删除有 blockers 检查 |
| 物料分类 | `category.html` | ✅ | ⚠ 缺编辑/导入/导出/下载模板按钮 | ✅（仅 /batch_import 集中入口） | 删除**缺物料引用校验** |
| 计量单位 | `unit.html` | ✅ | ⚠ 缺编辑/导入/导出/下载模板按钮 | ✅（仅 /batch_import 集中入口） | 删除 7 引用方都校验 |
| 供应商 | `supplier.html` | ✅ | ⚠ 缺编辑/新增/导入/导出/下载模板按钮 | ✅（仅 /batch_import 集中入口） | 删除 7 引用方校验 |
| 客户 | `customer.html` | ✅ | ⚠ 缺编辑/新增/导入/导出按钮 | ✅（仅 /batch_import 集中入口） | 删除校验按 name 字符串匹配 |
| 仓库 | `warehouse.html` | ✅ | ⚠ 缺新增/导入/导出/下载模板按钮 | ✅（仅 /batch_import 集中入口外无任何单页入口） | 删除有完整 blockers |
| 部门 | `department.html` | ✅ | ⚠ 缺新增/导入/导出/下载模板按钮 | ✅（仅 /batch_import 集中入口） | 删除 blockers 含领料单/采购申请 |
| 员工 | `employee.html` | ✅ | ⚠ 缺编辑/新增/导入/导出/下载模板按钮 | ✅（仅 /batch_import 集中入口） | **删除完全无引用校验** ⚠⚠ |

> 说明：所有 9 个模块的**后端接口**（`/material/import` 等）均已实现，单页「导入/导出/下载模板」按钮统一缺失（除物料外）；统一入口在 `/batch_import`。详见 §5。

---

## 2. 按钮与功能（按模块）

### 2.1 物料管理
- 修复空分类/单位 ✅ → `POST /material/fix_empty_fields`
- 标签模板 ✅（前端弹窗 + DB 持久化）
- 搜索/清除 ✅
- 编辑 ✅ → `GET /material/<id>` → `POST /material/edit/<id>`
- 复制 ✅ → `POST /material/<id>/copy`（自动生成新编码 + 名称追加"副本"）
- 打印标签 ✅
- 网上找图 ✅
- 删除 ✅ → `POST /material/delete`（含 15 类业务引用校验）
- 导入 ✅ → `POST /material/import`（CSRF + 文件扩展名校验）
- 每页条数 / 排序 / 分类树 ✅

### 2.2 合同/工程
- 新增 / 编辑 / 删除 / 导出 / 下载模板 ✅
- **导入按钮缺失** ⚠（后端 `import_contract` 路由不存在）
- 批量删除 ✅

### 2.3 物料分类
- 新增 / 编辑 / 树点编辑 ✅（通过 editCategory(id)）
- 删除 ✅
- **页面上缺「新增」「导入」「导出」「下载模板」触发按钮** ⚠
  - DOM 里有 `#importModal`（[category.html:351-370](file:///workspace/app/templates/category.html#L351-L370)）+ `#addModal`，但 `page-header` 仅 `<h2>`，无 button 触发。
  - 后端 `import_category` / `export_category` / `download_category_template` / `add_category` 均存在。
  - 用户实际要走 `/batch_import` 集中入口才能完成导入/下载模板。
- 搜索/排序/分页 ✅

### 2.4 计量单位
- 搜索/排序/分页 ✅
- 行级删除 ✅
- **缺编辑按钮**（无 `editUnit` 函数）⚠
- **页面上缺「新增」「导入」「导出」「下载模板」触发按钮** ⚠
- 后端 `add_unit` / `import_unit` / `export_unit` / `download_unit_template` 均存在

### 2.5 供应商
- 行级删除 ✅
- **缺编辑按钮**（无 `editSupplier` 函数）⚠
- **页面上缺「新增」「导入」「导出」「下载模板」触发按钮** ⚠
- DOM 里有 `#importModal`（[supplier.html:143-172](file:///workspace/app/templates/supplier.html#L143-L172)），但无 trigger。
- 后端齐全

### 2.6 客户
- 行级删除 ✅
- **缺编辑按钮**（无 `editCustomer` 函数）⚠
- **页面上缺「新增」「导入」「导出」触发按钮** ⚠
- DOM 里有 `#importModal` + 下载模板链接（[customer.html:143-172](file:///workspace/app/templates/customer.html#L143-L172)），但无 trigger 触发模态（仅有下载模板 link 在弹窗内部）。
- 后端齐全；删除按 name 字符串校验（业务单据是字符串字段）
- **F-04**：`/batch_import` 也未覆盖客户（`/customer/import` 后端已实现，集中页缺卡片）

### 2.7 仓库
- 行级编辑/删除 ✅（有 editWarehouse / deleteWarehouse 函数）
- 搜索/状态筛选/排序/分页 ✅
- **页面上缺「新增」「导入」「导出」「下载模板」触发按钮** ⚠
- DOM 里**完全没有** `addModal` / `importModal`（warehouse.html 全文 1-150 行无 bi-upload/bi-download）。
- 后端齐全（含 status active/inactive 切换）
- **F-04**：`/batch_import` 也未覆盖仓库

### 2.8 部门
- 行级编辑/删除 ✅（有 editDepartment / deleteDepartment 函数）
- 搜索/分页 ✅
- **页面上缺「新增」「导入」「导出」「下载模板」触发按钮** ⚠
- DOM 里**完全没有** `addModal` / `importModal`（department.html 同上）。
- 后端齐全
- **F-04**：`/batch_import` 也未覆盖部门

### 2.9 员工
- 行级删除 ✅
- 搜索/排序/分页 ✅
- **缺编辑按钮**（无 `editEmployee` 函数）⚠
- **页面上缺「新增」「导入」「导出」「下载模板」触发按钮** ⚠
- DOM 里有 `#importModal`（[employee.html:123-141](file:///workspace/app/templates/employee.html#L123-L141)），但无 trigger。
- 后端 `add_employee` / `import_employee` / `export_employee` / `download_employee_template` 均存在
- `Employee` 模型只有 `name/position/phone/created_at`，**没有 code / department_id 字段**（与"基础资料"主数据定位不符）

---

## 3. 关联矩阵与删除引用校验

| 主表 | 删除校验实现 | 缺失项 | 状态 |
|------|------------|--------|------|
| `Material` | 15 类业务引用 + LocationInventory 清理 + session 失败回滚 | — | ✅ 优秀 |
| `MaterialCategory` | 仅查子分类 | **未查 `Material.category_id` 引用** | ⚠ 物料可被孤儿化 |
| `Unit` | 7 类引用（物料/BOM/领料/委外/调拨/调整/请购） | — | ✅ 优秀 |
| `Supplier` | 7 类引用（入库/物料/采购单/委外3+发料+收货） | — | ✅ 优秀 |
| `Customer` | 按 name 字符串匹配出库单/售后出库单 | 未覆盖其他入库明细（客供勾选用 customer_id） | ⚠ |
| `Warehouse` | 7 类引用 blockers 报告（入库/领料/销售/调拨/调整/库位/流水） | — | ✅ 优秀 |
| `Department` | 领料单/采购申请部门 | 未查 `Employee.department_id`（但模型也未建此字段） | ⚠ 模型缺字段 |
| `Employee` | **无任何引用校验** | InOrder/OutOrder 操作员无阻断 | ⚠⚠ 致命 |
| `Contract` | 采购入库/领料/采购订单/销售订单 | 未覆盖 `InOrderDetail.contract_no` 字符串引用（业务单据冗余 contract_no） | ⚠ |

---

## 4. 校验规则核对

| 规则 | 实现 | 结论 |
|------|------|------|
| 物料编码唯一 | `Material.code` `unique=True, nullable=False` | ✅ |
| 物料名称+规格 组合 | 无 unique（业务上允许同名不同规格） | ✅ 符合业务 |
| 分类编码唯一 | `MaterialCategory.code/name` `unique=True` | ✅ |
| 单位编号/名称唯一 | `Unit.code/name` `unique=True` | ✅ |
| 供应商编号/名称唯一 | `Supplier.code/name` `unique=True` | ✅ |
| 客户编号/名称唯一 | `Customer.code/name` `unique=True` | ✅ |
| 仓库编号/名称唯一 | `Warehouse.code/name` `unique=True` | ✅ |
| 仓库是否默认仓 | **无 `is_default` 字段** | ⚠ 与需求不符 |
| 合同号唯一 | `Contract.contract_no` `unique=True` | ✅ |
| 合同 vs 工程区分 | `Contract` 单表，依赖 `contract_no` 前缀 | ⚠ 业务上靠命名约定 |
| 员工编号唯一 | `Employee` **无 code 字段** | ⚠ 与基础资料定位不符 |
| 员工电话唯一 | 无 | ⚠ |

---

## 5. 导入/导出/模板三件套（重新核对）

### 5.1 三类入口并存

WMS 实际存在 **三类** 导入导出入口，上一版报告只看到第①类：

#### ① 单据页内「导入/导出/下载模板」三件套

| 单据页 | 下载模板 | 导入按钮 | 导出按钮 | 状态 |
|---|---|---|---|---|
| 采购订单 [purchase_order.html:13-21](file:///workspace/app/templates/purchase_order.html#L13-L21) | `/export/template/purchase_order` | ✅ | ✅ | ✅ 齐全 |
| 销售订单 [sales_order.html:14-22](file:///workspace/app/templates/sales_order.html#L14-L22) | `/sales/download_template` | ✅ | ✅ | ✅ 齐全 |
| 出库单 [out_order.html:17-29](file:///workspace/app/templates/out_order.html#L17-L29) | `/export/template/out_order` | ✅ | ✅ | ✅ 齐全 |
| 盘点单 [check.html:11-21](file:///workspace/app/templates/check.html#L11-L21) | `/export/template/check` | ✅ | ✅ | ✅ 齐全 |
| 入库单 in_order.html | — | ✅ `/import/in_order` | ✅ `/in_order/export` | ⚠ 缺下载模板 |
| 调拨单 transfer.html | — | ✅ `/transfer/import` | ✅ `/transfer/export` | ⚠ 缺下载模板 |
| 调整单 adjustment.html | — | ✅ `/adjustment/import` | ✅ `/adjustment/export` | ⚠ 缺下载模板 |
| 领料申请 purchase_request.html | — | ✅ `/purchase_request/import` | ✅ `/purchase_request/export` | ⚠ 缺下载模板 |
| 委外加工 subcontract*.html | — | ✅ | ✅ | ⚠ 缺下载模板 |
| 售后出库 after_sale_out.html | — | ✅ | ✅ | ⚠ 缺下载模板 |
| 期初库存 opening_stock.html | — | ✅（粘贴导入） | ✅（`exportRows()`） | ✅ 有按钮 |

#### ② 基础资料单页「导入/导出/下载模板」

| 基础资料 | 后端接口 | 模板下载 | 导入 | 导出 | 页面 trigger |
|---|---|---|---|---|---|
| 物料 [material.html:754-777](file:///workspace/app/templates/material.html#L754-L777) | ✅ | ✅ | ✅ `/material/import` | ✅ `/material/export` | ✅ **页内有完整弹窗**（行内"导入"按钮） |
| 供应商 [supplier.html:143-172](file:///workspace/app/templates/supplier.html#L143-L172) | ✅ | ✅ | ✅ | ✅ | ⚠ 模态存在但**无 trigger**，需走集中入口 |
| 客户 [customer.html:143-172](file:///workspace/app/templates/customer.html#L143-L172) | ✅ | ✅ | ✅ | ✅ | ⚠ 模态存在但**无 trigger**，需走集中入口 |
| 物料分类 [category.html:351-370](file:///workspace/app/templates/category.html#L351-L370) | ✅ | ✅ | ✅ | ✅ | ⚠ 模态存在但**无 trigger**，需走集中入口 |
| 计量单位 [unit.html:152-170](file:///workspace/app/templates/unit.html#L152-L170) | ✅ | ✅ | ✅ | ✅ | ⚠ 模态存在但**无 trigger**，需走集中入口 |
| 员工 [employee.html:123-141](file:///workspace/app/templates/employee.html#L123-L141) | ✅ | ✅ | ✅ | ✅ | ⚠ 模态存在但**无 trigger**，需走集中入口 |
| 仓库 warehouse.html | ✅ | ✅ | ✅ | ✅ | ❌ **单页无任何 trigger** |
| 部门 department.html | ✅ | ✅ | ✅ | ✅ | ❌ **单页无任何 trigger** |
| 合同 [contract.html:12-16](file:///workspace/app/templates/contract.html#L12-L16) | ✅ | ✅ | ❌ **后端无 import_contract** | ✅ | ⚠ 有「导出」「导入模板」按钮，**缺「导入」按钮** |

#### ③ 集中式：批量导入页 `/batch_import`

由 [base.html:2246-2248](file:///workspace/app/templates/base.html#L2246-L2248) 和 [base.html:2425-2427](file:///workspace/app/templates/base.html#L2425-L2427) 顶部导航暴露，渲染 [batch_import.html](file:///workspace/app/templates/batch_import.html)，单页集中 **8 类** 数据导入卡片（每个卡片含「下载模板」+「导入」按钮）：

| 卡片 | 导入 URL | 模板下载 URL |
|---|---|---|
| 物料 | `/material/import` | `/export/template/material` |
| 采购入库单 | `/import/in_order` | `/export/template/in_order` |
| 领料单 | `/import/out_order` | `/export/template/out_order` |
| 物料分类 | `/category/import` | `/category/download_template` |
| 计量单位 | `/unit/import` | `/unit/download_template` |
| 供应商 | `/supplier/import` | `/supplier/download_template` |
| 员工 | `/employee/import` | `/employee/download_template` |
| BOM | `/bom/import` | `/export/template/bom` |

> 覆盖关系：物料、分类、单位、供应商、员工、仓库、部门 7 类基础资料的导入**都已经在 `/batch_import` 提供**。仓库/部门虽然在各自单页无 trigger，但通过 `/batch_import` 也**未覆盖**（`batch_import.html` 中没有仓库、部门、客户、合同卡片）——这一缺漏已在 F-04 中记录。

### 5.2 后端实现一致性

CSRF：所有 `import` 路由均未 `@csrf.exempt`，且加 `@require_role` + `@login_required`。✅
文件大小：仅 `validate_excel_extension`，未限制文件 size（潜在 OOM 风险，提示级）。
权限：`/contract/import` **未实现**（`@app.route` 全表无此 endpoint）—— F-03 仍然成立。

### 5.3 通用前端组件

[excel-import-export.js](file:///workspace/app/static/js/excel-import-export.js) 是一套统一的前端组件，封装「导出 / 导入 / 下载模板」三件套，可被任意明细表调用。`in_order_detail.html`、`out_order_detail.html` 已在用。

---

## 6. 红线扫描

| 红线 | 检查 | 结果 |
|------|------|------|
| 已完成入库单直接删除 | `delete_in_order` 在 `status!='pending'` 时返回 409 | ✅ |
| 物料硬删无引用校验 | `delete_material` 校验 15 类引用 + LocationInventory 清理 | ✅ |
| 供应商/客户删除无引用校验 | 均有（客户按 name 字符串） | ✅ |
| 分类树非叶子删除无提示 | `_contract_delete_blockers` 风格已应用：检测子分类 | ✅（但缺物料引用阻断） |
| 合同删除影响历史单据 | `_contract_delete_blockers` 阻断 4 类单据 | ✅ |
| 自动生成随机密码 | bootstrap 用固定 `'admin'`，reset 用用户输入 | ✅ |
| CSRF 缺失 | `CSRFProtect(app)`，仅 `/api/*` mobile API 豁免（合理） | ✅ |
| 删除全部物料 | `/material/delete_all` 返回 403 | ✅ |
| 死代码 / 旧函数 | 无 `submitForm` 残留 | ✅ |

---

## 7. 缺陷清单

### 7.1 致命（Critical）
- **F-01**：`Employee` 删除完全无引用校验（`app/app.py:23884`）。`InOrder.operator_id` / `OutOrder.operator_id` 等未阻断，员工被业务单据引用仍可硬删。
- **F-02**：`delete_category` 缺 `Material.category_id` 引用校验（`app/app.py:8902`）。分类被 N 个物料引用仍可删除，导致外键悬空。
- **F-03**：合同/工程缺导入路由（`/contract/import`）。后端无 `import_contract` 函数，模板侧"导入模板"按钮只下载不提交。
- **F-04**：`/batch_import` 集中入口**未覆盖仓库、部门、客户三类基础资料**。这三个模块的导入在后端已实现（`/warehouse/import`、`/department/import`、`/customer/import`），但集中页 `batch_import.html` 没有对应卡片；且三个单页无任何 trigger，导致这三类数据**当前没有可用的导入入口**（仅物料、分类、单位、供应商、员工、BOM、采购入库、领料单可通过 `/batch_import` 走通）。

### 7.2 一般（Major）
- **M-01**：`unit.html` / `supplier.html` / `customer.html` / `employee.html` 缺编辑按钮（无 `editXxx` 函数），用户无法修改已录入数据。`category.html` 仅在树行有 `editCategory`，表内行无编辑。
- **M-02（修正）**：8 个基础资料页面顶部均无「新增/导入/导出/下载模板」工具栏 trigger。
  - `category.html` / `unit.html` / `supplier.html` / `customer.html` / `employee.html`：DOM 里有 `addModal` / `importModal`，但 `page-header` 只有 `<h2>`，无任何 `<button>` 触发模态。
  - `warehouse.html` / `department.html`：连 `addModal` / `importModal` 都不存在。
  - `contract.html`：有「导出」「导入模板」按钮，缺「新增」「导入」按钮。
  - **物料 `material.html` 是唯一模板齐全的**——`page-header` 之外另有「导入」按钮 + 模态（[material.html:754-777](file:///workspace/app/templates/material.html#L754-L777)），是 9 个模块里唯一可由单页走完三件套的。
- **M-03**：`Warehouse` 模型无 `is_default` 字段，与"基础资料"主数据定位不符；`Warehouse` 无设为默认仓按钮。
- **M-04**：`Employee` 模型缺 `code` / `department_id` 字段，不能算完整的基础资料主数据。
- **M-05**：`Contract` 业务单据冗余 `contract_no` 字符串，`_contract_delete_blockers` 未覆盖 `InOrderDetail.contract_no` 等字符串字段。

### 7.3 提示（Minor）
- **m-01**：`delete_customer` 仅按 name 字符串校验；其他入库明细用 `customer_id`（FK），需补 FK 校验。
- **m-02**：`delete_department` 未查 `Employee.department_id`（模型也缺此字段），需先补模型字段。
- **m-03**：导入路由未限制文件大小，建议加 `MAX_CONTENT_LENGTH`。
- **m-04**：导航菜单"基础资料"包含"期初库存"项（`/opening_stock`），但该功能属于交易数据，非基础资料，建议归到"库存"或单列"初始建账"。
- **m-05**：合同/工程依赖 `contract_no` 前缀区分，缺少 `kind` 字段（合同/工程），业务上易混淆。

---

## 8. 修复建议（优先级）

| P | ID | 模块 | 改动 | 文件 | 行号 |
|---|----|------|------|------|------|
| P0 | F-01 | 员工 | `delete_employee` 增加 `InOrder.operator_id` / `OutOrder.operator_id` / 其他单据引用校验 | app/app.py | 23884 |
| P0 | F-02 | 分类 | `delete_category` 增加 `Material.query.filter_by(category_id=cat.id).count() > 0` 阻断 | app/app.py | 8902 |
| P0 | F-03 | 合同 | 新增 `import_contract` 路由 + contract.html 顶部工具栏"导入"按钮 | app/app.py + app/templates/contract.html | — |
| P0 | F-04 | 仓库/部门/客户 | `batch_import.html` 增 3 个卡片：仓库（`/warehouse/import` + `/warehouse/download_template`）、部门（`/department/import` + `/department/download_template`）、客户（`/customer/import` + `/customer/download_template`） | app/templates/batch_import.html | 全文 |
| P1 | M-01 | 基础 5 模块 | 各 `xxx.html` 添加 `editXxx(id)` 函数 + 行级编辑按钮 + 编辑弹窗 | unit/supplier/customer/employee/category.html | — |
| P1 | M-02 | 基础 8 模块 | 顶部工具栏统一增加"新增 / 导入 / 导出 / 模板下载"（参照 material.html:754-777 的实现） | 所有基础资料模板 | — |
| P1 | M-03 | 仓库 | `Warehouse` 模型加 `is_default` + 行级"设为默认"按钮 + 后端接口 | models + warehouse.html | — |
| P1 | M-04 | 员工 | `Employee` 模型加 `code`(unique) / `department_id`（FK） | models + 迁移 | — |
| P2 | M-05 | 合同 | `_contract_delete_blockers` 增 `InOrderDetail.contract_no == contract.contract_no` 字符串匹配 | app/app.py | 8115 |
| P2 | m-01 | 客户 | `delete_customer` 增 `OtherInOrderDetail.query.filter_by(customer_id=customer.id).count()` | app/app.py | 23751 |
| P2 | m-03 | 全局 | `app.config['MAX_CONTENT_LENGTH']` 限制 Excel ≤ 5MB | app/app.py | — |

---

## 9. 自检

- [x] 静态扫描完成（9 模块 / 5 关联 / 5 校验维度）
- [x] 动态验证：**未执行**（沙箱无 flask 包）
- [x] 关联矩阵全部覆盖（9 主表 × 引用方）
- [x] 9 个模块均已检查
- [x] 导入/导出入口**三类**全部核对（单据页 / 基础资料单页 / /batch_import 集中入口）
- [x] 未修改任何代码 / 数据
- [x] 未推送任何 commit

**修正记录**：v1 报告 §5 把所有基础资料"导入按钮"判为 ❌，是错误的——WMS 同时存在 `/batch_import` 集中入口，可覆盖 5 类基础资料（物料/分类/单位/供应商/员工）+ 2 类单据（入库/领料）+ BOM。物料是唯一单页自带完整三件套的模块。本版已修正并新增 F-04 记录「仓库/部门/客户」三类被集中入口漏覆盖的事实。

---

## 10. 复现动态验证步骤（本地可执行）

```bash
# 1. 启动服务
cd /workspace
python -m pip install -r requirements.txt
python app/run_server.py
# 或离线版：start_wms_offline.bat

# 2. 浏览器验证（admin / admin）
# 2.1 基础资料 → 物料管理
#   - 新增物料（含分类/单位/供应商）→ 列表显示
#   - 编辑该物料改分类 → 列表更新
#   - 复制该物料 → 新弹窗带"副本"后缀
#   - 删除该物料 → 列表消失
#   - 搜索/排序/分页/分类树 → URL 正确
# 2.2 基础资料 → 计量单位
#   - 期望看到"新增"按钮 → **现状：无**（F-M02）
# 2.3 基础资料 → 仓库档案
#   - 期望"设为默认仓" → **现状：无**（M-03）
# 2.4 基础资料 → 员工管理
#   - 期望编辑按钮 → **现状：无**（M-01）
#   - 尝试删除有业务引用的员工 → 期望被阻断 → **现状：直接删除**（F-01）

# 3. 跨模块关联
# 3.1 物料管理新增物料（分类=某旧分类）→ 基础资料 → 分类 → 删除该分类
#     期望：拒绝，提示"已被 N 个物料使用" → **现状：直接删除**（F-02）
# 3.2 合同/工程 → 尝试导入 Excel
#     期望：上传并入库 → **现状：后端无 import_contract 路由**（F-03）
# 3.3 /batch_import 集中入口核对
#     期望：8 张卡片覆盖所有基础资料 → **现状：仓库/部门/客户 3 张卡片缺失**（F-04）
# 3.4 基础资料 → 仓库档案
#     期望看到「新增」「导入」「导出」「下载模板」按钮 → **现状：page-header 仅 h2，无任何 button**（M-02 仓库）
# 3.5 基础资料 → 部门档案
#     期望同上 → **现状：page-header 仅 h2，无任何 button**（M-02 部门）
# 3.6 基础资料 → 供应商管理
#     期望同上 → **现状：page-header 仅 h2，无任何 button**（M-02 供应商）
```
