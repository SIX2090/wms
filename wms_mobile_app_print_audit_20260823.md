# WMS 手机APP打印功能全面审计报告

**审计对象**:https://github.com/SIX2090/wms(最新提交 `d3bb90ed`,2026-08-23)
**审计范围**:手机扫码 → 手工入库/出库 → 全自动连接本地电脑无人值守打印 全链路
**审计方式**:源码逐行审计 + 122 项自动化测试实测 + 真实启动服务端到端验证

---

## 一、总体结论

**你要的"手机扫码/手工出入库 → 本地电脑无人值守自动打印"链路，代码里已经完整存在，且核心链路实测跑通。** 最近 5 个提交全部围绕这条链路(SERVER-AUTOPRINT-01 内置打印代理、PRINT-ROUTING-F01 定向打印与告警)。

但审计发现 **3 个高危问题、5 个中危问题、4 个低危/体验问题**，其中**重复出纸**和**release 版 APP 连不上局域网服务器**两个会直接影响现场使用，建议优先修复。

### 全链路架构(已验证可用)

```
手机APP(Android原生)
  │  ① 扫码(ML Kit)/手工输码 → 本地攒批 → 一键提交
  │     POST /api/inbound | /api/outbound(Bearer令牌 + X-Idempotency-Key幂等键)
  ▼
WMS服务端(Windows本地电脑,waitress :8080)
  │  ② 同一事务内:生成单据 → 动库存 → enqueue_auto_print_job 自动建打印任务
  │     派发优先级:路由规则(在线) → 路由规则(离线也定向,等补打)
  │                 → 内置代理零配置兜底 → 非定向(网页工作站认领)
  ▼
内置本机打印代理(local_print_agent.py,服务进程内后台线程,随服务启动)
  │  ③ 每3秒认领定向任务 → PowerShell枚举/切换Windows默认打印机
  │     → Edge/Chrome --kiosk-printing 静默出纸(无打印对话框)
  │     → 回写 done/failed(打印页带10分钟短时效ptoken免登录 + autoprint=1自动打印)
  ▼
打印机出纸 ── 全程无人值守,无需开任何网页
  │
  └ ④ 失败兜底:僵尸任务5分钟回收重试(最多5次) → 仍失败/滞留10分钟/工作站离线
       → 系统内告警(铃铛+列表) → 可选微信推送告警卡片(限流防轰炸)
```

---

## 二、实测验证记录

### 2.1 自动化测试(全部通过)

| 测试套件 | 覆盖内容 | 结果 |
|---|---|---|
| test_print_queue.py | 打印队列认领/回收/租约/完成/失败 | ✅ 通过 |
| test_local_print_agent.py | 内置代理注册/心跳/认领/零配置兜底 | ✅ 通过 |
| test_print_agent_api.py | 代理API v1(令牌鉴权/claim/complete/fail/心跳) | ✅ 通过 |
| test_print_auto_after_scan.py | 扫码后自动建打印任务 | ✅ 通过 |
| test_print_alerts.py | 失败/滞留/离线告警与微信推送 | ✅ 通过 |
| test_mobile_scan_draft_flow.py | 手机扫码草稿→确认→动账→打印 | ✅ 通过 |
| test_print_routing_admin.py | 工作站/打印机/路由规则管理 | ✅ 通过 |
| **合计** | | **122 项全部通过** |

### 2.2 端到端实测(真实启动服务验证)

在沙箱中真实启动 `run_server.py`,模拟手机操作:

| 步骤 | 操作 | 实测结果 |
|---|---|---|
| 1 | APP登录 `POST /api/login` | ✅ 返回 7 天有效 Bearer token |
| 2 | 扫码入库 `POST /api/inbound` | ✅ 单据 `IN26080001` 创建,库存+5 |
| 3 | **同一幂等键重放**(模拟网络重试) | ✅ 返回同一单号,**未重复入库**(服务端幂等生效) |
| 4 | 入库后打印任务 | ✅ 自动生成,`source_event=scan_inbound` |
| 5 | 内置工作站 LOCAL-SERVER | ✅ 自动注册,心跳每 60 秒在线 |
| 6 | 注入在线打印机后再扫码入库 | ✅ 任务**自动定向**到内置工作站,**4 秒内被认领并标记 done**(kiosk 浏览器真实调起) |
| 7 | 手工连点两次"打印单据" | ⚠️ **同一单据生成 3 条任务**(见问题 P1) |

---

## 三、问题清单(按严重度)

### 🔴 高危(直接影响现场使用)

**P1. 同一单据可重复出纸,无任何防重(实测复现)**
- APP 端 `createPrintJob` 不带 `X-Idempotency-Key`(`WmsApiService.kt:111`),成功后打印按钮不置灰可反复点;服务端 `POST /print_queue/jobs`(`print_queue.py:413`)无幂等、无"该单据已有 pending/done 任务"校验。
- 实测:入库自动打 1 张 + 手工点 2 次 → **同一入库单 3 条 pending 任务,会出 3 张纸**。
- 修复建议:服务端按 `job_type+target_id` 做"存在未失败任务则复用/拒绝"的幂等;APP 端点击后禁用按钮并带幂等键。

**P2. release 版 APP 无法连接局域网 HTTP 服务器(部署即踩坑)**
- `network_security_config.xml` 的 release 明文白名单只有 `127.0.0.1 / localhost / 10.0.2.2`(模拟器),而本系统典型部署就是 `http://192.168.x.x:8080`。**release 包连局域网 IP 会被系统直接拦截**,只有 debug 包能连。
- 修复建议:把现场服务器局域网 IP 显式加入白名单,或给服务器上 HTTPS(推荐,顺带解决 token 明文传输问题)。

**P3. APP 重试时幂等键重新生成,服务端去重失效(边界场景重复入库/出库)**
- `WmsRepository.kt:58` `newRequestId()` 每次调用都生成新 UUID。入库/出库提交若超时,APP 层或用户重试会带**新**幂等键,服务端视为新请求 → 同一批扫码明细可能入两张单、扣两次库存。
- 服务端幂等本身实测是好的(同键重放返回原单),问题在 APP 端键的生命周期:应为"一批扫码明细"固定一个键,提交成功后才换。
- 修复建议:幂等键改为按批次生成(如本地批次号),存入批次状态,重试复用同一键。

### 🟡 中危(功能缺陷/运维风险)

**P4. "打印完成"以浏览器进程退出为准,存在假成功**
- `local_print_agent.py:239` `print_url_via_browser` 只要浏览器进程在 120 秒内退出就 `mark_job_printed(ok=True)`。浏览器崩溃、无显示环境、打印服务异常导致进程秒退,都会标记 done —— **纸没出来但系统显示已打印**,且不会触发告警。
- 建议:出纸确认改为打印页 JS 在 `window.print()` 后回调服务端 confirm 接口,或至少校验浏览器退出码。

**P5. 不支持连续扫码,大批量作业效率低**
- `ScannerDialog.kt` 扫到一码即关弹窗(`scannedFlag` 单次回调),扫 50 件物料要开 50 次扫码窗。
- 建议:加"连续扫码模式"开关(扫到不关窗,震动反馈+累加数量)。

**P6. 打印失败/滞留告警默认不发微信,管理员不盯网页就感知不到**
- 系统内告警(铃铛)默认开,但微信推送 `print_alert_wechat_enabled` **默认关**。无人值守场景下,打印失败主要靠微信触达才有意义。
- 建议:部署时开启微信推送并配置本机微信助手;或在部署文档中显著标注。

**P7. 内置代理 INFO 级日志默认不输出**
- `run_server.py` 只配置了 waitress 的日志器,`wms.local_print_agent` 的"开始打印任务 #X / 打印完成"等 INFO 日志被根日志器 WARNING 级别吞掉,现场排查"到底打没打"缺证据(实测日志里只有 WARNING 可见)。
- 建议:`run_server.py` 的 `_configure_console_logging` 把 `wms.local_print_agent` 纳入控制台 INFO 输出。

**P8. 手机端无打印任务状态查询/重打入口**
- APP 只能"创建打印任务",看不到任务是否成功、失败原因,失败后要回电脑前补打。
- 建议:加 `GET /print_queue/jobs?target_id=` 查询 + 失败任务一键重发。

### 🔵 低危(体验/健壮性)

**P9.** 仓库默认自动选中第一个仓库(`ScanViewModel.kt:173`),多仓场景易误提交到错仓库——建议默认不选、强制手选。
**P10.** 出库的领用人/部门字段 APP 未提供输入(恒为 null,`ScanScreens.kt:335`),入库业务类型硬编码"采购入库"——单据信息不全。
**P11.** 手工输码不校验物料是否存在即入列,错误留到提交时才整体报错——建议手工添加时即时校验。
**P12.** `mobile.py` 还有一套 H5 扫码草稿制(`/mobile/api/scan_batch_draft` → 待确认 → `/mobile/api/scan_draft_confirm` 才动账+打印),与原生 APP 的"提交即动账"是两套语义;团队需明确现场用哪一套,避免混用导致"以为确认了其实没确认"。

---

## 四、链路中做得好的地方(值得保留)

1. **单据与打印任务同一事务写入**(`native_api.py:333/418`):不会出现"入完库没建打印任务"的丢单。
2. **打印派发四级兜底**:路由规则在线 → 离线也定向等补打(不串台)→ 内置代理零配置兜底 → 非定向网页认领,零配置开箱即打。
3. **队列健壮性**:原子认领(条件更新防并发重打)、租约 token 防旧代理覆盖新结果、僵尸任务按认领时间回收、尝试上限 5 次。
4. **告警只增补不阻断**:所有告警独立小事务 + try/except,告警挂了不影响出纸;当日同类型同目标去重防轰炸;微信推送全局限流。
5. **ptoken 免登录打印页**:10 分钟短时效、绑定任务与工作站,代理无需账号密码;`--kiosk-printing` 独立 profile 不污染日常浏览器。
6. **告警只针对真异常**:失败告警 + 滞留巡检(默认 10 分钟)+ 离线工作站仍有定向任务,三类全覆盖。
7. **测试资产扎实**:打印与扫码链路 122 项测试全绿,含大量真实 BUG 回归(BUG-2026-08-19-008/009/010 等)。

---

## 五、部署核对清单(现场照此落地)

| 步骤 | 动作 | 依据 |
|---|---|---|
| 1 | Windows 电脑上 `install.bat` 装服务,设 `WMS_BOOTSTRAP_PASSWORD` 改默认 admin 密码 | 实测默认密码 admin 会启动告警 |
| 2 | 确认电脑装有 Edge 或 Chrome(kiosk 静默打印依赖) | `local_print_agent.find_kiosk_browser` |
| 3 | 手机 APP 登录页服务器地址填 `http://<电脑局域网IP>:8080` | **先用 debug 包,或先把 IP 加进 release 白名单(P2)** |
| 4 | 不修路由规则也能打:内置代理自动兜底;要指定打印机/分仓打印再去「系统管理→打印工作台路由」配规则 | `_resolve_job_assignment` |
| 5 | 系统设置开启 `print_alert_wechat_enabled` 并配微信助手,失败/滞留才有人知道 | P6 |
| 6 | 修复 P1/P3 后再上线,否则重复出纸/重复入库风险长期存在 | P1、P3 |

---

## 六、修复优先级建议

| 优先级 | 问题 | 预估工作量 |
|---|---|---|
| 第一梯队(上线前必修) | P1 重复出纸防重、P2 release 局域网白名单、P3 幂等键批次化 | 各约半天 |
| 第二梯队(上线后两周内) | P4 出纸真确认、P6 微信告警开启+文档、P8 APP 查打印状态 | 1~2 天 |
| 第三梯队(体验优化) | P5 连续扫码、P7 日志、P9~P12 | 按需排期 |

---

*报告生成时间:2026-08-23。审计基于仓库提交 d3bb90ed,测试环境 Python 3.11.1,端到端验证使用沙箱真实启动服务端完成。*
