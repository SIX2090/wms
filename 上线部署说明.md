# 仓库管理系统腾讯云部署说明

这份说明只针对当前项目固定部署到 Windows 腾讯云服务器的方式。

## 绝对不能删

清理服务器前，必须先保留这些内容：

```text
C:\wms\instance\inventory.db
C:\wms\backups
C:\wms\static\uploads
```

`inventory.db` 是业务数据库。服务器资料删掉以后，如果没有把这个文件拷回来，脚本只能启动空系统，不能恢复原来的单据和库存。

## 新服务器部署步骤

1. 安装 Python 3.11，安装时勾选 `Add python.exe to PATH`。
2. 把项目放到固定目录：

```text
C:\wms
```

3. 如果是旧系统迁移，把备份的数据库放回：

```text
C:\wms\instance\inventory.db
```

4. 运行部署脚本：

```bat
cd /d C:\wms
deploy_cloud.bat
```

脚本会做这些事：

- 自动备份 `instance\inventory.db` 到 `backups`
- 使用系统 Python 安装依赖
- 安装 `requirements.txt`
- 只停止仓库系统自己的 Python 进程
- 用 `python app.py` 启动系统
- 检查 `http://127.0.0.1:8080/login`

脚本不会删除数据库。

## 日常命令

启动：

```bat
cd /d C:\wms
启动仓库管理系统.bat
```

停止：

```bat
cd /d C:\wms
stop_wms.bat
```

重启：

```bat
cd /d C:\wms
restart_wms.bat
```

检查：

```bat
cd /d C:\wms
check_wms.bat
```

旧的 `restart_app.bat` 现在只会转到 `restart_wms.bat`，不会再杀掉服务器上所有 Python。

## 访问地址

服务器本机检查：

```text
http://127.0.0.1:8080/login
```

公网访问：

```text
https://gd2026.top/login
```

## Nginx 必须配置

公网必须由 Nginx 转发到本机 WMS：

```text
https://gd2026.top -> http://127.0.0.1:8080
```

本目录提供了配置模板：

```text
C:\wms\nginx_wms_server_block.conf
```

把这个 server block 放到当前正在使用的 Nginx `http { ... }` 里面，然后重启 Nginx 服务。

检查公网：

```bat
cd /d C:\wms
check_public_wms.bat
```

正常结果：

```text
https://gd2026.top/login  返回 200
```

## 如果重装或清空腾讯云

清空前先下载或复制：

```text
C:\wms\instance\inventory.db
C:\wms\backups
C:\wms\static\uploads
```

清空后重新把项目放到 `C:\wms`，再把 `inventory.db` 放回 `C:\wms\instance\inventory.db`，最后运行：

```bat
cd /d C:\wms
deploy_cloud.bat
```

## 日志位置

```text
C:\wms\logs\deploy_*.log
C:\wms\logs\service_stdout.log
C:\wms\logs\service_stderr.log
```

部署失败先看 `logs\deploy_*.log`。系统启动失败看 `service_stderr.log`。

## 只更新程序，不改数据库

如果生产库不能动，使用下面这个流程。这个流程只覆盖程序文件和页面静态文件，不复制 `instance`，也不会覆盖 `instance\inventory.db`。

在打包电脑上生成更新包：

```bat
cd /d C:\wms
make_server_deploy_package.bat
```

会生成：

```text
wms_update_年月日_时分秒.zip
```

把下面两个文件放到服务器同一个临时目录：

```text
apply_wms_update_on_server.bat
wms_update_*.zip
```

在服务器上双击或运行：

```bat
apply_wms_update_on_server.bat
```

这个更新脚本会：

- 先备份 `C:\wms\instance\inventory.db` 到 `C:\wms\backups`
- 只复制代码、模板、CSS、JS、CDN 静态文件
- 不复制 `instance`、`backups`、`logs`、`static\uploads`
- 创建 `C:\wms\WMS_NO_DB_TOUCH.flag`
- 用 `WMS_NO_DB_TOUCH=1` 重启系统，跳过启动时的自动迁移和初始化

数据库不能动时，不要运行这些安装/部署脚本：

```text
deploy_cloud.bat
tencent_cloud_onekey_deploy.bat
make_wms_offline_installer.ps1 生成的离线安装器
```

原因：新版程序启动默认会执行 `auto_migrate_database()` 和 `initialize_database()`，可能 `ALTER TABLE`、`CREATE TABLE` 或修复部分数据。只有上面的 `apply_wms_update_on_server.bat` 会默认禁止这些启动数据库变更。

如果以后确认允许新版程序升级数据库结构，再删除这个标记文件并正常重启：

```bat
del C:\wms\WMS_NO_DB_TOUCH.flag
cd /d C:\wms
restart_wms.bat
```
