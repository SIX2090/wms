# WMS Windows 部署与运行说明

> 当前服务端口固定为 `8080`。本文只记录仓库中实际存在的启动和部署入口。

## 数据保护

部署、更新或迁移前必须备份业务数据库、上传附件和备份目录：

```text
C:\wms\app\instance\inventory.db
C:\wms\app\backups\
C:\wms\app\static\uploads\
```

不同安装方式可能把运行目录放在项目根目录下。操作前应以当前 `SQLALCHEMY_DATABASE_URI` 和实际文件位置为准，不得凭文档路径删除数据库。

## 本机直接运行

开发仓库位于 `C:\Users\Administrator\Desktop\wms` 时，在项目根目录运行：

```bat
start_wms.bat
```

或使用便携 Python：

```bat
runtime\Python311\python.exe app\run_server.py
```

访问：

```text
http://127.0.0.1:8080/login
```

停止离线服务：

```bat
stop_wms_offline.bat
```

重启开发服务：

```bat
runtime\Python311\python.exe app\restart.py
```

## 腾讯云 Windows 部署

以管理员身份在项目根目录运行：

```bat
deploy_cloud.bat
```

当前脚本会使用 `main` 分支，将系统部署到 `C:\wms`，准备便携 Python，并通过 NSSM 注册名为 `WMS` 的 Windows 服务。部署前必须人工检查脚本中的仓库地址、服务账号和凭据配置。

### 手机 App 语音指令（腾讯云 ASR 密钥，必配）

手机 App 的语音指令功能由后端 `/mobile/api/asr` 中转腾讯云一句话识别实现，
**必须在服务环境变量中配置密钥，否则 App 端会一直报"语音识别服务异常"**：

```bat
nssm set WMS AppEnvironmentExtra TENCENTCLOUD_SECRET_ID=你的SecretId TENCENTCLOUD_SECRET_KEY=你的SecretKey TENCENTCLOUD_REGION=ap-guangzhou
nssm restart WMS
```

密钥在腾讯云控制台「访问管理 CAM → API 密钥管理」创建，并确认账号已开通
「语音识别 ASR」服务（一句话识别有免费额度）。服务启动时若密钥缺失，
日志会输出 `腾讯云 ASR 密钥未配置` 警告，可据此自查。

服务端程序入口为：

```text
C:\wms\app\start_wms_auto.bat
```

服务日志位于：

```text
C:\wms\app\logs\service_stdout.log
C:\wms\app\logs\service_stderr.log
```

## 离线安装

源码离线安装：

```bat
install.bat
```

E 盘安装入口：

```bat
install_e_wms.bat
```

便携发行包：

```bat
build_portable_dist.bat
```

安装完成后使用根目录 `wms.bat` 或 `start_wms_offline.bat` 启动。

## 更新规则

- 只允许从 GitHub `main` 更新，禁止创建或切换其他分支。
- 工作区存在未提交修改时，不得强行覆盖或重置。
- 更新代码前先备份数据库。
- 新代码可能包含数据库兼容迁移；正式库升级前必须先验证备份可恢复。
- 不得通过部署脚本修改或重置任何已有用户密码。

## 启动后验证

```powershell
Invoke-WebRequest http://127.0.0.1:8080/login -UseBasicParsing
Get-NetTCPConnection -LocalPort 8080 -State Listen
```

应确认：

1. 登录页返回 HTTP 200。
2. 只有一个 `run_server.py` 服务进程监听 `8080`。
3. 日志没有数据库迁移、端口冲突或依赖错误。
4. 使用真实页面完成一次只读查询；不得用正式数据自动完成测试单据。

更完整的发布门禁见 `PRODUCTION_DEPLOYMENT_CHECKLIST.md` 和 `docs/部署验证与回滚手册.md`。
